# Handoff — comprehensive sweep + documented-features round (2026-06-14 → 06-15)

Branch `dev`. Two rounds: (1) a full audit → docs-fix → code-fix sweep, then (2) a documented-features
round. All local (**git not pushed**). Build green Desktop + Web; full suite 2720 pass / 1 pre-existing fail.

> **2026-06-15 ROUND 2 — documented-but-missing features + composer fixes (DEPLOYED LIVE).**
> 11 `0615` commits implemented 9 features + 2 fixes: `markov` Note[]/Int[] corpus · `getSection`/
> `sectionSequences(Song,String)` · section N-arg overload dispatch · **`(play song)`** + `writeWav(path,song)` ·
> `Cmaj7q` chord+duration fusion (bare-digit chords like `Bb7` stay note+octave) · bare note-values
> `e/q/h/w/s` as shadowable NoteValue constants · `(break)`/`(continue)` (need the 3-arg `(if c t e)` form) ·
> **`jam` sparse named-args** (`over=`/`style=`/`seed=`/`key=`) via OverloadResolver default-fill · **progression
> now renders stacked block chords in a sensible register** (was sequential single notes, bass at C2) ·
> **cut-time shorthand `C/` `¢` `C|` → 2/2**. +180 tests. Docs reconciled (the stale `zip`/`jam`-seed notes
> flipped; new features documented). WASM bundle regenerated (`7ae032a`, flow-lang.wasm 969493 B) + redeployed
> to https://flow-music.pages.dev — all routes 200, fresh bundle + reconciled docs (e.g. `/docs/collections`)
> confirmed live. **PENDING owner browser-UAT** (see round-1 note below + the new features).

> **2026-06-15 ROUND 1 — sweep fixes (DEPLOYED LIVE).** WASM bundle regenerated (`98410e3`) so the
> playground runs the 83 sweep engine fixes; docs carry the 100 example + 70 prose fixes. All routes 200.

> **DEPLOYED LIVE 2026-06-15** to https://flow-music.pages.dev (`wrangler pages deploy`, authed
> noahfreelove@gmail.com). The WASM bundle was regenerated (`98410e3`, flow-lang.wasm 957205 B) so the
> live playground now runs the sweep engine fixes, and the docs (built from the in-repo `wiki/` seed)
> carry the 100 example + 70 prose fixes. All routes 200; new `.wasm` + fixed docs confirmed served.
> **PENDING owner browser-UAT:** audible playground audio + a fixed-bug piece (e.g. 6/8 tempo, upward
> pitchShift) + corrected docs rendering, across browsers. Git push still pending.

## What this session did

1. **Audited the whole repo** (parallel reviewers + adversarial verifiers + 8 dogfood pieces that
   actually render). Artifacts: `.planning/CODEBASE-AUDIT-2026-06-14.md` (88 confirmed code bugs:
   19 high / 45 med / 24 low) + `.planning/sweep-2026-06-14-findings.json`.
2. **Verified every doc example** by running it on the real interpreter:
   `.planning/DOCS-VERIFICATION-2026-06-14.md` + `docs-fixes-2026-06-14.json` (94 broken / 63 prose
   across 28 pages).
3. **Fixed docs first** (owner choice): `8461fd4` — 100 examples rewritten to valid, re-run-verified
   Flow + 70 prose corrections across 27 wiki/README/FEATURES pages.
4. **Fixed code** (sequential waves on `dev`, `fix(sweep-0614):` prefix): **83 of 88 bugs fixed**
   (74 in the main wave + 9 in a follow-up), **175 regression tests added**.

## Test state (final)

- `flow-lang.Tests`: **2624 passed / 1 failed / 19 skipped**. The 1 failure is
  **`Phase41ShowcaseRmsTests.Showcase_RmsWithinTolerance`** — PRE-EXISTING (1.06 dB window-1 drift,
  identical to the pre-sweep handoff; pulse.flow is 4/4 so the timesig fix didn't touch it). An owner
  should investigate the Phase 41 synth-path drift or regenerate `baselines/Phase41/showcase.wav`.
- `flow-midi.Tests`: **19 passed / 2 failed** — `One_Sequence_Per_Track...` + `Two_Octave_Range...`,
  PRE-EXISTING stale Phase30 RH/LH-split tests (SPEC-5 "one Sequence per source track" never wired;
  update or remove them, or ship the Plan 30-08 AddSplitTracks removal).
- Baselines legitimately regenerated by sweep fixes (committed): `Phase37/piano_warmth_smoke.wav`
  (SAMP-03), `Phase45/intro.wav` + `cut-time.wav` (timesig speed fix).

## High-severity fixes landed (highlights)

`if` evaluated both branches; non-4/4 timesig wall-clock speed (`NoteType`); JI/Pythagorean wrong in
non-C keys (`PitchConversion`); locale 10× literal corruption; `pitchShift` direction inverted; SFZ
crossfade/round-robin silence; sampled-instrument silent-no-advisory; pattern-match Note/Symbol arms;
`swing` no-op wired in; `midiOut` voice-block drop; midi2flow velocity + channel-9; interpreter
`config.toml` load; WASM fresh-engine-per-run. Plus **added** documented-but-missing `length`/`zip`/
`inspect`. (Two follow-up fixes corrected regressions the main wave introduced: charitable-note-typo
breaking multiline streams + `hAsB`; WASM two-run determinism.)

## Deferred to v1.6 / owner action (NOT done)

- **`(str 1.0)` decimal formatting** — deferred: a narrow fix shifts the Phase41 showcase RMS baseline
  (audio params flow through `str`); owner decides accept-and-regen vs scope-away-from-render-path.
- **WASM MIDI download** — C# side now exposes a real `Uint8Array` via `[JSExport] GetLastMidiBytes()`,
  but `flow-runtime.js` is FROZEN and still returns base64 `result.midi`, so the download stays corrupt
  until either a bundle republish (`sync-runtime.sh`, needs `wasm-tools`) wires `getLastMidiBytes()`, OR
  the flow-site consumer base64-decodes. Owner action.
- **`jam` sparse named-args** (`key=`/`seed=`) — needs OverloadResolver default-fill + a reserved-word
  fix for `key`; v1.6.
- **1 hardware-gated bug** — `openMidiOutput` handle/ALSA-port leak; needs a real MIDI port to verify +
  a `closeMidiOutput` builtin + finalizer. Owner UAT.
- **Ableton Link — CANCELLED** (was deferred-to-v1.6) per owner; see STATE.md Deferred Items.

## Not committed

- The **8 dogfood pieces** (`examples/sweep/*.flow` + `examples/jazz/combo.flow`) stay gitignored
  scratch — they're full of now-stale bug-workaround comments (they're what *found* the bugs). The jazz
  combo (`combo.flow`) renders clean (WAV+MIDI) and is worth polishing into a real `examples/jazz/`
  showcase (drop the ppp-ghost-bar workaround now that `_w` rests + chord-bracket articulations work).
- 38 local commits on `dev` are **unpushed** — `git push` when ready.

## Pre-existing test debt (not this sweep)

`Phase41ShowcaseRmsTests` (above); 2 flow-midi RH/LH split tests (above); the WASM determinism fix
disabled cross-collection test parallelism (+~20s suite) — a v1.6 injectable-TextWriter on FlowEngine
would make print/advisory capture race-immune in product code instead.
