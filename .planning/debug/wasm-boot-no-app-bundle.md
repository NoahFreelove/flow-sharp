---
slug: wasm-boot-no-app-bundle
status: fix_implemented_pending_browser_uat
trigger: Phase 48 WASM runtime does not boot in a browser — `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` produces no bootable WASM app bundle (no dotnet.boot.js, no _framework/, no AppBundle/). Browser console reports "Flow runtime boot failed: Failed to load config file dotnet.boot.js". flow-lang is a LIBRARY project; library publish with RuntimeIdentifier=browser-wasm emits runtime + native intermediates but never runs the app-bundle generation step.
created: 2026-05-30T17:32:05Z
updated: 2026-05-31T10:25:00Z
phase: 48-wasm-runtime-webaudio-backend
related_plan: 48-06
---

# Debug Session: wasm-boot-no-app-bundle

## Symptoms

- **Expected behavior:** After `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release`, serving the publish output and visiting `wasm/index.html` boots the Mono-WASM runtime, and clicking Run plays an audible 440 Hz tone via the WebAudio backend. The structured RunResult (stdout/errors[]) is visible in DevTools.
- **Actual behavior:** Runtime never boots. `flow-runtime.js`'s `import '../dotnet.js'` resolves (dotnet.js IS at publish root), but `dotnet.create()` then fetches its boot manifest `dotnet.boot.js` at the served root and 404s. No audio, no execution.
- **Error messages:**
  ```
  Flow runtime boot failed: Failed to load config file dotnet.boot.js
  TypeError: error loading dynamically imported module: http://localhost:8080/dotnet.boot.js
  ```
- **Timeline:** Surfaced 2026-05-30 on the first real-browser smoke (Plan 48-06 HUMAN-UAT, Chrome/Linux). Never worked in a browser — Plans 48-01..05 only presence-checked publish files on disk and ran `WasmEntry.RunFromJs` in-process on Desktop; the browser boot path was never exercised.
- **Reproduction:**
  1. `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release`
  2. `cd flow-lang/bin/Release/net10.0/browser-wasm/publish/ && python3 -m http.server 8080`
  3. Visit `http://localhost:8080/wasm/index.html` in Chrome 120+ with DevTools open
  4. Observe boot failure (dotnet.boot.js 404) in console

## Confirmed Root Cause (VERIFIED EMPIRICALLY 2026-05-30)

`flow-lang` is a **library** project (`<Project Sdk="Microsoft.NET.Sdk">`, no app head, no `Main`, `OutputType=Library`). The Mono-WASM runtime-targets pack (`Microsoft.NET.Runtime.WebAssembly.Sdk`, supplied by the `wasm-tools` workload) IS engaged for the native publish, but its app-bundle/boot-manifest target is gated OFF for libraries.

Decisive gate, `Microsoft.NET.Runtime.WebAssembly.Sdk/10.0.8/Sdk/WasmApp.Common.targets`:
- Line 163: `<_IsLibraryMode Condition="'$(OutputType)' == 'Library' and '$(UsingMicrosoftNETSdkWebAssembly)' == 'true'">true</_IsLibraryMode>`
- Line 165: `<IsBrowserWasmProject Condition="... ('$(OutputType)' != 'Library' or '$(_IsLibraryMode)' == 'true')">true</IsBrowserWasmProject>`
- Lines 175-176: `<WasmGenerateAppBundle Condition="... ('$(OutputType)' != 'Library' or '$(_IsLibraryMode)' == 'true')">true</WasmGenerateAppBundle>` else `false`.

Because this project uses plain `Microsoft.NET.Sdk` (NOT `Microsoft.NET.Sdk.WebAssembly`), `UsingMicrosoftNETSdkWebAssembly` is empty → `_IsLibraryMode` is empty → `IsBrowserWasmProject` and `WasmGenerateAppBundle` both default to **false**. So `_WasmGenerateAppBundle` never runs, `dotnet.boot.js` / `_framework/` / `AppBundle/` are never written, and `dotnet.create()` 404s on its boot manifest.

## Goal

Make `FlowTarget=Web` produce a **bootable** WASM app bundle (emit `dotnet.boot.js` + framework layout), reconcile `flow-runtime.js`'s `../dotnet.js` import + `wasm/` placement + `index.html` against the new bundle layout, and add a publish-output "boot-manifest-exists" assertion. Verify by **republish → serve → boot in a real browser**. This in-phase repair gates Plan 48-07 (the Phase 48 closer).

## Key Files

- `flow-lang/flow-lang.csproj` — FlowTarget=Web PropertyGroup/ItemGroup (D-48-01..05 build pipeline)
- `flow-lang/wasm/flow-runtime.js` — ES module, `import '../dotnet.js'` (D-48-12)
- `flow-lang/wasm/index.html` — dev smoke harness, D-48-09 user-gesture autoplay chain
- `flow-lang/wasm/trim-roots.xml` — trimmer roots
- `flow-lang/Runtime/WasmEntry.cs` — `[JSExport]` boundary (RunFromJs/PlayFromJs)
- `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` — `WasmPublish_ProducesAppBundle` MISNAMED; checks only dotnet.js/flow-lang.dll/dotnet.native.wasm, NOT dotnet.boot.js → this is exactly the defect-class gap.

## Constraints

- Must NOT break the Desktop target (`FlowTarget=Desktop` default — all P/Invoke/SFZ/OSC/live-coding intact).
- Single-source-of-truth Pattern A preferred over a separate project IF a library can emit a boot bundle.
- Zero new composer-facing NuGet packages where possible (Mono-WASM ships in .NET 10 SDK).
- Preserve bundle-size budget (D-48-05) and two-run determinism (D-48-16).

## Current Focus

- hypothesis: Forcing `WasmGenerateAppBundle=true` + `IsBrowserWasmProject=true` + `WasmMainJSPath` on the existing `Microsoft.NET.Sdk` library, gated under FlowTarget=Web, triggers the runtime-pack's app-bundle target and emits a bootable `_framework/dotnet.boot.js` WITHOUT swapping SDKs or adding a project. CONFIRMED by experiment.
- test: `dotnet publish ... -p:WasmGenerateAppBundle=true -p:IsBrowserWasmProject=true -p:WasmMainJSPath=wasm/flow-runtime.js`
- expecting: AppBundle/ with _framework/dotnet.boot.js + dotnet.js + dotnet.native.wasm + flow-lang.wasm. CONFIRMED.
- next_action: RESOLVED — human approved the minimal-lever fix; implemented + verified (see Resolution). Remaining: human real-browser re-smoke to fully close 48-06 (cannot be automated).
- next_action (cycle 3): RESOLVED — THIRD distinct browser-only defect (single-threaded-WASM Task.Run+Wait DEADLOCK → always kind="cancel" at 30s). Human pre-approved the synchronous-execution fix; implemented + verified (see cycle-3 Resolution). Remaining: human real-browser re-smoke to fully close 48-06 (audible 440 Hz tone, no cancel).
- next_action (cycle 4): RESOLVED — FOURTH distinct browser-only defect: ENTIRE builtin surface missing in-browser (`[eval] Function '<name>' not found` for print/add/createSineTone/play). Leading TrimMode=full hypothesis REFUTED via Cecil (all Register* bodies survive trimming). TRUE cause: the builtin surface is declared in `std.flow`, loaded from disk on Desktop but ABSENT from the WASM VFS (zero `.flow` in AppBundle). FIX (PREFERRED axis, all locked decisions intact — no TrimMode/trim-roots change, no checkpoint): embed stdlib `.flow` as `<EmbeddedResource>` on Web + `ModuleLoader` embedded fallback + explicit `@std` bootstrap at FlowEngine ctor. Proven end-to-end in a no-filesystem harness (3/3 surface probes resolve from embedded). Remaining: human real-browser re-smoke across a BROADER surface (print/add, tone, note stream, writeWav, writeMidi, collections) to confirm + surface any other gaps.
- next_action (cycle 5): RESOLVED — FIFTH issue is a BUILD REGRESSION introduced by cycle-1's own fix (NOT a new browser defect). Cycle-1 forced `IsBrowserWasmProject=true`/`WasmGenerateAppBundle=true`, which wires the runtime pack's `WasmBuildApp` AfterTargets hook so it ALSO fires during a plain `dotnet build -p:FlowTarget=Web` — but the `WasmAssembliesToBundle` item set is only gathered in the nested-PUBLISH flow, so plain build failed `WasmApp.Common.targets:368` with `WasmAssembliesToBundle item is empty`, breaking the documented `dotnet build -p:FlowTarget=Web` command and turning Phase47 `BuildConditioningSmokeTests.WebBuild_ExitCodeIsZero` RED. FIX (build-gating only, no checkpoint): set the SDK's own documented `<WasmBuildOnlyAfterPublish>true</WasmBuildOnlyAfterPublish>` inside the FlowTarget=Web PropertyGroup so the app-bundle target is skipped at plain-build time and runs only on publish. Verified empirically: plain build exit 0 (no bundle), publish exit 0 WITH `AppBundle/_framework/dotnet.boot.js` + 13 embedded stdlib resources intact, Phase47 9/9 GREEN, Phase48 26/26 GREEN, bundle size neutral. Remaining: same human real-browser re-smoke as cycle 4 (broad surface) to fully close 48-06.
- next_action (cycle 6): RESOLVED — SIXTH distinct browser-only defect, surfaced when the human re-smoked WITH `use "@audio"`. `audio.flow` declares `internal proc micBuffer(Second|Double)` (and `std.flow` declares `internal proc loadSfz(Symbol|String)`) whose C# impls are STRIPPED on Web (csproj `<Compile Remove>` InputFunctions.cs / Sfz/** per Phase 47 D-47-03). `Interpreter.ExecuteProcDeclaration` hard-`ReportError`'d "No C# implementation found" per stripped overload; the accumulated errors made ModuleLoader FAIL THE WHOLE `@audio` import (taking createSineTone/play down) and dirtied the ctor `@std` bootstrap. FIX (charitable, no checkpoint — aligned with project charitable-interpretation philosophy + Phase 47's stripped-feature advisory pattern): on the Web target only (gated on compile-time `FlowEngine.IsWebTarget`), SKIP the missing-impl overload and emit a one-shot `[target] builtin '<name>' unavailable on Web target` WarnOnce advisory keyed per proc-name; Desktop keeps the hard error. SWEEP confirmed micBuffer + loadSfz are the ONLY embedded-surface/stripped-impl pairs (OSC surface lives in non-embedded opt-in sfz.flow/osc.flow). VERIFIED ON THE WEB PATH (not just Desktop): a Web-flavored host-loadable PE harness with NO on-disk `.flow` proves `use "@audio"` imports + createSineTone/play/renderSong→writeWav/transpose/harmony all resolve while micBuffer/loadSfz charitably degrade; 5 new `[FlowTargetFact("Web")]` Facts PASS under `-p:FlowTarget=Web` (the in-process branch actually executes there) and SKIP on Desktop. Remaining: human real-browser re-smoke (`use "@audio"` + tone + note stream + writeWav + writeMidi).
- next_action (cycle 7): RESOLVED — SEVENTH distinct browser-only defect, surfaced on the human re-smoke once `use "@audio"` imported (cycles 1-6 all FIXED). `(play (createSineTone 440Hz 1.0 0.5))` FROZE the page ~30s then returned `[runtime] audio playback exceeded 30s cap` — no audio. SAME single-threaded-WASM `Task.Run(...)` + `workerTask.Wait(30s)` DEADLOCK cycle 3 fixed in `WasmEntry.RunFromJs` (a8c1911), but cycle 3 only fixed the Execute path — NOT the playback path: `WebAudioBackend.Play` wrapped the `FlowRuntimeInterop.PlayStereoFloat32` [JSImport] marshal in `Task.Run` then `workerTask.Wait(PlayTimeout=30s)`, so on the one browser main thread the marshal NEVER ran → 30s freeze → bogus cap advisory → no AudioBufferSourceNode. FIX (same human-approved cycle-3 approach — "run synchronously, best-effort cap"; NO new checkpoint): in `WebAudioBackend.Play` REMOVED the `Task.Run` + `workerTask.Wait` wrapper + the `PlayTimeout` const + the 30s-cap WarnOnce/throw block; the marshal now runs SYNCHRONOUSLY inside the existing `OperatingSystem.IsBrowser()` Pattern B guard, assigning the returned handle to `_activeSource`. Preserved: `_lock`/`_disposed` checks, D-48-07 PromoteToStereo BEFORE marshal, MemoryMarshal.AsBytes zero-copy reinterpret, the pinned IAudioBackend signature (cancellationToken stays — D-47-05; just no longer caps a blocking wait), the D-48-11 Desktop charitable no-op. SWEEP (Task.Run+`.Wait` across browser-reachable audio/runtime paths): WebAudioBackend.Play was the ONLY deadlock instance; PlaybackFunctions' 4 `Task.Run` are fire-and-forget (no `.Wait` — don't block the calling thread; loop/stream on Web is the separable v1.6 AudioWorklet streaming path), CoreAudioBackend's `_bufferAvailable.Wait(50ms)` is Desktop-only (untouched), WasmEntry's hits are cycle-3 doc comments. VERIFIED: Desktop build / Web build / Web publish all exit 0 (boot manifest + 13 embedded stdlib intact, ZERO IL2026/IL3050/IL2075); WebAudioBackendIntegrationTests 8/8 green (synchronous change preserved every Desktop-side contract); Phase47 9/9+8skip GREEN; Phase48 29/29+5skip GREEN (added 3 cycle-7 source-guard Facts asserting Play no longer references Task.Run/.Wait/PlayTimeout); AssemblyReferenceScan skip-clean; bundle NEUTRAL (flow-lang.wasm 1 KB SMALLER). Remaining: human real-browser re-smoke — `use "@audio"` + `(play (createSineTone 440Hz 1.0 0.5))` (expect AUDIBLE 440 Hz tone, no 30s freeze, no cap error) + note stream + writeWav + writeMidi. The audible tone validates the full synthesis→[JSImport]→WebAudio chain and closes Plan 48-06.

## Evidence

- timestamp: 2026-05-30T17:40Z — `dotnet msbuild ... -getProperty` on FlowTarget=Web: `UsingMicrosoftNETSdkWebAssembly=""`, `OutputType=Library`, `WasmGenerateAppBundle=false`, `_IsLibraryMode=""`, `IsBrowserWasmProject=""`. Root cause confirmed at the MSBuild-property level.
- timestamp: 2026-05-30T17:45Z — baseline `dotnet publish -p:FlowTarget=Web -c Release` exits 0; publish/ is FLAT and contains dotnet.js / dotnet.native.wasm / dotnet.runtime.js / flow-lang.dll PLUS leaked build intermediates (driver.c, corebindings.c, libmonosgen-2.0.a, emcc-link.rsp, *.h, wasm-props.json). NO dotnet.boot.js, NO _framework/, NO AppBundle/. `find` for dotnet.boot.js returns nothing. Symptom reproduced on disk.
- timestamp: 2026-05-30T17:55Z — EXPERIMENT: `dotnet publish -p:FlowTarget=Web -c Release -p:WasmGenerateAppBundle=true -p:IsBrowserWasmProject=true -p:WasmMainJSPath=wasm/flow-runtime.js` exits 0 and logs `Generated app bundle at .../browser-wasm/AppBundle/`. AppBundle/_framework/ contains dotnet.boot.js (5561 bytes, `mainAssemblyName: flow-lang.dll`), dotnet.js, dotnet.native.wasm, dotnet.runtime.js, dotnet.native.js, and all assemblies as `.wasm` (Webcil: flow-lang.wasm, System.*.wasm, Melanchall.DryWetMidi.wasm). AppBundle/ root has flow-runtime.js (copied via WasmMainJSPath) + package.json `{ "type":"module" }`. The minimal-lever fix is VIABLE without an SDK swap or separate project.
- timestamp: 2026-05-30T18:00Z — Layout reconciliation needed: (1) AppBundle places `flow-runtime.js` at AppBundle/ ROOT and runtime under AppBundle/_framework/, so flow-runtime.js's `import '../dotnet.js'` is wrong for the bundle (should be `./_framework/dotnet.js`). (2) index.html header (lines 11-13) + module comment (lines 66-67) were ALREADY written for an `AppBundle/wasm/` layout that the csproj never actually produced — neither the old `../dotnet.js` nor the `AppBundle/wasm/` assumption matches the generated bundle. (3) csproj currently CopyToPublishDirectory's flow-runtime.js+index.html into publish/wasm/ of the FLAT tree, divorced from the AppBundle tree.
- timestamp: 2026-05-30T18:05Z — Stdlib VFS note (follow-up, does NOT block boot): `ModuleLoader.ResolveStdlibPath` (line 280) resolves `@std`/etc. via `AppContext.BaseDirectory` + File IO. In WASM that's the Emscripten VFS root; `.flow` files must be mounted there for any script using `use "@std"`. The 48-06 smoke script `(play (createSineTone 440Hz 1.0 0.5))` imports no module, so boot + tone verification is unblocked; mounting stdlib into the bundle VFS is a separable concern.
- timestamp: 2026-05-30T19:10Z — SECOND, DISTINCT defect surfaced on the human re-smoke (boot defect FIXED + committed 08140bb; page now boots). Clicking Run on the default script `(play (createSineTone 440Hz 1.0 0.5))` threw `[runtime] JsonSerializerIsReflectionDisabled`. Cause CONFIRMED at source: `WasmEntry.cs:283` called `JsonSerializer.Serialize(result, _jsonOptions)` with a plain reflection-based `JsonSerializerOptions` (old line 132). `FlowTarget=Web` sets `<TrimMode>full</TrimMode>`, which DISABLES reflection-based System.Text.Json in the trimmed WASM build → the serializer throws at runtime in-browser. SAME "Desktop ≠ trimmed/browser" defect class as the boot bug: the Desktop in-process tests (WasmDeterminismTests) call RunFromJs where reflection JSON is ENABLED, so they never exercised the trimmed serializer path and did NOT catch it.
- timestamp: 2026-05-30T19:20Z — FIX implemented: added source-generated `FlowWasmJsonContext : JsonSerializerContext` (`[JsonSourceGenerationOptions(PropertyNamingPolicy=CamelCase, DefaultIgnoreCondition=WhenWritingNull)]` + `[JsonSerializable(typeof(RunResult))]` + `[JsonSerializable(typeof(RunError))]`) in WasmEntry.cs; switched the serialize call to `JsonSerializer.Serialize(result, FlowWasmJsonContext.Default.RunResult)`; retired the now-unused reflection-based `_jsonOptions` field (grep confirmed line 283 was its only consumer). Did NOT take the `JsonSerializerIsReflectionEnabledByDefault=true` escape hatch (would fight TrimMode=full + risk IL2026). Audited WasmEntry for other reflection-dependent paths reachable from RunFromJs/PlayFromJs (Activator/Type.GetType/GetProperty/other JsonSerializer): NONE found; the only JsonSerializer use in the whole library was this one call site.
- timestamp: 2026-05-30T19:25Z — VERIFICATION (green build alone NOT treated as sufficient): (1) `dotnet build -p:FlowTarget=Desktop` exit 0 (source-gen attributes compile on both targets; P/Invoke/SFZ/OSC/live-coding intact). (2) `dotnet publish -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050 trim warnings (source-gen eliminated the reflection-JSON trim concern; the only residual IL warning is the PRE-EXISTING IL2075 at ExpressionEvaluator.cs:960, unrelated). (3) `AppBundle/_framework/dotnet.boot.js` still emitted (prior-cycle boot gate intact). (4) Web deps.json forbidden-reference scan clean (no Rug.Osc/RtMidi/libpulse/AudioToolbox).
- timestamp: 2026-05-30T19:30Z — REGRESSION NET STRENGTHENED: added `WasmJsonSerializationTests` (Desktop-runnable, browser-free) — `SourceGenContext_Serializes_CamelCase_WithNullOmission` round-trips RunResult through `FlowWasmJsonContext.Default.RunResult` and asserts camelCase keys + null-omission (wav/midi/sourceSnippet omitted) + no PascalCase leak; `RunFromJs_Produces_CamelCase_RunResult_Shape` mirrors it end-to-end through the export. This catches the JsonSerializerIsReflectionDisabled defect class without a browser by pinning the source-gen CONTRACT.
- timestamp: 2026-05-30T19:35Z — TEST-INFRA RACE fixed (surfaced while strengthening the net, pre-existing latent): (a) adding a second RunFromJs-calling class exposed a process-wide `Console.SetOut`/`SetError` redirection race between RunFromJs-calling classes running in xUnit cross-class parallel → intermittent WasmDeterminismTests stdout-leak failure. Fixed by `WasmEntryConsoleCollection` serializing both classes. (b) The three FlowTarget=Web publish-shellout classes (WasmBuildPipelineTests/BundleSizeBudgetTests/DryWetMidiWasmPublishTests) raced on the shared `obj/.../browser-wasm` intermediate when run in parallel → intermittent `MarshalingPInvokeScanner: BadImageFormatException: Image is too small` (MSB4018). Fixed by `WasmWebPublishCollection` serializing the three. Each class passed in isolation before the fix; no assertion weakened. Full Phase48 suite now 21/21 green across 3 consecutive from-clean runs (was intermittently 19/21).
- timestamp: 2026-05-30T19:38Z — BUNDLE SIZE (from test-written 48-BUNDLE-SIZE.md): uncompressed 5,302,572 bytes (5.06 MB) / Brotli 1,611,266 bytes (1.54 MB) / ratio 30.4% → D-48-05 MONOLITHIC SHIP. Neutral-to-slightly-smaller vs the prior cycle's 5.24 MB uncompressed / 1.59 MB Brotli — source-gen added no bloat, as predicted.

- timestamp: 2026-05-30T20:05Z — THIRD, DISTINCT defect on the human re-smoke (boot FIXED 08140bb + reflection-JSON FIXED 5b80c01; page boots, Run dispatches). Clicking Run on `(play (createSineTone 440Hz 1.0 0.5))` returned the structured error `[cancel] evaluation exceeded 30s cap (D-48-10)` — no audio, no stdout, the script never executed. CONFIRMED ROOT CAUSE by reading `WasmEntry.cs:243-265`: `RunFromJs` enforced the D-48-10 30s cap via `var workerTask = Task.Run(() => engine.Execute(...)); if (!workerTask.Wait(RunTimeout)) { ...cancel... }`. Mono-WASM is SINGLE-THREADED by default — `Task.Run` queues to the one main thread, then `workerTask.Wait(30s)` BLOCKS that same thread, so `engine.Execute` can never run. It deadlocks and ALWAYS times out at exactly 30s, returning the cancel RunError. A hard wall-clock cap is fundamentally unenforceable by blocking in single-threaded WASM (no preemption). SAME "Desktop ≠ browser/WASM" defect class as the prior two: the Task.Run+Wait shape was carried over from Phase 38 LIVE-02 where a real Desktop thread pool exists; the Desktop in-process tests gave Task.Run a real worker thread, so they PASSED while the browser deadlocked.
- timestamp: 2026-05-30T20:15Z — Pre-implementation safety checks: (a) grep of `flow-lang.Tests/` + `flow-lang/wasm/` for `RunTimeout` / `kind="cancel"` / `exceeded 30s` / `D-48-10` → NO test or JS depends on the timeout/cancel behavior, so removing the blocking wrapper breaks nothing. (b) `FlowEngine.Execute(string, string?)` (FlowEngine.cs:296) is a plain SYNCHRONOUS `bool`-returning method with NO hidden off-thread dependency → the prescribed synchronous call is workable (no checkpoint needed).
- timestamp: 2026-05-30T20:20Z — FIX implemented (human-approved "run synchronously, cap best-effort"): in `WasmEntry.RunFromJs` replaced the `Task.Run(...)` + `workerTask.Wait(RunTimeout)` block with a direct synchronous `engine.Execute(source ?? string.Empty, "<wasm>"); errors = MapFlowErrors(engine.ErrorReporter.Errors);` on the calling thread. Kept the outer try/catch (kind="runtime" host-exception guard, T-48-15), the finally that restores Console.Out/Error (T-48-14), the Stopwatch + DurationMs, and the last-resort serializer guard. Removed the now-unused `RunTimeout` const + the `using System.Threading.Tasks;` import. The `"cancel"` RunError kind stays DEFINED in the D-48-14 contract (field names + kinds PINNED) but is no longer raised in single-threaded WASM. Updated the class-level XML doc, the inline "Pattern C" comment, the RunError remarks, and the MapFlowErrors remarks to state D-48-10 is hard-cap-on-Desktop / best-effort-non-preemptive-in-WASM, citing this debug session. Added an HONEST note in `flow-runtime.js` (header + `run:` doc) that a JS-side setTimeout CANNOT preempt a synchronous dotnet call (the event loop is blocked for the whole call) — so NO fake JS cap was added.
- timestamp: 2026-05-30T20:30Z — REGRESSION NET: added `WasmSynchronousExecutionTests` (Desktop-runnable, browser-free) — `RunFromJs_SimpleScript_RunsToCompletion_PopulatedStdout_NoErrors` asserts `(print "hi")` returns populated stdout + EMPTY errors (no kind="cancel"); `RunFromJs_ToneRender_RunsToCompletion_NoCancel` asserts a `use "@audio"` + `createSineTone` render runs to completion with no cancel error. HONESTLY documented as a PARTIAL PROXY: the Desktop xUnit runner is multi-threaded, so the old Task.Run shape would have PASSED here too — these Facts pin the post-fix "script runs to completion" contract, but the REAL confirmation is the human browser re-smoke. No happy-path coverage weakened; no existing test depended on the cancel path.
- timestamp: 2026-05-30T20:33Z — VERIFICATION (green build alone NOT treated as sufficient): (1) `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exit 0 (only pre-existing warnings; Desktop default intact). (2) `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050/IL2075 — boot manifest `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted, flow-runtime.js at AppBundle root with `import { dotnet } from './_framework/dotnet.js';` intact (prior boot + reflection-JSON gates preserved). (3) Phase48 xUnit suite 23/23 green (21 prior + 2 new), incl. cycle-1 boot-manifest gate + cycle-2 WasmJsonSerializationTests. (4) AssemblyReferenceScanTests skip-clean on Desktop (pre-existing [FlowTargetFact("Web")] gating, unchanged). (5) BUNDLE SIZE (test-written 48-BUNDLE-SIZE.md): 5,302,060 bytes (5.06 MB) uncompressed / 1,610,471 bytes (1.54 MB) Brotli → D-48-05 MONOLITHIC SHIP — NEUTRAL vs prior cycle (source change adds no bloat). (6) D-48-14/15 JSON shape unchanged (source-gen FlowWasmJsonContext untouched: camelCase + null-omission + same field names/kinds).

- timestamp: 2026-05-30T21:00Z — FOURTH, DISTINCT defect on the human re-smoke (boot 08140bb + reflection-JSON 5b80c01 + deadlock a8c1911 all FIXED; page boots, Run dispatches, executes). `(play (createSineTone 440Hz 1.0 0.5))` AND a bisect `(print "hello")` / `(add 1 2)` ALL return `[eval] Function '<name>' not found` — the ENTIRE builtin surface is missing in-browser. **LEADING HYPOTHESIS (TrimMode=full strips the builtin-registration surface) REFUTED EMPIRICALLY.** Inspected the trimmer's PE output `flow-lang/obj/Release/net10.0/browser-wasm/linked/flow-lang.dll` (= the Webcil payload shipped as `AppBundle/_framework/flow-lang.wasm`) with Mono.Cecil: EVERY registration body is fully intact post-trim — `RegisterStdLib`=1786 instrs, `RegisterAudio`=2015, `RegisterCollections`=597, `RegisterMath`=444, `RegisterMusicalNotationFunctions`=823, `RegisterContextDependentFunctions`=628; `FlowEngine..ctor`=145; `WasmEntry.RunFromJs`=107; `RegisterAllImplementations` still calls `RegisterStdLib/RegisterMath/RegisterCollections/...`. All registered-name string literals (createSineTone/print/reverb/renderSong/transpose/lowpass/writeWav/chordNotes) confirmed present in the trimmed PE AND in the shipped Webcil `.wasm` (verified with `strings -e l`, UTF-16LE — an earlier ASCII-`strings` pass FALSELY showed them absent and produced a false 'trimming' smoking gun; corrected). `dotnet publish -p:FlowTarget=Web -p:TrimmerSingleWarn=false` emits ZERO trim warnings touching the registration surface. **The C# InternalFunctionRegistry IS fully populated in the browser. Trimming is NOT the cause; no trim-roots change and no TrimMode relaxation is needed.**
- timestamp: 2026-05-30T21:05Z — TRUE ROOT CAUSE established. Flow's builtin CALLABLE SURFACE is declared as `internal proc` statements in `std.flow` (+ transitively `@collections`/`@bars`), NOT in C#. The C# registry holds only IMPLEMENTATIONS keyed by name; `Interpreter.cs:845-848` binds an impl ONLY when a matching `internal proc` surface overload already exists (else `ExecutionContext.cs:737-738` reports `Function '<name>' not found (0 overloads registered)`). On Desktop the surface is bootstrapped as a SIDE EFFECT of `FlowEngine` ctor → `StyleRegistry.LoadShippedAndUserPacks` (FlowEngine.cs:290): the shipped improv packs `use "@improv"` → `improv.flow` does `use "@std"`, transitively loading std/collections/bars. That load goes through `ModuleLoader.LoadModule` → `ResolveStdlibPath` → `AppContext.BaseDirectory` + `File.Exists`/`File.ReadAllText` (ModuleLoader.cs:106-114). VERIFIED on Desktop via `--verbose -e '(print "x")'`: logs `Loaded module: .../std.flow` + collections + bars + improv BEFORE `Executing <eval>` — i.e. bare `(print)` works ONLY because std.flow was implicitly loaded at ctor.
- timestamp: 2026-05-30T21:08Z — WASM VFS gap confirmed as the mechanism. `find flow-lang/bin/Release/net10.0/browser-wasm/AppBundle -name '*.flow'` → ZERO files; `_framework/dotnet.boot.js` resources lists NO `.flow` VFS assets. In the Emscripten VFS there is no host filesystem, so every stdlib `LoadModule` silently fails `File.Exists` → returns Error; `StyleRegistry.LoadShippedAndUserPacks` is charitable-by-design (one-shot advisory, continues), so std.flow NEVER loads and the builtin surface is EMPTY → every builtin lookup misses → `[eval] Function '<name>' not found`. This is EXACTLY the stdlib-VFS concern the cycle-1 18:05Z note deferred as 'separable' — but that note's premise ('the smoke script imports no module so boot+tone is verifiable without it') was WRONG: the builtin surface ITSELF lives in std.flow, so without it NOTHING (not even print/add) resolves. Same 'Desktop ≠ browser/WASM' defect class as the prior three (Desktop has a real filesystem; the in-process tests load the .flow files from disk, so they never exercised the no-filesystem path).
- timestamp: 2026-05-30T21:20Z — FIX implemented (PREFERRED axis; stays within ALL locked decisions — NO TrimMode change, NO trim-roots change, NO boot-config/VFS plumbing; the continuation context's trim-roots/relax-TrimMode levers were both moot once trimming was ruled out). THREE edits: (1) `flow-lang/flow-lang.csproj` (Web-only ItemGroup) — `<EmbeddedResource>` the 10 surviving stdlib `.flow` modules + the 3 improv style packs with stable `LogicalName=FlowLang.Stdlib.<relative>` (sfz.flow/osc.flow NOT embedded — stripped on Web per D-47-03; the ModuleLoader Web strip-gate rejects @sfz/@osc before any read). (2) `flow-lang/Runtime/ModuleLoader.cs` — `LoadModule` reads filesystem FIRST, then a new `TryReadEmbeddedModule` fallback (strips `AppContext.BaseDirectory` prefix → `FlowLang.Stdlib.<relative>` resource lookup). Desktop is byte-identical (File.Exists wins; Desktop build embeds nothing, so the fallback returns false and the original 'file not found' diagnostic still fires for genuinely-missing imports). (3) `flow-lang/Core/FlowEngine.cs` — explicit `_moduleLoader.LoadModule("@std", "<engine-init>", _context)` at ctor end, BEFORE the style-pack load, so the builtin SURFACE bootstraps regardless of the fragile improv-pack chain (idempotent: the later improv `use "@std"` hits AlreadyLoaded → Desktop ordering/behavior unchanged).
- timestamp: 2026-05-30T21:25Z — DECISIVE END-TO-END PROOF (the no-filesystem scenario, browser-free): built a Web-flavored PE (`-p:FlowTarget=Web` with the app-bundle props off so it's a plain loadable PE — FLOW_WEB defines + the embedded `.flow` resources), MOVED ALL on-disk `.flow` files away to simulate the Emscripten VFS, then loaded the assembly via reflection and constructed a `FlowEngine`. RESULT: 13 embedded stdlib resources present; `(print "...")` printed, `(add 1 2)`→3, `(mul 6 7)`→42 — the builtin surface resolved 3/3 PURELY from embedded resources with ZERO 'Function not found' errors. Pre-fix this scenario is exactly the browser failure (`[eval] Function 'print' not found`). This proves the fix closes the actual mechanism, not just a proxy.
- timestamp: 2026-05-30T21:30Z — REGRESSION NET (browser-free, Cecil-on-published-binary — mirrors AssemblyReferenceScanTests/DryWetMidiWasmPublishTests): added `flow-lang.Tests/Integration/Phase48/WasmStdlibEmbedTests.cs` (`[Collection(WasmWebPublishCollection.Name)]`, 3 Facts). `TrimmedWebAssembly_EmbedsStdlibFlowModules` publishes `-p:FlowTarget=Web -c Release` and asserts all 13 `FlowLang.Stdlib.*` resources survive into the TRIMMED `obj/Release/.../linked/flow-lang.dll` (the artifact the shipped Webcil is built from — proves trimming preserves them, not merely compilation). `EmbeddedStdFlow_ContainsBuiltinSurfaceDeclarations` asserts the embedded std.flow bytes contain `internal proc print` + `use "@collections"` + `use "@bars"`. The third Fact pins the fallback's graceful-degrade contract. WHY Cecil-on-published-output and not in-process: the Desktop xUnit runner reads `.flow` from disk, so an in-process FlowEngine test would NOT exercise the no-filesystem case — this asserts the property the browser actually depends on (embedded resources physically in the trimmed output). HONESTLY a strong proxy, not full closure: a human in-browser re-smoke across a BROADER surface is still required (the registry having been wholesale-empty means other browser-only gaps may remain until the full surface is exercised once).
- timestamp: 2026-05-30T21:35Z — VERIFICATION (green build alone NOT treated as sufficient). (1) `dotnet build -p:FlowTarget=Desktop` exit 0 (only pre-existing warnings; Desktop default intact). Desktop `(print)` / `(add)` still work (`-e` smoke). (2) `dotnet publish -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050 trim warnings; boot manifest `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted; flow-runtime.js import intact; shipped Webcil `flow-lang.wasm` grew 844,565 → 917,269 bytes (+72 KB = the embedded `.flow` text) and CONTAINS the embedded resource names + `internal proc print` content (UTF-8 strings). (3) Phase48 suite 26/26 green (23 prior + 3 new). (4) Full suite (collection-parallelism OFF) 2206 passed / 1 failed / 9 skipped — the single failure is the PRE-EXISTING `Phase47.BuildConditioningSmokeTests.WebBuild_ExitCodeIsZero` (verified identical RED on clean HEAD via git-stash: cycle-1's forced `WasmGenerateAppBundle=true` breaks plain `dotnet build -p:FlowTarget=Web` with `WasmAssembliesToBundle item is empty` — the ship path uses `publish`, which works; flagged for the closer, NOT a cycle-4 regression). With parallelism ON, 3-4 additional flaky failures appear that ALL pass in isolation (pre-existing Console.SetOut cross-collection race, documented cycle-2/3). (5) AssemblyReferenceScanTests skip-clean on Desktop (Web-gated, unchanged); embedding text resources cannot resurrect a stripped dep (resources carry no assembly refs); DryWetMidiWasmPublishTests forbidden-ref scan green. (6) BUNDLE SIZE (test-regenerated 48-BUNDLE-SIZE.md): 5,380,897 bytes (5.13 MB) uncompressed / 1,630,301 bytes (1.55 MB) Brotli → D-48-05 MONOLITHIC SHIP. Delta vs cycle-3 (5,302,060 / 1,610,471): +78,837 uncompressed / +19,830 Brotli (+19 KB) — the embedded stdlib text; comfortably under 15 MB target / 20 MB hard cap.
- timestamp: 2026-05-30T22:00Z — FIFTH issue (BUILD REGRESSION from cycle-1, NOT a browser defect; the pre-existing item flagged for the 48-07 closer at the end of the cycle-4 Resolution). REPRODUCED on dev HEAD: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` fails with `WasmApp.Common.targets(368,5): error : WasmAssembliesToBundle item is empty. No assemblies to process`; `dotnet publish -p:FlowTarget=Web -c Release` (the ship path) stays green. ROOT CAUSE established by reading the runtime pack targets (`~/.dotnet/packs/Microsoft.NET.Runtime.WebAssembly.Sdk/10.0.8/Sdk/WasmApp.Common.targets`): cycle-1's forced `IsBrowserWasmProject=true` wires the public `WasmBuildApp` target (line 275) via `AfterTargets=$(WasmBuildAppAfterThisTarget)`, gated `'$(WasmBuildOnlyAfterPublish)' != 'true'` (line 277). During a plain build `_IsPublishing`/`DeployOnBuild` are empty, so `WasmBuildOnlyAfterPublish` defaults FALSE (line 174) and the build-phase bundle target runs — but the assembly set feeding `WasmAssembliesToBundle` is only gathered in the nested-publish pass, so `PrepareInputsForWasmBuild` errors empty (line 368). The SDK itself auto-sets `WasmBuildOnlyAfterPublish=true` when publishing (line 173-174 comment: 'VS uses DeployOnBuild, and sdk sets _IsPublishing'); the documented intent (line 76) is 'Causes relinking to be done only for Publish.'
- timestamp: 2026-05-30T22:03Z — FIX implemented (build-gating only, all locked decisions intact, no checkpoint): added `<WasmBuildOnlyAfterPublish>true</WasmBuildOnlyAfterPublish>` to the existing FlowTarget=Web PropertyGroup in `flow-lang/flow-lang.csproj` (single edit; single-source-of-truth Pattern A — no SDK swap, no second project, no TrimMode/trim-roots/boot-config change). This unconditionally satisfies the `WasmBuildApp` skip-gate (`'$(WasmBuildOnlyAfterPublish)' != 'true'`) during plain build, while publish (which sets it anyway) still generates the bundle. Added an explanatory comment citing the exact SDK target lines.
- timestamp: 2026-05-30T22:06Z — VERIFICATION (green build alone NOT treated as sufficient): (1) `dotnet build -p:FlowTarget=Web` exit 0 — the `WasmAssembliesToBundle item is empty` error is GONE (regression fixed). (2) `dotnet build -p:FlowTarget=Desktop` exit 0 (Desktop default untouched — the prop is Web-PropertyGroup-gated). (3) `dotnet publish -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050; `AppBundle/_framework/dotnet.boot.js` (5561 bytes, mainAssemblyName flow-lang.dll) STILL emitted; AppBundle root still carries flow-runtime.js + index.html + package.json; shipped Webcil `flow-lang.wasm` still 917,269 bytes with 13 `FlowLang.Stdlib.*` embedded resources present (prior cycles 1-4 all intact). (4) Phase47 suite 9 passed / 8 skipped / 0 failed — `BuildConditioningSmokeTests.WebBuild_ExitCodeIsZero` is now GREEN (was the lone full-suite RED on clean HEAD). (5) Phase48 suite 26/26 GREEN (no regression of cycles 1-4 gates). (6) AssemblyReferenceScanTests skip-clean on Desktop (pre-existing [FlowTargetFact("Web")] gating, unchanged). (7) BUNDLE SIZE (test-regenerated 48-BUNDLE-SIZE.md): 5,380,897 bytes (5.13 MB) uncompressed / 1,630,070 bytes (1.55 MB) Brotli → D-48-05 MONOLITHIC SHIP — NEUTRAL vs cycle-4 (build-gating change adds no bytes, as predicted).


- timestamp: 2026-05-31T00:00Z — SIXTH, DISTINCT defect on the human re-smoke (boot 08140bb + reflection-JSON 5b80c01 + deadlock a8c1911 + empty-surface 5ccc10e + build-regression 35dd537 all FIXED; the human ran `use "@audio"` then a play). Symptom: `[eval] No C# implementation found for internal proc 'micBuffer' with signature micBuffer(Second)` (line 547) + `micBuffer(Double)` (line 548) + `[eval] Failed to import '@audio' (line 1)` — so `use "@audio"` FAILS to import, which takes createSineTone/play down with it (they live in audio.flow). CONFIRMED ROOT CAUSE (code-read + reproduced): `audio.flow:547-548` declares `internal proc micBuffer(Second|Double)`; the C# impl (InputFunctions.cs, Phase 38 AUDIO-IN) is STRIPPED on Web via `flow-lang.csproj:126 <Compile Remove="StandardLibrary\Audio\InputFunctions.cs" />` (Phase 47 strip list). At `Interpreter.cs ExecuteProcDeclaration` the `else` branch (no matching C# impl) called `_errorReporter.ReportError("No C# implementation found ...")`; those accumulated errors make ModuleLoader fail the whole `@audio` import. SAME 'Desktop ≠ browser/WASM' defect class as the prior five (InputFunctions.cs is present on Desktop, so the surface binds and the in-process tests never exercised the stripped-impl path).
- timestamp: 2026-05-31T00:02Z — SWEEP (csproj Compile Remove list × embedded `.flow` `internal proc` set). Stripped C# impls register: micBuffer (InputFunctions.cs), loadSfz/__enableSfzModule (Sfz/**), osc* / __enableOscModule (OscFunctions.cs). Embedded modules (13: std/collections/bars/audio/composition/generative/improv/notation/notation-io/patterns + improv/styles/{jazz,blues,classical}) declare `internal proc` for exactly TWO stripped names: `micBuffer` (audio.flow:547-548) and `loadSfz` (std.flow:385-386). The OSC surface + `__enable*` markers live in sfz.flow/osc.flow which are NOT embedded (opt-in @sfz/@osc, rejected by the ModuleLoader Web strip-gate before any read) → not auto-loaded, not affected. So micBuffer + loadSfz are the ONLY embedded-surface/stripped-impl pairs. `@std` (ctor-bootstrapped) carries the loadSfz pair → the same latent bug was firing (masked only because std bootstrap is charitable downstream); `@audio` carries the micBuffer pair.
- timestamp: 2026-05-31T00:05Z — FIX implemented (charitable, no checkpoint — project charitable-interpretation philosophy + Phase 47 'advisory at import-time for stripped features' pattern). Single edit at `flow-lang/Interpreter/Interpreter.cs ExecuteProcDeclaration`: inserted an `else if (FlowLang.Core.FlowEngine.IsWebTarget)` branch BEFORE the existing hard-error `else`. On Web it SKIPS declaring the missing-impl overload and emits `RenderingDiagnostics.WarnOnce($"target:stripped-builtin:{proc.Name}", "[target] builtin '<name>' unavailable on Web target — surface declared in stdlib but implementation stripped (Phase 47). Skipping; calls will report 'function not found'.")` (keyed per proc-name → at most once per name per process). Gated on the COMPILE-TIME `FlowEngine.IsWebTarget` const (Phase 47 D-47-10), NOT a runtime OS check, so under FlowTarget=Desktop the branch is unreachable and the hard `_errorReporter.ReportError("No C# implementation found ...")` still fires (a missing impl there IS a genuine bug — not masked). Used the same WarnOnce infra Phase 47 ModuleLoader uses.
- timestamp: 2026-05-31T00:08Z — WEB-PATH VERIFICATION (a green Desktop in-process test is INSUFFICIENT — IsWebTarget is false in the Desktop runner). Built a Web-flavored HOST-LOADABLE PE (`-p:FlowTarget=Web -p:WasmGenerateAppBundle=false -p:IsBrowserWasmProject=false -p:RuntimeIdentifier=` → FLOW_WEB defines + InputFunctions/Sfz stripped + 13 embedded `.flow`, but a normal loadable PE) and a reflection harness whose output dir contains ZERO `.flow` (simulates the Emscripten no-filesystem VFS → forces the embedded fallback AND makes IsWebTarget true). RESULT (ALL PASS): IsWebTarget=true; `@std` ctor bootstrap CLEAN (loadSfz charitably skipped, advisory once, no 'No C# implementation'); `(add 1 2)`→3; `use "@audio"` IMPORTS (micBuffer charitably skipped) and createSineTone + play RESOLVE (only residual is the headless-host 'No audio output available' backend advisory, NOT an import/resolution failure — WebAudio handles playback in-browser); calling `(micBuffer 0.5)` degrades to a normal `Function 'micBuffer' not found` while `@audio` STILL imports. BROAD surface re-probe with CORRECT names/imports (the human's re-smoke list): print/add, collections map+len, note-stream → renderSong("piano") → writeWav (wrote a 352,844-byte WAV from the embedded surface), writeMidi(String, Song) surface RESOLVES (residual 'Could not load Melanchall.DryWetMidi' is a HARNESS-RIG missing-dependency artifact — the real AppBundle ships Melanchall.DryWetMidi.wasm, covered by DryWetMidiWasmPublishTests — NOT a Web-target stripped surface), transpose+chordNotes. The two earlier 'renderSequence/length/writeMidi-without-import not found' probe failures were PROBE-SCRIPT name/import errors (renderSequence→renderSong, length→len, writeMidi needs `use "@audio"` on Desktop TOO — verified identical Desktop behavior), NOT Web gaps.
- timestamp: 2026-05-31T00:10Z — REGRESSION NET + full verification. NEW `flow-lang.Tests/Integration/Phase48/WasmStrippedBuiltinSurfaceTests.cs` — 5 `[FlowTargetFact("Web")]` Facts (mirrors Phase 47 WebTargetModuleLoaderTests): @audio imports with micBuffer skipped + advisory; createSineTone resolves post-import; micBuffer call degrades to 'function not found' (not 'No C# implementation'); @std ctor bootstrap clean with loadSfz advisory; stripped-builtin advisory fires at most once per name. Under `dotnet test -p:FlowTarget=Web` (FLOW_WEB → IsWebTarget true in-process, branch ACTUALLY executes) → 5/5 PASS. On the Desktop runner → 5/5 SKIP (Desktop behavior provably unchanged). `dotnet build -p:FlowTarget=Desktop` exit 0 (only pre-existing warnings); Desktop `(add 1 2)`→3 smoke OK; the hard 'No C# implementation found' ReportError path intact at Interpreter.cs (reachable on Desktop). `dotnet build -p:FlowTarget=Web` exit 0. `dotnet publish -p:FlowTarget=Web -c Release` exit 0, ZERO IL2026/IL3050; `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted; shipped Webcil `flow-lang.wasm` 917,269 → 917,781 bytes (+512 bytes interpreter logic only); all 13 `FlowLang.Stdlib.*` embedded resources present in the trimmed linked PE (Cecil count 13). Phase48 26/26 GREEN + 5 new Web-Facts SKIP on Desktop; Phase47 9 passed / 8 skipped / 0 failed (WebBuild_ExitCodeIsZero stays GREEN); AssemblyReferenceScanTests skip-clean on Desktop (text resources carry no assembly refs; cannot resurrect a stripped dep). Bundle size NEUTRAL (interpreter-logic-only change) → D-48-05 MONOLITHIC SHIP.

- timestamp: 2026-05-31T10:18Z — SEVENTH, DISTINCT defect on the human re-smoke (boot 08140bb + reflection-JSON 5b80c01 + Execute-deadlock a8c1911 + empty-surface 5ccc10e + build-regression 35dd537 + stripped-impl-import b46589c all FIXED; the human ran `use "@audio"` then `(play (createSineTone 440Hz 1.0 0.5))`). Symptom: the page FROZE ~30s then `[runtime] audio playback exceeded 30s cap — Plan 48-05 streaming backlog if encountered routinely`; the charitable loadSfz/micBuffer advisories fired correctly (b46589c working); no audio produced. CONFIRMED ROOT CAUSE by code-read of `WebAudioBackend.Play` (old lines 150-181): the `FlowRuntimeInterop.PlayStereoFloat32(...)` [JSImport] marshal was wrapped in `var workerTask = Task.Run(() => { ...marshal... }, cancellationToken);` then `if (!workerTask.Wait(PlayTimeout, cancellationToken))` (PlayTimeout = TimeSpan.FromSeconds(30), old line 52). This is the EXACT single-threaded-WASM deadlock cycle 3 (a8c1911) already fixed in `WasmEntry.RunFromJs` — but cycle 3 only fixed the Execute path, NOT this playback path. In single-threaded browser WASM there is no thread pool, so `Task.Run` queues the marshal to the one main thread and `Wait(30s)` blocks that same thread → the marshal NEVER runs → 30s freeze → the cap advisory (old line 176) fires + no AudioBufferSourceNode is created. The PlayStereoFloat32 marshal is fire-and-forget anyway (builds an AudioBufferSourceNode, calls `.start()`, returns immediately; WebAudio plays asynchronously), so wrapping it in a 30s-capped worker was never meaningful in the browser. Same 'Desktop != browser/WASM' defect class as the prior six (the Desktop in-process tests gave Task.Run a real worker thread and the Play path is browser-gated, so they never exercised the single-threaded marshal).
- timestamp: 2026-05-31T10:19Z — SWEEP (grep `Task.Run` + `.Wait(` across all browser-reachable audio + runtime paths, plus every FlowRuntimeInterop caller). RESULTS: (a) `WebAudioBackend.Play` — THE deadlock instance, fixed below. (b) `StandardLibrary/Audio/PlaybackFunctions.cs:104,116,163,211` — 4 `Task.Run(...)` calls (StreamSequence/StreamBuffer/LoopBufferInfiniteAsync/LoopBufferNAsync) that are FIRE-AND-FORGET (return `Value.Void()` immediately; NO `.Wait()` blocks the calling thread) → NOT the deadlock pattern; the `play` builtin the smoke uses routes through `PlayBuffer`→`PlaySamples`→`backend.Play` SYNCHRONOUSLY (no Task.Run), so the fix makes it deadlock-free. (loop/stream/preview on Web is the separable v1.6 AudioWorklet-streaming path per D-48-02 — out of 48-06 scope; left untouched.) (c) `Audio/CoreAudioBackend.cs:352` `_bufferAvailable.Wait(50, ct)` — Desktop-only backend (DetectBackend never picks it in-browser); explicitly NOT touched per prescription. (d) `Runtime/WasmEntry.cs:148,151,273,274` — cycle-3 DOC COMMENTS describing the already-removed pattern, not live code. (e) `FlowRuntimeInterop.cs` — pure [JSImport] binding declarations, no Task.Run/.Wait. So `WebAudioBackend.Play` is the ONLY browser-reachable deadlock; one fix closes it.
- timestamp: 2026-05-31T10:20Z — FIX implemented (same human-approved cycle-3 'run synchronously, cap best-effort' approach — extends it to the playback path cycle 3 missed; NO new checkpoint). In `flow-lang/Audio/WebAudioBackend.cs Play`: REMOVED the `var workerTask = Task.Run(() => { ... PlayStereoFloat32 ... }, cancellationToken);` wrapper and the `if (!workerTask.Wait(PlayTimeout, cancellationToken)) { WarnOnce("wasm-30s-cap", ...); throw new OperationCanceledException(...); }` block. The `PlayStereoFloat32` marshal now runs SYNCHRONOUSLY on the calling thread inside the existing `#pragma warning disable CA1416` Pattern B guard, with `_activeSource = FlowRuntimeInterop.PlayStereoFloat32(_audioContext, samplesAsBytes, channels: 2, sampleRate);`. Removed the now-unused `PlayTimeout` const (old line 52) and the unused `using FlowLang.Diagnostics;` (RenderingDiagnostics.WarnOnce no longer called). PRESERVED everything else: `samples`/`_disposed` early returns, the D-48-11 `if (!OperatingSystem.IsBrowser()) return;` Desktop no-op, `EnsureInitialized` + null-context guard, the `cancellationToken.IsCancellationRequested` early return, D-48-07 `PromoteToStereo` BEFORE the marshal, `MemoryMarshal.AsBytes` zero-copy reinterpret, and the PINNED IAudioBackend.Play signature (cancellationToken stays per D-47-05 — just no longer caps a blocking wait). Updated the class XML doc D-48-10 bullet to state the 30s cap is best-effort/non-enforceable in single-threaded WASM and the marshal now runs synchronously (consistent with cycle 3's a8c1911 D-48-10 amendment); kept the `wasm-30s-cap` sentinel OUT of the happy path (no longer fires for normal playback). Grep confirms NO `Task.Run`/`workerTask`/`.Wait(`/`PlayTimeout`/`wasm-30s-cap`/`RenderingDiagnostics` remain in executable code (only in the amendment doc comments); NO test or JS depended on the removed cap/`OperationCanceledException` behavior.
- timestamp: 2026-05-31T10:22Z — REGRESSION NET (browser-free source-level guard — the [JSImport] Play path is browser-only and cannot be auto-exercised on Desktop or in the in-process Web runner; honestly a PROXY). NEW `flow-lang.Tests/Integration/Phase48/WebAudioBackendSyncPlayTests.cs` — 3 `[Fact]` (plain, source-grep; mirrors CultureInvariantSweepTests' FindRepoRoot + File.ReadAllText pattern). After STRIPPING C# comments (so the cycle-7 amendment doc comments that describe the removed pattern don't trip the guard): `Play_DoesNotWrapMarshalInTaskRun_NoDeadlock` asserts NO `Task.Run`/`workerTask`/`.Wait(` in executable source; `PlayTimeout_ConstAndCapThrow_AreRemoved` asserts NO `PlayTimeout`/`wasm-30s-cap`/`OperationCanceledException`; `Play_CallsPlayStereoFloat32Synchronously_AssignsActiveSource` asserts the synchronous `_activeSource = FlowRuntimeInterop.PlayStereoFloat32` call + PromoteToStereo + MemoryMarshal.AsBytes + the OperatingSystem.IsBrowser() gate are all preserved. 3/3 PASS. WebAudioBackendIntegrationTests (the existing Desktop-side Facts) 8/8 GREEN — the synchronous change did not break PromoteToStereo / Dispose no-op / IsAvailable=false / Initialize charitable-false / WriteChunk-throws (Play is browser-gated, charitably no-ops on Desktop). The DECISIVE confirmation remains the human browser re-smoke (audible tone) — stated honestly.
- timestamp: 2026-05-31T10:24Z — VERIFICATION (green build alone NOT treated as sufficient). (1) `dotnet build -p:FlowTarget=Desktop` exit 0 (only pre-existing warnings: Rug.Osc NU1701 + the pre-existing duplicate-`FlowLang.Diagnostics`-using at ExpressionEvaluator.cs:14 + known nullability/unused-var — NONE from this change). (2) `dotnet build -p:FlowTarget=Web` exit 0 (only pre-existing CA1416 in TestRunner.cs). (3) `dotnet publish -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050/IL2075; `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted; `flow-runtime.js` import `./_framework/dotnet.js` intact; shipped Webcil `flow-lang.wasm` 917,781 -> 916,757 bytes (1 KB SMALLER — removed the Task.Run/Wait wrapper); 13 `FlowLang.Stdlib.*` embedded resources intact. (4) Phase47 suite 9 passed / 8 skipped / 0 failed (`WebBuild_ExitCodeIsZero` stays GREEN). (5) Phase48 suite 29 passed / 5 skipped / 0 failed (26 prior gates intact + 3 new cycle-7 source-guard Facts; the 5 `[FlowTargetFact("Web")]` Facts SKIP on Desktop). (6) WebAudioBackendIntegrationTests 8/8 GREEN. (7) AssemblyReferenceScanTests skip-clean on Desktop (Web-gated, unchanged). (8) BUNDLE SIZE: AppBundle `_framework/` 5,379,873 bytes (5.13 MB) uncompressed -> D-48-05 MONOLITHIC SHIP — NEUTRAL vs cycle 6 (interpreter/backend-logic-only change removed bytes, added none).

## Eliminated

- timestamp: 2026-05-30T17:50Z — "Just set `WasmGenerateAppBundle=true` alone" is INSUFFICIENT in isolation: `IsBrowserWasmProject` was also empty for the library, so the app-bundle AfterTargets hook (`WasmTriggerPublishAppAfterThisTarget`) would not be wired. The experiment set all three (`WasmGenerateAppBundle`+`IsBrowserWasmProject`+`WasmMainJSPath`); that combination is what fired the target.
- timestamp: 2026-05-30T17:35Z — Switching to `Microsoft.NET.Sdk.WebAssembly` SDK is NOT available as a standalone SDK in this install (it ships with the Blazor/`Microsoft.NET.Sdk.Web` stack, not the `wasm-tools` workload). The `_IsLibraryMode=true` native path is therefore not reachable without pulling the Blazor SDK — heavier than the property-forcing approach, which works today.

## Resolution

- **root_cause:** `flow-lang` is a plain `Microsoft.NET.Sdk` LIBRARY project, so the Mono-WASM runtime pack's app-bundle gate in `WasmApp.Common.targets` (`_IsLibraryMode` requires `UsingMicrosoftNETSdkWebAssembly == 'true'`, which is empty under the plain SDK) left `IsBrowserWasmProject` + `WasmGenerateAppBundle` defaulting to FALSE. The `_WasmGenerateAppBundle` target never ran, so no `dotnet.boot.js` / `_framework/` / `AppBundle/` was produced and the browser's `dotnet.create()` 404'd on its boot manifest.

- **fix (MINIMAL-LEVER, human-approved):** Force the runtime-pack app-bundle target ON via three properties inside the existing `FlowTarget=Web` PropertyGroup — `WasmGenerateAppBundle=true`, `IsBrowserWasmProject=true`, `WasmMainJSPath=wasm/flow-runtime.js` — plus `WasmMainHTMLPath=wasm/index.html` to drop the dev-smoke harness at the bundle root. Single-source-of-truth Pattern A: no SDK swap, no second project. Reconciled the three serve artifacts against the real generated layout (verified empirically, NOT guessed):
  - `flow-lang/wasm/flow-runtime.js` — import fixed `'../dotnet.js'` → `'./_framework/dotnet.js'` (flow-runtime.js sits at `AppBundle/` root, dotnet.js under `AppBundle/_framework/`). D-48-12 ES module + D-48-09 user-gesture chain + 5-name `setModuleImports('flow-runtime', …)` surface + `RunResult` JSON.parse path all preserved.
  - `flow-lang/wasm/index.html` — serve-path header comments corrected from the never-produced `AppBundle/wasm/` layout to the real `AppBundle/` root; in-file `import './flow-runtime.js'` confirmed correct (same dir).
  - `flow-lang/flow-lang.csproj` — old flat-tree `CopyToPublishDirectory` hooks for flow-runtime.js/index.html set to `Never` (the runtime pack now owns the AppBundle copy via WasmMainJSPath/WasmMainHTMLPath); trim-roots wiring intact.

- **layout learned:** App-bundle mode emits assemblies as **Webcil `.wasm`** (`flow-lang.wasm`, `Melanchall.DryWetMidi.wasm`), NOT `.dll`; the bootable bundle lands at `bin/Release/net10.0/browser-wasm/AppBundle/` (where `WasmAppDir` = `$(OutputPath)/AppBundle` defaults), a SIBLING of the old flat `publish/` tree (the flat tree still emits real PE `.dll`s alongside).

- **verification (automated — green build alone was NOT treated as sufficient):**
  - `dotnet publish -p:FlowTarget=Web -c Release` exit 0 AND `AppBundle/_framework/dotnet.boot.js` confirmed on disk (`mainAssemblyName: flow-lang.dll`), with `dotnet.js` + `dotnet.native.wasm` + `flow-lang.wasm` + `Melanchall.DryWetMidi.wasm` in a coherent servable layout.
  - `flow-runtime.js` import path + `index.html` load path confirmed against the real generated files in the bundle.
  - `dotnet build -p:FlowTarget=Desktop` exit 0 (P/Invoke / SFZ / OSC / live-coding intact).
  - Phase48 fixtures pass: `WasmBuildPipelineTests` (3/3 — the misnamed `WasmPublish_ProducesAppBundle` is now `WasmPublish_ProducesBootableAppBundle` asserting `dotnet.boot.js` EXISTS — the regression gate that would have caught the original defect), `DryWetMidiWasmPublishTests` (2/2 — Cecil scan reads the flat-tree PE `flow-lang.dll`; bundle presence-checks Webcil `flow-lang.wasm`), `BundleSizeBudgetTests` (2/2).
  - Web build forbidden-reference invariant confirmed clean (deps.json scan: only DryWetMidi + Mono runtime pack; NO Rug.Osc / RtMidi / libpulse / AudioToolbox). Phase47 `AssemblyReferenceScanTests` are `[FlowTargetFact("Web")]`-gated and skip on the Desktop runner (pre-existing, unchanged).
  - **Bundle size:** AppBundle `_framework/` = **1.59 MB Brotli / 5.24 MB uncompressed** → D-48-05 `MONOLITHIC SHIP`. This MEASURES THE BOOTABLE BUNDLE (Webcil), vs. the prior 3.07 MB-Brotli flat-publish-tree number (which also counted ICU `.dat` + stdlib `.flow`); not a regression — comfortably under the 15 MB target / 20 MB hard cap.

- **files_changed:**
  - `flow-lang/flow-lang.csproj`
  - `flow-lang/wasm/flow-runtime.js`
  - `flow-lang/wasm/index.html`
  - `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs`
  - `flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs`
  - `flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs`

- **STILL REQUIRED to fully close 48-06 (NOT done by this agent — cannot be):** A real-browser re-smoke (Plan 48-06 HUMAN-UAT Chrome/Firefox rows) — serve `bin/Release/net10.0/browser-wasm/AppBundle/` and load `index.html`, confirm boot + audible 440 Hz tone. The automated `dotnet.boot.js`-exists assertion is the strongest available proxy, NOT full closure.

- **separate follow-up (explicitly NOT solved here, per human decision):** stdlib `.flow` VFS mounting for in-browser `use "@std"` (ModuleLoader.ResolveStdlibPath resolves via AppContext.BaseDirectory + File IO = Emscripten VFS root). The 48-06 smoke script imports no module, so boot + tone is verifiable without it.

---

## Resolution — SECOND DEFECT (JsonSerializerIsReflectionDisabled), same Plan 48-06 gate

- **root_cause:** `WasmEntry.RunFromJs` serialized its `RunResult` via reflection-based System.Text.Json (`JsonSerializer.Serialize(result, _jsonOptions)`, plain `JsonSerializerOptions`). The `FlowTarget=Web` publish sets `<TrimMode>full</TrimMode>`, which disables reflection-based serialization in the trimmed WASM build, so the serializer threw `JsonSerializerIsReflectionDisabled` at runtime in the browser the moment Run was clicked. Identical "Desktop ≠ trimmed/browser" defect class as the boot bug — masked because the Desktop in-process tests run with reflection JSON enabled and never touched the trimmed serializer path.

- **fix:** Replaced reflection-based serialization with a SOURCE-GENERATED `System.Text.Json` context (the canonical AOT/WASM-safe pattern):
  - `flow-lang/Runtime/WasmEntry.cs` — added `internal partial class FlowWasmJsonContext : JsonSerializerContext` with `[JsonSourceGenerationOptions(PropertyNamingPolicy = JsonKnownNamingPolicy.CamelCase, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull)]` + `[JsonSerializable(typeof(RunResult))]` + `[JsonSerializable(typeof(RunError))]`; switched the serialize call to `JsonSerializer.Serialize(result, FlowWasmJsonContext.Default.RunResult)`; retired the now-unused reflection `_jsonOptions` field. The D-48-14/15 JSON shape (camelCase keys + null-omission for wav/midi/sourceSnippet) is preserved byte-for-byte — `JsonKnownNamingPolicy.CamelCase` is the source-gen equivalent of `JsonNamingPolicy.CamelCase`. Did NOT enable `JsonSerializerIsReflectionEnabledByDefault` (rejected — fights TrimMode=full, risks IL2026/bloat).
  - `flow-lang/wasm/trim-roots.xml` — refreshed the stale comment (the roots were said to be "reflected on by JsonSerializer (IL2026 at WasmEntry.cs:283)"; now documents the source-gen path + why the roots are still kept).
  - Audited every reflection-dependent path reachable from the [JSExport] surface (RunFromJs/PlayFromJs) — Activator / Type.GetType / GetProperty / other JsonSerializer calls: NONE; this was the only JsonSerializer use in the library.

- **regression net (browser-free proxy + infra hardening):**
  - `flow-lang.Tests/Integration/Phase48/WasmJsonSerializationTests.cs` (NEW) — pins serialization through `FlowWasmJsonContext.Default.RunResult` + asserts camelCase / null-omission / no-PascalCase-leak, plus an end-to-end `RunFromJs` shape assertion. Catches this defect class on the Desktop runner without a browser.
  - `flow-lang.Tests/Integration/Phase48/WasmEntryConsoleCollection.cs` (NEW) — serial xUnit collection for RunFromJs-calling classes (process-wide Console.SetOut redirection race).
  - `flow-lang.Tests/Integration/Phase48/WasmWebPublishCollection.cs` (NEW) — serial xUnit collection for FlowTarget=Web publish-shellout classes (shared obj/ intermediate race → MarshalingPInvokeScanner BadImageFormatException). Applied to WasmBuildPipelineTests / BundleSizeBudgetTests / DryWetMidiWasmPublishTests.
  - Both races were PRE-EXISTING latent flakiness; each affected class passed in isolation. Full Phase48 suite now 21/21 green across 3 consecutive from-clean runs.

- **verification:** Desktop build exit 0; Web publish exit 0 with ZERO IL2026/IL3050 (only the pre-existing unrelated IL2075 remains); `dotnet.boot.js` still emitted (prior boot gate intact); Web deps.json forbidden-ref scan clean; Phase48 suite 21/21. Bundle: 5.06 MB uncompressed / 1.54 MB Brotli — MONOLITHIC SHIP, neutral-to-smaller vs prior 5.24 / 1.59 MB.

- **files_changed:**
  - `flow-lang/Runtime/WasmEntry.cs`
  - `flow-lang/wasm/trim-roots.xml`
  - `flow-lang.Tests/Integration/Phase48/WasmJsonSerializationTests.cs` (new)
  - `flow-lang.Tests/Integration/Phase48/WasmEntryConsoleCollection.cs` (new)
  - `flow-lang.Tests/Integration/Phase48/WasmWebPublishCollection.cs` (new)
  - `flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs` (+[Collection])
  - `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` (+[Collection])
  - `flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs` (+[Collection])
  - `flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs` (+[Collection])

- **STILL REQUIRED to fully close Plan 48-06 (cannot be automated):** real-browser re-smoke — publish `-p:FlowTarget=Web -c Release`, serve `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`, load `index.html` in Chrome/Firefox, click Run, expect an AUDIBLE 440 Hz tone + structured stdout/errors in DevTools, with NO `JsonSerializerIsReflectionDisabled`. The source-gen contract test is the strongest available browser-free proxy, NOT full closure.


---

## Resolution — THIRD DEFECT (single-threaded-WASM Task.Run+Wait DEADLOCK → always kind="cancel"), same Plan 48-06 gate

- **root_cause:** `WasmEntry.RunFromJs` enforced the D-48-10 30s wall-clock cap with `var workerTask = Task.Run(() => engine.Execute(...)); if (!workerTask.Wait(RunTimeout)) { ...cancel... }` (Pattern C, carried over from Phase 38 LIVE-02's Desktop threading model). Mono-WASM is SINGLE-THREADED by default (dotnet/runtime#85592): `Task.Run` queues the work to the one main thread and `workerTask.Wait(30s)` then BLOCKS that same thread, so `engine.Execute` never runs. Every browser call deadlocked and returned the `kind="cancel"` RunError at exactly 30s — no audio, no stdout, the script never executed. A hard wall-clock cap is fundamentally unenforceable by blocking in a single-threaded runtime (no preemption). Identical "Desktop ≠ browser/WASM" defect class as the boot bug and the reflection-JSON bug — masked because the Desktop in-process tests gave `Task.Run` a real worker thread, so they passed while the browser deadlocked.

- **fix (human-approved "run synchronously, cap best-effort"):** In `flow-lang/Runtime/WasmEntry.cs`, `RunFromJs` now calls `engine.Execute(source ?? string.Empty, "<wasm>")` SYNCHRONOUSLY on the calling (main) thread, then `errors = MapFlowErrors(engine.ErrorReporter.Errors);`. The `Task.Run` + `workerTask.Wait(RunTimeout)` wrapper and the unused `RunTimeout` const + `using System.Threading.Tasks;` were removed. Preserved: the outer try/catch (`kind="runtime"` host-exception guard, T-48-15), the `finally` that restores `Console.Out`/`Console.Error` (T-48-14), the `Stopwatch` + `DurationMs`, and the last-resort serializer guard. The hard 30s cap becomes **best-effort / non-preemptive in single-threaded WASM**: a runaway Flow script hangs its own browser tab exactly like any synchronous single-threaded JS — the accepted tradeoff (composer controls their own script; matches the browser execution model; ergonomics-first per project philosophy). The `"cancel"` RunError kind stays **DEFINED** in the D-48-14 contract (field names + kinds are PINNED) but is no longer raised here.

- **D-48-10 AMENDMENT (LOCKED-decision browser-semantics change — flagged for the closer):** D-48-10 should be recorded by the Plan 48-07 closer / 48-VERIFICATION.md as **"hard cap on Desktop, best-effort (synchronous, non-preemptive) in single-threaded WASM"** so the locked-decision change is traceable. This agent did NOT edit ROADMAP/REQUIREMENTS/CLAUDE — that is the closer's job; the amendment is documented in-code (WasmEntry class XML doc + RunFromJs/MapFlowErrors/RunError remarks + flow-runtime.js header & `run:` doc) and here.

- **honest no-fake-cap note:** Added a comment in `flow-runtime.js` (header + `run:` doc) that a JS-side `setTimeout` CANNOT preempt a synchronous dotnet call (the JS event loop is blocked for the whole duration of `RunFromJs`), so NO non-functional JS-side cap was added. Kept honest and documented.

- **regression net (browser-free PARTIAL PROXY):** `flow-lang.Tests/Integration/Phase48/WasmSynchronousExecutionTests.cs` (NEW, `[Collection(WasmEntryConsoleCollection.Name)]`) — `RunFromJs_SimpleScript_RunsToCompletion_PopulatedStdout_NoErrors` (asserts `(print "hi")` → populated stdout + EMPTY errors, i.e. no `kind="cancel"`) and `RunFromJs_ToneRender_RunsToCompletion_NoCancel` (asserts a `use "@audio"` + `createSineTone` render runs to completion with no cancel error). Documented in the class XML doc as a PARTIAL PROXY: the Desktop xUnit runner is multi-threaded, so the old `Task.Run` shape would have passed here too — these Facts pin the post-fix "script runs to completion" contract; the REAL confirmation is the human browser re-smoke. No happy-path coverage weakened; grep confirmed NO existing test depended on the timeout/cancel behavior.

- **verification (automated — green build alone NOT treated as sufficient):**
  - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exit 0 (only pre-existing warnings; Desktop default intact).
  - `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050/IL2075; `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted; `flow-runtime.js` at AppBundle root with `import { dotnet } from './_framework/dotnet.js';` intact (prior boot + reflection-JSON gates preserved).
  - Phase48 xUnit suite **23/23 green** (21 prior + 2 new), incl. cycle-1 boot-manifest gate + cycle-2 `WasmJsonSerializationTests`.
  - `AssemblyReferenceScanTests` skip-clean on Desktop (pre-existing `[FlowTargetFact("Web")]` gating, unchanged).
  - **Bundle size** (test-written 48-BUNDLE-SIZE.md): 5,302,060 bytes (5.06 MB) uncompressed / 1,610,471 bytes (1.54 MB) Brotli → D-48-05 MONOLITHIC SHIP — NEUTRAL vs prior cycle (source change adds no bloat).
  - D-48-14/15 JSON shape unchanged (source-gen `FlowWasmJsonContext` untouched: camelCase + null-omission + same field names/kinds).

- **files_changed:**
  - `flow-lang/Runtime/WasmEntry.cs`
  - `flow-lang/wasm/flow-runtime.js`
  - `flow-lang.Tests/Integration/Phase48/WasmSynchronousExecutionTests.cs` (new)

- **STILL REQUIRED to fully close Plan 48-06 (cannot be automated):** real-browser re-smoke — `dotnet publish -p:FlowTarget=Web -c Release`, serve `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`, load `index.html` in Chrome/Firefox, click Run, expect an AUDIBLE 440 Hz tone + structured stdout/errors in DevTools, with NO `cancel` / NO `JsonSerializerIsReflectionDisabled` / NO boot 404. That final human pass is what closes Plan 48-06.


---

## Resolution — FOURTH DEFECT (entire builtin SURFACE missing in-browser — stdlib `.flow` absent from WASM VFS), same Plan 48-06 gate

- **leading hypothesis REFUTED:** The dispatched hypothesis was that `TrimMode=full` strips/hollows the builtin-registration surface. RULED OUT EMPIRICALLY: Mono.Cecil inspection of the TRIMMED `obj/Release/net10.0/browser-wasm/linked/flow-lang.dll` (= the Webcil payload shipped as `AppBundle/_framework/flow-lang.wasm`) shows EVERY `Register*` body fully intact post-trim (`RegisterStdLib`=1786 instrs, `RegisterAudio`=2015, `RegisterCollections`=597, `RegisterMath`=444, `FlowEngine..ctor`=145, `WasmEntry.RunFromJs`=107) and ALL registered-name string literals present (createSineTone/print/reverb/renderSong/...) in the trimmed PE AND the shipped Webcil (verified with `strings -e l`, UTF-16LE — an early ASCII-`strings` pass produced a FALSE 'trimming' smoking gun that was corrected). `dotnet publish -p:FlowTarget=Web -p:TrimmerSingleWarn=false` emits ZERO trim warnings touching the registration surface. The C# `InternalFunctionRegistry` IS fully populated in the browser. **No trim-roots change and no TrimMode relaxation were needed — the continuation-context FALLBACK checkpoint (relaxing TrimMode) was therefore never reached.**

- **root_cause:** Flow's builtin CALLABLE SURFACE is declared as `internal proc` statements in `std.flow` (which `use "@collections"` + `use "@bars"`), NOT in C#. The C# registry holds only IMPLEMENTATIONS keyed by name; the interpreter (`Interpreter.cs:845-848`) binds an impl ONLY when a matching `internal proc` surface overload already exists — else `ExecutionContext.cs:737-738` reports `Function '<name>' not found (0 overloads registered)`. On Desktop the surface is bootstrapped as a SIDE EFFECT of the `FlowEngine` ctor loading the shipped improv style packs (`StyleRegistry.LoadShippedAndUserPacks` → packs `use "@improv"` → `improv.flow` does `use "@std"`), which reads the `.flow` files from disk via `AppContext.BaseDirectory` + `File.ReadAllText`. The WASM AppBundle ships ZERO `.flow` files and mounts no VFS path for them (the Emscripten VFS has no host filesystem), so EVERY stdlib `LoadModule` silently fails `File.Exists` (charitable-by-design → one-shot advisory, continues), `std.flow` NEVER loads, and the builtin surface is EMPTY → every builtin lookup misses → `[eval] Function '<name>' not found` for print/add/createSineTone/play — the whole language. This is the stdlib-VFS concern the cycle-1 18:05Z evidence note deferred as 'separable'; that note's premise ('the smoke script imports no module so boot+tone is verifiable without it') was WRONG — the builtin surface ITSELF lives in `std.flow`. Same 'Desktop ≠ browser/WASM' defect class as the prior three (the in-process Desktop tests load the `.flow` files from disk, so they never exercised the no-filesystem path).

- **fix (PREFERRED axis — stays within ALL locked decisions; no checkpoint required):** Make the stdlib `.flow` source available in-browser via assembly EMBEDDED RESOURCES (the Emscripten VFS has no host filesystem to ship `CopyToPublishDirectory` files to), and bootstrap the surface explicitly:
  - `flow-lang/flow-lang.csproj` (Web-only `<ItemGroup Condition="'$(FlowTarget)' == 'Web'">`): `<EmbeddedResource>` the 10 surviving stdlib modules (std/collections/bars/audio/composition/generative/improv/notation/notation-io/patterns) + the 3 improv style packs (jazz/blues/classical), each with `<LogicalName>FlowLang.Stdlib.<relative></LogicalName>`. `sfz.flow`/`osc.flow` are NOT embedded (stripped on Web per D-47-03; the ModuleLoader Web strip-gate rejects `@sfz`/`@osc` before any read). No conflict with the existing `<None Update>` copy entries (publish exit 0, no duplicate-item MSB error).
  - `flow-lang/Runtime/ModuleLoader.cs`: `LoadModule` now reads the FILESYSTEM FIRST, then a new `TryReadEmbeddedModule(resolvedPath, out source)` fallback that strips the `AppContext.BaseDirectory` prefix to recover `<relative>` and looks up the `FlowLang.Stdlib.<relative>` manifest resource. Desktop is byte-identical: `File.Exists` wins on disk, and the Desktop build embeds nothing (the ItemGroup is `FlowTarget=Web`-gated), so the fallback returns false and the original 'Import file not found' diagnostic still fires for genuinely-missing imports.
  - `flow-lang/Core/FlowEngine.cs`: explicit `_moduleLoader.LoadModule("@std", "<engine-init>", _context)` at ctor end, BEFORE the style-pack load. This bootstraps the builtin surface DETERMINISTICALLY rather than depending on the fragile/incidental improv-pack chain (which does not fire at all on WASM — the style-pack directory does not exist in the VFS). Idempotent: the later improv `use "@std"` hits the `_loadedModules` `AlreadyLoaded` short-circuit, so Desktop ordering/behavior is unchanged.

- **why NOT the dispatched levers:** The continuation context's PREFERRED lever (expand `wasm/trim-roots.xml` to root the Register* surface) and FALLBACK lever (relax `TrimMode`) both target TRIMMING, which the Cecil evidence conclusively rules out as the cause. Rooting more types would have changed nothing (the bodies already survive); relaxing TrimMode would have bloated the bundle for no benefit. The real axis is the stdlib-`.flow`-VFS gap, fixed above WITHOUT touching D-48-02 (TrimMode=full) or the trim-roots descriptor.

- **decisive end-to-end proof (no-filesystem scenario, browser-free):** Built a Web-flavored PE (`-p:FlowTarget=Web` with app-bundle props off → plain loadable PE carrying the embedded `.flow` resources), MOVED ALL on-disk `.flow` files away to simulate the Emscripten VFS, loaded the assembly via reflection, constructed a `FlowEngine`, and ran `(print ...)` / `(add 1 2)` / `(mul 6 7)` — resolved 3/3 PURELY from embedded resources with ZERO 'Function not found' errors. Pre-fix this is exactly the browser failure.

- **regression net (browser-free, Cecil-on-published-binary):** `flow-lang.Tests/Integration/Phase48/WasmStdlibEmbedTests.cs` (NEW, `[Collection(WasmWebPublishCollection.Name)]`, 3 Facts) — publishes `-p:FlowTarget=Web -c Release` and asserts all 13 `FlowLang.Stdlib.*` resources survive into the TRIMMED `linked/flow-lang.dll`; asserts the embedded `std.flow` bytes contain the `internal proc print` + `use "@collections"`/`use "@bars"` surface; pins the fallback's graceful-degrade contract. Mirrors `DryWetMidiWasmPublishTests`/`AssemblyReferenceScanTests` (post-publish Cecil scan). Documented as a STRONG PROXY — it asserts the property the browser depends on (embedded resources physically in the trimmed output) without a browser — NOT full closure.

- **verification:**
  - `dotnet build -p:FlowTarget=Desktop` exit 0; Desktop `(print)`/`(add)` still work.
  - `dotnet publish -p:FlowTarget=Web -c Release` exit 0, ZERO IL2026/IL3050; boot manifest still emitted; shipped Webcil `flow-lang.wasm` 844,565 → 917,269 bytes (+72 KB embedded text) and CONTAINS the embedded resources + `internal proc print` content.
  - Phase48 suite 26/26 green (23 prior + 3 new). Full suite (collection-parallelism off) 2206 pass / 1 fail / 9 skip — the single fail is PRE-EXISTING `Phase47.BuildConditioningSmokeTests.WebBuild_ExitCodeIsZero` (verified identical RED on clean HEAD; cycle-1's forced app-bundle props break plain `dotnet build -p:FlowTarget=Web`; ship path uses `publish`). Parallel-on adds flaky failures that ALL pass in isolation (pre-existing Console.SetOut race).
  - AssemblyReferenceScanTests skip-clean on Desktop; embedding text resources cannot resurrect a stripped dep (resources carry no assembly refs); DryWetMidi forbidden-ref scan green.
  - BUNDLE SIZE (test-regenerated 48-BUNDLE-SIZE.md): 5.13 MB uncompressed / 1.55 MB Brotli → D-48-05 MONOLITHIC SHIP (+19 KB Brotli vs cycle-3; far under 15 MB target / 20 MB hard cap).

- **files_changed:**
  - `flow-lang/flow-lang.csproj`
  - `flow-lang/Runtime/ModuleLoader.cs`
  - `flow-lang/Core/FlowEngine.cs`
  - `flow-lang.Tests/Integration/Phase48/WasmStdlibEmbedTests.cs` (new)

- **KNOWN REMAINING BROWSER GAP (separable, NOT fixed here):** the improv STYLE PACKS (`jam` style content) are embedded but NOT loaded in-browser — `StyleRegistry.LoadDir` uses `Directory.GetFiles`, which returns nothing on WASM (no filesystem). The CORE builtin surface (print/add/str/createSineTone/play/note-streams/writeWav/writeMidi/collections/transforms/harmony) loads fine via the explicit `@std` bootstrap; only `@improv` `jam` with style packs is affected. Deferred to keep cycle 4 focused on the actual defect (empty surface). v1.6: route `StyleRegistry` shipped-pack discovery through the embedded-resource set on Web.

- **PRE-EXISTING ISSUE FLAGGED FOR THE 48-07 CLOSER:** `Phase47.BuildConditioningSmokeTests.WebBuild_ExitCodeIsZero` is RED on dev HEAD (independent of this fix) — cycle-1's `WasmGenerateAppBundle=true`/`IsBrowserWasmProject=true` make a plain `dotnet build -p:FlowTarget=Web` fail with `WasmAssembliesToBundle item is empty. No assemblies to process`. The bootable ship path uses `dotnet publish` (green). Either gate the app-bundle props to the publish phase, or update the test to assert `publish` rather than `build`.

- **STILL REQUIRED to fully close Plan 48-06 (cannot be automated):** real-browser re-smoke — `dotnet publish -p:FlowTarget=Web -c Release`, serve `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`, load `index.html` in Chrome/Firefox, click Run. Because the registry was wholesale-empty, the human should exercise a BROADER surface this time (NOT a single line): print/add, a `createSineTone`+`play` tone, an inline note stream, `writeWav`, `writeMidi`, a collections op — to surface any other browser-only gaps in one pass. Expect NO `Function not found` / NO `cancel` / NO `JsonSerializerIsReflectionDisabled` / NO boot 404, and an AUDIBLE 440 Hz tone.


---

## Resolution — FIFTH ISSUE (build regression from cycle-1: plain `dotnet build -p:FlowTarget=Web` fails `WasmAssembliesToBundle item is empty`), same Plan 48-06 gate

- **classification:** This is NOT a new browser defect — it is a BUILD REGRESSION introduced by cycle-1's own minimal-lever fix, and is exactly the 'PRE-EXISTING ISSUE FLAGGED FOR THE 48-07 CLOSER' noted at the end of the cycle-4 Resolution. Fixed here as a bounded build-gating change so the closer inherits a green tree.

- **root_cause:** Cycle-1 forced `IsBrowserWasmProject=true` + `WasmGenerateAppBundle=true` to trigger the Mono-WASM runtime pack's app-bundle target on this plain-`Microsoft.NET.Sdk` LIBRARY. `IsBrowserWasmProject=true` wires the public `WasmBuildApp` target (`WasmApp.Common.targets:275`, `AfterTargets=$(WasmBuildAppAfterThisTarget)`) whose only run-suppression for plain build is `'$(WasmBuildOnlyAfterPublish)' != 'true'` (line 277). On a plain `dotnet build` neither `_IsPublishing` nor `DeployOnBuild` is set, so `WasmBuildOnlyAfterPublish` defaults FALSE (line 174) and the build-phase app-bundle pipeline runs — but the assembly set that populates `WasmAssembliesToBundle` is gathered only in the nested-PUBLISH flow, so `PrepareInputsForWasmBuild` errors `WasmAssembliesToBundle item is empty. No assemblies to process` (line 368). Net effect: the documented `dotnet build -p:FlowTarget=Web` command (CLAUDE.md `## Compile-Target Flavors`) failed, and Phase47 `BuildConditioningSmokeTests.WebBuild_ExitCodeIsZero` was RED on clean HEAD. The publish ship path was always green because publish sets `_IsPublishing=true` → the SDK auto-sets `WasmBuildOnlyAfterPublish=true` and the bundle is generated in the publish pass.

- **fix (build-gating only — single edit, all locked decisions intact, no checkpoint):** Added `<WasmBuildOnlyAfterPublish>true</WasmBuildOnlyAfterPublish>` inside the existing `FlowTarget=Web` PropertyGroup in `flow-lang/flow-lang.csproj`. This is the SDK's OWN documented lever (`WasmApp.Common.targets:76` — 'Causes relinking to be done only for Publish'; the SDK auto-sets the same property when `_IsPublishing`/`DeployOnBuild` is true, lines 173-174). Setting it unconditionally makes the `WasmBuildApp` skip-gate (`'$(WasmBuildOnlyAfterPublish)' != 'true'`) evaluate false during plain build, so the build-phase bundle target is skipped; publish still runs the bundle (the publish flow gathers the assemblies and generates `AppBundle/_framework/dotnet.boot.js`). Single-source-of-truth Pattern A preserved: no SDK swap, no second project, no TrimMode/trim-roots/boot-config/VFS change. An inline comment cites the exact SDK target lines so a future contributor can re-derive the mechanism.

- **why this lever (not the alternatives):** Considered (a) `Condition="'$(_IsPublishing)' == 'true'"` on the four forced props — workable but `_IsPublishing` is an internal SDK sentinel and gating `IsBrowserWasmProject` itself off at build time changes more surface than necessary; (b) a publish-only `<Target>` wrapper — heavier and duplicates SDK plumbing. `WasmBuildOnlyAfterPublish` is the SDK's PURPOSE-BUILT, documented knob for exactly 'do the WASM relink/bundle only on publish', so it is the cleanest and most future-proof. Verified empirically (not guessed) against both `dotnet build` and `dotnet publish`.

- **verification (automated — green build alone NOT treated as sufficient):**
  - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exit 0 — the `WasmAssembliesToBundle item is empty` error is GONE (regression fixed).
  - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exit 0 (Desktop default untouched; the prop is Web-PropertyGroup-gated; P/Invoke/SFZ/OSC/live-coding intact).
  - `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050; `AppBundle/_framework/dotnet.boot.js` (5561 bytes, mainAssemblyName flow-lang.dll) STILL emitted; AppBundle root still has flow-runtime.js + index.html + package.json; shipped Webcil `flow-lang.wasm` still 917,269 bytes with all 13 `FlowLang.Stdlib.*` embedded resources present — cycles 1-4 (boot bundle / source-gen JSON / synchronous execute / embedded stdlib) ALL intact.
  - Phase47 suite 9 passed / 8 skipped / 0 failed — `WebBuild_ExitCodeIsZero` now GREEN (it was the single full-suite RED on clean HEAD, per cycle-4 evidence).
  - Phase48 suite 26/26 GREEN — no regression of the cycle-1..4 gates (boot-manifest / WasmJsonSerialization / WasmSynchronousExecution / WasmStdlibEmbed).
  - `AssemblyReferenceScanTests` skip-clean on Desktop (pre-existing `[FlowTargetFact("Web")]` gating, unchanged).
  - **Bundle size** (test-regenerated 48-BUNDLE-SIZE.md): 5,380,897 bytes (5.13 MB) uncompressed / 1,630,070 bytes (1.55 MB) Brotli → D-48-05 MONOLITHIC SHIP — NEUTRAL vs cycle-4's 5.13 MB / 1.55 MB (build-gating change adds no bytes, as predicted).

- **files_changed:**
  - `flow-lang/flow-lang.csproj` (added `<WasmBuildOnlyAfterPublish>true</WasmBuildOnlyAfterPublish>` + comment)

- **STILL REQUIRED to fully close Plan 48-06 (cannot be automated):** the SAME human real-browser re-smoke as cycle 4 — `dotnet publish -p:FlowTarget=Web -c Release`, serve `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`, load `index.html` in Chrome/Firefox, click Run, exercise a BROAD surface (print/add, a `createSineTone`+`play` tone, an inline note stream, `writeWav`, `writeMidi`, a collections op). Expect NO `Function not found` / NO `cancel` / NO `JsonSerializerIsReflectionDisabled` / NO boot 404, and an AUDIBLE 440 Hz tone. This cycle changes only build-vs-publish gating — it does not alter any runtime/browser behavior — so the cycle-4 re-smoke checklist is unchanged; status stays `fix_implemented_pending_browser_uat`.


---

## Resolution — SIXTH DEFECT (stripped-impl `internal proc` surface fails the whole Web import — `micBuffer`/`loadSfz`), same Plan 48-06 gate

- **root_cause:** Flow's builtin CALLABLE SURFACE is declared as `internal proc` in the stdlib `.flow` modules, but some of those builtins' C# IMPLEMENTATIONS are STRIPPED at compile time on the Web target (csproj `<Compile Remove>` per Phase 47 D-47-03). Specifically `audio.flow:547-548` declares `internal proc micBuffer(Second|Double)` whose impl (`StandardLibrary/Audio/InputFunctions.cs`, Phase 38 AUDIO-IN) is stripped, and `std.flow:385-386` declares `internal proc loadSfz(Symbol|String)` whose impl (`StandardLibrary/Audio/Sfz/**`) is stripped. At `Interpreter.cs ExecuteProcDeclaration` an internal proc with no matching C# impl hit a hard `_errorReporter.ReportError("No C# implementation found for internal proc '<name>' ...")`. Error accumulation (ErrorReporter design) then made `ModuleLoader` FAIL THE WHOLE import — so `use "@audio"` failed (`[eval] Failed to import '@audio'`), which took createSineTone/play/writeWav/etc. down with it, and the ctor `@std` bootstrap (cycle-4 fix) was dirtied by the loadSfz overloads. Same 'Desktop ≠ browser/WASM' defect class as the prior five: on Desktop InputFunctions.cs / Sfz/** are present, so the surface binds and the in-process tests never exercised the stripped-impl path.

- **sweep (ALL embedded-surface / stripped-impl pairs, not just micBuffer):** Cross-referenced the csproj Compile Remove list (PulseAudio*/CoreAudio/Sfz/**/OscFunctions/OscHandleData/InputFunctions) against the `internal proc` declarations in the 13 EMBEDDED `.flow` modules. Exactly TWO pairs exist: `micBuffer` (audio.flow, impl InputFunctions.cs) and `loadSfz` (std.flow, impl Sfz/**). The OSC surface (`oscSend`/`oscListen`/... + `__enableOscModule`/`__enableSfzModule`) lives in `sfz.flow`/`osc.flow`, which are NOT embedded (opt-in `@sfz`/`@osc`, rejected by the ModuleLoader Web strip-gate before any read) → not auto-loaded, not affected. The fix is generic (branches on "no impl found" regardless of name), so it covers both pairs and any future one.

- **fix (charitable interpretation — no checkpoint; aligned with the project's charitable-interpretation philosophy AND Phase 47's established "charitable advisory at import-time for stripped features" pattern):** Single edit at `flow-lang/Interpreter/Interpreter.cs ExecuteProcDeclaration` — inserted an `else if (FlowLang.Core.FlowEngine.IsWebTarget)` branch BEFORE the existing hard-error `else`. On the Web target it SKIPS declaring the missing-impl overload and emits a one-shot `RenderingDiagnostics.WarnOnce` advisory keyed `target:stripped-builtin:<name>` (fires at most once per proc-name per process): `[target] builtin '<name>' unavailable on Web target — surface declared in stdlib but implementation stripped (Phase 47). Skipping; calls will report 'function not found'.` Gated on the COMPILE-TIME `FlowEngine.IsWebTarget` const (Phase 47 D-47-10), NOT a runtime OS check, so under FlowTarget=Desktop the branch is unreachable and the hard `_errorReporter.ReportError("No C# implementation found ...")` STILL fires (a missing impl on Desktop is a genuine bug — explicitly NOT masked). Used the same WarnOnce infra Phase 47 ModuleLoader uses. Effect: on Web `@audio` imports successfully (micBuffer skipped → createSineTone/play/writeWav/renderSong/transpose/harmony all bind), `@std` bootstraps cleanly (loadSfz skipped), and calling micBuffer/loadSfz in-browser yields a normal "function not found" (acceptable — stripped by design). Desktop unchanged.

- **web-path verification (a green Desktop in-process test is INSUFFICIENT — IsWebTarget is false in the Desktop runner; the recurring blind spot):**
  - Built a Web-flavored HOST-LOADABLE PE (`-p:FlowTarget=Web -p:WasmGenerateAppBundle=false -p:IsBrowserWasmProject=false -p:RuntimeIdentifier=` → FLOW_WEB defines + InputFunctions/Sfz stripped + 13 embedded `.flow`, but a normal loadable PE) and a reflection harness whose output dir has ZERO on-disk `.flow` (simulates the Emscripten no-filesystem VFS → forces the embedded fallback AND makes IsWebTarget true in-process). HARNESS = ALL PASS: `@std` ctor bootstrap CLEAN (loadSfz charitably skipped, advisory once); `use "@audio"` IMPORTS (micBuffer skipped) and createSineTone + play RESOLVE; `(micBuffer 0.5)` degrades to a normal `Function 'micBuffer' not found` while `@audio` still imports. BROAD re-probe matching the human re-smoke list: print/add, collections (map+len), note-stream → renderSong("piano") → writeWav (wrote a 352,844-byte WAV from the embedded surface), writeMidi(String, Song) surface RESOLVES, transpose+chordNotes.
  - NEW `flow-lang.Tests/Integration/Phase48/WasmStrippedBuiltinSurfaceTests.cs` — 5 `[FlowTargetFact("Web")]` Facts (mirrors Phase 47 WebTargetModuleLoaderTests' charitable-advisory assertions). Under `dotnet test -p:FlowTarget=Web` (FLOW_WEB → IsWebTarget true in-process, the charitable branch ACTUALLY executes) → 5/5 PASS. On the Desktop runner → 5/5 SKIP (Desktop behavior provably unchanged).
  - `dotnet build -p:FlowTarget=Desktop` exit 0; Desktop `(add 1 2)`→3 smoke OK; hard "No C# implementation found" ReportError path intact (reachable on Desktop). `dotnet build -p:FlowTarget=Web` exit 0. `dotnet publish -p:FlowTarget=Web -c Release` exit 0, ZERO IL2026/IL3050; `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted; shipped Webcil `flow-lang.wasm` 917,269 → 917,781 bytes (+512 bytes interpreter logic only); all 13 `FlowLang.Stdlib.*` embedded resources present (Cecil count 13 in the trimmed linked PE).
  - Phase48 suite 26/26 GREEN (cycles 1-5 gates intact) + 5 new Web-Facts SKIP on Desktop. Phase47 suite 9 passed / 8 skipped / 0 failed (`WebBuild_ExitCodeIsZero` stays GREEN). AssemblyReferenceScanTests skip-clean on Desktop. Bundle size NEUTRAL (interpreter-logic-only change) → D-48-05 MONOLITHIC SHIP.

- **files_changed:**
  - `flow-lang/Interpreter/Interpreter.cs` (charitable Web-target branch in ExecuteProcDeclaration)
  - `flow-lang.Tests/Integration/Phase48/WasmStrippedBuiltinSurfaceTests.cs` (new — 5 `[FlowTargetFact("Web")]` regression Facts)

- **STILL REQUIRED to fully close Plan 48-06 (cannot be automated):** real-browser re-smoke — `dotnet publish -p:FlowTarget=Web -c Release`, serve `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`, load `index.html` in Chrome/Firefox, click Run, exercise `use "@audio"` + `(play (createSineTone 440Hz 1.0 0.5))` (expect AUDIBLE 440 Hz tone) PLUS an inline note stream + `renderSong`→`writeWav` + `writeMidi` to shake out any remaining surface. Expect NO `No C# implementation found` / NO `Failed to import '@audio'` / NO `Function not found` for createSineTone/play / NO `cancel` / NO `JsonSerializerIsReflectionDisabled` / NO boot 404.

---

## Resolution — SEVENTH DEFECT (single-threaded-WASM Task.Run+Wait DEADLOCK in the PLAYBACK path — `WebAudioBackend.Play` 30s freeze), same Plan 48-06 gate

- **classification:** The SAME single-threaded-WASM `Task.Run(...)` + `.Wait(30s)` deadlock class cycle 3 fixed in `WasmEntry.RunFromJs` (commit a8c1911) for the Execute path — but cycle 3 only fixed Execute; this fix extends the identical synchronous treatment to the PLAYBACK path it missed. No new checkpoint: covered by the human's cycle-3 approval of "run synchronously, best-effort cap."

- **root_cause:** `WebAudioBackend.Play` (old lines 150-181) wrapped the `FlowRuntimeInterop.PlayStereoFloat32(...)` [JSImport] marshal in `var workerTask = Task.Run(() => { ...marshal... }, cancellationToken);` then enforced a 30s cap via `if (!workerTask.Wait(PlayTimeout, cancellationToken))` (`PlayTimeout = TimeSpan.FromSeconds(30)`, old line 52). Mono-WASM is SINGLE-THREADED by default: `Task.Run` queues the marshal to the one browser main thread and `workerTask.Wait(30s)` then BLOCKS that same thread, so the marshal NEVER runs → the page freezes ~30s → the `wasm-30s-cap` WarnOnce advisory (`[runtime] audio playback exceeded 30s cap`) fires + an `OperationCanceledException` is thrown + NO `AudioBufferSourceNode` is ever created → no audio. A blocking wall-clock cap is fundamentally unenforceable in a single-threaded runtime (no preemption); worse, the `PlayStereoFloat32` marshal is fire-and-forget anyway (it builds an `AudioBufferSourceNode`, calls `.start()`, and returns immediately — WebAudio plays asynchronously), so wrapping it in a 30s-capped worker was never meaningful in the browser. Same 'Desktop != browser/WASM' defect class as the prior six: the Desktop in-process tests gave `Task.Run` a real worker thread AND the Play path is browser-gated (`OperatingSystem.IsBrowser()`), so they never exercised the single-threaded marshal deadlock.

- **sweep (every browser-reachable `Task.Run` + `.Wait` deadlock instance):** `WebAudioBackend.Play` was the ONLY one. `StandardLibrary/Audio/PlaybackFunctions.cs:104/116/163/211` have 4 `Task.Run(...)` calls but they are FIRE-AND-FORGET (return `Value.Void()` immediately; no `.Wait()` blocks the calling thread) — NOT the deadlock pattern; and the `play` builtin the 48-06 smoke uses routes through `PlayBuffer`→`PlaySamples`→`backend.Play` SYNCHRONOUSLY (no Task.Run), so the Play fix makes it deadlock-free. (loop/stream/preview on Web is the separable v1.6 AudioWorklet-streaming path per D-48-02 — out of 48-06 scope.) `Audio/CoreAudioBackend.cs:352` `_bufferAvailable.Wait(50ms)` is a Desktop-only backend (never picked in-browser) and was explicitly NOT touched. `WasmEntry.cs:148/151/273/274` are cycle-3 DOC COMMENTS, not live code. `FlowRuntimeInterop.cs` is pure [JSImport] binding declarations.

- **fix (human-approved cycle-3 'run synchronously, cap best-effort' — extended to the playback path; no checkpoint):** In `flow-lang/Audio/WebAudioBackend.cs Play`, REMOVED the `Task.Run(...)` wrapper + the `workerTask.Wait(PlayTimeout, cancellationToken)` block + the `WarnOnce("wasm-30s-cap", ...)` advisory + the `throw new OperationCanceledException(...)`. The `PlayStereoFloat32` marshal now runs SYNCHRONOUSLY on the calling thread inside the existing `#pragma warning disable CA1416` Pattern B guard: `_activeSource = FlowRuntimeInterop.PlayStereoFloat32(_audioContext, samplesAsBytes, channels: 2, sampleRate);`. Removed the now-unused `PlayTimeout` const and the now-unused `using FlowLang.Diagnostics;`. PRESERVED: the `samples`/`_disposed` early returns, the D-48-11 `if (!OperatingSystem.IsBrowser()) return;` Desktop charitable no-op, `EnsureInitialized` + null-context guard, the `cancellationToken.IsCancellationRequested` early return, D-48-07 `PromoteToStereo` BEFORE the marshal, the `MemoryMarshal.AsBytes` zero-copy reinterpret, and the Phase 47 D-47-05 PINNED `IAudioBackend.Play(float[], int, int, CancellationToken)` signature (the `cancellationToken` parameter stays — it simply no longer caps a blocking wait). Updated the class XML-doc D-48-10 bullet to state the 30s cap is best-effort / non-enforceable in single-threaded WASM and the marshal runs synchronously (consistent with cycle 3's a8c1911 amendment); the `wasm-30s-cap` sentinel no longer fires on the happy path.

- **D-48-10 AMENDMENT (extends cycle-3's amendment to the playback path — flagged for the closer):** D-48-10 should be recorded by the Plan 48-07 closer / 48-VERIFICATION.md as **"hard cap on Desktop; best-effort (synchronous, non-preemptive) in single-threaded WASM — for BOTH the Execute path (WasmEntry.RunFromJs, cycle 3) and the playback path (WebAudioBackend.Play, cycle 7)."** This agent did NOT edit ROADMAP/REQUIREMENTS/CLAUDE — that is the closer's job; the amendment is documented in-code (WebAudioBackend class XML doc D-48-10 bullet + inline Play comment) and here.

- **regression net (browser-free source-level guard — honestly a PROXY; the [JSImport] Play path is browser-only):** `flow-lang.Tests/Integration/Phase48/WebAudioBackendSyncPlayTests.cs` (NEW, 3 plain `[Fact]`, mirrors `CultureInvariantSweepTests`' FindRepoRoot + File.ReadAllText source-grep pattern). After stripping C# comments (so the cycle-7 amendment doc comments that describe the removed pattern don't trip the guard): `Play_DoesNotWrapMarshalInTaskRun_NoDeadlock` (no `Task.Run`/`workerTask`/`.Wait(` in executable source), `PlayTimeout_ConstAndCapThrow_AreRemoved` (no `PlayTimeout`/`wasm-30s-cap`/`OperationCanceledException`), `Play_CallsPlayStereoFloat32Synchronously_AssignsActiveSource` (the synchronous `_activeSource = FlowRuntimeInterop.PlayStereoFloat32` call + PromoteToStereo + MemoryMarshal.AsBytes + the OperatingSystem.IsBrowser() gate preserved). 3/3 PASS. The existing `WebAudioBackendIntegrationTests` (8 Desktop-side Facts) stay 8/8 GREEN — the synchronous change did not break PromoteToStereo / Dispose no-op / IsAvailable=false / Initialize charitable-false / WriteChunk-throws (Play is browser-gated, charitably no-ops on Desktop). The DECISIVE confirmation remains the human browser re-smoke (audible 440 Hz tone) — stated honestly: the actual synthesis→[JSImport]→WebAudio output is browser-only and cannot be auto-tested.

- **verification (automated — green build alone NOT treated as sufficient):**
  - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exit 0 (only pre-existing warnings; Desktop default intact).
  - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exit 0 (only pre-existing CA1416).
  - `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` exit 0 with ZERO IL2026/IL3050/IL2075; `AppBundle/_framework/dotnet.boot.js` (5561 bytes) still emitted; `flow-runtime.js` import `./_framework/dotnet.js` intact; shipped Webcil `flow-lang.wasm` 917,781 → 916,757 bytes (1 KB SMALLER — removed the Task.Run/Wait wrapper); all 13 `FlowLang.Stdlib.*` embedded resources intact (cycles 1-6 all preserved).
  - Phase47 suite 9 passed / 8 skipped / 0 failed (`WebBuild_ExitCodeIsZero` stays GREEN).
  - Phase48 suite 29 passed / 5 skipped / 0 failed (26 prior gates + 3 new cycle-7 source-guard Facts; the 5 `[FlowTargetFact("Web")]` Facts SKIP on Desktop).
  - `WebAudioBackendIntegrationTests` 8/8 GREEN (Desktop-side contracts preserved).
  - `AssemblyReferenceScanTests` skip-clean on Desktop (pre-existing `[FlowTargetFact("Web")]` gating, unchanged).
  - **Bundle size:** AppBundle `_framework/` = 5,379,873 bytes (5.13 MB) uncompressed → D-48-05 MONOLITHIC SHIP — NEUTRAL vs cycle 6 (backend-logic-only change removed bytes, added none).

- **files_changed:**
  - `flow-lang/Audio/WebAudioBackend.cs` (removed Task.Run+Wait deadlock wrapper + PlayTimeout const + 30s-cap WarnOnce/throw + unused FlowLang.Diagnostics using; synchronous PlayStereoFloat32 marshal; D-48-10 doc amendment)
  - `flow-lang.Tests/Integration/Phase48/WebAudioBackendSyncPlayTests.cs` (new — 3 source-level regression Facts)

- **STILL REQUIRED to fully close Plan 48-06 (cannot be automated):** real-browser re-smoke — `dotnet publish -p:FlowTarget=Web -c Release`, serve `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`, load `index.html` in Chrome/Firefox, click Run, exercise `use "@audio"` + `(play (createSineTone 440Hz 1.0 0.5))` (expect an AUDIBLE 440 Hz tone, NO ~30s freeze, NO `exceeded 30s cap` error) PLUS an inline note stream + `renderSong`→`writeWav` + `writeMidi`. Expect NO `exceeded 30s cap` / NO `No C# implementation found` / NO `Failed to import '@audio'` / NO `Function not found` / NO `cancel` / NO `JsonSerializerIsReflectionDisabled` / NO boot 404. The audible tone validates the full synthesis→[JSImport]→WebAudio chain and closes Plan 48-06.
