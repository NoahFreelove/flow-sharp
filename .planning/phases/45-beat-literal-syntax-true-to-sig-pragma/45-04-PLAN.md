---
phase: 45-beat-literal-syntax-true-to-sig-pragma
plan: 04
type: execute
wave: 3
depends_on: ["45-02", "45-03"]
files_modified:
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs
  - tests/test_beat_literal.flow
  - tests/test_beat_pragma_off.flow
  - tests/test_beat_pragma_on.flow
autonomous: true
requirements:
  - REQ-BEAT-AST-04
  - REQ-BEAT-TEST-01
  - REQ-BEAT-TEST-02
  - REQ-BEAT-TEST-03

must_haves:
  truths:
    - "ExpressionEvaluator dispatches BeatLiteralExpression to a new EvaluateBeatLiteral method (NOT the default NotSupportedException arm)."
    - "Multiplier formula `final = pragma_on ? raw × (4.0 / denom) : raw` is correct across all (timesig × pragma) combinations enumerated in Signal 4."
    - "Pragma-off mode is identity in every timesig (no surprise behavior for default scripts)."
    - "Pragma-on mode in default 4/4 (or no active timesig) is also identity (multiplier = 4/4 = 1.0) — D-02 prevents corruption of scripts that never set a timesig."
    - "Pragma-on mode in 6/8 produces multiplier 0.5; in 2/2 produces multiplier 2.0; in 7/8 produces multiplier 0.5; in 5/4 produces multiplier 1.0."
    - "Three composer-facing .flow smoke scripts run end-to-end without errors (test_beat_literal.flow / test_beat_pragma_off.flow / test_beat_pragma_on.flow)."
    - "Honors CONTEXT.md decisions D-02: (eval-time TimeSig lookup with 4/4 fallback identity), D-10: (ExpressionEvaluator switch arm for BeatLiteralExpression), and D-11: (two-track xUnit Facts + .flow smoke scripts per Phase 44 D-14 precedent)."
  artifacts:
    - path: flow-lang/Interpreter/ExpressionEvaluator.cs
      provides: "EvaluateBeatLiteral method + switch arm"
      contains: "EvaluateBeatLiteral"
    - path: tests/test_beat_literal.flow
      provides: "Lexer+parser+eval smoke for Nb literal"
      contains: "0.5b"
    - path: tests/test_beat_pragma_off.flow
      provides: "Identity-mode verification across 4/4, 6/8, 2/2"
      contains: "1b"
    - path: tests/test_beat_pragma_on.flow
      provides: "Multiplier matrix across 4/4, 6/8, 2/2, 5/4, 7/8"
      contains: "enable beat-true-to-sig;"
  key_links:
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Ast/Expressions/BeatLiteralExpression.cs
      via: "switch arm dispatches BeatLiteralExpression"
      pattern: "BeatLiteralExpression\\s+beatLit"
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Runtime/ExecutionContext.cs
      via: "_context.BeatTrueToSig read in EvaluateBeatLiteral"
      pattern: "_context\\.BeatTrueToSig"
---

<objective>
Wave 3 — close the literal-form half of Phase 45 by adding the `ExpressionEvaluator.EvaluateBeatLiteral` switch arm + multiplier formula. With this plan landed, `Nb` literals become end-to-end functional: lex (Plan 45-01) → parse (Plan 45-02) → pragma plumbing (Plan 45-03) → evaluate-with-multiplier (this plan). The `(beat N)` constructor migration lands separately in Plan 45-05 because it uses a different registration mechanism (`RegisterContextDependent`).

Purpose: This is the wave where composer-facing behavior becomes observable. The three `.flow` smoke scripts ship here so the rest of Phase 45 has end-to-end coverage. Plan 45-05 then adds the matching constructor path; Plan 45-06 adds the cross-file fixture + tutorials + doc updates.

Output:
- `EvaluateBeatLiteral` method in `ExpressionEvaluator.cs` implementing the multiplier formula
- Switch arm dispatching `BeatLiteralExpression` → `EvaluateBeatLiteral`
- Multiplier Theory grid (~12 cases from Signal 4) in `BeatTrueToSigPragmaTests.cs`
- 3 `.flow` smoke scripts under `tests/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-CONTEXT.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-RESEARCH.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-PATTERNS.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-02-PLAN.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-03-PLAN.md
@flow-lang/Interpreter/ExpressionEvaluator.cs
@flow-lang/Runtime/ExecutionContext.cs
@flow-lang/Runtime/MusicalContext.cs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From flow-lang/Interpreter/ExpressionEvaluator.cs (lines 35-58 — Evaluate switch dispatch):
```csharp
public virtual Value Evaluate(Expression expr)
{
    return expr switch
    {
        LiteralExpression lit => EvaluateLiteral(lit),
        VariableExpression var => EvaluateVariable(var),
        // ... ArrayLiteralExpression, TupleLiteralExpression, ChordLiteralExpression ...
        SymbolLiteralExpression symLit => EvaluateSymbolLiteral(symLit),
        LambdaExpression lambda => EvaluateLambda(lambda),
        // ...
        _ => throw new NotSupportedException(...)
    };
}
```

From flow-lang/Runtime/ExecutionContext.cs (the runtime accessor for active musical context — three-tier fallback):
- `_context.GetMusicalContext()` returns the resolved `MusicalContext` instance (call-stack → FlowConfig → default 4/4 + 120 BPM).
- `_context.GetMusicalContext().TimeSignature` is `TimeSignatureData?` — defaults to 4/4 via `ParseTimesigOrDefault(FlowConfig.Active.DefaultTimesig)` at ExecutionContext.cs:852.

From flow-lang/Runtime/MusicalContext.cs (TimeSignatureData shape):
- `TimeSignatureData.Numerator` (int, e.g., 6 for 6/8).
- `TimeSignatureData.Denominator` (int, e.g., 8 for 6/8).

Phase 45 multiplier formula (D-10): `multiplier = pragma_on ? (4.0 / denom) : 1.0; return Value.Beat(rawValue * multiplier);`.

Value constructor: `Value.Beat(double)` → returns Beat-typed Value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EvaluateBeatLiteral switch arm + multiplier method + Theory grid</name>
  <files>
    flow-lang/Interpreter/ExpressionEvaluator.cs,
    flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs
  </files>
  <read_first>
    flow-lang/Interpreter/ExpressionEvaluator.cs lines 35-58 (Evaluate switch — find the SymbolLiteralExpression arm at line 46 as the insertion anchor),
    flow-lang/Interpreter/ExpressionEvaluator.cs lines 75-150 (TryParseSpecialLiteral for the existing sibling literals — Phase 45 EXPLICITLY DOES NOT use this path; instead reads the AST node's RawValue directly),
    flow-lang/Runtime/ExecutionContext.cs lines 790-862 (GetMusicalContext three-tier resolution),
    flow-lang/Runtime/MusicalContext.cs (TimeSignatureData shape),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-PATTERNS.md §"flow-lang/Interpreter/ExpressionEvaluator.cs" (verbatim arm + method block),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-RESEARCH.md §Signal 4 (the 12-row multiplier Theory grid)
  </read_first>
  <behavior>
    - Test 1: `MultiplierFormula_PragmaOff_Identity` (Theory) — 4/4 / 6/8 / 2/2 each with `1b` produces `Value.Beat(1.0)` when pragma OFF.
    - Test 2: `MultiplierFormula_PragmaOn_4Over4` — `1b` in 4/4 with pragma on produces `Value.Beat(1.0)` (identity, denom=4 → multiplier=1.0).
    - Test 3: `MultiplierFormula_PragmaOn_6Over8` (Theory) — `1b → 0.5`, `2b → 1.0`, `0.5b → 0.25`.
    - Test 4: `MultiplierFormula_PragmaOn_2Over2` (Theory) — `1b → 2.0`, `0.5b → 1.0`.
    - Test 5: `MultiplierFormula_PragmaOn_5Over4` — `1b → 1.0` (identity).
    - Test 6: `MultiplierFormula_PragmaOn_7Over8` — `1b → 0.5`.
    - Test 7: `MultiplierFormula_NegativePassthrough` — pragma on in 4/4, `-2b` produces `Value.Beat(-2.0)`.
    - Test 8: `MultiplierFormula_NoActiveTimesig` — pragma on, no active timesig (default 4/4), `1b` produces `Value.Beat(1.0)` (Pitfall 4 — D-02 identity-in-default behavior).
  </behavior>
  <action>
    Step A — `flow-lang/Interpreter/ExpressionEvaluator.cs`. Add a new switch arm IMMEDIATELY AFTER the `SymbolLiteralExpression` arm at line 46, BEFORE `LambdaExpression` arm at line 47 (per D-10 / REQ-BEAT-AST-04):
    ```csharp
    BeatLiteralExpression beatLit => EvaluateBeatLiteral(beatLit),
    ```
    Step B — add the new private method `EvaluateBeatLiteral(BeatLiteralExpression beatLit)` in the same file. Place it near the other `Evaluate*Literal` helper methods (e.g., adjacent to `EvaluateSymbolLiteral`). Implementation per D-10 / REQ-BEAT-AST-04 (verbatim from 45-PATTERNS.md):
    ```csharp
    private Value EvaluateBeatLiteral(BeatLiteralExpression beatLit)
    {
        // Phase 45 D-10 multiplier formula:
        //   final = pragma_on ? raw × (4.0 / denom) : raw
        // GetMusicalContext() returns three-tier-fallback resolved instance;
        // TimeSignature defaults to 4/4 via FlowConfig.Active.DefaultTimesig.
        int denom = _context.GetMusicalContext().TimeSignature?.Denominator ?? 4;
        double multiplier = _context.BeatTrueToSig ? (4.0 / denom) : 1.0;
        return Value.Beat(beatLit.RawValue * multiplier);
    }
    ```
    Use the verbatim xmldoc-cross-ref form from 45-PATTERNS.md.

    Step C — extend `flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs` with the multiplier matrix Facts/Theory. Two approaches per case:
    - **End-to-end via FlowEngine.Execute** (preferred for cross-cutting verification): build a source string like `"enable beat-true-to-sig;\ntimesig 6/8 { Beat b = 1b; (print (str b)) }"`, capture stdout, assert the printed quarter value (e.g., "0.5"). Mirror `Phase44.ExecutionContextStrictModeTests` and Plan 45-03's Task 1 helpers.
    - **Direct evaluator** (for unit isolation): construct `BeatLiteralExpression(loc, raw)` manually, set `ctx.BeatTrueToSig` + push a `MusicalContext` frame with the desired denominator via `_context.PushBlockContext` (or whatever public API exists; otherwise use FlowEngine path).

    Recommended: use the FlowEngine.Execute path consistently — it exercises lex+parse+eval together and matches the Phase 44 strict-mode test style. For floating-point assertions, capture stdout with `(print (str b))` and use `Assert.Equal("0.5", stdout.Trim())` (Flow's `(str Beat)` emits the plain double per D-14 — round-trip is intentionally unsuffixed). For cases where exact double comparison is brittle (e.g., 5/4 → 0.8), capture as numeric and assert via `Math.Abs(actual - expected) &lt; 1e-9` in the xUnit assertion rather than fabricating a Flow `(round)` helper. Do NOT introduce a `(round value digits)` builtin in Phase 45 — out of scope.

    Cover all 8 behaviors enumerated above. Use `[Theory]` + `[InlineData(...)]` for the timesig × raw matrix. Use `[Fact]` for the standalone cases.

    NEVER use `Bash(cat &lt;&lt; 'EOF')` for file creation — use the Write/Edit tool.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang/ flow-lang.Tests/ 2>&amp;1 | tail -10 &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase45.BeatTrueToSigPragmaTests.MultiplierFormula" --no-restore 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All 8 multiplier Facts/Theory cases PASS GREEN.
    - `grep -c "EvaluateBeatLiteral" flow-lang/Interpreter/ExpressionEvaluator.cs` returns ≥ 2 (switch arm + method declaration).
    - `grep -n "_context.BeatTrueToSig" flow-lang/Interpreter/ExpressionEvaluator.cs` returns exactly 1 line.
    - Existing 10 Facts from Plan 45-03 + 20 Facts from Plans 45-01/45-02 still GREEN.
    - Zero regression to `Phase44.*` and `Phase35.*` evaluator tests.
  </acceptance_criteria>
  <done>EvaluateBeatLiteral implemented; multiplier formula proven correct across the full (timesig × pragma) matrix; ~38 Phase 45 xUnit Facts now GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Composer-facing .flow smoke scripts (3 of 4)</name>
  <files>
    tests/test_beat_literal.flow,
    tests/test_beat_pragma_off.flow,
    tests/test_beat_pragma_on.flow
  </files>
  <read_first>
    tests/test_pragma_isolation.flow + tests/test_pragma_isolation_module.flow (Phase 21 pragma cross-file pair — convention for `(print "...PASSED")` end-markers),
    tests/test_strict_axis_a_overload.flow (Phase 44 — pragma-on smoke convention),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-PATTERNS.md §"tests/test_beat_*.flow" (smoke test convention),
    flow-lang/audio.flow (for the `(str)`/(`print`) helpers and `secToBeat`/`beatToSec` reverse-conversion functions to verify multiplier output)
  </read_first>
  <action>
    Step A — `tests/test_beat_literal.flow` (REQ-BEAT-TEST-01) — lexer + parser + eval smoke for `Nb` literal. ~30-40 lines. Cover:
    - Assignment: `Beat b1 = 0.5b`
    - Function arg: print via `(print (str b1))` — expect "0.5"
    - Positive integer: `Beat b2 = 2b`; print expects "2"
    - Signed positive: `Beat b3 = +1b`; print expects "1"
    - Signed negative: `Beat b4 = -2b`; print expects "-2"
    - Arithmetic: `Double sum = (add 0.5b 0.5b)`; print expects "1"  (note: arithmetic between two Beats returns Double via the existing add convention since BeatType.IsCompatibleWith(Double); confirm by reading audio.flow / BeatType)
    - Tuple key: `Dict<<Note, Beat>, Int> d = (dict <<C4, 0.5b>> 100)` — verify Dict construction succeeds
    - End marker: `(print "test_beat_literal: PASSED")`
    No `enable beat-true-to-sig;` — this is identity-mode lex+parse+eval coverage only.

    Step B — `tests/test_beat_pragma_off.flow` (REQ-BEAT-TEST-02) — default identity behavior across multiple timesigs. ~30-40 lines. NO `enable beat-true-to-sig;`. Cover three timesig blocks:
    - `tempo 120 { timesig 4/4 { Beat a = 1b; (print (str a)) } }` — expect "1"
    - `tempo 120 { timesig 6/8 { Beat b = 1b; (print (str b)) } }` — expect "1" (pragma-off → identity even in 6/8)
    - `tempo 120 { timesig 2/2 { Beat c = 1b; (print (str c)) } }` — expect "1"
    - End marker: `(print "test_beat_pragma_off: PASSED")`

    Step C — `tests/test_beat_pragma_on.flow` (REQ-BEAT-TEST-03) — opens with `enable beat-true-to-sig;`. Cover the multiplier matrix across the 5 timesigs from Signal 4. ~50-60 lines:
    - `timesig 4/4 { Beat a = 1b; (print (str a)) }` — expect "1" (identity, denom=4)
    - `timesig 6/8 { Beat b = 1b; (print (str b)) }` — expect "0.5"
    - `timesig 6/8 { Beat b2 = 2b; (print (str b2)) }` — expect "1"
    - `timesig 6/8 { Beat b3 = 0.5b; (print (str b3)) }` — expect "0.25"
    - `timesig 2/2 { Beat c = 1b; (print (str c)) }` — expect "2"
    - `timesig 2/2 { Beat c2 = 0.5b; (print (str c2)) }` — expect "1"
    - `timesig 5/4 { Beat d = 1b; (print (str d)) }` — expect "1"
    - `timesig 7/8 { Beat e = 1b; (print (str e)) }` — expect "0.5"
    - End marker: `(print "test_beat_pragma_on: PASSED")`

    `(test_beat_cross_file.flow` ships in Plan 45-06 — it requires the helper file machinery + cross-file integration which belongs in the closer.)

    NEVER use `Bash(cat &lt;&lt; 'EOF')` for file creation — use the Write tool.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; for t in tests/test_beat_literal.flow tests/test_beat_pragma_off.flow tests/test_beat_pragma_on.flow; do echo "=== $t ==="; dotnet run --project flow-interpreter "$t" 2>&amp;1 | tail -15; done</automated>
  </verify>
  <acceptance_criteria>
    - All 3 `.flow` smoke scripts exit 0.
    - Each prints `"test_beat_<name>: PASSED"` as the last line.
    - `test_beat_pragma_on.flow` stdout contains literal `"0.5"` and `"2"` substrings (proves multiplier applied; "0.5" comes from 6/8 row, "2" comes from 2/2 row).
    - `test_beat_pragma_off.flow` stdout does NOT contain `"0.5"` (proves identity in pragma-off mode even in 6/8).
    - No `[error]` / `[warn]` advisories printed to stderr beyond pre-existing baseline noise (e.g., no `[module]` re-shadow advisories from stdlib imports).
  </acceptance_criteria>
  <done>3 composer-facing smoke scripts pass end-to-end; multiplier behavior visible at the composer surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Interpreter-internal evaluator + composer-facing tests. Trust boundary unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | Denial of Service | Multiplier formula could NaN/Inf with pathological raw values (e.g., `1e308b` or denom=0) | mitigate | Denominator comes from `TimeSignatureData.Denominator` which is constrained to `{1, 2, 4, 8, 16, 32, 64, 128}` by the parser at construction time (TimeSignatureType / timesig block parsing). `4.0 / denom` ∈ `{4.0, 2.0, 1.0, 0.5, 0.25, 0.125, 0.0625, 0.03125}` — bounded. `?? 4` fallback prevents divide-by-zero. Extreme `raw` values still produce finite (or Inf) doubles; no exception path. |
| T-45-10 | Tampering | Composer .flow smoke scripts could include hostile code | accept | Tests live in repo under `tests/` — same trust posture as every other `.flow` test. The runner is `dotnet run --project flow-interpreter <path>` with no I/O sandbox beyond Flow's existing surface. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages. |

**No-threats rationale for omitted ASVS categories:** Pure interpreter switch-dispatch. No auth, session, access, crypto. V5 input validation: `_context.BeatTrueToSig` is a bool field (no string injection), `TimeSignature?.Denominator` comes from typed AST (no string parsing at eval time).
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~Phase45"` total Facts/Theory ≥ 38 GREEN.
- `for t in tests/test_beat_*.flow; do dotnet run --project flow-interpreter "$t" || exit 1; done` exits 0.
- `dotnet test --filter "FullyQualifiedName~Phase35.Match|FullyQualifiedName~Phase26.Tuple|FullyQualifiedName~Phase44.Overload"` — zero new failures.
</verification>

<success_criteria>
- `BeatLiteralExpression` flows through the evaluator with correct multiplier.
- 8 multiplier Facts/Theory cases GREEN.
- 3 composer-facing `.flow` smoke scripts exit 0 with PASSED markers.
- Pragma-off behavior identity-confirmed across 4/4 / 6/8 / 2/2.
- Pragma-on multiplier matrix correct across 4/4 / 6/8 / 2/2 / 5/4 / 7/8.
- Identity behavior in default 4/4 (Pitfall 4 D-02 compliance).
</success_criteria>

<output>
Create `.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-04-SUMMARY.md` documenting:
- ExpressionEvaluator switch arm + method placement
- Multiplier formula final form
- 8 Theory/Fact case names
- 3 .flow smoke script line counts + expected stdout shapes
- Pragma-off identity confirmation
- Pitfall 4 default-4/4 identity confirmation
- Commit SHA(s)
</output>
