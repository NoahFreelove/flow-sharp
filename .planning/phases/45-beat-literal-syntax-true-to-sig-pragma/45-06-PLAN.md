---
phase: 45-beat-literal-syntax-true-to-sig-pragma
plan: 06
type: execute
wave: 4
depends_on: ["45-04", "45-05"]
files_modified:
  - tests/test_beat_cross_file.flow
  - tests/test_beat_cross_file_helper.flow
  - examples/beat/intro.flow
  - examples/beat/cut-time.flow
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VERIFICATION.md
  - flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs
  - flow-lang.Tests/baselines/Phase45/intro.wav
  - flow-lang.Tests/baselines/Phase45/cut-time.wav
autonomous: true
requirements:
  - REQ-BEAT-TEST-04
  - REQ-BEAT-TEST-05
  - REQ-BEAT-TEST-06
  - REQ-BEAT-TEST-07
  - REQ-BEAT-DOC-01
  - REQ-BEAT-DOC-02
  - REQ-BEAT-DOC-03
  - REQ-BEAT-DOC-04

must_haves:
  truths:
    - "Cross-file boundary works: pragma-on entry file `use`-s pragma-off helper file containing `proc bumpBeat Beat b => (beat 1)`; calling `(bumpBeat (beat 0))` from the entry returns `Value.Beat(1.0)` (raw quarters) regardless of entry's 6/8 timesig context."
    - "`examples/beat/intro.flow` 6/8 jig tutorial runs to completion, renders MIDI + WAV, and prints expected output."
    - "`examples/beat/cut-time.flow` 2/2 cut-time tutorial runs to completion, renders MIDI + WAV, prints expected output."
    - "Two-run cmp-clean: running `intro.flow` twice produces byte-identical WAV (SHA-256 match)."
    - "Two-run cmp-clean: running `cut-time.flow` twice produces byte-identical WAV (SHA-256 match)."
    - "CLAUDE.md Music Types Quick Reference table has the Beat row replaced with the D-13 row; Music-Specific section gets a one-line pragma-family expansion mention."
    - "REQUIREMENTS.md has a new Phase 45 section with all 25 REQ-BEAT-NN entries marked [x] closed."
    - "ROADMAP.md Phase 45 entry shows 6 plans, all complete, with goal/requirements/plans-list reconciled."
    - "STATE.md updated: Phase 45 marked complete; v1.5 progress 8/12 phases."
    - "(str someBeat) emits plain double (no 'b' suffix) regardless of pragma mode — D-14 lock pinned by Facts."
    - "Honors CONTEXT.md decisions D-12: (two tutorial files examples/beat/intro.flow + cut-time.flow), D-13: (CLAUDE.md music-types table Beat row + pragma-family-expansion mention), D-15: (REPL :beat-true-to-sig toggle DEFERRED to one-commit follow-up — no implementation in Phase 45), D-16: (Phase 44 strict-mode interaction is documentary carry-forward, NOT a Phase 45 implementation task — strict Axis A already covers it), D-17: (dotted-rhythm Nb. syntax DEFERRED indefinitely — composers write 0.75b directly)."
  artifacts:
    - path: tests/test_beat_cross_file.flow
      provides: "Pragma-on entry; imports pragma-off helper; asserts Beat values flow as quarters"
      contains: "enable beat-true-to-sig;"
    - path: tests/test_beat_cross_file_helper.flow
      provides: "Pragma-off helper file with bumpBeat proc"
      contains: "proc bumpBeat"
    - path: examples/beat/intro.flow
      provides: "6/8 jig tutorial demonstrating with/without pragma; renders MIDI+WAV"
      contains: "enable beat-true-to-sig;"
      min_lines: 50
    - path: examples/beat/cut-time.flow
      provides: "2/2 cut-time tutorial demonstrating `1b = half`; renders MIDI+WAV"
      contains: "timesig 2/2"
      min_lines: 30
    - path: flow-lang.Tests/baselines/Phase45/intro.wav
      provides: "Reference rendering for intro.flow (two-run cmp-clean baseline)"
    - path: flow-lang.Tests/baselines/Phase45/cut-time.wav
      provides: "Reference rendering for cut-time.flow"
    - path: .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VERIFICATION.md
      provides: "Phase 45 closure verification with all 25 REQ-BEAT-NN evidence rows"
      min_lines: 100
  key_links:
    - from: tests/test_beat_cross_file.flow
      to: tests/test_beat_cross_file_helper.flow
      via: "use './test_beat_cross_file_helper.flow'"
      pattern: "use \"./test_beat_cross_file_helper.flow\""
    - from: CLAUDE.md
      to: examples/beat/intro.flow
      via: "Music Types Quick Reference table mentions Nb literal + beat-true-to-sig pragma"
      pattern: "0.5b.*Beat"
---

<objective>
Wave 4 closer — finish Phase 45 with the cross-file boundary test (Pitfall 3 verification at the composer-source level), the two composer-facing tutorial files in `examples/beat/`, committed audio baselines for two-run cmp-clean determinism, CLAUDE.md documentation update, REQUIREMENTS.md / ROADMAP.md / STATE.md tracking-file sweep, and the 45-VERIFICATION.md closure deliverable.

Purpose: This is the moment composers can write a 6/8 jig with `enable beat-true-to-sig;` and have it work end-to-end with the musician-intuition path. All 25 REQ-BEAT-NN requirements close here. Phase 45 closes; v1.5 progress moves to 8/12 phases (35 + 36 + 37 + 38 + 39 + 42 + 43 + 44 + 45).

Output:
- 1 cross-file test pair (entry + helper)
- 2 tutorial `.flow` files + 2 committed WAV baselines + `(str)` round-trip Facts
- CLAUDE.md edits (Music Types table + Music-Specific section)
- REQUIREMENTS.md Phase 45 section
- ROADMAP.md Phase 45 closure
- STATE.md v1.5 progress update
- 45-VERIFICATION.md closure deliverable
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-CONTEXT.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-RESEARCH.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-PATTERNS.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VALIDATION.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-01-PLAN.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-02-PLAN.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-03-PLAN.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-04-PLAN.md
@.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-05-PLAN.md
@examples/scala/intro.flow
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. -->

From examples/scala/intro.flow (tutorial header pattern for examples/beat/intro.flow):
```flow
enable justIntonation;

use "@std"
use "@audio"
use "@composition"

Note: ============================================================
Note:  Chapter: Scala Microtonal Tunings (Phase 32)
Note:  Run: dotnet run --project flow-interpreter examples/scala/intro.flow
Note: ============================================================
```

From CLAUDE.md "Music Types Quick Reference" table — the row to REPLACE (currently `1.5 (Beat-tagged)` is now MISLEADING post-Phase-45 since literal form exists):
```markdown
| `1.5` (Beat-tagged) | `Beat` | `Double`, `Float` | beat-position arithmetic |
```
becomes (D-13 verbatim):
```markdown
| `0.5b` (Beat literal) | `Beat` | `Double`, `Float` | beat-position arithmetic; `enable beat-true-to-sig;` opt-in retunes literal to active timesig's beat unit (default 4/4 → `1b = quarter`) |
```

From .planning/REQUIREMENTS.md (Phase 44 REQ-STRICT-NN section is the precedent shape — add a new Phase 45 section after Phase 44).
From .planning/ROADMAP.md lines 444-462 — Phase 45 placeholder block to be reconciled (Plans count 0 → 6; goal stays; requirements TBD → enumerated; Plans-list populated).

Phase 43/44 baseline location convention: `flow-lang.Tests/baselines/Phase{NN}/`. Phase 45 baselines go at `flow-lang.Tests/baselines/Phase45/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cross-file test pair + (str) round-trip Facts</name>
  <files>
    tests/test_beat_cross_file.flow,
    tests/test_beat_cross_file_helper.flow,
    flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs
  </files>
  <read_first>
    tests/test_pragma_isolation.flow + tests/test_pragma_isolation_module.flow (Phase 21 cross-file pair convention),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-PATTERNS.md §"tests/test_beat_*.flow" (cross-file pair pattern),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-RESEARCH.md Pitfall 3 (cross-file behavior — declaring file's pragma bit governs the proc's behavior) + §Signal 6 (str round-trip lock)
  </read_first>
  <action>
    Step A — create `tests/test_beat_cross_file_helper.flow` (REQ-BEAT-TEST-04 helper). NO pragma. Declare exported procs that construct Beat values via `(beat N)`:
    ```flow
    use "@std"
    use "@audio"

    proc bumpBeat Beat b
        return (beat 1)

    proc helperRawBeat
        return (beat 0.5)

    Note: This helper file does NOT enable beat-true-to-sig.
    Note: Per D-04, its (beat N) calls always produce raw-quarter Beat values,
    Note: regardless of whether the importer has the pragma enabled.

    (print "test_beat_cross_file_helper: loaded")
    ```

    Step B — create `tests/test_beat_cross_file.flow` (REQ-BEAT-TEST-04 entry). Pragma-on importer:
    ```flow
    enable beat-true-to-sig;

    use "@std"
    use "./test_beat_cross_file_helper.flow"

    Note: Entry file is pragma-on. Inside timesig 6/8 { }, 1b SHOULD multiply to 0.5.
    Note: But calling bumpBeat (declared in pragma-OFF helper file) MUST return Value.Beat(1.0)
    Note: — the helper's declaring file lacks the pragma, so its (beat 1) sees no multiplier.

    tempo 120 {
        timesig 6/8 {
            Beat localLit = 1b
            (print (concat "local 1b in 6/8 pragma-on = " (str localLit)))   Note: expects 0.5

            Beat fromHelper = (bumpBeat (beat 0))
            (print (concat "helper (beat 1) called from 6/8 pragma-on = " (str fromHelper)))   Note: expects 1.0 — pragma is file-scope per D-04
        }
    }

    (print "test_beat_cross_file: PASSED")
    ```

    Step C — extend `flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs` with the 4 `(str)` round-trip Facts per Signal 6 / D-14:
    - `StrEmitsPlainDoublePragmaOff` — `(str (beat 0.5))` → `"0.5"` (no `b` suffix).
    - `StrEmitsPlainDoublePragmaOn4Over4` — `(str (beat 0.5))` → `"0.5"`.
    - `StrEmitsQuarterValuePragmaOn6Over8` — under pragma-on 6/8, `(str (beat 1.0))` → `"0.5"` (multiplier applied at construction; str shows quarter value).
    - `StrEmitsQuarterValuePragmaOn2Over2` — under pragma-on 2/2, `(str (beat 0.5))` → `"1"` (multiplier 2.0 applied).

    Add `CrossFileSmokeFact` xUnit Fact that runs `dotnet run --project flow-interpreter tests/test_beat_cross_file.flow` via Process.Start, asserts exit 0, asserts stdout contains both `"local 1b in 6/8 pragma-on = 0.5"` AND `"helper (beat 1) called from 6/8 pragma-on = 1"` substrings + `"PASSED"` end marker.

    NEVER use `Bash(cat &lt;&lt; 'EOF')` for file creation — use the Write tool.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet run --project flow-interpreter tests/test_beat_cross_file.flow 2>&amp;1 | tail -10 &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase45.BeatTrueToSigPragmaTests.Str|FullyQualifiedName~Phase45.BeatTrueToSigPragmaTests.CrossFileSmoke" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_beat_cross_file.flow` exits 0 and prints `"test_beat_cross_file: PASSED"`.
    - stdout shows `"local 1b in 6/8 pragma-on = 0.5"` (literal sees multiplier) AND `"helper (beat 1) called from 6/8 pragma-on = 1"` (cross-file boundary preserves declaring file's pragma bit per D-04 / Pitfall 3).
    - 4 `(str)` Facts PASS GREEN.
    - `CrossFileSmokeFact` PASSES (exit 0 + PASSED marker).
  </acceptance_criteria>
  <done>Cross-file pragma boundary verified at both .flow and xUnit layers; (str) round-trip lock (D-14) pinned.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Composer tutorials + audio baselines + two-run cmp-clean Fact</name>
  <files>
    examples/beat/intro.flow,
    examples/beat/cut-time.flow,
    flow-lang.Tests/baselines/Phase45/intro.wav,
    flow-lang.Tests/baselines/Phase45/cut-time.wav,
    flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs
  </files>
  <read_first>
    examples/scala/intro.flow (tutorial chapter convention — header banner / chapter delimiters / writeMidi + writeWav at the end),
    examples/dsp/granular.flow (composer-facing DSP tutorial format),
    flow-lang.Tests/baselines/Phase28/ (audio baseline directory convention — if present),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-PATTERNS.md §"examples/beat/intro.flow + examples/beat/cut-time.flow",
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-CONTEXT.md D-12 (tutorial scope per file)
  </read_first>
  <action>
    Step A — create `examples/beat/intro.flow` (REQ-BEAT-DOC-03). 50-80 lines per D-12. Two chapters:
    1. **Chapter 1: Default (pragma-off) behavior in 6/8.** Show that `1b = quarter` even in 6/8 — composer must manually write `0.5b` to mean "one eighth". Render a short 8-bar 6/8 phrase using `(beat 0.5)` durations for eighths.
    2. **Chapter 2: With `enable beat-true-to-sig;`** — show the same phrase using `1b` for eighths (which now multiplies to 0.5 quarters under 6/8). Demonstrates the musician-intuition path.
    
    File top must use `enable beat-true-to-sig;` to enable the pragma. Use Phase 28 articulations + Phase 29 sampled piano via `renderSong song "piano"`. End with `writeMidi` + `writeWav` writing to `/tmp/beat_intro.mid` + `/tmp/beat_intro.wav` (NOT into the repo — keep test artifacts ephemeral). Print PASSED end marker.

    Step B — create `examples/beat/cut-time.flow` (REQ-BEAT-DOC-04). 30-50 lines. Single chapter showing `enable beat-true-to-sig;` + `timesig 2/2 {}` so `1b = half`. Render a short orchestral-feel march in cut time using `1b` durations for halves and `0.5b` durations for quarters. writeMidi + writeWav to `/tmp/beat_cut_time.mid` + `/tmp/beat_cut_time.wav`. PASSED end marker.

    Step C — generate audio baselines. Run each tutorial twice; on the second run, COPY the rendered WAV from `/tmp/` into `flow-lang.Tests/baselines/Phase45/intro.wav` and `flow-lang.Tests/baselines/Phase45/cut-time.wav`. These become the committed reference renders per Open Question 3 / 45-PATTERNS.md §"Two-Run Cmp-Clean Determinism" (Phase 28 precedent — committed because dither RNG is seeded deterministically). Tutorials cannot exit early with "rendering skipped" — they MUST produce a real WAV both times.

    Step D — extend `flow-lang.Tests/Integration/Phase45/BeatTrueToSigPragmaTests.cs` with the two-run cmp-clean Fact pair (REQ-BEAT-TEST-07):
    ```csharp
    [Fact]
    public void TutorialTwoRunCmpClean_Intro() { /* run examples/beat/intro.flow twice via Process.Start; SHA-256 the /tmp/beat_intro.wav from each run; assert equality. */ }

    [Fact]
    public void TutorialTwoRunCmpClean_CutTime() { /* mirror — examples/beat/cut-time.flow */ }

    [Fact]
    public void TutorialMatchesBaseline_Intro() { /* run examples/beat/intro.flow once; SHA-256 the result vs flow-lang.Tests/baselines/Phase45/intro.wav; assert equality. */ }

    [Fact]
    public void TutorialMatchesBaseline_CutTime() { /* mirror */ }
    ```
    Use the `RmsRegressionTests.AssertWavMatchesBaseline` helper if present (per CLAUDE.md "Conventions"); otherwise raw `SHA256.HashData` on file bytes is sufficient because no PRNG sites exist in Phase 45.

    NEVER use `Bash(cat &lt;&lt; 'EOF')` for file creation — use the Write tool.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet run --project flow-interpreter examples/beat/intro.flow 2>&amp;1 | tail -10 &amp;&amp; dotnet run --project flow-interpreter examples/beat/cut-time.flow 2>&amp;1 | tail -10 &amp;&amp; test -f flow-lang.Tests/baselines/Phase45/intro.wav &amp;&amp; test -f flow-lang.Tests/baselines/Phase45/cut-time.wav &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase45.BeatTrueToSigPragmaTests.Tutorial" --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `examples/beat/intro.flow` exits 0; produces `/tmp/beat_intro.wav` + `/tmp/beat_intro.mid`; prints PASSED.
    - `examples/beat/cut-time.flow` exits 0; produces `/tmp/beat_cut_time.wav` + `/tmp/beat_cut_time.mid`; prints PASSED.
    - `flow-lang.Tests/baselines/Phase45/intro.wav` exists and is a valid WAV (≥10 KB).
    - `flow-lang.Tests/baselines/Phase45/cut-time.wav` exists and is a valid WAV (≥10 KB).
    - 4 tutorial Facts PASS (two-run cmp-clean × 2 + baseline-match × 2).
    - No `[error]` or `[warn]` advisories beyond pre-existing stdlib advisory baseline.
  </acceptance_criteria>
  <done>Two tutorial chapters render musically; baselines committed; two-run cmp-clean preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CLAUDE.md + REQUIREMENTS.md + ROADMAP.md + STATE.md + 45-VERIFICATION.md tracking-file sweep</name>
  <files>
    CLAUDE.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md,
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VERIFICATION.md
  </files>
  <read_first>
    CLAUDE.md (Music Types Quick Reference table around line 99 + Music-Specific section),
    .planning/REQUIREMENTS.md (find the end of the Phase 44 REQ-STRICT-NN section — Phase 45 entries go after),
    .planning/ROADMAP.md lines 444-462 (Phase 45 placeholder block to reconcile),
    .planning/STATE.md (v1.5 phase map table + Phase 43 closure highlights pattern as the template for Phase 45 closure highlights),
    .planning/phases/43-module-names-qualified-imports/43-VERIFICATION.md (closure deliverable shape — header / per-REQ rows / known caveats / phase metrics),
    .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-CONTEXT.md D-13 (CLAUDE.md edit specifics)
  </read_first>
  <action>
    Step A — `CLAUDE.md` Music Types Quick Reference table edit (REQ-BEAT-DOC-01). REPLACE the existing row `| `1.5` (Beat-tagged) | `Beat` | `Double`, `Float` | beat-position arithmetic |` with the D-13 verbatim row:
    ```
    | `0.5b` (Beat literal) | `Beat` | `Double`, `Float` | beat-position arithmetic; `enable beat-true-to-sig;` opt-in retunes literal to active timesig's beat unit (default 4/4 → `1b = quarter`) |
    ```

    Step B — `CLAUDE.md` Music-Specific section pragma-family expansion mention (REQ-BEAT-DOC-02). Find the existing pragma list (e.g., the `enable hAsB;` / `enable justIntonation;` / etc. mentions). Add a one-line pragma family expansion note:
    > **Pragmas:** `hAsB`, `justIntonation`, `pythagorean`, `equalTemperament`, `scaleLint`, `matchExhaustive`, `strict`, `beat-true-to-sig` (Phase 45 — opt-in: `Nb` literals + `(beat N)` constructor calls multiply by `4/denominator` in active timesig).

    Adapt to the exact existing pragma documentation shape — if CLAUDE.md has no explicit pragma-list section, add it under "Language Features § Core" or "Music-Specific" section per D-13's "Adjacent CLAUDE.md 'Music-Specific' section gets a one-line addition" wording.

    Step C — `.planning/REQUIREMENTS.md` — append a new `### Beat Literal Syntax & True-to-Sig Pragma (Phase 45)` section AFTER the Phase 44 REQ-STRICT-NN section. Use the Phase 44 section header format. Enumerate all 25 REQ-BEAT-NN entries marked `[x]` with bullets matching the shape of REQ-STRICT-01..15. Group by category (LEX / AST / PRAGMA / CONSTRUCTOR / TEST / DOC) per RESEARCH.md §Phase Requirements table. Each bullet: requirement ID + behavior + plan reference + closure commit (commit SHA placeholder `TBD` to be filled by closer).

    Step D — `.planning/ROADMAP.md` Phase 45 entry reconciliation. Replace placeholders:
    - `**Requirements**: TBD (defined at plan-phase)` → `**Requirements**: REQ-BEAT-LEX-01..04, REQ-BEAT-AST-01..04, REQ-BEAT-PRAGMA-01..04, REQ-BEAT-PRAGMA-HYPHEN-01, REQ-BEAT-CONSTRUCTOR-01..02, REQ-BEAT-TEST-01..07, REQ-BEAT-DOC-01..04`
    - `**Plans:** 0 plans` → `**Plans:** 6 plans`
    - Replace the single `- [ ] TBD (run /gsd-plan-phase 45 ...)` line with the 6-plan list:
      ```
      - [x] 45-01-PLAN.md — Lexer foundation (TokenType.BeatLiteral + signed/unsigned suffix branches + PragmaScanner hyphen gap)
      - [x] 45-02-PLAN.md — BeatLiteralExpression AST + Parser arm + literal-token-set
      - [x] 45-03-PLAN.md — PragmaRegistry entry + ExecutionContext.BeatTrueToSig + FlowEngine.ApplyBeatTrueToSigPragma + ModuleLoader push/pop
      - [x] 45-04-PLAN.md — EvaluateBeatLiteral switch arm + multiplier formula + 3 composer-facing .flow smokes
      - [x] 45-05-PLAN.md — (beat N) constructor migration to RegisterContextDependent + DICT-01 regression
      - [x] 45-06-PLAN.md — Cross-file pair + tutorials + audio baselines + CLAUDE.md + tracking-file sweep + 45-VERIFICATION.md (closer)
      ```
    
    Update the Phase progress table at lines 300+ to add a Phase 45 row.

    Step E — `.planning/STATE.md` Phase 45 closure. Update:
    - Frontmatter `completed_phases`: 7 → 8 (after Phase 45 ships).
    - Frontmatter `completed_plans`: 54 → 60 (+6 Phase 45 plans).
    - Frontmatter `percent`: recalculate (60 / 59 → ~65% — wait, 60/65 = ~92% if total_plans was 65; verify against actual STATE.md totals).
    - Frontmatter `last_updated` to today's date.
    - Frontmatter `stopped_at` to "Phase 45 complete".
    - Update v1.5 Phase Map table — Phase 45 row from Pending to Shipped today.
    - Add a "Phase 45 highlights" block matching the Phase 43/44 closure highlights pattern. Cover: Nb literal lex+parse+eval surface; pragma plumbing single-field design; (beat N) constructor migration; DICT-01 regression preserved; two tutorial chapters; two-run cmp-clean baselines; xUnit Fact counts (e.g., 8 lex + 5 AST + 10 pragma + 8 multiplier + 7 constructor/DICT + 4 str round-trip + 1 cross-file smoke + 4 tutorial = ~47 Phase 45 Facts).

    Step F — `45-VERIFICATION.md` closure deliverable. Create at `.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VERIFICATION.md`. Mirror `43-VERIFICATION.md` shape:
    - Header: phase + closure date + scope summary.
    - §1 Phase 45 Requirements Closure Table: one row per REQ-BEAT-NN (~25 rows) with columns `Req ID | Description | Plan | Evidence (Fact name or .flow path) | Status`.
    - §2 xUnit Fact Counts: total Phase 45 Facts breakdown.
    - §3 .flow Smoke Inventory: 4 smoke scripts + 2 tutorial files.
    - §4 Two-Run Cmp-Clean Evidence: SHA-256 hashes of baselined WAVs + matching policy.
    - §5 Known Caveats: e.g., if `(str)` Round-Trip Lock (D-14) is reconfirmed; if any deferred ideas (D-14/D-15/D-17) need re-tracking.
    - §6 Phase 45 Metrics: lines added/removed; files created/modified; commits per plan.

    NEVER use `Bash(cat &lt;&lt; 'EOF')` for file creation — use the Write/Edit tool.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; grep -c "beat-true-to-sig" CLAUDE.md &amp;&amp; grep -c "REQ-BEAT-LEX-01" .planning/REQUIREMENTS.md &amp;&amp; grep -c "45-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -c "Phase 45" .planning/STATE.md &amp;&amp; test -f .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VERIFICATION.md &amp;&amp; wc -l .planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-VERIFICATION.md &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase45" --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "0.5b" CLAUDE.md` ≥ 1 (D-13 row landed).
    - `grep -c "beat-true-to-sig" CLAUDE.md` ≥ 1 (pragma documented).
    - `.planning/REQUIREMENTS.md` has the Phase 45 section with all 25 REQ-BEAT-NN entries marked `[x]`.
    - `.planning/ROADMAP.md` Phase 45 entry shows 6 plans listed; requirements enumerated (not TBD); Plans-list checkbox boxes all `[x]`.
    - `.planning/STATE.md` Phase 45 marked complete in the v1.5 phase map; highlights block present; completed_phases=8 / completed_plans=60.
    - `45-VERIFICATION.md` exists, ≥100 lines, has all 25 REQ-BEAT-NN rows + xUnit Fact counts + two-run cmp-clean evidence.
    - `dotnet test --filter "FullyQualifiedName~Phase45"` total ≥ 47 Facts GREEN.
    - Whole-suite parity check: `dotnet test 2>&amp;1 | tail -5` shows the Phase 44 closer pass/fail baseline plus exactly Phase 45's net additions (≥47 GREEN), zero new RED.
  </acceptance_criteria>
  <done>Phase 45 documentation reconciled; all tracking files updated; 45-VERIFICATION.md closure deliverable shipped; v1.5 progresses to 8/12 phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation + composer tutorial files. Trust boundary unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-14 | Tampering | Stale CLAUDE.md row (still shows `1.5 (Beat-tagged)`) would mislead composers into not knowing `Nb` literal exists | mitigate | D-13 explicit REPLACE (not ADD) — the old misleading row goes away. Acceptance grep on `0.5b` in CLAUDE.md pins the new row. |
| T-45-15 | Tampering | Baseline WAV files could regress silently if a future phase legitimately changes synth output (similar to Phase 28's articulation rewrite) | accept | Two-run cmp-clean preservation contract per CLAUDE.md "Conventions" — `RmsRegressionTests` (or hash-match) baselines are committed because no PRNG sites exist in Phase 45. If a future phase legitimately moves bytes, that phase's plan-phase regenerates Phase 45 baselines (Phase 28 precedent). |
| T-45-16 | Information Disclosure | Tutorial files in `examples/beat/` are composer-facing and committed to repo | accept | Same trust posture as `examples/scala/intro.flow`, `examples/dsp/granular.flow`, every other shipped example. No secrets, no PII. |
| T-45-17 | Tampering | REQUIREMENTS.md / ROADMAP.md / STATE.md sweep could miscount totals | mitigate | Acceptance grep checks pin the IDs exist; the tracking-file sweep is mechanical — Phase 44 closer pattern is the template. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages. |

**No-threats rationale for omitted ASVS categories:** Documentation + tutorial + tracking-file sweep. No code surface beyond test files. No auth/session/access/crypto. V5: tracking-file content is composer-facing markdown, no exec surface.
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~Phase45"` — ~47 Facts GREEN.
- `for t in tests/test_beat_*.flow; do dotnet run --project flow-interpreter "$t" || exit 1; done` — all 4 smoke scripts PASS.
- `dotnet run --project flow-interpreter examples/beat/intro.flow` + `examples/beat/cut-time.flow` — both produce WAV + MIDI and exit 0.
- CLAUDE.md grep pinned.
- REQUIREMENTS.md / ROADMAP.md / STATE.md grep pinned.
- 45-VERIFICATION.md present with closure tables.
- Whole-suite parity: same pass/fail count as Phase 44 closer baseline PLUS exactly 47 new GREEN (zero new RED).
</verification>

<success_criteria>
- 4 `.flow` smokes (REQ-BEAT-TEST-01..04) all exit 0.
- 2 tutorial files render and produce committed baseline WAVs.
- 4 tutorial xUnit Facts (two-run cmp-clean + baseline-match × 2 each) GREEN.
- 4 `(str)` round-trip Facts GREEN (D-14 lock).
- CLAUDE.md Music Types row replaced with D-13 row + pragma list updated.
- REQUIREMENTS.md Phase 45 section present with all 25 IDs.
- ROADMAP.md Phase 45 entry reconciled (6 plans listed, requirements enumerated, all `[x]`).
- STATE.md v1.5 progress 8/12 phases; Phase 45 highlights present.
- 45-VERIFICATION.md closure deliverable shipped.
</success_criteria>

<output>
Create `.planning/phases/45-beat-literal-syntax-true-to-sig-pragma/45-06-SUMMARY.md` documenting:
- Cross-file test pair file contents + cross-file boundary behavior verified
- Tutorial file scope (chapter list per file)
- Baseline WAV SHA-256 hashes
- CLAUDE.md edit positions
- REQUIREMENTS.md / ROADMAP.md / STATE.md / 45-VERIFICATION.md final shapes
- Total Phase 45 Fact count
- All 25 REQ-BEAT-NN closure mapping
- Phase 45 final commit SHA + the closing chain of commits across all 6 plans
</output>
