---
phase: 47-compile-target-flavors
plan: 06
type: execute
wave: 6
depends_on:
  - 47-01
  - 47-02
  - 47-03
  - 47-04
  - 47-05
files_modified:
  - .planning/phases/47-compile-target-flavors/47-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements:
  - REQ-WEB-TARGET-01
  - REQ-WEB-TARGET-02
  - REQ-WEB-TARGET-03
  - REQ-WEB-TARGET-04
  - REQ-WEB-TARGET-05
  - REQ-WEB-TARGET-06
  - REQ-WEB-TARGET-07
  - REQ-WEB-TARGET-08
  - REQ-WEB-TARGET-09
  - REQ-WEB-TARGET-10
must_haves:
  truths:
    - "Phase 47 VERIFICATION.md exists with completion table (10 REQ-WEB-TARGET-NN rows) + acceptance evidence (commands run + outputs captured) + known caveats (e.g., DryWetMidi WASM-compat outcome from Plan 47-04 Task 2; whether test-project Web build was attempted)"
    - "ROADMAP.md Phase 47 section flipped from `[ ] **Phase 47:** TBD ...` to `[x] **Phase 47: Compile-Target Flavors** — ... completed YYYY-MM-DD`; phase row in Progress table flipped to `47. Compile-Target Flavors | v1.5 | 6/6 | Complete | <date>`. Requirements field updated to enumerate REQ-WEB-TARGET-01..10 (10 IDs)."
    - "ROADMAP.md Phase 47 detail section's `Plans:` line + the `- [ ] TBD` placeholder REPLACED with the 6 enumerated `[x] 47-NN-PLAN.md` plan lines with one-line objectives + commit SHAs"
    - "STATE.md progress block updated: total_plans goes from 65 → 71 (added 6 Phase 47 plans); completed_plans goes from 54 → 60; v1.5 phase map gets a row for Phase 47 with status `Shipped <YYYY-MM-DD>`; `Current Position` block updated to indicate Phase 47 shipped + next-step is Phase 48"
    - "STATE.md `Phase 47 highlights` block lands with bullets summarizing what landed: FlowTarget MSBuild conditioning, WebAudioBackend stub, central #if !FLOW_WEB guards at 3 call sites, ModuleLoader stripped-module gate, Parser live-block gate, FlowTargetFactAttribute, AssemblyReferenceScanTests via Mono.Cecil"
    - "REQUIREMENTS.md gets a new `### Compile-Target Flavors (Phase 47)` section with 10 REQ-WEB-TARGET-NN entries marked `[x]` + paired commit SHAs (or `closure: Plan 47-NN`)"
    - "CLAUDE.md gets a new section `## Compile-Target Flavors` (or appended at the end of the `## Build & Run Commands` section) documenting `FlowTarget=Desktop|Web` build surface — composer-facing reference: how to invoke, what's stripped on Web, where the guards live (FlowEngine.IsWebTarget + ModuleLoader gate + Parser gate)"
    - "Full test suite GREEN: `dotnet test flow-lang.Tests/flow-lang.Tests.csproj` (Desktop default) reports zero NEW failures vs pre-Phase-47 baseline. Pre-existing Phase 28/29/35/38 baseline failures (per STATE.md Phase 43 highlights) remain as-is — Phase 47 introduces zero new failures."
    - "Both build modes succeed at phase close: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` AND `-p:FlowTarget=Web` both exit 0"
    - "Single commit per task; closer commits land as separate commits per CLAUDE.md GSD discipline (NOT amended)"
  artifacts:
    - path: ".planning/phases/47-compile-target-flavors/47-VERIFICATION.md"
      provides: "Phase 47 completion record with REQ-WEB-TARGET-01..10 evidence"
      contains: "REQ-WEB-TARGET-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 47 marked [x] complete + 6-plan enumeration + Progress table row complete"
      contains: "Phase 47"
    - path: ".planning/STATE.md"
      provides: "Updated progress block + Phase 47 highlights bullet block + Current Position pointing at Phase 48"
      contains: "Phase 47"
    - path: ".planning/REQUIREMENTS.md"
      provides: "10 REQ-WEB-TARGET-NN entries marked [x]"
      contains: "REQ-WEB-TARGET-01"
    - path: "CLAUDE.md"
      provides: "Compile-Target Flavors section documenting FlowTarget build surface for future contributors"
      contains: "FlowTarget"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 47 detail)"
      to: ".planning/phases/47-compile-target-flavors/47-VERIFICATION.md"
      via: "phase-complete narrative"
      pattern: "47-VERIFICATION"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 47 plan SUMMARYs"
      via: "REQ-WEB-TARGET-NN closure citations"
      pattern: "Plan 47-\\d{2}"
---

<objective>
Close Phase 47. Produce `47-VERIFICATION.md` with REQ-WEB-TARGET-01..10 acceptance evidence; flip ROADMAP/STATE/REQUIREMENTS/CLAUDE.md tracking entries; verify full test suite GREEN under Desktop default; document any known caveats (DryWetMidi WASM-compat outcome, test-project Web build status).

After this plan: Phase 47 is shipped. Phase 48 (`WASM Runtime + WebAudioBackend`) is unblocked — its Plan 48-01 dry-run consumes FlowTarget=Web infrastructure delivered here.

Purpose: provide the canonical record of what shipped + close all tracking surfaces in one atomic plan. No production code changes in Plan 47-06 — pure documentation + tracking sweep.

Output: 1 new VERIFICATION.md + 4 tracking-file updates (ROADMAP, STATE, REQUIREMENTS, CLAUDE.md).

Per phase-closer convention established by Plan 44-11, Plan 43-05, Plan 42-04. Tracking sweep MUST land in a single commit at the end of Wave 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-compile-target-flavors/47-CONTEXT.md
@.planning/phases/47-compile-target-flavors/47-PATTERNS.md
@.planning/phases/47-compile-target-flavors/47-01-PLAN.md
@.planning/phases/47-compile-target-flavors/47-02-PLAN.md
@.planning/phases/47-compile-target-flavors/47-03-PLAN.md
@.planning/phases/47-compile-target-flavors/47-04-PLAN.md
@.planning/phases/47-compile-target-flavors/47-05-PLAN.md

<interfaces>
<!-- Existing phase-closer precedents the executor can mirror -->

Phase 44 closer's VERIFICATION.md path: `.planning/phases/44-strict-mode/44-VERIFICATION.md` — open this file (if it exists) to copy the section structure.

ROADMAP.md Phase 47 detail block to edit: see .planning/ROADMAP.md lines 522-549 (currently `[ ]` placeholder with `Plans: 0 plans` + `Plans: - [ ] TBD (run /gsd-plan-phase 47 to break down)`).

STATE.md frontmatter to update: see .planning/STATE.md lines 1-15 — keys `total_plans`, `completed_plans`, `percent`.

REQUIREMENTS.md format to follow: see lines 47-50 for the `### Pattern Algebra (Phase 36)` section structure — each REQ-PAT-NN as a `[x] **REQ-ID**: description ... — closure: Plan NN-NN commits ABCDEFG / HIJKLMN`.

CLAUDE.md insertion point: between the existing `## Conventions` section near the bottom and the new section. Insertion lands new `## Compile-Target Flavors` h2 section (or as a child of `## Build & Run Commands` — choose the placement that best matches existing CLAUDE.md flow).

10 REQ-WEB-TARGET-NN assignments (anchored to the 6 plans):
- REQ-WEB-TARGET-01: FlowTarget MSBuild property + DefineConstants — Plan 47-01
- REQ-WEB-TARGET-02: Conditional &lt;Compile Remove&gt; strip list (7 entries) — Plan 47-01
- REQ-WEB-TARGET-03: Samples + Rug.Osc strip via conditional ItemGroups — Plan 47-01
- REQ-WEB-TARGET-04: WebAudioBackend stub + IAudioBackend integration — Plan 47-02 + 47-03 (DryWetMidi smoke also covered here per Plan 47-04 D-47-04 forward-look)
- REQ-WEB-TARGET-05: Central #if !FLOW_WEB guards at FlowEngine.cs:185, FlowEngine.cs:202, BuiltInFunctions.cs:1027 — Plan 47-03
- REQ-WEB-TARGET-06: FlowEngine.IsWebTarget + SupportsLiveBlocks static flags — Plan 47-03
- REQ-WEB-TARGET-07: Parser live-block parse-time gate + ModuleLoader stripped-module gate with charitable advisory — Plan 47-03
- REQ-WEB-TARGET-08: FlowTargetFactAttribute + DryWetMidi WASM-compat smoke + Web-side guard test files — Plan 47-04
- REQ-WEB-TARGET-09: AudioPlaybackManager Web-first probe ordering — Plan 47-02
- REQ-WEB-TARGET-10: AssemblyReferenceScanTests via Mono.Cecil — Plan 47-05

The orchestrator instructed: REQ-WEB-TARGET-01..10 are anchor CANDIDATES; final assignment is part of plan output. The 10 above are the locked assignments for Plan 47-06.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce 47-VERIFICATION.md with REQ-WEB-TARGET-01..10 acceptance evidence + known caveats</name>
  <files>.planning/phases/47-compile-target-flavors/47-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/47-compile-target-flavors/47-01-PLAN.md (success criteria)
    - .planning/phases/47-compile-target-flavors/47-02-PLAN.md (success criteria)
    - .planning/phases/47-compile-target-flavors/47-03-PLAN.md (success criteria)
    - .planning/phases/47-compile-target-flavors/47-04-PLAN.md (success criteria, including DryWetMidi outcome notes)
    - .planning/phases/47-compile-target-flavors/47-05-PLAN.md (success criteria)
    - .planning/phases/44-strict-mode/44-VERIFICATION.md (if exists — copy structure)
    - All 5 SUMMARY files: `.planning/phases/47-compile-target-flavors/47-0{1,2,3,4,5}-SUMMARY.md` (if executor produced them in earlier plans)
  </read_first>
  <action>
Create `.planning/phases/47-compile-target-flavors/47-VERIFICATION.md` with the following structure:

```markdown
# Phase 47: Compile-Target Flavors — Verification

**Phase:** 47
**Status:** Shipped <YYYY-MM-DD>
**Plans:** 6/6 complete
**Requirements:** 10 REQ-WEB-TARGET-NN closed

## Outcome Summary

Phase 47 introduces `FlowTarget=Desktop|Web` MSBuild conditioning so the `flow-lang.dll`
library compiles cleanly under WASM by stripping features that cannot run in a
browser sandbox. Foundation for Phase 48 (WASM Runtime + WebAudioBackend) and
Phase 49 (flowlang.dev site).

**Build surface:**
- Default: `dotnet build flow-lang/flow-lang.csproj` → FlowTarget=Desktop (byte-identical to pre-Phase-47 behavior)
- Explicit Desktop: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop`
- Web: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` → produces flow-lang.dll with stripped SFZ/OSC/PulseAudio/CoreAudio/InputFunctions

**Composer-facing behavior on Web target:**
- `use "@sfz"` or `use "@osc"` → ModuleLoader emits `[target] module '@X' unavailable on Web target — line N. Build with FlowTarget=Desktop to enable.` advisory; ModuleLoadResult.Error.
- `live 1bar { ... }` → Parser throws ParseException `` `live` block requires Desktop target — line N. Build with FlowTarget=Desktop or run with `flow run script.flow` locally. ``
- `(micBuffer 1.0)` → Function not found (InputFunctions stripped); composer sees runtime error.
- Audio playback → WebAudioBackend stub throws PlatformNotSupportedException with `"WebAudioBackend stub — Phase 48 will implement via [JSImport]"` message (Phase 48 lands the JSImport bodies).

## Requirement Closure Table

| REQ-ID | Description | Closure |
|--------|-------------|---------|
| REQ-WEB-TARGET-01 | `<FlowTarget>` MSBuild property + Web-conditional `<DefineConstants>` adding `FLOW_WEB` | Plan 47-01 commit `<SHA>` |
| REQ-WEB-TARGET-02 | Conditional `<Compile Remove>` strip list (7 entries: PulseAudioSimpleBackend, PulseAudioCaptureBackend, CoreAudioBackend, Sfz/**/*.cs, OscFunctions, OscHandleData, InputFunctions) | Plan 47-01 commit `<SHA>` |
| REQ-WEB-TARGET-03 | Samples bundle + Rug.Osc + sfz.flow/osc.flow stdlib stripped via conditional ItemGroups | Plan 47-01 commit `<SHA>` |
| REQ-WEB-TARGET-04 | WebAudioBackend stub at `Audio/WebAudioBackend.cs` implementing IAudioBackend; all methods except IsAvailable throw PlatformNotSupportedException with pinned StubMessage | Plan 47-02 commit `<SHA>` |
| REQ-WEB-TARGET-05 | Central `#if !FLOW_WEB` guards at FlowEngine.cs:185 (SfzBuiltins.Register), FlowEngine.cs:202 (OscFunctions.Register), BuiltInFunctions.cs:1027 (Audio.InputFunctions.RegisterContextDependent) | Plan 47-03 commit `<SHA>` |
| REQ-WEB-TARGET-06 | `FlowEngine.IsWebTarget` + `FlowEngine.SupportsLiveBlocks` static properties; compile-time-constant initializers | Plan 47-03 commit `<SHA>` |
| REQ-WEB-TARGET-07 | Parser parse-time gate for `live` keyword + ModuleLoader.LoadModule stripped-module gate (charitable `[target]` WarnOnce advisory; ModuleLoadResult.Error return) | Plan 47-03 commit `<SHA>` |
| REQ-WEB-TARGET-08 | `FlowTargetFactAttribute` xUnit attribute + Web-target-side guard test files (WebTargetParserTests + WebTargetModuleLoaderTests) + DryWetMidi WASM-compat smoke | Plan 47-04 commit `<SHA>` |
| REQ-WEB-TARGET-09 | `AudioPlaybackManager.DetectBackend` probes Web FIRST via `WebAudioBackend.IsAvailable()` (OperatingSystem.IsBrowser() JIT intrinsic); existing CoreAudio + PulseAudio branches wrapped in `#if !FLOW_WEB` | Plan 47-02 commit `<SHA>` |
| REQ-WEB-TARGET-10 | `AssemblyReferenceScanTests` reflective scan via Mono.Cecil 0.11.5 — asserts zero references to Rug.Osc / RtMidi.Core / FileSystemWatcher / libpulse / AudioToolbox in Web flow-lang.dll | Plan 47-05 commit `<SHA>` |

## Acceptance Evidence

(Capture command + output for each acceptance check below.)

### 1. Desktop build byte-identical
```
$ dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet
<output: build succeeded, no errors>
```

### 2. Web build links cleanly
```
$ dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet
<output: build succeeded, no errors>
```

### 3. Desktop test suite GREEN
```
$ dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "Phase47" --no-build
<output: N passed, 0 failed; Web-only tests skipped per FlowTargetFact>
```

### 4. AssemblyReferenceScanTests under Web
```
$ dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "AssemblyReferenceScanTests" -p:FlowTarget=Web
<output: 2 passed / 2 skipped depending on test-project Web buildability — record actual outcome>
```

## Plan Summary

| Plan | Wave | Description | Outcome |
|------|------|-------------|---------|
| 47-01 | 1 | MSBuild conditioning foundation (FlowTarget property + strip list + smoke tests) | Shipped |
| 47-02 | 2 | WebAudioBackend stub + AudioPlaybackManager Web-first probe | Shipped |
| 47-03 | 3 | Central #if !FLOW_WEB guards + IsWebTarget/SupportsLiveBlocks + Parser/ModuleLoader gates | Shipped |
| 47-04 | 4 | FlowTargetFactAttribute + DryWetMidi smoke + Web-side guard tests | Shipped |
| 47-05 | 5 | AssemblyReferenceScanTests via Mono.Cecil 0.11.5 | Shipped |
| 47-06 | 6 | Closer (this file + ROADMAP/STATE/REQUIREMENTS/CLAUDE.md sweep) | Shipped |

## Known Caveats

(Fill in based on actual outcomes captured during Plan 47-04 + 47-05 execution.)

### Caveat 1: DryWetMidi WASM compatibility
Plan 47-04 Task 2 shipped `DryWetMidiWasmCompatTests` with 2 `[FlowTargetFact("Desktop", "Web")]` Facts.
- Desktop outcome: 2 PASSED.
- Web outcome: `<TBD — fill in based on Plan 47-04 Task 2 actual result>`.
  - If GREEN: DryWetMidi 8.0.3 is WASM-compatible; `writeMidi` stays available on Web target.
  - If RED at compile: Plan 47-06 closer added a follow-up edit moving DryWetMidi into a `Condition="'$(FlowTarget)' != 'Web'"` ItemGroup + adding a writeMidi parse-time gate in BuiltInFunctions.cs (mirror of `live` block gate); record the follow-up commit SHA here.
  - If RED at runtime: same fix; record runtime error excerpt.

### Caveat 2: flow-lang.Tests project buildability under FlowTarget=Web
Plan 47-04 Task 1 documented whether `dotnet test -p:FlowTarget=Web` runs end-to-end:
- If the test project builds under FlowTarget=Web: Phase 47 Web-only Facts can be RUN (not just skipped on Desktop).
- If the test project fails to build (ProjectReferences to flow-lsp/flow-midi/flow-interpreter cascade-fail because those projects pull native deps): Phase 47 ships the scaffolding only — Web-only Facts STAY skipped on Desktop and are unverified live under Web. Document the failure mode + a Phase 48 task to harden test-project Web buildability.
- Record the actual outcome here.

### Caveat 3: PATTERNS.md §Discrepancies acted upon
- Discrepancy 1 (CONTEXT.md `RegisterSfz`/`RegisterOsc`/`RegisterMicInput`/`RegisterLiveBlock` method names don't exist): Plan 47-03 used the actual call sites at FlowEngine.cs:185, FlowEngine.cs:202, BuiltInFunctions.cs:1027.
- Discrepancy 2 (no `NullAudioBackend` exists at IAudioBackend.cs:21-29): Plan 47-02 kept the existing throw-on-no-backend behavior; introduced WebAudioBackend stub only.
- Discrepancy 3 (`flow-lang/Samples/**` has no `<None Update>` entry today): Plan 47-01 still added `<Content Remove="Samples\**" />` + `<None Remove="Samples\**" />` belt-and-suspenders.
- Discrepancy 4 (CONTEXT.md `StandardLibrary/Network/Osc/**/*.cs` glob doesn't match — actual files are flat `OscFunctions.cs` + `OscHandleData.cs`): Plan 47-01 used the actual paths.
- Discrepancy 5 (CONTEXT.md `Live/**/*.cs` directory doesn't exist; artifacts scattered): Plan 47-01 STRIP LIST EXCLUDES live-coding artifacts because they're consumed from Interpreter.cs:133 + ExecutionContext.cs:292 + Parser.cs:221; Plan 47-03 added a parse-time throw instead (PATTERNS.md Option (a)).

## Next Steps

- Phase 48 unblocked. `/gsd:plan-phase 48` consumes Phase 47 deliverables: FlowTarget=Web build infrastructure + WebAudioBackend stub + AssemblyReferenceScanTests invariant.
- Phase 49 (flowlang.dev site) blocked on Phase 48.
```

Fill in `<YYYY-MM-DD>` with today's date (2026-05-25 per env block).
Fill in `<SHA>` placeholders with actual git SHAs from the closer Bash invocation OR leave as `<SHA>` if commits are not yet recorded (executor will need to update post-commit).
Fill in TBD items in Caveats 1 + 2 based on Plan 47-04 + 47-05 actual outcomes.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-compile-target-flavors/47-VERIFICATION.md &amp;&amp; echo VERIFICATION_EXISTS</automated>
    <automated>grep -c "REQ-WEB-TARGET-" .planning/phases/47-compile-target-flavors/47-VERIFICATION.md</automated>
    <automated>grep -c "Known Caveats" .planning/phases/47-compile-target-flavors/47-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file exists at the documented path
    - Source assertion: `grep -c "REQ-WEB-TARGET-" .planning/phases/47-compile-target-flavors/47-VERIFICATION.md` returns at least `12` (table header reference + 10 row entries + bullet mentions)
    - Source assertion: file contains `## Acceptance Evidence`, `## Plan Summary`, `## Known Caveats`, `## Next Steps` sections
    - Source assertion: 6 plan rows in Plan Summary table (47-01 through 47-06)
    - Source assertion: all 10 REQ-WEB-TARGET-NN IDs cited in the closure table; each row references its closing plan
    - Source assertion: 3 known caveats documented (DryWetMidi, test-project Web build, PATTERNS.md discrepancies)
  </acceptance_criteria>
  <done>47-VERIFICATION.md lands with REQ-WEB-TARGET-01..10 acceptance evidence + known caveats + next-steps pointer to Phase 48.</done>
</task>

<task type="auto">
  <name>Task 2: Flip ROADMAP.md Phase 47 entries to complete + enumerate the 6 plans + update Progress table</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 522-549 (Phase 47 detail section to edit)
    - .planning/ROADMAP.md lines 300-352 (Progress table — find Phase 47 row if exists, otherwise add)
    - .planning/ROADMAP.md lines 10 (v1.5 milestone narrative — Phase 47 was added 2026-05-25; need to flip from "added" to "shipped")
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` with FIVE changes:

(A) **Update the Phase 47 detail section header** at line 522. Current:
```markdown
### Phase 47: Compile-Target Flavors
```

Change to:
```markdown
### Phase 47: Compile-Target Flavors — SHIPPED <YYYY-MM-DD>
```

(B) **Update the `**Requirements:**` line at line 544** (currently: `**Requirements**: TBD (defined at plan-phase — anchor candidates: REQ-WEB-TARGET-01..08 ...)`):

Replace with:
```markdown
**Requirements**: REQ-WEB-TARGET-01, REQ-WEB-TARGET-02, REQ-WEB-TARGET-03, REQ-WEB-TARGET-04, REQ-WEB-TARGET-05, REQ-WEB-TARGET-06, REQ-WEB-TARGET-07, REQ-WEB-TARGET-08, REQ-WEB-TARGET-09, REQ-WEB-TARGET-10
```

(C) **Update the `**Plans:**` count line at line 545** (currently: `**Plans:** 0 plans`):

Replace with:
```markdown
**Plans:** 6/6 plans complete
```

(D) **Replace the plan placeholder at lines 547-549** (currently: `Plans:\n\n- [ ] TBD (run /gsd-plan-phase 47 to break down)`):

Replace with:
```markdown
Plans:

**Wave 1**

- [x] 47-01-PLAN.md — MSBuild conditioning foundation (FlowTarget property + strip list + BuildConditioningSmokeTests) — `<SHA>`

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 47-02-PLAN.md — WebAudioBackend stub + AudioPlaybackManager Web-first probe (D-47-05..07) — `<SHA>`

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 47-03-PLAN.md — Central #if !FLOW_WEB guards + FlowEngine.IsWebTarget/SupportsLiveBlocks + Parser/ModuleLoader gates (D-47-08..10) — `<SHA>`

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 47-04-PLAN.md — FlowTargetFactAttribute + DryWetMidi WASM-compat smoke + Web-side guard tests (D-47-04/13) — `<SHA>`

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 47-05-PLAN.md — AssemblyReferenceScanTests via Mono.Cecil 0.11.5 (D-47-14) — `<SHA>`

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 47-06-PLAN.md — Closer: 47-VERIFICATION.md + ROADMAP/STATE/REQUIREMENTS/CLAUDE.md sweep — `<SHA>`
```

(E) **Add a Phase 47 row to the Progress table** at lines 300-352. Locate the row for Phase 46 (or the last v1.5 row currently in the table). Insert a new row immediately after:

```
| 47. Compile-Target Flavors | v1.5 | 6/6 | Complete | <YYYY-MM-DD> |
```

If Phase 47 row doesn't exist (the current ROADMAP.md table at line 351 ends at Phase 46), append the new row. If the table already has a Phase 47 row marked Not started, REPLACE that row with the complete row above.

DO NOT modify Phase 48 or Phase 49 sections — they stay at `[ ] **Phase 48:** ...` placeholders.

Replace `<YYYY-MM-DD>` with today (2026-05-25).
Replace `<SHA>` placeholders with actual git short SHAs IF available; otherwise leave as `<SHA>` placeholder (the executor's commit step will fill them in post-commit).
  </action>
  <verify>
    <automated>grep -c "Phase 47: Compile-Target Flavors — SHIPPED" .planning/ROADMAP.md</automated>
    <automated>grep -c "REQ-WEB-TARGET-01" .planning/ROADMAP.md</automated>
    <automated>grep -c "47-01-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -c "47-06-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep "47. Compile-Target Flavors.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: Phase 47 header includes "— SHIPPED"
    - Source assertion: all 10 REQ-WEB-TARGET-NN IDs in the `**Requirements**:` line
    - Source assertion: 6 distinct `47-NN-PLAN.md` references in the Phase 47 detail section (47-01 through 47-06)
    - Source assertion: Progress table row for Phase 47 marked Complete with date
    - Source assertion: Phase 48 + Phase 49 sections UNCHANGED (still `[ ]` placeholders)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 47 marked SHIPPED with 6 enumerated plans + Progress table updated; Phase 48/49 untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md frontmatter progress block + add Phase 47 highlights bullets + flip Current Position to Phase 48</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md lines 1-50 (frontmatter + Current Position block)
    - .planning/STATE.md lines 38-53 (v1.5 Phase Map table — needs new Phase 47 row)
    - .planning/STATE.md lines 70-149 (existing phase-highlights pattern — Phase 38 + 39 + 42 + 43 examples to mirror)
  </read_first>
  <action>
Edit `.planning/STATE.md` with THREE updates:

(A) **Update frontmatter** (lines 1-15). Current values: `total_plans: 65, completed_plans: 54, percent: 58`.

Change to:
```yaml
progress:
  total_phases: 12
  completed_phases: 8
  total_plans: 71
  completed_plans: 60
  percent: 64
```

(B) **Update v1.5 Phase Map table** (lines 38-53). Add a new row for Phase 47:
```
| 47 | Compile-Target Flavors | REQ-WEB-TARGET-01..10 | 10 | Shipped <YYYY-MM-DD> |
```

Insert this row immediately after the existing Phase 46 row (or, if absent, at the end of the v1.5 Phase Map table — preserving alphabetical/numerical order).

Also update the table description line above the table (line 38: "### v1.5 Phase Map (10 phases, 75 REQs)") to "### v1.5 Phase Map (13 phases, 85 REQs)" — Phase 47 adds 10 new REQ-WEB-TARGET-NN.

Update the summary line below the table (currently line 53: `**v1.5 progress: 7/10 phases complete**`) to:
```
**v1.5 progress: 8/13 phases complete** (35 + 36 + 37 + 38 + 39 + 42 + 43 + 44 + 47). 85 v1.5 REQs total; 78 closed + Phase 40/41/45/46/48/49 still pending.
```

(C) **Add Phase 47 highlights bullets** — insert IMMEDIATELY AFTER the existing "**Phase 43 highlights (2026-05-24):**" / "**Phase 44 highlights**" block. Use the precedent shape established by Phase 38/43/44 highlights blocks:

```markdown
**Phase 47 highlights (<YYYY-MM-DD>):**

- `FlowTarget=Desktop|Web` MSBuild conditioning lands in `flow-lang/flow-lang.csproj` (D-47-01..03). Default value Desktop; `dotnet build -p:FlowTarget=Web` activates `FLOW_WEB` preprocessor symbol via Web-conditional `<DefineConstants>`. Conditional `<ItemGroup>` strips 7 files from C# compile (PulseAudioSimpleBackend/PulseAudioCaptureBackend/CoreAudioBackend/Sfz/**/OscFunctions/OscHandleData/InputFunctions) + Rug.Osc PackageReference + Samples/sfz.flow/osc.flow from publish output. Convention introduced — future phases reuse (Phase 41 cross-platform binaries; Phase 48 WASM).
- `Audio/WebAudioBackend.cs` stub class lands implementing IAudioBackend (D-47-05). `IsAvailable()` returns `OperatingSystem.IsBrowser()` — JIT intrinsic dead-code-eliminated on Desktop trim-mode builds. All other surface methods throw `PlatformNotSupportedException` with pinned `"WebAudioBackend stub — Phase 48 will implement via [JSImport]"` message. `AudioPlaybackManager.DetectBackend` adds Web probe as FIRST branch (D-47-06); existing CoreAudio + PulseAudio branches wrapped in `#if !FLOW_WEB`.
- Central `#if !FLOW_WEB` guards land at the ACTUAL call sites (47-PATTERNS.md §Discrepancy 1 reconciled): `FlowEngine.cs:185` (SfzBuiltins.Register), `FlowEngine.cs:202` (OscFunctions.Register), `BuiltInFunctions.cs:1027` (Audio.InputFunctions.RegisterContextDependent). CONTEXT.md's hypothetical `RegisterSfz()`/`RegisterOsc()`/`RegisterMicInput()`/`RegisterLiveBlock()` wrapper-method approach was wrong — actual sites used.
- `FlowEngine.IsWebTarget` (bool) + `FlowEngine.SupportsLiveBlocks` (bool) static properties land near existing `CurrentSampleCache`/`CurrentSfzSampleCache`/`CurrentExecutionContext` static props (D-47-10). Compile-time-constant initializers via `#if FLOW_WEB` ternary; no runtime mutation. Read by Parser (parse-time `live` block gate) and ModuleLoader (stripped-stdlib gate).
- Parser parse-time gate on `TokenType.Live` at `Parser.cs:220` throws Rust-style ParseException pointing at line when `!FlowEngine.SupportsLiveBlocks`. `LiveBlockStatement.cs` / `LiveBlockRegistry.cs` / `LambdaCaptureAuditor.cs` STAY in Web build (Interpreter.cs:133 case-dispatch + ExecutionContext.cs:292 property would cascade-break if stripped) — parse-time throw prevents construction; AST types remain referenceable but unreachable at runtime.
- ModuleLoader.LoadModule gates `@sfz` + `@osc` imports on Web with charitable `[target] module '@X' unavailable on Web target` advisory via existing `RenderingDiagnostics.WarnOnce` infra + `ModuleLoadResult.Error` return (D-47-09). `@notation-io` + `@patterns` + `@generative` + `@improv` STAY available (pure-Flow stdlibs, hand-rolled XmlWriter — no native deps).
- `FlowTargetFactAttribute` xUnit attribute lands at `flow-lang.Tests/Helpers/FlowTargetFactAttribute.cs` (D-47-13). Subclasses `Xunit.FactAttribute`; sets inherited `Skip` property when current target doesn't match. `CurrentTarget` const exposed as `"Web"` under `FLOW_WEB`, `"Desktop"` otherwise. Web-side guard test scaffolds shipped: `WebTargetParserTests` + `WebTargetModuleLoaderTests` + `AssemblyReferenceScanTests`.
- `AssemblyReferenceScanTests` lands as the long-term invariant gate (D-47-14) — Mono.Cecil 0.11.5 (MIT, jbevain, package-legitimacy-gated) reflective scan asserts zero references to `Rug.Osc` / `RtMidi.Core` / `System.IO.FileSystemWatcher` (type-ref scan) + zero P/Invoke targets matching `libpulse` / `AudioToolbox` (PInvokeInfo scan). Catches PR drift if a future contributor accidentally re-introduces a stripped dep via transitive reference.
- DryWetMidi 8.0.3 WASM-compat smoke test lands per D-47-04 forward-look (Plan 47-04 Task 2). 2 `[FlowTargetFact("Desktop", "Web")]` Facts verify MidiFile.Write + MidiFile.Read end-to-end. Outcome: `<TBD — Phase 48 D-48-04 decision deferred until Web test-runner buildability resolved>`.
- One package added across all 6 Phase 47 plans (Mono.Cecil 0.11.5 — test-only, package-legitimacy gate ratified). Brings Flow's external-dep count from 5 (after Phase 38 PrettyPrompt + Rug.Osc adds) to 6 — but Mono.Cecil is test-only, not shipped in flow-lang.dll itself.
```

Replace `<YYYY-MM-DD>` with `2026-05-25`.

Also **update the Current Position block** at lines 26-37. Current:
```
Phase: 45
Plan: Not started
Next step: `/clear` then one of (composer pick):
  - `/gsd:plan-phase 44` — Strict Mode ...
  - `/gsd:context-phase 40` — Studio Sync ...
Status: Ready to execute
Last activity: 2026-05-26 -- Phase 45 planning complete
```

Change to:
```
Phase: 47 -- shipped <YYYY-MM-DD>
Plan: 6/6 complete
Next step: `/clear` then one of:
  - `/gsd:plan-phase 48` — WASM Runtime + WebAudioBackend (unblocked by Phase 47; consumes FlowTarget=Web build infrastructure + WebAudioBackend stub)
  - `/gsd:plan-phase 45` — Beat Literal Syntax & True-to-Sig Pragma (independent of Phase 47; carryover from Phase 43)
  - `/gsd:context-phase 40` — Studio Sync (carryover; orthogonal to 47-49 track)
Status: Phase 47 closed
Last activity: <YYYY-MM-DD> -- Phase 47 closer landed
```

DO NOT modify other sections of STATE.md (Performance Metrics, Phase 30 highlights, Phase 38 highlights, etc.). Preserve all existing highlights blocks below.
  </action>
  <verify>
    <automated>grep "total_plans: 71" .planning/STATE.md</automated>
    <automated>grep "completed_plans: 60" .planning/STATE.md</automated>
    <automated>grep "Phase 47 highlights" .planning/STATE.md</automated>
    <automated>grep -c "REQ-WEB-TARGET" .planning/STATE.md</automated>
    <automated>grep "Phase 48" .planning/STATE.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: STATE.md frontmatter `total_plans: 71` + `completed_plans: 60`
    - Source assertion: Phase 47 row in v1.5 Phase Map table with status "Shipped"
    - Source assertion: "Phase 47 highlights" block lands with at least 8 bullets covering MSBuild conditioning + WebAudioBackend stub + central guards + IsWebTarget + Parser gate + ModuleLoader gate + FlowTargetFactAttribute + AssemblyReferenceScanTests
    - Source assertion: Current Position block flipped to Phase 48 as next step
    - Source assertion: existing Phase 38/39/42/43 highlights blocks PRESERVED (not deleted)
  </acceptance_criteria>
  <done>STATE.md frontmatter + Phase Map + highlights + Current Position all updated; existing content preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Add Phase 47 REQ-WEB-TARGET-NN section to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 1-30 (top + category-introduction line — needs new `REQ-WEB-TARGET-*` category mention)
    - .planning/REQUIREMENTS.md lines 47-65 (existing v1.5 phase requirement section structure to mirror — e.g. `### Pattern Algebra (Phase 36)`)
    - .planning/REQUIREMENTS.md end-of-file location (where to insert Phase 47 section)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` with TWO additions:

(A) **Update the categories-introduction line** at line 7. Current text mentions categories `LANG-*`, `TEST-*`, ..., `JET-*`, `SHOWCASE-*`. Append `, REQ-WEB-TARGET-*` to the end of that comma-separated list. Reads as:

```markdown
REQ-ID numbering continues from v1.4 close. New categories `LANG-*`, `TEST-*`, `HK-*`, `PAT-*`, `GEN-*`, `SECT-*`, `IMPROV-*`, `DSP-*`, `MIX-*`, `SAMP-*`, `PIANO-*`, `FLUTE-*`, `DRUM-*`, `LIVE-*`, `REPL-*`, `AUDIO-IN-*`, `OSC-*`, `XML-*`, `LILY-*`, `ABC-*`, `MML-*`, `MIDI-RT-*`, `CLOCK-*`, `LINK-*`, `JACK-*`, `WASM-*`, `WASAPI-*`, `COREAUDIO-*`, `BIN-*`, `DOC-*`, `JET-*`, `SHOWCASE-*`, `REQ-AUDIT-*`, `REQ-MOD-*`, `REQ-STRICT-*`, `REQ-BEAT-*`, `REQ-WEB-TARGET-*` introduced this milestone.
```

(Note: depending on what's already there from Phase 42/43/44/45/46, some of these may already be present. The instruction is to ADD `REQ-WEB-TARGET-*` if not already present.)

(B) **Append a new section at end of file** documenting Phase 47's 10 REQs. Use the existing section structure (mirroring `### Pattern Algebra (Phase 36)` at line 45):

```markdown

### Compile-Target Flavors (Phase 47)

- [x] **REQ-WEB-TARGET-01**: `<FlowTarget>Desktop</FlowTarget>` MSBuild property in `flow-lang/flow-lang.csproj` PropertyGroup; conditional `<PropertyGroup Condition="'$(FlowTarget)' == 'Web'">` appends `;FLOW_WEB` to `$(DefineConstants)`. Per D-47-01/02: single source of truth in flow-lang.csproj (no separate Web csproj); asymmetric preprocessor symbol (no FLOW_DESKTOP). — closure: Plan 47-01 commit `<SHA>`
- [x] **REQ-WEB-TARGET-02**: Conditional `<ItemGroup Condition="'$(FlowTarget)' == 'Web'">` strip list of 7 C# files: PulseAudioSimpleBackend, PulseAudioCaptureBackend, CoreAudioBackend, StandardLibrary/Audio/Sfz/**/*.cs, StandardLibrary/Network/OscFunctions, OscHandleData, StandardLibrary/Audio/InputFunctions. Per D-47-03 + 47-PATTERNS.md §Discrepancy 1/4/5 reconciliation. — closure: Plan 47-01 commit `<SHA>`
- [x] **REQ-WEB-TARGET-03**: Conditional `<Content Remove="Samples\**" />` + `<None Remove="Samples\**" />` + `<None Remove="sfz.flow" />` + `<None Remove="osc.flow" />` exclude embedded resources from Web publish output. Rug.Osc PackageReference moved into a `Condition="'$(FlowTarget)' != 'Web'"` ItemGroup. Per D-47-11. — closure: Plan 47-01 commit `<SHA>`
- [x] **REQ-WEB-TARGET-04**: `Audio/WebAudioBackend.cs` stub class implements IAudioBackend; all surface methods except `IsAvailable()` and `Dispose()` throw `PlatformNotSupportedException` with pinned StubMessage `"WebAudioBackend stub — Phase 48 will implement via [JSImport]"`. Phase 48 fills the JSImport bodies. Per D-47-05/07. — closure: Plan 47-02 commit `<SHA>`
- [x] **REQ-WEB-TARGET-05**: Central `#if !FLOW_WEB` guards at actual call sites: FlowEngine.cs:185 (SfzBuiltins.Register), FlowEngine.cs:202 (OscFunctions.Register), BuiltInFunctions.cs:1027 (Audio.InputFunctions.RegisterContextDependent). Per D-47-08 + 47-PATTERNS.md §Discrepancy 1 reconciliation (the misnamed `RegisterSfz()`/`RegisterOsc()`/`RegisterMicInput()`/`RegisterLiveBlock()` methods in CONTEXT.md don't exist; actual call sites used). — closure: Plan 47-03 commit `<SHA>`
- [x] **REQ-WEB-TARGET-06**: `FlowEngine.IsWebTarget` (bool, compile-time constant via `#if FLOW_WEB`) + `FlowEngine.SupportsLiveBlocks` (bool, !IsWebTarget) public static properties land near existing static props. No runtime mutation; constant-folded by the C# compiler. Per D-47-10. — closure: Plan 47-03 commit `<SHA>`
- [x] **REQ-WEB-TARGET-07**: Parser parse-time gate on `TokenType.Live` throws Rust-style ParseException `` `live` block requires Desktop target — line N. Build with FlowTarget=Desktop ... `` when `!FlowEngine.SupportsLiveBlocks`. ModuleLoader gate on `@sfz`/`@osc` emits charitable `[target] module '@X' unavailable on Web target — line N. ...` advisory via WarnOnce + returns ModuleLoadResult.Error. Per D-47-09. LiveBlockStatement.cs/LiveBlockRegistry.cs/LambdaCaptureAuditor.cs STAY in Web build (cascade-break risk per Interpreter.cs:133 / ExecutionContext.cs:292 consumers). — closure: Plan 47-03 commit `<SHA>`
- [x] **REQ-WEB-TARGET-08**: `FlowTargetFactAttribute` xUnit attribute at `flow-lang.Tests/Helpers/FlowTargetFactAttribute.cs` subclasses `Xunit.FactAttribute`; Skip property set based on FLOW_WEB-defined `CurrentTarget` constant. Web-side guard tests land: WebTargetParserTests (3 Facts), WebTargetModuleLoaderTests (3 Facts incl. negative @notation-io non-gating), DryWetMidiWasmCompatTests (2 cross-target Facts). Per D-47-04/13. — closure: Plan 47-04 commit `<SHA>`
- [x] **REQ-WEB-TARGET-09**: `AudioPlaybackManager.DetectBackend` probes Web FIRST via `WebAudioBackend.IsAvailable()` (OperatingSystem.IsBrowser() JIT intrinsic); existing CoreAudio + PulseAudio branches wrapped in `#if !FLOW_WEB`. PATTERNS.md §Discrepancy 2 reconciled: no NullAudioBackend introduced — existing throw-on-no-backend preserved (minimum surface change). Per D-47-06. — closure: Plan 47-02 commit `<SHA>`
- [x] **REQ-WEB-TARGET-10**: `AssemblyReferenceScanTests` via Mono.Cecil 0.11.5 (MIT, jbevain, package-legitimacy-gated) — Web-target invariant test asserting zero references to forbidden type-ref prefixes (Rug.Osc, RtMidi.Core, System.IO.FileSystemWatcher) + zero forbidden P/Invoke substrings (libpulse, AudioToolbox). Sanity-check Fact verifies legitimate refs retained. Catches PR drift if a future change accidentally re-introduces a stripped dep via transitive reference. Per D-47-14. — closure: Plan 47-05 commit `<SHA>`
```

Replace `<SHA>` placeholders with actual git short SHAs IF available; otherwise leave as placeholder for post-commit fill.
  </action>
  <verify>
    <automated>grep -c "REQ-WEB-TARGET-" .planning/REQUIREMENTS.md</automated>
    <automated>grep "Compile-Target Flavors (Phase 47)" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "\\[x\\] \\*\\*REQ-WEB-TARGET-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "\\[x\\] \\*\\*REQ-WEB-TARGET-" .planning/REQUIREMENTS.md` returns exactly `10`
    - Source assertion: section header `### Compile-Target Flavors (Phase 47)` present
    - Source assertion: introductory category line at line 7 includes `REQ-WEB-TARGET-*` in the comma-separated list
    - Source assertion: each REQ entry cites its closing Plan (Plan 47-NN) and has a `<SHA>` placeholder or actual SHA
  </acceptance_criteria>
  <done>REQUIREMENTS.md gains Phase 47 section with 10 REQ-WEB-TARGET-NN entries all marked [x].</done>
</task>

<task type="auto">
  <name>Task 5: Add Compile-Target Flavors section to CLAUDE.md for future-contributor reference</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — locate the `## Build & Run Commands` section and `## Conventions` section as insertion-point candidates)
  </read_first>
  <action>
Edit `CLAUDE.md` to add a new top-level section `## Compile-Target Flavors`. Place it immediately AFTER the existing `## Build & Run Commands` section (search for that header). The new section:

```markdown
## Compile-Target Flavors

Phase 47 introduced `FlowTarget=Desktop|Web` MSBuild conditioning so `flow-lang.dll` compiles cleanly under WASM. Desktop is the default; Web is opt-in.

```bash
# Default — Desktop (preserves all P/Invoke / SFZ / OSC / live coding / mic input)
dotnet build flow-lang/flow-lang.csproj

# Explicit Desktop
dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop

# Web — strips features that cannot run in a browser sandbox
dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web
```

**What is stripped under FlowTarget=Web** (per D-47-01..14):

- Native audio backends (P/Invoke unavailable in browser): `Audio/PulseAudioSimpleBackend.cs`, `Audio/PulseAudioCaptureBackend.cs`, `Audio/CoreAudioBackend.cs`
- SFZ sampler: `StandardLibrary/Audio/Sfz/**/*.cs` + `sfz.flow` stdlib + `@sfz` opt-in module
- OSC server/client: `StandardLibrary/Network/OscFunctions.cs` + `OscHandleData.cs` + `osc.flow` + Rug.Osc PackageReference
- Audio input: `StandardLibrary/Audio/InputFunctions.cs` (`(micBuffer ...)`)
- U-Iowa MIS sample bundle: `flow-lang/Samples/**` (Phase 29 sampled instruments charitably fall back to synthesis)
- (Future Phase 40) MIDI hardware: RtMidi.Core

**What stays** (~85% of language surface): full core language (lexer/parser/AST/evaluator/type system), pattern matching (Phase 35), all music types, all synthesis (sine/saw/square/triangle/noise/drums/organ/wavetable), all DSP (reverb/filters/compress/delay/granular/stretch/pitchShift), Phase 36 stdlibs (`@patterns`/`@generative`/`@improv`), Phase 39 notation IO export (`@notation-io` — hand-rolled XmlWriter, no native deps), MIDI file write via DryWetMidi 8.0.3 (subject to Phase 48 D-48-04 verification — see `47-VERIFICATION.md` Caveat 1).

**Composer-facing UX on Web target:**

- `use "@sfz"` or `use "@osc"` → `ModuleLoader` emits charitable advisory `[target] module '@X' unavailable on Web target — line N. Build with FlowTarget=Desktop to enable.` and returns ModuleLoadResult.Error.
- `live <quantize> { ... }` → `Parser` throws Rust-style ParseException pointing at the line; live coding requires `FileSystemWatcher` (browser-unavailable).
- `(micBuffer N)` → function not found (InputFunctions stripped on Web).

**Guard locations** (future-contributor reference):

- `flow-lang/Core/FlowEngine.cs:185,202` — `#if !FLOW_WEB` wraps SfzBuiltins.Register + OscFunctions.Register
- `flow-lang/StandardLibrary/BuiltInFunctions.cs:1027` — `#if !FLOW_WEB` wraps Audio.InputFunctions.RegisterContextDependent
- `flow-lang/Audio/AudioPlaybackManager.cs:138-160` — `WebAudioBackend.IsAvailable()` probe FIRST, existing CoreAudio + PulseAudio branches wrapped in `#if !FLOW_WEB`
- `flow-lang/Runtime/ModuleLoader.cs:LoadModule` — `IsStrippedOnWeb` gate at top
- `flow-lang/Parsing/Parser.cs:220` — `TokenType.Live` gate inside the dispatch branch
- `flow-lang/Core/FlowEngine.cs:80-110` (approximate) — `FlowEngine.IsWebTarget` + `FlowEngine.SupportsLiveBlocks` static properties (compile-time constants via `#if FLOW_WEB` initializer)
- `flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs` — Mono.Cecil reflective invariant gate; asserts no forbidden namespaces/PInvokes in the Web-compiled flow-lang.dll

When adding new audio/network/IO features that may not work in the browser, follow the established pattern:
1. Add a guard at the actual call site (NOT a wrapper method): `#if !FLOW_WEB ... #endif`.
2. If the feature is composer-invoked (parse-time or import-time), add a charitable advisory at the Parser or ModuleLoader.
3. Add a `[FlowTargetFact("Desktop")]` attribute on tests that exercise the stripped feature.
4. If the feature pulls a new package, add it to `AssemblyReferenceScanTests.ForbiddenTypeRefPrefixes` so Web build drift is caught.
```

DO NOT modify any other section of CLAUDE.md. Preserve all existing content (Goals/Non-Goals, Build & Run Commands, Project Structure, Execution Pipeline, etc.).
  </action>
  <verify>
    <automated>grep -c "## Compile-Target Flavors" CLAUDE.md</automated>
    <automated>grep -c "FlowTarget=Web" CLAUDE.md</automated>
    <automated>grep -c "WebAudioBackend" CLAUDE.md</automated>
    <automated>grep -c "FlowEngine.IsWebTarget" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "## Compile-Target Flavors" CLAUDE.md` returns `1`
    - Source assertion: `grep -c "FlowTarget=Web" CLAUDE.md` returns at least `2` (in the bash block + in the descriptive prose)
    - Source assertion: existing `## Build & Run Commands` section preserved + new section is positioned AFTER it (verify line ordering)
    - Source assertion: existing `## Goals & Non-Goals`, `## Project Structure`, etc. sections all preserved
  </acceptance_criteria>
  <done>CLAUDE.md gains `## Compile-Target Flavors` section documenting build surface + composer-facing UX + guard locations for future contributors.</done>
</task>

<task type="auto">
  <name>Task 6: Verify full test suite GREEN under Desktop default and capture phase-close metrics</name>
  <files>.planning/phases/47-compile-target-flavors/47-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/47-compile-target-flavors/47-VERIFICATION.md (Task 1 output — verify exists)
  </read_first>
  <action>
Run the full test suite under FlowTarget=Desktop (default) and capture the results. Append actual outputs to `47-VERIFICATION.md` § `## Acceptance Evidence` (replacing the `<output: ...>` placeholders from Task 1).

Commands to run + paste outputs:

```bash
# 1. Desktop build
$ dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet
# 2. Web build
$ dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet
# 3. Desktop test suite — full sweep
$ dotnet test flow-lang.Tests/flow-lang.Tests.csproj --no-build 2>&1 | tail -20
# 4. Phase 47 tests specifically
$ dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase47" --no-build 2>&1 | tail -15
# 5. Pre-existing baseline failure count (per STATE.md Phase 43 highlights: 36 pre-existing failures from Phase 28/29/35/38 baselines)
$ dotnet test flow-lang.Tests/flow-lang.Tests.csproj --no-build 2>&1 | grep -E "Passed|Failed|Skipped" | tail -3
```

For each, capture exit code + relevant tail lines.

If the suite shows MORE than 36 failures (the documented pre-existing baseline), Phase 47 introduced new regressions. Investigate via:
- Run `git diff` to confirm no production code outside flow-lang.csproj / FlowEngine.cs / BuiltInFunctions.cs / ModuleLoader.cs / Parser.cs / WebAudioBackend.cs was changed.
- If a test was tagged `[FlowTargetFact("Desktop")]` but its underlying code was inadvertently stripped, fix tagging.
- If a stripped class is referenced from a test that wasn't tagged Desktop-only, fix.

Once baseline is verified preserved, finalize 47-VERIFICATION.md by replacing the `<TBD ...>` placeholders in Caveat 1 (DryWetMidi WASM-compat outcome — record actual result of Plan 47-04 Task 2 Web run) + Caveat 2 (flow-lang.Tests project Web buildability outcome).

If DryWetMidi WASM-compat smoke test FAILED at compile under FlowTarget=Web, run a follow-up edit:
- Move `<PackageReference Include="Melanchall.DryWetMidi" Version="8.0.3" />` from the unconditional ItemGroup in `flow-lang/flow-lang.csproj` into a `Condition="'$(FlowTarget)' != 'Web'"` ItemGroup (mirror of Rug.Osc move pattern from Plan 47-01).
- Add `#if !FLOW_WEB` guard around the MidiExport.RegisterContextDependent call at the appropriate BuiltInFunctions.cs line (grep `MidiExport.RegisterContextDependent` to find).
- Add ParseException-style gate in Parser.cs for the `writeMidi` builtin OR a runtime "function not found" advisory (lighter touch). Re-run smoke test + AssemblyReferenceScanTests to confirm DryWetMidi reference is now absent from Web build.
- Record the follow-up edit in 47-VERIFICATION.md Caveat 1 with commit SHA.

If DryWetMidi WASM-compat smoke test PASSED, no follow-up needed — DryWetMidi stays in both Desktop and Web builds.
  </action>
  <verify>
    <automated>grep -c "Acceptance Evidence" .planning/phases/47-compile-target-flavors/47-VERIFICATION.md</automated>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet 2>&amp;1 | tail -3</automated>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet 2>&amp;1 | tail -3</automated>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --no-build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Build assertion: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exits 0
    - Build assertion: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exits 0
    - Behavior assertion: Desktop test suite reports total failure count ≤ 36 (pre-existing baseline per STATE.md). If higher, investigate before claiming phase close.
    - Source assertion: 47-VERIFICATION.md Acceptance Evidence section contains real captured outputs (not placeholder `<output: ...>` text)
    - Source assertion: Caveat 1 + Caveat 2 in 47-VERIFICATION.md have actual outcomes recorded (not `<TBD>` placeholders)
  </acceptance_criteria>
  <done>Full test suite verified GREEN against pre-existing baseline; 47-VERIFICATION.md acceptance evidence + caveats finalized with real outcomes; any DryWetMidi follow-up edits land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation + tracking sweep. No new runtime input perimeter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | | | | Phase 47 closer is pure tracking/documentation; no production code changes. |

No new attack surface in Plan 47-06.
</threat_model>

<verification>
- `47-VERIFICATION.md` exists with REQ-WEB-TARGET-01..10 acceptance evidence + 3 known caveats
- `.planning/ROADMAP.md` Phase 47 marked SHIPPED with 6 plans enumerated + Progress table row complete
- `.planning/STATE.md` frontmatter (total_plans=71, completed_plans=60, percent=64) + v1.5 Phase Map row + highlights bullets + Current Position pointing at Phase 48
- `.planning/REQUIREMENTS.md` Compile-Target Flavors section with 10 [x] REQ-WEB-TARGET-NN entries
- `CLAUDE.md` Compile-Target Flavors section documents build surface for future contributors
- `dotnet test` (Desktop default) reports ≤36 failures (pre-existing baseline preserved)
- `dotnet build -p:FlowTarget=Web` AND `-p:FlowTarget=Desktop` both exit 0
</verification>

<success_criteria>
- Phase 47 closed: VERIFICATION.md + ROADMAP + STATE + REQUIREMENTS + CLAUDE.md all flipped to shipped state
- Test suite GREEN under Desktop default (zero new failures vs pre-existing baseline)
- Both build targets succeed at phase close
- DryWetMidi WASM-compat outcome recorded in Caveat 1 (either GREEN-stays-in-Web, or RED-follow-up-edit-stripped + SHA)
- Phase 48 unblocked
</success_criteria>

<output>
Create `.planning/phases/47-compile-target-flavors/47-06-SUMMARY.md` when done.
</output>
