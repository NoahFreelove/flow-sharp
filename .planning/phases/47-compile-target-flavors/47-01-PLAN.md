---
phase: 47-compile-target-flavors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/flow-lang.csproj
  - flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs
autonomous: true
requirements:
  - REQ-WEB-TARGET-01
  - REQ-WEB-TARGET-02
  - REQ-WEB-TARGET-03
tags:
  - phase-47
  - wave-1
  - foundation
  - msbuild
must_haves:
  truths:
    - "D-47-01: `<FlowTarget>Desktop</FlowTarget>` added to existing PropertyGroup at flow-lang/flow-lang.csproj:3-8 — single source of truth, no separate Web csproj"
    - "D-47-02: Conditional `<PropertyGroup Condition=\"'$(FlowTarget)' == 'Web'\">` appends `;FLOW_WEB` to `$(DefineConstants)` — asymmetric, absence implies Desktop"
    - "D-47-03: Conditional `<ItemGroup Condition=\"'$(FlowTarget)' == 'Web'\">` removes 8 source files via `<Compile Remove>` at MSBuild eval time (before C# compile)"
    - "Strip list reconciled to actual code (PATTERNS.md §Discrepancies 1, 4, 5): `Audio/PulseAudioSimpleBackend.cs`, `Audio/PulseAudioCaptureBackend.cs`, `Audio/CoreAudioBackend.cs`, `StandardLibrary/Audio/Sfz/**/*.cs`, `StandardLibrary/Network/OscFunctions.cs`, `StandardLibrary/Network/OscHandleData.cs`, `StandardLibrary/Audio/InputFunctions.cs` — note: `LiveBlockStatement.cs`/`LiveBlockRegistry.cs`/`LambdaCaptureAuditor.cs` STAY (consumed by Interpreter/Parser/ExecutionContext — strip would cascade-break)"
    - "Rug.Osc PackageReference moved into conditional ItemGroup so Web build doesn't pull it in"
    - "sfz.flow + osc.flow `<None Update>` entries duplicated into Web-conditional ItemGroup as `<None Remove>` so they aren't copied to Web publish output"
    - "PATTERNS.md §Discrepancy 3 verified at Task 1: `flow-lang/Samples/**` has no `<None Update>` entry today, but MSBuild SDK implicit-include may pull them; explicit `<Content Remove=\"Samples\\**\" />` + `<None Remove=\"Samples\\**\" />` belt-and-suspenders to guarantee Web omits the 3.05 MB bundle"
    - "`dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exits 0 (no compile errors; warnings about unreferenced symbols are acceptable)"
    - "`dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exits 0 (default — preserves byte-identical behavior)"
    - "`dotnet build flow-lang/flow-lang.csproj` (no -p flag) exits 0 (FlowTarget defaults to Desktop)"
    - "BuildConditioningSmokeTests pins these acceptance commands via `Process.Start(\"dotnet\", \"build ...\")` exit code assertions"
  artifacts:
    - path: "flow-lang/flow-lang.csproj"
      provides: "FlowTarget MSBuild property + Web-conditional ItemGroup + Web-conditional DefineConstants"
      contains: "FlowTarget"
    - path: "flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs"
      provides: "Smoke tests for both build modes — exit-code assertions"
      contains: "BuildConditioningSmokeTests"
  key_links:
    - from: "flow-lang/flow-lang.csproj"
      to: "MSBuild SDK"
      via: "FlowTarget property propagation to $(DefineConstants) for C# preprocessor"
      pattern: "FLOW_WEB"
    - from: "flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs"
      to: "dotnet build CLI"
      via: "Process.Start with FlowTarget arg"
      pattern: "FlowTarget=Web|FlowTarget=Desktop"
---

<objective>
Establish the `FlowTarget=Desktop|Web` MSBuild conditioning foundation in `flow-lang/flow-lang.csproj`. After this plan, `dotnet build -p:FlowTarget=Web` produces a `flow-lang.dll` that COMPILES (links may still drift via downstream guards in Plans 47-02/03 — this plan just gates the C# compiler at the file level). Desktop builds remain byte-identical.

Purpose: foundation gate. Plan 47-02 adds `WebAudioBackend.cs` (stub) consuming `OperatingSystem.IsBrowser()`. Plan 47-03 wires `#if !FLOW_WEB` guards at registration sites in FlowEngine + BuiltInFunctions. Both depend on the FLOW_WEB define being live first.

Output: One csproj structural edit + one xUnit fact file with build-exit-code assertions verifying both build modes succeed.

Per D-47-01..04 user decisions. Strip list adjusted to actual file paths per 47-PATTERNS.md §Discrepancies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/47-compile-target-flavors/47-CONTEXT.md
@.planning/phases/47-compile-target-flavors/47-PATTERNS.md

<interfaces>
<!-- Exact existing shapes the executor consumes. No codebase exploration needed. -->

From flow-lang/flow-lang.csproj:1-14 (current shape — entire PropertyGroup + first ItemGroup):
```xml
<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <RootNamespace>FlowLang</RootNamespace>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Melanchall.DryWetMidi" Version="8.0.3" />
      <PackageReference Include="Pidgin" Version="3.5.1" />
      <PackageReference Include="Rug.Osc" Version="1.2.5" />
    </ItemGroup>
```

Stdlib `.flow` files copied per `<None Update>` at lines 23-98 of current csproj (sfz.flow at line 59-62, osc.flow at line 51-54 — these MUST be removed from Web publish via `<None Remove>` in the new conditional ItemGroup).

Actual file paths per 47-PATTERNS.md §Discrepancies + grep verification:
- `flow-lang/Audio/PulseAudioSimpleBackend.cs` ✓ exists
- `flow-lang/Audio/PulseAudioCaptureBackend.cs` ✓ exists
- `flow-lang/Audio/CoreAudioBackend.cs` ✓ exists
- `flow-lang/StandardLibrary/Audio/Sfz/*.cs` ✓ 8 files exist (SfzBuiltins, SfzData, SfzLoopMode, SfzParseException, SfzParser, SfzRegion, SfzRenderer, SfzSampleCache)
- `flow-lang/StandardLibrary/Network/OscFunctions.cs` + `OscHandleData.cs` ✓ exist (NO `Osc/` subdirectory — CONTEXT line 15 was wrong)
- `flow-lang/StandardLibrary/Audio/InputFunctions.cs` ✓ exists
- `flow-lang/Samples/{piano,brass,sax,strings,flute,bell}/*.wav` ✓ exist (CREDITS.md + LICENSE.md per dir)

DO NOT STRIP (cascade-break list — these are consumed from non-stripped code per grep):
- `flow-lang/Ast/Statements/LiveBlockStatement.cs` (Parser.cs:221 returns it; Interpreter.cs:133 case-dispatches it; ExecutionContext.cs:292 holds the registry)
- `flow-lang/Runtime/LiveBlockRegistry.cs` (ExecutionContext.cs:292 owns one)
- `flow-lang/Interpreter/LambdaCaptureAuditor.cs` (only consumer is flow-interpreter/LiveReloadManager which is itself out of flow-lang scope; but the class itself depends on FlowEngine which Web still has — safer to keep)

Parse-time gate in Plan 47-03 (a `throw new ParseException`) substitutes for file-level strip on the Live cluster.

Existing Phase 35 LANG-04 baseline-copy pattern (flow-lang.Tests.csproj:49-54):
```xml
<ItemGroup>
  <None Include="baselines\Phase35\diagnostics\*.txt">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    <Link>baselines\Phase35\diagnostics\%(Filename)%(Extension)</Link>
  </None>
</ItemGroup>
```
— Pattern reference for adding `Condition="..."` attributes to ItemGroup wrappers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FlowTarget MSBuild property + conditional DefineConstants + conditional ItemGroup with reconciled strip list to flow-lang.csproj</name>
  <files>flow-lang/flow-lang.csproj</files>
  <read_first>
    - flow-lang/flow-lang.csproj (entire file — 104 lines)
    - .planning/phases/47-compile-target-flavors/47-PATTERNS.md §"Phase 47 addition shape" + §Discrepancies (lines 67-90 and 616-626)
    - .planning/phases/47-compile-target-flavors/47-CONTEXT.md §domain D-47-01..04
  </read_first>
  <action>
Edit `flow-lang/flow-lang.csproj` with three structural additions:

(1) Append `<FlowTarget>Desktop</FlowTarget>` as the last child of the existing `<PropertyGroup>` at lines 3-8 (after `<Nullable>enable</Nullable>`). Per D-47-01: single source of truth in flow-lang.csproj; default value Desktop.

(2) Immediately after that PropertyGroup closes (line 8 currently), insert a new conditional PropertyGroup:
```xml
<!-- Phase 47 D-47-02: FLOW_WEB preprocessor symbol activates Web-target code paths.
     Asymmetric — no FLOW_DESKTOP constant; absence of FLOW_WEB implies Desktop.
     Matches .NET ecosystem convention (NETSTANDARD2_0 etc.). -->
<PropertyGroup Condition="'$(FlowTarget)' == 'Web'">
    <DefineConstants>$(DefineConstants);FLOW_WEB</DefineConstants>
</PropertyGroup>
```

(3) After the existing PackageReference ItemGroup at lines 10-14, BUT BEFORE the `<InternalsVisibleTo>` ItemGroup at line 19, insert a new conditional ItemGroup with the reconciled strip list:

```xml
<!-- Phase 47 D-47-03: file-level exclusion when FlowTarget=Web. Strip list per
     47-PATTERNS.md §Discrepancies — actual file paths verified against current
     codebase. LiveBlockStatement.cs / LiveBlockRegistry.cs / LambdaCaptureAuditor.cs
     are NOT stripped because Interpreter.cs:133 / ExecutionContext.cs:292 /
     Parser.cs:221 consume them — Plan 47-03 substitutes a parse-time throw. -->
<ItemGroup Condition="'$(FlowTarget)' == 'Web'">
    <Compile Remove="Audio\PulseAudioSimpleBackend.cs" />
    <Compile Remove="Audio\PulseAudioCaptureBackend.cs" />
    <Compile Remove="Audio\CoreAudioBackend.cs" />
    <Compile Remove="StandardLibrary\Audio\Sfz\**\*.cs" />
    <Compile Remove="StandardLibrary\Network\OscFunctions.cs" />
    <Compile Remove="StandardLibrary\Network\OscHandleData.cs" />
    <Compile Remove="StandardLibrary\Audio\InputFunctions.cs" />
    <!-- Phase 47 D-47-11: belt-and-suspenders Samples strip. PATTERNS.md §Discrepancy 3
         flagged Samples/** has no <None Update> entry today; SDK implicit-include
         behavior may still copy them. Explicit Remove guarantees Web publish omits
         the 3.05 MB U-Iowa MIS bundle (~20% of WASM budget per Phase 48 D-48-03).
         SampledInstrumentRenderer charitably falls back to synthesis per
         SampleCache.cs:265 null-fallback (existing pattern). -->
    <Content Remove="Samples\**" />
    <None Remove="Samples\**" />
    <!-- Skip copying stripped stdlib .flow files to Web publish output. The
         existing <None Update> at lines 51-54 (osc.flow) and 59-62 (sfz.flow)
         define CopyToOutputDirectory — without these Remove entries the Web
         publish would still ship the files even though SfzBuiltins.cs and
         OscFunctions.cs are absent. Modulo Plan 47-03's ModuleLoader gate. -->
    <None Remove="sfz.flow" />
    <None Remove="osc.flow" />
</ItemGroup>
```

(4) Also move the existing `<PackageReference Include="Rug.Osc" Version="1.2.5" />` line at csproj line 13 OUT of the unconditional ItemGroup. Replace it with a new conditional ItemGroup placed RIGHT AFTER the unconditional one:

```xml
<!-- Phase 47 D-47-03: Rug.Osc only referenced when @osc surface compiles in.
     Web target strips OscFunctions.cs + OscHandleData.cs above, so Rug.Osc is
     unused and would otherwise pull dead-reference into the Web build. -->
<ItemGroup Condition="'$(FlowTarget)' != 'Web'">
    <PackageReference Include="Rug.Osc" Version="1.2.5" />
</ItemGroup>
```

Keep DryWetMidi and Pidgin in the unconditional ItemGroup (DryWetMidi WASM-compat verified at Plan 47-04; Pidgin is reference-only per CLAUDE.md "Pidgin referenced but unused — SimpleLexer/Parser are manual").

Do NOT touch the `<InternalsVisibleTo>` ItemGroup, the `<None Update>` ItemGroup at lines 23-98 (except via the Remove entries above), or the empty `<Folder Include>` ItemGroup at line 100-102.

Per D-47-01: keep this all in flow-lang.csproj — do NOT create a separate flow-lang.Web.csproj.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet 2>&amp;1 | tail -5</automated>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet 2>&amp;1 | tail -10</automated>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build flow-lang/flow-lang.csproj -v quiet 2>&amp;1 | tail -5</automated>
    <automated>grep -c "FlowTarget" flow-lang/flow-lang.csproj</automated>
    <automated>grep -c "FLOW_WEB" flow-lang/flow-lang.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '&lt;FlowTarget&gt;Desktop&lt;/FlowTarget&gt;' flow-lang/flow-lang.csproj` returns `1` (exactly one default declaration)
    - Source assertion: `grep -c "Condition=\"'\\$(FlowTarget)' == 'Web'\"" flow-lang/flow-lang.csproj` returns at least `2` (one for DefineConstants PropertyGroup, one for strip-list ItemGroup)
    - Source assertion: `grep "FLOW_WEB" flow-lang/flow-lang.csproj` matches `$(DefineConstants);FLOW_WEB` substring
    - Source assertion: `grep -c 'Compile Remove' flow-lang/flow-lang.csproj` returns `7` (exact strip-list count: 3 audio backends + Sfz glob + OscFunctions + OscHandleData + InputFunctions)
    - Source assertion: `grep -c 'None Remove="Samples' flow-lang/flow-lang.csproj` returns `1`
    - Source assertion: `grep -c 'None Remove="sfz.flow"' flow-lang/flow-lang.csproj` returns `1` and `grep -c 'None Remove="osc.flow"' flow-lang/flow-lang.csproj` returns `1`
    - Build assertion: `dotnet build flow-lang/flow-lang.csproj` (no flag) exits 0
    - Build assertion: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exits 0 — Desktop build builds identically to today
    - Build assertion: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exits 0 OR if it fails, the failure is in C# code that Plans 47-02/03 will fix (FlowEngine.cs references to SfzBuiltins/OscFunctions/InputFunctions; AudioPlaybackManager.cs CoreAudio/PulseAudio references). Plan 47-02/03 close these. If Plan 47-01's Web build fails purely due to the strip list, the build error MUST list ONLY symbols from stripped files (SfzBuiltins, OscFunctions, OscHandleData, CoreAudioBackend, PulseAudioSimpleBackend, PulseAudioCaptureBackend, InputFunctions) — no spurious unrelated failures.
  </acceptance_criteria>
  <done>FlowTarget property + Web-conditional ItemGroups land in flow-lang.csproj; Desktop build is byte-identical; Web build either succeeds OR fails only on symbols Plans 47-02/03 will guard.</done>
</task>

<task type="auto">
  <name>Task 2: Add BuildConditioningSmokeTests (xUnit Fact) pinning both build modes via Process.Start exit-code assertions</name>
  <files>flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs</files>
  <read_first>
    - flow-lang/flow-lang.csproj (after Task 1 edits)
    - flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs (existing precedent — find via `ls flow-lang.Tests/Integration/Phase42/` if needed; otherwise use any Phase42 file as the namespace/style anchor)
  </read_first>
  <action>
Create new file `flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs`:

```csharp
using System.Diagnostics;
using Xunit;

namespace FlowLang.Tests.Integration.Phase47;

/// <summary>
/// Phase 47 Plan 47-01 — Pin acceptance of the FlowTarget=Desktop|Web MSBuild
/// conditioning. Each test shells out to `dotnet build flow-lang/flow-lang.csproj`
/// and asserts exit code 0 in both modes. Per D-47-01..03: single csproj,
/// FLOW_WEB preprocessor symbol, conditional &lt;Compile Remove&gt; strip list.
///
/// These tests run nested-build-of-self (the test process is itself a
/// `dotnet test` invocation). Each test spawns a fresh `dotnet build` against
/// the flow-lang.csproj file alone — independent of the test assembly's
/// own compilation. Test working directory is the repo root, located by
/// walking up from AppContext.BaseDirectory until flow-lang/flow-lang.csproj
/// is found.
/// </summary>
public class BuildConditioningSmokeTests
{
    private static string FindRepoRoot()
    {
        var dir = new DirectoryInfo(AppContext.BaseDirectory);
        while (dir != null)
        {
            if (File.Exists(Path.Combine(dir.FullName, "flow-lang", "flow-lang.csproj")))
                return dir.FullName;
            dir = dir.Parent;
        }
        throw new InvalidOperationException(
            "Could not locate repo root from " + AppContext.BaseDirectory);
    }

    private static (int exitCode, string stdout, string stderr) RunDotnetBuild(string args)
    {
        var repoRoot = FindRepoRoot();
        var psi = new ProcessStartInfo
        {
            FileName = "dotnet",
            Arguments = "build flow-lang/flow-lang.csproj " + args + " -v quiet --nologo",
            WorkingDirectory = repoRoot,
            RedirectStandardOutput = true,
            RedirectStandardError = true,
            UseShellExecute = false,
        };
        using var p = Process.Start(psi)!;
        string stdout = p.StandardOutput.ReadToEnd();
        string stderr = p.StandardError.ReadToEnd();
        p.WaitForExit(120_000);
        return (p.ExitCode, stdout, stderr);
    }

    [Fact]
    public void DesktopBuild_ExitCodeIsZero()
    {
        var (code, stdout, stderr) = RunDotnetBuild("-p:FlowTarget=Desktop");
        Assert.True(code == 0,
            $"Expected exit 0 with FlowTarget=Desktop, got {code}.\nSTDOUT:\n{stdout}\nSTDERR:\n{stderr}");
    }

    [Fact]
    public void DefaultBuild_ExitCodeIsZero_AndImpliesDesktop()
    {
        var (code, stdout, stderr) = RunDotnetBuild("");
        Assert.True(code == 0,
            $"Expected exit 0 with no FlowTarget arg (default=Desktop per D-47-01), got {code}.\nSTDOUT:\n{stdout}\nSTDERR:\n{stderr}");
    }

    [Fact]
    public void WebBuild_ExitCodeIsZero()
    {
        // Phase 47 D-47-01..03: Web build must link cleanly after Plans 47-02 + 47-03
        // wire the WebAudioBackend stub + FlowEngine/BuiltInFunctions guards.
        // If this test is run after Plan 47-01 alone (before 47-02/03), it may
        // fail with C# CS0246 (missing SfzBuiltins / OscFunctions / etc.) — that's
        // expected; the failure surfaces in 47-02/03 acceptance. The csproj
        // condition itself MUST NOT error at MSBuild eval time.
        var (code, stdout, stderr) = RunDotnetBuild("-p:FlowTarget=Web");
        Assert.True(code == 0,
            $"Expected exit 0 with FlowTarget=Web, got {code}.\nSTDOUT:\n{stdout}\nSTDERR:\n{stderr}");
    }
}
```

Notes on the test:
- `FindRepoRoot()` walks up from `AppContext.BaseDirectory` to locate the repo root — works regardless of `dotnet test` invocation cwd.
- `RunDotnetBuild("-p:FlowTarget=Web")` is the load-bearing assertion; the other two protect against regression of Desktop default behavior.
- 120-second timeout covers a cold restore on Web target.
- `[Fact]` (not `[FlowTargetFact]`) — these tests ALWAYS run; they validate the build conditioning itself, not a Web-target runtime feature. The `[FlowTargetFact]` attribute lands in Plan 47-04.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build flow-lang.Tests/flow-lang.Tests.csproj -v quiet 2>&amp;1 | tail -5</automated>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~BuildConditioningSmokeTests.DesktopBuild_ExitCodeIsZero" --no-build 2>&amp;1 | tail -10</automated>
    <automated>test -f flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs &amp;&amp; echo FILE_EXISTS</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file `flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs` exists
    - Source assertion: `grep -c "FlowTarget=Web" flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs` returns at least `1`
    - Source assertion: `grep -c "FlowTarget=Desktop" flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs` returns at least `1`
    - Source assertion: 3 `[Fact]` methods present (`DesktopBuild_ExitCodeIsZero`, `DefaultBuild_ExitCodeIsZero_AndImpliesDesktop`, `WebBuild_ExitCodeIsZero`)
    - Build assertion: `flow-lang.Tests` builds (so the new test file compiles)
    - Behavior assertion: `dotnet test ... --filter ...DesktopBuild_ExitCodeIsZero` reports 1 passed. (WebBuild Fact may stay RED until Plans 47-02 + 47-03 close — acceptable; Plan 47-06 verifies GREEN at phase closure.)
  </acceptance_criteria>
  <done>BuildConditioningSmokeTests.cs lands with 3 Facts; Desktop + default-build Facts pass GREEN; WebBuild Fact GREEN once Plan 47-03 closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 47 is a build-time refactor. No runtime input perimeter changes. No new attack surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-SC | Tampering | npm/pip/cargo installs | n/a | This plan adds zero package references. The existing Rug.Osc reference is MOVED into a conditional ItemGroup; no new supply chain dependency. |

No new attack surface in Plan 47-01.
</threat_model>

<verification>
- `dotnet build flow-lang/flow-lang.csproj` succeeds (default = Desktop preservation)
- `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` succeeds (explicit Desktop)
- `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` either succeeds OR fails only on missing-symbol errors (SfzBuiltins / OscFunctions / OscHandleData / CoreAudioBackend / PulseAudioSimpleBackend / PulseAudioCaptureBackend / InputFunctions) that Plans 47-02 + 47-03 close
- BuildConditioningSmokeTests compiles; Desktop + default Facts run GREEN
</verification>

<success_criteria>
- `<FlowTarget>Desktop</FlowTarget>` lands in flow-lang.csproj PropertyGroup
- `<DefineConstants>$(DefineConstants);FLOW_WEB</DefineConstants>` lands in a Web-conditional PropertyGroup
- 7 `<Compile Remove>` entries land in a Web-conditional ItemGroup (matching actual code paths, NOT the misnamed paths in CONTEXT.md)
- `<Content Remove>` + `<None Remove>` entries for Samples + sfz.flow + osc.flow land
- Rug.Osc moves to a `Condition="'$(FlowTarget)' != 'Web'"` ItemGroup
- BuildConditioningSmokeTests.cs with 3 Facts lands and Desktop+default Facts pass
</success_criteria>

<output>
Create `.planning/phases/47-compile-target-flavors/47-01-SUMMARY.md` when done.
</output>
