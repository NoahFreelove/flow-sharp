---
phase: 26.2
slug: music-type-ergonomics-fx-overloads-inserted
status: shipped
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-10
shipped: 2026-05-10
---

# Phase 26.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit (existing — `flow-lang.Tests/flow-lang.Tests.csproj`) |
| **Config file** | `flow-lang.Tests/flow-lang.Tests.csproj` |
| **Quick run command** | `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase26_2"` |
| **Full suite command** | `dotnet test` |
| **Flow-script smoke** | `dotnet run --project flow-interpreter tests/test_NAME.flow` |
| **Estimated runtime** | ~5 min full; ~30s per filtered Phase26_2 |

---

## Sampling Rate

- **After every task commit:** Run the relevant `*Facts` filter (e.g. `dotnet test --filter "FullyQualifiedName~MusicTypeNumericCompatFacts"`).
- **After every plan wave:** Run `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase26_2"` PLUS the Phase 18/25 byte-identical regression filters.
- **Before `/gsd-verify-work`:** Full `dotnet test` suite must be green (with the 2 pre-existing `DecibelBeatNumericCompatFacts` flipped to GREEN by Wave 0).
- **Max feedback latency:** ~30 seconds per filtered run; ~5 min for full suite.

---

## Per-Task Verification Map

> Authoritative test list lives in 26.2-RESEARCH.md "Validation Architecture" → "Phase Requirements → Test Map". This file pins the policy and Wave 0 scaffolding requirements.

| Phase Behavior | Requirement | Test Type | Automated Command | Status |
|----------------|-------------|-----------|-------------------|--------|
| Ms/Sec IsCompatibleWith(Double|Float) (4 facts) | ERG-01 | unit | `dotnet test --filter "MusicTypeNumericCompatFacts"` | ⬜ pending |
| Semitone STAYS Int-only (regression canary) | ERG-01 | unit | `dotnet test --filter "MusicTypeNumericCompatFacts.Semitone_NotCompatibleWith_Double"` | ⬜ pending |
| Hertz IsCompatibleWith(Double|Float) | ERG-04 | unit | `dotnet test --filter "MusicTypeNumericCompatFacts.Hertz_IsCompatibleWith_"` | ⬜ pending |
| FX Ms/Decibel/Second overloads byte-identical equiv | ERG-02 | engine-eval | `dotnet test --filter "MusicTypeFXOverloadFacts"` | ⬜ pending |
| `volume(Buffer, Double)` linear semantics + clip warning | ERG-03 | engine-eval | `dotnet test --filter "VolumeFunctionFacts"` | ⬜ pending |
| `gain(Buffer, 0.5)` STAYS dB (D-07 canary) | ERG-03 | engine-eval | `dotnet test --filter "VolumeFunctionFacts.Gain_BareDoubleSubOne_StillDecibel"` | ⬜ pending |
| Hertz literal lex (`800Hz`, `1.5kHz`) → canonical Hz | ERG-04 | unit | `dotnet test --filter "HertzLiteralFacts"` | ⬜ pending |
| Hertz overloads on filters/createXxxTone byte-identical | ERG-04 | engine-eval | `dotnet test --filter "HertzFXOverloadFacts"` | ⬜ pending |
| `(gain src -12dB)` engine-eval flips RED→GREEN | ERG-05 (D-14) | engine-eval | `dotnet test --filter "DecibelBeatNumericCompatFacts.GainWith"` | 🟥 RED → ⬜ pending |
| Phase 18 byte-identical showcase regression | (preserve) | engine-eval | `dotnet test --filter "Phase18ByteIdenticalShowcaseTests"` | ✅ green (must stay) |
| Phase 25 byte-identical Gaussian regression | (preserve) | engine-eval | `dotnet test --filter "Phase25ByteIdenticalShowcaseGaussianTests"` | ✅ green (must stay) |
| FlowScriptTests theory rows | (preserve) | engine-eval | `dotnet test --filter "FlowScriptTests"` | ✅ green (must stay) |

*Status: ⬜ pending · ✅ green · 🟥 red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `flow-lang.Tests/Unit/Phase26_2/MusicTypeNumericCompatFacts.cs` — IsCompatibleWith facts for Ms/Sec/Semitone/Hertz
- [ ] `flow-lang.Tests/Unit/Phase26_2/MusicTypeFXOverloadFacts.cs` — engine-eval facts for delay/compress/sidechain/reverb music-type overloads
- [ ] `flow-lang.Tests/Unit/Phase26_2/VolumeFunctionFacts.cs` — volume linear semantics + clip warning + gain dB-canary
- [ ] `flow-lang.Tests/Unit/Phase26_2/HertzLiteralLexingFacts.cs` — Hz/kHz lexer + canonical Hz value
- [ ] `flow-lang.Tests/Unit/Phase26_2/HertzFXOverloadFacts.cs` — engine-eval facts for filter / createXxxTone Hertz overloads
- [ ] `flow-lang.Tests/Unit/Phase26_2/NegativeMusicLiteralFacts.cs` — `-12dB`/`+6dB`/`-100ms`/`-50c`/`+440Hz` at expression-start (mirror of DecibelBeatNumericCompatFacts.cs lex pattern)

*xUnit framework already installed; no new dependencies needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CLAUDE.md gain-vs-volume documentation reads correctly | ERG-03 | Prose review | After Wave 5, read CLAUDE.md "Language Features → Core" — verify the `gain` (dB) vs `volume` (linear) split is described and the Hertz literal syntax is mentioned |
| REQUIREMENTS.md ERG-01..05 entries match landed scope | (closure) | Doc cross-ref | After Wave 5, read REQUIREMENTS.md ERG-* entries — verify each acceptance idiom matches what the engine-eval facts pin |

*All other behaviors have automated verification.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (6 new `*Facts.cs` files in Wave 0)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s per filtered run
- [ ] `nyquist_compliant: true` set in frontmatter on closure
- [ ] Phase 18 + Phase 25 byte-identical regression contracts verified GREEN at every wave boundary

**Approval:** approved 2026-05-10 (planner + executor + checker per GSD).
