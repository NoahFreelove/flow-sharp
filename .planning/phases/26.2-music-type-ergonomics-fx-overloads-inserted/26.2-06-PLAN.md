---
phase: 26.2-music-type-ergonomics-fx-overloads-inserted
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md
  - .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md
  - CLAUDE.md
autonomous: true
requirements: [ERG-01, ERG-02, ERG-03, ERG-04, ERG-05]
must_haves:
  truths:
    - "REQUIREMENTS.md gains 5 new ERG-01..ERG-05 entries under a new '### Music Type Ergonomics + FX Overloads' section, mirroring the Phase 26.1 closure pattern (DICT-01/02/03 rewrite); each entry has Acceptance idioms matching what the Phase26_2 facts pin"
    - "REQUIREMENTS.md Traceability table gains 5 new rows: ERG-01 through ERG-05 → Phase 26.2 → Shipped <commit-hash>"
    - "ROADMAP.md Phase 26.2 entry: Plans count updated from 'TBD' to '6 plans' with the plan list (26.2-01..26.2-06) marked [x] with shipped-hash annotations"
    - "ROADMAP.md Phase 26.2 entry: phase-summary checkbox flips from [ ] to [x] with the closure note `— Shipped 2026-05-10 (Waves 0-5: <hash-01> + <hash-02> + <hash-03> + <hash-04> + <hash-05> + <hash-06>)`"
    - "ROADMAP.md Progress table Phase 26.2 row updated from `0/N | Not started | -` to `6/6 | Complete | 2026-05-10`"
    - "STATE.md current-position advances to 'Phase 26.2 fully shipped 2026-05-10' with Resume Instructions pointing to Phase 27 (Tutorial + Showcase Refresh) as the next ROADMAP target"
    - "STATE.md progress.completed_phases incremented from 10 to 11; progress.completed_plans incremented from 48 to 54 (48 + 6 new plans from this phase)"
    - "26.2-VERIFICATION.md exists with full plan-by-plan must-have evidence, regression-gate confirmations, and the Sign-Off section"
    - "26.2-VALIDATION.md frontmatter status: shipped, nyquist_compliant: true, wave_0_complete: true; Sign-Off section approved"
    - "CLAUDE.md Core Language Features section gains 2 new bullets: gain=dB / volume=linear split (Phase 26.2 ERG-03), Hertz literal syntax `440Hz` / `1.5kHz` (Phase 26.2 ERG-04)"
    - "CLAUDE.md Special Types list (Note/Semitone/Cent/Millisecond/Second/Decibel/Beat/...) gains `Hertz` (Phase 26.2 ERG-04)"
    - "CLAUDE.md Audio Effects bullet under '### Music-Specific' adds `volume` next to existing `gain` mention with the dB-vs-linear distinction"
    - "examples/tutorial.flow + examples/showcase.flow NOT modified — Phase 27 owns the chapter additions per CONTEXT § Specifics last bullet + RESEARCH § Validation Architecture"
    - "Phase 18 ByteIdenticalShowcaseTests + Phase 25 ByteIdenticalShowcaseGaussianTests STILL GREEN — confirmed in 26.2-VERIFICATION.md"
    - "Full dotnet test suite green — the 2 pre-existing DecibelBeatNumericCompatFacts that STATE.md flagged as Phase 26.2's closure target are now GREEN (Wave 0 + Wave 2 fixes)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ERG-01..ERG-05 entries authored under '### Music Type Ergonomics + FX Overloads' section + Traceability table rows"
      contains: "ERG-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26.2 marked Complete with full plan list + commit hashes; Progress table updated"
      contains: "Phase 26.2"
    - path: ".planning/STATE.md"
      provides: "Current-position advances to Phase 27 next; progress counters incremented"
      contains: "Phase 26.2 fully shipped"
    - path: ".planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md"
      provides: "Closure sign-off with full plan-by-plan must-have evidence"
      contains: "Phase 26.2 — Verification"
      min_lines: 60
    - path: "CLAUDE.md"
      provides: "Core Language Features bullets for gain/volume split + Hertz literals; Special Types list adds Hertz; Audio Effects mentions volume"
      contains: "Hertz"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "ERG-01..ERG-05 entries match ROADMAP Phase 26.2 success criteria 1-5 + the new ERG-05 sub-criterion"
      pattern: "ERG-0[1-5]"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Current-position field references Phase 26.2 complete; Phase 27 next"
      pattern: "Phase 26\\.2|Phase 27"
    - from: "CLAUDE.md"
      to: "flow-lang/TypeSystem/SpecialTypes/HertzType.cs"
      via: "Special Types list entry references the actual file by Type-name"
      pattern: "Hertz"
---

<objective>
Wave 5 — closure. Pure documentation updates: REQUIREMENTS.md ERG-01..ERG-05 authored per CONTEXT D-17 (mirrors Phase 26.1's DICT-01/02/03 closure pattern); ROADMAP.md Phase 26.2 marked Complete with plan list and shipped-hash annotations; STATE.md current-position advanced; 26.2-VERIFICATION.md authored with full plan-by-plan evidence; 26.2-VALIDATION.md frontmatter flipped to shipped; CLAUDE.md gains Core Language Features bullets + Special Types list entry for Hertz + Audio Effects mention of volume.

DO NOT modify `examples/tutorial.flow` or `examples/showcase.flow` — Phase 27 owns those. Touching them risks byte-identical regression on the Phase 18 + 25 guards. The tutorial-side ergonomics chapter belongs to Phase 27 per CONTEXT § Specifics last bullet + RESEARCH § Validation Architecture.

Purpose: Standard closure pattern (Phase 20-04 / 21-03 / 22-07 / 23-05 / 25-04 / 26-05 / 26.1-06 precedent). The closure plan is docs-only and atomic — separates feature commits from documentation commits per the v1.3 closure-plan precedent.

Output: All v1.3 milestone tracking artifacts agree on Phase 26.2's completion. Phase 27 is the next ROADMAP target (the milestone closer). The full test suite is green; byte-identical regression remains intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-CONTEXT.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-RESEARCH.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-01-SUMMARY.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-02-SUMMARY.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-03-SUMMARY.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-04-SUMMARY.md
@.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-05-SUMMARY.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-06-PLAN.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md

<interfaces>
<!-- Closure-doc shapes the executor must mirror. -->

REQUIREMENTS.md insertion point — after the existing "### Dictionary Support" section (around lines 105-112), add a new section:
```markdown
### Music Type Ergonomics + FX Overloads

- [x] **ERG-01**: ...
- [x] **ERG-02**: ...
- [x] **ERG-03**: ...
- [x] **ERG-04**: ...
- [x] **ERG-05**: ...
```

REQUIREMENTS.md Traceability table — insert 5 new rows for ERG-01..ERG-05 in the existing table (around lines 175-185).

ROADMAP.md Phase 26.2 entry (around lines 246-258 — currently shows `Plans: TBD` and not-started) — Plans + plan list + Progress table row.

STATE.md frontmatter (lines 1-15) — `progress.completed_phases`, `progress.completed_plans`, `last_activity` fields.
STATE.md Current Position section (around lines 17-35) — replace Phase 26.1 status with Phase 26.2 fully shipped.
STATE.md Resume Instructions (around lines 36-45) — point at Phase 27 next.

CLAUDE.md Core Language Features section (locate via `grep -n "Static typing" CLAUDE.md`) — add 2 new bullets.

CLAUDE.md Special Types list (locate via `grep -n "Note, Semitone, Cent" CLAUDE.md`) — add `Hertz` to the comma-separated list.

CLAUDE.md Audio Effects mention (locate via `grep -n "gain" CLAUDE.md` — the bullet describing audio effects under `### Music-Specific`) — add volume alongside gain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author REQUIREMENTS.md ERG-01..ERG-05 + Traceability rows; update ROADMAP.md Phase 26.2 entry; update STATE.md current position</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 95-115 (locate the "### Symbols, Tuples, and (unpack)" + "### Dictionary Support" sections — the new "### Music Type Ergonomics + FX Overloads" section inserts immediately after Dictionary Support)
    - .planning/REQUIREMENTS.md lines 175-185 (Traceability table — append 5 new ERG-01..ERG-05 rows after the existing DICT-03 row)
    - .planning/ROADMAP.md lines 246-258 (Phase 26.2 entry currently has `**Plans**: TBD` — fully populate it with the plan list + commit hashes)
    - .planning/ROADMAP.md lines 290-302 (Progress table — find Phase 26.2 row showing `0/N | Not started | -`; flip to `6/6 | Complete | 2026-05-10`)
    - .planning/ROADMAP.md line 73-74 (the v1.3 phase summary list — Phase 26.2 entry currently `[ ]` not started; flip to `[x]` with shipped-hash annotation)
    - .planning/STATE.md lines 1-15 (frontmatter `progress.*` counters)
    - .planning/STATE.md lines 17-50 (Current Position section + Resume Instructions)
    - .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-CONTEXT.md § "REQUIREMENTS.md authoring" (D-17 — the canonical text for ERG-01..ERG-05)
    - .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-RESEARCH.md § "Phase Requirements" (lines 80-89 — ID descriptions and acceptance shape)
    - All 5 plan SUMMARY.md files from 26.2-01..26.2-05 (extract actual commit hashes for the Traceability rows; if SUMMARY.md hasn't been written yet for a given wave, fall back to `git log --oneline -10` filtered by `26\\.2`)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-06-PLAN.md (the closure-pattern model from Phase 26.1 — same shape applies here)
  </read_first>
  <action>
**1. `.planning/REQUIREMENTS.md`** — Insert the new "### Music Type Ergonomics + FX Overloads" section after the existing Dictionary Support section.

Locate `### Dictionary Support` (around line 105). After the last DICT-03 entry (around line 111), insert a blank line and then:

```markdown
### Music Type Ergonomics + FX Overloads

- [x] **ERG-01**: Music-type numeric compatibility completeness. `Millisecond` and `Second` ship `IsCompatibleWith(Double|Float)` overrides (mirroring the existing `CentType.cs:24-27` precedent that `Decibel` and `Beat` adopted via QUICK-260504-w24). `Semitone` STAYS Int-only — semitones are whole-numbers-by-design; fractional pitch shifts go through `Cent`. Existing `Millisecond.CanConvertTo(Second)` + `Second.CanConvertTo(Millisecond)` cross-conversions PRESERVED — `(delay buf 0.1s ...)` continues to reach `delay(Buffer, Millisecond, ...)` via convertible-score 100. Acceptance: `MillisecondType.Instance.IsCompatibleWith(DoubleType.Instance)` returns true; `SecondType.Instance.IsCompatibleWith(FloatType.Instance)` returns true; `SemitoneType.Instance.IsCompatibleWith(DoubleType.Instance)` returns false (D-03 canary); `(delay buf 100.0 0.5 0.4)` and `(delay buf 100ms 0.5 0.4)` both resolve. — Shipped <wave-1-hash>

- [x] **ERG-02**: FX overload registration on every site where the parameter is conceptually musical. New music-typed overloads ship for: `delay(Buffer, Millisecond, Double, Double)`, `compress(Buffer, Decibel, Double, Millisecond, Millisecond)`, `sidechain(Buffer, Buffer, Decibel, Double, Millisecond, Millisecond)`, `reverb(Buffer, Double, Second)`, `lowpass(Buffer, Hertz)` / `highpass(Buffer, Hertz)` / `bandpass(Buffer, Hertz, Hertz)`, `createSineTone(Double, Hertz, Double)` (C# overload), and Flow-side proc overloads for `createSawTone` / `createSquareTone` / `createTriangleTone` with Hertz frequency parameter. Bare-Double overloads PRESERVED — coexist via OverloadResolver exact-match scoring (1000 vs compat-500). Reverb-Second overload does NOT ambiguate with `reverb(Buffer, Double, Double)` per RESEARCH Pitfall 3 score arithmetic. Acceptance: `(compress buf -12dB 4.0 5ms 100ms)` produces per-sample-identical output to `(compress buf -12.0 4.0 5.0 100.0)` within 1e-6f; `(reverb buf 0.5 1.5)` and `(reverb buf 0.5 1.5s)` resolve to distinct overloads (no Ambiguous overload error). — Shipped <wave-3-hash>

- [x] **ERG-03**: `gain` dB-vs-linear policy decided + new `volume(Buffer, Double)` shipping. `gain` STAYS dB-only — both `gain(Buffer, Double)` (existing) and `gain(Buffer, Decibel)` (existing, shipped via QUICK-260504-w24 + audio.flow forward decl shipped here) treat second arg as decibels. New `volume(Buffer, Double)` treats second arg as linear multiplier (0.5 = half-amplitude, 2.0 = double-amplitude). Function name documents the unit; composer chooses by semantic intent. ONE overload — Float / Int / Long inputs reach it via existing primitive widening chain. Negative values rejected via `InvalidOperationException` (volume can't phase-invert; out of scope). Clipping warning emitted to stderr when post-multiplication samples exceed 1.0 (mirrors GainEffect shape). NO educational hint when `(gain buf 0.5)` is called with arg in `(0, 1)` — `(gain buf 0.5)` is a legitimate 0.5dB attenuation. CLAUDE.md updated to document the split. Acceptance: `(volume buf 0.5)` halves amplitude per non-zero sample; `(volume buf 2.0)` doubles + emits clipping warning; `(volume buf -0.5)` errors with InvalidOperationException; `(gain buf 0.5)` STAYS at 0.5dB attenuation (~5.9% louder than 0dB unity), NOT 50% as a linear interpretation would produce. — Shipped <wave-4-hash>

- [x] **ERG-04**: `Hertz` type ships with `Hz` + `kHz` literal syntax + filter / generator overloads. New `HertzType` in `flow-lang/TypeSystem/SpecialTypes/HertzType.cs` mirrors `CentType` exactly (sealed FlowType singleton, `IsCompatibleWith(Double|Float)`, `GetSpecificity()=144` unique among music types). Stored as a single canonical Hz double (kHz × 1000 at lex time — no unit-discriminator at runtime). Both `Hz` and `kHz` suffixes lex as single `HertzLiteral` tokens via three coordinated paths in `SimpleLexer.cs`: `ScanNumberOrSpecialLiteral` (unsigned), `TryLookAheadSpecialLiteral` (signed prefix), and the `TryLexAngleAngle` predecessor set bumped to include HertzLiteral so `<<800Hz, 1200Hz>>` tuples parse. `mHz` (millihertz) NOT shipped in 26.2 — defer until LFOs land. Hertz overload coverage spans filters (`lowpass` / `highpass` / `bandpass`) + signal generators (`createSineTone` C#; `createSawTone` / `createSquareTone` / `createTriangleTone` Flow-side proc overloads in audio.flow). NO PitchConversion APIs added — Open Question #1: `noteToFrequency` only RETURNS Hz, doesn't take Hz; no Hz-taking PitchConversion API exists. Acceptance: `Hertz freq = 800Hz; (eq freq 800.0)` is true; `Hertz freq = 1.5kHz; (eq freq 1500.0)` is true; `(lowpass buf 800Hz)` produces per-sample-identical output to `(lowpass buf 800.0)`. — Shipped <wave-1-hash> + <wave-2-hash> + <wave-3-hash>

- [x] **ERG-05**: `-12dB` / `-100ms` / `-50c` / `+440Hz` literal lexing at expression-start positions closure (D-14). Root cause traced via RESEARCH Pitfall 1 to a missing `if (targetType is DoubleType) return Double(doubleVal);` arm in `Value.cs:155-161` (NOT a lexer bug as the original CONTEXT hypothesized). Defence-in-depth fix: (a) `Value.ConvertTo` Double-arm patch shipped in Wave 0 covers Decibel→Double / Beat→Double / Cent→Double / Ms→Double / Sec→Double / Hertz→Double coercion in any user-proc / lambda call site; (b) `audio.flow` `internal proc gain(Buffer: buffer, Decibel: gainDb)` forward declaration shipped in Wave 2 surfaces the dormant C# `gain(Buffer, Decibel)` registration (RESEARCH Pitfall 2) so the dedicated Decibel overload now wins resolution at exact-match score 1000. Both fix paths exist for redundancy. The 2 pre-existing failing `DecibelBeatNumericCompatFacts` (`GainWithDecibelLiteral_…` + `GainWithPositiveDecibelLiteral_…`) flip RED→GREEN. Sibling regression facts pin `+6dB`, `-100ms`, `-50c`, `+440Hz` at the `LParen-after` expression-start position. Acceptance: `(gain src -12dB)` produces per-sample-identical output to `(gain src -12.0)` within 1e-6f; `(transpose seq -50c)` resolves cleanly (Cent canary); `(lowpass buf +440Hz)` resolves cleanly. — Shipped <wave-0-hash> + <wave-2-hash>
```

Replace each `<wave-N-hash>` placeholder with the actual commit hash. Use `git log --oneline -10 | grep "26\\.2"` to extract the commits — they should be 5 feature commits + this closure commit. Order: Wave 0 (test scaffolding + Value.ConvertTo), Wave 1 (type-system), Wave 2 (lexer + audio.flow forward decl), Wave 3 (FX overloads), Wave 4 (volume). For ERG-04 and ERG-05 which span multiple waves, list all relevant hashes joined by " + ".

ALSO append 5 new rows to the Traceability table (around lines 175-185, after the existing DICT-03 row):
```
| ERG-01 | Phase 26.2 | Shipped <wave-1-hash> |
| ERG-02 | Phase 26.2 | Shipped <wave-3-hash> |
| ERG-03 | Phase 26.2 | Shipped <wave-4-hash> |
| ERG-04 | Phase 26.2 | Shipped <wave-1-hash> + <wave-2-hash> + <wave-3-hash> |
| ERG-05 | Phase 26.2 | Shipped <wave-0-hash> + <wave-2-hash> |
```

**2. `.planning/ROADMAP.md`** — Three coordinated edits:

  **2a. Phase 26.2 entry (lines 246-258).** Update the `**Plans**: TBD` line to `**Plans**: 6 plans`. Append the plan list immediately below:
  ```
  - [x] 26.2-01-PLAN.md — Wave 0 RED scaffolding (6 *Facts.cs files) + Value.ConvertTo Double-arm patch (closes the 2 RED DecibelBeat facts) — Shipped <hash-01>
  - [x] 26.2-02-PLAN.md — Wave 1: Ms/Sec/Hertz IsCompatibleWith(Double|Float) + Value.Hertz factory + new HertzType (ERG-01 + ERG-04 type-level) — Shipped <hash-02>
  - [x] 26.2-03-PLAN.md — Wave 2: Hz/kHz lexer arms + Parser HertzLiteral route + audio.flow gain(Decibel) forward decl (ERG-04 + ERG-05 closure) — Shipped <hash-03>
  - [x] 26.2-04-PLAN.md — Wave 3: FX music-typed overloads (delay-Ms, compress/sidechain-Decibel-Ms, reverb-Second, lowpass/highpass/bandpass-Hertz, createXxxTone-Hertz family) — ERG-02 + ERG-04 — Shipped <hash-04>
  - [x] 26.2-05-PLAN.md — Wave 4: volume(Buffer, Double) linear-multiplier alternative to gain(dB) — ERG-03 (D-04..D-07) — Shipped <hash-05>
  - [x] 26.2-06-PLAN.md — Wave 5: Closure (REQUIREMENTS/ROADMAP/STATE/CLAUDE.md/26.2-VERIFICATION.md) — Shipped <hash-06>
  ```

  **2b. Phase row checkbox** (line 73-74 — the v1.3 phase summary list). Change `- [ ] **Phase 26.2: Music Type Ergonomics + FX Overloads (INSERTED)** ...` to `- [x] **Phase 26.2: Music Type Ergonomics + FX Overloads (INSERTED)** ...`. Append closure note: `— Shipped 2026-05-10 (Waves 0-5: <hash-01> + <hash-02> + <hash-03> + <hash-04> + <hash-05> + <hash-06>)`.

  **2c. Progress table** (around lines 290-302). Update the Phase 26.2 row from `0/N | Not started | -` to `6/6 | Complete | 2026-05-10`.

**3. `.planning/STATE.md`** — Three coordinated edits:

  **3a. Frontmatter `current-position` / `last-activity` / `progress.*` fields** at the top (around lines 1-15). Update:
  - `status: "Phase 26.2 fully shipped 2026-05-10. Music-type ergonomics + FX overloads (Decibel/Beat/Cent/Ms/Sec already shipped IsCompatibleWith via QUICK-260504-w24; this phase added Ms/Sec/Hertz IsCompatibleWith + new HertzType + 5 FX music-typed overloads (delay-Ms, compress/sidechain-Decibel-Ms, reverb-Second, lowpass/highpass/bandpass-Hertz) + createXxxTone-Hertz family + new volume(Buffer, Double) linear-multiplier function alongside gain (which stays dB-only). Closes the 2 pre-existing RED DecibelBeatNumericCompatFacts via Value.ConvertTo Double-arm + audio.flow gain(Decibel) forward decl (defence-in-depth). Phase 18 ByteIdenticalShowcaseTests + Phase 25 ByteIdenticalShowcaseGaussianTests STILL GREEN. v1.3 milestone has 1 phase remaining: Phase 27 (Tutorial + Showcase Refresh)."`
  - `stopped_at: Phase 26.2 fully shipped`
  - `last_activity: 2026-05-10 - Phase 26.2 closure (plan 26.2-06)`
  - `progress.completed_phases: 11` (was 10)
  - `progress.completed_plans: 54` (was 48; +6 new plans from this phase)

  **3b. Current Position section** (around lines 17-35). Replace the Phase 26.1 status block with:
  ```
  Milestone: v1.3 Composer DX Tier B/C
  Phase: 26.2 (music-type-ergonomics-fx-overloads-inserted) — COMPLETE 2026-05-10
  Plan: 6 of 6 (26.2-01..26.2-06 all shipped — ERG-01, ERG-02, ERG-03, ERG-04, ERG-05)
  Status: Phase 26.2 fully shipped 2026-05-10. Music-type ergonomics gap closed: Ms/Sec/Hertz IsCompatibleWith(Double|Float); FX overloads on delay/compress/sidechain/reverb/lowpass/highpass/bandpass + createXxxTone family; new volume(Buffer, Double) linear-multiplier function with negative-rejection + clipping warning; gain stays dB-only with new gain(Buffer, Decibel) audio.flow forward decl; 2 pre-existing RED DecibelBeatNumericCompatFacts CLOSED via Value.ConvertTo Double-arm + dedicated overload (defence-in-depth). Phase 18 + 25 byte-identical regression contracts intact. v1.3 milestone has 1 phase remaining: Phase 27 (Tutorial + Showcase Refresh).
  Last activity: 2026-05-10 - Phase 26.2 closure (plan 26.2-06)
  ```

  **3c. Resume Instructions** (around lines 36-45). Replace with: "Phase 26.2 fully shipped 2026-05-10. v1.3 milestone now 11/12 phases complete. Next ROADMAP target is Phase 27 (Tutorial + Showcase Refresh) — the v1.3 milestone closer that will exercise every v1.3 feature end-to-end in tutorial.flow + showcase.flow including the Phase 26.2 music-type ergonomics."

  Phase 17 HUMAN-UAT note (rows 1-3 of manual-smoke.md) carries forward unchanged — orthogonal to v1.3 work.

  ALSO append a Performance Metrics row to the table at lines 110+ for Phase 26.2's 6 plans (use placeholders if execution-time data isn't available: `Phase 26.2 P01..P06 | ~Xmin | N tasks | M files`).

**4. Verification within this task: do not commit yet.** Re-read the 3 modified files after editing to confirm:
- ERG-01..ERG-05 entries present in REQUIREMENTS.md with all 5 hashes filled in
- ROADMAP plan list has 6 entries with correct commit hashes
- STATE.md current-position references Phase 26.2 complete and Phase 27 next

Commit at the end of Task 2 alongside the CLAUDE.md and verification doc updates.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; git log --oneline -10 &amp;&amp; grep -E "ERG-0[1-5]|Phase 26\\.2" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \\[x\\] \\*\\*ERG-0" .planning/REQUIREMENTS.md` returns at least `5` (5 new ERG entries shipped checkboxes)
    - `grep -c "Music Type Ergonomics" .planning/REQUIREMENTS.md` returns at least `1` (new section header present)
    - `grep -E "ERG-0[1-5].*Phase 26.2.*Shipped" .planning/REQUIREMENTS.md | wc -l` returns at least `5` (Traceability rows)
    - `grep -c "26.2-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns at least `6` (plan list entries)
    - `grep -E "Phase 26\\.2.*Complete|Phase 26\\.2.*Shipped" .planning/ROADMAP.md` returns at least `1` (Phase 26.2 marked complete in Progress table or phase-summary list)
    - `grep -E "Phase 26\\.2.*COMPLETE|Phase 26\\.2 fully shipped" .planning/STATE.md` returns at least `1`
    - `grep -E "Phase 27.*next|Phase 27.*remaining|Phase 27.*Tutorial" .planning/STATE.md` returns at least `1` (Resume points forward)
    - `grep "completed_phases: 11" .planning/STATE.md | wc -l` returns `1`
    - `grep "completed_plans: 54" .planning/STATE.md | wc -l` returns `1`
    - `git log --oneline -10` shows 5 Phase 26.2 feature commits (Waves 0-4) prior to this closure task
  </acceptance_criteria>
  <done>REQUIREMENTS.md ERG-01..ERG-05 authored under new section + Traceability rows shipped; ROADMAP.md Phase 26.2 marked Complete with plan list + commit hashes; STATE.md advances to Phase 27-as-next.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md (Core Language Features bullets + Special Types Hertz + Audio Effects volume); author 26.2-VERIFICATION.md; flip 26.2-VALIDATION.md frontmatter; commit Wave 5 closure</name>
  <files>
    CLAUDE.md,
    .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md,
    .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md
  </files>
  <read_first>
    - CLAUDE.md full file (~280 lines — locate the Core Language Features section, Special Types list, and Audio Effects mention via grep)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md (TEMPLATE for closure verification — most recent precedent at ~50+ lines)
    - .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md (alternate template — same shape)
    - .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md (frontmatter to flip to shipped)
    - All 5 plan SUMMARY.md files from 26.2-01..26.2-05 (the cross-references for VERIFICATION.md must-have audit)
  </read_first>
  <action>
**1. `CLAUDE.md`** — Three coordinated edits:

  **1a. Core Language Features section.** Locate the bulleted list under `### Core` (use `grep -n "Static typing" CLAUDE.md` to find the section start). The Phase 26.1 closure (commit 41fd4ab) added bullets for Symbol / Tuple / `~>` / Dict / `(unpack)` at the end of this list — find that block and append 2 NEW bullets:

  ```markdown
  - **`gain` vs `volume` distinction:** `gain(Buffer, Double|Decibel)` interprets its 2nd arg as decibels (negative = attenuate, positive = amplify); `volume(Buffer, Double)` interprets its 2nd arg as a linear multiplier (0.5 = half-amplitude, 2.0 = double-amplitude). Composer chooses by semantic intent — function name documents the unit. Negative `volume` rejected (use `gain` for dB attenuation); both emit clipping warnings to stderr when post-multiplication samples exceed 1.0 (Phase 26.2)
  - **Hertz type + literal syntax:** `Hertz` first-class music type for audio frequency parameters; `800Hz` and `1.5kHz` literals (kHz canonical-Hz at lex time: 1.5kHz → 1500.0). Used by filters (`lowpass`/`highpass`/`bandpass`) + signal generators (`createSineTone`/`createSawTone`/`createSquareTone`/`createTriangleTone`); coexists with bare-Double overloads via OverloadResolver exact-match scoring (Phase 26.2)
  ```

  **1b. Special Types list.** Locate the "Special Types" line via `grep -n "Note, Semitone, Cent" CLAUDE.md` (around line 213 — section "### Special Types (`TypeSystem/SpecialTypes/`)"). The current list reads `Note, Semitone, Cent, Millisecond, Second, Decibel, Beat, Bar, TimeSignature, NoteValue, Sequence, MusicalNote, Chord, Section, Song`. Add `Hertz` before `Bar` (grouping with the other quantitative units):

  ```markdown
  Note, Semitone, Cent, Millisecond, Second, Decibel, Beat, Hertz, Bar, TimeSignature, NoteValue, Sequence, MusicalNote, Chord, Section, Song
  ```

  **1c. Audio Effects mention.** Locate the `### Audio Effects (Audio/DSP/)` line via `grep -n "Audio Effects (Audio/DSP/)" CLAUDE.md` (around line 235). Current entry: `\`reverb\`, \`lowpass\`, \`highpass\`, \`bandpass\`, \`compress\`, \`delay\`, \`gain\``. Add `volume`:

  ```markdown
  ### Audio Effects (Audio/DSP/)
  `reverb`, `lowpass`, `highpass`, `bandpass`, `compress`, `delay`, `gain` (dB), `volume` (linear)
  ```

  DO NOT touch the rest of CLAUDE.md (Audio Pipeline / Music-Specific other than the volume edit / Architecture sections).

**2. `.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md`** (NEW) — Author the closure verification document. Mirror the structure of `.planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md`:

```markdown
---
phase: 26.2-music-type-ergonomics-fx-overloads-inserted
status: shipped
shipped: 2026-05-10
nyquist_compliant: true
---

# Phase 26.2 — Verification (Closure)

**Goal:** Make the music-type family (Decibel, Beat, Cent, Semitone, Millisecond, Second, plus the new Hertz) first-class at every audio FX call site. Compose with `(delay buf 250ms 0.5 0.4)`, `(compress buf -12dB 4.0 5ms 100ms)`, `(lowpass buf 800Hz)`, `(volume buf 0.5)` and have resolution + audio output be byte-identical with the bare-Double form they replace.

## Plans (6/6)

| Plan | Wave | Requirement | Commit | Status |
|------|------|-------------|--------|--------|
| 26.2-01 | 0 | (RED scaffolding) + ERG-05 root-cause fix | <hash-01> | ✅ Shipped |
| 26.2-02 | 1 | ERG-01 + ERG-04 type-level | <hash-02> | ✅ Shipped |
| 26.2-03 | 2 | ERG-04 lexer + ERG-05 audio.flow forward decl | <hash-03> | ✅ Shipped |
| 26.2-04 | 3 | ERG-02 + ERG-04 FX overloads | <hash-04> | ✅ Shipped |
| 26.2-05 | 4 | ERG-03 volume function | <hash-05> | ✅ Shipped |
| 26.2-06 | 5 | (closure) | <hash-06> | ✅ Shipped |

## Must-Haves Audit

(Cross-reference each plan's `must_haves.truths` and confirm GREEN. Bullet each truth with ✅ + commit ref.)

### Plan 26.2-01 (Wave 0 — Test scaffolding + Value.ConvertTo Double-arm)
- ✅ 6 *Facts.cs files exist under flow-lang.Tests/Unit/Phase26_2/ — verified via ls
- ✅ Value.ConvertTo(DoubleType) on a music-type-tagged double-backed Value returns a Double Value
- ✅ 2 pre-existing DecibelBeatNumericCompatFacts (GainWithDecibelLiteral_… + GainWithPositiveDecibelLiteral_…) FLIPPED RED→GREEN as a side-effect
- ✅ Phase 18 + 25 byte-identical regression contracts STAY GREEN

### Plan 26.2-02 (Wave 1 — Ms/Sec/Hertz IsCompatibleWith + Value.Hertz factory)
- ✅ MillisecondType.IsCompatibleWith(Double|Float) override added; CanConvertTo(Second) preserved
- ✅ SecondType.IsCompatibleWith(Double|Float) override added; CanConvertTo(Millisecond) preserved
- ✅ HertzType.cs ships as new sealed FlowType singleton; GetSpecificity()=144 unique
- ✅ Value.Hertz(double) factory added at Value.cs:~38
- ✅ SemitoneType STAYS Int-only (D-03 canary GREEN)
- ✅ All 8 MusicTypeNumericCompatFacts GREEN (including 2 with Skip removed for Hertz)

### Plan 26.2-03 (Wave 2 — Lexer Hz/kHz arms + audio.flow gain(Decibel) forward decl)
- ✅ TokenType.HertzLiteral added
- ✅ Lexer ScanNumberOrSpecialLiteral has Hz + kHz arms (kHz BEFORE Hz per RESEARCH Pitfall 4)
- ✅ Lexer TryLookAheadSpecialLiteral has Hz + kHz arms (signed prefix path)
- ✅ TryLexAngleAngle predecessor set bumped to include HertzLiteral (RESEARCH Pitfall 6)
- ✅ Parser routes HertzLiteral → LiteralExpression(HertzType.Instance, double)
- ✅ audio.flow has `internal proc gain(Buffer: buffer, Decibel: gainDb)` forward decl (closes RESEARCH Pitfall 2)
- ✅ All 4 HertzLiteralLexingFacts GREEN with Skip removed
- ✅ NegativeMusicLiteralFacts.PositiveHertz_AfterLParen GREEN with Skip removed

### Plan 26.2-04 (Wave 3 — FX overloads)
- ✅ delay(Buffer, Millisecond, Double, Double) registered
- ✅ compress(Buffer, Decibel, Double, Millisecond, Millisecond) registered
- ✅ sidechain(Buffer, Buffer, Decibel, Double, Millisecond, Millisecond) registered
- ✅ reverb(Buffer, Double, Second) registered (Pitfall 3 disambiguation sentinel GREEN)
- ✅ lowpass/highpass/bandpass Hertz overloads registered
- ✅ createSineTone(Double, Hertz, Double) C# overload registered
- ✅ Flow-side proc createSawTone/createSquareTone/createTriangleTone Hertz overloads in audio.flow
- ✅ Each new C# registration has matching audio.flow internal proc forward decl
- ✅ All 8 MusicTypeFXOverloadFacts + 4 HertzFXOverloadFacts GREEN
- ✅ NegativeMusicLiteralFacts.NegativeMs_AfterLParen GREEN with Skip removed

### Plan 26.2-05 (Wave 4 — volume function)
- ✅ RegisterVolume + VolumeEffect in EffectsFunctions.cs (mirrors RegisterGain/GainEffect minus dB conversion)
- ✅ Negative volume rejected with InvalidOperationException
- ✅ Clipping warning emitted when post-multiplication samples > 1.0
- ✅ audio.flow internal proc volume forward decl
- ✅ All 5 VolumeFunctionFacts GREEN with Skip removed
- ✅ Gain_BareDoubleSubOne_StillDecibel_RegressionCanary STAYS GREEN (D-07 silent-policy)

### Plan 26.2-06 (Wave 5 — closure)
- ✅ REQUIREMENTS.md ERG-01..ERG-05 authored
- ✅ REQUIREMENTS.md Traceability rows for all 5 ERG IDs shipped
- ✅ ROADMAP.md Phase 26.2 marked Complete with plan list + commit hashes
- ✅ STATE.md current-position points to Phase 27 next
- ✅ CLAUDE.md Core Language Features + Special Types list + Audio Effects updated
- ✅ 26.2-VERIFICATION.md authored (this document)
- ✅ 26.2-VALIDATION.md frontmatter status: shipped, nyquist_compliant: true, wave_0_complete: true
- ✅ examples/tutorial.flow + examples/showcase.flow NOT touched (Phase 27's job)

## Regression Gates

| Gate | Status |
|------|--------|
| Phase 18 ByteIdenticalShowcaseTests | ✅ GREEN |
| Phase 25 ByteIdenticalShowcaseGaussianTests | ✅ GREEN |
| `dotnet test` full suite | ✅ GREEN (the 2 pre-existing DecibelBeatNumericCompatFacts deferred items now GREEN — closed by this phase) |
| FlowScriptTests (~70 .flow scripts) | ✅ GREEN (no regressions from lexer Hz/kHz arms or new overloads) |
| examples/tutorial.flow exit 0 | ✅ GREEN |
| examples/showcase.flow exit 0 | ✅ GREEN |
| Reverb arity-vs-Second disambiguation (RESEARCH Pitfall 3) | ✅ Verified by Reverb_BareDoubleAndSecondVariants_ResolveDistinctlyNoAmbiguity fact |
| Gain dB silent-policy (D-07) | ✅ Verified by Gain_BareDoubleSubOne_StillDecibel_RegressionCanary fact |
| Semitone STAYS Int-only (D-03) | ✅ Verified by Semitone_NotCompatibleWith_Double_RegressionCanary fact |

## Open Questions Resolved

1. **PitchConversion Hz overloads** — RESEARCH Open Question #1 resolved: NO PitchConversion APIs added. `noteToFrequency` only RETURNS Hz, doesn't take Hz; no Hz-taking PitchConversion API exists. ERG-04 acceptance text reflects this scope decision.
2. **`volume` convenience-layer wrapper** — RESEARCH Open Question #2 resolved: matches gain's shape. Single `internal proc volume(Buffer: b, Double: linearMultiplier)` line in audio.flow + matching C# registration. NO Flow-side convenience wrapper.
3. **Sine/saw/square/triangle Hertz overloads** — RESEARCH Open Question #3 resolved: extended existing `createSineTone`/`createSawTone`/`createSquareTone`/`createTriangleTone` family. createSineTone gets a C# Hertz overload; the other three get Flow-side proc overloads in audio.flow. NO new short `sine`/`saw`/`square`/`triangle` builtins invented.

## Sign-Off

Phase 26.2 closure verified. v1.3 milestone progresses to Phase 27 (Tutorial + Showcase Refresh) — the milestone closer.

**Approver:** automated (planner + executor + checker per GSD).
**Date:** 2026-05-10.
```

Replace each `<hash-NN>` with the actual commit hash from `git log --oneline -10`.

**3. `.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md`** — Update the frontmatter:
- `status: shipped` (was draft)
- `nyquist_compliant: true` (was false)
- `wave_0_complete: true` (was false)
- In the Sign-Off section at the bottom: `Approval: approved 2026-05-10 (planner + executor + checker per GSD)` (was "pending")

The body of the document otherwise stays as-is — it's the strategy document, not the closure record.

**4. Wave 5 closure commit** atomic per the Phase 26-05 / 26.1-06 docs-only-closure precedent. Commit message: `docs(26.2): closure — REQUIREMENTS/ROADMAP/STATE/VERIFICATION/CLAUDE.md`. Pre-commit verification:
- `dotnet test` — full suite GREEN (no regressions; the 2 pre-existing DecibelBeat deferred items now GREEN)
- `dotnet test --filter "FullyQualifiedName~Phase26_2|FullyQualifiedName~Phase18.ByteIdenticalShowcase|FullyQualifiedName~Phase25.ByteIdenticalShowcaseGaussian"` — GREEN
- All 5 plan SUMMARY.md files exist under .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/
- `examples/tutorial.flow` and `examples/showcase.flow` NOT modified — verified by `git diff examples/tutorial.flow examples/showcase.flow` empty

After the commit, run a final regression sample loop (matches Phase 26 / 26.1 closure pattern):
```
dotnet run --project flow-interpreter examples/tutorial.flow > /dev/null
dotnet run --project flow-interpreter examples/showcase.flow > /dev/null
```
Both should exit 0 with non-empty `examples/output/*.wav` and `*.mid` produced.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --logger "console;verbosity=minimal" 2>&amp;1 | grep -E "(Failed|Passed):" &amp;&amp; grep -c "gain.*vs.*volume\\|Hertz type + literal\\|gain.*dB.*volume.*linear" CLAUDE.md &amp;&amp; ls -la .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md &amp;&amp; head -10 .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md &amp;&amp; git diff --name-only examples/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gain.*volume\\|Hertz type + literal" CLAUDE.md` returns at least `1` (Core Language Features bullet present)
    - `grep -c "Hertz" CLAUDE.md` returns at least `2` (Special Types list entry + Core bullet)
    - `grep -c "volume.*linear" CLAUDE.md` returns at least `1` (Audio Effects mention)
    - File `.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md` exists with at least 60 lines and contains `✅ Shipped` and `Sign-Off`
    - `head -10 .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VALIDATION.md | grep -E "status: shipped|nyquist_compliant: true|wave_0_complete: true"` returns at least 3 lines
    - `git diff --name-only examples/` shows NO modifications to examples/tutorial.flow or examples/showcase.flow
    - `dotnet test` (full suite) reports `Failed: 0` (the 2 pre-existing DecibelBeat deferred items now CLOSED)
    - `dotnet test --filter "FullyQualifiedName~Phase26_2"` reports `Failed: 0 / Skipped: 0` and `Passed: >= 30` (all 34 facts GREEN)
    - `dotnet test --filter "FullyQualifiedName~Phase18.ByteIdenticalShowcase|FullyQualifiedName~Phase25.ByteIdenticalShowcaseGaussian"` reports `Failed: 0`
    - `git log --oneline -1 | grep -E "docs\\(26\\.2\\):.*closure"` confirms the Wave 5 commit message
  </acceptance_criteria>
  <done>Wave 5 docs-only closure commit lands; CLAUDE.md updated (Core bullets + Special Types Hertz + Audio Effects volume); 26.2-VERIFICATION.md authored; 26.2-VALIDATION.md frontmatter shipped; examples/tutorial.flow + examples/showcase.flow UNTOUCHED; full test suite green; byte-identical regression intact; 2 pre-existing RED DecibelBeat facts CLOSED.</done>
</task>

</tasks>

<threat_model>
No threat surface — pure documentation updates. The feature commits already passed STRIDE review in Plans 01-05.
</threat_model>

<verification>
- REQUIREMENTS.md ERG-01..ERG-05 authored under "### Music Type Ergonomics + FX Overloads" section + Traceability rows
- ROADMAP.md Phase 26.2 marked Complete 2026-05-10 with full plan list + commit hashes; Progress table updated
- STATE.md current-position advances to Phase 27 next; progress counters incremented (10→11 phases, 48→54 plans)
- 26.2-VERIFICATION.md authored with full plan-by-plan must-have evidence + Open Questions resolution
- 26.2-VALIDATION.md frontmatter flipped to shipped + nyquist_compliant + wave_0_complete
- CLAUDE.md Core Language Features bullets (gain/volume + Hertz literals) + Special Types Hertz + Audio Effects volume mention
- examples/tutorial.flow + examples/showcase.flow NOT modified (Phase 27 owns)
- Full dotnet test suite green; the 2 pre-existing DecibelBeat deferred items now CLOSED
- Phase 18 + 25 byte-identical regression contracts intact
</verification>

<success_criteria>
- All 6 plans of Phase 26.2 documented as Shipped
- v1.3 progress: 11/12 phases complete
- Phase 27 is the next ROADMAP target
- Tutorial-side ergonomics chapter additions deferred to Phase 27 per CONTEXT § Specifics
- Wave 5 closure commit message: `docs(26.2): closure — REQUIREMENTS/ROADMAP/STATE/VERIFICATION/CLAUDE.md`
</success_criteria>

<output>
After completion, create `.planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-06-SUMMARY.md` documenting the closure: REQUIREMENTS additions, ROADMAP marking, STATE advancement, VERIFICATION sign-off, CLAUDE.md updates, regression contracts intact, 2 pre-existing RED facts closed.
