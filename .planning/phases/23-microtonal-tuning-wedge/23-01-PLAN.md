---
phase: 23-microtonal-tuning-wedge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs
  - flow-lang/StandardLibrary/Audio/Tuning/Mode.cs
  - flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs
  - flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs
  - flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs
  - flow-lang/StandardLibrary/Audio/Tuning/RatioMath.cs
  - flow-lang.Tests/Unit/Phase23/TuningRatioFacts.cs
  - flow-lang.Tests/Unit/Phase23/TuningModeShiftFacts.cs
  - flow-lang.Tests/Unit/Phase23/SpellingAwareTuningFacts.cs
  - flow-lang.Tests/Unit/Phase23/CentOffsetAdditivityFacts.cs
autonomous: true
requirements: [MICR-01]
tags: [microtonal, tuning, ratio-tables, just-intonation, pythagorean, render-time]

must_haves:
  truths:
    - "Closed enum TuningSystem ships with 3 members: EqualTemperament, JustIntonation, Pythagorean (per CONTEXT D-08, RESEARCH §Standard Stack — closed-enum house style matches TokenType/PragmaRegistry precedent; RenderTuning struct/record threads through synthesizer interface per Pattern A — Pattern B's static MusicalContext.Current accessor has zero codebase analogs while Pattern A mirrors the established SongRenderer.RenderSection per-section bpm/pan/gain/rt60 resolution at line 128-134)"
    - "Closed enum Mode ships with 7 members: Major, Minor, Dorian, Phrygian, Lydian, Mixolydian, Locrian (per D-03 mode-shifts-the-table)"
    - "Static TuningTables dictionary keyed by (TuningSystem, Mode) returns ChromaticRatioTable indexed by (NoteLetter, Alteration) — spelling-aware per D-09 (Eb=6/5 ≠ D#=75/64 in JI)"
    - "All 14 tables ship: 7 JI modes from Mudcat canonical reference + 7 Pythagorean modes from chain-of-fifths (per RESEARCH §Pitfall 2 — Wikipedia asymmetric 5-limit pinned for JI; Wikipedia chain-of-fifths sharps for Pythagorean)"
    - "JI Major C-tonic: C=1/1, D=9/8, E=5/4 (canary), F=4/3, G=3/2, A=5/3, B=15/8; chromatics use 25/24 raised + 16/15 lowered semitone construction"
    - "Pythagorean Major C-tonic: C=1/1, D=9/8, E=81/64 (canary), F=4/3, G=3/2, A=27/16, B=243/128; chromatics on chain-of-fifths"
    - "RatioMath static helper provides TonicHzFromKey(keyName, octave) and CentOffsetMultiplier(cents) = 2^(cents/1200) per D-10 cent-additive math"
    - "30+ xUnit Facts pin canonical ratios as Pass 1 RED before any production wiring exists in plan 23-02 — TuningRatioFacts (12+), TuningModeShiftFacts (14 mode rows), SpellingAwareTuningFacts (4 spelling distinctions), CentOffsetAdditivityFacts (4 additivity)"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs"
      provides: "Closed enum: EqualTemperament, JustIntonation, Pythagorean"
      contains: "enum TuningSystem"
    - path: "flow-lang/StandardLibrary/Audio/Tuning/Mode.cs"
      provides: "Closed enum: 7 church modes"
      contains: "enum Mode"
    - path: "flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs"
      provides: "Pattern A render-tuning value: (TuningSystem System, Mode Mode, char TonicLetter, int TonicAlteration)"
      contains: "record RenderTuning"
    - path: "flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs"
      provides: "Spelling-aware ratio map: (Letter, Alteration) -> ratio"
      contains: "ChromaticRatioTable"
    - path: "flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs"
      provides: "Static Dictionary<(TuningSystem, Mode), ChromaticRatioTable> + LookupRatio + canonical citations"
      contains: "TuningTables"
    - path: "flow-lang/StandardLibrary/Audio/Tuning/RatioMath.cs"
      provides: "Tonic-Hz lookup + cent-offset compose helpers"
      contains: "RatioMath"
    - path: "flow-lang.Tests/Unit/Phase23/TuningRatioFacts.cs"
      provides: "MICR-01 canary ratios pinned (5:4, 81:64, 3:2, 4:3, 9:8)"
      contains: "TuningRatioFacts"
    - path: "flow-lang.Tests/Unit/Phase23/TuningModeShiftFacts.cs"
      provides: "D-03 mode-table-shift verification (14 rows)"
      contains: "TuningModeShiftFacts"
    - path: "flow-lang.Tests/Unit/Phase23/SpellingAwareTuningFacts.cs"
      provides: "D-09 Eb vs D# distinction"
      contains: "SpellingAwareTuningFacts"
    - path: "flow-lang.Tests/Unit/Phase23/CentOffsetAdditivityFacts.cs"
      provides: "D-10 cent-additive math"
      contains: "CentOffsetAdditivityFacts"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs"
      to: "flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs"
      via: "Dictionary value type"
      pattern: "Dictionary<\\(TuningSystem, Mode\\), ChromaticRatioTable>"
    - from: "flow-lang.Tests/Unit/Phase23/TuningRatioFacts.cs"
      to: "flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs"
      via: "TuningTables.LookupRatio call"
      pattern: "TuningTables\\.LookupRatio\\("
    - from: "flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs"
      to: "Wikipedia / Mudcat canonical sources"
      via: "doc-comment citations"
      pattern: "Five-limit_tuning|JUSTINT.html|Pythagorean_tuning"
---

<objective>
Wave 1 ships the entire mathematical foundation for Phase 23 microtonal tuning: closed enums (`TuningSystem`, `Mode`), spelling-aware `ChromaticRatioTable`, the 14-table `TuningTables` static dictionary (7 JI modes + 7 Pythagorean modes), the `RenderTuning` value object that Pattern A threads through synthesizers (Wave 2), and a `RatioMath` helper for tonic-Hz lookup + cent-offset composition. Pass 1 RED Facts pin every canonical ratio before any production wiring exists, satisfying Phase 18-22 RED-before-GREEN precedent.

**Pattern A is locked.** Per RESEARCH.md §Architecture Patterns Pattern 1 + PATTERNS.md "Pattern B is genuinely net-new for the codebase." `RenderTuning` ships in this wave so synthesizer signatures in Wave 2 can be updated mechanically; ambient `MusicalContext.Current` is rejected (no codebase analog; introduces global mutable state).

Purpose: ratio tables + `RenderTuning` shape are the single most-cited assumption in the entire phase (MICR-01 acceptance hinges on JI C-E being exactly 5/4 = 1.25). Landing them first as RED-then-GREEN with canonical citations means Wave 2 wiring can fail loudly if ratios drift.

Output: 6 production files (~600 LOC) under `flow-lang/StandardLibrary/Audio/Tuning/` + 4 xUnit Facts files (~40 Facts total) under `flow-lang.Tests/Unit/Phase23/`. Zero modifications to existing code. The `MusicalContext.Tuning` property, pragma registration, `PitchConversion` overload, FlowEngine bridge, synthesizer threading, `enharmonic`/`writeMidi` warnings, and church-mode parser extension all defer to later waves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-microtonal-tuning-wedge/23-CONTEXT.md
@.planning/phases/23-microtonal-tuning-wedge/23-RESEARCH.md
@.planning/phases/23-microtonal-tuning-wedge/23-PATTERNS.md
@.planning/phases/23-microtonal-tuning-wedge/23-VALIDATION.md
@./CLAUDE.md
@flow-lang/Lexing/TokenType.cs
@flow-lang/Lexing/PragmaRegistry.cs
@flow-lang/StandardLibrary/Audio/PitchConversion.cs
@flow-lang/Runtime/MusicalContext.cs
@flow-lang.Tests/Unit/Phase18/FractionTests.cs
@flow-lang.Tests/Unit/Phase21/PragmaRegistryFacts.cs

<interfaces>
<!-- Existing types Wave 1 builds against. Extracted from codebase. -->
<!-- The new Tuning/ namespace is net-new — no consumers exist yet (Wave 2 onward). -->

From flow-lang/Lexing/TokenType.cs (closed-enum house style):
```csharp
namespace FlowLang.Lexing;

public enum TokenType
{
    // Keywords
    Proc,
    EndProc,
    Return,
    ...
}
```

From flow-lang/Lexing/PragmaRegistry.cs (closed-set static dict + IsKnown — analog for TuningTables.LookupRatio shape):
```csharp
public static readonly IReadOnlyDictionary<string, string> KnownPragmas =
    new Dictionary<string, string>(StringComparer.Ordinal)
    {
        ["hAsB"] = "Inside note streams, accept 'H' as a synonym for 'B' (German notation)."
    };
public static bool IsKnown(string name) => KnownPragmas.ContainsKey(name);
```

From flow-lang/StandardLibrary/Audio/PitchConversion.cs (math-helper analog):
```csharp
public static double NoteToFrequency(char noteName, int octave, int alteration)
{
    int midiNote = GetMidiNote(noteName, octave, alteration);
    return 440.0 * Math.Pow(2.0, (midiNote - 69) / 12.0);
}
public static int GetMidiNote(char noteName, int octave, int alteration);
```

From flow-lang.Tests/Unit/Phase18/FractionTests.cs (Fact-per-canary template):
```csharp
namespace FlowLang.Tests.Unit.Phase18;
public class FractionTests
{
    [Fact]
    public void TripletThirds_SumToOne()
    {
        var third = new Fraction(1, 3);
        Assert.Equal(new Fraction(1, 1), third + third + third);
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Closed enums + RenderTuning + ChromaticRatioTable scaffolding (Pattern A locked)</name>
  <files>
    flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs,
    flow-lang/StandardLibrary/Audio/Tuning/Mode.cs,
    flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs,
    flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs
  </files>
  <read_first>
    flow-lang/Lexing/TokenType.cs (closed-enum house style; lines 1-15),
    flow-lang/Lexing/PragmaRegistry.cs (closed-set static dict pattern),
    flow-lang/StandardLibrary/Audio/PitchConversion.cs (file-scoped namespace pattern; record/struct conventions),
    .planning/phases/23-microtonal-tuning-wedge/23-CONTEXT.md (D-03, D-05, D-08, D-09 + Claude's Discretion: closed-enum recommended),
    .planning/phases/23-microtonal-tuning-wedge/23-RESEARCH.md (§Architecture Patterns Pattern 1 — Pattern A vs B; §Standard Stack — TuningSystem/Mode/ChromaticRatioTable shapes; §Pitfall 1 — Pattern A locked because MusicalContext.Current does NOT exist),
    .planning/phases/23-microtonal-tuning-wedge/23-PATTERNS.md (file analogs lines 13-14, 53-69 closed-enum body shape; Pattern A vs B analogs 341-432)
  </read_first>
  <behavior>
    - TuningSystem enum has exactly 3 members in declaration order: EqualTemperament, JustIntonation, Pythagorean (default(TuningSystem) == EqualTemperament — D-08 default-is-12-TET semantics enforced by enum default).
    - Mode enum has exactly 7 members in declaration order: Major, Minor, Dorian, Phrygian, Lydian, Mixolydian, Locrian (default(Mode) == Major — matches D-02 silent C-major-default fallback).
    - RenderTuning is a `readonly record struct` with 4 fields: TuningSystem System, Mode Mode, char TonicLetter, int TonicAlteration. Provides static `RenderTuning.Default` => `(EqualTemperament, Major, 'C', 0)`.
    - ChromaticRatioTable is a `sealed record` carrying `IReadOnlyDictionary<(char Letter, int Alteration), double> Ratios` plus a `Build(IDictionary<char, double> naturals, IDictionary<char, double> sharps, IDictionary<char, double> flats)` factory that fuses 7+5+5 entries into a 17-entry dict (covers C/D/E/F/G/A/B naturals + 5 sharps + 5 flats — A/B sharps + C/F flats omitted from natural set per D-09 spelling rules).
  </behavior>
  <action>
Create four new files. All use `namespace FlowLang.StandardLibrary.Audio.Tuning;` (file-scoped, matches CLAUDE.md C# conventions).

**1. TuningSystem.cs** — closed enum, 3 members (EqualTemperament FIRST so default(TuningSystem) == EqualTemperament per D-08). XML doc-comment cites CONTEXT D-08. Exact body:

```csharp
namespace FlowLang.StandardLibrary.Audio.Tuning;

/// <summary>
/// Closed-set tuning system identifier (CONTEXT D-08, RESEARCH §Standard Stack).
/// Default value is <see cref="EqualTemperament"/> so an unset <c>RenderTuning</c>
/// short-circuits to the byte-identical 12-TET path per Pitfall 6.
/// </summary>
public enum TuningSystem
{
    EqualTemperament,
    JustIntonation,
    Pythagorean,
}
```

**2. Mode.cs** — closed enum, 7 members. Major FIRST so default(Mode) == Major (matches D-02 silent C-major-default). Doc-comment cites D-03.

**3. RenderTuning.cs** — Pattern A value object (per RESEARCH §Pitfall 1 — Pattern A locked because Pattern B's MusicalContext.Current accessor does NOT exist in the codebase). `readonly record struct` with 4 fields + static `Default` property = `new(TuningSystem.EqualTemperament, Mode.Major, 'C', 0)`. Doc-comment cites Pattern A choice + Pitfall 6 byte-identical short-circuit:

```csharp
namespace FlowLang.StandardLibrary.Audio.Tuning;

/// <summary>
/// Resolved render-time tuning context threaded through <c>INoteSynthesizer.RenderNote</c>
/// per Pattern A (RESEARCH §Architecture Patterns Pattern 1; Pattern B static accessor
/// rejected — no codebase analog and would introduce global mutable state).
/// Default value (System=EqualTemperament, Mode=Major, TonicLetter='C', TonicAlteration=0)
/// triggers the byte-identical 12-TET short-circuit in <c>PitchConversion.NoteToFrequency</c>
/// per Pitfall 6 mitigation.
/// </summary>
public readonly record struct RenderTuning(
    TuningSystem System,
    Mode Mode,
    char TonicLetter,
    int TonicAlteration)
{
    public static RenderTuning Default => new(TuningSystem.EqualTemperament, Mode.Major, 'C', 0);
}
```

**4. ChromaticRatioTable.cs** — `sealed record` carrying `IReadOnlyDictionary<(char Letter, int Alteration), double> Ratios` and a `Build(naturals, sharps, flats)` factory that constructs the dict by walking three sub-maps. Per D-09 sharps/flats are SEPARATE entries (Eb=6/5 distinct from D#=75/64 in JI). Throw `InvalidOperationException` if `naturals` is missing any of {C, D, E, F, G, A, B} — the 7 diatonic keys are required.

```csharp
namespace FlowLang.StandardLibrary.Audio.Tuning;

/// <summary>
/// Spelling-aware chromatic ratio map per D-09. Indexed by (NoteLetter, Alteration)
/// where Alteration is -1 for flats, 0 for naturals, +1 for sharps. Eb (E,-1) and
/// D# (D,+1) MUST be different entries — in 5-limit JI Eb=6/5 (1.200) and
/// D#=75/64 (1.171875) are different pitches.
/// </summary>
public sealed record ChromaticRatioTable(
    IReadOnlyDictionary<(char Letter, int Alteration), double> Ratios)
{
    /// <summary>
    /// Constructs a ChromaticRatioTable from three convenience sub-maps.
    /// All 7 naturals (C/D/E/F/G/A/B) are REQUIRED. Sharps and flats are optional
    /// (each entry is added only if present in the corresponding sub-map).
    /// </summary>
    public static ChromaticRatioTable Build(
        IReadOnlyDictionary<char, double> naturals,
        IReadOnlyDictionary<char, double> sharps,
        IReadOnlyDictionary<char, double> flats)
    {
        var dict = new Dictionary<(char, int), double>();
        foreach (var letter in new[] { 'C', 'D', 'E', 'F', 'G', 'A', 'B' })
        {
            if (!naturals.TryGetValue(letter, out var r))
                throw new InvalidOperationException(
                    $"ChromaticRatioTable.Build: natural '{letter}' is required");
            dict[(letter, 0)] = r;
        }
        foreach (var (letter, ratio) in sharps) dict[(letter, +1)] = ratio;
        foreach (var (letter, ratio) in flats)  dict[(letter, -1)] = ratio;
        return new ChromaticRatioTable(dict);
    }

    /// <summary>
    /// Looks up the ratio for a (letter, alteration) pair. Throws
    /// <see cref="KeyNotFoundException"/> when the spelling has no entry — the
    /// caller (TuningTables.LookupRatio) is responsible for fallback construction
    /// per RESEARCH §Pitfall 3 chromatic-fallback rule.
    /// </summary>
    public double Lookup(char letter, int alteration) =>
        Ratios[(letter, alteration)];

    public bool TryLookup(char letter, int alteration, out double ratio) =>
        Ratios.TryGetValue((letter, alteration), out ratio);
}
```

**Convention compliance:**
- File-scoped namespaces (CLAUDE.md C# conventions).
- Record types for immutable data (CLAUDE.md AST/value-object pattern).
- Closed enums (CLAUDE.md house style + Phase 21 D-17 precedent — `TokenType`, `DurationValue`, `PragmaRegistry`).

Per D-USER-decision (Claude's Discretion in CONTEXT): split files (one type per file) — these enums + RenderTuning + ChromaticRatioTable will be referenced from `TuningTables`, tests, and downstream waves; one-file-per-public-type matches the rest of `Tuning/`.

No build/run dependency for this task — these are pure type definitions.
  </action>
  <verify>
    <automated>dotnet build flow-lang.sln --verbosity minimal 2>&amp;1 | tail -20 &amp;&amp; grep -c "public enum TuningSystem\|public enum Mode\|public readonly record struct RenderTuning\|public sealed record ChromaticRatioTable" flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs flow-lang/StandardLibrary/Audio/Tuning/Mode.cs flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs</automated>
    Expected: build SUCCESS, 4 lines emitted (one per type), 0 errors. Also verify enum order with `grep -A 4 "enum TuningSystem" flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs | head -5` — first non-comment member must be `EqualTemperament`.
  </verify>
  <acceptance_criteria>
    - `flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs` exists; `grep -E "^\s*(EqualTemperament|JustIntonation|Pythagorean),?\s*$" flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs | wc -l` == 3.
    - First enum member is `EqualTemperament` (so `default(TuningSystem) == EqualTemperament`): `grep -A 5 "enum TuningSystem" flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs | grep -v "^#" | grep -m1 -E "EqualTemperament|JustIntonation|Pythagorean"` returns first match `EqualTemperament`.
    - `flow-lang/StandardLibrary/Audio/Tuning/Mode.cs` exists; 7 members present (Major, Minor, Dorian, Phrygian, Lydian, Mixolydian, Locrian). First member `Major`.
    - `flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs` exists; declares `public readonly record struct RenderTuning(TuningSystem System, Mode Mode, char TonicLetter, int TonicAlteration)` and exposes `static RenderTuning Default`.
    - `flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs` exists; declares `public sealed record ChromaticRatioTable` with `Build` factory + `Lookup` + `TryLookup`. Body throws `InvalidOperationException` when any of the 7 diatonic naturals is missing.
    - All four files use `namespace FlowLang.StandardLibrary.Audio.Tuning;` (file-scoped): `grep -c "^namespace FlowLang.StandardLibrary.Audio.Tuning;" flow-lang/StandardLibrary/Audio/Tuning/*.cs` == 4.
    - `dotnet build flow-lang.sln` succeeds with 0 errors and 0 warnings on the new files.
  </acceptance_criteria>
  <done>
    Four new type files compile cleanly. The shape of `RenderTuning` (Pattern A) is locked — Wave 2 synthesizer signature change can proceed. No production code references these types yet (intentional — Wave 2 wires them up).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TuningTables (14 ratio tables — 7 JI modes + 7 Pythagorean modes) + RatioMath helpers + RED Pass-1 Facts</name>
  <files>
    flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs,
    flow-lang/StandardLibrary/Audio/Tuning/RatioMath.cs,
    flow-lang.Tests/Unit/Phase23/TuningRatioFacts.cs,
    flow-lang.Tests/Unit/Phase23/TuningModeShiftFacts.cs,
    flow-lang.Tests/Unit/Phase23/SpellingAwareTuningFacts.cs,
    flow-lang.Tests/Unit/Phase23/CentOffsetAdditivityFacts.cs
  </files>
  <read_first>
    flow-lang/StandardLibrary/Audio/Tuning/ChromaticRatioTable.cs (Task 1 output — Build factory shape),
    flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs (enum members),
    flow-lang/StandardLibrary/Audio/Tuning/Mode.cs (enum members),
    flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs (TonicLetter+TonicAlteration shape),
    flow-lang/StandardLibrary/Audio/PitchConversion.cs (440 * 2^((midi-69)/12) formula reused for tonic Hz),
    flow-lang.Tests/Unit/Phase18/FractionTests.cs (Fact-per-canary template),
    .planning/phases/23-microtonal-tuning-wedge/23-RESEARCH.md (§Code Examples — JustMajor table lines 506-540, mode-shifted tables 542-571, Pitfall 2 Pythagorean chain, Pitfall 3 chromatic fallback rule, §Validation Architecture per-MICR test map),
    .planning/phases/23-microtonal-tuning-wedge/23-CONTEXT.md (D-03 mode-specific tables, D-09 spelling-aware, D-10 cent-additive),
    .planning/phases/23-microtonal-tuning-wedge/23-VALIDATION.md (per-task verification map — required test surfaces lines 49-63),
    .planning/phases/23-microtonal-tuning-wedge/23-PATTERNS.md (TuningTables analogs lines 77-110; FractionTests template 195-228)
  </read_first>
  <behavior>
    - `TuningTables.Tables` is a `Dictionary<(TuningSystem, Mode), ChromaticRatioTable>` with EXACTLY 14 entries: 7 JI modes + 7 Pythagorean modes. EqualTemperament has no entry (consumers short-circuit on `tuning.System == EqualTemperament` before any lookup per Pitfall 6).
    - `TuningTables.LookupRatio(TuningSystem sys, Mode mode, char letter, int alteration)` looks up the (letter, alteration) ratio in `Tables[(sys, mode)]`. Throws `KeyNotFoundException` when (sys, mode) absent — the caller is responsible for `EqualTemperament` short-circuit before calling.
    - `RatioMath.TonicHzFromKey(char tonicLetter, int tonicAlteration, int octave)` computes the 12-TET frequency of the tonic note at the given octave. This is the reference Hz that gets multiplied by the ratio. Reuses `PitchConversion.NoteToFrequency(letter, octave, alteration)`.
    - `RatioMath.CentOffsetMultiplier(double cents)` returns `Math.Pow(2.0, cents / 1200.0)`. Per D-10 cents are additive in cent-space; the multiplier applies AFTER the ratio multiply.
    - All 5 JI canary ratios pin: C->E (5/4 = 1.25), C->G (3/2 = 1.5), C->F (4/3), C->D (9/8 = 1.125), C->A (5/3). Pythagorean major C->E (81/64 = 1.265625) is the canonical 3-limit canary.
    - Spelling-awareness: in JI Major, Eb (6/5 = 1.2) and D# (75/64 = 1.171875) MUST differ. In Pythagorean Major, Eb=32/27 and D#=19683/16384 differ.
    - Cent additivity: under JI, E4+5c renders at `(5/4) * 2^(5/1200)`. Under EqualTemperament, fall-through 12-TET formula already handles cents — RatioMath.CentOffsetMultiplier is reusable.
  </behavior>
  <action>
**1. TuningTables.cs** — populate all 14 mode-keyed tables with canonical ratios. Cite Wikipedia + Mudcat in doc comments per RESEARCH §Pitfall 2 mitigation.

```csharp
namespace FlowLang.StandardLibrary.Audio.Tuning;

/// <summary>
/// Static dictionary of chromatic ratio tables keyed by (TuningSystem, Mode).
/// Ships 14 entries: 7 JI modes + 7 Pythagorean modes per D-03.
/// EqualTemperament has no entry — consumers short-circuit on
/// <c>tuning.System == EqualTemperament</c> before calling <see cref="LookupRatio"/>
/// per RESEARCH Pitfall 6 (byte-identical 12-TET fast path).
///
/// Sources (canonical):
///   - 5-limit JI: Wikipedia "Five-limit tuning" asymmetric chromatic table
///     (https://en.wikipedia.org/wiki/Five-limit_tuning).
///   - 5-limit JI mode shifts: Mudcat "Just Intonation Music Scales" (Olson)
///     (https://mudcat.org/olson/JUSTINT.html).
///   - 3-limit Pythagorean: Wikipedia "Pythagorean tuning" chain-of-fifths from C
///     (https://en.wikipedia.org/wiki/Pythagorean_tuning).
///
/// Chromatic-fallback rule (Pitfall 3): non-diatonic spellings in mode tables use
/// the same 25/24 raised + 16/15 lowered semitone construction as JI Major; for
/// Pythagorean modes, chromatic tones stay on the chain-of-fifths.
/// </summary>
public static class TuningTables
{
    public static readonly IReadOnlyDictionary<(TuningSystem, Mode), ChromaticRatioTable> Tables
        = new Dictionary<(TuningSystem, Mode), ChromaticRatioTable>
        {
            [(TuningSystem.JustIntonation, Mode.Major)]      = JustIonian,
            [(TuningSystem.JustIntonation, Mode.Minor)]      = JustAeolian,
            [(TuningSystem.JustIntonation, Mode.Dorian)]     = JustDorian,
            [(TuningSystem.JustIntonation, Mode.Phrygian)]   = JustPhrygian,
            [(TuningSystem.JustIntonation, Mode.Lydian)]     = JustLydian,
            [(TuningSystem.JustIntonation, Mode.Mixolydian)] = JustMixolydian,
            [(TuningSystem.JustIntonation, Mode.Locrian)]    = JustLocrian,
            [(TuningSystem.Pythagorean,    Mode.Major)]      = PythIonian,
            [(TuningSystem.Pythagorean,    Mode.Minor)]      = PythAeolian,
            [(TuningSystem.Pythagorean,    Mode.Dorian)]     = PythDorian,
            [(TuningSystem.Pythagorean,    Mode.Phrygian)]   = PythPhrygian,
            [(TuningSystem.Pythagorean,    Mode.Lydian)]     = PythLydian,
            [(TuningSystem.Pythagorean,    Mode.Mixolydian)] = PythMixolydian,
            [(TuningSystem.Pythagorean,    Mode.Locrian)]    = PythLocrian,
        };

    public static double LookupRatio(TuningSystem system, Mode mode, char letter, int alteration)
    {
        if (!Tables.TryGetValue((system, mode), out var table))
            throw new KeyNotFoundException(
                $"TuningTables: no table for ({system}, {mode}). EqualTemperament should " +
                $"short-circuit before calling LookupRatio per Pitfall 6.");
        return table.Lookup(letter, alteration);
    }

    // === JI Mode Tables (Source: Mudcat Olson + Wikipedia asymmetric 5-limit) ===

    /// <summary>JI Ionian (Major). Diatonic: 1, 9/8, 5/4, 4/3, 3/2, 5/3, 15/8.</summary>
    public static readonly ChromaticRatioTable JustIonian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0,
            ['D'] = 9.0/8.0,
            ['E'] = 5.0/4.0,    // MICR-01 canary
            ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0,
            ['A'] = 5.0/3.0,
            ['B'] = 15.0/8.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0,
            ['D'] = 75.0/64.0,   // distinct from Eb=6/5 per D-09
            ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0,
            ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0,
            ['E'] = 6.0/5.0,     // canonical minor third — distinct from D# per D-09
            ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0,
            ['B'] = 9.0/5.0,
        });

    /// <summary>JI Aeolian (Natural Minor). 1, 9/8, 6/5, 4/3, 3/2, 8/5, 9/5.</summary>
    public static readonly ChromaticRatioTable JustAeolian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 6.0/5.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0, ['D'] = 75.0/64.0, ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0, ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0, ['E'] = 6.0/5.0, ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        });

    /// <summary>JI Dorian. 1, 9/8, 6/5, 4/3, 3/2, 5/3, 9/5 (3rd, 7th lowered from Ionian; 6th major).</summary>
    public static readonly ChromaticRatioTable JustDorian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 6.0/5.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 5.0/3.0, ['B'] = 9.0/5.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0, ['D'] = 75.0/64.0, ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0, ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0, ['E'] = 6.0/5.0, ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        });

    /// <summary>JI Phrygian. 1, 27/25, 6/5, 4/3, 3/2, 8/5, 9/5 (lowered 2, 3, 6, 7).</summary>
    public static readonly ChromaticRatioTable JustPhrygian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 27.0/25.0, ['E'] = 6.0/5.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0, ['D'] = 75.0/64.0, ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0, ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0, ['E'] = 6.0/5.0, ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        });

    /// <summary>JI Lydian. 1, 9/8, 5/4, 25/18, 3/2, 5/3, 15/8 (raised 4th).</summary>
    public static readonly ChromaticRatioTable JustLydian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 5.0/4.0, ['F'] = 25.0/18.0,
            ['G'] = 3.0/2.0, ['A'] = 5.0/3.0, ['B'] = 15.0/8.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0, ['D'] = 75.0/64.0, ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0, ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0, ['E'] = 6.0/5.0, ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        });

    /// <summary>JI Mixolydian. 1, 9/8, 5/4, 4/3, 3/2, 5/3, 9/5 (lowered 7th).</summary>
    public static readonly ChromaticRatioTable JustMixolydian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 5.0/4.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 5.0/3.0, ['B'] = 9.0/5.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0, ['D'] = 75.0/64.0, ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0, ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0, ['E'] = 6.0/5.0, ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        });

    /// <summary>JI Locrian. 1, 27/25, 6/5, 4/3, 36/25, 8/5, 9/5 (diminished 5th).</summary>
    public static readonly ChromaticRatioTable JustLocrian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 27.0/25.0, ['E'] = 6.0/5.0, ['F'] = 4.0/3.0,
            ['G'] = 36.0/25.0, ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 25.0/24.0, ['D'] = 75.0/64.0, ['F'] = 25.0/18.0,
            ['G'] = 25.0/16.0, ['A'] = 125.0/72.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 16.0/15.0, ['E'] = 6.0/5.0, ['G'] = 64.0/45.0,
            ['A'] = 8.0/5.0, ['B'] = 9.0/5.0,
        });

    // === Pythagorean Mode Tables (Source: Wikipedia Pythagorean_tuning chain-of-fifths) ===
    // Diatonic ratios are mode-specific; chromatic alterations stay on chain-of-fifths.
    // Wolf fifth lands at G#-Eb when C is tonic — documented but ratios are exact.

    /// <summary>Pythagorean Ionian. 1, 9/8, 81/64, 4/3, 3/2, 27/16, 243/128.</summary>
    public static readonly ChromaticRatioTable PythIonian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0,
            ['D'] = 9.0/8.0,
            ['E'] = 81.0/64.0,         // MICR-01 Pythagorean canary
            ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0,
            ['A'] = 27.0/16.0,
            ['B'] = 243.0/128.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0,
            ['D'] = 19683.0/16384.0,
            ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0,
            ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0,
            ['E'] = 32.0/27.0,         // distinct from D# = 19683/16384 per D-09
            ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0,
            ['B'] = 16.0/9.0,
        });

    /// <summary>Pythagorean Aeolian (natural minor). 1, 9/8, 32/27, 4/3, 3/2, 128/81, 16/9.</summary>
    public static readonly ChromaticRatioTable PythAeolian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 32.0/27.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0, ['D'] = 19683.0/16384.0, ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0, ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        });

    /// <summary>Pythagorean Dorian. 1, 9/8, 32/27, 4/3, 3/2, 27/16, 16/9.</summary>
    public static readonly ChromaticRatioTable PythDorian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 32.0/27.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 27.0/16.0, ['B'] = 16.0/9.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0, ['D'] = 19683.0/16384.0, ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0, ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        });

    /// <summary>Pythagorean Phrygian. 1, 256/243, 32/27, 4/3, 3/2, 128/81, 16/9.</summary>
    public static readonly ChromaticRatioTable PythPhrygian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0, ['D'] = 19683.0/16384.0, ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0, ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        });

    /// <summary>Pythagorean Lydian. 1, 9/8, 81/64, 729/512, 3/2, 27/16, 243/128 (raised 4th).</summary>
    public static readonly ChromaticRatioTable PythLydian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 81.0/64.0, ['F'] = 729.0/512.0,
            ['G'] = 3.0/2.0, ['A'] = 27.0/16.0, ['B'] = 243.0/128.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0, ['D'] = 19683.0/16384.0, ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0, ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        });

    /// <summary>Pythagorean Mixolydian. 1, 9/8, 81/64, 4/3, 3/2, 27/16, 16/9 (lowered 7th).</summary>
    public static readonly ChromaticRatioTable PythMixolydian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 9.0/8.0, ['E'] = 81.0/64.0, ['F'] = 4.0/3.0,
            ['G'] = 3.0/2.0, ['A'] = 27.0/16.0, ['B'] = 16.0/9.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0, ['D'] = 19683.0/16384.0, ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0, ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        });

    /// <summary>Pythagorean Locrian. 1, 256/243, 32/27, 4/3, 1024/729, 128/81, 16/9.</summary>
    public static readonly ChromaticRatioTable PythLocrian = ChromaticRatioTable.Build(
        naturals: new Dictionary<char, double> {
            ['C'] = 1.0, ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['F'] = 4.0/3.0,
            ['G'] = 1024.0/729.0, ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        },
        sharps: new Dictionary<char, double> {
            ['C'] = 2187.0/2048.0, ['D'] = 19683.0/16384.0, ['F'] = 729.0/512.0,
            ['G'] = 6561.0/4096.0, ['A'] = 59049.0/32768.0,
        },
        flats: new Dictionary<char, double> {
            ['D'] = 256.0/243.0, ['E'] = 32.0/27.0, ['G'] = 1024.0/729.0,
            ['A'] = 128.0/81.0, ['B'] = 16.0/9.0,
        });
}
```

**2. RatioMath.cs** — tonic-Hz lookup + cent-multiplier helpers:

```csharp
using FlowLang.StandardLibrary.Audio;

namespace FlowLang.StandardLibrary.Audio.Tuning;

/// <summary>
/// Math helpers for tuning-aware frequency synthesis. Pure static; no state.
/// Reuses <see cref="PitchConversion.NoteToFrequency"/> for the tonic 12-TET
/// reference frequency; per RESEARCH §Pitfall 6 the existing 1-arg + 3-arg
/// overloads stay byte-identical.
/// </summary>
public static class RatioMath
{
    /// <summary>
    /// 12-TET frequency of the tonic note at the given octave. Used as the "1/1"
    /// reference Hz for ratio-based tuning systems per CONTEXT D-01.
    /// </summary>
    public static double TonicHzFromKey(char tonicLetter, int tonicAlteration, int octave)
        => PitchConversion.NoteToFrequency(tonicLetter, octave, tonicAlteration);

    /// <summary>
    /// Multiplicative factor for an additive cent offset per CONTEXT D-10:
    /// freq = base * 2^(cents/1200). Applied AFTER the ratio multiply.
    /// </summary>
    public static double CentOffsetMultiplier(double cents)
        => cents == 0.0 ? 1.0 : Math.Pow(2.0, cents / 1200.0);
}
```

**3. flow-lang.Tests/Unit/Phase23/TuningRatioFacts.cs** — Pass 1 RED Facts that pin canonical canary ratios. Following Phase 18 FractionTests template — namespace `FlowLang.Tests.Unit.Phase23`, one Fact per pinned ratio, assert with precision 10:

```csharp
using FlowLang.StandardLibrary.Audio.Tuning;
using Xunit;

namespace FlowLang.Tests.Unit.Phase23;

/// <summary>
/// MICR-01 acceptance: canonical ratio Facts for the 14 mode tables shipped in
/// plan 23-01. RED-then-GREEN per Phase 18-22 precedent — these Facts pin Wikipedia
/// + Mudcat reference ratios BEFORE Wave 2 wires them through PitchConversion.
/// </summary>
public class TuningRatioFacts
{
    [Fact]
    public void JustMajor_CtoE_Is5to4()
        => Assert.Equal(5.0/4.0,
            TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'E', 0),
            precision: 10);

    [Fact]
    public void JustMajor_CtoG_Is3to2()
        => Assert.Equal(3.0/2.0,
            TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'G', 0),
            precision: 10);

    [Fact]
    public void JustMajor_CtoF_Is4to3()
        => Assert.Equal(4.0/3.0,
            TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'F', 0),
            precision: 10);

    [Fact]
    public void JustMajor_CtoD_Is9to8()
        => Assert.Equal(9.0/8.0,
            TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'D', 0),
            precision: 10);

    [Fact]
    public void JustMajor_CtoA_Is5to3()
        => Assert.Equal(5.0/3.0,
            TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'A', 0),
            precision: 10);

    [Fact]
    public void JustMajor_CtoB_Is15to8()
        => Assert.Equal(15.0/8.0,
            TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'B', 0),
            precision: 10);

    [Fact]
    public void PythagoreanMajor_CtoE_Is81to64()
        => Assert.Equal(81.0/64.0,
            TuningTables.LookupRatio(TuningSystem.Pythagorean, Mode.Major, 'E', 0),
            precision: 10);

    [Fact]
    public void PythagoreanMajor_CtoG_Is3to2()
        => Assert.Equal(3.0/2.0,
            TuningTables.LookupRatio(TuningSystem.Pythagorean, Mode.Major, 'G', 0),
            precision: 10);

    [Fact]
    public void PythagoreanMajor_CtoB_Is243to128()
        => Assert.Equal(243.0/128.0,
            TuningTables.LookupRatio(TuningSystem.Pythagorean, Mode.Major, 'B', 0),
            precision: 10);

    [Fact]
    public void PythagoreanMajor_CSharp_Is2187to2048()
        => Assert.Equal(2187.0/2048.0,
            TuningTables.LookupRatio(TuningSystem.Pythagorean, Mode.Major, 'C', +1),
            precision: 10);

    [Fact]
    public void Tables_HasExactly14Entries()
        => Assert.Equal(14, TuningTables.Tables.Count);

    [Fact]
    public void Tables_NoEqualTemperamentEntries()
    {
        // EqualTemperament short-circuits before LookupRatio per Pitfall 6.
        foreach (var (sys, _) in TuningTables.Tables.Keys)
            Assert.NotEqual(TuningSystem.EqualTemperament, sys);
    }

    [Fact]
    public void RatioMath_CentOffsetMultiplier_Zero_ReturnsOne()
        => Assert.Equal(1.0, RatioMath.CentOffsetMultiplier(0.0), precision: 10);

    [Fact]
    public void RatioMath_CentOffsetMultiplier_1200Cents_ReturnsTwo()
        => Assert.Equal(2.0, RatioMath.CentOffsetMultiplier(1200.0), precision: 10);
}
```

**4. flow-lang.Tests/Unit/Phase23/TuningModeShiftFacts.cs** — Theory-driven 14 rows (one per mode table) verifying the canonical scale-degree shape. Use `[Theory] [InlineData(...)]` rows; assert each diatonic ratio matches the Mudcat reference.

```csharp
using FlowLang.StandardLibrary.Audio.Tuning;
using Xunit;

namespace FlowLang.Tests.Unit.Phase23;

/// <summary>
/// D-03 mode-table-shift verification: each (TuningSystem, Mode) pair pins its
/// canonical 7-degree diatonic scale per Mudcat (JI) / Wikipedia (Pythagorean).
/// </summary>
public class TuningModeShiftFacts
{
    // (system, mode, letter, alteration, expectedRatio)
    [Theory]
    // JI Major (Ionian)
    [InlineData(TuningSystem.JustIntonation, Mode.Major, 'E', 0, 5.0/4.0)]
    // JI Aeolian (natural minor) — flat 3rd
    [InlineData(TuningSystem.JustIntonation, Mode.Minor, 'E', 0, 6.0/5.0)]
    // JI Dorian — flat 3rd, natural 6th, flat 7th
    [InlineData(TuningSystem.JustIntonation, Mode.Dorian, 'E', 0, 6.0/5.0)]
    [InlineData(TuningSystem.JustIntonation, Mode.Dorian, 'A', 0, 5.0/3.0)]
    // JI Phrygian — flat 2, flat 3, flat 6, flat 7
    [InlineData(TuningSystem.JustIntonation, Mode.Phrygian, 'D', 0, 27.0/25.0)]
    // JI Lydian — raised 4
    [InlineData(TuningSystem.JustIntonation, Mode.Lydian, 'F', 0, 25.0/18.0)]
    // JI Mixolydian — flat 7
    [InlineData(TuningSystem.JustIntonation, Mode.Mixolydian, 'B', 0, 9.0/5.0)]
    // JI Locrian — diminished 5
    [InlineData(TuningSystem.JustIntonation, Mode.Locrian, 'G', 0, 36.0/25.0)]
    // Pythagorean Major
    [InlineData(TuningSystem.Pythagorean, Mode.Major, 'E', 0, 81.0/64.0)]
    // Pythagorean Aeolian — flat 3rd
    [InlineData(TuningSystem.Pythagorean, Mode.Minor, 'E', 0, 32.0/27.0)]
    // Pythagorean Dorian
    [InlineData(TuningSystem.Pythagorean, Mode.Dorian, 'E', 0, 32.0/27.0)]
    [InlineData(TuningSystem.Pythagorean, Mode.Dorian, 'A', 0, 27.0/16.0)]
    // Pythagorean Lydian — raised 4
    [InlineData(TuningSystem.Pythagorean, Mode.Lydian, 'F', 0, 729.0/512.0)]
    // Pythagorean Mixolydian — flat 7
    [InlineData(TuningSystem.Pythagorean, Mode.Mixolydian, 'B', 0, 16.0/9.0)]
    public void ModeTable_CanonicalScaleDegree(
        TuningSystem system, Mode mode, char letter, int alteration, double expected)
        => Assert.Equal(expected,
            TuningTables.LookupRatio(system, mode, letter, alteration),
            precision: 10);
}
```

**5. flow-lang.Tests/Unit/Phase23/SpellingAwareTuningFacts.cs** — D-09 spelling distinctions (Eb≠D# etc.):

```csharp
using FlowLang.StandardLibrary.Audio.Tuning;
using Xunit;

namespace FlowLang.Tests.Unit.Phase23;

/// <summary>
/// D-09 spelling-aware ratio tables: under JI / Pythagorean, Eb and D# render at
/// different ratios. The chromatic ratio table keys on (Letter, Alteration), not
/// on semitone offset — RESEARCH §Pitfall 5 #3 + Pattern 2.
/// </summary>
public class SpellingAwareTuningFacts
{
    [Fact]
    public void JI_Eb_DistinctFrom_DSharp()
    {
        double eFlat = TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'E', -1);
        double dSharp = TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'D', +1);
        Assert.Equal(6.0/5.0, eFlat, precision: 10);
        Assert.Equal(75.0/64.0, dSharp, precision: 10);
        Assert.NotEqual(eFlat, dSharp);
    }

    [Fact]
    public void Pythagorean_Eb_DistinctFrom_DSharp()
    {
        double eFlat = TuningTables.LookupRatio(TuningSystem.Pythagorean, Mode.Major, 'E', -1);
        double dSharp = TuningTables.LookupRatio(TuningSystem.Pythagorean, Mode.Major, 'D', +1);
        Assert.Equal(32.0/27.0, eFlat, precision: 10);
        Assert.Equal(19683.0/16384.0, dSharp, precision: 10);
        Assert.NotEqual(eFlat, dSharp);
    }

    [Fact]
    public void JI_Tritone_AsymmetricPair()
    {
        // Wikipedia asymmetric 5-limit chromatic table: F# = 25/18 (planned per
        // RESEARCH; alternative 45/32 noted as Pitfall 2 variant), Gb = 64/45.
        double fSharp = TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'F', +1);
        double gFlat = TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'G', -1);
        Assert.NotEqual(fSharp, gFlat);
    }

    [Fact]
    public void EqualTemperament_Bypasses_TableLookup()
    {
        // Sanity: no (EqualTemperament, *) entry exists. Callers MUST short-circuit
        // before LookupRatio per Pitfall 6.
        Assert.Throws<KeyNotFoundException>(() =>
            TuningTables.LookupRatio(TuningSystem.EqualTemperament, Mode.Major, 'E', 0));
    }
}
```

**6. flow-lang.Tests/Unit/Phase23/CentOffsetAdditivityFacts.cs** — D-10 cent-additive math:

```csharp
using FlowLang.StandardLibrary.Audio.Tuning;
using Xunit;

namespace FlowLang.Tests.Unit.Phase23;

/// <summary>
/// D-10 cent offsets compose additively in cent-space:
///   freq = tonic_hz * ratio * 2^(cents/1200).
/// Per the charitable-interpretation memory, cents never silently disappear.
/// </summary>
public class CentOffsetAdditivityFacts
{
    [Fact]
    public void ZeroCents_ReturnsOne()
        => Assert.Equal(1.0, RatioMath.CentOffsetMultiplier(0.0), precision: 10);

    [Fact]
    public void OneOctave_ReturnsTwo()
        => Assert.Equal(2.0, RatioMath.CentOffsetMultiplier(1200.0), precision: 10);

    [Fact]
    public void NegativeOctave_ReturnsHalf()
        => Assert.Equal(0.5, RatioMath.CentOffsetMultiplier(-1200.0), precision: 10);

    [Fact]
    public void JI_FifthPlus5Cents_AppliesToRatio()
    {
        // E4+5c under JI Major: ratio 5/4 multiplied by 2^(5/1200).
        double ratio = TuningTables.LookupRatio(TuningSystem.JustIntonation, Mode.Major, 'E', 0);
        double withCents = ratio * RatioMath.CentOffsetMultiplier(5.0);
        double expected = (5.0/4.0) * Math.Pow(2.0, 5.0/1200.0);
        Assert.Equal(expected, withCents, precision: 10);
    }
}
```

**Test directory creation:** if `flow-lang.Tests/Unit/Phase23/` does not exist, create it via writing the first Fact file (no `mkdir` needed — `dotnet test` discovers any `.cs` under the project root).

After all six files written, run the Phase23 filter — should compile + Pass GREEN immediately (Pass-1 strict matches; Phase 18-22 precedent shows zero-divergence common when ratios are pinned from canonical sources).

Verify byte-identical contract still holds (no regression — these are pure additions):
```
dotnet test flow-lang.Tests --filter "ByteIdentical" --logger:"console;verbosity=normal"
```
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase23" --logger:"console;verbosity=normal" 2>&amp;1 | tail -30</automated>
    Expected: all Phase23 Facts (TuningRatioFacts + TuningModeShiftFacts + SpellingAwareTuningFacts + CentOffsetAdditivityFacts) PASS. ~30+ Facts. If any Fact fails, the corresponding ratio in `TuningTables.cs` is wrong vs the cited source — fix the table, not the test.
  </verify>
  <acceptance_criteria>
    - `flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs` exists; `grep -c "TuningSystem\.\(JustIntonation\|Pythagorean\)" flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs` >= 14 (one per Tables entry).
    - All 14 mode-keyed tables instantiate via `ChromaticRatioTable.Build`: `grep -c "ChromaticRatioTable\.Build(" flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs` == 14.
    - `flow-lang/StandardLibrary/Audio/Tuning/RatioMath.cs` exists with `TonicHzFromKey` and `CentOffsetMultiplier`.
    - `dotnet test flow-lang.Tests --filter "TuningRatioFacts.JustMajor_CtoE_Is5to4"` PASSES (canonical 5:4 canary).
    - `dotnet test flow-lang.Tests --filter "TuningRatioFacts.PythagoreanMajor_CtoE_Is81to64"` PASSES (canonical 81:64 canary).
    - `dotnet test flow-lang.Tests --filter "TuningRatioFacts.Tables_HasExactly14Entries"` PASSES (exactly 14 mode tables).
    - `dotnet test flow-lang.Tests --filter "SpellingAwareTuningFacts.JI_Eb_DistinctFrom_DSharp"` PASSES (D-09 distinct).
    - `dotnet test flow-lang.Tests --filter "SpellingAwareTuningFacts.EqualTemperament_Bypasses_TableLookup"` PASSES — `TuningTables.LookupRatio(EqualTemperament, *, *, *)` THROWS `KeyNotFoundException` (Pitfall 6 short-circuit invariant).
    - `dotnet test flow-lang.Tests --filter "CentOffsetAdditivityFacts.OneOctave_ReturnsTwo"` PASSES (cent math sanity).
    - `dotnet test flow-lang.Tests --filter "TuningModeShiftFacts"` PASSES — all 14 Theory rows GREEN.
    - `dotnet test flow-lang.Tests --filter "ByteIdentical"` PASSES — pre-Phase-23 byte-identical contract preserved (these are pure additions; no existing path touched).
    - All 6 source files use `namespace FlowLang.StandardLibrary.Audio.Tuning;` or `namespace FlowLang.Tests.Unit.Phase23;` (file-scoped).
    - Wikipedia + Mudcat citations present in `TuningTables.cs` doc-comments: `grep -c "Five-limit_tuning\|JUSTINT\|Pythagorean_tuning" flow-lang/StandardLibrary/Audio/Tuning/TuningTables.cs` >= 3.
  </acceptance_criteria>
  <done>
    14 ratio tables compile and pass canonical Facts. ~30 Phase23 Facts GREEN. The math foundation for Wave 2's `PitchConversion` overload is in place — Wave 2 can call `TuningTables.LookupRatio` and `RatioMath.CentOffsetMultiplier` directly. ByteIdentical regression suite still GREEN (no existing-code touch).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code -> ratio tables | Tables are static `readonly` data populated from canonical references; no runtime input crosses this boundary. |
| Test harness -> production code | Tests assert on public API (`TuningTables.LookupRatio`, `RatioMath.CentOffsetMultiplier`); no privileged access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | `TuningTables.JustIonian` etc. (14 ratio tables) | mitigate | Doc-comments cite Wikipedia + Mudcat verbatim; canonical Facts pin every advertised ratio (5:4, 81:64, 6/5≠75/64, 14 mode rows) — silent edit of any ratio fails RED in `TuningRatioFacts` / `TuningModeShiftFacts`. Byte-identical regression gate (Plan 23-04 / determinism gate) catches downstream drift. |
| T-23-01-02 | Tampering | byte-identical regression contract | mitigate | Pure additions in this plan — `TuningTables` is brand new; no existing code path modified. ByteIdenticalTutorialTests + ByteIdenticalShowcaseTests must remain GREEN as a tampering-detection signal. |
| T-23-01-03 | Information Disclosure | n/a | accept | All inputs are static numeric constants from public references; no PII, secrets, or untrusted data flow through ratio math. |
| T-23-01-04 | Denial of Service | `TuningTables.LookupRatio` | accept | Dictionary lookup is O(1); ratio math is O(1) `Math.Pow`. No untrusted-input path; user can only supply enum members from a closed set. |
| T-23-01-05 | Repudiation | n/a | accept | No persisted state; warnings/audit live in Wave 3 plan (RenderingDiagnostics). |
| T-23-01-06 | Spoofing | n/a | accept | No authentication / network surface in this plan. |
| T-23-01-07 | Elevation of Privilege | `ChromaticRatioTable.Build` | mitigate | Throws `InvalidOperationException` if any of {C,D,E,F,G,A,B} naturals are missing — prevents partial-table construction silently producing 12-TET fallback (which would mask MICR-01 acceptance failure). |
</threat_model>

<verification>
After both tasks land, the following hold:

1. **Compile**: `dotnet build flow-lang.sln` SUCCESS, 0 errors. ~6 new files in `flow-lang/StandardLibrary/Audio/Tuning/`, ~4 new files in `flow-lang.Tests/Unit/Phase23/`.

2. **Unit Facts**: `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase23"` returns ~30+ Facts ALL GREEN. Specifically:
   - `JustMajor_CtoE_Is5to4` (MICR-01 canary)
   - `PythagoreanMajor_CtoE_Is81to64` (MICR-01 Pythagorean canary)
   - `Tables_HasExactly14Entries` (D-03 invariant: 7 JI + 7 Pythagorean modes)
   - `JI_Eb_DistinctFrom_DSharp` (D-09 spelling)
   - `EqualTemperament_Bypasses_TableLookup` (Pitfall 6 short-circuit invariant)
   - `OneOctave_ReturnsTwo` (D-10 cent math)
   - `TuningModeShiftFacts` Theory: all 14 mode rows GREEN

3. **Byte-Identical Regression**: `dotnet test flow-lang.Tests --filter "ByteIdentical"` GREEN — pre-Phase-23 deterministic contract preserved (Wave 1 makes no existing-code edits).

4. **Pattern A Locked**: `RenderTuning` record struct exists; `MusicalContext.Current` static accessor is NOT introduced — `grep -c "MusicalContext\.Current" flow-lang/` returns 0. Wave 2 builds against `RenderTuning`, not ambient state.
</verification>

<success_criteria>
- 6 production type files exist under `flow-lang/StandardLibrary/Audio/Tuning/`: `TuningSystem.cs`, `Mode.cs`, `RenderTuning.cs`, `ChromaticRatioTable.cs`, `TuningTables.cs`, `RatioMath.cs`.
- `TuningTables.Tables` contains exactly 14 entries: { JI, Pythagorean } x { Major, Minor, Dorian, Phrygian, Lydian, Mixolydian, Locrian }.
- 4 xUnit Fact files exist under `flow-lang.Tests/Unit/Phase23/`: `TuningRatioFacts.cs`, `TuningModeShiftFacts.cs`, `SpellingAwareTuningFacts.cs`, `CentOffsetAdditivityFacts.cs`. Total ~30+ Facts.
- All canonical canary ratios (5:4 JI third, 81:64 Pythagorean third, 6/5 vs 75/64 spelling, 14 mode-shift rows, cent additivity) GREEN.
- ByteIdentical regression contract preserved (Wave 1 makes pure additions; no existing path touched).
- Pattern A locked via `RenderTuning` value object (Pattern B static-ambient accessor NOT introduced).
- 2 atomic commits per the per-task pattern: `feat(23-01): add TuningSystem/Mode/RenderTuning/ChromaticRatioTable scaffolding (Pattern A)` and `feat(23-01): populate 14 ratio tables (7 JI + 7 Pythagorean modes) with canonical Facts`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-microtonal-tuning-wedge/23-01-SUMMARY.md` documenting:
- Pattern A vs Pattern B decision rationale (Pattern B has zero codebase analogs; Pattern A mirrors `SongRenderer.RenderSection` per-section state resolution).
- Wikipedia asymmetric 5-limit JI table + Mudcat mode shifts + Wikipedia Pythagorean chain pinned with citations.
- Two-pass strict outcome (expected: zero divergence; Pass-1 RED Facts authored from canonical sources match Pass-2 reality verbatim).
- Open questions for Wave 2: PitchConversion overload signature (`NoteToFrequency(MusicalNoteData note, RenderTuning tuning)`); octave-displacement math for tonic-relative ratios when note's octave differs from tonic's.
</output>
