---
phase: 29-instrument-realism
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/29-instrument-realism/29-VERIFICATION.md
  - examples/output/realism_ab/answer_key.txt
  - examples/output/realism_ab/A_piano.wav
  - examples/output/realism_ab/B_piano.wav
  - examples/output/realism_ab/A_brass.wav
  - examples/output/realism_ab/B_brass.wav
  - examples/output/realism_ab/A_sax.wav
  - examples/output/realism_ab/B_sax.wav
  - examples/output/realism_ab/A_strings.wav
  - examples/output/realism_ab/B_strings.wav
  - examples/output/realism_ab/A_flute.wav
  - examples/output/realism_ab/B_flute.wav
  - examples/output/realism_ab/A_drums.wav
  - examples/output/realism_ab/B_drums.wav
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - CLAUDE.md
autonomous: false
user_setup:
  - "Composer listens to all 12 A/B WAVs (A_*.wav vs B_*.wav per fixture)."
  - "Composer writes A/B guesses into 29-VERIFICATION.md 'Blind A/B Sign-off' section per fixture."
  - "Composer unseals answer_key.txt and tallies correct guesses (≥ 5/6 required)."
  - "Composer writes one reflection paragraph per fixture (6 paragraphs) in 29-VERIFICATION.md 'Closure Reflection' section."
  - "Composer manually re-verifies each LICENSE.md Source: URL leads to a verified CC0 / public-domain page (Gate C closure-time review)."
requirements: [REQ-7, REQ-8]
must_haves:
  truths:
    - "29-VERIFICATION.md exists with 5 explicit gate sections + a 'Blind A/B Sign-off' subsection + a 'Closure Reflection' subsection (per SPEC D-30)"
    - "Gate A: composer's A/B tally is N ≥ 5/6 correct; answer_key.txt unsealed and committed (per SPEC D-29 Gate A)"
    - "Gate B: `du -sh flow-lang/Samples/` ≤ 5 MB (per SPEC D-29 Gate B); RepoSizeTests green"
    - "Gate C: each flow-lang/Samples/{instrument}/LICENSE.md verified at closure time (Source: URL leads to verified CC0 / public-domain declaration) (per SPEC D-29 Gate C); LicenseAuditTests green"
    - "Gate D: `dotnet test flow-lang.Tests --nologo` exits 0 (per SPEC D-29 Gate D)"
    - "Gate E: 6 composer reflection paragraphs (one per A/B fixture) written in 29-VERIFICATION.md (per SPEC D-29 Gate E)"
    - "ROADMAP.md Phase 29 entry status updated to Complete (or appropriate closure indicator matching project conventions)"
    - "STATE.md updated with Phase 29 closure marker"
    - "CLAUDE.md updated to mention Phase 29 features under 'Core' or 'Music-Specific' Language Features section"
  artifacts:
    - path: ".planning/phases/29-instrument-realism/29-VERIFICATION.md"
      provides: "REQ-8 5-gate closure contract + REQ-7 A/B sign-off"
      contains: "Gate A|Gate B|Gate C|Gate D|Gate E|Blind A/B Sign-off|Closure Reflection"
      min_lines: 80
    - path: "examples/output/realism_ab/answer_key.txt"
      provides: "REQ-7 sealed-then-unsealed answer key"
      contains: "piano:|brass:|sax:|strings:|flute:|drums:"
      min_lines: 8
    - path: "examples/output/realism_ab/A_piano.wav"
      provides: "REQ-7 A/B WAV (Phase 28 or Phase 29 — randomized)"
      contains: null
      min_lines: null
    - path: ".planning/ROADMAP.md"
      provides: "REQ-8 closure marker — Phase 29 status updated"
      contains: "29. Instrument Realism.*Complete|29\\. Instrument Realism.*✓"
      min_lines: null  # MODIFIED — table row update
    - path: "CLAUDE.md"
      provides: "Documentation continuity — Phase 29 features appear in language-features section"
      contains: "Phase 29|sample-based|SampledInstrumentRenderer"
      min_lines: null  # MODIFIED — additive
  key_links:
    - from: ".planning/phases/29-instrument-realism/29-VERIFICATION.md"
      to: "examples/output/realism_ab/answer_key.txt"
      via: "Gate A section references the unsealed answer key + composer's recorded guesses + tally"
      pattern: "answer_key|Gate A"
    - from: ".planning/phases/29-instrument-realism/29-VERIFICATION.md"
      to: "flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs"
      via: "Gate B section asserts du -sh ≤ 5 MB; cites RepoSizeTests for automated enforcement"
      pattern: "Gate B|RepoSizeTests"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/29-instrument-realism/29-VERIFICATION.md"
      via: "Phase 29 closure marker in ROADMAP points to 29-VERIFICATION.md for the verifiable evidence"
      pattern: "29. Instrument Realism"
---

<objective>
Wave 5 — closure. Run the A/B render script (with composer present), capture blind A/B answers, unseal the key, write the 5-gate closure document (29-VERIFICATION.md), and stamp Phase 29 complete in ROADMAP / STATE / CLAUDE.md.

This plan is non-autonomous because the composer must manually:
1. Listen to all 12 A/B WAVs.
2. Write guesses into 29-VERIFICATION.md.
3. Unseal the answer key (single command).
4. Tally correctness; if < 5/6, Phase 29 closure BLOCKS (per SPEC D-29 Gate A — no force-close exemption).
5. Write reflection paragraphs (Gate E).
6. Re-verify license source URLs (Gate C closure review).

Purpose: REQ-7 (A/B sign-off ≥ 5/6) + REQ-8 (5 closure gates ALL pass — no force-close exemption per SPEC D-29).

Output: 29-VERIFICATION.md + 12 A/B WAVs (committed for traceability) + answer_key.txt (sealed then unsealed) + ROADMAP / STATE / CLAUDE.md updates marking Phase 29 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-instrument-realism/29-SPEC.md
@.planning/phases/29-instrument-realism/29-CONTEXT.md
@.planning/phases/29-instrument-realism/29-RESEARCH.md
@.planning/phases/29-instrument-realism/29-VALIDATION.md
@CLAUDE.md
@examples/scripts/realism_ab_render.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render Phase 28 baseline WAVs for each fixture</name>
  <files>examples/output/realism_ab/phase28_baseline/piano_rendered.wav, examples/output/realism_ab/phase28_baseline/brass_rendered.wav, examples/output/realism_ab/phase28_baseline/sax_rendered.wav, examples/output/realism_ab/phase28_baseline/strings_rendered.wav, examples/output/realism_ab/phase28_baseline/flute_rendered.wav, examples/output/realism_ab/phase28_baseline/drums_rendered.wav</files>
  <read_first>
    .planning/phases/28-midi-audio-polyphony-articulation-rewrite/28-SPEC.md (Phase 28 closure marker — git rev to check out for baseline)
    examples/scripts/realism_ab_render.sh (path expectations)
  </read_first>
  <action>
    Render each of the 6 fixtures (examples/tests/realism_ab/*.flow) under the Phase 28 closure state.

    Steps:
    1. Identify the git commit hash of Phase 28 closure (from `.planning/STATE.md` or `git log --grep="Phase 28.*closure" --oneline` or `git log --grep="28.*VERIFICATION"`)
    2. Check out a temporary worktree at that commit: `git worktree add /tmp/phase28-baseline <phase28-closure-sha>`
    3. In that worktree, run each fixture: `cd /tmp/phase28-baseline && dotnet run --project flow-interpreter examples/tests/realism_ab/<instrument>.flow`
       NOTE: The fixtures themselves are NEW in Plan 06 (post-Phase-28); they don't exist at Phase 28's commit. Workaround: copy fixtures into the worktree before rendering: `cp examples/tests/realism_ab/*.flow /tmp/phase28-baseline/examples/tests/realism_ab/`
    4. Move outputs to `examples/output/realism_ab/phase28_baseline/<instrument>_rendered.wav` (in main worktree)
    5. Remove the temporary worktree: `git worktree remove /tmp/phase28-baseline`

    Alternative if Phase 28 closure state has incompatible script syntax (e.g. fixtures use Phase 28 Legato `leg` syntax that didn't exist pre-Phase-28): the fixtures need only render under the Phase 28 closure tag, which DOES have Legato. The dependency chain in this plan is: Phase 28 closed → fixtures authored at Plan 06 use Phase 28 features → baseline render uses Phase 28's CLOSURE state, which has those features.

    If the temporary-worktree approach is fragile, alternate route: hand-mark Phase 29's render with the synth changes reverted (`git stash` synth changes, render baselines, `git stash pop`). Either way, the artifact is the same — 6 baseline WAVs in `examples/output/realism_ab/phase28_baseline/`.
  </action>
  <verify>
    `ls examples/output/realism_ab/phase28_baseline/` shows 6 .wav files.
    `for f in examples/output/realism_ab/phase28_baseline/*.wav; do test -s "$f" || echo "EMPTY: $f"; done` outputs nothing.
  </verify>
  <acceptance_criteria>
    - 6 baseline WAVs exist at `examples/output/realism_ab/phase28_baseline/{piano,brass,sax,strings,flute,drums}_rendered.wav`
    - Each baseline WAV is non-empty (file size > 1 KB at minimum)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the A/B render script with composer present</name>
  <files>examples/output/realism_ab/A_*.wav, examples/output/realism_ab/B_*.wav, examples/output/realism_ab/answer_key.txt</files>
  <read_first>
    examples/scripts/realism_ab_render.sh
  </read_first>
  <action>
    Execute the A/B render script:
    ```bash
    bash examples/scripts/realism_ab_render.sh
    ```
    Verify the script:
    1. Renders all 6 fixtures under Phase 29.
    2. Combines with phase28_baseline/*.wav.
    3. Produces 12 A/B WAVs in examples/output/realism_ab/.
    4. Writes a sealed answer_key.txt.

    IMPORTANT: The composer must NOT view answer_key.txt yet. The next task is the listening phase; viewing the key first invalidates the test.
  </action>
  <verify>
    `ls examples/output/realism_ab/A_*.wav examples/output/realism_ab/B_*.wav | wc -l` returns 12.
    `test -s examples/output/realism_ab/answer_key.txt` passes.
    `grep -c "piano:\|brass:\|sax:\|strings:\|flute:\|drums:" examples/output/realism_ab/answer_key.txt` returns 6.
  </verify>
  <acceptance_criteria>
    - 12 A/B WAV files exist (6 instruments × A/B labels)
    - answer_key.txt exists with 6 instrument lines, each followed by either "A" or "B"
    - Composer has NOT yet viewed answer_key.txt
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Initialize 29-VERIFICATION.md with closure-gate skeleton</name>
  <files>.planning/phases/29-instrument-realism/29-VERIFICATION.md</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-SPEC.md (Gate definitions — Gates A/B/C/D/E)
    .planning/phases/29-instrument-realism/29-VALIDATION.md (Manual UAT section)
    .planning/phases/26.2-music-type-ergonomics-fx-overloads-inserted/26.2-VERIFICATION.md (recent prior closure example to match style)
  </read_first>
  <action>
    Create `.planning/phases/29-instrument-realism/29-VERIFICATION.md`:
    ```markdown
    # Phase 29 — Verification & Closure

    **Created:** 2026-05-10
    **Closure status:** pending (5 gates required; no force-close)

    ## Summary

    Phase 29 ships sample-based playback for 6 tonal instruments (Piano, Brass, Sax, Strings, Flute, Bell) backed by a curated CC0 sample library bundled in-repo (≤ 5 MB), DSP improvements for 3 retained synthesizers (Drums, Organ, Wavetable) with ≥ 20% harmonic-richness gain each, and Phase 28's articulation envelopes applied on top of the sample for the tonal instruments. Closure requires all 5 gates green.

    ---

    ## Gate A — Blind A/B Listening Test (REQ-7)

    **Threshold:** composer correctly identifies Phase 29 as more realistic on ≥ 5 of 6 fixtures.

    **Procedure:**
    1. Listen to A_piano.wav vs B_piano.wav. Decide which sounds more realistic. Record below.
    2. Repeat for brass, sax, strings, flute, drums.
    3. Run `cat examples/output/realism_ab/answer_key.txt` to unseal the key.
    4. Tally correctness.

    ### Blind A/B Sign-off

    | Fixture | Composer's Guess (A or B = Phase 29) | Actual (from answer_key.txt) | Correct? |
    |---------|--------------------------------------|------------------------------|----------|
    | piano   | _____                                | _____                        | _____    |
    | brass   | _____                                | _____                        | _____    |
    | sax     | _____                                | _____                        | _____    |
    | strings | _____                                | _____                        | _____    |
    | flute   | _____                                | _____                        | _____    |
    | drums   | _____                                | _____                        | _____    |

    **Correct: N/6**

    **Gate A status:** [ ] PASS (N ≥ 5)  [ ] FAIL (N < 5 — Phase 29 closure blocks per SPEC D-29; do not force-close)

    ---

    ## Gate B — Repo Size Cap (REQ-2)

    **Threshold:** `du -sh flow-lang/Samples/` ≤ 5 MB.

    **Verification:**
    ```
    $ du -sh flow-lang/Samples/
    <output here>
    ```

    **Automated enforcement:** `flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs` asserts ≤ 5*1024*1024 bytes; runs in CI.

    **Gate B status:** [ ] PASS  [ ] FAIL

    ---

    ## Gate C — Per-Instrument License Docs (REQ-2)

    **Threshold:** every `flow-lang/Samples/{instrument}/LICENSE.md` exists with `License:` + `Source:` lines, declaring CC0 or Public Domain (no CC-BY).

    **Verification (automated):** `flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs` runs the audit.

    **Verification (manual, closure-time):** For each instrument, open the `Source:` URL from LICENSE.md and confirm the destination page declares CC0 / public-domain. Document below:

    | Instrument | LICENSE.md path                         | Source URL (open in browser) | CC0 / PD confirmed at URL? |
    |------------|-----------------------------------------|------------------------------|---------------------------|
    | piano      | flow-lang/Samples/piano/LICENSE.md      | <url>                        | [ ] yes  [ ] no            |
    | brass      | flow-lang/Samples/brass/LICENSE.md      | <url>                        | [ ] yes  [ ] no            |
    | sax        | flow-lang/Samples/sax/LICENSE.md        | <url>                        | [ ] yes  [ ] no            |
    | strings    | flow-lang/Samples/strings/LICENSE.md    | <url>                        | [ ] yes  [ ] no            |
    | flute      | flow-lang/Samples/flute/LICENSE.md      | <url>                        | [ ] yes  [ ] no            |
    | bell       | flow-lang/Samples/bell/LICENSE.md       | <url>                        | [ ] yes  [ ] no            |

    **Gate C status:** [ ] PASS  [ ] FAIL

    ---

    ## Gate D — Full Unit Suite Green (REQ-8)

    **Threshold:** `dotnet test flow-lang.Tests --nologo` exits 0.

    **Verification:**
    ```
    $ dotnet test flow-lang.Tests --nologo
    <test summary here — paste output>
    ```

    **Expected:** zero failures; Phase 29 added N new tests (LicenseAuditTests 6, RepoSizeTests 1, SampleCacheTests 3, SampledInstrumentSmokeTests 6, VelocityLayerTests 6, ArticulationOnSampleTests 7, HarmonicRichnessTests 6, AbFixtureSmokeTests 6, Phase29ByteIdenticalTests 6) = approximately 47 new tests, all green.

    **Gate D status:** [ ] PASS  [ ] FAIL

    ---

    ## Gate E — Per-Fixture Reflection (REQ-7 / REQ-8)

    **Threshold:** 6 composer reflection paragraphs (one per A/B fixture) explaining which timbral aspects improved.

    ### Closure Reflection

    **Piano:** _(1 paragraph — what audibly improved? velocity-layer crossfade audible? articulation envelopes distinct?)_

    **Brass:** _(1 paragraph)_

    **Sax:** _(1 paragraph)_

    **Strings:** _(1 paragraph)_

    **Flute:** _(1 paragraph)_

    **Drums:** _(1 paragraph — harmonic-richness gain audible? multi-component synthesis recipe working?)_

    **Gate E status:** [ ] PASS (6 paragraphs written)  [ ] FAIL

    ---

    ## Two-Run Determinism Continuity

    Phase 28's two-run determinism contract continues. Phase 29's `Phase29ByteIdenticalTests` extends the pattern.

    **Verification:**
    ```
    $ dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase29ByteIdenticalTests" --nologo
    <output>
    ```

    [ ] All 6 fixtures pass byte-identical two-run determinism

    ---

    ## Phase 29 Closure Decision

    All 5 gates must pass. If ANY gate fails, Phase 29 does NOT close (per SPEC D-29 — no force-close exemption).

    - [ ] Gate A: A/B ≥ 5/6 correct
    - [ ] Gate B: ≤ 5 MB
    - [ ] Gate C: per-instrument licenses verified
    - [ ] Gate D: full unit suite green
    - [ ] Gate E: 6 reflection paragraphs

    **Closure date:** _____
    **Composer signature (Noah):** _____
    **Closure commit hash (after this file lands):** _____
    ```
  </action>
  <verify>
    `test -s .planning/phases/29-instrument-realism/29-VERIFICATION.md` passes.
    `grep -c "Gate A\|Gate B\|Gate C\|Gate D\|Gate E" .planning/phases/29-instrument-realism/29-VERIFICATION.md` returns ≥ 10 (each gate referenced multiple times).
  </verify>
  <acceptance_criteria>
    - 29-VERIFICATION.md exists with 5 gate sections + Blind A/B Sign-off subsection + Closure Reflection subsection
    - Each gate has a clear threshold + verification procedure + checkbox
    - Tables are templated (composer fills in during Tasks 4-6 below)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Composer A/B listen + record guesses + unseal + tally</name>
  <files>.planning/phases/29-instrument-realism/29-VERIFICATION.md</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-VERIFICATION.md (template skeleton from Task 3)
    examples/output/realism_ab/A_*.wav, examples/output/realism_ab/B_*.wav (listen to all 12)
  </read_first>
  <action>
    **(Composer task — manual.)** Listen to each A/B pair in isolation. For each fixture:
    1. Play A_<instrument>.wav.
    2. Play B_<instrument>.wav.
    3. Decide which sounds more realistic / closer to a real recording. Write the letter (A or B) under "Composer's Guess".
    4. Move on. Do NOT view answer_key.txt yet.

    After all 6 guesses are written:
    1. Run `cat examples/output/realism_ab/answer_key.txt`.
    2. Fill in the "Actual" column.
    3. Mark each row Correct? = yes if guess == actual, else no.
    4. Total in "Correct: N/6".

    If N ≥ 5: Gate A passes — check the [PASS] box.
    If N < 5: Gate A fails — phase closure is BLOCKED (per SPEC D-29). Do NOT mark closure. The composer must either:
    (a) Accept that the realism gain wasn't perceived → roll back Phase 29 + investigate sample quality / synth improvements.
    (b) Identify a confound (e.g. headphone vs speakers, listening at low volume, focus distractions) → re-render + re-listen.
  </action>
  <verify>
    `grep -A 10 "Blind A/B Sign-off" .planning/phases/29-instrument-realism/29-VERIFICATION.md` shows all 6 rows filled in (no _____ placeholders remaining in the A/B table).
    `grep "Correct: [0-9]/6" .planning/phases/29-instrument-realism/29-VERIFICATION.md` matches.
    `grep "Gate A status.*PASS" .planning/phases/29-instrument-realism/29-VERIFICATION.md` matches (or document Gate A failure path).
  </verify>
  <acceptance_criteria>
    - 6 A/B rows filled in (no `_____` placeholders left in the Blind A/B Sign-off table)
    - Correct N tally written
    - If N ≥ 5, Gate A [PASS] box checked
    - If N < 5, closure is blocked (per SPEC D-29); plan halts and flags blocker
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Composer per-fixture reflection paragraphs</name>
  <files>.planning/phases/29-instrument-realism/29-VERIFICATION.md</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-VERIFICATION.md (Closure Reflection section)
  </read_first>
  <action>
    **(Composer task — manual.)** For each of 6 A/B fixtures, write a 1-paragraph reflection (3-6 sentences) explaining which timbral aspects of Phase 29 improved vs Phase 28. Examples of aspects to discuss:
    - **Piano:** velocity-layer crossfade audible? Did soft notes sound softer in timbre (not just amplitude)? Did Staccato vs Legato envelope shaping land cleanly on top of the sample?
    - **Brass:** sample tone vs hand-rolled sawtooth-plus-octave? More "horn-like"?
    - **Sax:** breath / mouthpiece noise audible from sample that wasn't in hand-rolled?
    - **Strings:** bow attack / sustain texture realistic?
    - **Flute:** breath / overblow / attack character realistic?
    - **Drums:** harmonic-richness gain audible? Multi-component kick "fatter"? Snare "snappier"?

    Write into the 6 paragraph slots in the Closure Reflection section.

    After all 6 paragraphs written: check Gate E [PASS] box.
  </action>
  <verify>
    `awk '/Closure Reflection/,/Gate E status/' .planning/phases/29-instrument-realism/29-VERIFICATION.md | grep -c "^\*\*" ` returns ≥ 6 (each paragraph starts with `**Instrument:**`).
    Each paragraph has ≥ 3 sentences (length proxy: `grep -A 1 "^\*\*Piano" .planning/phases/29-instrument-realism/29-VERIFICATION.md | wc -w` returns ≥ 20).
    Gate E [PASS] box checked.
  </verify>
  <acceptance_criteria>
    - 6 reflection paragraphs written (one per fixture), each ≥ 3 sentences
    - Gate E [PASS] box checked
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Composer manual license-URL verification (Gate C closure review)</name>
  <files>.planning/phases/29-instrument-realism/29-VERIFICATION.md</files>
  <read_first>
    flow-lang/Samples/piano/LICENSE.md, flow-lang/Samples/brass/LICENSE.md, flow-lang/Samples/sax/LICENSE.md, flow-lang/Samples/strings/LICENSE.md, flow-lang/Samples/flute/LICENSE.md, flow-lang/Samples/bell/LICENSE.md
    .planning/phases/29-instrument-realism/29-VERIFICATION.md (Gate C section)
  </read_first>
  <action>
    **(Composer task — manual.)** For each of 6 instrument subdirs:
    1. Open `flow-lang/Samples/<instrument>/LICENSE.md`. Note the `Source:` URL.
    2. Open the URL in a browser.
    3. Confirm the destination page declares CC0 / public-domain explicitly. (Freesound: should show "Creative Commons 0" license badge. Internet Archive: should show "public domain" or CC0 license metadata. Self-recorded: should be a personal release statement.)
    4. Fill in the URL + check the "CC0 / PD confirmed at URL?" box for that row.

    If any URL fails verification (URL 404, page declares a different license, page no longer exists):
    - **Option 1:** find an alternate verified-CC0 source for that sample. Replace the WAV + update LICENSE.md.
    - **Option 2:** record the sample fresh (Route 4 from RESEARCH) and update LICENSE.md to declare it self-recorded under CC0.
    - **Option 3 (last resort):** archive the original CC0 source page via web.archive.org and update LICENSE.md's Source: line to point to the archived version.

    After all 6 rows verified: check Gate C [PASS] box.
  </action>
  <verify>
    `grep -c "\[ \] yes" .planning/phases/29-instrument-realism/29-VERIFICATION.md` returns 0 (or ≤ 0 — every row marked yes).
    `grep -c "\[x\] yes" .planning/phases/29-instrument-realism/29-VERIFICATION.md` returns 6.
    Gate C [PASS] box checked.
  </verify>
  <acceptance_criteria>
    - All 6 rows in Gate C's verification table have "CC0 / PD confirmed at URL?" checked yes
    - Gate C [PASS] box checked
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Run final full-suite + size check; fill Gates B + D</name>
  <files>.planning/phases/29-instrument-realism/29-VERIFICATION.md</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-VERIFICATION.md (Gate B + Gate D sections)
  </read_first>
  <action>
    Run final verification commands and paste output into 29-VERIFICATION.md:
    1. `du -sh flow-lang/Samples/` — paste output under Gate B. Check [PASS] if ≤ 5 MB.
    2. `dotnet test flow-lang.Tests --nologo` — paste summary under Gate D. Check [PASS] if exit 0 + Phase29 test pass count matches expected.
    3. `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase29ByteIdenticalTests" --nologo` — paste under "Two-Run Determinism Continuity". Check passing.

    If any check fails, the corresponding gate FAILS and Phase 29 closure blocks. No force-close.
  </action>
  <verify>
    `grep "Gate B status.*PASS" .planning/phases/29-instrument-realism/29-VERIFICATION.md` matches.
    `grep "Gate D status.*PASS" .planning/phases/29-instrument-realism/29-VERIFICATION.md` matches.
    All 5 gate checkboxes in the "Phase 29 Closure Decision" section are checked.
  </verify>
  <acceptance_criteria>
    - Gate B passes (du -sh ≤ 5 MB)
    - Gate D passes (dotnet test exits 0)
    - Two-Run Determinism passes
    - All 5 gates in the Closure Decision section are checked
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Update ROADMAP, STATE, CLAUDE.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, CLAUDE.md</files>
  <read_first>
    .planning/ROADMAP.md (find Phase 29 progress-table row)
    .planning/STATE.md (find current state pointer + Phase 29 marker if any)
    CLAUDE.md (find "Language Features" section, then "Core" or "Music-Specific" subsection)
  </read_first>
  <action>
    Three updates:

    **1. `.planning/ROADMAP.md`** — In the Progress table, update the Phase 29 row from "Not started | -" to "Complete   | <today's date>" with plan count "7/7". The exact column count + format must match the surrounding table rows (look at Phase 26.2 row for current style).

    **2. `.planning/STATE.md`** — Update STATUS to reflect Phase 29 closed. Add Phase 29 to "Completed Phases" list with closure commit hash placeholder (to be filled when this plan's closure commit lands). Set "Current Phase" to next unstarted phase (or v1.4 closure milestone if Phase 29 is the v1.4 terminal phase).

    **3. `CLAUDE.md`** — In the `## Language Features` section, add a bullet under "Music-Specific" (or wherever fits the existing structure):
    ```
    - **Sample-based playback for tonal instruments:** Piano / Brass / Sax / Strings / Flute / Bell render via bundled CC0 sample library (≤ 5 MB at `flow-lang/Samples/`); piano has pp + ff velocity layers with linear crossfade; other tonal instruments use single-velocity with amplitude scaling; Phase 28's articulation envelopes (Staccato/Tenuto/Legato/Accent/Marcato/Sforzando) apply on top of sample (Phase 29)
    ```
    Also add a bullet under the "Audio" Architecture section (around the "INoteSynthesizer" mention) noting that 6 tonal instruments delegate to `SampledInstrumentRenderer` + per-FlowEngine `SampleCache` with eager-load on `renderSong` entry.

    Also update the architecture diagram or component-responsibilities table if it references "all 9 synths use hand-rolled synthesis" — that's no longer accurate.
  </action>
  <verify>
    `grep "29. Instrument Realism.*Complete\|29\\. Instrument Realism.*✓" .planning/ROADMAP.md` matches.
    `grep "Phase 29" .planning/STATE.md` returns ≥ 1 hit referring to closure.
    `grep "Phase 29\|sample-based\|SampledInstrumentRenderer\|SampleCache" CLAUDE.md` returns ≥ 2 hits.
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 29 row shows "Complete" + date
    - `.planning/STATE.md` updated to reflect Phase 29 closure
    - `CLAUDE.md` mentions Phase 29's sample-based playback feature + SampledInstrumentRenderer / SampleCache in appropriate sections
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 9: Closure commit</name>
  <files>(commits)</files>
  <read_first>
    git status (verify all changes staged appropriately)
  </read_first>
  <action>
    Stage and commit all closure artifacts:
    ```bash
    git add .planning/phases/29-instrument-realism/29-VERIFICATION.md
    git add examples/output/realism_ab/
    git add .planning/ROADMAP.md .planning/STATE.md CLAUDE.md
    git commit -m "$(cat <<'EOF'
    docs(29): closure — REQUIREMENTS/ROADMAP/STATE/VERIFICATION/CLAUDE.md

    Phase 29 (Instrument Realism) closure:
    - 5 gates pass: A/B 5+/6 correct, ≤ 5 MB, licenses verified, suite green, 6 reflections
    - 6 tonal instruments now sample-based; 3 retained synths +≥ 20% harmonic richness
    - Two-run determinism preserved (Phase 28 contract continues)
    EOF
    )"
    ```

    Then commit the answer key separately (per SPEC D-26 — "sealed in a separate commit"):
    ```bash
    git add examples/output/realism_ab/answer_key.txt
    git commit -m "docs(29): seal A/B answer key (post-listen unseal evidence)"
    ```

    Record the final closure commit hash into `29-VERIFICATION.md` "Closure commit hash" field; then amend that file in one final docs commit:
    ```bash
    CLOSURE_HASH=$(git rev-parse HEAD)
    # Edit 29-VERIFICATION.md to fill in CLOSURE_HASH
    git add .planning/phases/29-instrument-realism/29-VERIFICATION.md
    git commit -m "docs(29): record closure hash $CLOSURE_HASH in VERIFICATION"
    ```
  </action>
  <verify>
    `git log --oneline -3` shows the 3 closure commits.
    `git log --grep="Phase 29" --oneline | wc -l` shows ≥ 3 closure commits.
    `grep "Closure commit hash" .planning/phases/29-instrument-realism/29-VERIFICATION.md` shows a non-placeholder hash.
  </verify>
  <acceptance_criteria>
    - Closure commit with VERIFICATION + ROADMAP + STATE + CLAUDE.md staged
    - Separate commit for the unsealed answer_key.txt
    - 29-VERIFICATION.md updated with the final closure commit hash
    - `git log` shows the closure commits
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer self-A/B → 29-VERIFICATION.md | Composer is the trusted listener; no third-party panel. Trust-on-honor. |
| Gate threshold (≥ 5/6) → closure decision | Project policy locked in SPEC D-29; no force-close exemption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-V5-13 | Tampering | Closure decision overridden after gate failure | accept | Force-close path is explicitly rejected (SPEC D-29). Plan halts at any gate failure; composer must address root cause. |
| T-29-V5-14 | Repudiation | Composer changes A/B guess after viewing answer key | accept | Self-A/B is trust-on-honor; SPEC's manual protocol relies on listening before unsealing. The sealed-then-committed key creates an audit trail. |
</threat_model>

<verification>
- 29-VERIFICATION.md exists with 5 gate sections, all 5 PASS
- 12 A/B WAVs + answer_key.txt committed
- ROADMAP / STATE / CLAUDE.md updated to reflect closure
- Closure commits land
- Full unit suite GREEN (Gate D)
- du -sh flow-lang/Samples/ ≤ 5 MB (Gate B)
- Composer reflection paragraphs (6) present (Gate E)
- A/B blind result ≥ 5/6 correct (Gate A)
- License audit verified at closure-time (Gate C)
</verification>

<success_criteria>
- All 5 closure gates pass (no force-close)
- Phase 29 status updated to Complete in ROADMAP / STATE
- Documentation continuity preserved (CLAUDE.md mentions Phase 29 features)
- Closure commit hash recorded in 29-VERIFICATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/29-instrument-realism/29-07-SUMMARY.md` listing: actual A/B tally (e.g. "6/6 correct" or "5/6 correct"), du -sh measurement, full test suite pass count delta from pre-Phase-29, list of files removed (deleted hand-rolled synth lines), final bundle size in MB, total Phase 29 changes summary (LOC added, LOC removed, files added, files modified, files deleted).
</output>
