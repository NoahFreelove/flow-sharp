---
phase: 29-instrument-realism
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-lang/Samples/piano/LICENSE.md
  - flow-lang/Samples/piano/C2_pp.wav
  - flow-lang/Samples/piano/C2_ff.wav
  - flow-lang/Samples/piano/C3_pp.wav
  - flow-lang/Samples/piano/C3_ff.wav
  - flow-lang/Samples/piano/C4_pp.wav
  - flow-lang/Samples/piano/C4_ff.wav
  - flow-lang/Samples/piano/C5_pp.wav
  - flow-lang/Samples/piano/C5_ff.wav
  - flow-lang/Samples/piano/C6_pp.wav
  - flow-lang/Samples/piano/C6_ff.wav
  - flow-lang/Samples/brass/LICENSE.md
  - flow-lang/Samples/brass/A3.wav
  - flow-lang/Samples/brass/A4.wav
  - flow-lang/Samples/brass/A5.wav
  - flow-lang/Samples/sax/LICENSE.md
  - flow-lang/Samples/sax/F4.wav
  - flow-lang/Samples/sax/C5.wav
  - flow-lang/Samples/strings/LICENSE.md
  - flow-lang/Samples/strings/D3.wav
  - flow-lang/Samples/strings/D4.wav
  - flow-lang/Samples/strings/D5.wav
  - flow-lang/Samples/flute/LICENSE.md
  - flow-lang/Samples/flute/G4.wav
  - flow-lang/Samples/flute/G5.wav
  - flow-lang/Samples/bell/LICENSE.md
  - flow-lang/Samples/bell/C5.wav
  - flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs
  - flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs
  - .gitignore
autonomous: false
user_setup:
  - "License relaxed 2026-05-11: CC0 OR CC-BY (with attribution). NOT CC-BY-SA or CC-BY-NC. Each sample's source page must declare one of the accepted licenses."
  - "Recommended sources (composer's-choice; mix-and-match): University of Iowa Musical Instrument Samples (https://theremin.music.uiowa.edu/MIS.html — single-pitch recordings at named dynamics, free of charge with attribution); Philharmonia Orchestra (https://philharmonia.co.uk/resources/sound-samples/ — CC-BY 3.0, single notes by orchestra players); Salamander Grand Piano by Alexander Holm (CC-BY 3.0 — Yamaha C5 grand at multiple velocity layers); Versilian Studios Chamber Orchestra Community Edition (https://vis.versilstudios.com/vsco-community.html — CC0)."
  - "Mix-to-mono and convert to 44.1 kHz / 16-bit PCM WAV format (use sox / Audacity / ffmpeg). For sox: `sox INPUT.wav -r 44100 -b 16 -c 1 OUTPUT.wav trim 0 1.5` (1.5s for staccato instruments, 2.0s for sustained brass/strings)."
  - "Trim to ≤ 1.5 sec per sample (or ≤ 2 sec for sustained-tone instruments like strings/brass)."
  - "For CC-BY entries: add an `Attribution:` line to that instrument's LICENSE.md naming the creator, and add a matching line to a new bundle-wide `flow-lang/Samples/CREDITS.md` aggregating all attributions."
requirements: [REQ-2]
must_haves:
  truths:
    - "flow-lang/Samples/ directory exists with 6 instrument subdirectories: piano/, brass/, sax/, strings/, flute/, bell/"
    - "21 .wav files total: piano (10 = 5 pitches × pp/ff), brass (3 = A3/A4/A5), sax (2 = F4/C5), strings (3 = D3/D4/D5), flute (2 = G4/G5), bell (1 = C5)"
    - "Every WAV is 44.1 kHz / 16-bit PCM mono — verifiable via `file flow-lang/Samples/*/*.wav` output containing 'mono, 16 bit, 44100 Hz'"
    - "Every instrument subdirectory has a LICENSE.md containing literal strings 'License:' and 'Source:'"
    - "Every LICENSE.md declares 'License: CC0', 'License: Public Domain', OR 'License: CC-BY' (any 3.0/4.0 version). CC-BY-SA and CC-BY-NC are REJECTED."
    - "Every LICENSE.md that declares CC-BY additionally contains an 'Attribution:' line naming the original creator"
    - "If any sample is CC-BY, a bundle-wide flow-lang/Samples/CREDITS.md exists with one line per attribution (consolidated for end users)"
    - "Total `flow-lang/Samples/` size ≤ 5 MB measured by sum of FileInfo.Length over all .wav + .md files"
    - "LicenseAuditTests.cs Theory passes for all 6 instruments — verifies LICENSE.md fields"
    - "RepoSizeTests.cs Fact passes — verifies size cap"
  artifacts:
    - path: "flow-lang/Samples/piano/LICENSE.md"
      provides: "REQ-2 piano license documentation — CC0 declaration + Freesound (or developer-recorded) source URL + file manifest"
      contains: "License:"
      min_lines: 15
    - path: "flow-lang/Samples/brass/LICENSE.md"
      provides: "REQ-2 brass license — CC0 declaration + source URL"
      contains: "License:"
      min_lines: 8
    - path: "flow-lang/Samples/sax/LICENSE.md"
      provides: "REQ-2 sax license — CC0 declaration + source URL"
      contains: "License:"
      min_lines: 8
    - path: "flow-lang/Samples/strings/LICENSE.md"
      provides: "REQ-2 strings license — CC0 declaration + source URL"
      contains: "License:"
      min_lines: 8
    - path: "flow-lang/Samples/flute/LICENSE.md"
      provides: "REQ-2 flute license — CC0 declaration + source URL"
      contains: "License:"
      min_lines: 8
    - path: "flow-lang/Samples/bell/LICENSE.md"
      provides: "REQ-2 bell license — CC0 declaration + source URL"
      contains: "License:"
      min_lines: 8
    - path: "flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs"
      provides: "REQ-2 license audit gate — xUnit Theory over 6 instruments"
      contains: "[InlineData(\"piano\")]"
      min_lines: 40
    - path: "flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs"
      provides: "REQ-2 size cap CI gate — asserts ≤ 5 MB"
      contains: "FIVE_MB"
      min_lines: 25
  key_links:
    - from: "flow-lang/Samples/{instr}/LICENSE.md"
      to: "flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs"
      via: "Theory + InlineData reads each LICENSE.md and asserts CC0 declaration"
      pattern: "License: CC0|License: Public Domain"
    - from: "flow-lang/Samples/"
      to: "flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs"
      via: "Directory.EnumerateFiles recursive sum of FileInfo.Length asserted ≤ 5 MB"
      pattern: "FIVE_MB|5L \\* 1024 \\* 1024"
---

<objective>
Wave 0 — establish the bundled sample library and its closure gates (license + size). This plan is non-autonomous because sample sourcing requires human curation (downloading CC0 samples, listening for quality, verifying license pages on Freesound / Internet Archive / etc.). The closure gates (LICENSE audit + size cap) ship in this plan as the verification floor — they protect every later plan from drift.

Purpose: REQ-2 (sample library + license docs ≤ 5 MB CC0) is satisfied entirely by this plan. Two-tier velocity for piano (pp + ff layers) is staged here at the data layer; the renderer that USES the layers ships in Plan 03.

Output: 21 .wav files + 6 LICENSE.md files in flow-lang/Samples/, plus 2 new test files enforcing the 5-gate closure invariants from day one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-instrument-realism/29-SPEC.md
@.planning/phases/29-instrument-realism/29-CONTEXT.md
@.planning/phases/29-instrument-realism/29-RESEARCH.md
@.planning/phases/29-instrument-realism/29-PATTERNS.md
@flow-lang.Tests/FlowScriptData.cs
@flow-lang.Tests/Integration/Phase18/ByteIdenticalShowcaseTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate piano CC0 samples + write piano LICENSE.md</name>
  <files>flow-lang/Samples/piano/LICENSE.md, flow-lang/Samples/piano/C2_pp.wav, flow-lang/Samples/piano/C2_ff.wav, flow-lang/Samples/piano/C3_pp.wav, flow-lang/Samples/piano/C3_ff.wav, flow-lang/Samples/piano/C4_pp.wav, flow-lang/Samples/piano/C4_ff.wav, flow-lang/Samples/piano/C5_pp.wav, flow-lang/Samples/piano/C5_ff.wav, flow-lang/Samples/piano/C6_pp.wav, flow-lang/Samples/piano/C6_ff.wav</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-RESEARCH.md (§Sample Library Sourcing Route 1 — Freesound CC0 filter URL; recommended search queries per instrument; Pitfall 1 — CC0 vs CC-BY distinction)
    .planning/phases/29-instrument-realism/29-SPEC.md (Constraints — pitch coverage strategy, sample format)
  </read_first>
  <action>
    Source 10 CC0 piano samples from Freesound.org (filter `license:"Creative Commons 0"`) covering C2/C3/C4/C5/C6 at two velocity layers each (pp = soft tone ~v0.2, ff = loud tone ~v0.95). Fall back to Route 4 (developer-recorded + explicit CC0 release) if any pitch is unavailable.

    For each sample:
    - Convert to 44.1 kHz / 16-bit PCM mono WAV (mix-to-mono if stereo): `sox input.wav -r 44100 -b 16 -c 1 flow-lang/Samples/piano/C{N}_{vel}.wav` or equivalent in Audacity / ffmpeg.
    - Trim to ≤ 2 sec sustained tone (longer not useful for varispeed-based playback; eats bundle budget).
    - Verify with `file flow-lang/Samples/piano/C{N}_{vel}.wav` — output must contain "RIFF (little-endian) data, WAVE audio, Microsoft PCM, 16 bit, mono 44100 Hz".

    Write `flow-lang/Samples/piano/LICENSE.md` with this template (replace bracketed sections):
    ```
    # Piano Samples

    License: CC0
    Source: https://freesound.org/people/{user}/sounds/{id}/
    (or for self-recorded: "Source: Self-recorded; explicitly released under CC0 by {composer name}")

    ## Files

    - C2_pp.wav — C2 (MIDI 36) at velocity ~0.2; source: <URL>
    - C2_ff.wav — C2 (MIDI 36) at velocity ~0.95; source: <URL>
    - C3_pp.wav, C3_ff.wav — C3 (MIDI 48); source: <URL>
    - C4_pp.wav, C4_ff.wav — C4 (MIDI 60); source: <URL>
    - C5_pp.wav, C5_ff.wav — C5 (MIDI 72); source: <URL>
    - C6_pp.wav, C6_ff.wav — C6 (MIDI 84); source: <URL>

    ## License Statement

    To the extent possible under law, the source author(s) have waived all copyright
    and related or neighboring rights to these audio files under CC0 1.0 Universal
    (https://creativecommons.org/publicdomain/zero/1.0/). The files may be used
    commercially or non-commercially without attribution.
    ```
  </action>
  <verify>
    `ls flow-lang/Samples/piano/` shows 11 files: 10 .wav + LICENSE.md.
    `file flow-lang/Samples/piano/*.wav | grep -v "16 bit, mono 44100"` returns empty (all 10 match).
    `grep -c "License: CC0\|License: Public Domain" flow-lang/Samples/piano/LICENSE.md` returns ≥ 1.
    `grep -c "License: CC-BY" flow-lang/Samples/piano/LICENSE.md` returns 0.
    `du -sh flow-lang/Samples/piano/` returns ≤ 3 MB (piano alone gets ~50% of bundle headroom).
  </verify>
  <acceptance_criteria>
    - 10 .wav files exist at `flow-lang/Samples/piano/{C2,C3,C4,C5,C6}_{pp,ff}.wav`
    - Each WAV passes `file` check for 44.1 kHz / 16-bit / mono
    - `flow-lang/Samples/piano/LICENSE.md` exists with `License:` line containing "CC0" or "Public Domain"
    - `flow-lang/Samples/piano/LICENSE.md` contains `Source:` line per sample (or one Source: URL covering the bundle)
    - `flow-lang/Samples/piano/LICENSE.md` does NOT contain "License: CC-BY"
    - Total piano dir size ≤ 3 MB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Curate brass CC0 samples + LICENSE.md</name>
  <files>flow-lang/Samples/brass/LICENSE.md, flow-lang/Samples/brass/A3.wav, flow-lang/Samples/brass/A4.wav, flow-lang/Samples/brass/A5.wav</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-RESEARCH.md (§Sample Library Sourcing — brass row)
    flow-lang/Samples/piano/LICENSE.md (just-written piano LICENSE.md as a template)
  </read_first>
  <action>
    Source 3 CC0 brass / horn / trumpet single-note sustained samples covering A3, A4, A5. Search queries: `trumpet sustained A3 license:"Creative Commons 0"`, `horn A4 sustained license:"Creative Commons 0"`, etc. Convert each to 44.1 kHz / 16-bit / mono / ≤ 1.5 sec.

    Write `flow-lang/Samples/brass/LICENSE.md` mirroring piano LICENSE.md structure (License: CC0, Source: URL, file manifest, license statement). Target size ≤ 600 KB total for brass.
  </action>
  <verify>
    `ls flow-lang/Samples/brass/` shows 4 files (3 .wav + LICENSE.md).
    `file flow-lang/Samples/brass/*.wav` — all 3 match `16 bit, mono 44100 Hz`.
    `grep "License:" flow-lang/Samples/brass/LICENSE.md` matches CC0 or Public Domain.
    `du -sh flow-lang/Samples/brass/` ≤ 600 KB.
  </verify>
  <acceptance_criteria>
    - 3 .wav files exist at `flow-lang/Samples/brass/{A3,A4,A5}.wav` matching format
    - LICENSE.md present, declares CC0 / Public Domain, has Source: line, no CC-BY
    - Total brass dir size ≤ 600 KB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Curate sax CC0 samples + LICENSE.md</name>
  <files>flow-lang/Samples/sax/LICENSE.md, flow-lang/Samples/sax/F4.wav, flow-lang/Samples/sax/C5.wav</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-RESEARCH.md (§Sample Library Sourcing — sax row)
    flow-lang/Samples/piano/LICENSE.md (template)
  </read_first>
  <action>
    Source 2 CC0 saxophone single-note sustained samples covering F4 + C5. Search: `saxophone F4 sustained license:"Creative Commons 0"`, `alto sax C5 sustained license:"Creative Commons 0"`. Convert to 44.1 kHz / 16-bit / mono / ≤ 1.5 sec each.

    Write `flow-lang/Samples/sax/LICENSE.md` with same structure as brass LICENSE.md. Target size ≤ 400 KB.
  </action>
  <verify>
    `ls flow-lang/Samples/sax/` shows 3 files (2 .wav + LICENSE.md).
    `file flow-lang/Samples/sax/*.wav` — both match `16 bit, mono 44100 Hz`.
    `du -sh flow-lang/Samples/sax/` ≤ 400 KB.
  </verify>
  <acceptance_criteria>
    - 2 .wav files at `flow-lang/Samples/sax/{F4,C5}.wav` matching format
    - LICENSE.md present, declares CC0 / Public Domain, no CC-BY
    - Total sax dir size ≤ 400 KB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Curate strings CC0 samples + LICENSE.md</name>
  <files>flow-lang/Samples/strings/LICENSE.md, flow-lang/Samples/strings/D3.wav, flow-lang/Samples/strings/D4.wav, flow-lang/Samples/strings/D5.wav</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-RESEARCH.md (§Sample Library Sourcing — strings row)
  </read_first>
  <action>
    Source 3 CC0 violin (or cello/strings) sustained samples at D3, D4, D5. Search: `violin sustained D4 license:"Creative Commons 0"`, etc. Convert to 44.1 kHz / 16-bit / mono / ≤ 2 sec each (strings benefit from slightly longer sustained samples).

    Write `flow-lang/Samples/strings/LICENSE.md` with standard structure. Target size ≤ 700 KB.
  </action>
  <verify>
    `ls flow-lang/Samples/strings/` shows 4 files.
    `file flow-lang/Samples/strings/*.wav` — all match.
    `du -sh flow-lang/Samples/strings/` ≤ 700 KB.
  </verify>
  <acceptance_criteria>
    - 3 .wav files at `flow-lang/Samples/strings/{D3,D4,D5}.wav` matching format
    - LICENSE.md present, declares CC0 / Public Domain, no CC-BY
    - Total strings dir size ≤ 700 KB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Curate flute CC0 samples + LICENSE.md</name>
  <files>flow-lang/Samples/flute/LICENSE.md, flow-lang/Samples/flute/G4.wav, flow-lang/Samples/flute/G5.wav</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-RESEARCH.md (§Sample Library Sourcing — flute row)
  </read_first>
  <action>
    Source 2 CC0 flute sustained samples at G4 and G5. Convert to 44.1 kHz / 16-bit / mono / ≤ 1.5 sec.

    Write `flow-lang/Samples/flute/LICENSE.md` standard structure. Target ≤ 400 KB.
  </action>
  <verify>
    `ls flow-lang/Samples/flute/` shows 3 files.
    `du -sh flow-lang/Samples/flute/` ≤ 400 KB.
  </verify>
  <acceptance_criteria>
    - 2 .wav files at `flow-lang/Samples/flute/{G4,G5}.wav` matching format
    - LICENSE.md present, declares CC0 / Public Domain, no CC-BY
    - Total flute dir size ≤ 400 KB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Curate bell CC0 sample + LICENSE.md</name>
  <files>flow-lang/Samples/bell/LICENSE.md, flow-lang/Samples/bell/C5.wav</files>
  <read_first>
    .planning/phases/29-instrument-realism/29-RESEARCH.md (§Sample Library Sourcing — bell row; Open Question #2)
  </read_first>
  <action>
    Source 1 CC0 bell / tubular-bell / hand-bell / struck-glass sample at C5. Trim to ≤ 1.5 sec (bell decay tail is captured naturally). Convert to 44.1 kHz / 16-bit / mono.

    Write `flow-lang/Samples/bell/LICENSE.md`. Target ≤ 200 KB.
  </action>
  <verify>
    `ls flow-lang/Samples/bell/` shows 2 files.
    `du -sh flow-lang/Samples/bell/` ≤ 200 KB.
  </verify>
  <acceptance_criteria>
    - 1 .wav file at `flow-lang/Samples/bell/C5.wav` matching format
    - LICENSE.md present, declares CC0 / Public Domain, no CC-BY
    - Total bell dir size ≤ 200 KB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Write LicenseAuditTests.cs</name>
  <files>flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs</files>
  <read_first>
    flow-lang.Tests/FlowScriptData.cs (FindTestsRoot helper signature)
    flow-lang.Tests/Integration/Phase18/ByteIdenticalShowcaseTests.cs (file-walking + path-resolution idiom)
    .planning/phases/29-instrument-realism/29-PATTERNS.md (LicenseAuditTests pattern)
  </read_first>
  <action>
    Create `flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs` with this content:
    ```csharp
    using System.IO;
    using FlowLang.Tests;
    using Xunit;

    namespace FlowLang.Tests.Integration.Phase29;

    /// <summary>
    /// Phase 29 Gate C — license audit. Every flow-lang/Samples/{instrument}/LICENSE.md
    /// must exist, declare CC0 or Public Domain, and contain a Source: line. CC-BY is
    /// explicitly rejected (SPEC D-07).
    /// </summary>
    public class LicenseAuditTests
    {
        [Theory]
        [InlineData("piano")]
        [InlineData("brass")]
        [InlineData("sax")]
        [InlineData("strings")]
        [InlineData("flute")]
        [InlineData("bell")]
        public void EachInstrumentLicenseFile_HasRequiredFields(string instrument)
        {
            string testsRoot = FlowScriptData.FindTestsRoot();
            string repoRoot = Path.GetFullPath(Path.Combine(testsRoot, ".."));
            string licensePath = Path.Combine(repoRoot, "flow-lang", "Samples", instrument, "LICENSE.md");

            Assert.True(File.Exists(licensePath), $"LICENSE.md missing for instrument '{instrument}' at {licensePath}");
            string contents = File.ReadAllText(licensePath);

            Assert.Contains("License:", contents);
            Assert.Contains("Source:", contents);

            bool isCc0OrPd = contents.Contains("License: CC0") || contents.Contains("License: Public Domain");
            Assert.True(isCc0OrPd, $"LICENSE.md for '{instrument}' must declare CC0 or Public Domain. Got header: {contents.Substring(0, System.Math.Min(200, contents.Length))}");

            Assert.DoesNotContain("License: CC-BY", contents);
        }
    }
    ```
  </action>
  <verify>
    `dotnet test flow-lang.Tests --filter "Name~LicenseAuditTests" --nologo` exits 0 with 6 passing tests.
  </verify>
  <acceptance_criteria>
    - File exists at `flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs`
    - Contains `[InlineData("piano")]` (and similar for all 6 instruments)
    - Test class is in namespace `FlowLang.Tests.Integration.Phase29`
    - Running `dotnet test --filter "Name~LicenseAuditTests"` reports 6 passed / 0 failed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: Write RepoSizeTests.cs</name>
  <files>flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs</files>
  <read_first>
    flow-lang.Tests/FlowScriptData.cs (FindTestsRoot helper)
    .planning/phases/29-instrument-realism/29-PATTERNS.md (RepoSizeTests pattern)
  </read_first>
  <action>
    Create `flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs` with:
    ```csharp
    using System.IO;
    using System.Linq;
    using FlowLang.Tests;
    using Xunit;

    namespace FlowLang.Tests.Integration.Phase29;

    /// <summary>
    /// Phase 29 Gate B — repo size cap. flow-lang/Samples/ must be ≤ 5 MB total
    /// (SPEC constraint: "Repo size cap: 5 MB"). Cross-platform: uses .NET file
    /// enumeration, not `du -sh`, so it runs identically on Linux/macOS/Windows.
    /// </summary>
    public class RepoSizeTests
    {
        private const long FIVE_MB = 5L * 1024 * 1024;

        [Fact]
        public void SamplesDirectory_DoesNotExceed5MB()
        {
            string testsRoot = FlowScriptData.FindTestsRoot();
            string repoRoot = Path.GetFullPath(Path.Combine(testsRoot, ".."));
            string samplesRoot = Path.Combine(repoRoot, "flow-lang", "Samples");

            Assert.True(Directory.Exists(samplesRoot), $"flow-lang/Samples/ must exist at {samplesRoot}");

            long totalBytes = Directory.EnumerateFiles(samplesRoot, "*.*", SearchOption.AllDirectories)
                .Sum(f => new FileInfo(f).Length);

            Assert.True(totalBytes <= FIVE_MB,
                $"flow-lang/Samples/ is {totalBytes / 1024.0 / 1024.0:F2} MB; must be ≤ 5 MB (got {totalBytes} bytes vs cap {FIVE_MB} bytes)");
        }
    }
    ```
  </action>
  <verify>
    `dotnet test flow-lang.Tests --filter "Name~RepoSizeTests" --nologo` exits 0 with 1 passing test.
    `du -sh flow-lang/Samples/` (Linux only sanity) shows ≤ 5 MB.
  </verify>
  <acceptance_criteria>
    - File exists at `flow-lang.Tests/Integration/Phase29/RepoSizeTests.cs`
    - Contains `FIVE_MB` constant
    - Test class is in namespace `FlowLang.Tests.Integration.Phase29`
    - Running `dotnet test --filter "Name~RepoSizeTests"` reports 1 passed / 0 failed
    - Verifies sum of all file sizes in flow-lang/Samples/ ≤ 5 MB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 9: Update .gitignore to ensure samples ARE tracked</name>
  <files>.gitignore</files>
  <read_first>
    .gitignore (current state — verify no `*.wav` blanket ignore that would exclude flow-lang/Samples/)
  </read_first>
  <action>
    Read `.gitignore`. If any pattern excludes `*.wav` from being tracked (e.g. `*.wav`, `**/*.wav`, or excludes `flow-lang/Samples/`), add explicit `!flow-lang/Samples/` and `!flow-lang/Samples/**/*.wav` un-ignore rules. If no exclusion exists for `.wav` or `flow-lang/Samples/`, the file may already work — verify with `git check-ignore flow-lang/Samples/piano/C4_ff.wav` (returns empty / non-zero exit = not ignored = good).

    If `flow-lang/Samples/**/*.wav` is currently ignored, add at the bottom of `.gitignore`:
    ```
    # Phase 29: bundled CC0 sample library (must be tracked)
    !flow-lang/Samples/
    !flow-lang/Samples/**
    ```
  </action>
  <verify>
    `git check-ignore -v flow-lang/Samples/piano/C4_ff.wav` returns empty (no ignore rule applies).
    `git add flow-lang/Samples/` doesn't error or skip files.
    `git status --short flow-lang/Samples/ | wc -l` shows ≥ 27 entries (21 wav + 6 LICENSE.md, modulo whether they've been committed).
  </verify>
  <acceptance_criteria>
    - `git check-ignore flow-lang/Samples/piano/C4_ff.wav` exits non-zero (file not ignored)
    - All 21 wav + 6 LICENSE.md files are stageable via `git add`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External web → developer machine | CC0 samples downloaded from Freesound / Internet Archive. Untrusted binary input. |
| Sample file → flow-lang process | At render-time, samples are loaded via existing `loadWav` (Phase 22 hardened parser). Phase 29 doesn't expand this attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | flow-lang/Samples/{instr}/LICENSE.md | mitigate | License-audit unit test enforces CC0 declaration + Source: line. Closure-time manual review re-verifies each Source: URL leads to a verified CC0 declaration. |
| T-29-02 | Repudiation | Sample provenance | mitigate | Each LICENSE.md records Source URL; CC0 = no attribution required, so even if source URL goes 404, the CC0 waiver is irrevocable. |
| T-29-03 | DoS | Malformed WAV from CC0 source | accept | Existing Phase 22 RIFF parser is hardened; Phase 29 doesn't expand the attack surface. |
| T-29-04 | Information Disclosure | None — samples are public-domain | accept | No PII or proprietary data in WAVs. |
</threat_model>

<verification>
- All 9 sample-curation tasks complete; 27 files committed under flow-lang/Samples/
- `dotnet test flow-lang.Tests --filter "Name~LicenseAuditTests" --nologo` → 6 passed
- `dotnet test flow-lang.Tests --filter "Name~RepoSizeTests" --nologo` → 1 passed
- `du -sh flow-lang/Samples/` reports ≤ 5 MB
- Manual review: each LICENSE.md `Source:` URL opens to a verified CC0 / public-domain page (closure-time gate; documented in 29-VERIFICATION.md)
</verification>

<success_criteria>
- 21 .wav files + 6 LICENSE.md files committed under `flow-lang/Samples/{piano,brass,sax,strings,flute,bell}/`
- All WAVs are 44.1 kHz / 16-bit / mono PCM
- All LICENSE.md files declare CC0 or Public Domain (no CC-BY)
- LicenseAuditTests + RepoSizeTests both green
- `du -sh flow-lang/Samples/` ≤ 5 MB
</success_criteria>

<output>
After completion, create `.planning/phases/29-instrument-realism/29-01-SUMMARY.md` listing sample sources (URLs per instrument), final bundle size (du -sh output), and any deviations from the locked pitch coverage.
</output>
