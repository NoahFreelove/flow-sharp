---
phase: 38-live-coding-2-0
plan: 03
type: execute
wave: 3
depends_on: ["38-01", "38-02"]
files_modified:
  - flow-lang/StandardLibrary/Audio/Voice.cs
  - flow-lang/StandardLibrary/Audio/VoiceAllocator.cs
  - flow-lang/StandardLibrary/Audio/SongRenderer.cs
  - flow-lang/Interpreter/LambdaCaptureAuditor.cs
  - flow-interpreter/LiveReloadManager.cs
  - flow-lang.Tests/Integration/Phase38/VoicePoolNameDiffTests.cs
  - flow-lang.Tests/Integration/Phase38/StaleClosureDetectionTests.cs
  - flow-lang.Tests/Integration/Phase38/PrngReseedAtSwapTests.cs
  - flow-lang.Tests/Integration/Phase38/TimeoutRevertTests.cs
autonomous: true
requirements: [LIVE-03, LIVE-02]
must_haves:
  truths:
    - "Voice carries a stable `Name` property (e.g., \"piano:0\", \"drums:1\") set by SongRenderer at allocation; format matches the Phase 28 voice-allocator doc convention."
    - "On live-block swap, voices whose Name SURVIVES in the new render preserve their playback offset + envelope phase via `Voice.CopyStateFrom(prev)` — composer hears no click, no envelope retrigger. Voices whose Name is dropped from the new render fade out via the existing `VoiceAllocator.ApplyFadeOut` helper."
    - "On live-block swap, `engine.Context.PrngRegistry.ResetAtRenderBoundary()` fires once — Phase 36 Plan 36-01 API consumed verbatim per RESEARCH §D."
    - "Lambdas inside a live block body that reference a now-removed file-scope binding trigger `[live] stale closure: references removed binding '{name}' at line N — keeping previous version` via LambdaCaptureAuditor; the swap is skipped; the previous buffer keeps playing (no audible glitch)."
    - "30s evaluation timeout from Plan 38-01 dispatches `[live] evaluation timed out at 30s — keeping previous version` and reverts to previous buffer + reverts voice state (no partial mutation)."
    - "File-scope edits OUTSIDE any `live { }` block trigger a single dedup'd advisory `[live] file-scope edit detected outside live blocks at line N — restart 'flow watch' to apply` per D-38-04 (does NOT auto-restart — Pitfall #12 \"live session never dies mid-set\" lock)."
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Voice.cs"
      provides: "Extended Voice class with `public string Name { get; init; }` property + `public void CopyStateFrom(Voice prev)` method"
      contains: "public string Name"
    - path: "flow-lang/StandardLibrary/Audio/VoiceAllocator.cs"
      provides: "New static `DiffByVoiceName(prev, next) → (Preserved, Dropped, Added)` helper per RESEARCH §B"
      contains: "DiffByVoiceName"
    - path: "flow-lang/Interpreter/LambdaCaptureAuditor.cs"
      provides: "Static `CollectFileScopeReferences(IReadOnlyList<Statement> body) → HashSet<string>` AST walker"
      min_lines: 100
    - path: "flow-interpreter/LiveReloadManager.cs"
      provides: "New StagePendingBuffers + PreserveVoiceState + DetectFileScopeEdit methods invoked after RenderScript per RESEARCH §F lines 530-535"
      contains: "StagePendingBuffers"
  key_links:
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "flow-lang/Runtime/PrngRegistry.cs"
      via: "engine.Context.PrngRegistry.ResetAtRenderBoundary() called inside StagePendingBuffers per swap"
      pattern: "ResetAtRenderBoundary"
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "flow-lang/StandardLibrary/Audio/VoiceAllocator.cs"
      via: "PreserveVoiceState calls VoiceAllocator.DiffByVoiceName(prev, next) then Voice.CopyStateFrom on preserved entries"
      pattern: "DiffByVoiceName"
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "flow-lang/Interpreter/LambdaCaptureAuditor.cs"
      via: "StagePendingBuffers walks each new LiveBlockRegistration.Body via LambdaCaptureAuditor.CollectFileScopeReferences and aborts swap on stale closure"
      pattern: "LambdaCaptureAuditor\\.CollectFileScopeReferences"
---

<objective>
Wire LIVE-03 state preservation across the live-block swap path. Add the missing `Voice.Name` property (CRITICAL FINDING per PATTERNS.md line 1221 — currently absent on flow-lang/StandardLibrary/Audio/Voice.cs:1-40). Add `VoiceAllocator.DiffByVoiceName` per RESEARCH §B. Create `LambdaCaptureAuditor` per RESEARCH §C (~100 LOC AST walker). Wire `LiveReloadManager.StagePendingBuffers` to: (1) walk new live-block bodies for stale closures, (2) reset PRNG at swap boundary via `engine.Context.PrngRegistry.ResetAtRenderBoundary()` (Phase 36 Plan 36-01 API), (3) call `DiffByVoiceName` and `Voice.CopyStateFrom` on preserved voices, (4) fade-out dropped voices via existing `VoiceAllocator.ApplyFadeOut`. Also wire D-38-04 file-scope-edit detection (compares the edited line range against active LiveBlockStatement.Body line ranges) and complete the LIVE-02 30s timeout-revert behavior staged by Plan 38-01.

Purpose: Closes the LIVE-03 "voice-pool preserved IF voice name still exists post-edit" promise. Without DiffByVoiceName + CopyStateFrom, every live-block swap retriggers envelopes — composer hears click on every save. Without LambdaCaptureAuditor, stale closures silently misbehave (per CONTEXT D-38-07 the recovery path is explicit revert + advisory).

Output: Extended Voice class; extended VoiceAllocator (new helper, existing code unchanged); SongRenderer voice-allocation site sets Voice.Name; new LambdaCaptureAuditor.cs; rewired LiveReloadManager StagePendingBuffers + PreserveVoiceState + DetectFileScopeEdit + TimeoutRevert; 4 Wave 0 xUnit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@CLAUDE.md
@flow-lang/StandardLibrary/Audio/Voice.cs
@flow-lang/StandardLibrary/Audio/VoiceAllocator.cs
@flow-lang/StandardLibrary/Audio/SongRenderer.cs
@flow-lang/Runtime/PrngRegistry.cs
@flow-lang/Runtime/LiveBlockRegistry.cs
@flow-lang/Interpreter/ExpressionEvaluator.cs
@flow-interpreter/LiveReloadManager.cs

<interfaces>
<!-- Extracted from existing tree + Plan 38-02 output -->

From flow-lang/StandardLibrary/Audio/Voice.cs (CRITICAL: lines 1-40 have NO Name property):
- public class Voice {
    public AudioBuffer Buffer { get; }
    public double OffsetBeats { get; set; }
    public double Gain { get; set; }
    public double Pan { get; set; }
    public Voice(AudioBuffer buffer, double offsetBeats) { ... }
  }
  (ADD: public string Name { get; init; } = ""; — Phase 38 LIVE-03)
  (ADD: public void CopyStateFrom(Voice prev) — transfers OffsetBeats + envelope phase; RESEARCH §B step 3 line 690-693)

From flow-lang/StandardLibrary/Audio/VoiceAllocator.cs (~170 LOC):
- public static List<Voice> Allocate(...)
- public static List<Voice> AllocateWithPool(IReadOnlyList<Voice> voices, int poolSize, int sampleRate)  // lines 124-169 — preserved
- public static void ApplyFadeOut(Voice voice, int sampleRate)  // lines 87-104 — REUSE for dropped voices
- public static AsyncLocal<int?> _lastPoolSizeUsedForTests  // lines 23-28 — Phase 28 instrumentation precedent
- (ADD: public static (List<Voice>, List<Voice>, List<Voice>) DiffByVoiceName(prev, next) — RESEARCH §B lines 662-684)

From flow-lang/Runtime/PrngRegistry.cs (Plan 36-01):
- public void ResetAtRenderBoundary()  // line 122 — verbatim consume per RESEARCH §D

From Plan 38-02 output:
- flow-lang/Ast/Statements/LiveBlockStatement.cs (record with Body, BlockId)
- flow-lang/Runtime/LiveBlockRegistry.cs (Snapshot() → IReadOnlyDictionary<int, LiveBlockRegistration>)
- flow-lang/Runtime/ExecutionContext.cs (LiveBlockRegistry property)

From Plan 38-01 output:
- flow-interpreter/LiveReloadManager.cs (LiveStatusPanel _panel; 30s CancellationToken wrap; Dictionary<int, LiveBlockBuffer> _pendingPerBlock)
- flow-interpreter/LiveStatusPanel.cs (PublishAdvisory API)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Voice.Name + Voice.CopyStateFrom + VoiceAllocator.DiffByVoiceName + SongRenderer wiring + 1 Wave 0 test</name>
  <files>flow-lang/StandardLibrary/Audio/Voice.cs, flow-lang/StandardLibrary/Audio/VoiceAllocator.cs, flow-lang/StandardLibrary/Audio/SongRenderer.cs, flow-lang.Tests/Integration/Phase38/VoicePoolNameDiffTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§B lines 652-697 voice-pool name-key preservation + DiffByVoiceName sketch + step 3 CopyStateFrom)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (Voice.cs analog lines 544-575 + VoiceAllocator analog lines 506-541 + CRITICAL FINDING line 1221 "Voice has NO Name property")
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38 LIVE-03 voice-name preservation)
    flow-lang/StandardLibrary/Audio/Voice.cs (lines 1-40 entire class)
    flow-lang/StandardLibrary/Audio/VoiceAllocator.cs (lines 23-28 AsyncLocal precedent; 87-104 ApplyFadeOut; 124-169 AllocateWithPool)
    flow-lang/StandardLibrary/Audio/SongRenderer.cs (search for voice-allocation sites: `new Voice(...)` constructor calls — these must be extended with the `Name` init property)
  </read_first>
  <behavior>
    - Voice.Name exists as `public string Name { get; init; } = "";` — settable at construction via `new Voice(...) { Name = "piano:0" }`.
    - Voice.CopyStateFrom(prev) transfers `OffsetBeats` + envelope-phase state from `prev` (per RESEARCH §B step 3 line 690-693). The envelope-phase transfer reads/writes whatever ADSR-related fields exist on the Voice today (`Buffer` carries the rendered samples; the live-mode preserve mode wants the cursor position into that buffer, not the buffer itself). Implementation: copy `OffsetBeats` field directly; if the Voice exposes any explicit envelope-cursor or playback-cursor field, copy that too; otherwise the OffsetBeats transfer alone is sufficient for v1.5 (HUMAN-UAT defers envelope-detail tuning if needed).
    - VoiceAllocator.DiffByVoiceName(prev, next) returns the three-tuple per RESEARCH §B lines 662-684: voices in prev∩next go to `Preserved`, prev\next to `Dropped`, next\prev to `Added`. Preserved entries receive `newVoice.CopyStateFrom(prevByName[name])` at the swap site (called by LiveReloadManager.PreserveVoiceState).
    - SongRenderer sets Voice.Name at allocation time per Phase 28 doc convention `"{instrumentLabel}:{ordinalIdx}"` — e.g. allocating the 3rd piano voice produces `Name = "piano:2"`.
    - VoicePoolNameDiffTests.DiffByVoiceName_DistinguishesPreservedDroppedAdded: build a `prev` list `[piano:0, piano:1, brass:0]` and a `next` list `[piano:0, brass:0, drums:0]`; assert Preserved == [piano:0, brass:0] (by reference equality to the NEW voices), Dropped == [piano:1] (from prev), Added == [drums:0] (from next). Use the `next` voice instances for Preserved (per RESEARCH §B line 675 "preserved.Add(newVoice)").
    - VoicePoolNameDiffTests.CopyStateFrom_TransfersOffsetBeats: construct `prev` Voice with OffsetBeats=1.5; construct `next` Voice (same Name) with OffsetBeats=0.0; call `next.CopyStateFrom(prev)`; assert `next.OffsetBeats == 1.5`.
  </behavior>
  <action>
    Edit `flow-lang/StandardLibrary/Audio/Voice.cs`: add `public string Name { get; init; } = "";` property between Pan and the ctor. Add `public void CopyStateFrom(Voice prev) { OffsetBeats = prev.OffsetBeats; }` — keep minimal per RESEARCH §B line 693 (envelope-phase transfer is a no-op in v1.5 because the Voice class does not yet expose explicit envelope cursors; if any private-state mirror surfaces during execution, extend in a follow-up plan). xmldoc on Name cites Phase 38 LIVE-03 + the `"{instrument}:{ordinal}"` convention per Phase 28 CLAUDE.md doc.

    Edit `flow-lang/StandardLibrary/Audio/VoiceAllocator.cs`: add static method `DiffByVoiceName(IReadOnlyList<Voice> prev, IReadOnlyList<Voice> next)` with body per RESEARCH §B lines 662-684 (full sketch — dictionaries keyed on `v.Name` with `StringComparer.Ordinal`; iterate prev, check membership in next; iterate next, check membership in prev; return tuple). Place adjacent to `Allocate` and `AllocateWithPool` methods.

    Edit `flow-lang/StandardLibrary/Audio/SongRenderer.cs`: at every `new Voice(buffer, offsetBeats)` site, extend the construction with `{ Name = $"{instrumentLabel}:{ordinalIdx}" }` initializer where `instrumentLabel` is the sequence's instrument-name (already known at the allocation site — used for the per-instrument breakdown in Phase 28) and `ordinalIdx` is the integer index within that instrument's voice list at that allocation pass. The label MUST match exactly the format used by the Phase 28 per-instrument breakdown (panel row 3 will display the same labels per UI-SPEC line 152). If the SongRenderer's allocation loop already tracks both label + index, this is a one-liner per site. Add an inline `// Phase 38 LIVE-03: stable Name for live-swap diff` comment.

    Create `flow-lang.Tests/Integration/Phase38/VoicePoolNameDiffTests.cs` with the two assertions above + the standard `[Collection("FlowScripts")]` + IDisposable + ResetForTesting ctor/Dispose.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.VoicePoolNameDiffTests" --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <done>VoicePoolNameDiffTests GREEN. `grep -c "public string Name" flow-lang/StandardLibrary/Audio/Voice.cs` returns 1. `grep -c "DiffByVoiceName" flow-lang/StandardLibrary/Audio/VoiceAllocator.cs` returns >=2 (declaration + xmldoc). `grep -c "Name = " flow-lang/StandardLibrary/Audio/SongRenderer.cs` returns >=1 (allocation site wiring). Existing Phase 28 voice-allocator + SongRenderer tests still GREEN — `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase28" --no-restore` exits 0 (Name init is additive, default value "").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LambdaCaptureAuditor + StaleClosureDetectionTests</name>
  <files>flow-lang/Interpreter/LambdaCaptureAuditor.cs, flow-lang.Tests/Integration/Phase38/StaleClosureDetectionTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§C lines 698-765 stale-closure detection + LambdaCaptureAuditor sketch lines 706-744 + the swap-time consumer pattern lines 747-759)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (LambdaCaptureAuditor analog lines 229-251)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-07 stale closure → revert + dedup'd advisory)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory catalog line 331 stale-closure dedup key)
    flow-lang/Interpreter/ExpressionEvaluator.cs (switch-on-AST-record-type dispatch idiom — the LambdaCaptureAuditor walker mirrors this style per CLAUDE.md C# Conventions)
    flow-lang/Ast/Expressions/LambdaExpression.cs (the node carrying captured lambda body)
    flow-lang/Ast/Statements/ (all statement records for the WalkStatement switch)
    flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce — caller will use sentinel `live-stale-closure:<name>:<line>`)
  </read_first>
  <behavior>
    - `LambdaCaptureAuditor.CollectFileScopeReferences(IReadOnlyList<Statement> body) → HashSet<string>` walks every Statement / Expression node in `body` (post-order); collects all `VariableExpression.Name` references that are NOT shadowed by a local declaration inside the body (i.e., names declared at file scope but referenced from within the live-block body OR within any lambda nested inside it).
    - Local-binding tracking: a `VariableDeclaration` / `ProcDeclaration` inside the body's scope (or any nested scope down to the deepest lambda) SHADOWS the same name from the result set. The walker maintains an enclosing-scope stack of declared names; only names that escape ALL local scopes count as "file-scope references."
    - `LambdaExpression` recursion: walk into `LambdaExpression.Body`; each lambda parameter introduces a local binding for its scope.
    - StaleClosureDetectionTests.LambdaReferencingFileScopeBinding_Detected: body `(fn Int x => (add x globalCount))`; assert `CollectFileScopeReferences(body)` contains `"globalCount"` and not `"x"` (parameter shadowed).
    - StaleClosureDetectionTests.LocalShadowingFileScope_NotReported: body `{ Int globalCount = 5; (add x globalCount) }`; assert `globalCount` NOT in result (locally declared).
    - StaleClosureDetectionTests.NestedLambdaCapture_Detected: body `(map list (fn Int x => (add x globalCount)))`; assert `globalCount` IS in the result (escapes the lambda's local scope).
    - StaleClosureDetectionTests.NoFileScopeReferences_ReturnsEmpty: body `{ Int x = 1; (add x 2) }`; assert `CollectFileScopeReferences(body)` is empty.
  </behavior>
  <action>
    Create `flow-lang/Interpreter/LambdaCaptureAuditor.cs` (~120 LOC) as a static class in `FlowLang.Interpreter` namespace. Public API: `public static HashSet<string> CollectFileScopeReferences(IReadOnlyList<Statement> body)`. Internal helpers: `WalkStatement(Statement s, HashSet<string> result, Stack<HashSet<string>> scopes)` and `WalkExpression(Expression e, HashSet<string> result, Stack<HashSet<string>> scopes)`. Each walker uses a `switch` expression on the AST record type per CLAUDE.md C# Conventions ("Pattern matching switch expressions for node dispatch rather than visitor pattern"). The `scopes` stack tracks declared names per nesting level; a new HashSet is pushed at each `LambdaExpression` (with parameter names pre-populated) and each `IReadOnlyList<Statement>` block (for VariableDeclaration / ProcDeclaration tracking). On `VariableExpression v`, check if `v.Name` exists in ANY scope in the stack; if not, add to `result` (this is a "file-scope reference" — bound from outside the body's scopes).

    Handle every Expression record type (`LiteralExpression` / `VariableExpression` / `FunctionCallExpression` / `FlowExpression` / `ArrayLiteralExpression` / `ArrayIndexExpression` / `LazyExpression` / `LambdaExpression` / `MemberAccessExpression` / `ChordLiteralExpression` / `NoteStreamExpression` / `SongExpression` / `SymbolLiteralExpression` / `TupleLiteralExpression` / `TupleUnpackFlowExpression`) per CLAUDE.md AST node list. Statements: VariableDeclaration / AssignmentStatement / ProcDeclaration / ReturnStatement / ExpressionStatement / ImportStatement / MusicalContextStatement / SectionDeclaration / TupleDestructureStatement (CLAUDE.md statement list) plus the NEW LiveBlockStatement (recursively walks its Body).

    Default in the switch: silently skip unknown nodes (charitable per D-v1.5-05; new AST nodes added in future phases won't crash the auditor — they just don't contribute file-scope references; the stale-closure detection is a best-effort guard, not a soundness gate).

    Create `flow-lang.Tests/Integration/Phase38/StaleClosureDetectionTests.cs` exercising the 4 cases above. Use `FlowEngine.ParseToProgram(...)` (or the equivalent existing entrypoint) to obtain `IReadOnlyList<Statement>` from a Flow source string, then pass to `CollectFileScopeReferences`.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.StaleClosureDetectionTests" --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <done>StaleClosureDetectionTests GREEN (4 facts). LambdaCaptureAuditor.cs exists with public static CollectFileScopeReferences. Walker handles every AST record type per the CLAUDE.md AST node lists. Charitable default skip for unknown nodes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire LiveReloadManager.StagePendingBuffers + PreserveVoiceState + DetectFileScopeEdit + TimeoutRevert + 2 Wave 0 tests</name>
  <files>flow-interpreter/LiveReloadManager.cs, flow-lang.Tests/Integration/Phase38/PrngReseedAtSwapTests.cs, flow-lang.Tests/Integration/Phase38/TimeoutRevertTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§D lines 766-775 PRNG reseed; §F lines 530-535 StagePendingBuffers + PreserveVoiceState callback chain; §C lines 747-759 stale-closure consumer)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (LiveReloadManager rewrite checklist lines 254-325; Shared Pattern S6 PRNG routing)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-04 file-scope edit advisory + Pitfall #12 lock; D-38-07 timeout + stale-closure → revert)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory catalog lines 322-341 — all live-* dedup keys + body wording)
    flow-interpreter/LiveReloadManager.cs (Plan 38-01 state: LiveStatusPanel _panel; Dictionary<int, LiveBlockBuffer> _pendingPerBlock; 30s CancellationToken wrap)
    flow-lang/Runtime/LiveBlockRegistry.cs (Plan 38-02 — Snapshot() API)
    flow-lang/Runtime/PrngRegistry.cs (line 122 ResetAtRenderBoundary)
  </read_first>
  <behavior>
    - StagePendingBuffers(perBlockBuffers, musicalContext, engine) per RESEARCH §F lines 530-535: walks each new LiveBlockRegistration body via `LambdaCaptureAuditor.CollectFileScopeReferences(reg.Body)`; for each name not present in `engine.Context.GlobalFrame.HasVariable(name) || HasFunction(name)`, publishes `_panel.PublishAdvisory($"[live] stale closure: references removed binding '{name}' at line {reg.Location.Line} — keeping previous version", AdvisoryLevel.Error, dedupKey: $"live-stale-closure:{name}:{reg.Location.Line}")` and SKIPS staging that block's buffer (previous block buffer continues playing — Pitfall #12 lock).
    - After ALL blocks pass the stale-closure gate, calls `engine.Context.PrngRegistry.ResetAtRenderBoundary()` ONCE (per RESEARCH §D line 770). Calls `PreserveVoiceState(perBlockBuffers, prevVoices, newVoices)` once. Stages all surviving buffers into `_pendingPerBlock`.
    - PreserveVoiceState(perBlockBuffers, prevVoices, newVoices): calls `VoiceAllocator.DiffByVoiceName(prevVoices, newVoices)`; for each Preserved voice, calls `preservedVoice.CopyStateFrom(prevByName[name])`; for each Dropped voice, calls `VoiceAllocator.ApplyFadeOut(droppedVoice, sampleRate)` per RESEARCH §B line 685.
    - DetectFileScopeEdit(filePath, editedLineRange, liveBlockBodyRanges) per D-38-04: if the edited line range falls OUTSIDE all live-block bodies, publish `_panel.PublishAdvisory($"[live] file-scope edit detected outside live blocks at line {editedLine} — restart 'flow watch' to apply", AdvisoryLevel.Warning, dedupKey: $"live-fscope-edit:{filePath}:{editedLine}")` per UI-SPEC line 334. Does NOT auto-restart per Pitfall #12.
    - 30s timeout-revert: when the Task.Run + Wait(30s) from Plan 38-01 returns false (timeout), publish `_panel.PublishAdvisory($"[live] evaluation timed out at 30s at line {N} — keeping previous version", AdvisoryLevel.Error, dedupKey: $"live-timeout:{currentRenderFilePath}")` per UI-SPEC line 330. Voice state stays at the previous render (no partial mutation — the swap callback chain is gated on the worker SUCCEEDING).
    - PrngReseedAtSwapTests.SwapBoundary_CallsResetAtRenderBoundary: drive a synthetic swap callback through StagePendingBuffers; assert `engine.Context.PrngRegistry.ResetAtRenderBoundary()` was invoked exactly once. Use a small instrumentation hook (counter property on PrngRegistry, or test-only spy via a wrapper) — minimal additional surface.
    - TimeoutRevertTests.WorkerExceeds30s_PublishesAdvisory_KeepsPreviousBuffer: inject a synthetic FlowEngine-stub that sleeps 35s on Execute (or use a test-time-wait-helper that shrinks the timeout to 100ms for the test); assert the timeout-revert advisory was published with the locked wording; assert `_pendingPerBlock` is NOT updated (previous buffer is unchanged).
  </behavior>
  <action>
    Extend `flow-interpreter/LiveReloadManager.cs` with three new private methods per RESEARCH §F lines 530-535:
    - `private void StagePendingBuffers(Dictionary<int, LiveBlockBuffer> newBuffers, FlowEngine engine, IReadOnlyDictionary<int, LiveBlockRegistration> newBlocks)` — walks newBlocks, runs LambdaCaptureAuditor + missing-binding check, publishes stale-closure advisory + skips block if stale, calls engine.Context.PrngRegistry.ResetAtRenderBoundary(), populates _pendingPerBlock with surviving buffers.
    - `private void PreserveVoiceState(IReadOnlyList<Voice> prevVoices, IReadOnlyList<Voice> newVoices, int sampleRate)` — DiffByVoiceName + CopyStateFrom + ApplyFadeOut per Task 1 + Task 2 outputs.
    - `private void DetectFileScopeEdit(string filePath, int editedLine, IReadOnlyDictionary<int, LiveBlockRegistration> activeBlocks)` — per RESEARCH §C / D-38-04. The editedLine comes from the file-watcher event diff (use whatever existing mechanism the manager already has for tracking changes; if none, this is a best-effort gate that checks whether the new program text differs from the previous text outside any live-block source-span).

    Hook StagePendingBuffers into the existing background-render success path (after the 30s Task.Run + Wait succeeds and RenderScript returned a fresh `perBlockBuffers` dict per Plan 38-01). Hook the 30s-timeout-failure branch to publish the Error-level advisory and NOT call StagePendingBuffers (previous buffer keeps playing).

    Hook DetectFileScopeEdit into the FileSystemWatcher Changed event handler — invoked before the debounce gate fires the background render. Use the active LiveBlockRegistry.Snapshot() to know the live-block body line ranges.

    Create `flow-lang.Tests/Integration/Phase38/PrngReseedAtSwapTests.cs` + `flow-lang.Tests/Integration/Phase38/TimeoutRevertTests.cs` per the behaviors above. Each carries `[Collection("FlowScripts")]` + IDisposable. Use either a test-only PrngRegistry spy (extend the existing class with an `internal int ResetCallCount { get; private set; }` instrumentation analogous to AsyncLocal _lastPoolSizeUsedForTests precedent at VoiceAllocator.cs:23-28) or wrap PrngRegistry in a Func<>-driven counter at the test seam. The TimeoutRevert test uses a synthetic engine-stub or shrinks the timeout via a `protected internal` constant override in a test-subclass.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All Phase38 tests GREEN — 11+ facts across the 6 test classes from Plans 38-01 + 38-02 + 38-03. `grep -c "StagePendingBuffers" flow-interpreter/LiveReloadManager.cs` returns >=2. `grep -c "PreserveVoiceState" flow-interpreter/LiveReloadManager.cs` returns >=2. `grep -c "ResetAtRenderBoundary" flow-interpreter/LiveReloadManager.cs` returns >=1. `grep -c "DetectFileScopeEdit" flow-interpreter/LiveReloadManager.cs` returns >=2. `grep -c "live-stale-closure" flow-interpreter/LiveReloadManager.cs` returns >=1. `grep -c "live-fscope-edit" flow-interpreter/LiveReloadManager.cs` returns >=1. `grep -c "live-timeout" flow-interpreter/LiveReloadManager.cs` returns >=1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LambdaCaptureAuditor walker -> Composer source AST | Read-only walk; no AST mutation; trusted composer code |
| Live-block swap callback -> Voice mutation | Plan 38-03 mutates Voice.OffsetBeats via CopyStateFrom; bounded to the live-block swap pathway; offline render path (Phase 28) unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-12 | Denial of Service | Live-block swap with stale closure spams stderr | mitigate | WarnOnce dedup keyed `live-stale-closure:<name>:<line>` per UI-SPEC + PATTERNS Shared Pattern S2 |
| T-38-CLO | Tampering | LambdaCaptureAuditor false negative — misses a captured reference and lets a swap succeed with stale closure | accept | Best-effort guard per RESEARCH §C; v1.5 charitable interpretation default (D-v1.5-05); if HUMAN-UAT reports composer confusion, tighten in v1.6 |
| T-38-VOI | Tampering | DiffByVoiceName mis-attributes a preserved voice (different musical role, same Name) | mitigate | RESEARCH §B Pitfall #7 line 1467 — Name format `"{instrument}:{ordinal}"` keeps ordinal stable across renders when instrument set is unchanged; CopyStateFrom only transfers OffsetBeats (no envelope-cursor leak today) |
| T-38-PRN | Tampering | PrngRegistry.ResetAtRenderBoundary called at wrong moment | mitigate | StagePendingBuffers is the SINGLE call site per RESEARCH §D line 770; PrngReseedAtSwapTests assert exactly-once invocation |
</threat_model>

<verification>
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38" --no-restore` exits 0 (11+ facts across 7 test classes).
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase28" --no-restore` exits 0 (Phase 28 voice tests unaffected — Name init is additive default "").
- `grep -nE "(Name |CopyStateFrom|DiffByVoiceName)" flow-lang/StandardLibrary/Audio/Voice.cs flow-lang/StandardLibrary/Audio/VoiceAllocator.cs` confirms the new surface area.
- `grep -n "CollectFileScopeReferences" flow-lang/Interpreter/LambdaCaptureAuditor.cs` returns >=1.
- Smoke: `dotnet run --project flow-interpreter -e 'Int foo = 5; live 1bar { (print foo) }'` runs without stale-closure advisory (foo exists in file scope).
- Smoke: hand-edit the script to remove `Int foo = 5;` while `flow watch` is running — verify `[live] stale closure: references removed binding 'foo' at line N — keeping previous version` advisory appears once.
</verification>

<success_criteria>
- Voice.Name + Voice.CopyStateFrom shipped
- VoiceAllocator.DiffByVoiceName shipped
- SongRenderer wired to set Name on allocation
- LambdaCaptureAuditor shipped + walks all AST node types charitably
- LiveReloadManager.StagePendingBuffers wired (stale-closure gate + PRNG reseed + voice preservation)
- 30s timeout-revert path complete (Plan 38-01 staged the wrap; this plan completes the revert behavior)
- D-38-04 file-scope-edit advisory shipped
- 4 Wave 0 xUnit tests GREEN
- Pitfall #12 "live session never dies mid-set" lock honored — every failure path biases to revert + advisory, never throw/halt
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-03-SUMMARY.md` recording: Voice surface diff, LambdaCaptureAuditor LOC, advisory wording verification (UI-SPEC alignment), test pass count, smoke-test results for stale-closure + file-scope-edit advisories.
</output>
