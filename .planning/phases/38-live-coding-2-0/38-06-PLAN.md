---
phase: 38-live-coding-2-0
plan: 06
type: execute
wave: 3
depends_on: ["38-01", "38-05"]
files_modified:
  - flow-lang/StandardLibrary/Network/OscFunctions.cs
  - flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs
  - flow-lang/Runtime/Value.cs
  - flow-lang/StandardLibrary/BuiltInFunctions.cs
  - flow-lang/osc.flow
  - flow-lang/flow-lang.csproj
  - flow-lang.Tests/Integration/Phase38/OscTypeTagInferenceTests.cs
  - flow-lang.Tests/Integration/Phase38/OscRateLimitTests.cs
  - flow-lang.Tests/Integration/Phase38/OscLoopbackTests.cs
  - flow-lang.Tests/Integration/Phase38/OscBundleTests.cs
  - flow-lang.Tests/Integration/Phase38/OscBundleDepthCapTests.cs
autonomous: true
requirements: [OSC-01, OSC-02]
user_setup:
  - service: nuget
    why: "Rug.Osc 1.2.5 — OSC 1.0 client+server with bundle support; MIT; zero deps; .NET Standard 2.0 (compatible with .NET 10); verified live on NuGet 2026-05-23 per RESEARCH §J + Package Legitimacy Audit"
    install_cmd: "dotnet add flow-lang package Rug.Osc --version 1.2.5"
must_haves:
  truths:
    - "Composer calls `(oscSend host port path arg1 arg2 ...)` and Flow infers OSC 1.0 type tags charitably per D-38-13: Int->,i Long->,h Float->,f Double->,d String/Symbol->,s Bool->,T/,F Buffer->,b (OVERRIDES REQUIREMENTS.md OSC-02 strict-by-arg wording per D-v1.5-01 + D-v1.5-05 charitable interpretation default)."
    - "Composer calls `(oscListen port path handler)` and receives an OscHandle reference-identity value (per Phase 32/33/36 precedent); the handler is invoked once per matching incoming message; rate-limited to 200 Hz per path via drop-newest sample-and-hold (D-38-14)."
    - "Composer calls `(oscBundle msg1 msg2 ...)` and `(oscSendBundle host port bundle [timetag])` — bundles sent both ways; timetag honored on receive; bundle nesting depth capped at 8 with charitable advisory at overflow (D-38-15 mirroring T-36-17 / D-39-19)."
    - "Composer calls `(oscStop handle)` to cancel the listener; underlying Task.Run UDP receive loop terminates cleanly via the OscHandle's CancellationTokenSource."
    - "Opt-in module activation via `use \"@osc\"` mirrors Phase 33 `@sfz` + Phase 39 `@notation-io` shape per PATTERNS Pattern S3."
  artifacts:
    - path: "flow-lang/StandardLibrary/Network/OscFunctions.cs"
      provides: "Register(InternalFunctionRegistry) for oscSend / oscListen / oscStop / oscBundle / oscSendBundle; type-tag inference; rate-limit gate; bundle dispatch with depth-cap 8"
      min_lines: 350
      contains: "Rug.Osc"
    - path: "flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs"
      provides: "Sealed singleton FlowType (specificity 151) per Phase 32/33/36 precedent"
      min_lines: 16
    - path: "flow-lang/Runtime/Value.cs"
      provides: "Extended with `public static Value OscHandle(OscHandleData h) => new(h, OscHandleType.Instance);` factory"
      contains: "OscHandle"
    - path: "flow-lang/osc.flow"
      provides: "use \"@osc\" activation module with internal proc forward decls + __enableOscModule side-effect"
      min_lines: 20
  key_links:
    - from: "flow-lang/StandardLibrary/Network/OscFunctions.cs"
      to: "Rug.Osc OscReceiver/OscSender/OscMessage/OscBundle"
      via: "Task.Run UDP loop + per-path _lastFireTimeMs gate + bundle DispatchPacket recursion with depth limit"
      pattern: "OscReceiver"
    - from: "flow-lang/StandardLibrary/BuiltInFunctions.cs"
      to: "Network.OscFunctions.Register"
      via: "RegisterAllImplementations adds Network.OscFunctions.Register(registry); gated by ExecutionContext.OscEnabled flag set by __enableOscModule"
      pattern: "OscFunctions\\.Register"
---

<objective>
Ship OSC-01 + OSC-02 — composer can send and receive OSC messages and bundles over UDP using the Rug.Osc 1.2.5 surface. Five new builtins (`oscSend`, `oscListen`, `oscStop`, `oscBundle`, `oscSendBundle`) registered conditionally on `use "@osc"` per PATTERNS Pattern S3. Charitable smallest-tag-that-fits type-tag inference per D-38-13 (OVERRIDES REQUIREMENTS.md OSC-02 strict-tag wording — closer plan 38-07 sweeps the REQUIREMENTS file). Drop-newest sample-and-hold rate limit at 200 Hz per path per D-38-14. Full bundle support both directions with depth cap 8 per D-38-15. OscHandle reference-identity value type at specificity 151 per Phase 32 Tuning / Phase 33 Sfz / Phase 36 MarkovModel/LsystemModel precedent.

Purpose: OSC is the composer's network surface to TouchOSC, hardware controllers (Lemur, modular synth bridges, Bitwig + REAPER OSC IO), and other Flow processes on the LAN. With Phase 40's MIDI clock not yet shipped, OSC is the FASTEST way to drive Flow live-mode parameters from an external controller in v1.5.

Output: New `flow-lang/StandardLibrary/Network/OscFunctions.cs` (~350 LOC); new `OscHandleType.cs`; extended `Value.cs` with OscHandle factory; new `flow-lang/osc.flow` module; `flow-lang.csproj` adds Rug.Osc 1.2.5 PackageReference + osc.flow `<None Update>`; BuiltInFunctions wires the conditional registration; 5 Wave 0 xUnit tests (`OscTypeTagInferenceTests`, `OscRateLimitTests`, `OscLoopbackTests`, `OscBundleTests`, `OscBundleDepthCapTests`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@CLAUDE.md
@flow-lang/flow-lang.csproj
@flow-lang/sfz.flow
@flow-lang/notation-io.flow
@flow-lang/TypeSystem/SpecialTypes/SfzType.cs
@flow-lang/Runtime/Value.cs
@flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs
@flow-lang/StandardLibrary/BuiltInFunctions.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs

<interfaces>
<!-- Extracted from existing tree + Rug.Osc 1.2.5 API surface -->

From flow-lang/TypeSystem/SpecialTypes/SfzType.cs (analog 36 LOC):
- public sealed class SfzType : FlowType
- private SfzType() { } public static SfzType Instance { get; } = new();
- public override string Name => "Sfz";
- public override int GetSpecificity() => 150;
- public override bool IsCompatibleWith(FlowType target) => target is SfzType;
- public override bool CanConvertTo(FlowType target) => target is SfzType;
- (OscHandleType mirrors verbatim; Name => "OscHandle"; specificity => 151 per PATTERNS line 697)

From flow-lang/Runtime/Value.cs (lines 60-101 reference-identity factories):
- public static Value Tuning(...) / Sfz(...) / MarkovModel(...) / LsystemModel(...)
- (add: public static Value OscHandle(StandardLibrary.Network.OscHandleData h) => new(h, OscHandleType.Instance);)

From flow-lang/sfz.flow / flow-lang/notation-io.flow (PATTERN S3 module-activation):
- Header Note: block + use "@std" + internal proc __enableXxxModule + N internal proc forward decls + trailing (__enableXxxModule) side-effect

From flow-lang/StandardLibrary/BuiltInFunctions.cs:
- RegisterAllImplementations at lines 35-54 — add Network.OscFunctions.Register(registry) per PATTERNS line 832

From Rug.Osc 1.2.5 (NEW NuGet dep — verified on NuGet flatcontainer API 2026-05-23):
- public class OscReceiver(int port) { Connect(); OscPacket Receive(); Dispose(); }  // blocking Receive on Task.Run loop
- public class OscSender(string host, int port) { Connect(); Send(OscMessage); Send(OscBundle); Dispose(); }
- public class OscMessage(string address, params object[] args)  // args are int/long/float/double/string/bool/byte[]
- public class OscBundle(OscTimeTag timetag, params OscPacket[] packets)
- public class OscTimeTag(DateTime utc)  // OscTimeTag.Immediate = special timetag value 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package legitimacy gate (blocking-human checkpoint) + Rug.Osc PackageReference + OscHandleType + Value.OscHandle factory + osc.flow module + 1 Wave 0 test for type-tag inference</name>
  <files>flow-lang/flow-lang.csproj, flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs, flow-lang/Runtime/Value.cs, flow-lang/osc.flow, flow-lang.Tests/Integration/Phase38/OscTypeTagInferenceTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (lines 168-181 Package Legitimacy Audit; §K lines 1078-1160 Rug.Osc API surface; §L lines 1161-1209 type-tag inference)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (OscHandleType analog lines 678-697; Value.OscHandle factory lines 703-723; osc.flow analog lines 840-870; csproj edit lines 912-930; Pattern S3 module activation lines 1137-1147)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-13 OSC type-tag inference + REQUIREMENTS.md OSC-02 override flag)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory catalog lines 337-340 osc-bind / osc-infer / osc-bundle-depth / osc-flood dedup keys + body wording; Registry Safety line 381 Rug.Osc license gate)
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (line 64 OscTypeTagInferenceTests)
    flow-lang/flow-lang.csproj (lines 10-13 PackageReference cluster + lines 43-46 .flow file <None Update> cluster)
    flow-lang/TypeSystem/SpecialTypes/SfzType.cs (entire 36-LOC analog)
    flow-lang/Runtime/Value.cs (lines 60-101 reference-identity factory cluster)
    flow-lang/sfz.flow (full file 77 LOC) + flow-lang/notation-io.flow (full file 42 LOC) — module-activation analog
  </read_first>
  <behavior>
    PRE-PLAN package legitimacy gate per RESEARCH lines 168-181:
    - Rug.Osc 1.2.5 verified on NuGet flatcontainer API 2026-05-23 — MIT license, zero transitive deps, .NET Standard 2.0, stable since Jan 2014 (OSC 1.0 spec stable since 2002), source at bitbucket.org/rugcode/rug.osc, "100% thread safe" per author docs.
    - slopcheck [SLOP] verdict is a confirmed false positive — slopcheck only queries PyPI; Rug.Osc is a .NET-only package; NuGet API confirms existence + license + source.
    - Disposition: Approved per RESEARCH §"Package Legitimacy Audit" line 174.

    - OscHandleType is a sealed singleton mirroring SfzType.cs verbatim with class name + Name string + specificity changed: `Name => "OscHandle"`, `GetSpecificity() => 151` (Phase 32 Tuning=150, Phase 33 Sfz=150, Phase 36 MarkovModel=148/LsystemModel=149, Phase 38 OscHandle=151).
    - Value.OscHandle factory mirrors Value.Sfz at Value.cs:71-72: `public static Value OscHandle(FlowLang.StandardLibrary.Network.OscHandleData h) => new(h, OscHandleType.Instance);` with xmldoc citing D-38-16 + reference-identity precedent.
    - osc.flow mirrors notation-io.flow's 42-LOC shape (the simpler of the two): header `Note:` block citing D-38-13..16 + `use "@std"` + `internal proc __enableOscModule ()` + 5 `internal proc` forward decls (`oscSend`/`oscListen`/`oscStop`/`oscBundle`/`oscSendBundle`) + trailing `(__enableOscModule)` side-effect line.
    - flow-lang.csproj adds `<PackageReference Include="Rug.Osc" Version="1.2.5" />` inside the existing `<ItemGroup>` at lines 10-13; adds `<None Update="osc.flow"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory><CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory></None>` adjacent to the notation-io.flow entry per PATTERNS line 924.
    - OscTypeTagInferenceTests.Send_InfersSmallestTagPerArg: instantiate the (yet-to-be-written) InferOscArgs helper directly; call with `[Value.Int(1), Value.Float(1.5), Value.String("hi"), Value.Bool(true)]`; assert the resulting `object[]` is `[1 (int), 1.5f (float), "hi" (string), true (bool)]` and that the implied Rug.Osc type-tag string would be `,ifsT`.
    - OscTypeTagInferenceTests.Send_BufferAsBlob: pass an AudioBuffer Value; assert the resulting object is a `byte[]` blob.
    - OscTypeTagInferenceTests.Send_DoubleStaysDouble: pass `Value.Double(1.5)` (explicit Double); assert `1.5 (double)` not `1.5f (float)` — the inference is type-based, not value-based (composer explicit-cast escape hatch).
    - OscTypeTagInferenceTests.Send_UnsupportedType_Throws: pass a Sequence Value; assert ArgumentException with body matching `[osc] unsupported arg type at index 0` per RESEARCH §L line 1199.
  </behavior>
  <action>
    Edit `flow-lang/flow-lang.csproj`: add `<PackageReference Include="Rug.Osc" Version="1.2.5" />` inside the existing `<ItemGroup>` at lines 10-13. Add `<None Update="osc.flow"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory><CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory></None>` block per PATTERNS line 924.

    Run `dotnet restore flow-lang` to fetch the package.

    Create `flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs` — copy SfzType.cs verbatim with class name OscHandleType + `Name => "OscHandle"` + `GetSpecificity() => 151`. xmldoc cites Phase 38 OSC-01 + the Phase 32/33/36 reference-identity precedent.

    Edit `flow-lang/Runtime/Value.cs`: add the OscHandle factory adjacent to the Sfz factory at line 71-72. xmldoc cites D-38-16 + the reference-identity contract: two `(oscListen ...)` calls produce distinct Values even at the same port/path (no internal cache; mirrors Phase 33 Sfz contract).

    Define `OscHandleData` record in `flow-lang/StandardLibrary/Network/` (file path matches the OscFunctions.cs file from Task 2; declare the record in a small standalone file `flow-lang/StandardLibrary/Network/OscHandleData.cs` OR co-locate with OscFunctions if smaller — researcher's call). Record shape: `public sealed class OscHandleData { public int Port; public string Path; public Rug.Osc.OscReceiver Receiver; public CancellationTokenSource Cts; public Task ListenerTask; }` per PATTERNS line 626-630.

    Create `flow-lang/osc.flow` per the behaviors above — full module file mirroring `flow-lang/notation-io.flow` shape with the OSC-specific forward decls.

    Create `flow-lang.Tests/Integration/Phase38/OscTypeTagInferenceTests.cs` with the 4 cases above. Carries `[Collection("FlowScripts")]` + IDisposable + ResetForTesting. References the (yet-to-be-written) `InferOscArgs` helper in OscFunctions; compile-fail expected until Task 2 lands.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet restore flow-lang &amp;&amp; dotnet build flow-lang 2>&amp;1 | tail -5 &amp;&amp; ls flow-lang/osc.flow flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs 2>&amp;1</automated>
  </verify>
  <done>Rug.Osc 1.2.5 restored. flow-lang compiles (with the new OscHandleType + Value.OscHandle factory). osc.flow exists with the 5 forward decls + __enableOscModule. OscHandleType.Instance is accessible. OscTypeTagInferenceTests compile-fails on InferOscArgs reference (expected; Task 2 lands the function).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OscFunctions (5 builtins: send / listen / stop / bundle / sendBundle) + register in BuiltInFunctions + remaining 4 Wave 0 tests (rate-limit + loopback + bundle + depth-cap)</name>
  <files>flow-lang/StandardLibrary/Network/OscFunctions.cs, flow-lang/StandardLibrary/BuiltInFunctions.cs, flow-lang.Tests/Integration/Phase38/OscRateLimitTests.cs, flow-lang.Tests/Integration/Phase38/OscLoopbackTests.cs, flow-lang.Tests/Integration/Phase38/OscBundleTests.cs, flow-lang.Tests/Integration/Phase38/OscBundleDepthCapTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§K lines 1078-1160 Rug.Osc API + lifecycle pattern lines 1093-1147 OscListen sketch + bundle DispatchPacket depth-cap lines 1119-1147; §L lines 1161-1209 InferOscArgs sketch; §M lines 1210-1236 rate-limit gate)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (OscFunctions analog lines 578-660 registration + lifecycle; type-tag dispatch lines 633-657; rate-limit gate lines 662-673; bundle depth-cap line 659)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-13 charitable inference + D-38-14 drop-newest sample-and-hold + D-38-15 bundle both ways + nesting cap 8 + D-38-16 OscHandle lifecycle)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory catalog lines 337-340 — osc-bind / osc-infer / osc-bundle-depth dedup keys + body wording)
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 65-69 OSC test mappings — rate-limit + loopback + bundle + depth-cap)
    flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs (registration shape lines 20-30; Task.Run + CancellationToken lifecycle precedent)
    flow-lang/Audio/PulseAudioSimpleBackend.cs (lines 80-155 — Task.Run lifecycle pattern analog per PATTERNS line 581)
    flow-lang/StandardLibrary/BuiltInFunctions.cs (RegisterAllImplementations at lines 35-54)
    flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce — used for osc-bind / osc-infer / osc-bundle-depth advisories per Pattern S2 PATTERNS lines 1107-1135)
  </read_first>
  <behavior>
    - OscFunctions.Register(InternalFunctionRegistry registry) registers 5 builtins per PATTERNS lines 586-600:
      - `oscSend(String host, Int port, String path, ...varargs) -> Void` — Varargs=true
      - `oscListen(Int port, String path, Function handler) -> OscHandle` — handler is `(Args... => Void)` lambda
      - `oscStop(OscHandle handle) -> Void`
      - `oscBundle(...varargs OscMessage|OscBundle) -> OscBundle` — Varargs=true; returns a Value wrapping a Rug.Osc OscBundle
      - `oscSendBundle(String host, Int port, OscBundle bundle, [OscTimeTag timetag]) -> Void`
    - InferOscArgs(IReadOnlyList<Value> flowArgs) -> object[] per RESEARCH §L lines 1180-1201: switch on `v.Type`: IntType → int, LongType → long, FloatType → float (cast from double), DoubleType → double, StringType → string, SymbolType → string (interned identity collapses to string on the wire per PATTERNS line 145), BoolType → bool, BufferType → byte[] blob via AudioBufferToBlob helper. Unsupported throws ArgumentException with body `[osc] unsupported arg type at index {i}: {type.Name} — use Int/Long/Float/Double/String/Symbol/Bool/Buffer` per PATTERNS line 651.
    - OscSend body: call InferOscArgs on the varargs slice; instantiate `new Rug.Osc.OscMessage(path, oscArgs)`; instantiate `new Rug.Osc.OscSender(host, port)`; Send; Dispose. On first inference of a non-trivial tag (when arg counts >1 OR includes a non-Int non-String type), emit `RenderingDiagnostics.WarnOnce($"osc-infer:{path}:0", $"[osc] type-tag inferred as '{tagString}' for arg 0 at {path} — pass an explicit cast for finer control")` per UI-SPEC line 338. The one-shot per (path, arg-index) dedup means flooding the same path with the same type signature only advises once per process.
    - OscListen body per RESEARCH §K lines 1093-1118: instantiate `var receiver = new Rug.Osc.OscReceiver(port)`; try `receiver.Connect()` — on SocketException (bind failed) emit `RenderingDiagnostics.WarnOnce($"osc-bind:{port}", $"[osc] bind failed on port {port} — port already in use; oscListen returned no handle")` per UI-SPEC line 337 + return Value.Nil (no handle). On success: spawn `Task.Run(...)` UDP receive loop with CancellationToken; loop calls `receiver.Receive()` (blocking) + DispatchPacket(packet, path, handler, depth=0); on OperationCanceledException break; on other exception emit a non-dedup'd `[osc] receive error on port {port}: {ex.Message}` + continue. Return `Value.OscHandle(new OscHandleData { Port = port, Path = path, Receiver = receiver, Cts = cts, ListenerTask = task })`.
    - DispatchPacket(packet, path, handler, depth): per RESEARCH §K lines 1119-1147: if depth > 8, emit `WarnOnce($"osc-bundle-depth:{path}", "[osc] bundle nesting depth exceeds 8 at {path} — collapsing to flat dispatch")` per UI-SPEC line 339 + recurse only to depth 8 (charitable cap per D-38-15). For OscBundle: honor timetag — if timetag is `OscTimeTag.Immediate` (value 1 per OSC 1.0 spec) dispatch synchronously per D-38-15; else schedule on TaskScheduler queue for the future time. For OscMessage: rate-limit gate (per RESEARCH §M lines 1218-1228 + D-38-14) — `var nowMs = Environment.TickCount64; var lastMs = _lastFireTimeMs.GetOrAdd(path, 0L); if (nowMs - lastMs < 5) return; _lastFireTimeMs[path] = nowMs;` — then invoke the Flow handler lambda via the existing FlowEngine invoke-lambda path with the OscMessage's args translated back to Flow Values (Int/Long/Float/Double/String/Bool/Buffer per the inverse of InferOscArgs).
    - OscStop body: call `handle.Data.Cts.Cancel()`; wait briefly for the ListenerTask to complete (with 1s timeout); call `handle.Data.Receiver.Dispose()`. Pitfall #5 per RESEARCH line 1455: `receiver.Receive()` may block forever ignoring Cts.Cancel(); workaround is `receiver.Dispose()` from outside which forces an exception inside the blocked Receive call, breaking the loop charitably.
    - OscBundle body: collect varargs; each must be an OscHandle-wrapped OscMessage or OscBundle (Value type check); construct `new Rug.Osc.OscBundle(OscTimeTag.Immediate, packets.ToArray())`; return `Value.OscBundle(bundle)` (NEW Value wrapper if needed — researcher may decide to use a new OscBundleType OR reuse a generic "RugOscObject" wrapper). For v1.5, the simplest path is to wrap in `Value.OscHandle` with a discriminator on OscHandleData distinguishing message vs bundle, OR add a sibling `OscBundleType` + `Value.OscBundle(...)`; the researcher picks at implementation time.
    - OscSendBundle body: type-check the bundle arg via OscBundleType (or the OscHandle discriminator); attach the optional timetag arg (default OscTimeTag.Immediate); send via OscSender.
    - Registration in BuiltInFunctions per PATTERNS line 832: `Network.OscFunctions.Register(registry);` adjacent to other Register calls in RegisterAllImplementations. The `@osc` enable flag follows the Phase 33 SFZ pattern — `ExecutionContext.OscEnabled` boolean flipped by the `__enableOscModule` internal proc; builtins registered ALWAYS but error charitably (return Void + advisory) if flag is false per PATTERNS line 836.

    - OscRateLimitTests.SamePath_DropNewestWithinWindow: dispatch two OscMessage handlers 1ms apart to the same path; assert handler invoked exactly once.
    - OscRateLimitTests.DifferentPaths_BothFire: dispatch two OscMessage handlers 1ms apart to different paths; assert both invoked.
    - OscRateLimitTests.SamePath_220msApart_BothFire: dispatch with 220ms gap; assert both fire (above the 5ms / 200Hz window).
    - OscLoopbackTests.RoundTrip_127001_EphemeralPort_PreservesPayload: bind oscListen on `127.0.0.1` port 0 (ephemeral); send oscSend to the bound port with payload `["hello", 1, 1.5]`; assert the handler received `["hello", 1, 1.5]` byte-equivalent within a 2s timeout.
    - OscBundleTests.SendBundle_TwoMessages_DispatchesInOrder: construct oscBundle of two messages targeting different paths; send via oscSendBundle to a loopback listener; assert both handlers fired in bundle order.
    - OscBundleTests.ReceiveBundle_TimetagImmediate_DispatchesSync: bind oscListen; send a bundle with timetag immediate; assert handler fires synchronously (no scheduling delay).
    - OscBundleDepthCapTests.NestedBundlesDepth9_ClampedAt8_EmitsAdvisory: send a 10-level deep bundle; assert the listener receives the outer 8 levels worth of dispatches; assert stderr contains `[osc] bundle nesting depth exceeds 8 at <path> — collapsing to flat dispatch` exactly once.
  </behavior>
  <action>
    Create `flow-lang/StandardLibrary/Network/OscFunctions.cs` (~350 LOC) per the behaviors. Static class in `FlowLang.StandardLibrary.Network` namespace. Public `static void Register(InternalFunctionRegistry registry)`. Private static helpers: `InferOscArgs`, `DispatchPacket(packet, path, handler, depth)`, `AudioBufferToBlob`, `RugOscArgToFlowValue` (inverse of InferOscArgs for received messages). Per-path rate-limit gate state: `private static readonly ConcurrentDictionary<string, long> _lastFireTimeMs = new();` per RESEARCH §M line 1213.

    Optional sibling files: `OscBundleType.cs` + corresponding `Value.OscBundle(...)` factory if the researcher elects to model OscBundle as a distinct Value type (specificity 152). If using the discriminator approach on OscHandleData, no extra file is needed.

    Edit `flow-lang/StandardLibrary/BuiltInFunctions.cs`: in `RegisterAllImplementations` at lines 35-54, add `Network.OscFunctions.Register(registry);` adjacent to `Audio.InputFunctions.Register(registry);` (Plan 38-05 adjacent line).

    Create the 4 remaining test files under `flow-lang.Tests/Integration/Phase38/`. Each carries `[Collection("FlowScripts")]` + IDisposable + ResetForTesting. Use real UDP loopback (bind ephemeral `127.0.0.1:0`) for OscLoopbackTests + OscBundleTests + OscBundleDepthCapTests per VALIDATION.md line 66 ("UDP loopback round-trip 127.0.0.1:ephemeral preserves payload"). Pitfall #10 per RESEARCH line 1491: tests use ephemeral ports + 2s timeout + retry-on-port-bind-fail to avoid CI flakiness.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.Osc" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All 5 OSC test classes GREEN — OscTypeTagInferenceTests + OscRateLimitTests + OscLoopbackTests + OscBundleTests + OscBundleDepthCapTests. `grep -c "Rug.Osc" flow-lang/StandardLibrary/Network/OscFunctions.cs` returns >=3 (using + ctor + Dispose). `grep -c "_lastFireTimeMs" flow-lang/StandardLibrary/Network/OscFunctions.cs` returns >=2. `grep -c "osc-bundle-depth" flow-lang/StandardLibrary/Network/OscFunctions.cs` returns >=1. `grep -c "InferOscArgs" flow-lang/StandardLibrary/Network/OscFunctions.cs` returns >=2. `grep -c "OscFunctions.Register" flow-lang/StandardLibrary/BuiltInFunctions.cs` returns 1. UDP loopback round-trip works within 2s timeout (OscLoopbackTests is the gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UDP network -> flow-lang process | Composer-bound OSC port receives traffic from network; bound to specific port number; default loopback for tests |
| OSC handler lambda -> Flow runtime | Composer-defined handler executes per-message; trusted composer code |
| Rug.Osc NuGet (MIT, bitbucket.org/rugcode/rug.osc) -> flow-lang.dll | First external dep added to flow-lang for Phase 38 OSC scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-SC | Tampering | Rug.Osc 1.2.5 supply-chain (slopcheck [SLOP] false positive — slopcheck only queries PyPI) | mitigate | Package legitimacy gate satisfied at Task 1 plan-start: NuGet flatcontainer API verifies 1.2.5; license MIT; source bitbucket.org/rugcode/rug.osc verified via WebFetch in RESEARCH; 12+ years stable; no transitive deps |
| T-38-10 | Denial of Service | OSC message flood at >200 Hz/path | mitigate | Per-path drop-newest sample-and-hold gate at 5ms window per D-38-14 + RESEARCH §M; OscRateLimitTests assert |
| T-38-15 | Denial of Service | Deeply nested OSC bundle exhausts stack | mitigate | DispatchPacket depth cap at 8 per D-38-15 + UI-SPEC line 339; OscBundleDepthCapTests assert; mirrors Phase 36 T-36-17 + Phase 39 D-39-19 precedent |
| T-38-NET | Information Disclosure | Composer accidentally binds to 0.0.0.0 and receives WAN traffic | accept | Composer responsibility — `oscListen 7777 "/path" handler` binds to all interfaces by default per OSC 1.0 spec; documented in examples/live/osc_controller.flow chapter (Plan 38-07); composer can pass an explicit bind-host string in v1.6 if demand surfaces |
| T-38-RUN | Tampering | Malformed OSC message crashes Rug.Osc Receive loop | mitigate | Receive loop catches all exceptions per RESEARCH §K line 1110 + emits non-dedup'd error advisory; loop continues running; never throws to FlowEngine |
| T-38-PORT | Spoofing | osc-bind: failure (port already in use) | mitigate | Bind failure emits Error advisory + returns no handle per UI-SPEC line 337; composer can retry with a different port; never throws |
</threat_model>

<verification>
- `dotnet build flow-lang flow-lang.Tests` exits 0.
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.Osc" --no-restore` exits 0 (5 test classes GREEN; ~10 facts).
- `grep -n "Rug.Osc" flow-lang/flow-lang.csproj` returns the 1.2.5 PackageReference.
- `grep -n "osc.flow" flow-lang/flow-lang.csproj` returns the <None Update> entry.
- `grep -nE "(oscSend|oscListen|oscStop|oscBundle|oscSendBundle)" flow-lang/StandardLibrary/Network/OscFunctions.cs` shows all 5 builtin registrations.
- Smoke (deferred to Plan 38-07): `dotnet run --project flow-interpreter -e 'use "@osc"; OscHandle h = (oscListen 7777 "/test" (fn Int x => (print x))); (oscSend "127.0.0.1" 7777 "/test" 42); (oscStop h)'` prints `42` and exits 0.
</verification>

<success_criteria>
- Rug.Osc 1.2.5 PackageReference + osc.flow <None Update> added to flow-lang.csproj
- OscHandleType (specificity 151) shipped
- Value.OscHandle factory shipped + OscHandleData record
- osc.flow module with 5 forward decls + __enableOscModule shipped
- OscFunctions.cs registers oscSend / oscListen / oscStop / oscBundle / oscSendBundle
- Charitable type-tag inference per D-38-13 (smallest-tag-that-fits via Value.Type switch)
- Drop-newest sample-and-hold rate limit at 200 Hz/path per D-38-14
- Full bundle support both directions with timetag honored + nesting cap 8 per D-38-15
- BuiltInFunctions wires OscFunctions.Register
- 5 Wave 0 xUnit tests GREEN
- Package legitimacy gate satisfied at Task 1 (Rug.Osc MIT + zero deps verified live on NuGet)
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-06-SUMMARY.md` recording: Rug.Osc version + license, OscHandleType specificity (151), OscFunctions LOC, type-tag inference table verification, rate-limit window (5ms = 1/200Hz), bundle depth cap (8), test pass count, UDP loopback flake-rate per Pitfall #10.
</output>
