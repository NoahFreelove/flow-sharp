---
phase: 38-live-coding-2-0
plan: 07
type: execute
wave: 4
depends_on: ["38-01", "38-02", "38-03", "38-04", "38-05", "38-06"]
files_modified:
  - examples/live/hello_live.flow
  - examples/live/multi_block.flow
  - examples/live/mic_granular.flow
  - examples/live/osc_controller.flow
  - examples/live/repl_session.md
  - tests/test_live_hello.flow
  - tests/test_live_multi_block.flow
  - tests/test_live_mic_granular.flow
  - tests/test_live_osc_controller.flow
  - .planning/phases/38-live-coding-2-0/38-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: false
requirements: [LIVE-01, LIVE-02, LIVE-03, REPL-01, REPL-02, REPL-03, REPL-04, AUDIO-IN-01, AUDIO-IN-02, OSC-01, OSC-02]
must_haves:
  truths:
    - "5 composer-facing tutorial chapters exist under examples/live/ (hello_live.flow, multi_block.flow, mic_granular.flow, osc_controller.flow, repl_session.md) mirroring the Phase 36/39 chapter pattern; each .flow chapter runs cleanly via dotnet run --project flow-interpreter."
    - "5 paired `.flow` regression tests under tests/ (test_live_hello.flow, test_live_multi_block.flow, test_live_mic_granular.flow, test_live_osc_controller.flow) print PASS sentinels per the existing tests/test_*.flow convention."
    - "38-VERIFICATION.md records all 11 REQs as CLOSED (LIVE-01..03, REPL-01..04, AUDIO-IN-01..02, OSC-01..02) with file:line citations of the shipping artifacts."
    - "REQUIREMENTS.md wording overrides applied per D-38-09 (REPL-02 :help fn not ?fn), D-38-10 (REPL-04 ships BOTH (inspect seq) AND (visualize seq) alias), D-38-13 (OSC-02 charitable type-tag inference not strict-by-arg) — D-v1.5-01 pre-traction single-commit migration latitude justifies the in-place wording sweep."
    - "ROADMAP.md Phase 38 row marked Complete with the plan-count table updated; STATE.md frontmatter updated to reflect v1.5 milestone progress 5/7 phases (35 + 36 + 37 + 38 + 39 complete; 40 + 41 remaining)."
    - "CLAUDE.md gains a 'Live Coding 2.0 (Phase 38)' bullet under '## Language Features' summarizing the live { quantize } block + REPL polish + audio input + OSC surfaces per Phase 36/37/39 closer precedent."
    - "Composer hand-runs the 5 manual smoke verifications from 38-VALIDATION.md lines 95-101 (ANSI panel cross-terminal smoke, Ctrl+R history search, real-mic capture, OSC controller round-trip, live performance hot-edit) and records outcomes in 38-HUMAN-UAT.md (this plan creates the file; manual rows can be deferred to first composer use per Phase 17 precedent)."
  artifacts:
    - path: ".planning/phases/38-live-coding-2-0/38-VERIFICATION.md"
      provides: "Per-REQ verification log; shipped + closed status + file:line evidence for all 11 REQs"
      contains: "LIVE-01"
    - path: "examples/live/hello_live.flow"
      provides: "Minimal `live 1bar { }` tutorial chapter"
      min_lines: 30
    - path: "examples/live/multi_block.flow"
      provides: "Multi-block independent swap demo (D-38-02)"
      min_lines: 40
    - path: "examples/live/mic_granular.flow"
      provides: "AUDIO-IN + DSP-01 composition demo"
      min_lines: 25
    - path: "examples/live/osc_controller.flow"
      provides: "OSC server + client loopback round-trip demo"
      min_lines: 30
    - path: "examples/live/repl_session.md"
      provides: "Narrated REPL transcript demonstrating :help, (inspect seq), Tab completion"
      min_lines: 50
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 38 wording overrides"
      via: "Plan 38-07 sweeps REPL-02 + REPL-04 + OSC-02 lines per D-38-09 / D-38-10 / D-38-13"
      pattern: "(:help fn|charitable type-tag|inspect seq|visualize seq)"
    - from: ".planning/ROADMAP.md"
      to: "Phase 38 status update"
      via: "Plan table row updated to 7/7 Complete + STATE.md progress percent updated"
      pattern: "Phase 38.*Complete"
    - from: "CLAUDE.md"
      to: "Live Coding 2.0 bullet"
      via: "Language Features section gains a Phase 38 entry summarizing the live block + REPL + audio-in + OSC surfaces"
      pattern: "Phase 38"
---

<objective>
Closer for Phase 38: ship the 5 composer-facing tutorial chapters under `examples/live/` per CONTEXT.md `<canonical_refs>` lines 151-156 (mirrors Phase 36/39 chapter-as-test pattern); ship 4 paired `.flow` regression tests under `tests/`; produce `38-VERIFICATION.md` with all 11 REQ closures; SWEEP `REQUIREMENTS.md` to apply the three wording overrides per D-v1.5-01 single-commit migration justification (REPL-02 `?fn` → `:help fn`; REPL-04 `(inspect seq)` → ships both `(inspect seq)` AND `(visualize seq)` alias; OSC-02 strict-tag → charitable inference per D-38-13); update `ROADMAP.md` Phase 38 row + `STATE.md` frontmatter for v1.5 milestone progress 5/7; update `CLAUDE.md` with a Phase 38 Language Features bullet matching the Phase 36/37/39 closer precedent; create `38-HUMAN-UAT.md` with the 5 manual smoke verifications from 38-VALIDATION.md lines 95-101.

Purpose: This is a docs-only closer that turns the 6 implementation plans into composer-facing material + sweeps the project tracking docs. Without it, the work shipped but composers don't know it; without the REQUIREMENTS sweep, future plan-checkers will flag the wording overrides as discrepancies.

Output: 5 `examples/live/*.flow` + `repl_session.md` chapters; 4 paired `tests/test_live_*.flow` regression tests; new `38-VERIFICATION.md`; new `38-HUMAN-UAT.md` (per Phase 17 precedent); updated `ROADMAP.md` + `STATE.md` + `REQUIREMENTS.md` + `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@examples/notation/to_musicxml.flow
@examples/dsp/granular.flow
@.planning/phases/37-sound-design-sampler-polish/37-VERIFICATION.md
@.planning/phases/39-notation-citizenship/39-VERIFICATION.md

<interfaces>
<!-- Patterns to copy from prior closers -->

From .planning/phases/37-sound-design-sampler-polish/37-07-PLAN.md (Phase 37 closer pattern):
- VERIFICATION.md format: per-REQ status with file:line evidence
- ROADMAP.md sweep: plan table row → Complete + date + plan list
- STATE.md: frontmatter progress + Resume Instructions update
- REQUIREMENTS.md: cross-insert section per phase
- CLAUDE.md: language-features bullet per phase

From .planning/phases/39-notation-citizenship/39-05-PLAN.md (Phase 39 closer pattern):
- Examples directory chapter pattern (each .flow opens with `Note: ===== {name} — Phase XX {REQ} chapter =====`)
- Paired tests/test_*.flow with PASS sentinel
- CLAUDE.md sweep updating the "Language Features" + standard-library modules sections

From examples/notation/to_musicxml.flow (Phase 39 chapter shape):
- 30-line Note: header block with Run: / Output: / Verify: sections + use "@notation-io" + body

From REQUIREMENTS.md Phase 38 section (lines 89-110):
- REPL-02 line 98 currently reads "Inline `?fn` help" — SWEEP to "Inline `:help fn` help (D-38-09 — :help fn not bare ?fn)"
- REPL-04 line 100 currently reads "Pretty piano-roll on `(inspect seq)`" — SWEEP to "Pretty piano-roll on `(inspect seq)` / `(visualize seq)` alias pair (D-38-10 — both names ship backed by one impl)"
- OSC-02 line 110 currently reads "Args explicitly typed at the type-tag level (no implicit conversion)" — SWEEP to "Args charitable smallest-tag-that-fits inference (D-38-13 — Int→,i Long→,h Float→,f Double→,d ...; composer escape hatch via explicit cast)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship 5 examples/live/ tutorial chapters + 4 paired tests/test_live_*.flow regressions</name>
  <files>examples/live/hello_live.flow, examples/live/multi_block.flow, examples/live/mic_granular.flow, examples/live/osc_controller.flow, examples/live/repl_session.md, tests/test_live_hello.flow, tests/test_live_multi_block.flow, tests/test_live_mic_granular.flow, tests/test_live_osc_controller.flow</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (canonical_refs Examples to ship lines 151-156)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (examples/live/ chapter analog lines 1045-1083; tests/test_*.flow analog lines 996-1042)
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 82-87 — 5 .flow chapter files double as regression tests per Phase 36/39 precedent)
    examples/notation/to_musicxml.flow (Phase 39 chapter shape lines 1-23 with Run: / Output: / Verify: header block)
    examples/dsp/granular.flow (Phase 37 chapter shape — Note: header with W4 LOCK + composer-facing knobs section)
    tests/test_live_reload.flow (analog format 20 LOC)
    tests/test_repl_autoimport.flow (PASS sentinel pattern)
  </read_first>
  <action>
    Create the 5 example chapters under `examples/live/` per CONTEXT.md `<canonical_refs>` lines 151-156:

    1. `examples/live/hello_live.flow` — minimal `live 1bar { ... }` with one synth voice. Header `Note: ===== hello_live.flow — Phase 38 LIVE-01 chapter =====` per Phase 39 to_musicxml.flow shape lines 1-23. Body: `use "@std" + use "@audio" + tempo 120 { live 1bar { section main { Sequence melody = | C4q D4q E4q F4q | } Song song = [main] Buffer result = (renderSong song "piano") (play result) } }`. Composer-edit cue: comment-block at the top says "Save this file with `flow watch examples/live/hello_live.flow` running, then edit the melody — hear the swap at the next bar boundary."

    2. `examples/live/multi_block.flow` — `live 1bar { drums }` + `live 2bar { pad }` per D-38-02 multi-block independent swap. Two separate live blocks, both register independently, swap on their own quantize timelines.

    3. `examples/live/mic_granular.flow` — `(micBuffer 4s) -> (granular grain=50ms density=20Hz jitter=0.3) -> (gain -6dB) -> play`. Comment-block at top notes the -20 dB attenuation advisory + the resample advisory will fire once. Real PA dependency for the live demo; the smoke test (Task 1 part 2 below) uses a fixture path.

    4. `examples/live/osc_controller.flow` — `(oscListen 7777 "/fader/1" (fn Double v => (print (concat "fader=" (str v))))) -> as h; (oscSend "127.0.0.1" 7777 "/fader/1" 0.5); (oscStop h)` round-trip demo. Comment-block notes the charitable type-tag inference (D-38-13) + the rate-limit semantics (D-38-14).

    5. `examples/live/repl_session.md` — NARRATED MARKDOWN transcript (not a .flow chapter) demonstrating `:help transpose`, `(inspect seq)`, Tab completion behavior + sample REPL output. Mirrors `examples/notation/README.md` shape per PATTERNS line 1081. Includes 3 sample sessions: discovering a builtin via `:help`, visualizing a Sequence via `(inspect seq)`, completing a partial identifier via Tab.

    Each `.flow` chapter follows the `Note: ===== ... =====` header + `Run:` + `Output:` + `use "@..."` import block convention per Phase 39 precedent.

    Create the 4 paired regression tests under `tests/`:
    - `tests/test_live_hello.flow` — wraps hello_live.flow body in a minimal smoke; prints `PASS: live hello smoke ran` sentinel. Does NOT exercise the live-reload runtime (that requires a file save event); just validates the parser + interpreter path for `live 1bar { }` blocks.
    - `tests/test_live_multi_block.flow` — validates two distinct LiveBlockStatements register independently in LiveBlockRegistry; prints `PASS: multi-block live registration` after asserting via a `(print (str (length blocks)))` helper IF the runtime exposes it OR simply by running the script through interpreter exit-0.
    - `tests/test_live_mic_granular.flow` — uses fixture WAV path via the `__setMicMockFixture` test seam (if Task 38-05 added it) OR via real PA; prints `PASS: mic + granular pipeline composes`.
    - `tests/test_live_osc_controller.flow` — UDP loopback `127.0.0.1:0` ephemeral port; sends a message; receives it; prints `PASS: OSC loopback round-trip`. Bounded by 2s timeout per Pitfall #10 anti-flake.

    Each test follows the `Note:` header + `use` block + body + `(print "PASS: ...")` convention.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; for f in tests/test_live_hello.flow tests/test_live_multi_block.flow tests/test_live_osc_controller.flow; do dotnet run --project flow-interpreter "$f" 2>&amp;1 | grep "PASS:" || echo "FAIL: $f"; done</automated>
  </verify>
  <done>5 examples/live/*.{flow,md} files exist and the .flow chapters parse + execute cleanly. 4 tests/test_live_*.flow files print their respective PASS sentinels (test_live_mic_granular.flow may require manual run on a host with PulseAudio + default mic device — that's acceptable per Phase 17 precedent if the test seam isn't wired).</done>
</task>

<task type="auto">
  <name>Task 2: Write 38-VERIFICATION.md + sweep REQUIREMENTS.md wording overrides + 38-HUMAN-UAT.md</name>
  <files>.planning/phases/38-live-coding-2-0/38-VERIFICATION.md, .planning/phases/38-live-coding-2-0/38-HUMAN-UAT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 95-101 manual-only verifications)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-09 + D-38-10 + D-38-13 wording overrides + REQUIREMENTS.md sweep mandate at line 193)
    .planning/REQUIREMENTS.md (lines 89-110 Phase 38 section + line 109 OSC-02 strict wording)
    .planning/phases/37-sound-design-sampler-polish/37-VERIFICATION.md (analog format)
    .planning/phases/39-notation-citizenship/39-VERIFICATION.md (closer analog)
    .planning/phases/17-flow-language-server/17-HUMAN-UAT.md (HUMAN-UAT analog with row table per Phase 17 precedent)
  </read_first>
  <action>
    Create `.planning/phases/38-live-coding-2-0/38-VERIFICATION.md` per the Phase 37/39 closer analog format. Header: `# Phase 38 Verification`. Body: per-REQ table with columns `| REQ | Status | Evidence (file:line) | Notes |`. Mark all 11 REQs (LIVE-01, LIVE-02, LIVE-03, REPL-01, REPL-02, REPL-03, REPL-04, AUDIO-IN-01, AUDIO-IN-02, OSC-01, OSC-02) as CLOSED with file:line citations of the shipping artifacts:
    - LIVE-01 → flow-lang/Ast/Statements/LiveBlockStatement.cs + flow-lang/Parsing/Parser.cs ParseLiveBlockStatement + flow-lang/Interpreter/Interpreter.cs ExecuteLiveBlock
    - LIVE-02 → flow-interpreter/LiveReloadManager.cs (200ms debounce + 30s CancellationToken + ANSI panel) + flow-interpreter/LiveStatusPanel.cs
    - LIVE-03 → flow-lang/StandardLibrary/Audio/Voice.cs (Name + CopyStateFrom) + VoiceAllocator.cs DiffByVoiceName + Interpreter/LambdaCaptureAuditor.cs + LiveReloadManager.StagePendingBuffers
    - REPL-01..04 → flow-interpreter/Repl.cs + ReplLineEditor.cs + flow-lang/StandardLibrary/VisualizationFunctions.cs
    - AUDIO-IN-01..02 → flow-lang/Audio/PulseAudioCaptureBackend.cs + flow-lang/StandardLibrary/Audio/InputFunctions.cs
    - OSC-01..02 → flow-lang/StandardLibrary/Network/OscFunctions.cs + flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs + flow-lang/osc.flow

    Footer section "Manual-only smoke verifications (deferred to first composer use per Phase 17 precedent)" lists the 5 manual rows from 38-VALIDATION.md.

    Create `.planning/phases/38-live-coding-2-0/38-HUMAN-UAT.md` per the Phase 17 17-HUMAN-UAT.md analog. Header + table with rows for the 5 manual-only verifications from 38-VALIDATION.md lines 95-101:
    | Row | Behavior | REQ | Status | Composer notes |
    | 1 | ANSI status panel cross-terminal smoke | LIVE-02 | pending | (composer fills in) |
    | 2 | Ctrl+R history search interactive feel | REPL-03 | pending | |
    | 3 | Real-microphone capture loopback | AUDIO-IN-01 | pending | |
    | 4 | OSC controller round-trip with a real surface | OSC-01, OSC-02 | pending | |
    | 5 | Live performance feel — composer hot-edits during playback | LIVE-01..03 | pending | |

    Sweep `.planning/REQUIREMENTS.md` per D-v1.5-01 single-commit migration latitude (pre-traction no-deprecation per CLAUDE.md):
    - Line 98 REPL-02: rewrite the body to `Inline `:help fn` meta-command — `:help transpose` prints signature + doc-comment + 1-line example from `BuiltInDocs` table (Phase 31 LSP table — 104 entries reused by REPL). Composer-asks via `:help <name>` per D-38-09 consistency with existing `:quit`/`:help`/`:clear`/`:stop` meta-command family (overrides earlier `?fn` wording).` Keep the [ ] checkbox; this is a wording update, not a status update.
    - Line 100 REPL-04: rewrite to `Pretty piano-roll on `(inspect seq)` / `(visualize seq)` alias pair (D-38-10 — both names ship backed by one impl) — ASCII piano-roll with pitch on Y axis, time on X axis; tick marks at bar boundaries; articulation glyphs (`>` Accent, `.` Staccato, `^` Marcato, `_` Tenuto, `!` Sforzando, `~` Legato) at note onsets per UI-SPEC §"Glyph Inventory".`
    - Line 110 OSC-02: rewrite to `OSC client — `(oscSend host port path arg1 arg2 ...)`. Args charitable smallest-tag-that-fits inference per D-38-13: Int→,i Long→,h Float→,f Double→,d String/Symbol→,s Bool→,T/,F Buffer→,b. Composer escape hatch via explicit cast (`1.5d` for explicit Double, `(toLong 1)` for Long). Uses Rug.Osc 1.2.5.`
    - Also update the Phase 38 Traceability table entries lower in the file if present (REPL-02 / REPL-04 / OSC-02 rows) to "Shipped (Plan 38-04 / 38-04 / 38-06)" with the relevant plan-id commit refs filled in after Plan 38-XX completion.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; ls .planning/phases/38-live-coding-2-0/38-VERIFICATION.md .planning/phases/38-live-coding-2-0/38-HUMAN-UAT.md &amp;&amp; grep -E "(:help fn|charitable.*type-tag|inspect.*visualize alias)" .planning/REQUIREMENTS.md | head -5</automated>
  </verify>
  <done>38-VERIFICATION.md exists with all 11 REQ closures + file:line evidence. 38-HUMAN-UAT.md exists with 5 manual rows. REQUIREMENTS.md REPL-02 / REPL-04 / OSC-02 lines rewritten per D-38-09 / D-38-10 / D-38-13. `grep -c ":help fn" .planning/REQUIREMENTS.md` returns >=1. `grep -c "charitable smallest-tag" .planning/REQUIREMENTS.md` returns >=1. `grep -c "inspect seq.*visualize seq.*alias" .planning/REQUIREMENTS.md` returns >=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Composer reviews Phase 38 closer + 5 manual smoke verifications</name>
  <what-built>All 6 implementation plans (38-01..06) shipped; 5 examples/live/ chapters + 4 paired tests/test_live_*.flow regressions exist; 38-VERIFICATION.md + 38-HUMAN-UAT.md created; REQUIREMENTS.md REPL-02/REPL-04/OSC-02 swept per D-v1.5-01 single-commit migration latitude. ROADMAP.md + STATE.md + CLAUDE.md sweeps deferred to Task 4 (post-approval, automated).</what-built>
  <how-to-verify>
    1. Run the 5 manual-only verifications from 38-VALIDATION.md lines 95-101:
       a. `dotnet run --project flow-interpreter -- watch examples/live/hello_live.flow` in your terminal (xterm / iTerm2 / Konsole / Windows Terminal — whichever you use); verify the 4-row ANSI panel redraws cleanly without flicker or stale rows; verify row 1 (tempo+timesig+bar), row 2 (live blocks; should show `live 1bar @ L<N>`), row 3 (voices), row 4 (sticky advisory clears after 8s). Pipe through `cat` and verify plain-line fallback emits `[watch] tempo=... bar=...` lines.
       b. `dotnet run --project flow-interpreter` (interactive REPL); type a few commands; press Ctrl+R; type a substring; verify the most-recent matching history entry surfaces. Type `:help transpose` and verify the 3-block layout (bold+green header + dim signature + body + Example).
       c. `dotnet run --project flow-interpreter -e '(micBuffer 5s) -> (writeWav "/tmp/mic.wav")'`; verify /tmp/mic.wav exists and is audible when played back.
       d. Open TouchOSC (or any OSC controller) on a phone; point at the dev machine on port 7777; in another terminal: `dotnet run --project flow-interpreter -e 'use "@osc"; (oscListen 7777 "/touch/1" (fn Double v => (print (concat "received: " (str v)))))'`; touch the controller; verify console prints "received: <value>" lines.
       e. `dotnet run --project flow-interpreter -- watch examples/live/multi_block.flow`; while playback runs, edit a live block body in your editor, save; listen for clean swap at next bar boundary; verify the row-4 sticky advisory reads `[live] block @L<N> swapped at bar <M>` (green).
    2. Fill in the 5 rows of 38-HUMAN-UAT.md with your observations (PASS / partial / fail + notes).
    3. If any of the 5 verifications fail, file the failure mode against the responsible plan (38-01 panel; 38-04 REPL; 38-05 mic; 38-06 OSC; 38-02 / 38-03 live block lifecycle).
  </how-to-verify>
  <resume-signal>Type "approved — proceed to closer task 4" once all 5 manual verifications complete (PASS or documented partial), OR "deferred — accept partial closure" to mark unhanded rows as "deferred to first release tag" per Phase 17 precedent.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md + STATE.md + CLAUDE.md per Phase 37/39 closer precedent</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, CLAUDE.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 38 row at line 125 + plan-count line + Progress table line 319)
    .planning/STATE.md (frontmatter progress + Current Position + Resume Instructions)
    CLAUDE.md (Language Features section + Standard Library Modules section + ## Generative subsection)
    .planning/phases/37-sound-design-sampler-polish/37-07-PLAN.md (closer sweep precedent — search for ROADMAP / STATE / CLAUDE patches)
    .planning/phases/39-notation-citizenship/39-05-PLAN.md (Phase 39 closer sweep precedent)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`:
    1. Line 125 (current "[ ] Phase 38: Live Coding 2.0 — `live <quantize> { ... }` block + modernized watch mode + REPL polish ..."): flip to `[x] Phase 38: Live Coding 2.0` with closure date `(completed 2026-MM-DD)` per Phase 37/39 precedent. Replace `Plans: TBD` with `Plans: 7 plans`.
    2. Add the per-plan table block under the Phase 38 detail section (mirroring Phase 37's Plans 37-01..37-07 block at lines 199-205):
       - `- [x] 38-01-PLAN.md — Modernized watch + ANSI status panel + 200ms debounce + 30s CancellationToken (LIVE-02)`
       - `- [x] 38-02-PLAN.md — live { quantize } block AST + Lexer/Parser/Interpreter + LiveBlockRegistry (LIVE-01)`
       - `- [x] 38-03-PLAN.md — State preservation across reload: Voice.Name + DiffByVoiceName + LambdaCaptureAuditor + PRNG reseed (LIVE-03)`
       - `- [x] 38-04-PLAN.md — REPL polish: PrettyPrompt + in-process LSP completion + :help fn + (inspect seq) alias + articulation glyphs (REPL-01..04)`
       - `- [x] 38-05-PLAN.md — Audio input: PulseAudioCaptureBackend + micBuffer + -20 dB attenuation + 44.1 kHz resample (AUDIO-IN-01..02)`
       - `- [x] 38-06-PLAN.md — OSC: Rug.Osc + oscSend/oscListen/oscStop/oscBundle/oscSendBundle + charitable type-tag inference + 200Hz rate limit + bundle depth-cap 8 (OSC-01..02)`
       - `- [x] 38-07-PLAN.md — Closer (5 examples + 4 paired tests + 38-VERIFICATION + ROADMAP/STATE/REQUIREMENTS/CLAUDE sweep)`
    3. Progress table at line 319: flip Phase 38 row to `| 38. Live Coding 2.0 | v1.5 | 7/7 | Complete | 2026-MM-DD |`.

    Edit `.planning/STATE.md`:
    1. Frontmatter: increment `completed_phases` from 4 to 5; recompute `percent`.
    2. `last_activity` line: replace with `"2026-MM-DD — Phase 38 Live Coding 2.0 SHIPPED — live { quantize } block + modernized watch + ANSI panel + REPL polish + audio input + OSC; PrettyPrompt 4.1.1 + Rug.Osc 1.2.5 NuGets added; 11/11 REQs CLOSED per 38-VERIFICATION.md"`.
    3. Resume Instructions: update to reflect Phase 38 complete; remaining v1.5 work is Phase 40 (Studio Sync) then Phase 41 (Reach + Closer). Mention "Phase 38 + Phase 40 are commutative on the original plan; composer already picked Phase 38; next: `/clear` then `/gsd:context-phase 40`."
    4. Add a "Phase 38 highlights:" bullet block mirroring the existing Phase 36/37/39 highlights cluster (key surfaces, file paths, key decisions D-38-09/10/13/15 wording overrides per D-v1.5-01).

    Edit `CLAUDE.md`:
    1. Under `## Language Features` add a `### Live Coding 2.0 (Phase 38)` entry mirroring the Phase 36/37/39 closer additions. Cover: `live <quantize> { body }` block + ANSI status panel + REPL polish (`:help fn` + Tab completion + history search + `(inspect seq)` alias + articulation glyphs) + audio input (`(micBuffer)`) + OSC surface (`oscSend`/`oscListen`/`oscStop`/`oscBundle`/`oscSendBundle` via `use "@osc"`) + the 3 REQUIREMENTS.md wording overrides per D-38-09/10/13 + D-v1.5-07 live determinism opt-out.
    2. Under `## Standard Library Modules (.flow files)`, add `osc.flow — OSC client + server (Phase 38 OSC-01..02; opt-in via use "@osc"); Rug.Osc 1.2.5 backed.` adjacent to the existing `notation-io.flow` entry.
    3. Under `### Audio Effects (Audio/DSP/)`, add a sentence noting `(micBuffer duration)` audio input is now available per Phase 38 AUDIO-IN-01.
    4. Under `### Generative` / `## Built-in Function Categories`, add a `### Network` subsection citing the 5 OSC builtins.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; grep -c "Phase 38" .planning/ROADMAP.md &amp;&amp; grep -c "Live Coding 2.0" CLAUDE.md &amp;&amp; grep -c "completed_phases: 5" .planning/STATE.md</automated>
  </verify>
  <done>ROADMAP.md Phase 38 row marked Complete; 7 plans listed. STATE.md frontmatter shows completed_phases=5. CLAUDE.md gains the Live Coding 2.0 section under Language Features. `grep -c "osc.flow" CLAUDE.md` returns >=1 (Standard Library Modules entry). All file:line citations in 38-VERIFICATION.md resolve via grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer reads docs -> understands surface | UI-SPEC + CLAUDE.md + examples/live/ are the composer-facing surface; trusted local rendering |
| Plan-checker reads REQUIREMENTS.md -> verifies coverage | Plan-checker assumes REQUIREMENTS.md is the floor; wording overrides per D-v1.5-01 are explicitly justified in the file + in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-DOC | Information Disclosure | Composer follows examples/live/osc_controller.flow and unwittingly binds to 0.0.0.0 receiving WAN traffic | accept | Composer responsibility — chapter comment-block notes the binding semantics; v1.6 may add explicit-host argument to oscListen |
| T-38-REQ | Tampering | REQUIREMENTS.md wording override could re-introduce contradictions if a future plan-checker reads the original wording | mitigate | This plan applies the sweep in-place; the 38-VERIFICATION.md + this plan + CONTEXT.md all cite D-38-09 / D-38-10 / D-38-13 + D-v1.5-01 single-commit justification |
</threat_model>

<verification>
- `for f in examples/live/*.flow; do dotnet run --project flow-interpreter "$f" || exit 1; done` exits 0 (per VALIDATION.md line 69 closer smoke).
- `for t in tests/test_live_*.flow; do dotnet run --project flow-interpreter "$t" || exit 1; done` exits 0 (mic test may be marked manual-only per Phase 17 precedent if PA unavailable).
- `grep -c "LIVE-01\|LIVE-02\|LIVE-03\|REPL-01\|REPL-02\|REPL-03\|REPL-04\|AUDIO-IN-01\|AUDIO-IN-02\|OSC-01\|OSC-02" .planning/phases/38-live-coding-2-0/38-VERIFICATION.md` returns 11 (all REQ IDs cited).
- `grep -E "(:help fn|charitable smallest-tag-that-fits|inspect.*visualize)" .planning/REQUIREMENTS.md` returns the 3 wording overrides per VALIDATION.md line 70.
- ROADMAP.md Phase 38 row reads "Complete" + 7/7 plan count.
- STATE.md frontmatter: `completed_phases: 5` + last_activity updated.
- CLAUDE.md contains a `### Live Coding 2.0 (Phase 38)` section + `osc.flow` in Standard Library Modules list.
- 38-HUMAN-UAT.md created with 5 manual rows (may be marked pending per Phase 17 precedent if composer defers).
</verification>

<success_criteria>
- 5 examples/live/ tutorial chapters shipped (4 .flow + 1 narrated .md)
- 4 paired tests/test_live_*.flow regression tests print PASS sentinels
- 38-VERIFICATION.md exists with all 11 REQ closures + file:line evidence
- 38-HUMAN-UAT.md exists with 5 manual smoke rows
- REQUIREMENTS.md REPL-02 / REPL-04 / OSC-02 wording overrides applied per D-v1.5-01
- ROADMAP.md Phase 38 row marked Complete with 7-plan table
- STATE.md frontmatter updated (completed_phases=5; last_activity + Resume Instructions updated)
- CLAUDE.md gains Phase 38 Language Features section + osc.flow standard-library entry
- Composer hand-runs the 5 manual smoke verifications (or defers per Phase 17 precedent)
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-07-SUMMARY.md` recording: 5 example chapter file list, 4 paired test file list, 11-REQ closure verification, REQUIREMENTS.md sweep line ranges, ROADMAP/STATE/CLAUDE diff summary, manual UAT row status.
</output>
