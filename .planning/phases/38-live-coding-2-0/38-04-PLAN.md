---
phase: 38-live-coding-2-0
plan: 04
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - flow-interpreter/Repl.cs
  - flow-interpreter/ReplLineEditor.cs
  - flow-interpreter/flow-interpreter.csproj
  - flow-lang/StandardLibrary/VisualizationFunctions.cs
  - flow-lang.Tests/Integration/Phase38/ReplCompletionTests.cs
  - flow-lang.Tests/Integration/Phase38/ReplHelpMetaCommandTests.cs
  - flow-lang.Tests/Integration/Phase38/ReplMultiLineTests.cs
  - flow-lang.Tests/Integration/Phase38/ReplHistorySearchTests.cs
  - flow-lang.Tests/Integration/Phase38/VisualizeArticulationGlyphTests.cs
  - flow-lang.Tests/Integration/Phase38/InspectAliasTests.cs
  - flow-lang.Tests/Integration/Phase38/GlyphCollisionTests.cs
autonomous: true
requirements: [REPL-01, REPL-02, REPL-03, REPL-04]
user_setup:
  - service: nuget
    why: "PrettyPrompt 4.1.1 readline library (D-38-11 readline gate winner per RESEARCH §H; MPL-2.0; .NET 6+ compatible with .NET 10; verified live on NuGet 2026-05-23)"
    install_cmd: "dotnet add flow-interpreter package PrettyPrompt --version 4.1.1"
must_haves:
  truths:
    - "Composer launches `flow` REPL; pressing Tab on a partial identifier (e.g., `transp<Tab>`) shows in-process LSP-backed completion items from `CompletionHandler.BuildItems()` (Phase 31 flow-lsp surface consumed directly per RESEARCH §G + D-38-12 SIMPLIFICATION FINDING)."
    - "Composer types `:help transpose` and the REPL prints a 3-block layout: bold+green header line with proc name, dim signature lines, default-weight body with the doc-comment, and a `Example:` block with a 1-line example pulled from `BuiltInDocs.TryGet(\"transpose\")`."
    - "Composer types multi-line input via paren-balanced continuation (continuation prompt `... `) AND via backslash-at-EOL — both preserved per existing Repl.cs:117-119 + 182-208 lexer-based detection."
    - "Composer presses Ctrl+R during the REPL session; PrettyPrompt's HistorySearchBackward navigates `~/.config/flow/history` (XDG-compatible, mode 0600 on Linux/macOS, 10k-entry cap rotated at append) per UI-SPEC line 295-302."
    - "Composer calls `(inspect seq)` or `(visualize seq)` and gets an ASCII piano-roll with: existing pitch+time grid + bar tick-mark row above the first pitch row + articulation glyphs at note onsets (`>` Accent, `.` Staccato, `^` Marcato, `_` Tenuto, `!` Sforzando, `~` Legato in gap-cells); existing scripts that don't carry articulation render with `#` only (backward-compatible)."
  artifacts:
    - path: "flow-interpreter/ReplLineEditor.cs"
      provides: "PrettyPrompt wrapper exposing FlowPromptCallbacks (Tab→CompletionHandler.BuildItems, multi-line continuation via existing IsInputComplete logic)"
      min_lines: 200
    - path: "flow-interpreter/Repl.cs"
      provides: "Extended REPL with `:help <name>` meta-command branch + ReplLineEditor swap of Console.ReadLine"
      contains: ":help"
    - path: "flow-lang/StandardLibrary/VisualizationFunctions.cs"
      provides: "Extended (visualize seq) with articulation glyphs + tick-mark row + (inspect seq) alias registration"
      contains: "inspect"
  key_links:
    - from: "flow-interpreter/Repl.cs"
      to: "flow-lang/StandardLibrary/BuiltInDocs.cs"
      via: ":help <name> calls BuiltInDocs.TryGet(name) per Phase 31 LSP doc-table"
      pattern: "BuiltInDocs\\.TryGet"
    - from: "flow-interpreter/ReplLineEditor.cs"
      to: "flow-lsp/Handlers/CompletionHandler.cs"
      via: "FlowPromptCallbacks.GetCompletionItemsAsync calls the static BuildItems(uri, text, ast, tokens, cursor, 4 indices)"
      pattern: "CompletionHandler\\.BuildItems"
    - from: "flow-lang/StandardLibrary/VisualizationFunctions.cs"
      to: "Phase 28 Articulation enum"
      via: "switch expression maps Articulation.{Accent|Staccato|Marcato|Tenuto|Sforzando|Legato} to single-cell ASCII glyph at note-onset cell"
      pattern: "Articulation\\.Accent"
---

<objective>
Polish the REPL surface per REPL-01..04: swap `Console.ReadLine`-based input for PrettyPrompt 4.1.1 (D-38-11 readline gate winner per RESEARCH §H — MPL-2.0, .NET 6+, REPL-03 Ctrl+R + multi-line satisfied out of the box; ReadLine 2.0.1 rejected for lacking both); embed `flow-lsp/Handlers/CompletionHandler.BuildItems()` STATIC method directly per D-38-12 SIMPLIFICATION FINDING from RESEARCH §G (no MemoryStream LanguageServer plumbing — BuildItems is already static + transport-decoupled); ship `:help <name>` meta-command branch on existing `HandleCommand` dispatch (D-38-09 OVERRIDES REQUIREMENTS.md REPL-02 `?fn` wording per D-v1.5-01 single-commit migration latitude); extend `(visualize seq)` with articulation glyphs (Phase 28 enum mapping) + bar tick-mark row + register `(inspect seq)` builtin-level alias (D-38-10 OVERRIDES REQUIREMENTS.md REPL-04 wording to ship BOTH names).

Purpose: REPL is the second core composer surface alongside `flow watch`. Phase 31 shipped the LSP completion + doc table but they were transport-locked to LSP clients; this plan unlocks them for the REPL. The piano-roll extension makes `(inspect seq)` musically informative (you can SEE the staccato vs accent vs legato shape on screen).

Output: New `ReplLineEditor.cs` (~250 LOC PrettyPrompt wrapper); extended `Repl.cs` (`:help <name>` branch + line-editor wiring); extended `VisualizationFunctions.cs` (articulation glyph switch + tick-mark row + `(inspect seq)` Register entry); `flow-interpreter.csproj` adds PrettyPrompt PackageReference + `flow-lsp` ProjectReference; 7 Wave 0 xUnit tests covering completion / help / multi-line / history search / articulation glyphs / inspect alias / glyph collision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@CLAUDE.md
@flow-interpreter/Repl.cs
@flow-interpreter/flow-interpreter.csproj
@flow-lang/StandardLibrary/VisualizationFunctions.cs
@flow-lang/StandardLibrary/BuiltInDocs.cs
@flow-lsp/Handlers/CompletionHandler.cs
@flow-lsp/Handlers/HoverHandler.cs

<interfaces>
<!-- Extracted from existing tree -->

From flow-interpreter/Repl.cs (preserved + extended):
- Meta-command dispatch at lines 210-220: switch on `:quit/:q/:exit`, `:help/:h`, `:clear/:cls`, `:stop`, else UnknownCommand
- ReadCompleteInput at lines 105-147 + IsInputComplete at lines 182-208: paren-balanced + backslash continuation (lexer-based) — REUSED inside ReplLineEditor.IPromptCallbacks
- Ctrl+C handler at lines 31-37: stops audio without exiting REPL — PRESERVED via PrettyPrompt's IPromptCallbacks hook

From flow-lsp/Handlers/CompletionHandler.cs (Phase 31 surface):
- public static List<CompletionItem> BuildItems(string uri, string text, Program? ast, IReadOnlyList<Token> tokens, Position cursor, BuiltInIndex builtIns, StdlibSymbolIndex stdlib, KeywordIndex keywords, UserSymbolIndex userSymbols)
  // lines 95-144 — directly callable, no transport coupling (RESEARCH §G SIMPLIFICATION FINDING)

From flow-lang/StandardLibrary/BuiltInDocs.cs (Phase 31):
- public static bool TryGet(string identifier, out BuiltInDocEntry entry)
- BuiltInDocEntry: { string Name, string Signature, string Description, string? Example }

From flow-lang/StandardLibrary/VisualizationFunctions.cs (~150 LOC):
- Register(InternalFunctionRegistry registry) at lines 19-29 — currently registers `visualize(Sequence)` + `visualize(Buffer)` overloads
- Note-placement loop at lines 117-131 (modify per RESEARCH §N lines 1258-1300)
- Bottom separator pattern at lines 166-177 (reuse shape for the new tick-mark row above)

From flow-lang/Runtime/Value.cs:
- Sequence value carries IReadOnlyList<MusicalNoteData> where each note has an Articulation enum (Phase 28 — Accent/Staccato/Marcato/Tenuto/Sforzando/Legato/Normal)

From PrettyPrompt 4.1.1 (new NuGet dep):
- public sealed class Prompt(string promptText, PromptConfiguration config, IConsole? console = null)
- IPromptCallbacks interface: GetCompletionItemsAsync(text, caret, ...) -> Task<IReadOnlyList<CompletionItem>>
- IPromptCallbacks.DetermineKeyPressBehaviorAsync(...) -> Task<KeyPressBehavior>  (use existing IsInputComplete to drive multi-line continuation)
- HistorySearch via Ctrl+R built-in
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PrettyPrompt + flow-lsp ProjectReference + write 7 Wave 0 failing tests</name>
  <files>flow-interpreter/flow-interpreter.csproj, flow-lang.Tests/Integration/Phase38/ReplCompletionTests.cs, flow-lang.Tests/Integration/Phase38/ReplHelpMetaCommandTests.cs, flow-lang.Tests/Integration/Phase38/ReplMultiLineTests.cs, flow-lang.Tests/Integration/Phase38/ReplHistorySearchTests.cs, flow-lang.Tests/Integration/Phase38/VisualizeArticulationGlyphTests.cs, flow-lang.Tests/Integration/Phase38/InspectAliasTests.cs, flow-lang.Tests/Integration/Phase38/GlyphCollisionTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§G lines 854-930 in-process LSP embed + SIMPLIFICATION; §H lines 931-956 PrettyPrompt selection + license verification; §N lines 1237-1314 visualize extension)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (Repl.cs extension lines 374-405; VisualizationFunctions extension lines 728-811; flow-interpreter.csproj lines 934-946)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (REPL keybinding lines 238-256; :help fn format lines 259-291; glyph inventory + composition rules lines 183-228; history file spec lines 295-302)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-09, D-38-10, D-38-11, D-38-12)
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 54-60 REPL test mappings)
    flow-interpreter/flow-interpreter.csproj (current ProjectReference list)
    flow-lsp/Handlers/CompletionHandler.cs (lines 95-144 BuildItems static + 4-index parameter shape)
  </read_first>
  <behavior>
    PRE-PLAN package legitimacy gate per RESEARCH lines 168-181: PrettyPrompt 4.1.1 verified on NuGet flatcontainer API 2026-05-23 — MPL-2.0, last published Sept 30 2023, 104.4K downloads, 199 stars, source github.com/waf/PrettyPrompt. Approved.

    - ReplCompletionTests.TabAtPartialIdentifier_ReturnsBuildItemsResults: instantiate FlowPromptCallbacks (new class from Task 2); call GetCompletionItemsAsync with text `"(transp"` at caret position 7; assert result contains a CompletionItem labeled `"transpose"` (from BuiltInDocs).
    - ReplCompletionTests.TabOnEmptyInput_ReturnsKeywordAndBuiltInsList: call GetCompletionItemsAsync with text `""` at caret 0; assert result is non-empty and includes both keywords (e.g., `"proc"`, `"tempo"`) and builtins (e.g., `"play"`, `"renderSong"`).
    - ReplHelpMetaCommandTests.HelpWithName_PrintsHeaderSignatureBodyExample: capture stdout while calling Repl.HandleCommand(":help transpose"); assert the output contains "transpose" (header), "(transpose Sequence" (signature), and the body text from BuiltInDocs; assert at least 3 distinct sections separated by blank lines.
    - ReplHelpMetaCommandTests.HelpWithUnknownName_PrintsAdvisory: capture stdout/stderr while calling :help fooBar; assert it contains `[help] no documentation entry for 'fooBar' — try ':help' for the meta-command list` (yellow per UI-SPEC line 290; advisory text is the locked wording).
    - ReplMultiLineTests.UnbalancedParens_RequestsContinuation: call the existing IsInputComplete check (or its PrettyPrompt-wrapped equivalent) with input `"(add 1"`; assert the result indicates "incomplete — request continuation".
    - ReplMultiLineTests.BackslashContinuation_RequestsContinuation: call IsInputComplete with input `"Int x = 5 \\"`; assert the result indicates continuation.
    - ReplHistorySearchTests.LoadHistoryFromConfig_ReturnsEntries: write a fixture file at a temp `~/.config/flow/history` path with 3 entries; instantiate the REPL line editor pointed at that path; call its `LoadHistory()` method; assert all 3 entries returned in most-recent-first order. (Skip the actual Ctrl+R interactive flow; that is the PrettyPrompt-internal HistorySearchBackward — covered by manual smoke in 38-VALIDATION manual-only table.)
    - ReplHistorySearchTests.HistoryFile_OnLinuxMacOS_Has0600Permissions: on platforms where File.SetUnixFileMode is available, write the history file then assert its mode is 0600.
    - VisualizeArticulationGlyphTests.AccentNote_RendersAngleBracketAtOnset: build a Sequence containing one MusicalNoteData with Articulation.Accent; call (visualize seq) and capture the rendered string; assert the onset cell contains `>` followed by `#` sustain cells (per UI-SPEC line 210 example).
    - VisualizeArticulationGlyphTests.AllSixArticulations_RenderCorrectGlyphs: parameterized [Theory] with the 6 Phase 28 articulations + Normal; assert glyph mapping per UI-SPEC Glyph Inventory table lines 189-201 (Accent=>, Staccato=., Marcato=^, Tenuto=_, Sforzando=!, Legato=~, Normal=#).
    - VisualizeArticulationGlyphTests.SingleCellStaccato_RendersDotOnly: 1-column duration note with Staccato; assert the cell is `.` alone with no trailing `#`.
    - InspectAliasTests.InspectAndVisualize_ProduceIdenticalOutput: build a non-trivial Sequence; call both `(inspect seq)` and `(visualize seq)`; assert outputs are byte-identical.
    - GlyphCollisionTests.BarLineWinsOverSustainHash: place a note onset that crosses a bar boundary; assert the bar-line column shows `|` not `#` per UI-SPEC line 214.
    - GlyphCollisionTests.OnsetGlyphWinsOverSustainHash: place an accented note at startCol; assert the onset cell shows `>` not `#` (the onset is the same cell as the sustain start).
  </behavior>
  <action>
    Edit `flow-interpreter/flow-interpreter.csproj`: add `<ProjectReference Include="..\flow-lsp\flow-lsp.csproj" />` in the existing `<ItemGroup>` (PATTERNS line 942-944). Add a new `<ItemGroup>` with `<PackageReference Include="PrettyPrompt" Version="4.1.1" />` per PATTERNS line 944 + RESEARCH §H selection.

    Run `dotnet add flow-interpreter package PrettyPrompt --version 4.1.1` if the manual XML edit needs verification; equivalently a plain XML edit is fine. Run `dotnet restore` to fetch the package.

    Create the 7 test files under `flow-lang.Tests/Integration/Phase38/`. Use `[Collection("FlowScripts")]` + IDisposable + ResetForTesting on every class. The completion + multi-line tests will compile-fail initially (ReplLineEditor / FlowPromptCallbacks don't exist yet). The help/visualize/inspect/collision tests can use the existing public APIs (BuiltInDocs.TryGet + InternalFunctionRegistry + Sequence factories) but reference the NEW `(inspect seq)` builtin which won't exist yet — compile-fail expected for those too.

    Test fixture for ReplHistorySearchTests: place `flow-lang.Tests/Integration/Phase38/TestFixtures/historyfile.example` with three sample lines.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet restore flow-interpreter &amp;&amp; dotnet build flow-lang.Tests 2>&amp;1 | grep -E "(error CS|Build succeeded|PrettyPrompt)" | head -10</automated>
  </verify>
  <done>flow-interpreter.csproj contains the new ProjectReference + PackageReference. `dotnet restore flow-interpreter` succeeds (PrettyPrompt 4.1.1 + flow-lsp linked). The 7 test files compile-fail with references to undefined symbols (ReplLineEditor / FlowPromptCallbacks / inspect builtin) — that is the correct RED state for Tasks 2-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ReplLineEditor (PrettyPrompt wrapper + FlowPromptCallbacks calling CompletionHandler.BuildItems) + extend Repl.cs with :help meta-command</name>
  <files>flow-interpreter/ReplLineEditor.cs, flow-interpreter/Repl.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§G lines 854-930 in-process LSP embed + Code Examples lines 1501-1535 BuildItems wrap; §H selection + IPromptCallbacks contract)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (Repl.cs extension lines 374-405; ReplLineEditor/FlowPromptCallbacks pattern lines 19-21 NEW; Shared Pattern S2 for advisory dedup)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (REPL keybinding lines 238-256; :help fn format lines 259-291; history file spec lines 295-302; copywriting tone line 368)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-09 :help fn meta-command; D-38-11 readline gate; D-38-12 in-process LSP simplification)
    flow-interpreter/Repl.cs (entire file 272 lines — preserve Ctrl+C handler at 31-37 + ReadCompleteInput at 105-147 + IsInputComplete at 182-208 + meta-command dispatch at 210-220)
    flow-lsp/Handlers/CompletionHandler.cs (BuildItems static lines 95-144 + the 4-index parameter contract)
    flow-lsp/Handlers/HoverHandler.cs (lines 46-65 — existing BuiltInDocs.TryGet consumer pattern; :help fn mirrors this shape)
    flow-lang/StandardLibrary/BuiltInDocs.cs (TryGet + BuiltInDocEntry shape)
  </read_first>
  <behavior>
    - ReplLineEditor wraps PrettyPrompt.Prompt with a FlowPromptCallbacks instance. Ctor signature: `ReplLineEditor(string promptText = "> ", string continuationPrompt = "... ", string? historyFilePath = null)`. historyFilePath defaults to `Path.Combine(Environment.GetFolderPath(SpecialFolder.UserProfile), ".config", "flow", "history")` per UI-SPEC line 297.
    - History file: 10k entry cap with rotation-on-append per UI-SPEC line 299; mode 0600 on Linux/macOS via `File.SetUnixFileMode` per UI-SPEC line 300.
    - FlowPromptCallbacks (inner class or sibling) overrides `IPromptCallbacks.GetCompletionItemsAsync(text, caret, ...)`: lex the text via existing `SimpleLexer`; parse via existing `ParseSession.Parse` (or whatever the project's parser entry is); call `CompletionHandler.BuildItems(uri: "repl:line", text, ast, tokens, cursor, 4 indices)`. Instantiate the 4 indices (BuiltInIndex / StdlibSymbolIndex / KeywordIndex / UserSymbolIndex) ONCE at REPL ctor time per RESEARCH §G line 871-901 (cold-load timing argument).
    - FlowPromptCallbacks.DetermineKeyPressBehaviorAsync: reuse the existing Repl.IsInputComplete logic to drive Enter-on-incomplete-input → continuation prompt; Shift+Enter → soft newline (PrettyPrompt default); backslash-at-EOL continuation preserved per existing Repl.cs:117-119.
    - Ctrl+C preserved: route through PrettyPrompt's cancellation hook to call the existing `StopAudio()` helper without exiting the REPL.
    - Repl.cs extension: add a `:help <name>` arm in the `HandleCommand` switch at line 210-220 — parse the arg after `:help`; if empty, fall through to existing `:help` branch (ShowHelp); else call `BuiltInDocs.TryGet(name)` and render per UI-SPEC §":help fn Meta-Command" format lines 263-280:
      - header: `\x1b[1m\x1b[32m{name}\x1b[0m` (bold + green)
      - 2 blank rows
      - signature lines: `  \x1b[2m{signature}\x1b[0m` (dim, indented 2 spaces)
      - blank row
      - body: indented 2 spaces, default attribute
      - blank row
      - `  Example:` (dim) + indented 2 spaces example line
    - Unknown name yields the locked yellow advisory `[help] no documentation entry for '{name}' — try ':help' for the meta-command list` per UI-SPEC line 289 (yellow = `\x1b[33m`).
    - Lowercase first word of advisory body + em-dash recovery format per UI-SPEC line 369.
  </behavior>
  <action>
    Create `flow-interpreter/ReplLineEditor.cs` (~250 LOC) per the behaviors above. Public API: ctor, `async Task<string> ReadLineAsync(CancellationToken ct)`, `void AppendHistory(string entry)`, `IReadOnlyList<string> LoadHistory()` (used by ReplHistorySearchTests), `Dispose()`. Inner FlowPromptCallbacks subclass `PrettyPrompt.PromptCallbacks` override implementations for GetCompletionItemsAsync and DetermineKeyPressBehaviorAsync per RESEARCH §G Code Example lines 1501-1535.

    Cache the 4 LSP indices (BuiltInIndex / StdlibSymbolIndex / KeywordIndex / UserSymbolIndex) as private readonly fields on ReplLineEditor; instantiate once per RESEARCH §G line 871 ("instantiate 4 indices once at Repl ctor; query BuildItems on each Tab").

    Edit `flow-interpreter/Repl.cs`:
    1. Replace the `Console.ReadLine`-based loop in the existing `Run()` method (around lines 105-147 + 182-208) with calls to `_lineEditor.ReadLineAsync(ct)` while preserving the surrounding multi-line state machine (the IsInputComplete logic moves INTO FlowPromptCallbacks but stays callable from the existing post-read fallback path for safety).
    2. Extend `HandleCommand` switch at lines 210-220: detect `command.StartsWith(":help ") && command.Length > 6`; split on the first space; look up the name via `BuiltInDocs.TryGet(name, out var entry)`; render per the locked format above using ConsoleColor + ANSI escapes; on miss, write the yellow advisory.
    3. Update the existing `:help` (bare) branch's ShowHelp output to append one extra line `  :help <name>     - Show docs for a builtin (e.g. ':help transpose')` per UI-SPEC line 362.
    4. Construct `_lineEditor = new ReplLineEditor(promptText: "> ", continuationPrompt: "... ")` at REPL Run() entry.
    5. Preserve the Ctrl+C handler at lines 31-37 unchanged — PrettyPrompt's IPromptCallbacks cancellation propagates to it.

    Wire FlowPromptCallbacks.GetCompletionItemsAsync: lex via `SimpleLexer.Tokenize(text)`; parse via existing parser entry returning `Program?` (null on parse failure → BuildItems uses null ast + falls back to token-heuristic per Phase 31 Plan 31-XX precedent); compute Position from caret index + text; call BuildItems and translate the returned `LspCompletionItem` shape to PrettyPrompt's `CompletionItem` shape (label = item.Label, replacement text = item.InsertText, documentation = item.Detail).

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.ReplCompletionTests|FullyQualifiedName~Phase38.ReplHelpMetaCommandTests|FullyQualifiedName~Phase38.ReplMultiLineTests|FullyQualifiedName~Phase38.ReplHistorySearchTests" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <done>ReplCompletionTests + ReplHelpMetaCommandTests + ReplMultiLineTests + ReplHistorySearchTests all GREEN. `grep -c ':help' flow-interpreter/Repl.cs` returns >=2 (existing :help branch + new :help <name> arm). `grep -c 'CompletionHandler.BuildItems' flow-interpreter/ReplLineEditor.cs` returns >=1. `grep -c 'IPromptCallbacks' flow-interpreter/ReplLineEditor.cs` returns >=1. History file is created at `~/.config/flow/history` on first append with mode 0600 (verified by ReplHistorySearchTests on supported platforms).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend VisualizationFunctions with articulation glyphs + tick-mark row + (inspect seq) alias</name>
  <files>flow-lang/StandardLibrary/VisualizationFunctions.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§N lines 1237-1314 visualize extension code sketch)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (VisualizationFunctions extension lines 728-811)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (glyph inventory lines 187-201 LOCKED; glyph composition rules lines 208-214; tick-mark row format lines 218-228; backwards-compat lines 230-234)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-10 visualize extension + (inspect seq) alias + REQUIREMENTS.md REPL-04 wording override)
    flow-lang/StandardLibrary/VisualizationFunctions.cs (entire ~150 LOC — Register at 19-29; note-placement loop at 117-131; bottom separator pattern at 166-177)
    flow-lang/Runtime/Sequence.cs (or wherever Sequence/MusicalNoteData lives — confirm the Articulation field name and the Phase 28 enum values)
  </read_first>
  <behavior>
    - Note-placement loop at lines 117-131 extends per RESEARCH §N lines 1258-1280: at each note, compute `char onsetGlyph = articulation switch { Accent => '>', Staccato => '.', Marcato => '^', Tenuto => '_', Sforzando => '!', _ => '#' }`. Fill the first cell (startCol) with onsetGlyph; subsequent cells with `#` per UI-SPEC line 210.
    - Legato handled separately per UI-SPEC line 212: when a note has Articulation.Legato AND the previous note on the same pitch row ends at the cell immediately before this note's startCol, fill the GAP cell (the empty space between the two notes, on the LATER note's row) with `~`. If no such adjacent note exists, treat Legato as Normal (no glyph) — charitable per D-v1.5-05.
    - Single-cell note collapse per UI-SPEC line 211: when end-start < 1 column, the cell is the onsetGlyph ALONE (no trailing `#`).
    - Tick-mark row per UI-SPEC lines 218-228: rendered ABOVE the first pitch row; format `<label-gutter spaces>+<-+--+--+...>` with `+` at each bar-line column and `-` otherwise; bar numbers (`1`, `2`, ...) sit at the column of each bar's first beat (just below the `+`/`-` rule, OR inline on the same row depending on rendering — UI-SPEC line 222 places them on the row directly below the rule).
    - Bar line wins over sustain per UI-SPEC line 214 (existing behavior unchanged).
    - Onset glyph wins over sustain per UI-SPEC line 213 (already true because they share the same cell).
    - `(inspect seq)` registered as a NEW builtin signature `inspect(Sequence)` calling the SAME Visualize method per PATTERNS line 808 ("Same dispatch"). No separate implementation.
    - Backwards-compat: sequences with NO articulation data (Normal or unset) render with `#` only — existing tests/test_visualize_*.flow regression suite passes unchanged.
  </behavior>
  <action>
    Edit `flow-lang/StandardLibrary/VisualizationFunctions.cs`:
    1. Modify the note-placement loop at lines 117-131 to extract the articulation off the MusicalNoteData / Sequence element being placed (the field name is `Articulation` per CLAUDE.md Phase 28 — confirm via Read of Sequence.cs / MusicalNoteData.cs if uncertain). Apply the onsetGlyph switch per the behaviors above.
    2. Add the Legato gap-fill pass AFTER the main note-placement loop: walk the grid, for each row, find pairs of adjacent-on-row notes where the LATER note carries Articulation.Legato; fill the gap cell with `~` (charitable skip if no gap exists or gap > 1 cell).
    3. Add the tick-mark row rendering BEFORE the first pitch row is emitted to the StringBuilder. Reuse the bottom-separator `+`/`-` pattern at lines 166-177 (which already handles bar-line column detection). Beat numbers row below the rule.
    4. Add the `(inspect seq)` registration in the Register method at lines 19-29: `var sig3 = new FunctionSignature("inspect", [SequenceType.Instance], ParameterNames: ["seq"]); registry.Register("inspect", sig3, Visualize);` per PATTERNS line 808 (Same dispatch — no new method needed).
    5. Optionally add `(inspect buf)` overload via `(visualize buf)` if backwards-compat warrants — UI-SPEC line 234 says `(visualize buf)` is unchanged this phase, so this is optional; skip if it adds non-trivial code.

    Add inline xmldoc comments citing Phase 38 REPL-04 + D-38-10 alias + the Phase 28 articulation enum source. Preserve all existing behavior for sequences with Articulation.Normal.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.VisualizeArticulationGlyphTests|FullyQualifiedName~Phase38.InspectAliasTests|FullyQualifiedName~Phase38.GlyphCollisionTests" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <done>VisualizeArticulationGlyphTests + InspectAliasTests + GlyphCollisionTests all GREEN. `grep -c 'inspect' flow-lang/StandardLibrary/VisualizationFunctions.cs` returns >=1 (Register entry). `grep -c "Articulation.Accent" flow-lang/StandardLibrary/VisualizationFunctions.cs` returns >=1 (switch arm). Existing `tests/test_visualize_*.flow` smoke scripts still run successfully (`for f in tests/test_visualize_*.flow; do dotnet run --project flow-interpreter "$f" || exit 1; done` exits 0; backwards-compat preserved). All 7+ Wave 0 tests for Plan 38-04 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer REPL input -> Parser | Composer-controlled local input (trusted) |
| `~/.config/flow/history` file -> ReplLineEditor | Composer's local file (mode 0600 on Linux/macOS — private to user; UI-SPEC line 300) |
| PrettyPrompt NuGet (MPL-2.0) -> flow-interpreter | First external package added to flow-interpreter project; MPL-2.0 file-scope copyleft is compatible with Flow's MIT distribution per RESEARCH §H line 132 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-SC | Tampering | PrettyPrompt 4.1.1 (MPL-2.0, github.com/waf/PrettyPrompt) supply-chain | mitigate | Package legitimacy gate satisfied at Task 1 plan-start: NuGet flatcontainer API verifies 4.1.1 exists; github.com/waf/PrettyPrompt 199 stars + active maintenance; license confirmed MPL-2.0 per RESEARCH §H line 132; not [SLOP] |
| T-38-13 | Tampering | LSP completion partial-parse with malformed input | mitigate | CompletionHandler.BuildItems is robust to null ast per Phase 31 precedent (token-heuristic fallback); ReplCompletionTests cover both success and failure cases |
| T-38-HIS | Information Disclosure | Composer types secrets into REPL → persisted to ~/.config/flow/history | mitigate | History file mode 0600 on Linux/macOS per UI-SPEC line 300; documented behavior per UI-SPEC line 302 ("composer may type secrets into REPL"); composer responsibility |
| T-38-09 | Denial of Service | Slow parse on every Tab keypress blocks REPL | mitigate | BuildItems takes O(n) on text length; for REPL line lengths <1000 chars completes <5ms; PrettyPrompt's async signature lets the worker run off the input thread per RESEARCH §G Pitfall #9 |
</threat_model>

<verification>
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38" --no-restore` exits 0 — 7 Plan-38-04 tests + 11+ tests from Plans 38-01/02/03 all GREEN.
- `dotnet build flow-interpreter` exits 0; csproj contains PrettyPrompt 4.1.1 + flow-lsp ProjectReference.
- `grep -n 'PrettyPrompt' flow-interpreter/flow-interpreter.csproj` returns the 4.1.1 reference.
- `grep -n 'flow-lsp' flow-interpreter/flow-interpreter.csproj` returns the ProjectReference.
- `grep -nE '(":help"|StartsWith\(":help )' flow-interpreter/Repl.cs` returns both branches.
- Manual REPL smoke (deferred to Plan 38-07): `dotnet run --project flow-interpreter` → type `:help transpose` and verify bold+green header + dim signature + body + Example block render in real terminal.
- Manual visualize smoke (deferred to Plan 38-07): run an existing `tests/test_visualize_*.flow` and confirm the new tick-mark row appears + glyph palette per UI-SPEC.
</verification>

<success_criteria>
- PrettyPrompt 4.1.1 + flow-lsp ProjectReference added (PackageReference + ProjectReference)
- ReplLineEditor.cs (~250 LOC) shipped wrapping PrettyPrompt with FlowPromptCallbacks
- Repl.cs extended with `:help <name>` meta-command branch using BuiltInDocs.TryGet
- VisualizationFunctions extended with articulation glyph switch + Legato gap-fill + tick-mark row + (inspect seq) alias
- 7 Wave 0 xUnit tests GREEN
- Existing tests/test_visualize_*.flow + tests/test_repl_autoimport.flow continue to PASS (additive change only)
- Ctrl+C preserved → stops audio without exiting REPL
- `~/.config/flow/history` 10k-cap + 0600 mode on Linux/macOS
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-04-SUMMARY.md` recording: PrettyPrompt version + license, ReplLineEditor LOC, BuildItems wiring citation (RESEARCH §G simplification), :help fn format verification (UI-SPEC alignment), visualize extension LOC + glyph inventory verification, test pass count, package legitimacy gate note.
</output>
