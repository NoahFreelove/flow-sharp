---
phase: 38-live-coding-2-0
plan: 05
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - flow-lang/Audio/PulseAudioCaptureBackend.cs
  - flow-lang/StandardLibrary/Audio/InputFunctions.cs
  - flow-lang/StandardLibrary/BuiltInFunctions.cs
  - flow-lang/audio.flow
  - flow-lang.Tests/Integration/Phase38/MicBufferAttenuationTests.cs
  - flow-lang.Tests/Integration/Phase38/MicBufferResampleTests.cs
  - flow-lang.Tests/Integration/Phase38/TestFixtures/mic_fixture.wav
  - tests/test_audio_in_pipeline.flow
autonomous: true
requirements: [AUDIO-IN-01, AUDIO-IN-02]
must_haves:
  truths:
    - "Composer calls `(micBuffer 4s)` (or `(micBuffer 4.0)` Double overload per CLAUDE.md Music Types Quick Reference) and receives an AudioBuffer captured from the default PulseAudio input device."
    - "Captured buffer is auto-attenuated -20 dB on open per AUDIO-IN-01 spec; one-shot stderr advisory `[audio-in] mic stream attenuated -20 dB on open to prevent feedback` fires once per process per UI-SPEC line 335."
    - "If native rate != 44100 Hz, capture-side linear-interpolation resample to 44100 Hz; one-shot advisory `[audio-in] resampling capture stream from <N> Hz to 44100 Hz (linear interpolation)` per native-rate per process per UI-SPEC line 336."
    - "Resulting AudioBuffer composes with existing `mix` / `play` / `writeWav` / `granular` builtins — `(micBuffer 4s) -> (granular grain=50ms density=20Hz) -> writeWav \"out.wav\"` round-trips cleanly (AUDIO-IN-02)."
  artifacts:
    - path: "flow-lang/Audio/PulseAudioCaptureBackend.cs"
      provides: "Sibling class to PulseAudioSimpleBackend; P/Invoke for PA_STREAM_RECORD = 2 + pa_simple_read; CaptureSamples(int frames, out string? error) -> float[]"
      min_lines: 200
      contains: "PA_STREAM_RECORD"
    - path: "flow-lang/StandardLibrary/Audio/InputFunctions.cs"
      provides: "Register(InternalFunctionRegistry) registering micBuffer(Second) + micBuffer(Double); -20 dB attenuation + 44.1 kHz resample"
      min_lines: 120
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/InputFunctions.cs"
      to: "flow-lang/Audio/PulseAudioCaptureBackend.cs"
      via: "MicBuffer() instantiates PulseAudioCaptureBackend, calls CaptureSamples, applies attenuation + resample"
      pattern: "PulseAudioCaptureBackend"
    - from: "flow-lang/StandardLibrary/BuiltInFunctions.cs"
      to: "InputFunctions.Register"
      via: "RegisterAllImplementations adds Audio.InputFunctions.Register(registry) adjacent to VisualizationFunctions"
      pattern: "InputFunctions\\.Register"
    - from: "flow-lang/audio.flow"
      to: "micBuffer forward decl"
      via: "Two `internal proc micBuffer(...)` declarations for Second + Double overloads"
      pattern: "internal proc micBuffer"
---

<objective>
Ship AUDIO-IN-01 + AUDIO-IN-02: composer-facing `(micBuffer duration)` builtin reading from the default PulseAudio input device. Create new `PulseAudioCaptureBackend.cs` (sibling to existing `PulseAudioSimpleBackend.cs` per RESEARCH §I line 962 "Recommend new sibling") mirroring the existing playback P/Invoke pattern with the direction flag flipped to `PA_STREAM_RECORD = 2` + new `pa_simple_read()` P/Invoke. Create `flow-lang/StandardLibrary/Audio/InputFunctions.cs` registering the builtin with both Second and Double overloads per CLAUDE.md Music Types Quick Reference (Second IsCompatibleWith Double|Float). Apply -20 dB auto-attenuation on open (Pitfall #24 feedback guard). Apply linear-interpolation 44.1 kHz resample (RESEARCH §J ~30 LOC). Register in `BuiltInFunctions.cs` per PATTERNS Pattern S4. Add forward decls in `audio.flow`.

Purpose: AUDIO-IN-01/02 unlock composability with the Phase 37 granular DSP — composer can `(micBuffer 4s) -> (granular ...) -> writeWav` to create granular textures from mic input. This is the headline AUDIO-IN demo (per CONTEXT.md `<canonical_refs>` `examples/live/mic_granular.flow`).

Output: New PulseAudioCaptureBackend.cs (~250 LOC mirror of PulseAudioSimpleBackend.cs:36-156 with direction-swapped P/Invoke); new InputFunctions.cs (~120 LOC); BuiltInFunctions.cs wired; audio.flow forward decls; 2 Wave 0 xUnit tests + 1 smoke test fixture + 1 .flow smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@CLAUDE.md
@flow-lang/Audio/PulseAudioSimpleBackend.cs
@flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs
@flow-lang/StandardLibrary/BuiltInFunctions.cs
@flow-lang/audio.flow
@flow-lang/Runtime/Value.cs
@flow-lang/StandardLibrary/Audio/AudioBuffer.cs

<interfaces>
<!-- Extracted from existing tree -->

From flow-lang/Audio/PulseAudioSimpleBackend.cs (~313 LOC):
- private const int PA_STREAM_PLAYBACK = 1;        // mirror with PA_STREAM_RECORD = 2
- private const int PA_SAMPLE_FLOAT32LE = 5;
- [StructLayout(LayoutKind.Sequential)] private struct pa_sample_spec { int format; uint rate; byte channels; }
- [DllImport("libpulse-simple.so.0")] static IntPtr pa_simple_new(IntPtr server, string name, int dir, IntPtr dev, string streamName, ref pa_sample_spec ss, IntPtr channelMap, IntPtr attr, out int error);
- [DllImport("libpulse-simple.so.0")] static int pa_simple_write(IntPtr s, IntPtr data, nuint bytes, out int error);  // mirror with pa_simple_read for capture
- [DllImport("libpulse-simple.so.0")] static void pa_simple_free(IntPtr s);
- [DllImport("libpulse-simple.so.0")] static IntPtr pa_strerror(int errno);
- Initialize logic at lines 36-78 (Cdecl + sample spec construction)
- Locking idiom `lock (_lock)` at lines 43-77 + 122-129 — PRESERVE in capture sibling

From flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs:
- public static void Register(InternalFunctionRegistry registry, AudioPlaybackManager manager) at lines 22-26 — registration pattern; InputFunctions uses the no-manager overload (capture owns its own backend per RESEARCH §I line 962)

From flow-lang/StandardLibrary/BuiltInFunctions.cs:
- RegisterAllImplementations at lines 35-54 — add `Audio.InputFunctions.Register(registry)` adjacent to VisualizationFunctions.Register at line 49 per PATTERNS line 832

From flow-lang/Runtime/Value.cs:
- public static Value Buffer(AudioBuffer buf) — return-value factory

From flow-lang/StandardLibrary/Audio/AudioBuffer.cs:
- new AudioBuffer(float[] samples, int sampleRate, int channels)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PulseAudioCaptureBackend (sibling P/Invoke class) + commit mic_fixture.wav</name>
  <files>flow-lang/Audio/PulseAudioCaptureBackend.cs, flow-lang.Tests/Integration/Phase38/TestFixtures/mic_fixture.wav</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§I lines 957-1031 PulseAudio capture P/Invoke + CaptureSamples sketch lines 990-1021)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (PulseAudioCaptureBackend analog lines 443-502 with full P/Invoke mirror; locking idiom line 501)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (AUDIO-IN-01 + canonical_refs PulseAudioSimpleBackend.cs scout note line 132)
    flow-lang/Audio/PulseAudioSimpleBackend.cs (entire 313 LOC — the analog file with full playback P/Invoke pattern)
  </read_first>
  <behavior>
    - PulseAudioCaptureBackend in `FlowLang.Audio` namespace; ctor `PulseAudioCaptureBackend(int sampleRate, int channels, string streamName = "capture")`; methods `bool Initialize(out string error)` + `float[]? CaptureSamples(int frames, out string? error)` + `void Dispose()`.
    - P/Invoke surface mirrors PulseAudioSimpleBackend.cs:273-311 byte-for-byte except: `PA_STREAM_RECORD = 2` constant; `pa_simple_read(IntPtr s, IntPtr data, nuint bytes, out int error)` P/Invoke (same Cdecl, libpulse-simple.so.0).
    - Initialize calls `pa_simple_new(IntPtr.Zero, "flow-lang", PA_STREAM_RECORD, IntPtr.Zero, "capture", ref sampleSpec, IntPtr.Zero, IntPtr.Zero, out error)` per PATTERNS lines 486-497.
    - CaptureSamples allocates a managed `float[]` of `frames * channels` floats; pins via `fixed` or marshals via `Marshal.AllocHGlobal`/`Marshal.Copy`; calls `pa_simple_read(_connection, ptr, (nuint)(frames * channels * sizeof(float)), out error)`; copies back; returns the float array. Error path: return null + populate error from `pa_strerror`.
    - All `_connection`-touching code under `lock (_lock)` per PATTERNS line 501 (preserves PulseAudioSimpleBackend.cs:43-77 locking discipline).
    - Charitable failure mode: Initialize false → caller emits Error advisory + returns silent buffer per D-v1.5-05 "live session never dies mid-set" + Pitfall #12.
    - mic_fixture.wav: 1 second 440 Hz sine wave at 48 kHz 16-bit mono per VALIDATION.md line 79 (192 KB binary committed).
  </behavior>
  <action>
    Create `flow-lang/Audio/PulseAudioCaptureBackend.cs` (~250 LOC) per the behaviors. Mirror the structure of `flow-lang/Audio/PulseAudioSimpleBackend.cs` exactly: same using block + namespace; same `private readonly object _lock = new();`; same `IntPtr _connection` + `bool _isInitialized` + `pa_sample_spec _sampleSpec` fields; Initialize body matching lines 36-78 pattern with PA_STREAM_RECORD swapped in + "capture" streamName + Cdecl P/Invoke signatures verbatim; new CaptureSamples method analogous to the existing Play method at lines 80-156 but using pa_simple_read; Dispose calling pa_simple_free under the lock. xmldoc on the class citing Phase 38 AUDIO-IN-01 + the analog + the swapped direction flag per RESEARCH §I line 962.

    Generate mic_fixture.wav: write a small one-off helper (test-only Init Fact OR pre-generate via Python `scipy.io.wavfile`) producing 1s 440Hz sine at 48000Hz 16-bit mono PCM. Commit to `flow-lang.Tests/Integration/Phase38/TestFixtures/mic_fixture.wav`. Add a corresponding `<None Update>` entry in `flow-lang.Tests/flow-lang.Tests.csproj` if needed for CopyToOutputDirectory on test runs.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang 2>&amp;1 | tail -5</automated>
  </verify>
  <done>flow-lang compiles. `grep -c "PA_STREAM_RECORD" flow-lang/Audio/PulseAudioCaptureBackend.cs` returns >=1. `grep -c "pa_simple_read" flow-lang/Audio/PulseAudioCaptureBackend.cs` returns >=2 (P/Invoke decl + call site). `ls -la flow-lang.Tests/Integration/Phase38/TestFixtures/mic_fixture.wav` shows a binary file > 50 KB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create InputFunctions + wire into BuiltInFunctions + audio.flow forward decls + 2 Wave 0 tests</name>
  <files>flow-lang/StandardLibrary/Audio/InputFunctions.cs, flow-lang/StandardLibrary/BuiltInFunctions.cs, flow-lang/audio.flow, flow-lang.Tests/Integration/Phase38/MicBufferAttenuationTests.cs, flow-lang.Tests/Integration/Phase38/MicBufferResampleTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§J lines 1032-1077 linear-interp resample sketch; §I CaptureSamples integration)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (InputFunctions analog lines 410-441; Pattern S4 lines 1151-1164; Pattern S5 two-overload Music Type compat lines 1168-1172)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory catalog lines 335-336 audio-in-attenuate:open + audio-in-resample:<N> dedup keys + body wording)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (AUDIO-IN-01/02 + canonical_refs Articulation glyph mapping is irrelevant here but the InputFunctions ctor is)
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 61-63 attenuation + resample + smoke tests)
    flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs (registration pattern at lines 22-26)
    flow-lang/StandardLibrary/BuiltInFunctions.cs (RegisterAllImplementations at lines 35-54)
    flow-lang/audio.flow (forward decl pattern at lines 6-31)
  </read_first>
  <behavior>
    - InputFunctions.Register(InternalFunctionRegistry registry) registers TWO overloads of `micBuffer`: (Second) and (Double) per CLAUDE.md Music Types Quick Reference (Second IsCompatibleWith Double|Float) + PATTERNS Pattern S5. Both overloads delegate to the same MicBuffer(args, durationSeconds) helper.
    - MicBuffer body per RESEARCH §I lines 990-1021 + PATTERNS lines 426-434:
      1. `RenderingDiagnostics.WarnOnce("audio-in-attenuate:open", "[audio-in] mic stream attenuated -20 dB on open to prevent feedback")` per UI-SPEC line 335.
      2. Construct PulseAudioCaptureBackend(sampleRate: deviceNativeRate, channels: 2). Query device native rate by attempting Initialize at a default rate (44100 first try); if that fails with EFORMAT, retry at common rates (48000, 96000); else fail charitably.
      3. CaptureSamples(int frames = (int)(durationSeconds * deviceNativeRate * channels)); on null result + non-null error → emit Error advisory `[audio-in] capture failed: {error}` + return silent buffer of the requested duration (D-v1.5-05 charitable; live session never dies).
      4. If deviceNativeRate != 44100, call `ResampleLinear(rawSamples, deviceNativeRate, 44100, channels)` per RESEARCH §J ~30 LOC: linear interpolation between adjacent source samples per output index `srcIdxF = outIdx * deviceNativeRate / 44100.0; srcIdx = floor; frac = srcIdxF - srcIdx; out[outIdx] = src[srcIdx] * (1-frac) + src[srcIdx+1] * frac` per channel. Emit `RenderingDiagnostics.WarnOnce($"audio-in-resample:{deviceNativeRate}", "[audio-in] resampling capture stream from {deviceNativeRate} Hz to 44100 Hz (linear interpolation)")` per UI-SPEC line 336.
      5. Apply scalar -20 dB attenuation: multiply every sample by `Math.Pow(10.0, -20.0 / 20.0) = 0.1`.
      6. Wrap into `new AudioBuffer(samples, 44100, channels)` and return `Value.Buffer(buf)`.
    - The `_lastFireTimeMs` and similar gates are NOT needed (no rate limit on micBuffer; one call = one capture session).
    - MicBufferAttenuationTests.MicBuffer_Open_EmitsAttenuationAdvisoryOnce: capture stderr while calling MicBuffer(args, 1.0) via the registered InternalFunctionRegistry path; assert stderr contains `[audio-in] mic stream attenuated -20 dB on open to prevent feedback` exactly once; call again; assert dedup (still exactly one occurrence). The PulseAudio call itself can be mocked or short-circuited via a test seam — instantiate a synthetic backend stub that returns the fixture WAV bytes per RESEARCH §I line 1003 "test seam" recommendation.
    - MicBufferAttenuationTests.MicBuffer_AppliesMinus20dBScalar: feed a synthetic 0.5-amplitude sine through the MicBuffer pipeline via the test seam; assert the returned AudioBuffer's peak sample is approximately 0.05 (= 0.5 * 0.1) within float epsilon.
    - MicBufferResampleTests.MicBuffer_NativeRate48000_ResamplesTo44100PreservingDuration: feed the 1-second 48000Hz fixture; assert the returned AudioBuffer has SampleRate==44100 and Frames==44100 ±1 sample tolerance per VALIDATION.md line 62.
    - MicBufferResampleTests.MicBuffer_NativeRate44100_NoResample: feed 1-second 44100Hz fixture; assert no resample advisory fires; SampleRate==44100, Frames==44100.
  </behavior>
  <action>
    Create `flow-lang/StandardLibrary/Audio/InputFunctions.cs` (~120 LOC) per the behaviors above. Static class in `FlowLang.StandardLibrary.Audio` namespace. Public `static void Register(InternalFunctionRegistry registry)`. Two `FunctionSignature` constructions per PATTERNS Pattern S5: one for `[SecondType.Instance]`, one for `[DoubleType.Instance]`. Both delegate to private static `MicBuffer(IReadOnlyList<Value> args, double durationSeconds)`. ResampleLinear helper as private static method.

    Inject a testability seam — add an internal-visibility `Func<int, int, int, float[]?>` delegate `_captureOverride` (default null = real PulseAudio); if non-null, MicBuffer calls the override instead of instantiating PulseAudioCaptureBackend. Tests set this delegate to return fixture bytes. Mark `InternalsVisibleTo("flow-lang.Tests")` in flow-lang's AssemblyInfo if not already present.

    Edit `flow-lang/StandardLibrary/BuiltInFunctions.cs`: in `RegisterAllImplementations` at lines 35-54, add `Audio.InputFunctions.Register(registry);` adjacent to the existing `VisualizationFunctions.Register(registry);` at line 49 per PATTERNS line 832 + Pattern S4.

    Edit `flow-lang/audio.flow`: add `internal proc micBuffer(Second: duration)` and `internal proc micBuffer(Double: duration)` to the existing `internal proc` forward-decl cluster at lines 6-31 per PATTERNS line 886.

    Create the two test files under `flow-lang.Tests/Integration/Phase38/`. Each carries `[Collection("FlowScripts")]` + IDisposable + ResetForTesting. Use the `_captureOverride` delegate to inject fixture data. Read the fixture WAV via existing `loadWav` builtin OR direct `File.ReadAllBytes` + manual PCM16-to-float32 conversion (or reuse any in-tree WAV-reader helper).

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.MicBufferAttenuationTests|FullyQualifiedName~Phase38.MicBufferResampleTests" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <done>MicBufferAttenuationTests + MicBufferResampleTests GREEN (4+ facts). `grep -c "InputFunctions.Register" flow-lang/StandardLibrary/BuiltInFunctions.cs` returns 1. `grep -c "internal proc micBuffer" flow-lang/audio.flow` returns 2. `grep -c "audio-in-attenuate:open" flow-lang/StandardLibrary/Audio/InputFunctions.cs` returns 1. `grep -c "audio-in-resample" flow-lang/StandardLibrary/Audio/InputFunctions.cs` returns >=1. `grep -c "ResampleLinear" flow-lang/StandardLibrary/Audio/InputFunctions.cs` returns >=2 (declaration + call site).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write test_audio_in_pipeline.flow smoke (micBuffer -> granular -> writeWav)</name>
  <files>tests/test_audio_in_pipeline.flow</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (line 63 smoke test spec)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (canonical_refs examples/live/mic_granular.flow)
    tests/test_live_reload.flow (analog format — Note: header + use blocks + body)
    tests/test_repl_autoimport.flow (analog with PASS sentinel print)
    flow-lang/StandardLibrary/Audio/DSP/ (granular surface from Phase 37 DSP-01)
  </read_first>
  <behavior>
    - `tests/test_audio_in_pipeline.flow` composes (micBuffer 1s) (using the capture-override test seam path — the test runs through the same code path but via a fixture-feeding stub) -> (granular grain=50ms density=20Hz) -> writeWav "/tmp/test_audio_in.wav" -> prints PASS sentinel.
    - The .flow file uses the same test-harness convention as `tests/test_repl_autoimport.flow`: `Note:` header + `use "@std"` + `use "@audio"` + body + `(print "PASS: audio-in pipeline composes with granular + writeWav")` sentinel.
    - Confirms AUDIO-IN-02 composability claim end-to-end through the registered builtins (not unit-level mocking).
    - The smoke test runs via `dotnet run --project flow-interpreter tests/test_audio_in_pipeline.flow`; success = process exit 0 + console output contains the PASS sentinel.
    - Because real PulseAudio capture is NOT available in CI, the test composer-facing path is to set `FlowLang.StandardLibrary.Audio.InputFunctions._captureOverride` via an env var or test-only init proc — OR to add a `--mock-mic` flag to flow-interpreter. The simpler path per VALIDATION.md line 63 "smoke" is: the .flow script lives in tests/ and is run via dotnet, but the underlying micBuffer call uses the test seam injected by a test-only `internal proc __setMicMockFixture(String: path)` builtin that bypasses PulseAudioCaptureBackend and reads the fixture WAV directly. Researcher confirms exact seam at execution time.
  </behavior>
  <action>
    Create `tests/test_audio_in_pipeline.flow` following the `tests/test_repl_autoimport.flow` convention. Body composes the (micBuffer) -> (granular) -> writeWav chain. At the top, optionally call a test-mode mocking proc (`__setMicMockFixture "flow-lang.Tests/Integration/Phase38/TestFixtures/mic_fixture.wav"`) IF the executor introduces that seam in Task 2 (otherwise the .flow runs against the real default input device — which is an acceptable manual-smoke fallback per VALIDATION.md line 99 manual-only AUDIO-IN-01 row).

    If the test seam is not yet in place, mark this .flow file with a header `Note: Requires either real PulseAudio default input device OR test-mode __setMicMockFixture seam — see flow-lang.Tests/Integration/Phase38/MicBuffer*Tests.cs for the seam pattern` so the executor and any future debugger has the trace.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; ls -la tests/test_audio_in_pipeline.flow &amp;&amp; head -10 tests/test_audio_in_pipeline.flow</automated>
  </verify>
  <done>tests/test_audio_in_pipeline.flow exists with PASS sentinel + `(micBuffer)` -> `(granular)` -> `writeWav` chain. Manual smoke (deferred to Plan 38-07 closer): on a machine with real PulseAudio default input device, `dotnet run --project flow-interpreter tests/test_audio_in_pipeline.flow` writes /tmp/test_audio_in.wav and prints the PASS sentinel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PulseAudio system library -> flow-lang.dll | P/Invoke into libpulse-simple.so.0; system-level trust; no remote network surface |
| Microphone capture buffer -> Composer code | Composer receives raw mic data; -20 dB attenuation + capture-side resample are the only transformations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-24 | Information Disclosure | Mic capture without composer awareness | accept | Composer explicitly calls `(micBuffer ...)` — capture is opt-in by API; one-shot stderr advisory documents the attenuation |
| T-38-MIC | Denial of Service | Long-duration `(micBuffer 3600s)` blocks the FlowEngine thread | accept | Composer responsibility per CLAUDE.md "composer ergonomics first — easy cases fast, flexible cases flexible"; 30s CancellationToken from Plan 38-01 covers the LIVE-block path; bare REPL one-shot `(micBuffer)` is composer-controlled |
| T-38-FB | Spoofing | Audio feedback loop on speakers→mic with playback active | mitigate | -20 dB attenuation on open (Pitfall #24) reduces feedback risk; composer can manually attenuate further |
| T-38-PA | Tampering | libpulse-simple.so.0 NOT present on host (macOS/Windows; CI containers) | accept | Charitable Initialize false path returns silent buffer + Error advisory; tests use mock seam |
</threat_model>

<verification>
- `dotnet build flow-lang flow-lang.Tests` exits 0.
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.MicBuffer" --no-restore` exits 0 (4+ facts GREEN).
- `grep -c "PA_STREAM_RECORD" flow-lang/Audio/PulseAudioCaptureBackend.cs` returns 1.
- `grep -c "InputFunctions.Register" flow-lang/StandardLibrary/BuiltInFunctions.cs` returns 1.
- `grep -c "internal proc micBuffer" flow-lang/audio.flow` returns 2.
- `ls -la flow-lang.Tests/Integration/Phase38/TestFixtures/mic_fixture.wav` shows file > 50 KB.
- Smoke (manual, machine-dependent): `dotnet run --project flow-interpreter -e '(micBuffer 2s) -> (writeWav "/tmp/mic.wav")'` writes a valid WAV file on a host with PulseAudio + default input device available.
</verification>

<success_criteria>
- PulseAudioCaptureBackend.cs sibling class shipped with PA_STREAM_RECORD + pa_simple_read P/Invoke
- InputFunctions.cs shipped with micBuffer(Second) + micBuffer(Double) overloads + ResampleLinear + -20 dB scalar
- BuiltInFunctions wires InputFunctions.Register
- audio.flow has both forward decls
- mic_fixture.wav fixture committed at flow-lang.Tests/Integration/Phase38/TestFixtures/
- tests/test_audio_in_pipeline.flow .flow smoke present
- 2 Wave 0 xUnit tests GREEN (4+ facts)
- Charitable failure mode: Initialize false → silent buffer + Error advisory; never throws
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-05-SUMMARY.md` recording: PulseAudioCaptureBackend LOC, P/Invoke surface confirmation (PA_STREAM_RECORD + pa_simple_read), InputFunctions LOC, ResampleLinear citation (RESEARCH §J), test pass count, mic_fixture.wav file size.
</output>
