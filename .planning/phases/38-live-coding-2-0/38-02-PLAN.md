---
phase: 38-live-coding-2-0
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - flow-lang/Ast/Statements/LiveBlockStatement.cs
  - flow-lang/Lexing/TokenType.cs
  - flow-lang/Lexing/SimpleLexer.cs
  - flow-lang/Parsing/Parser.cs
  - flow-lang/Interpreter/Interpreter.cs
  - flow-lang/Runtime/LiveBlockRegistry.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang.Tests/Integration/Phase38/LiveBlockParserTests.cs
  - flow-lang.Tests/Integration/Phase38/MultiLiveBlockTests.cs
  - flow-lang.Tests/Integration/Phase38/LiveBlockDeterminismAdvisoryTests.cs
autonomous: true
requirements: [LIVE-01]
must_haves:
  truths:
    - "Composer writes `live 1bar { ... }` or `live 2bar { ... }` or `live q { ... }` and the parser produces a LiveBlockStatement AST node carrying the quantize expression + body + a stable BlockId (FNV-1a of SourceLocation per RESEARCH §A line 446-457)."
    - "Multiple `live` blocks in one file are tracked independently — each gets its own BlockId; the interpreter's LiveBlockRegistry holds a per-block LiveBlockRegistration; Plan 38-03 will wire the swap consumer."
    - "Entering ANY `live { }` block fires `[live] entering live block at line N — opts OUT of two-run cmp-clean determinism` exactly once per (line, process) via RenderingDiagnostics.WarnOnce with sentinel `live-determinism-optout:<line>` (D-v1.5-07)."
    - "Whole-script swap mode per D-38-01 keeps working unchanged for files with NO `live { }` block — the parser change is purely additive; existing tests/test_live_reload.flow continues to render byte-identical."
  artifacts:
    - path: "flow-lang/Ast/Statements/LiveBlockStatement.cs"
      provides: "Record `LiveBlockStatement(SourceLocation Location, Expression QuantizeValue, IReadOnlyList<Statement> Body, int BlockId, Span? Span = null) : Statement(Location)`"
      contains: "public record LiveBlockStatement"
      min_lines: 20
    - path: "flow-lang/Runtime/LiveBlockRegistry.cs"
      provides: "ConcurrentDictionary<int, LiveBlockRegistration> + Register / Snapshot / Clear APIs; mirrors PrngRegistry shape per Plan 38-03 consumer"
      min_lines: 60
  key_links:
    - from: "flow-lang/Parsing/Parser.cs"
      to: "flow-lang/Ast/Statements/LiveBlockStatement.cs"
      via: "ParseLiveBlockStatement() called from the keyword-block dispatch after `Tuning` branch at ~line 171"
      pattern: "ParseLiveBlockStatement"
    - from: "flow-lang/Interpreter/Interpreter.cs"
      to: "flow-lang/Runtime/LiveBlockRegistry.cs"
      via: "ExecuteLiveBlock pushes a LiveBlockRegistration into _context.LiveBlockRegistry on first eval"
      pattern: "LiveBlockRegistry\\.Register"
    - from: "flow-lang/Interpreter/Interpreter.cs"
      to: "RenderingDiagnostics.WarnOnce"
      via: "ExecuteLiveBlock emits the D-v1.5-07 stderr advisory on every entry"
      pattern: "live-determinism-optout"
---

<objective>
Ship the `live <quantize> { body }` block surface per LIVE-01: lexer keyword + AST record + parser dispatch + interpreter execution branch + runtime `LiveBlockRegistry` so Plan 38-03 can hang state-preservation off it. Multiple `live` blocks per file each get an independent BlockId (FNV-1a of SourceLocation per RESEARCH §A) so Plan 38-01's `_pendingPerBlock` Dict<int, LiveBlockBuffer> can be filled per D-38-02 independent-swap. Every `live { }` block entry emits the D-v1.5-07 stderr advisory exactly once per (line, process).

Purpose: Without this AST node, Plan 38-03 cannot hook voice-pool name-key preservation + PRNG reseed + stale-closure detection to a per-block swap. LIVE-01 is the user-facing composer surface that turns Phase 38 from "modernized watch" into "Sonic Pi `live_loop`-class live coding."

Output: New `LiveBlockStatement` AST record (~22 LOC mirroring `MusicalContextStatement.cs`); new `LiveBlockRegistry.cs` runtime (~60 LOC mirroring `PrngRegistry.cs`); lexer `Live` token + keyword entry; parser `ParseLiveBlockStatement` (`live <quantize-expr> { body }` matching `tempo <bpm> { body }` shape); interpreter `ExecuteLiveBlock` branch; new property on `ExecutionContext` exposing the registry; 3 Wave 0 xUnit tests (`LiveBlockParserTests`, `MultiLiveBlockTests`, `LiveBlockDeterminismAdvisoryTests`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@CLAUDE.md
@flow-lang/Ast/Statements/MusicalContextStatement.cs
@flow-lang/Lexing/TokenType.cs
@flow-lang/Lexing/SimpleLexer.cs
@flow-lang/Parsing/Parser.cs
@flow-lang/Interpreter/Interpreter.cs
@flow-lang/Runtime/PrngRegistry.cs
@flow-lang/Runtime/ExecutionContext.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs

<interfaces>
<!-- Extracted from existing tree -->

From flow-lang/Ast/Statements/MusicalContextStatement.cs (analog file 22 LOC):
- public record MusicalContextStatement(
    SourceLocation Location, MusicalContextType ContextType,
    Expression Value, Expression? Value2,
    IReadOnlyList<Statement> Body, Span? Span = null) : Statement(Location);

From flow-lang/Lexing/TokenType.cs (lines 17-30 musical-context cluster):
- Timesig, Tempo, Swing, Key, ..., VoicePool, SustainPedal, Tuning, Match  (Live is added adjacent)

From flow-lang/Lexing/SimpleLexer.cs (keyword switch lines 875-896):
- "voicePool" => TokenType.VoicePool, "sustainPedal" => TokenType.SustainPedal, "tuning" => TokenType.Tuning, "match" => TokenType.Match
  (add "live" => TokenType.Live in the same cluster)

From flow-lang/Parsing/Parser.cs (musical-context dispatch lines 104-171):
- `if (Match(TokenType.Tempo)) return ParseMusicalContextStatement(MusicalContextType.Tempo);`
- `if (Match(TokenType.Tuning)) return ParseTuningContextStatement();`
- (add `if (Match(TokenType.Live)) return ParseLiveBlockStatement();` after Tuning)

From flow-lang/Interpreter/Interpreter.cs (statement dispatch lines 110-145):
- `case MusicalContextStatement ctx: ExecuteMusicalContext(ctx); break;`
- (add `case LiveBlockStatement live: ExecuteLiveBlock(live); break;`)

From flow-lang/Runtime/PrngRegistry.cs (lines 100-126 — analog for LiveBlockRegistry):
- private readonly ConcurrentDictionary<...> _registry
- public void ResetAtRenderBoundary() { _registry.Clear(); _drawCounts.Clear(); }

From flow-lang/Runtime/ExecutionContext.cs:
- public PrngRegistry PrngRegistry { get; }   // existing — add `public LiveBlockRegistry LiveBlockRegistry { get; }` next to it
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 3 Wave 0 failing tests (parser AST, multi-block independent, determinism advisory)</name>
  <files>flow-lang.Tests/Integration/Phase38/LiveBlockParserTests.cs, flow-lang.Tests/Integration/Phase38/MultiLiveBlockTests.cs, flow-lang.Tests/Integration/Phase38/LiveBlockDeterminismAdvisoryTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 47-49)
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§A lines 624-651)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (LiveBlockStatement excerpt at lines 44-74 + Lexer/Parser/Interpreter pattern lines 78-198)
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-01, D-38-02, D-38-04, D-v1.5-07)
    flow-lang.Tests/Integration/Phase37/GranularSynthesisTests.cs (Phase 37 directory ctor + Dispose pattern)
    flow-lang/Ast/Statements/MusicalContextStatement.cs (analog record shape)
  </read_first>
  <behavior>
    - LiveBlockParserTests.SingleLiveBlock_With1barQuantize_ProducesLiveBlockStatement: parse the source `tempo 120 { live 1bar { Sequence s = | C4q D4q | } }`; walk the AST; assert exactly one `LiveBlockStatement` node exists; its `Body` contains a `VariableDeclaration`; its `QuantizeValue` evaluates to `Bar(1)` or equivalent (NoteValue or Bar literal — both legal per CONTEXT.md). Use `FlowEngine.ParseToProgram(source)` or whatever the existing parser-public entry is (PATTERNS analog ParseSession.Parse).
    - LiveBlockParserTests.LiveBlockWithQuarterNoteQuantize_ProducesLiveBlockStatement: parse `live q { Sequence s = | C4q | }`; assert QuantizeValue resolves to a NoteValue with quarter duration.
    - LiveBlockParserTests.LiveBlockWithoutQuantize_DefaultsTo1bar: parse `live { Sequence s = | C4q | }`; assert QuantizeValue is the canonical "1bar" default per CONTEXT D-38 (use whatever default the parser ships — confirm one bar).
    - LiveBlockParserTests.BlockId_DeterministicAcrossParses: parse the same source twice; assert both LiveBlockStatement instances have IDENTICAL `BlockId` (FNV-1a of SourceLocation per RESEARCH §A line 446-457).
    - MultiLiveBlockTests.TwoLiveBlocksInOneFile_GetDistinctBlockIds: parse `tempo 120 { live 1bar { Sequence a = | C4q | } live 2bar { Sequence b = | E4q | } }`; assert two LiveBlockStatement nodes; assert their BlockIds differ; assert both registered in ExecutionContext.LiveBlockRegistry after Interpreter.Run(); assert `_context.LiveBlockRegistry.Snapshot().Count == 2`.
    - LiveBlockDeterminismAdvisoryTests.EnterLiveBlock_EmitsDV15_07AdvisoryOncePerLine: execute a script containing `live 1bar { (print "hi") }` via FlowEngine.Execute; capture stderr; assert it contains `[live] entering live block at line N — opts OUT of two-run cmp-clean determinism` exactly once; re-execute the same script in the SAME process; assert stderr STILL contains only ONE such advisory line (dedup via WarnOnce sentinel `live-determinism-optout:<line>`).

    All test classes carry `[Collection("FlowScripts")]` decorator + IDisposable ctor/Dispose calling `RenderingDiagnostics.ResetForTesting()` per Phase 37 precedent.
  </behavior>
  <action>
    Create the three xUnit test files under `flow-lang.Tests/Integration/Phase38/`. Each test uses `FlowEngine` (or `ParseSession` per PATTERNS.md) to parse and execute Flow source from string literals. Tests will FAIL initially because `TokenType.Live` does not exist + `LiveBlockStatement` record does not exist + `LiveBlockRegistry` does not exist — that is the correct RED state for TDD.

    Tests assert behavior, not implementation; do NOT inline expected ANSI escape sequences (LiveBlockParserTests work on AST nodes; LiveBlockDeterminismAdvisoryTests inspects captured stderr text).

    No fenced code in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests 2>&amp;1 | grep -E "(error CS|Build succeeded)" | head -10</automated>
  </verify>
  <done>Three test files compile-fail (expected — references to LiveBlockStatement/Live/LiveBlockRegistry symbols that don't yet exist). xUnit can discover the [Fact] methods once Task 2-4 land the production symbols. Verify by reading the test failure messages: they should be compile errors on `LiveBlockStatement` / `TokenType.Live` / `_context.LiveBlockRegistry` references, NOT on test framework setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LiveBlockStatement AST record + Lexer Live token + keyword entry</name>
  <files>flow-lang/Ast/Statements/LiveBlockStatement.cs, flow-lang/Lexing/TokenType.cs, flow-lang/Lexing/SimpleLexer.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (LiveBlockStatement lines 44-74; TokenType lines 78-93; SimpleLexer lines 97-114)
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§A lines 414-457 ParseLiveBlockStatement sketch + BlockId FNV-1a formula)
    flow-lang/Ast/Statements/MusicalContextStatement.cs (entire 22 LOC — the analog file)
    flow-lang/Lexing/TokenType.cs (lines 17-30 musical-context cluster)
    flow-lang/Lexing/SimpleLexer.cs (lines 875-896 keyword switch)
  </read_first>
  <behavior>
    - `LiveBlockStatement` record exists in `FlowLang.Ast.Statements` namespace with the signature `public record LiveBlockStatement(SourceLocation Location, Expression QuantizeValue, IReadOnlyList<Statement> Body, int BlockId, Span? Span = null) : Statement(Location)`.
    - A static helper `public static int ComputeBlockId(SourceLocation loc)` on the same record (or a sibling utility class in the same file) computes FNV-1a 32-bit over the bytes of `loc.FilePath || loc.Line << 16 || loc.Column` per RESEARCH §A line 446-457 — deterministic across runs.
    - `TokenType.Live` enum value exists with trailing comment `// Phase 38 (LIVE-01) — live <quantize> { ... } block (D-38-02)` per PATTERNS line 93.
    - `SimpleLexer` keyword switch has `"live" => TokenType.Live` slotted adjacent to `voicePool`/`sustainPedal`/`tuning` per PATTERNS line 113 + RESEARCH §A line 630.
  </behavior>
  <action>
    Create `flow-lang/Ast/Statements/LiveBlockStatement.cs` (~25 LOC) mirroring `MusicalContextStatement.cs` lines 1-22 structure: `using FlowLang.Core; namespace FlowLang.Ast.Statements;` + xmldoc citing Phase 38 LIVE-01 + the record. The `ComputeBlockId(SourceLocation)` static is on the record per RESEARCH §A line 446-457 — uses FNV-1a 32-bit offset-basis `2166136261u`, prime `16777619u`, hashed over UTF-8 bytes of `$"{loc.FilePath}:{loc.Line}:{loc.Column}"`. Add xmldoc on `BlockId` explaining the determinism contract (same source location across re-renders → same BlockId → registry slot stable).

    Add `Live` to `flow-lang/Lexing/TokenType.cs` adjacent to `Tuning` (after `Tuning,` and before `Match,` per the existing cluster ordering). Trailing comment per PATTERNS line 93.

    Add `"live" => TokenType.Live,` to the keyword switch in `flow-lang/Lexing/SimpleLexer.cs` at lines 875-896 — slot adjacent to `voicePool` / `sustainPedal` / `tuning`.

    No fenced code blocks in this action — directive prose only.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang 2>&amp;1 | grep -E "(error|Build succeeded)" | head -5</automated>
  </verify>
  <done>flow-lang compiles. `grep -c "TokenType.Live" flow-lang/Lexing/SimpleLexer.cs` returns 1. `grep -c "Live" flow-lang/Lexing/TokenType.cs` shows the new enum value. `grep -c "public record LiveBlockStatement" flow-lang/Ast/Statements/LiveBlockStatement.cs` returns 1. `grep -c "ComputeBlockId" flow-lang/Ast/Statements/LiveBlockStatement.cs` returns 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ParseLiveBlockStatement to Parser + ExecuteLiveBlock to Interpreter + LiveBlockRegistry to Runtime</name>
  <files>flow-lang/Parsing/Parser.cs, flow-lang/Interpreter/Interpreter.cs, flow-lang/Runtime/LiveBlockRegistry.cs, flow-lang/Runtime/ExecutionContext.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (Parser pattern lines 117-147; Interpreter pattern lines 151-198; LiveBlockRegistry pattern lines 201-225)
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§A lines 414-457 ParseLiveBlockStatement sketch; §A lines 638-648 LiveBlockRegistration record shape)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory catalog line 333 — live-determinism-optout dedup key + body wording)
    flow-lang/Parsing/Parser.cs (lines 104-171 musical-context dispatch)
    flow-lang/Interpreter/Interpreter.cs (statement dispatch lines 110-145; ExecuteMusicalContext at 147+)
    flow-lang/Runtime/PrngRegistry.cs (entire file — analog for the new registry shape; lines 100-126 reset-at-boundary API)
    flow-lang/Runtime/ExecutionContext.cs (PrngRegistry property — add LiveBlockRegistry sibling property)
    flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce signature)
  </read_first>
  <behavior>
    - `Parser.ParseLiveBlockStatement()` parses `live <quantize-expr-OR-omitted> { body }`. Quantize accepts: (a) an Int literal followed by `bar`/`bars` identifier suffix per RESEARCH §A line 414-457; (b) a NoteValue literal (`q`/`h`/`w`/`e`/`s`); (c) omitted (defaults to `Bar(1)`). Body is `IReadOnlyList<Statement>` parsed by the existing statement-loop-until-RBrace pattern from `ParseMusicalContextStatement` (Parser.cs lines 104-171 analog).
    - The resulting LiveBlockStatement's `BlockId = LiveBlockStatement.ComputeBlockId(location)`.
    - `Interpreter.ExecuteLiveBlock(LiveBlockStatement live)` first calls `RenderingDiagnostics.WarnOnce($"live-determinism-optout:{live.Location.Line}", $"[live] entering live block at line {live.Location.Line} — opts OUT of two-run cmp-clean determinism")` per D-v1.5-07 + UI-SPEC line 333.
    - Then resolves the QuantizeValue to a `double quantizeBeats` (NoteValue → beats from CLAUDE.md Music Types map; Bar → numerator × beats-per-bar from active MusicalContext).
    - Then registers a `LiveBlockRegistration(BlockId, Location, Body, quantizeBeats)` (new record in `flow-lang/Runtime/`) into `_context.LiveBlockRegistry`.
    - Then executes the body once via the existing statement loop (so initial render captures the per-block buffer; Plan 38-01's `_pendingPerBlock` dict will key off the BlockId in Plan 38-03's swap callback).
    - `LiveBlockRegistry.cs` has: ctor (no args); `Register(LiveBlockRegistration reg)` — replaces existing entry at same BlockId; `Snapshot() : IReadOnlyDictionary<int, LiveBlockRegistration>`; `Clear()`. Backed by `ConcurrentDictionary<int, LiveBlockRegistration>`. Mirrors PrngRegistry shape per PATTERNS Pattern S1.
    - `ExecutionContext.LiveBlockRegistry` property exposes the per-context registry (constructed in ctor next to PrngRegistry).
  </behavior>
  <action>
    In `flow-lang/Parsing/Parser.cs` add a `ParseLiveBlockStatement()` method following the `ParseMusicalContextStatement` pattern at lines 104-171. The dispatch line `if (Match(TokenType.Live)) return ParseLiveBlockStatement();` slots AFTER the `Tuning` branch at ~line 171. ParseLiveBlockStatement: capture `location = Current.Location`; if next token is `LBrace`, default quantize to a synthesized `LiteralExpression(BarLiteral(1), location)`; else parse a quantize-expression using either ParseNoteValueLiteral (existing helper) OR Int literal + optional `bar`/`bars` identifier (peek + Match per RESEARCH §A line 425-438); `Expect(LBrace, "expected '{' to open live block")`; loop `body.Add(ParseStatement())` until `RBrace`; consume `RBrace`; compute `blockId = LiveBlockStatement.ComputeBlockId(location)`; return `new LiveBlockStatement(location, quantizeExpr, body, blockId)`.

    In `flow-lang/Interpreter/Interpreter.cs` add the statement-dispatch arm `case LiveBlockStatement live: ExecuteLiveBlock(live); break;` adjacent to `case MusicalContextStatement ctx:`. Implement `private void ExecuteLiveBlock(LiveBlockStatement live)` per the behaviors above: WarnOnce advisory, resolve QuantizeValue (use existing `_evaluator.Evaluate(...)` + `Value` unwrap helpers — Bar+Int → numerator * MusicalContext.BeatsPerBar; NoteValue → existing beats-conversion helper used by `tempoRamp` / `delay`), register LiveBlockRegistration, execute body via the existing statement loop. Push/pop a frame around the body via `_context.PushFrame()/PopFrame()` for scope isolation (matches ExecuteMusicalContext at lines 149-150).

    Create `flow-lang/Runtime/LiveBlockRegistry.cs` (~60 LOC) mirroring `PrngRegistry.cs` shape: `namespace FlowLang.Runtime;` + `public sealed class LiveBlockRegistry` + `private readonly ConcurrentDictionary<int, LiveBlockRegistration> _registry = new();` + `Register/Snapshot/Clear` methods. Define `LiveBlockRegistration(int BlockId, SourceLocation Location, IReadOnlyList<Statement> Body, double QuantizeBeats)` as a record in the same file (per RESEARCH §A line 638-648). xmldoc cites Plan 38-03 swap-consumer + D-38-02 multi-block.

    In `flow-lang/Runtime/ExecutionContext.cs` add `public LiveBlockRegistry LiveBlockRegistry { get; }` property; construct in the ctor adjacent to PrngRegistry instantiation.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.LiveBlockParserTests|FullyQualifiedName~Phase38.MultiLiveBlockTests|FullyQualifiedName~Phase38.LiveBlockDeterminismAdvisoryTests" --no-restore 2>&amp;1 | tail -15</automated>
  </verify>
  <done>All 6+ Task 1 tests GREEN. `grep -c "ParseLiveBlockStatement" flow-lang/Parsing/Parser.cs` returns >=2 (declaration + dispatch). `grep -c "ExecuteLiveBlock" flow-lang/Interpreter/Interpreter.cs` returns >=2 (case + method). `grep -c "live-determinism-optout" flow-lang/Interpreter/Interpreter.cs` returns 1. LiveBlockRegistry.cs exists with ConcurrentDictionary. ExecutionContext exposes LiveBlockRegistry property. Existing tests/test_live_reload.flow (no `live { }` block) continues to run successfully (`dotnet run --project flow-interpreter tests/test_live_reload.flow` exit 0) — additive parser change does not break the no-`live{}` path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer source `.flow` -> Parser | Composer-controlled local code (trusted for v1.5 single-user model) |
| `live { }` body -> Interpreter | Composer-controlled; same trust as outer file scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-AST | Tampering | LiveBlockStatement.BlockId collision (two distinct live blocks happen to FNV-1a-collide to the same int) | accept | FNV-1a 32-bit collision probability on a single source file's source-location set is astronomically low (composer would need >65k distinct live blocks per file); RESEARCH §A line 456 documents the accept |
| T-38-ADV | Information Disclosure | WarnOnce advisory may leak source line number to stderr | accept | Line numbers are already in parse-error advisories shipped pre-Phase 38; no new disclosure surface |
| T-38-REG | Denial of Service | LiveBlockRegistry growing unboundedly across many sessions | accept | Plan 38-03's `Clear()` at swap boundary bounds the dictionary to one entry per live block per file per process; bounded by composer's edit pace, not adversarial input |
</threat_model>

<verification>
- `dotnet build flow-lang flow-lang.Tests` exits 0.
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38" --no-restore` exits 0 — Task 1 + 3 tests GREEN.
- `dotnet run --project flow-interpreter tests/test_live_reload.flow` exits 0 (existing no-`live{}` test path unaffected per D-38-01).
- Smoke: `dotnet run --project flow-interpreter -e 'live 1bar { (print "in live") }'` prints "in live" once and writes to stderr `[live] entering live block at line 1 — opts OUT of two-run cmp-clean determinism`.
</verification>

<success_criteria>
- LiveBlockStatement record exists with ComputeBlockId helper
- TokenType.Live + lexer keyword entry shipped
- Parser dispatches `live` to ParseLiveBlockStatement (defaulting to 1bar when quantize omitted)
- Interpreter ExecuteLiveBlock emits D-v1.5-07 advisory + registers LiveBlockRegistration
- ExecutionContext.LiveBlockRegistry property exposed (LiveBlockRegistry mirrors PrngRegistry shape)
- 3 Wave 0 xUnit tests GREEN
- Pre-existing no-`live{}` test scripts unaffected
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-02-SUMMARY.md` recording: AST record LOC, registry LOC, FNV-1a constants used, file-list touched, test pass count.
</output>
