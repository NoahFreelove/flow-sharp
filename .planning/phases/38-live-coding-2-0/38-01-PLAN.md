---
phase: 38-live-coding-2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-interpreter/LiveReloadManager.cs
  - flow-interpreter/LiveStatusPanel.cs
  - flow-interpreter/flow-interpreter.csproj
  - flow-lang.Tests/Integration/Phase38/WatchDebounceTests.cs
  - flow-lang.Tests/Integration/Phase38/AnsiPanelRenderTests.cs
  - flow-lang.Tests/Integration/Phase38/PanelTtyFallbackTests.cs
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "Running `flow watch <file>` redraws a 4-row ANSI live status panel at the top of the terminal at <=2 Hz, with row 1 = tempo+timesig+bar, row 2 = active live blocks (omitted if zero), row 3 = voices N/M + per-instrument breakdown, row 4 = sticky [live]/[osc]/[audio-in] advisory auto-clearing after 8s."
    - "File saves during `flow watch` debounce at 200ms (not 500ms); rapid back-to-back saves coalesce into a single render."
    - "When stdout is piped / NO_COLOR=1 / TERM=dumb / --no-color flag set, the panel disables ANSI escapes and emits one plain `[watch] tempo=N timesig=N/N bar=N voices=N/M` line per state change."
    - "Per-render wall-clock evaluation is wrapped in a 30s CancellationToken via Task.Run + Wait(timeout) (RESEARCH §E Option A); on timeout the previous buffer keeps playing and `[live] evaluation timed out at 30s — keeping previous version` is published once per source line via RenderingDiagnostics.WarnOnce."
    - "Phase 28 voice pool steal-oldest + 64-sample equal-power crossfade + bar-boundary detection primitives at flow-interpreter/LiveReloadManager.cs:230-268 are preserved byte-identical (composer-visible behavior unchanged for whole-script swap mode per D-38-01)."
  artifacts:
    - path: "flow-interpreter/LiveStatusPanel.cs"
      provides: "ANSI 4-row panel + TTY-fallback renderer with PublishAdvisory(body, level, dedupKey) API"
      min_lines: 200
    - path: "flow-interpreter/LiveReloadManager.cs"
      provides: "Rewritten orchestration: 200ms debounce + LiveStatusPanel field + 30s CancellationToken wrap + per-block pending-buffer Dict<int, LiveBlockBuffer> (replaces single _pendingBuffer field at line 23)"
      contains: "LiveStatusPanel"
    - path: "flow-lang.Tests/Integration/Phase38/WatchDebounceTests.cs"
      provides: "Asserts 200ms coalescing"
    - path: "flow-lang.Tests/Integration/Phase38/AnsiPanelRenderTests.cs"
      provides: "Asserts 4-row layout via stdout redirect + ANSI sequence inspection"
    - path: "flow-lang.Tests/Integration/Phase38/PanelTtyFallbackTests.cs"
      provides: "Asserts plain-line mode under Console.IsOutputRedirected"
  key_links:
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "flow-interpreter/LiveStatusPanel.cs"
      via: "private LiveStatusPanel _panel field constructed at Run() entry; PublishAdvisory replaces inline Console.ForegroundColor calls at lines 60-64, 185-187, 209-211"
      pattern: "_panel\\.PublishAdvisory\\("
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "30s timeout wrap"
      via: "Task.Run(() => engine.Execute(...)) + .Wait(TimeSpan.FromSeconds(30))"
      pattern: "\\.Wait\\(TimeSpan\\.FromSeconds\\(30\\)\\)"
---

<objective>
Rewrite `flow-interpreter/LiveReloadManager.cs` orchestration to ship the modernized `flow watch`: ANSI live status panel (4-row D-38-08), 200ms debounce (D-38-05 / Pitfall #21), 30s CancellationToken wall-clock cap (D-38-07 / RESEARCH §E Option A), and per-block pending-buffer dictionary keyed by `BlockId` (D-38-02 prep for Plan 38-02). Preserve the bar-boundary detection (`CheckBarBoundary` lines 230-245), 64-sample equal-power crossfade (`ApplyCrossfade` lines 251-268), and capture-mode FlowEngine render primitive (`RenderScript` lines 328-370) byte-identical (D-38-06).

Purpose: Plan 38-02 and Plan 38-03 hang `live { }` block AST + state-preservation off this scaffolding; without the modernized orchestration in place, those plans cannot land. The ANSI panel + structured advisory surface is the user-facing modernization promised by LIVE-02.

Output: Modernized `LiveReloadManager`; new `LiveStatusPanel.cs` (~250 LOC); 3 Wave 0 xUnit tests (`WatchDebounceTests` + `AnsiPanelRenderTests` + `PanelTtyFallbackTests`) under new `flow-lang.Tests/Integration/Phase38/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-coding-2-0/38-CONTEXT.md
@.planning/phases/38-live-coding-2-0/38-RESEARCH.md
@.planning/phases/38-live-coding-2-0/38-PATTERNS.md
@.planning/phases/38-live-coding-2-0/38-UI-SPEC.md
@.planning/phases/38-live-coding-2-0/38-VALIDATION.md
@CLAUDE.md
@flow-interpreter/LiveReloadManager.cs
@flow-interpreter/flow-interpreter.csproj
@flow-lang/Core/FlowEngine.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs
@flow-lang.Tests/Integration/Phase37/GranularSynthesisTests.cs

<interfaces>
<!-- Key types the executor consumes — extracted from existing tree -->

From flow-interpreter/LiveReloadManager.cs (current state):
- private const int CrossfadeSamples = 64;            // PRESERVE byte-identical
- private const int ChunkSamples = 1024;               // PRESERVE
- private DateTime _lastChangeTime;                    // 500ms debounce at line 277 — CHANGE to 200ms
- private byte[]? _pendingBuffer;                      // single field at line 23 — REPLACE with Dictionary<int, LiveBlockBuffer>
- private (bool, int) CheckBarBoundary(int)            // lines 230-245 — PRESERVE
- private static void ApplyCrossfade(...)              // lines 251-268 — PRESERVE byte-identical
- private (byte[]?, string?) RenderScript(...)         // lines 328-370 — extend signature with `out Dictionary<int, LiveBlockBuffer>? perBlockBuffers` per RESEARCH §F line 500
- Console.ForegroundColor = ConsoleColor.{Cyan,Green,Red}  // lines 60-64, 185-187, 209-211 — REPLACE with _panel.PublishAdvisory(...)

From flow-lang/Core/FlowEngine.cs:
- public bool Execute(string source, string? fileName = null)  // NO CancellationToken parameter — use RESEARCH §E Option A (Task.Run + Wait(timeout); cooperative orphan-worker leak documented in code comment)

From flow-lang/Diagnostics/RenderingDiagnostics.cs:
- public static void WarnOnce(string sentinelKey, string message)
- public static void ResetForTesting()                 // call in test ctor/Dispose

From System.Console (standard .NET):
- bool Console.IsOutputRedirected                      // TTY detection
- Environment.GetEnvironmentVariable("NO_COLOR")
- Environment.GetEnvironmentVariable("TERM") == "dumb"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase38 test directory + 3 Wave 0 xUnit scaffolds (debounce, panel render, TTY fallback)</name>
  <files>flow-lang.Tests/Integration/Phase38/WatchDebounceTests.cs, flow-lang.Tests/Integration/Phase38/AnsiPanelRenderTests.cs, flow-lang.Tests/Integration/Phase38/PanelTtyFallbackTests.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (rows 122-180 ANSI Live Status Panel; rows 174-180 TTY-Detection Fallback locked)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (Shared Pattern S2 WarnOnce + Pattern S1 reference identity)
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§E lines 786-800 Task.Run+Wait Option A; §F lines 804-826 ANSI panel + TTY fallback)
    .planning/phases/38-live-coding-2-0/38-VALIDATION.md (lines 44-46 first three Phase 38 test mappings)
    flow-lang.Tests/Integration/Phase37/GranularSynthesisTests.cs (lines 1-45 — Phase 37 directory pattern + [Collection("FlowScripts")] decorator + IDisposable ctor/Dispose with RenderingDiagnostics.ResetForTesting() and FlowConfig.Reset())
  </read_first>
  <behavior>
    - WatchDebounceTests.RapidSaves_Within200ms_CoalesceIntoOneRender: simulate two FileSystemWatcher Changed events 50ms apart; assert that the render-trigger callback is invoked exactly once (not twice). Mock the render trigger as an Action<> counter so the test does not exercise FlowEngine.
    - WatchDebounceTests.RapidSaves_220msApart_TriggerTwoRenders: simulate two Changed events 220ms apart; assert render-trigger counter == 2 (above the 200ms threshold).
    - AnsiPanelRenderTests.Render_With2LiveBlocks_Emits4RowsWithBarTempoVoicesAdvisory: instantiate LiveStatusPanel against a redirected `TextWriter` (StringBuilder-backed), call PublishState(tempo=120, timesig=4/4, bar=47, blocks=[2 blocks], voices=8/32, instruments=piano:3 brass:2 strings:3); assert the captured output contains row 1 substring "Tempo: 120 BPM | TimeSig: 4/4 | Bar: 47", row 2 contains "live 1bar @ L12", row 3 contains "Voices: 8/32 | piano:3", row 4 starts empty until first PublishAdvisory.
    - AnsiPanelRenderTests.Render_With0LiveBlocks_OmitsRow2_Renders3Rows: same as above but blocks=[]; assert output has 3 rows not 4 (row 2 omitted per UI-SPEC line 145).
    - PanelTtyFallbackTests.WhenOutputRedirected_EmitsPlainLineNoAnsi: set `Console.IsOutputRedirected` via testable seam (panel accepts a `bool forceTtyMode = false` ctor arg for tests); assert the captured output contains NO `\x1b[` escape sequences and contains `[watch] tempo=120 timesig=4/4 bar=47 voices=8/32` exactly once per state change.
    - PanelTtyFallbackTests.WhenNoColorEnvSet_DisablesAnsiEscapes: set NO_COLOR=1 via env (use IDisposable env scope helper); assert output has no `\x1b[` escapes.
    All test classes carry `[Collection("FlowScripts")]` + IDisposable ctor/Dispose calling `RenderingDiagnostics.ResetForTesting()` per Phase 37 GranularSynthesisTests precedent.
  </behavior>
  <action>
    Create `flow-lang.Tests/Integration/Phase38/` directory (matches Phase 37/39 layout; verified missing per PATTERNS.md Critical Audit line 1224). Write three xUnit test files with the failing behaviors above. Mark `[Collection("FlowScripts")]` decorator on each class. Implement `IDisposable` ctor calling `RenderingDiagnostics.ResetForTesting()` + `FlowConfig.Reset()` per Phase 37 GranularSynthesisTests:1-39 precedent. Each test uses xUnit `[Fact]` (or `[Theory]` for parameterized cases). Tests will FAIL initially (LiveStatusPanel does not yet exist + LiveReloadManager debounce is still 500ms) — that is correct RED state.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests 2>&amp;1 | grep -E "(error|Build succeeded)" | head -5</automated>
  </verify>
  <done>Three test files compile; xUnit discovers all named test methods; running them returns RED (panel + debounce not yet rewritten — expected before Task 2/3 land).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LiveStatusPanel.cs (ANSI 4-row + TTY fallback + PublishAdvisory API)</name>
  <files>flow-interpreter/LiveStatusPanel.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (rows 122-180 panel spec; rows 112-118 color-disable detection block; rows 306-349 advisory catalog + dedup keys)
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§F lines 804-852 panel rendering + ANSI escape table)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (§"flow-interpreter/LiveStatusPanel.cs" lines 328-369; Shared Pattern S2 WarnOnce dedup)
    flow-interpreter/LiveReloadManager.cs (lines 60-64, 185-187, 209-211 — existing Console.ForegroundColor usage to replace)
    flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce signature)
  </read_first>
  <action>
    Create `flow-interpreter/LiveStatusPanel.cs` as a new top-level class in the `FlowInterpreter` namespace. Public API surface: ctor `LiveStatusPanel(TextWriter? out = null, bool forceTtyMode = false)` (test seam); `PublishState(double tempo, (int, int) timesig, int bar, IReadOnlyList&lt;LiveBlockDisplay&gt; blocks, int activeVoices, int poolSize, IReadOnlyDictionary&lt;string,int&gt; perInstrumentCount)`; `PublishAdvisory(string body, AdvisoryLevel level, string? dedupKey = null)`; `Dispose()` releasing the 2 Hz heartbeat Timer.

    Define enum `AdvisoryLevel { Info, Success, Warning, Error }` mapping to UI-SPEC §Color: Info=default, Success=green (`\x1b[32m`), Warning=yellow (`\x1b[33m`), Error=red (`\x1b[31m`).

    Color-disable detection block (copy verbatim per UI-SPEC line 113-118):
    `isColorEnabled = string.IsNullOrEmpty(Environment.GetEnvironmentVariable("NO_COLOR")) && !cliArgs.Contains("--no-color") && !Console.IsOutputRedirected && Environment.GetEnvironmentVariable("TERM") != "dumb"` — gated by `forceTtyMode` test seam.

    Use a `System.Threading.Timer` with 500ms period (2 Hz per UI-SPEC line 169 — NEVER on the audio thread per Pitfall #21) for the "ago" timer tick + sticky-advisory clear timer. The timer calls `RedrawPanelIfStateChanged()`; state-comparison short-circuits the redraw when nothing changed (saves cycles).

    ANSI escape table per RESEARCH §F lines 808-826:
    - `\x1b[2K` line clear
    - `\x1b[{N};1H` row N positioning (1-indexed)
    - `\x1b[1m` bold, `\x1b[2m` dim, `\x1b[0m` reset
    - colors per AdvisoryLevel enum above

    Row layout per UI-SPEC §"Panel Layout" lines 127-133:
    - Row 1: `Tempo: <N> BPM | TimeSig: <N>/<N> | Bar: <N>` (labels dim, values default)
    - Row 2: `Live blocks: live <Q> @ L<N> (last swap bar M, Xs ago)` per block (field-gap ` | ` between blocks; OMIT entire row when zero blocks)
    - Row 3: `Voices: N/M | <inst>:<count> <inst>:<count> ...`
    - Row 4: sticky advisory (cleared after 8s OR replaced by newer; yellow for Warning, red for Error)

    "ago" format per UI-SPEC line 148: `<60s` -> `Ns`; `<60m` -> `NmNs`; `>=60m` -> `NhNmNs`.

    Plain-line fallback per UI-SPEC line 174-180: when ANSI disabled, emit `[watch] tempo=<N> timesig=<N>/<N> bar=<N> voices=<N>/<M>` ONCE per state change (track last-emitted state for change detection); advisory lines emit identically as `[<surface>] <body>` in both modes.

    PublishAdvisory calls `RenderingDiagnostics.WarnOnce(dedupKey, body)` when `dedupKey != null` to keep the row-4 sticky and the stderr `[live]`/`[osc]` log in sync per UI-SPEC line 367.

    Implements `IDisposable` releasing the heartbeat Timer.

    No fenced code blocks above — directive prose only per critical_rules.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.AnsiPanelRenderTests|FullyQualifiedName~Phase38.PanelTtyFallbackTests" --no-restore 2>&amp;1 | tail -20</automated>
  </verify>
  <done>AnsiPanelRenderTests and PanelTtyFallbackTests pass (5 facts GREEN); panel file compiles; ANSI escape sequences absent under redirected stdout; 4-row layout under TTY mode; row 2 omitted when blocks list empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite LiveReloadManager orchestration — 200ms debounce + LiveStatusPanel field + 30s CancellationToken wrap + per-block pending dictionary</name>
  <files>flow-interpreter/LiveReloadManager.cs</files>
  <read_first>
    .planning/phases/38-live-coding-2-0/38-CONTEXT.md (D-38-01 whole-script drop-in default; D-38-05 200ms; D-38-06 crossfade preserved; D-38-07 30s cap + revert)
    .planning/phases/38-live-coding-2-0/38-RESEARCH.md (§E lines 776-803 30s CancellationToken Task.Run+Wait Option A; §F lines 854-852 orchestration changes)
    .planning/phases/38-live-coding-2-0/38-PATTERNS.md (§"LiveReloadManager.cs REWRITE" lines 254-326 with Primitives Preserved + Orchestration Rewrite checklist)
    .planning/phases/38-live-coding-2-0/38-UI-SPEC.md (Advisory Catalog lines 322-341)
    flow-interpreter/LiveReloadManager.cs (whole file 389 lines)
    flow-lang/Core/FlowEngine.cs (Execute(source, fileName) signature — no CT parameter; confirms Option A required)
  </read_first>
  <behavior>
    - 200ms debounce: line 277 constant `500` -> `200`. WatchDebounceTests.RapidSaves_Within200ms_CoalesceIntoOneRender passes; RapidSaves_220msApart_TriggerTwoRenders passes.
    - LiveStatusPanel field installed at Run() entry; `_panel = new LiveStatusPanel(cliArgs: args)`; the three existing Console.ForegroundColor blocks at lines 60-64, 185-187, 209-211 replaced with `_panel.PublishAdvisory(...)` calls using the locked advisory wording from UI-SPEC §Advisory Catalog.
    - Successful swap publishes `_panel.PublishAdvisory($"[live] reloaded at bar {barNumber}", AdvisoryLevel.Success, dedupKey: null)`.
    - Write error publishes `_panel.PublishAdvisory($"[live] audio write error: {ex.Message}", AdvisoryLevel.Error, dedupKey: $"live-write:{ex.GetType().Name}")`.
    - Per-block pending-buffer dict replaces `_pendingBuffer` field at line 23: `private Dictionary<int, LiveBlockBuffer>? _pendingPerBlock = null;`. Define new nested record `internal sealed record LiveBlockBuffer(int BlockId, byte[] Bytes, int Length);` adjacent to the class. NOTE: Plan 38-02 will populate this dict from `RenderScript`'s new out param; for this plan the dict is constructed wholesale around the existing single-buffer-shaped data (whole-script swap mode per D-38-01) using a sentinel BlockId of 0 so the streaming loop's swap branch at lines 159-189 can be updated to iterate the dict instead of reading a single field.
    - 30s CancellationToken wrap (RESEARCH §E Option A): the existing `TriggerBackgroundRender` worker that currently calls `RenderScript` synchronously becomes `var workerTask = Task.Run(() => RenderScript(...)); if (!workerTask.Wait(TimeSpan.FromSeconds(30))) { /* timeout — publish [live] advisory + keep previous buffer + leave worker orphaned per RESEARCH §E line 600 code comment */ }`. Cooperative-orphan caveat documented in inline `// RESEARCH §E Option A: workers that exceed 30s leak as orphans — acceptable for v1.5 per D-38-07; Plan 38-XX may revisit if HUMAN-UAT reports worker accumulation` comment.
    - Timeout advisory uses dedup key `$"live-timeout:{currentRenderFilePath}"` per UI-SPEC Advisory Catalog line 330.
    - Preserved BYTE-IDENTICAL: CheckBarBoundary (230-245), ApplyCrossfade (251-268), RenderScript body (328-370). For RenderScript only the SIGNATURE may grow with the new `out Dictionary<int, LiveBlockBuffer>? perBlockBuffers` per RESEARCH §F line 500 — Plan 38-02 fills this in; this plan stubs the new out-param to `null`.
  </behavior>
  <action>
    Edit `flow-interpreter/LiveReloadManager.cs` per the behaviors above. Preserve the CheckBarBoundary / ApplyCrossfade / RenderScript core primitives BYTE-IDENTICAL (CONTEXT D-38-06; touching them invalidates Phase 28/29/33 byte-identical determinism contract for the no-`live{}`-block default path per D-38-01). Add the `LiveBlockBuffer` nested record adjacent to the class. Hook `_panel.PublishState(...)` calls at three points: at Run() entry after FlowEngine ctor (initial state); on every successful swap inside `StreamingLoop` (after the per-block crossfade applies); and at file-watch event reception with the active bar number unchanged (lets row 4 of the panel surface the "save detected" event). The 30s timeout dispatch publishes a Warning-level advisory; preserves the previous buffer in `_currentBuffer` (no swap). Add the cooperative-orphan code comment per RESEARCH §E line 600.

    Update `flow-interpreter/flow-interpreter.csproj` only if the new file requires no new dependencies — it should not (uses only `System.Console`, `System.Threading.Timer`, and the existing `FlowLang.Diagnostics.RenderingDiagnostics`). If the csproj needs a `System.Threading.Channels` reference for the heartbeat timer, that is already implicit in .NET 10.

    Wire the WatchDebounceTests by exposing a small testable seam: introduce an internal `protected virtual void OnRenderTriggered()` method invoked by the debounce gate so the test subclasses can count invocations without booting FlowEngine. The 200ms debounce constant becomes `private const int DebounceMs = 200;` — referenced by both the production gate and the WatchDebounceTests subclass for assertion.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38" --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All 5+ Phase38 tests from Tasks 1+2+3 are GREEN: debounce coalesces saves within 200ms; ANSI panel renders 4 rows / 3 rows (no blocks); plain-line fallback under IsOutputRedirected; 30s CancellationToken wrap dispatches Warning advisory on timeout. Existing whole-script `flow watch foo.flow` (no `live { }` block per D-38-01) continues to produce byte-identical playback for unchanged scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> flow process | FileSystemWatcher Changed events on `.flow` file edits (composer-controlled local file; trusted for v1.5 single-user model) |
| Console stdout/stderr -> terminal emulator | ANSI escape sequences written to terminal (terminal-controlled rendering; trusted) |
| Wall-clock -> 30s eviction | Composer's local clock drives the timeout (no security boundary; just composer-trust) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-21 | Denial of Service | FileSystemWatcher Changed-event flood | mitigate | 200ms debounce gate at LiveReloadManager.TriggerBackgroundRender; rapid saves coalesce into one render (WatchDebounceTests assert) |
| T-38-22 | Denial of Service | Worker-task accumulation if FlowEngine.Execute never returns | accept | RESEARCH §E Option A leaks orphan workers past 30s; documented in code comment + reserved as Plan 38-XX revisit if HUMAN-UAT reports accumulation per D-38-07 |
| T-38-08 | Spoofing | ANSI escape sequence injected into terminal via maliciously-crafted file path | mitigate | PublishAdvisory passes through RenderingDiagnostics.WarnOnce which writes via Console.Error.WriteLine (no ANSI passthrough); panel renders only sanitized state values it computes from MusicalContext |
</threat_model>

<verification>
- `dotnet build flow-lang flow-interpreter flow-lang.Tests` exits 0 (no compile errors).
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38" --no-restore` exits 0 (5+ Wave 0 tests GREEN).
- `grep -n "DebounceMs = 200" flow-interpreter/LiveReloadManager.cs` returns exactly 1 hit.
- `grep -nE "(CheckBarBoundary|ApplyCrossfade|RenderScript)" flow-interpreter/LiveReloadManager.cs` confirms all three primitives still present.
- `grep -c "_panel\.PublishAdvisory" flow-interpreter/LiveReloadManager.cs` returns >=3 (replaces the 3 existing Console.ForegroundColor blocks).
- Manual smoke (deferred to Plan 38-07 closer): `dotnet run --project flow-interpreter -- watch tests/test_live_reload.flow` renders 4-row panel in real terminal; piping the output through `cat` shows plain-line fallback.
</verification>

<success_criteria>
Plan 38-01 complete when:
- LiveStatusPanel.cs exists with the public API + ANSI + plain-line dual mode
- LiveReloadManager debounce constant is 200 (not 500)
- 30s CancellationToken wrap with Task.Run + Wait(TimeSpan) in place
- Per-block pending-buffer Dict<int, LiveBlockBuffer> field replaces single _pendingBuffer
- 3 Wave 0 xUnit test files exist under flow-lang.Tests/Integration/Phase38/ and are GREEN
- Bar-boundary + crossfade + RenderScript primitives preserved byte-identical (CONTEXT D-38-06)
- Console.ForegroundColor blocks replaced with _panel.PublishAdvisory(...) calls
</success_criteria>

<output>
Create `.planning/phases/38-live-coding-2-0/38-01-SUMMARY.md` recording: panel file LOC count, debounce change verified, 30s wrap pattern citation (RESEARCH §E Option A), test pass count.
</output>
