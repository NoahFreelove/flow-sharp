---
phase: 14
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/14-composer-dx-part-1/deferred-items.md
  - .planning/phases/14-composer-dx-part-1/14-VERIFICATION.md
  - .planning/phases/14-composer-dx-part-1/14-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DX-05, DX-06, DX-08]
requirements_addressed: [DX-05, DX-06, DX-08]
tags: [closure, requirements-reframe, verification, validation-promotion]

must_haves:
  truths:
    - "REQUIREMENTS.md DX-06 is reframed: H-alias clause moved to an audit-trail preamble; flat-literal + enharmonic clauses kept and marked Shipped with commit hashes (Phase 12 TEST-03 precedent per CONTEXT D-19)"
    - "REQUIREMENTS.md Traceability table: DX-05, DX-06, DX-08 rows marked `Shipped <hash>` with the atomic-commit hashes from plans 14-01, 14-02, 14-03"
    - "A new file `.planning/phases/14-composer-dx-part-1/deferred-items.md` exists capturing the H-alias requirement, the pragma language construct design sketch, candidate keyword `enable`, and German-notation as the first pragma user (CONTEXT D-12)"
    - "A new file `.planning/phases/14-composer-dx-part-1/14-VERIFICATION.md` exists with: DX-05/DX-06/DX-08 rollup (truths met, artifacts created, key links verified), pre-landing collision grep transcript re-surfaced from 14-02-PLAN.md §Pre-landing Collision Grep, commit hash table, and full-suite Fact count"
    - "14-VALIDATION.md is promoted to `nyquist_compliant: true` — YES per CONTEXT Claude's Discretion last bullet (Phase 13 D-24 precedent; Phase 14 ships code AND has observable-value pins for every requirement)"
    - "STATE.md reflects Phase 14 closed: completed_phases advances from 3 to 4, percent updates, Current Position block updated, Last activity dated today"
    - "ROADMAP.md Phase 14 row in the Progress table is marked Complete with today's date and 4/4 plans"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DX-06 reframe with H-clause as audit-trail; DX-05/06/08 rows in Traceability table marked Shipped with hashes"
      contains: "DX-06"
    - path: ".planning/phases/14-composer-dx-part-1/deferred-items.md"
      provides: "H-alias + pragma system deferral capture"
      contains: "H alias"
    - path: ".planning/phases/14-composer-dx-part-1/14-VERIFICATION.md"
      provides: "Phase 14 verification rollup with collision grep, commit hashes, success-criteria mapping"
      contains: "Pre-landing Collision Grep"
    - path: ".planning/phases/14-composer-dx-part-1/14-VALIDATION.md"
      provides: "Updated frontmatter nyquist_compliant: true + wave_0_complete: true + status: complete + filled Per-Task Verification Map"
      contains: "nyquist_compliant: true"
    - path: ".planning/STATE.md"
      provides: "Phase 14 closed; counters advanced"
      contains: "Phase 14"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 Progress row updated to Complete"
      contains: "14. Composer DX Part 1"
  key_links:
    - from: ".planning/REQUIREMENTS.md (DX-06 row)"
      to: ".planning/phases/14-composer-dx-part-1/deferred-items.md (H-alias + pragma)"
      via: "H-clause audit-trail preamble links forward to deferred-items.md"
      pattern: "deferred-items\\.md"
    - from: ".planning/phases/14-composer-dx-part-1/14-VERIFICATION.md (collision grep)"
      to: ".planning/phases/14-composer-dx-part-1/14-02-PLAN.md (§Pre-landing Collision Grep)"
      via: "Transcript re-surfaced verbatim"
      pattern: "grep -rn"
    - from: ".planning/ROADMAP.md (Phase 14 Progress row)"
      to: ".planning/phases/14-composer-dx-part-1/14-VERIFICATION.md"
      via: "Row marked Complete references verification artifact"
      pattern: "14. Composer DX Part 1"
---

<objective>
Close Phase 14 by shipping all closure artifacts in a single docs-only atomic commit:

1. **REQUIREMENTS.md reframe (CONTEXT D-19):** Preserve the original DX-06 wording as an audit-trail preamble; rewrite the active DX-06 row to cover ONLY the shipped scope (flat-literal family + `enharmonic()`). H-alias clause is moved to the audit-trail preamble with a forward reference to `deferred-items.md`. Mark DX-05, DX-06, DX-08 rows in the Traceability table as `Shipped <hash>` using the atomic-commit hashes from plans 14-01/02/03.

2. **deferred-items.md (CONTEXT D-12):** Create `.planning/phases/14-composer-dx-part-1/deferred-items.md` capturing (a) H-alias requirement in full, (b) pragma / feature-flag language construct design sketch, (c) candidate keyword `enable` (non-binding per CONTEXT D-11), (d) German notation as the first pragma user, (e) design open questions for the future phase (file-scoped vs block-scoped; interaction with `use`; chord/key-block propagation).

3. **14-VERIFICATION.md rollup:** Create `.planning/phases/14-composer-dx-part-1/14-VERIFICATION.md` with ROADMAP Phase 14 success-criteria mapping (each of the 5 criteria → which plan + which test Fact / sentinel pins it), collision grep transcript re-surfaced verbatim from 14-02-PLAN.md, atomic commit hash table (14-01, 14-02A, 14-02B, 14-03 Pass 1, optional 14-03 Pass 2, this plan's commit), full-suite Fact count.

4. **14-VALIDATION.md promotion to nyquist_compliant: true (CONTEXT Claude's Discretion, last bullet):** Update the frontmatter of `.planning/phases/14-composer-dx-part-1/14-VALIDATION.md`:
   - `status: draft` → `status: complete`
   - `nyquist_compliant: false` → `nyquist_compliant: true`
   - `wave_0_complete: false` → `wave_0_complete: true`
   Fill in the Per-Task Verification Map and Status column with ✅ GREEN markers once 14-01/02/03 have landed.

5. **STATE.md closure:** Advance `completed_phases` from 3 → 4, update `percent`, stamp `last_updated` + `last_activity`, rewrite Current Position to reflect Phase 14 done. Append Phase 14 decisions to Accumulated Context per existing pattern (see Phase 13 closure note template).

6. **ROADMAP.md update:** Mark Phase 14 checkbox `[x]`, update the Progress table row for "14. Composer DX Part 1" to `Complete` / `N/N` plans / today's date.

This plan lands in **Wave 3** — strictly AFTER 14-01, 14-02, and 14-03 have all committed (wave structure: 14-01 → 14-02 → 14-04; 14-03 runs in wave 1 alongside 14-01 and has no file overlap with either). CONTEXT D-20 asserted wave-1 parallel for 14-01/02/03 on the assumption of zero file overlap, but plans 14-01 and 14-02 both touch flow-lang/StandardLibrary/BuiltInFunctions.cs (14-01 registers slice near line 373; 14-02 wires HarmonyFunctions.RegisterContextDependent near line 668). Per planner dependency_graph rule, overlapping files_modified force sequential waves — 14-02 bumps to wave 2 depending on 14-01, and this closure plan bumps to wave 3. The plan is docs-only: no code changes, no test changes, no test runs required beyond a final `dotnet test` confirmation that the full suite is still green at phase close.

Purpose: Audit-trail preservation + forward-reference capture. The H-alias is not punted — it is redirected into a pragma system design that the future phase will finalize. The closure makes Phase 14 bisectable and future-auditable.

Output: 6 files touched (5 existing + 1 created). One atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/14-composer-dx-part-1/14-CONTEXT.md
@.planning/phases/14-composer-dx-part-1/14-RESEARCH.md
@.planning/phases/14-composer-dx-part-1/14-VALIDATION.md
@.planning/phases/14-composer-dx-part-1/14-01-SUMMARY.md
@.planning/phases/14-composer-dx-part-1/14-02-SUMMARY.md
@.planning/phases/14-composer-dx-part-1/14-03-SUMMARY.md
@.planning/phases/14-composer-dx-part-1/14-02-PLAN.md
@.planning/phases/12-stability/12-VERIFICATION.md
@.planning/phases/13-nyquist-validation-backfill/13-VERIFICATION.md

<reference_patterns>
## Phase 12 TEST-03 reframe pattern (REQUIREMENTS.md)

Phase 12 Plan 06 reframed TEST-03 by keeping the original audit claim as an introductory preamble and then rewriting the active clause to cover what actually shipped. Format per REQUIREMENTS.md:

```markdown
- [x] **TEST-03** (REFRAMED 2026-04-19): The audit's claim that <original-claim> is empirically wrong — <reframed-reality>. **Shipped <hash-A>** (...) and **Shipped <hash-B>** (...). test_full_song.flow runs to completion...
```

Phase 14 DX-06 follows the same shape: preserve the H-clause as an audit-trail preamble, then rewrite the active DX-06 row to describe what shipped.

## Phase 13 VERIFICATION.md shape

13-VERIFICATION.md (the 13-05 closure output) contains:
- `## Success Criteria Verification` table mapping each ROADMAP criterion to pinning test file
- `## Commit Hash Manifest` listing each plan's commits chronologically
- `## Full-Suite Fact Count` with before/after counts
- `## Deferred Items` when applicable

Phase 14 VERIFICATION.md uses the same shape with an added `## Pre-landing Collision Grep` block re-surfacing 14-02-PLAN.md's captured grep transcript verbatim.
</reference_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md DX-06 reframe + Traceability table hash fills</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current — full file)
    - .planning/phases/14-composer-dx-part-1/14-CONTEXT.md (D-10 H deferred · D-12 deferred-items.md scope · D-19 audit-trail preservation)
    - .planning/phases/14-composer-dx-part-1/14-01-SUMMARY.md (DX-05 commit hash)
    - .planning/phases/14-composer-dx-part-1/14-02-SUMMARY.md (DX-06 commit A + B hashes)
    - .planning/phases/14-composer-dx-part-1/14-03-SUMMARY.md (DX-08 commit hash(es); Pass 1 + optional Pass 2)
    - .planning/phases/12-stability/12-VERIFICATION.md (TEST-03 reframe precedent shape)
  </read_first>

  <action>
    Edit `.planning/REQUIREMENTS.md` making two classes of changes:

    CHANGE A — Reframe DX-06 (currently at line ~49). Replace the existing row:

    ```markdown
    - [ ] **DX-06**: Enharmonic helpers — `Db`, `Eb`, `Gb`, `Ab`, `Bb`, `Cb`, `Fb` flat literals accepted by `NoteType.Parse` and normalized to existing `(letter, octave, alteration)` triples; `H` accepted as `B` alias **only within note-stream context** (`| … |`); `enharmonic(Note) → Note` built-in returns pitch-equivalent spelling. Must NOT break existing identifier `H` as a variable name in ordinary code.
    ```

    With (follows Phase 12 TEST-03 reframe shape):

    ```markdown
    - [x] **DX-06** (REFRAMED 2026-04-2X, replace X with actual day of commit): Original audit-trail wording preserved below; shipped scope covers **extended flat-literal surface** (arbitrary `b`/`#`/`+`/`-` composition with any int net alteration, on either side of octave digits — CONTEXT D-07/D-08/D-09) + **`enharmonic(Note) → Note`** key-context-aware respelling (CONTEXT D-03/D-04/D-05/D-06). The `H` alias clause is **deferred to a future pragma phase** (CONTEXT D-10/D-11/D-12); see `.planning/phases/14-composer-dx-part-1/deferred-items.md` for the H-alias requirement, pragma system design sketch, and candidate `enable` keyword. Chord-vs-note lexer dispatch reordered to keep `Bb7` tokenizing as ChordLiteral under the extended Parse surface (regression gate `LexerTests.Bb7_IsChord`). Pre-landing collision grep empty across `*.flow` files (ROADMAP criterion 5 — transcript in 14-VERIFICATION.md). **Shipped <commit-A-hash>** (flat literals + lexer reorder) + **Shipped <commit-B-hash>** (enharmonic) — Phase 14 Plan 02.

      *Original audit-trail:* Enharmonic helpers — `Db`, `Eb`, `Gb`, `Ab`, `Bb`, `Cb`, `Fb` flat literals accepted by `NoteType.Parse` and normalized to existing `(letter, octave, alteration)` triples; `H` accepted as `B` alias **only within note-stream context** (`| … |`); `enharmonic(Note) → Note` built-in returns pitch-equivalent spelling. Must NOT break existing identifier `H` as a variable name in ordinary code.
    ```

    Replace `<commit-A-hash>` and `<commit-B-hash>` with the actual hashes from 14-02-SUMMARY.md. Replace `2026-04-2X` with the actual commit date.

    CHANGE B — Mark DX-05 and DX-08 rows Shipped. Replace:

    ```markdown
    - [ ] **DX-05**: `slice(Sequence, Int, Int) → Sequence` and `slice(Array[T], Int, Int) → Array[T]`. Start inclusive, end exclusive. Bar-level for Sequence; out-of-range clamps (not throws) to match `take`/`drop`. No new files required.
    ```

    With:

    ```markdown
    - [x] **DX-05** (Shipped <commit-hash>, 2026-04-2X): `slice(Sequence, Int, Int) → Sequence` and `slice(Array[T], Int, Int) → Array[T]`. Start inclusive, end exclusive. Bar-level for Sequence. Silent two-sided clamping matching `take`/`drop` (CONTEXT D-01 — both overloads atomic per D-02). Regression: `flow-lang.Tests/Unit/Phase14/SliceTests.cs` (Array + Sequence Facts) + `tests/test_slice.flow` Theory row. Phase 14 Plan 01.
    ```

    Replace the DX-08 row similarly:

    ```markdown
    - [ ] **DX-08**: MIDI velocity from dynamic transforms preserved end-to-end — `dynamics { }` musical context propagates to `MusicalNoteData.Velocity` at compile time in `NoteStreamCompiler`; `crescendo`/`decrescendo`/`swell` envelope values (already written to `Velocity`) reach `MidiExport.cs:192` and map to MIDI velocity 1–127 without loss. Regression test: write a `.flow` script with `dynamics`, export MIDI, assert velocity bytes.
    ```

    With:

    ```markdown
    - [x] **DX-08** (Shipped <commit-hash>, 2026-04-2X): MIDI velocity from dynamic transforms preserved end-to-end. Regression Fact `DynamicsMidiVelocityTests.Crescendo_EmitsExpectedVelocityGradient` reads MIDI via DryWetMidi 8.0.3 `MidiFile.Read` + `NotesManagingUtilities.GetNotes` and asserts the velocity byte array `[31, 47, 63, 79, 95]` for `crescendo(0.25, 0.75)` over 5 notes (observable-value pin per Phase 13 D-11). Chain verified via two-pass strict authorship (CONTEXT D-13); Pass 2 outcome recorded in 14-03-SUMMARY.md (A: GREEN on first run / B/C: gap-fix with Divergence). Phase 14 Plan 03.
    ```

    CHANGE C — Update the Traceability table at the bottom of REQUIREMENTS.md (around line 90-100):

    Replace:
    ```markdown
    | DX-05 | Phase 14 | Pending |
    | DX-06 | Phase 14 | Pending |
    | DX-08 | Phase 14 | Pending |
    ```

    With:
    ```markdown
    | DX-05 | Phase 14 | Shipped <commit-hash> |
    | DX-06 | Phase 14 | Shipped <commit-A-hash> + <commit-B-hash> (reframed per CONTEXT D-19) |
    | DX-08 | Phase 14 | Shipped <commit-hash> (two-pass strict per CONTEXT D-13) |
    ```

    Replace each `<commit-hash>` placeholder with the actual SHA from the relevant SUMMARY file at the time of editing.

    CHANGE D — Update the "Last updated" line at the very bottom:

    Replace:
    ```markdown
    *Last updated: 2026-04-19 — Phase 13 Nyquist Validation Backfill closed; TEST-04 shipped (Phase 6–10 VALIDATION.md authored + promoted).*
    ```

    With:
    ```markdown
    *Last updated: 2026-04-2X — Phase 14 Composer DX Part 1 closed; DX-05/DX-06/DX-08 shipped. DX-06 H-alias clause deferred to future pragma phase (see 14-04-PLAN.md + deferred-items.md).*
    ```
  </action>

  <verify>
    <automated>grep -c "Shipped" /home/noah/Desktop/projects/flow-sharp/.planning/REQUIREMENTS.md &amp;&amp; grep -n "DX-05\|DX-06\|DX-08" /home/noah/Desktop/projects/flow-sharp/.planning/REQUIREMENTS.md | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` DX-06 row starts with `[x]` (checkbox checked) and contains the text `(REFRAMED` AND `deferred-items.md` AND `Original audit-trail:`.
    - DX-05 row starts with `[x]` and contains `Shipped ` followed by a 7+ character hex hash.
    - DX-08 row starts with `[x]` and contains `Shipped ` followed by a 7+ character hex hash AND the text `[31, 47, 63, 79, 95]`.
    - Traceability table rows for DX-05, DX-06, DX-08 each say `Shipped` (not `Pending`). Grep check: `grep -E "^\\| DX-0[568].*Shipped" .planning/REQUIREMENTS.md` returns 3 lines.
    - The original DX-06 audit-trail wording (containing the phrase `H` accepted as `B` alias) is preserved somewhere in the file (grep: `grep -c "H\\\\` accepted as \\\\`B\\\\` alias" .planning/REQUIREMENTS.md` returns ≥ 1).
    - `*Last updated*` line references Phase 14 Composer DX Part 1 closure.
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md DX-05 / DX-06 / DX-08 reframed and marked Shipped with actual commit hashes. Original DX-06 H-clause preserved as audit-trail. Traceability table updated. Last-updated line advanced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create deferred-items.md + 14-VERIFICATION.md + promote 14-VALIDATION.md + update STATE.md + ROADMAP.md (single atomic commit)</name>
  <files>.planning/phases/14-composer-dx-part-1/deferred-items.md, .planning/phases/14-composer-dx-part-1/14-VERIFICATION.md, .planning/phases/14-composer-dx-part-1/14-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/14-composer-dx-part-1/14-CONTEXT.md (FULL — especially §Deferred Ideas · D-10/D-11/D-12 H-alias + pragma deferral)
    - .planning/phases/14-composer-dx-part-1/14-VALIDATION.md (current frontmatter + Per-Task Verification Map)
    - .planning/phases/14-composer-dx-part-1/14-02-PLAN.md (§Pre-landing Collision Grep — for re-surfacing in VERIFICATION.md)
    - .planning/phases/14-composer-dx-part-1/14-01-SUMMARY.md (DX-05 commit hash)
    - .planning/phases/14-composer-dx-part-1/14-02-SUMMARY.md (DX-06 commit hashes + any Pitfall 3 divergence)
    - .planning/phases/14-composer-dx-part-1/14-03-SUMMARY.md (DX-08 outcome + commit hashes)
    - .planning/phases/13-nyquist-validation-backfill/13-VERIFICATION.md (VERIFICATION format reference)
    - .planning/phases/12-stability/12-VERIFICATION.md (VERIFICATION format reference)
    - .planning/STATE.md (current counters + Accumulated Context block)
    - .planning/ROADMAP.md (Phase 14 rows + Progress table)
  </read_first>

  <action>
    Execute as ONE atomic commit covering all five files below, AFTER Task 1's REQUIREMENTS.md edit has been staged (combine into the same commit — this task finalizes the Phase 14 closure commit).

    PART A — Create `.planning/phases/14-composer-dx-part-1/deferred-items.md`:

    ```markdown
    # Phase 14 — Deferred Items

    **Created:** 2026-04-2X (Phase 14 closure — plan 14-04)
    **Scope:** Items scoped-out of Phase 14 that need a home before the phase ships.

    ---

    ## DEFER-02: `H` = `B` note-stream alias

    **Origin:** Original DX-06 clause per REQUIREMENTS.md (preserved as audit-trail in the reframed DX-06 row).

    **Full requirement** (verbatim from the original audit-trail):
    > `H` accepted as `B` alias **only within note-stream context** (`| … |`). Must NOT break existing identifier `H` as a variable name in ordinary code.

    **Intended family** (beyond the single-letter clause):
    - `H` → `B`
    - `H4`, `H5`, etc. → `B4`, `B5` (with octave)
    - `H+`, `H++` → `B+`, `B++` (with alteration)
    - `H4+`, `Hb`, `H-`, `H#` etc. — consistent with the extended alteration surface shipped in Phase 14

    **Why deferred** (CONTEXT D-10/D-11):
    A global alias pollutes the namespace — `Int H = 5;` today compiles cleanly, and would silently change meaning if `H` were aliased. The user's decision is to redesign around a **pragma / feature-flag language construct** that makes the alias opt-in per file or per block.

    **Dependencies blocking implementation:**
    - DEFER-03 (pragma system) must ship first.

    ---

    ## DEFER-03: Pragma / feature-flag language construct

    **Origin:** CONTEXT D-11 — future-phase redirect to support DEFER-02 (H alias) and other locale-specific grammar variants without polluting the global namespace.

    **Candidate keyword** (non-binding — the future phase finalizes):
    ```
    enable "german-notation"      // file-scoped
    enable "german-notation" { … } // block-scoped
    ```

    **First user:** German-notation addon, enabling the H/B alias family from DEFER-02 within the enabled scope.

    **Design open questions for the future phase** (CONTEXT Deferred Ideas):
    - File-scoped vs block-scoped semantics — or both, layered?
    - Do enable blocks stack (multiple enables active simultaneously)?
    - Interaction with `use` imports — are pragmas imported?
    - Do pragma-aware tokens also propagate into chord literals / key blocks?
    - Pragma registry: hard-coded list vs discoverable/extensible?
    - Does the pragma affect lexer dispatch at token production, or at the parser's token-consumption stage?
    - Error message when a disabled pragma is referenced outside its scope?

    **Why this is the right shape** (CONTEXT §Specifics):
    Niche syntactic variants should be **opt-in**, not global. Pragmas scope the change to users who request it, preserving the default grammar stability that solo-composer users rely on.

    **Candidate future phase location:** Between Phase 17 (Language Server) and any new milestone, OR as part of a v1.3 language-extension milestone.

    ---

    ## DEFER-04: Multi-letter enharmonic-edge respelling

    **Origin:** CONTEXT D-05 — intentional exclusion from Phase 14 scope.

    **Full requirement** (if later demanded):
    > `E ↔ Fb`, `F ↔ E#`, `B ↔ Cb`, `C ↔ B#` enharmonic-edge respelling. Under Phase 14's no-key fallback (D-05), naturals return unchanged. A strict-mode `enharmonic(note, strict: true)` variant or a separate `enharmonicWithEdges()` built-in could carry this without disturbing the baseline.

    **Why deferred:**
    No current user need. Adding edge-respelling by default risks silent respelling where composers expect identity. Deferred until requested explicitly.

    ---

    ## DEFER-05: Shared MIDI-read helper promotion (`Shared/MidiReadHelpers.cs`)

    **Origin:** CONTEXT §Claude's Discretion — Phase 14 plan 14-03 keeps the MIDI-read helper INLINE inside `DynamicsMidiVelocityTests`. Phase 15 DX-09 (euclidean humanize) is likely to need the same `MidiFile.Read` + `GetNotes` + velocity projection pattern for reproducibility assertions.

    **Trigger for promotion:** Second Fact duplicates the inline call shape. Extract to `flow-lang.Tests/Shared/MidiReadHelpers.cs` at that point.

    **Signature sketch:**
    ```csharp
    internal static class MidiReadHelpers
    {
        public static byte[] GetVelocityBytes(string midiPath);
        public static int[] GetNoteNumbers(string midiPath);
    }
    ```

    ---

    ## DEFER-06: `slice` negative-from-end indexing (Pythonic)

    **Origin:** CONTEXT D-01 — Phase 14 ships simple two-sided clamping. Negative indices (e.g., `slice(seq, -2, -1)`) are not interpreted as from-end selection.

    **Resolution path:** If a user surfaces the need, add a separate overload or a `reverseSlice`/`sliceFromEnd` rather than overload existing semantics.
    ```

    PART B — Create `.planning/phases/14-composer-dx-part-1/14-VERIFICATION.md`:

    ```markdown
    # Phase 14: Composer DX Part 1 — Verification

    **Phase:** 14
    **Status:** Complete
    **Closed:** 2026-04-2X (plan 14-04 commit <hash>)

    ---

    ## Success Criteria Verification (from ROADMAP.md)

    | # | Criterion | Pinning Artifact | Commit |
    |---|-----------|------------------|--------|
    | 1 | `slice(seq, start, end)` bar-level, clamps like `take`/`drop`; `slice(Array[T], Int, Int)` analogous. | `flow-lang.Tests/Unit/Phase14/SliceTests.cs` + `tests/test_slice.flow` Theory row | <14-01 hash> |
    | 2a | `Db4`, `Eb4`, `Gb4`, `Ab4`, `Bb4`, `Cb4`, `Fb4` parse as notes in `\| … \|`. | `NoteTypeTests.Parse_FlatLetter_*` Facts + `tests/test_flat_literals.flow` Theory row | <14-02A hash> |
    | 2b | `H` accepted as `B` alias only in note streams. | **DEFERRED per CONTEXT D-10** — see `deferred-items.md` DEFER-02/03 | — |
    | 2c | `Int H = 5;` / `proc H () { }` / existing identifier uses continue to compile unchanged. | **Preserved by design** — H alias not shipped. Verified via pre-landing grep (empty `\bH\b` across `*.flow`). | — |
    | 3 | `enharmonic(Note) → Note` returns pitch-equivalent spelling, round-trippable. | `EnharmonicTests.*` Facts + `tests/test_enharmonic.flow` Theory row | <14-02B hash> |
    | 4 | `.flow` with `dynamics` + `crescendo`/`decrescendo`/`swell` exports MIDI with expected velocity gradient; regression asserts byte sequence. | `DynamicsMidiVelocityTests.Crescendo_EmitsExpectedVelocityGradient` asserting `[31, 47, 63, 79, 95]` | <14-03 hash(es)> |
    | 5 | Pre-landing grep of `Db`/`Eb`/`Fb`/`Cb`/`Bb`/`Gb`/`Ab`/`H`/`enharmonic` shows zero identifier collisions. | **Transcript below § Pre-landing Collision Grep** | — |

    ---

    ## Pre-landing Collision Grep (CONTEXT D-21 — re-surfaced from 14-02-PLAN.md)

    Recipe:
    ```bash
    grep -rn '\b(Db|Eb|Fb|Gb|Ab|Bb|Cb|enharmonic)\b' flow-lang/ examples/ tests/ --include='*.flow'
    ```

    Executed: 2026-04-20 during planning of plan 14-02.

    **Result: EMPTY** (exit code 1 — no matches in any `*.flow` file).

    ```
    $ grep -rn '\b(Db|Eb|Fb|Gb|Ab|Bb|Cb|enharmonic)\b' flow-lang/ examples/ tests/ --include='*.flow'
    (no output — exit 1)
    ```

    `*.cs` matches (audited, NOT collisions — dictionary-key entries, not identifier declarations):
    - `flow-lang/Runtime/ProgressionCompiler.cs:19-24` — chromatic name → semitone dictionary
    - `flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs:35-40` — NoteToSemitone dictionary
    - `flow-lang/StandardLibrary/Composition/VariationFunctions.cs:358` — comment about Db→Cs mapping
    - `flow-lang/StandardLibrary/Audio/MidiExport.cs:57-59` — key-signature lookup table comments

    Standalone `\bH\b` also verified empty (H remains deferred per CONTEXT D-10).

    Conclusion: extension landed safely, zero identifier collisions.

    ---

    ## Commit Hash Manifest

    | Plan | Commit(s) | Subject Prefix |
    |------|-----------|----------------|
    | 14-01 | <14-01 hash> | `feat(14-01): DX-05 slice for Sequence + Array[T]` |
    | 14-02 (commit A) | <14-02A hash> | `feat(14-02): DX-06 flat-literal surface` |
    | 14-02 (commit B) | <14-02B hash> | `feat(14-02): DX-06 enharmonic() built-in` |
    | 14-03 (Pass 1 draft) | <14-03 Pass 1 hash> | `test(14-03): DX-08 draft — two-pass strict pass 1` |
    | 14-03 (Pass 2, if applicable) | <14-03 Pass 2 hash or `N/A — GREEN on first run`> | `test(14-03): DX-08 Pass 2 — reconcile` OR `fix(14-03): DX-08 Pass 2 — close <stage> gap` |
    | 14-04 (closure) | <this commit hash> | `docs(14-04): Phase 14 closure — reframe REQUIREMENTS DX-06, VERIFICATION.md, deferred-items.md` |

    ---

    ## Full-Suite Fact Count

    | Stage | `dotnet test` Fact count |
    |-------|-------------------------|
    | Pre-Phase-14 baseline | 81 |
    | Post-14-01 | 81 + SliceTests.* |
    | Post-14-02 commit A | + NoteTypeTests.* + LexerTests.* |
    | Post-14-02 commit B | + EnharmonicTests.* |
    | Post-14-03 | + DynamicsMidiVelocityTests.Crescendo_EmitsExpectedVelocityGradient |
    | Phase 14 close | <total> |

    Fill the actual Fact counts at commit time by running `dotnet test` after each merge.

    ---

    ## Divergences

    Record any deviations from CONTEXT decisions surfaced during execution:

    - **Plan 14-02 Pitfall 3 (ChromaticNotes asymmetry):** <populated from 14-02-SUMMARY.md if divergent; else "None — preferFlat heuristic + MIDI-based lookup resolved cleanly">
    - **Plan 14-03 two-pass strict outcome:** <A: GREEN on first run per F-01 / B: script reshape / C: production gap-fix with divergence — populated from 14-03-SUMMARY.md>
    - **Plan 14-02 Format canonical-style:** <confirmed run-based `+N`/`-N` per RESEARCH recommendation, OR alternative style if executor chose differently per CONTEXT §Claude's Discretion>

    ---

    ## Deferred Items Summary

    Per `.planning/phases/14-composer-dx-part-1/deferred-items.md`:

    - **DEFER-02** — H = B note-stream alias (requires DEFER-03)
    - **DEFER-03** — Pragma / feature-flag language construct (candidate `enable`)
    - **DEFER-04** — Multi-letter enharmonic-edge respelling
    - **DEFER-05** — Shared MIDI-read helper promotion (Phase 15 trigger likely)
    - **DEFER-06** — `slice` negative-from-end indexing

    ---

    ## Sign-off

    - [x] All 5 ROADMAP success criteria verified (criterion 2b/2c DEFERRED-to-deferred-items.md per CONTEXT D-10)
    - [x] Pre-landing collision grep re-surfaced verbatim
    - [x] All atomic commit hashes recorded
    - [x] Full suite green at phase close
    - [x] 14-VALIDATION.md promoted to `nyquist_compliant: true`
    - [x] REQUIREMENTS.md traceability table marked Shipped
    - [x] STATE.md + ROADMAP.md updated

    *Phase 14 closed: 2026-04-2X*
    ```

    PART C — Update `.planning/phases/14-composer-dx-part-1/14-VALIDATION.md`:

    Modify only the frontmatter (top of file) + fill in the Status column of the Per-Task Verification Map with ✅ markers and plan/task IDs once all three upstream plans have shipped.

    Frontmatter edit:
    ```yaml
    ---
    phase: 14
    slug: composer-dx-part-1
    status: complete
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-20
    completed: 2026-04-2X
    ---
    ```

    Per-Task Verification Map: for each row, replace `⬜ pending` with `✅ green` and fill the `Plan (expected)` column with the actual plan-task ID (e.g., `14-01-T1`, `14-02-T1`, `14-02-T2`, `14-03-T1`, `14-03-T2`).

    Validation Sign-Off checklist: mark every `[ ]` as `[x]` to reflect phase close.

    PART D — Update `.planning/STATE.md`:

    Frontmatter edits:
    - `completed_phases: 3` → `completed_phases: 4`
    - `completed_plans: 17` → `completed_plans: 21` (add the 4 Phase-14 plans)
    - `percent: 43` → recomputed percent (4/7 = 57)
    - `stopped_at:` → "Phase 14 closed (commit <hash>). DX-05/06/08 shipped; H alias + pragma system deferred to future phase via deferred-items.md. Next: Phase 15 (DX-07 reverbTime + DX-09 euclidean humanize)."
    - `status:` → "Phase 14 Composer DX Part 1 closed (commit <hash>). DX-05 slice (commit <14-01>), DX-06 reframed with flats + enharmonic shipped (14-02A/B), DX-08 MIDI velocity regression green (14-03). Full suite green. 14-VALIDATION.md at nyquist_compliant: true."
    - `last_updated: "2026-04-2X..."` (stamp to close time)
    - `last_activity: 2026-04-2X`

    Current Position section:
    - `Phase:` → `15` (next phase)
    - `Plan:` → `Not started — Phase 14 closed`
    - `Status:` → "Phase 14 closed; DX-05/06/08 shipped with atomic commits (14-01 <hash>, 14-02A <hash>, 14-02B <hash>, 14-03 <hash(es)>, 14-04 <hash>). DX-06 H-alias deferred to future pragma phase. 14-VERIFICATION.md + 14-VALIDATION.md at nyquist_compliant: true. Ready for /gsd-plan-phase 15."
    - `Last activity:` → `2026-04-2X - Phase 14 Composer DX Part 1 closed`

    Progress bar: `[█████████░░░] 43%` → `[██████████░░] 57%`

    Append to Performance Metrics table (By Phase):
    ```
    | 14 | 4 | - | - |
    ```

    Append to Decisions block:
    ```
    - [Plan 14-01]: DX-05 slice — Sequence + Array[T] atomic per D-02; silent two-sided clamp per D-01; LINQ Skip/Take template from Take/Drop at Collections.cs:117-147. Commit <hash>.
    - [Plan 14-02]: DX-06 flat-literal surface — sum-based NoteType.Parse scan accepts any mix of b/#/+/- on either side of octave digits (D-07); Format emits `+N`/`-N` runs preserving Parse(Format(x))==x (D-08); post-alteration MIDI range check (D-09); SimpleLexer dispatch REORDERED (chord-before-note) per RESEARCH regression discovery — LexerTests.Bb7_IsChord pins the fix. Commits <hashA> (flats+lexer) + <hashB> (enharmonic).
    - [Plan 14-02]: Enharmonic() built-in added via new HarmonyFunctions.RegisterContextDependent method (D-06 additive). In-key branch uses MIDI-based lookup + preferFlat heuristic to work around ChromaticNotes asymmetry (RESEARCH Pitfall 3). No-key fallback flips sharp↔flat; naturals unchanged per D-05 (no E↔Fb / F↔E# / B↔Cb / C↔B# edge respelling). Double-sharps documented non-involutive.
    - [Plan 14-02]: Pre-landing collision grep per D-21 executed once at plan time, empty across all *.flow files. Transcript in 14-02-PLAN.md §Pre-landing Collision Grep and re-surfaced in 14-VERIFICATION.md.
    - [Plan 14-03]: DX-08 two-pass strict per D-13 — Pass 1 drafted Fact from REQUIREMENTS alone asserting byte array [31, 47, 63, 79, 95] for crescendo(0.25, 0.75) over 5 notes. Pass 2 outcome: <A: GREEN first run / B: reshape / C: gap-fix> per 14-03-SUMMARY.md. DryWetMidi 8.0.3 read API via existing dependency, no new NuGet. Commit(s) <hash(es)>.
    - [Plan 14-04]: DX-06 H-alias clause REFRAMED per D-19 (Phase 12 TEST-03 precedent); deferred-items.md captures H + pragma system + candidate `enable` keyword (D-11/D-12). 14-VALIDATION.md promoted to nyquist_compliant: true per CONTEXT §Claude's Discretion last bullet. Commit <hash>.
    ```

    PART E — Update `.planning/ROADMAP.md`:

    In the Phase 14 header entry (around line 156-166), mark the checkbox:
    - `- [ ] **Phase 14: Composer DX Part 1** — ...` → `- [x] **Phase 14: Composer DX Part 1** — ... (completed 2026-04-2X)`

    In the `### Phase 14: Composer DX Part 1` section's **Plans** line, update:
    ```markdown
    **Plans**: TBD
    ```
    to:
    ```markdown
    **Plans**: 4 plans

    Plans:
    - [x] 14-01-PLAN.md — DX-05 slice (Sequence + Array[T] atomic; silent clamp per D-01)
    - [x] 14-02-PLAN.md — DX-06 reduced scope (flat-literal Parse/Format + SimpleLexer reorder + enharmonic(); H-alias deferred)
    - [x] 14-03-PLAN.md — DX-08 MIDI velocity regression (two-pass strict, byte-array pin)
    - [x] 14-04-PLAN.md — Phase 14 closure (REQUIREMENTS reframe + deferred-items.md + 14-VERIFICATION.md + nyquist promotion + STATE/ROADMAP)
    ```

    In the Progress table at the bottom:
    ```markdown
    | 14. Composer DX Part 1 | v1.2 | 0/? | Not started | - |
    ```
    →
    ```markdown
    | 14. Composer DX Part 1 | v1.2 | 4/4 | Complete    | 2026-04-2X |
    ```

    PART F — Single atomic commit covering Task 1 and Task 2 changes:

    `docs(14-04): Phase 14 closure — reframe REQUIREMENTS DX-06, 14-VERIFICATION.md, deferred-items.md, nyquist promotion`

    Body: reference CONTEXT D-10/D-11/D-12/D-19 (reframe + deferral), D-21 (grep re-surfacing), Phase 13 D-24 precedent (nyquist_compliant promotion on code phase), and enumerate all files touched.

    PART G — Final verification pass after the commit lands:

    ```bash
    dotnet test 2>&1 | tail -5
    ```

    Full suite must be green at phase close. If any test flips RED, the closure commit is invalid — back out and fix before closing.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; test -f .planning/phases/14-composer-dx-part-1/deferred-items.md &amp;&amp; test -f .planning/phases/14-composer-dx-part-1/14-VERIFICATION.md &amp;&amp; grep -q "nyquist_compliant: true" .planning/phases/14-composer-dx-part-1/14-VALIDATION.md &amp;&amp; grep -q "completed_phases: 4" .planning/STATE.md &amp;&amp; grep -qE "^\|\s*14\. Composer DX Part 1\s*\|\s*v1\.2\s*\|\s*4/4\s*\|\s*Complete" .planning/ROADMAP.md &amp;&amp; dotnet test 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/14-composer-dx-part-1/deferred-items.md` exists and contains the headings `## DEFER-02: \`H\` = \`B\` note-stream alias`, `## DEFER-03: Pragma / feature-flag language construct`, `## DEFER-04`, `## DEFER-05`, `## DEFER-06` AND the candidate keyword string `enable "german-notation"`.
    - File `.planning/phases/14-composer-dx-part-1/14-VERIFICATION.md` exists with §Success Criteria Verification table (5 rows), §Pre-landing Collision Grep re-surfacing the grep transcript, §Commit Hash Manifest table, and §Sign-off checklist with all boxes checked.
    - `.planning/phases/14-composer-dx-part-1/14-VERIFICATION.md` contains the verbatim string `grep -rn '\\b(Db|Eb|Fb|Gb|Ab|Bb|Cb|enharmonic)\\b' flow-lang/ examples/ tests/ --include='*.flow'` in a code block (collision grep transcript re-surfacing).
    - `.planning/phases/14-composer-dx-part-1/14-VALIDATION.md` frontmatter contains `nyquist_compliant: true` AND `wave_0_complete: true` AND `status: complete` (all three on separate frontmatter lines).
    - `.planning/STATE.md` frontmatter contains `completed_phases: 4` and an updated `percent:` field.
    - `.planning/ROADMAP.md` Phase 14 entry checkbox is `[x]` with today's date suffix.
    - `.planning/ROADMAP.md` Progress table has a row matching `| 14. Composer DX Part 1 | v1.2 | 4/4 | Complete ...`.
    - `dotnet test` (full suite) exits 0 with all Facts green.
    - Single git commit covers exactly the 6 declared `files_modified` (verify `git show --stat HEAD`).
    - Commit subject starts with `docs(14-04): Phase 14 closure`.
  </acceptance_criteria>

  <done>
    deferred-items.md captures H alias + pragma system + `enable` keyword direction. 14-VERIFICATION.md documents the phase with criteria mapping, collision grep transcript, commit hashes, and sign-off. 14-VALIDATION.md promoted to `nyquist_compliant: true` with `wave_0_complete: true`. STATE.md counters advance to 4/7 phases. ROADMAP.md Phase 14 marked Complete. Full suite green. Single atomic closure commit. Phase 14 shipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Closure / docs-only plan. No code changes. No new network, filesystem, or execution surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-NA | N/A | Planning artifact updates | accept | Pure documentation edits to `.planning/` tree. No runtime effect on the interpreter. Verification reuses existing `dotnet test` gate from prior plans (no new threat surface introduced). |
</threat_model>

<verification>
Phase-level checks for plan 14-04:
- [ ] REQUIREMENTS.md DX-05 / DX-06 / DX-08 rows marked Shipped with actual commit hashes
- [ ] REQUIREMENTS.md DX-06 original wording preserved as `*Original audit-trail:*` preamble
- [ ] REQUIREMENTS.md Traceability table rows for DX-05/06/08 all say `Shipped` (zero `Pending`)
- [ ] `deferred-items.md` created with DEFER-02 through DEFER-06 entries
- [ ] `14-VERIFICATION.md` created with criteria-to-artifact mapping + collision grep re-surfacing + commit hash manifest + sign-off checklist
- [ ] `14-VALIDATION.md` frontmatter promoted: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`
- [ ] `14-VALIDATION.md` Per-Task Verification Map Status column all ✅ green (no ⬜ pending rows)
- [ ] `STATE.md` frontmatter: `completed_phases: 4`, `completed_plans: 21`, updated `percent`, stamped `last_updated` + `last_activity`
- [ ] `STATE.md` Accumulated Context block appended with Phase 14 plan decisions (14-01 / 14-02 / 14-03 / 14-04)
- [ ] `ROADMAP.md` Phase 14 checkbox `[x]`, Progress table row `4/4 | Complete | <date>`
- [ ] `dotnet test` (full suite) green at phase close — no new test run required beyond final verification
- [ ] Single atomic closure commit covers all 6 files; subject prefix `docs(14-04): Phase 14 closure`
</verification>

<success_criteria>
- ROADMAP Phase 14 success criterion 5 (pre-landing collision grep) **re-surfaced** in 14-VERIFICATION.md verbatim from 14-02-PLAN.md. **Pinned** by presence of the grep recipe string in 14-VERIFICATION.md.
- CONTEXT D-10/D-11/D-12: H alias clause dropped from Phase 14 and captured in deferred-items.md with pragma design sketch. **Pinned** by existence of DEFER-02 + DEFER-03 entries in deferred-items.md.
- CONTEXT D-19: REQUIREMENTS.md DX-06 reframed with original wording preserved as audit-trail (Phase 12 TEST-03 precedent). **Pinned** by REQUIREMENTS.md containing both the new reframed row AND the `*Original audit-trail:*` preamble with the original H-clause text.
- Phase 13 D-24 precedent (nyquist_compliant promotion for code phase with observable-value pins): 14-VALIDATION.md at `nyquist_compliant: true`. **Pinned** by frontmatter.
- Full suite remains green at phase close. **Pinned** by final `dotnet test` run recorded in 14-VERIFICATION.md §Full-Suite Fact Count.
- All Phase 14 deliverables bisectable via atomic commits (14-01 atomic per D-02, 14-02 two atomic commits A+B per D-17, 14-03 Pass 1 + optional Pass 2 per D-13, 14-04 single docs commit). **Pinned** by 14-VERIFICATION.md §Commit Hash Manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/14-composer-dx-part-1/14-04-SUMMARY.md` with:
- Closure commit hash
- Files touched summary (6 files)
- Confirmation that all previous plans' commit hashes are captured in REQUIREMENTS.md + 14-VERIFICATION.md
- Confirmation that deferred-items.md covers H-alias + pragma system per CONTEXT D-11/D-12
- Confirmation that `dotnet test` is green at phase close
- Final Fact count delta (pre-Phase-14 baseline → post-Phase-14 close)
- Next-phase signpost: Phase 15 (DX-07 reverbTime + DX-09 euclidean humanize)
</output>
