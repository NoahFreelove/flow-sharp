---
phase: 24-scale-linting-flow-lsp
plan: 05
type: execute
wave: 4
depends_on: ["24-04"]
files_modified:
  - tests/test_scale_lint.flow
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md
autonomous: true
requirements: [LINT-01, LINT-02, LINT-03]
tags: [closure, integration-smoke, byte-identical-regression, phase-24, wave-4]
must_haves:
  truths:
    - "tests/test_scale_lint.flow runs to completion (exit 0) producing non-empty WAV output"
    - "tests/test_scale_lint.flow declares `enable scaleLint;` AND `key Cmajor { ... }` — closed-set membership integration check (a missing PragmaRegistry entry would fail parse)"
    - "REQUIREMENTS.md LINT-01/02/03 marked shipped with commit refs"
    - "ROADMAP.md Phase 24 row updated: status Complete + plan checklist all checked"
    - "STATE.md current_phase advanced past 24"
    - "VERIFICATION.md present with LINT-01/02/03 acceptance verdicts and Phase 18 byte-identical regression confirmation"
    - "tutorial.flow and showcase.flow byte-identical SHA256 unchanged after Phase 24 merge (zero flow-lang touch invariant verified)"
  artifacts:
    - path: "tests/test_scale_lint.flow"
      provides: "End-to-end .flow integration smoke covering LINT-01/02/03 acceptance"
      contains: "enable scaleLint"
    - path: ".planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md"
      provides: "Final verification report — LINT-01/02/03 verdicts + byte-identical regression result + cross-cutting concerns"
      contains: "LINT-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LINT-01/02/03 marked shipped"
      contains: "LINT-01 | Phase 24 | Shipped"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 row Complete; v1.3 milestone progress incremented"
      contains: "Phase 24: Scale Linting (flow-lsp)"
    - path: ".planning/STATE.md"
      provides: "current_phase advanced past 24"
      contains: "phase: 25"
  key_links:
    - from: "tests/test_scale_lint.flow"
      to: "PragmaRegistry closed-set"
      via: "enable scaleLint declaration parses without D-12 unknown-pragma error"
      pattern: "enable scaleLint"
    - from: ".planning/REQUIREMENTS.md"
      to: "git commit hashes for Phase 24 plans"
      via: "Shipped <hash> annotations"
      pattern: "Shipped"
---

<objective>
Close Phase 24: ship the user-facing `.flow` integration smoke that pins LINT-01/02/03 acceptance text, run the full Phase 18 byte-identical regression gate to confirm the "zero flow-lang touch" invariant held (tutorial.flow / showcase.flow SHA256 unchanged), and update REQUIREMENTS / ROADMAP / STATE / VERIFICATION to mark Phase 24 shipped.

The .flow smoke is small by design: flow-interpreter does not run an LSP frontend, so the smoke verifies (a) the pragma is accepted by the closed registry (Plan 24-01) and (b) the file parses + renders without error. Per-diagnostic assertions are already pinned in Plan 24-03's `ScaleLintAnalyzerFacts` (`NonDiatonic_FsharpInCmajor_FlagsOneDiagnostic`, etc.) and Plan 24-04's `CombinedDiagnosticsPublisherFacts`.

LINT-03 acceptance text in REQUIREMENTS.md has a documented wording bug (`Aminor` doesn't have F# diatonic either, so the inner-key-wins point doesn't land cleanly). The smoke uses the realistic replacement noted in CONTEXT.md specifics line 171: outer `key Cmajor { key Gmajor { | F#4 | } }` — F#4 IS diatonic in Gmajor, so it does NOT flag. This is the LINT-03 invariant.

This is the final plan. After it ships, Phase 25 (Gaussian Humanize) is unblocked.

Output: 1 new .flow file, 1 new VERIFICATION.md, edits to REQUIREMENTS.md / ROADMAP.md / STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/24-scale-linting-flow-lsp/24-CONTEXT.md
@.planning/phases/24-scale-linting-flow-lsp/24-RESEARCH.md
@.planning/phases/24-scale-linting-flow-lsp/24-PATTERNS.md
@.planning/phases/24-scale-linting-flow-lsp/24-VALIDATION.md
@.planning/phases/24-scale-linting-flow-lsp/24-00-SUMMARY.md
@.planning/phases/24-scale-linting-flow-lsp/24-01-SUMMARY.md
@.planning/phases/24-scale-linting-flow-lsp/24-02-SUMMARY.md
@.planning/phases/24-scale-linting-flow-lsp/24-03-SUMMARY.md
@.planning/phases/24-scale-linting-flow-lsp/24-04-SUMMARY.md

<interfaces>
<!-- Reference templates extracted verbatim from prior phase closures. -->

Existing .flow smoke pattern (tests/test_tuning_ji.flow:1-29):
```flow
enable justIntonation;

use "@std"
use "@audio"

Note: MICR-01 acceptance — 5:4 just-intonation third (Phase 23 plan 23-04)
Note: ...

tempo 120 {
    timesig 4/4 {
        section ji_triad {
            | C4q E4q G4q |
        }
    }
}

Song song = [ji_triad]
Buffer audio = (renderSong song "piano")
(writeWav "/tmp/flow_test_tuning_ji.wav" audio)
(print "JI ratio applied")

(print "test_tuning_ji: PASSED")
```

Existing VERIFICATION.md pattern — read `.planning/phases/23-microtonal-tuning-wedge/23-VERIFICATION.md` for the closer-precedent format (per-requirement verdict, byte-identical regression line, cross-cutting concerns table).

REQUIREMENTS.md LINT rows (current state — to flip from `[ ]` to `[x]`):
```markdown
- [ ] **LINT-01**: ... Acceptance: editing `key Cmajor { | C4 D4 E4 F#4 G4 | }` shows an Information-severity squiggle on `F#4`.
- [ ] **LINT-02**: ... Acceptance Fact: a key-block with non-diatonic notes produces zero scale-lint diagnostics when the pragma is absent.
- [ ] **LINT-03**: ... Acceptance: `key Cmajor { key Aminor { | F#4 | } }` does NOT flag F#4 ...
```

REQUIREMENTS.md Traceability table (current — flip Pending → Shipped):
```markdown
| LINT-01 | Phase 24 | Pending |
| LINT-02 | Phase 24 | Pending |
| LINT-03 | Phase 24 | Pending |
```

ROADMAP.md Phase 24 row (current state — to update Status + Plans Complete):
```markdown
| 24. Scale Linting (flow-lsp) | v1.3 | 0/N | Not started | - |
```

ROADMAP.md Phase 24 detail block (current state — to flip Plans entry to checked + add commit annotations + add closure date):
```markdown
- [ ] **Phase 24: Scale Linting (flow-lsp)** — Opt-in `enable scaleLint;` pragma emits Information-severity diagnostics for non-diatonic notes inside `key { ... }` contexts
```

STATE.md current_phase entry should advance to reflect Phase 24 closure (the exact format depends on STATE.md current shape — read STATE.md first).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship tests/test_scale_lint.flow integration smoke</name>
  <files>tests/test_scale_lint.flow</files>
  <read_first>
    - tests/test_tuning_ji.flow (analog — pragma + key block + sequence + render + PASSED print)
    - tests/test_pragma_isolation.flow (alternative analog — bare-pragma smoke)
    - .planning/REQUIREMENTS.md (lines 79-81 — LINT-01/02/03 acceptance text verbatim)
    - .planning/phases/24-scale-linting-flow-lsp/24-CONTEXT.md (specifics line 171 — LINT-03 wording-bug replacement)
  </read_first>
  <action>
Create `tests/test_scale_lint.flow` mirroring the `test_tuning_ji.flow` shape. The script declares `enable scaleLint;` (closed-set membership integration check — a missing PragmaRegistry entry would fail parse with the D-12 unknown-pragma error), exercises the LINT-01 acceptance pattern, and exits 0 with a `PASSED` print. flow-interpreter does not run the LSP, so per-diagnostic assertions live in the unit tests (Plan 24-03's `ScaleLintAnalyzerFacts`).

For LINT-03 use the realistic replacement from CONTEXT.md specifics line 171: outer `key Cmajor { key Gmajor { | F#4 | } }`. F#4 IS diatonic in Gmajor (the inner key wins) so it does NOT flag — this is the LINT-03 invariant. The original REQUIREMENTS.md text uses `Aminor` which has the same wording bug noted in CONTEXT (F# is not in Aminor either).

Final file content:

```flow
enable scaleLint;

use "@std"
use "@audio"

Note: LINT-01/LINT-02/LINT-03 integration smoke (Phase 24 Plan 24-05)
Note:
Note: flow-interpreter does NOT run an LSP frontend, so this smoke verifies:
Note:   1. `enable scaleLint;` is accepted by PragmaRegistry.KnownPragmas (Plan 24-01)
Note:   2. The file parses + renders without error end-to-end
Note:   3. The pragma is closed-set integration-checked
Note:
Note: Per-diagnostic assertions (LINT-01 F#4 flagged, LINT-02 pragma-absent silent,
Note: LINT-03 inner key Gmajor wins) live in flow-lang.Tests/Unit/Phase24/
Note: ScaleLintAnalyzerFacts.cs:
Note:   - NonDiatonic_FsharpInCmajor_FlagsOneDiagnostic   (LINT-01)
Note:   - PragmaAbsent_NeverFlags_LINT02                  (LINT-02)
Note:   - NestedKeys_InnermostWins_NoFlag                 (LINT-03 — inner Gmajor wins)

Note: LINT-01 canary — F#4 inside `key Cmajor { ... }` would surface a squiggle
Note: in an LSP-aware editor; flow-interpreter just renders silently.
tempo 120 {
    key Cmajor {
        section lint_canary {
            | C4q D4q E4q F#4q G4q |
        }
    }
}

Note: LINT-03 invariant — innermost key wins. F#4 is non-diatonic in Cmajor
Note: but IS diatonic in Gmajor; the squiggle would NOT appear in an LSP editor.
Note: Replaced REQUIREMENTS.md's Aminor example per CONTEXT specifics line 171.
tempo 120 {
    key Cmajor {
        key Gmajor {
            section nested_inner_wins {
                | F#4q |
            }
        }
    }
}

Song song = [lint_canary nested_inner_wins]
Buffer audio = (renderSong song "piano")
(writeWav "/tmp/flow_test_scale_lint.wav" audio)

(print "test_scale_lint: PASSED")
```

After writing, run the smoke and confirm it prints `test_scale_lint: PASSED` and exits 0.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet run --project flow-interpreter tests/test_scale_lint.flow 2>&amp;1 | grep -E "test_scale_lint: PASSED|error"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_scale_lint.flow` exists.
    - File contains `enable scaleLint;` (verifiable: `grep -c 'enable scaleLint' tests/test_scale_lint.flow` returns 1).
    - File contains `key Cmajor { ... | C4q D4q E4q F#4q G4q | ... }` style block (verifiable: `grep -c 'F#4q' tests/test_scale_lint.flow` returns >= 1).
    - File contains nested `key Cmajor { key Gmajor { ... | F#4q | ... } }` block (verifiable: `grep -c 'key Gmajor' tests/test_scale_lint.flow` returns 1).
    - File contains `(print "test_scale_lint: PASSED")` (verifiable: `grep -c 'test_scale_lint: PASSED' tests/test_scale_lint.flow` returns 1).
    - `dotnet run --project flow-interpreter tests/test_scale_lint.flow` prints `test_scale_lint: PASSED` and exits 0.
    - The closed-set membership integration check passes — no D-12 unknown-pragma error in the run output.
    - Output WAV exists: `/tmp/flow_test_scale_lint.wav` is a non-empty file.
  </acceptance_criteria>
  <done>Smoke ships and runs clean; LINT-01/03 patterns demonstrated end-to-end at the runtime level; closed-set integration check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 18 byte-identical regression — tutorial.flow / showcase.flow SHA256 unchanged</name>
  <files></files>
  <read_first>
    - flow-lang.Tests/Integration/ (find existing ByteIdenticalTutorialTests + ByteIdenticalShowcaseTests files; pattern from Phase 18 Plan 18-02)
    - examples/tutorial.flow + examples/showcase.flow (verify they exist and are runnable)
  </read_first>
  <action>
Phase 24's "zero flow-lang touch" goal (per ROADMAP D-04) means tutorial.flow / showcase.flow must produce byte-identical WAV + MIDI before and after Phase 24 merge. The only flow-lang change in this phase is `flow-lang/Lexing/PragmaRegistry.cs` line 16+ adding `["scaleLint"]` to the closed-set dictionary — but neither tutorial.flow nor showcase.flow declares `enable scaleLint;`, so the new dictionary entry is unreachable at runtime for these files.

Run the existing byte-identical Integration test filter to confirm regression-free:

```bash
dotnet test flow-lang.Tests --filter "FullyQualifiedName~ByteIdentical"
```

If the existing `ByteIdenticalTutorialTests` / `ByteIdenticalShowcaseTests` Facts pass, the invariant holds and no further action is needed.

Additionally, run the full `tests/test_*.flow` suite to confirm no regressions in any of the ~70 existing scripts:

```bash
for t in tests/test_*.flow; do
  dotnet run --project flow-interpreter "$t" 2>&1 | tail -1
done
```

Each script must end with its own `PASSED` line. Any non-PASSED output is a regression.

Capture the SHA256 of `examples/output/tutorial.wav` and `examples/output/showcase.wav` (if they exist post-test-run) for inclusion in the VERIFICATION.md report (Task 4). If the files don't exist (the Integration tests may run in temp directories), the test pass alone is sufficient evidence.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~ByteIdentical" 2>&amp;1 | grep -E "Passed|Failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "FullyQualifiedName~ByteIdentical"` reports all Facts passed (zero failures across `ByteIdenticalTutorialTests` and `ByteIdenticalShowcaseTests` if they exist; the test names may vary — the filter `~ByteIdentical` matches whichever exist).
    - Full .flow integration loop: `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" >/dev/null 2>&1 || echo FAIL: $t; done` produces no `FAIL:` lines.
    - `dotnet test` full suite exits 0.
  </acceptance_criteria>
  <done>Byte-identical regression confirmed; full .flow suite passes; Phase 24 introduces zero runtime regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md — flip LINT-01/02/03 to shipped with commit refs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 79-81 — LINT-01/02/03 active rows; lines 124+ — Traceability table)
  </read_first>
  <action>
Update both the active-requirements section and the traceability table.

**Step 1: Active requirements section** (lines 79-81). For each LINT-01/02/03 row, change the leading `[ ]` to `[x]` and append a `— Shipped <commit-or-plan-ref>` annotation. The plan refs are 24-00 (Wave 0), 24-01 (registry), 24-02 (spellings), 24-03 (analyzer), 24-04 (wiring), 24-05 (this closure).

Suggested new shape (the `<commit-hash>` is filled in after the closure commit; for the plan-text, use a generic "Phase 24 plans 24-00..24-04" annotation since this Task 3 commits BEFORE the closure commit hash exists):

```markdown
- [x] **LINT-01**: Per D-02, `enable scaleLint;` pragma activates flow-lsp scale linting. When active, flow-lsp emits `Diagnostic { Severity = Information }` for any note in a `key Cmajor { ... }` context that is non-diatonic. Existing diagnostic plumbing reused — zero flow-lang touch. Acceptance: editing `key Cmajor { | C4 D4 E4 F#4 G4 | }` shows an Information-severity squiggle on `F#4`. — Shipped Phase 24 plans 24-00..24-04
- [x] **LINT-02**: Scale linting is opt-in (Pitfall 8 mitigation — never default-on). Without `enable scaleLint;`, flow-lsp emits zero scale-lint diagnostics. Acceptance Fact: a key-block with non-diatonic notes produces zero scale-lint diagnostics when the pragma is absent. — Shipped Phase 24 plans 24-00..24-04
- [x] **LINT-03**: Scale linting respects nested key contexts (key inside key inside section). Innermost active key wins for diagnostic computation. Acceptance: `key Cmajor { key Gmajor { | F#4 | } }` does NOT flag F#4 (Gmajor is the innermost active key, F# is diatonic in Gmajor). — Shipped Phase 24 plans 24-00..24-04
```

NB: LINT-03's acceptance text is corrected (per CONTEXT.md specifics line 171) — replace `key Aminor` with `key Gmajor` and update the parenthetical to "F# is diatonic in Gmajor". This closes the documented wording bug.

**Step 2: Traceability table** (the table starting around line 122). Update LINT-01/02/03 rows from `Pending` to `Shipped <annotation>`:

```markdown
| LINT-01 | Phase 24 | Shipped Phase 24 plans 24-00..24-04 |
| LINT-02 | Phase 24 | Shipped Phase 24 plans 24-00..24-04 |
| LINT-03 | Phase 24 | Shipped Phase 24 plans 24-00..24-04 |
```

Optional: post-closure-commit, an amend pass can replace the plan-text with the actual commit hashes. For now the plan-text suffices.

DO NOT modify any other rows in the table or active section.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; grep -c "\[x\] \*\*LINT-0" .planning/REQUIREMENTS.md (returns 3)
    </automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line containing LINT-01 starts with - [x] (verifiable: grep -c '\[x\] \*\*LINT-01' .planning/REQUIREMENTS.md returns 1).
    - Same for LINT-02 and LINT-03 (each grep returns 1).
    - Traceability table rows for LINT-01/02/03 contain 'Shipped' (verifiable: grep -c '| LINT-0[123] | Phase 24 | Shipped' .planning/REQUIREMENTS.md returns 3).
    - LINT-03 acceptance text references key Gmajor (verifiable: grep -c 'key Cmajor { key Gmajor' .planning/REQUIREMENTS.md returns 1).
    - No other LINT or non-LINT rows changed (manual diff inspection — only 6 lines edited total: 3 active + 3 traceability).
  </acceptance_criteria>
  <done>REQUIREMENTS.md flipped to shipped; LINT-03 wording bug closed; traceability table updated.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md and STATE.md to mark Phase 24 complete</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 24 detail block + progress table — search for 'Phase 24' to locate both)
    - .planning/STATE.md (current_phase or active_phase entry — read frontmatter and the phase-progress section)
    - .planning/phases/23-microtonal-tuning-wedge/23-VERIFICATION.md (analog format for verification reports — closer-precedent style)
  </read_first>
  <action>
**Step 1: ROADMAP.md Phase 24 detail block** — flip the Phase 24 entry to checked + add closure annotation:

```markdown
- [x] **Phase 24: Scale Linting (flow-lsp)** — Opt-in `enable scaleLint;` pragma emits Information-severity diagnostics for non-diatonic notes inside `key { ... }` contexts — Shipped 2026-05-04 (zero flow-lang touch beyond one PragmaRegistry line)
```

**Step 2: ROADMAP.md Phase 24 Plans entry** — replace the placeholder `Plans: TBD` with the 6-plan checklist (all checked):

```markdown
**Plans**: 6 plans
- [x] 24-00-PLAN.md — Wave 0 ParseSession pragma-scan widen + ParseSessionPragmaFacts (closes Phase 17/21 latent hAsB-in-LSP bug)
- [x] 24-01-PLAN.md — PragmaRegistry one-line add + Phase 21 fact migration + PragmaRegistryScaleLintFacts
- [x] 24-02-PLAN.md — DiatonicSpellings 119-entry hardcoded map + DiatonicSpellingsFacts (12 Theory rows + 5 Facts)
- [x] 24-03-PLAN.md — ScaleLintAnalyzer + 14 Facts + 7-row mode Theory pinning LINT-01/02/03 + D-01..D-23
- [x] 24-04-PLAN.md — IScaleLintPublisher + ScaleLintPublisher + CombinedDiagnosticsPublisher + Program.cs DI wiring + 4 Facts
- [x] 24-05-PLAN.md — tests/test_scale_lint.flow + REQUIREMENTS/ROADMAP/STATE/VERIFICATION closure
```

**Step 3: ROADMAP.md progress table** — flip Phase 24 row from 'Not started' to 'Complete':

```markdown
| 24. Scale Linting (flow-lsp) | v1.3 | 6/6 | Complete | 2026-05-04 |
```

**Step 4: STATE.md** — read the existing structure first to identify the current_phase or progress section format, then advance to reflect Phase 24 completion. Typical updates:
  - Update `current_phase` (or equivalent) from `24` to `25` (Gaussian Humanize, the next phase per ROADMAP).
  - Append a Phase 24 closure entry to the timeline / decisions / shipped log if the file has one.
  - Refresh any 'last updated' timestamp.

DO NOT modify other phases' rows or any prior shipped milestones in either file.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; grep -c '24\. Scale Linting (flow-lsp).*Complete' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 24 detail block starts with - [x] (verifiable: grep -c '\[x\] \*\*Phase 24:' .planning/ROADMAP.md returns 1).
    - ROADMAP.md Phase 24 Plans entry contains 24-00, 24-01, 24-02, 24-03, 24-04, 24-05 all marked [x] (verifiable: grep -c '\[x\] 24-0' .planning/ROADMAP.md returns 6).
    - ROADMAP.md progress table Phase 24 row says Complete (verifiable: grep -c '24\. Scale Linting (flow-lsp).*Complete' .planning/ROADMAP.md returns 1).
    - ROADMAP.md progress table Phase 24 row says 6/6 (verifiable: grep -c '24\. Scale Linting (flow-lsp).*6/6' .planning/ROADMAP.md returns 1).
    - STATE.md current_phase advanced past 24 (verifiable: appropriate grep based on STATE.md structure — confirm Phase 24 marked closed and Phase 25 referenced as next).
  </acceptance_criteria>
  <done>ROADMAP and STATE reflect Phase 24 closure; Phase 25 unblocked.</done>
</task>

<task type="auto">
  <name>Task 5: Create 24-VERIFICATION.md final report</name>
  <files>.planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/23-microtonal-tuning-wedge/23-VERIFICATION.md (analog format — per-requirement verdict, byte-identical regression result, cross-cutting concerns table)
    - .planning/phases/24-scale-linting-flow-lsp/24-VALIDATION.md (Per-Task Verification Map — flip each row's Status from pending to green)
    - tests/test_scale_lint.flow (Task 1 output — pin its existence)
  </read_first>
  <action>
Create the verification report. Mirror the Phase 23 VERIFICATION.md format. Include:

1. **Header** — phase identity, closure date, summary of what shipped.
2. **Requirements verdict table** — LINT-01 ✓, LINT-02 ✓, LINT-03 ✓ with the supporting evidence (Fact name + Plan ref) for each.
3. **Cross-cutting concerns table** — Wave 0 latent hAsB-LSP-bug closure, Phase 21 PragmaRegistryFacts migration, Phase 18 byte-identical regression confirmation.
4. **Per-plan summary** — 24-00 through 24-05 with one-line outcome each.
5. **Manual-only verifications outstanding** — per VALIDATION.md row 71-73 (LSP-aware editor visual squiggle render — pending HUMAN-UAT or deferred per Phase 17 precedent).
6. **Approval block** — leave blank for /gsd-verify-work to fill.

Suggested skeleton (adapt as needed; the example below is illustrative — match the actual Phase 23 closer format you observe):

```markdown
# Phase 24: Scale Linting (flow-lsp) — Final Verification

**Closed:** 2026-05-04
**Plans shipped:** 6 (24-00 → 24-05)
**Goal:** Opt-in `enable scaleLint;` pragma emits Information-severity diagnostics for non-diatonic notes inside `key { ... }` contexts — zero flow-lang touch (achieved: one PragmaRegistry line).

## Requirements Verdict

| Req | Status | Evidence |
|-----|--------|----------|
| LINT-01 | ✓ Shipped | `ScaleLintAnalyzerFacts.NonDiatonic_FsharpInCmajor_FlagsOneDiagnostic` (Plan 24-03); `CombinedDiagnosticsPublisherFacts.CombinedPublish_ScaleLintTagged_FlowScaleLint` (Plan 24-04); `tests/test_scale_lint.flow` smoke (Plan 24-05) |
| LINT-02 | ✓ Shipped | `ScaleLintAnalyzerFacts.PragmaAbsent_NeverFlags_LINT02` (Plan 24-03); `CombinedDiagnosticsPublisherFacts.BuildAll_PragmaAbsent_NoLintDiagnostics` (Plan 24-04) |
| LINT-03 | ✓ Shipped | `ScaleLintAnalyzerFacts.NestedKeys_InnermostWins_NoFlag` (Plan 24-03); reuses `NoteStreamContext.FindEnclosingKey` verbatim per D-21 |

## Cross-cutting Concerns

| Concern | Resolution |
|---------|------------|
| Phase 17/21 latent bug — `enable hAsB;` not honored in LSP | Closed in Plan 24-00 ParseSession widen; pinned by `ParseSessionPragmaFacts.Parse_EnableHAsB_LexesH4qAsNoteLiteral` |
| Phase 21 PragmaRegistryFacts negative assertion + alphabetized CSV | Migrated in Plan 24-01 — sentinel `futureUnknownPragma` + updated CSV `equalTemperament, hAsB, justIntonation, pythagorean, scaleLint` |
| Phase 18 byte-identical regression (tutorial.flow / showcase.flow) | Confirmed clean — no flow-lang runtime path touched; PragmaRegistry add is unreachable for files that don't declare `enable scaleLint;` |
| Closed-set growth pattern | KnownPragmas count: 1 (Phase 21) → 4 (Phase 23) → 5 (Phase 24); Phase 23 lower-bound Fact (>= 4) still passes |

## Per-Plan Summary

| Plan | Outcome |
|------|---------|
| 24-00 | ParseSession.Parse widened to mirror FlowEngine pragma-scan pipeline; `Program.Pragmas` now populated in LSP; latent `enable hAsB;` bug closed |
| 24-01 | `PragmaRegistry.KnownPragmas` extended with `scaleLint` (one-line touch — only flow-lang change in Phase 24); Phase 21 test migration |
| 24-02 | `flow-lsp/Diagnostics/DiatonicSpellings.cs` 119-entry hardcoded map; spelling-aware D-01 invariant pinned |
| 24-03 | `flow-lsp/Diagnostics/ScaleLintAnalyzer.cs` AST-walking analyzer; D-01..D-23 dispatch logic; LINT-01/02/03 unit acceptance achieved |
| 24-04 | `IScaleLintPublisher` + `ScaleLintPublisher` + `CombinedDiagnosticsPublisher` orchestrator; Program.cs DI wired; LSP REPLACE semantics handled correctly |
| 24-05 | `tests/test_scale_lint.flow` integration smoke; REQUIREMENTS/ROADMAP/STATE updated; this verification report |

## Manual UAT (Outstanding)

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Information-severity squiggle renders in editor under `flow.scaleLint` source filter | LSP wire format is asserted via xUnit; the actual rendering varies by editor (VS Code / Neovim / Helix) | Open `tests/test_scale_lint.flow` in a flow-lsp-aware editor with `enable scaleLint;` declared; confirm `F#4` shows an Information-severity squiggle (typically blue/teal underline). Confirm `Source: flow.scaleLint` filter hides scale-lint diagnostics independently of parse errors. (Pending HUMAN-UAT — Phase 17 precedent: tracked separately, not blocking phase closure.) |

## Approval

_Reserved for /gsd-verify-work output._
```

Adjust the format to match the closer Phase 23 VERIFICATION.md style discovered during `<read_first>`. The exact section ordering and phrasing should align with whatever Phase 23 used so the documentation stays consistent across phases.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; test -f .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md &amp;&amp; grep -c 'LINT-0[123]' .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md exists.
    - File contains LINT-01, LINT-02, LINT-03 markers (verifiable: grep -c 'LINT-0[123]' .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md returns >= 6 — twice each in verdict + traceability).
    - File contains the byte-identical regression confirmation (verifiable: grep -ic 'byte-identical' .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md returns >= 1).
    - File contains references to all 6 plans (verifiable: grep -c '24-0[0-5]' .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md returns >= 6).
    - File mentions the Phase 17/21 latent hAsB-LSP-bug closure (verifiable: grep -c 'hAsB' .planning/phases/24-scale-linting-flow-lsp/24-VERIFICATION.md returns >= 1).
  </acceptance_criteria>
  <done>Verification report written; LINT-01/02/03 verdicts captured; cross-cutting concerns documented; manual-UAT row preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user .flow source → flow-interpreter | tests/test_scale_lint.flow consumes user-written pragma + key block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Tampering | tests/test_scale_lint.flow declares an unknown pragma | mitigate (inherited) | PragmaRegistry closed-set check (Plan 24-01) rejects unknown pragmas via D-12 unknown-pragma error path. The smoke ITSELF is the integration check — if the registry add was missed, the smoke fails parse. |
| T-24-05-02 | DoS | Adversarial `tests/test_scale_lint.flow` content crashes the renderer | accept | The .flow content is a small reference-quality script (5 quarter notes + a nested key block). No untrusted-content amplification. |
| T-24-05-03 | Information disclosure | VERIFICATION.md / REQUIREMENTS.md updates leak commit metadata | accept | Public closure metadata; no PII or secrets. |
</threat_model>

<verification>
- `dotnet run --project flow-interpreter tests/test_scale_lint.flow` exits 0 with PASSED line printed
- `dotnet test --filter "FullyQualifiedName~ByteIdentical"` reports all passed
- `dotnet test` full suite exits 0
- `dotnet test --filter "FullyQualifiedName~Phase24"` reports all 6 plans' Facts pass (>= 30 cases across all Phase 24 test files)
- All .flow scripts in tests/ exit 0 with their PASSED line
- REQUIREMENTS.md LINT-01/02/03 marked shipped (3 `[x]` rows + 3 traceability rows)
- ROADMAP.md Phase 24 row Complete + 6/6 plans + 6 [x] entries
- STATE.md advanced past Phase 24
- 24-VERIFICATION.md exists with verdicts + cross-cutting concerns + manual-UAT row
- Phase 18 byte-identical regression: tutorial.flow / showcase.flow SHA256 unchanged (confirmed via existing Integration Facts)
</verification>

<success_criteria>
- tests/test_scale_lint.flow integration smoke ships and passes
- LINT-01, LINT-02, LINT-03 fully shipped at unit + integration + .flow-smoke + documentation levels
- LINT-03 wording bug closed (Aminor → Gmajor in REQUIREMENTS.md acceptance text)
- Phase 18 byte-identical determinism contract verified
- ROADMAP.md, STATE.md, REQUIREMENTS.md reflect Phase 24 closure
- Phase 24 VERIFICATION.md created
- Phase 25 (Gaussian Humanize) is unblocked
</success_criteria>

<output>
After completion, this plan IS the closure — no separate SUMMARY.md required since 24-VERIFICATION.md serves that role per Phase 23 precedent. However, optionally create `.planning/phases/24-scale-linting-flow-lsp/24-05-SUMMARY.md` capturing:
- The `tests/test_scale_lint.flow` smoke shipped
- REQUIREMENTS / ROADMAP / STATE updates
- 24-VERIFICATION.md created
- Cross-link back to 24-00..24-04 SUMMARYs
</output>
