---
phase: 34-symphony-showcase-v1-4-closer-pre-public-public-pivot
plan: 03
type: execute
wave: 5
depends_on: [34-05]
files_modified:
  - README.md
autonomous: false
requirements: [SYM-04]
must_haves:
  truths:
    - "D-601: top-level README.md gains a NEW `## Showcase` section, positioned AFTER `## Features` (line ~33) and BEFORE `## Install (Linux x64)` (line ~35). 1-2 sentence framing, GitHub-native inline audio player, source link, reproduction link, release link."
    - "RESEARCH Pitfall 1 (CRITICAL): GitHub strips raw `<video>` / `<audio>` / `<iframe>` HTML from README markdown EXCEPT when the `<video controls src=\"...\">` `src` is a `https://github.com/user-attachments/assets/<uuid>` URL produced uniquely by the GitHub web-UI drag-drop flow. CONTEXT D-601's 'bare release-asset URL renders as player' assumption is INCORRECT."
    - "RESEARCH Priority 2 Option A: manual drag-drop step in the GitHub web editor. This plan REQUIRES plan 34-05 to have already published the release (so the release-asset download URL also exists as a coexisting plain link)."
    - "Plain release-asset URL is documented as a Download fallback alongside the inline player (both URLs coexist — listener can press play in browser OR download the MP3)."
  artifacts:
    - path: README.md
      provides: "Top-level repo landing page with new `## Showcase` section containing inline audio player + 4 links (source / reproduction / MP3 download / release page)."
      contains: '## Showcase'
  key_links:
    - from: README.md
      to: GitHub user-attachments/assets/<uuid>
      via: '<video controls src="..."></video> inline player (drag-drop only; not bare URL)'
      pattern: 'user-attachments'
    - from: README.md
      to: examples/symphony/symphony.flow
      via: 'plain markdown link `[Source: examples/symphony/symphony.flow](./examples/symphony/symphony.flow)`'
      pattern: 'examples/symphony/symphony.flow'
    - from: README.md
      to: examples/symphony/README.md
      via: 'plain markdown link `[How to reproduce locally](./examples/symphony/README.md)`'
      pattern: 'examples/symphony/README.md'
    - from: README.md
      to: GitHub Releases page for v1.4.0
      via: 'plain markdown link `[v1.4.0 release](https://github.com/OWNER/REPO/releases/tag/v1.4.0)`'
      pattern: 'releases/tag/v1.4.0'
---

<objective>
Insert a new `## Showcase` section into the top-level `README.md` positioning the symphony as the v1.4 headline artifact — with a GitHub-native inline audio player (drag-drop user-attachments URL), source/reproduction/release links, and a plain MP3 download fallback. This section is the first interaction new repo visitors have with v1.4 ("listener lands on README and HEARS the symphony immediately") per RESEARCH Pitfall 1 Option A recommendation.

This plan MUST run AFTER plan 34-05 because (a) the user-attachments drag-drop happens in the GitHub web UI (composer manual step) and (b) the release-asset download fallback link requires the release to exist publicly.

Purpose: SYM-04 (top-level README showcase section with audio embed).

Output: top-level `README.md` modified with the new `## Showcase` section between `## Features` and `## Install (Linux x64)`. The inline `<video>` tag uses the user-attachments URL captured during the manual drag-drop step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-05-SUMMARY.md
@README.md
@examples/symphony/symphony.flow
@examples/symphony/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Composer drag-drops the MP3 via GitHub web UI to obtain user-attachments URL</name>
  <files>(no local file changes — manual GitHub UI step)</files>
  <read_first>
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Priority 2 (GitHub Audio Embed Mechanics)" — full text, especially Option A steps.
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 3 (CRITICAL Pitfall 1 callout)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-05-SUMMARY.md (confirms release v1.4.0 is live; release-asset URL is real)
    - /tmp/flow-symphony-v1.4.0.mp3 (the composer-local MP3 from plan 34-05 Task 2 — still on disk for re-upload via drag-drop)
  </read_first>
  <action>HUMAN CHECKPOINT — this is a manual verification step performed by the composer. See <how-to-verify> for the procedure and <acceptance_criteria> for the pass conditions. No automated action is taken by the executor; the executor pauses and waits for the resume-signal.</action>
  <verify>
    <human-check>Composer follows the procedure in &lt;how-to-verify&gt; and replies with the resume-signal text described in &lt;resume-signal&gt;.</human-check>
  </verify>
  <done>Composer provides resume-signal indicating acceptance criteria are satisfied (per &lt;acceptance_criteria&gt;).</done>
  <what>
This task is genuinely manual — GitHub does NOT expose the user-attachments URL via any CLI or API. The only path to obtain it is the web-UI drag-drop flow (RESEARCH Pitfall 1).

The composer:
1. Opens the GitHub README edit page in the browser: `https://github.com/OWNER/REPO/edit/dev/README.md` (substitute the canonical OWNER/REPO from `git remote get-url origin`).
2. Scrolls to the position where the `## Showcase` section will land (between `## Features` and `## Install (Linux x64)`).
3. Drags `/tmp/flow-symphony-v1.4.0.mp3` from the local filesystem into the editor pane. GitHub uploads the file to its user-attachments CDN and inserts a snippet at the cursor position. The inserted snippet has the form:
   ```
   <video controls src="https://github.com/user-attachments/assets/<UUID>"></video>
   ```
   The `<UUID>` is what we need.
4. The composer COPIES that URL (`https://github.com/user-attachments/assets/<UUID>`) — both the full snippet AND the URL alone, for safety.
5. The composer can DISCARD the in-browser edit (do NOT click "Commit changes" in the web UI — the actual README edit lands via Task 2's local commit instead). The user-attachments URL persists even if the README edit is never committed, because GitHub permanently stores the uploaded file once drag-drop completes.

(Source of truth: GitHub Community Discussion #53410 + GitHub Docs "Attaching files" — both verified by RESEARCH Priority 2.)
  </what>
  <how-to-verify>
Composer pastes the captured user-attachments URL into the resume-signal. Verify:
- URL matches pattern `https://github.com/user-attachments/assets/[0-9a-f-]+` (UUID).
- URL is reachable: `curl -sI <URL> 2>/dev/null | head -1 | grep -q '200\|302'` (200 OK or 302 redirect — GitHub may CDN-redirect).
- Composer confirms the MP3 played correctly when GitHub auto-rendered the inline preview in the edit page (sanity check that the uploaded file is intact).
  </how-to-verify>
  <acceptance_criteria>
    - Composer supplies a URL of the form `https://github.com/user-attachments/assets/<UUID>`.
    - The URL responds with HTTP 200 or 302 when curled.
    - Composer confirms (subjectively) that GitHub's inline preview in the edit page played the audio correctly.
  </acceptance_criteria>
  <resume-signal>Composer pastes the `https://github.com/user-attachments/assets/<uuid>` URL exactly as captured. Task 2 reads this URL as input to the README edit. Format the message as: `URL: https://github.com/user-attachments/assets/<uuid>`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Insert `## Showcase` section into README.md using the captured user-attachments URL</name>
  <files>README.md</files>
  <read_first>
    - README.md (current file — identify the exact line boundary between `## Features` and `## Install (Linux x64)`; the section insertion point is BETWEEN them)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 3 (README insertion shape pattern + CRITICAL Pitfall 1 callout)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md D-601 (4-link contents requirement)
    - examples/symphony/symphony.flow (extract the composer-final working title for the framing sentence — e.g. "In Five Voices")
    - The URL pasted by composer in Task 1's resume-signal.
  </read_first>
  <action>
Insert the new section into README.md.

Steps:
1. Resolve `OWNER/REPO` from `git remote get-url origin` (strip prefix/suffix).
2. Resolve the symphony's working title from `examples/symphony/symphony.flow`'s top comment.
3. Use Read tool to load README.md and identify the EXACT line range to insert at: AFTER the `## Features` H2 (and its single-line body `See [FEATURES.md](./FEATURES.md) for a complete list of features.`) and BEFORE the `## Install (Linux x64)` H2.
4. Use Edit tool to insert the new section. Section body MUST contain (with `<USER-ATTACHMENTS-URL>` and `<TITLE>` and `OWNER/REPO` substituted):

```
## Showcase

Listen to *<TITLE>* — ~60 seconds for 5 orchestral instruments
(violin, cello, flute, horn, timpani), rendered entirely from
[`examples/symphony/symphony.flow`](./examples/symphony/symphony.flow)
via the v1.4 SFZ sampler against VSCO Community CE 1.1.0.

<video controls src="<USER-ATTACHMENTS-URL>"></video>

- [Source: `examples/symphony/symphony.flow`](./examples/symphony/symphony.flow)
- [How to reproduce locally](./examples/symphony/README.md)
- [Download MP3 (1.5 MB)](https://github.com/OWNER/REPO/releases/download/v1.4.0/flow-symphony-v1.4.0.mp3)
- [v1.4.0 release](https://github.com/OWNER/REPO/releases/tag/v1.4.0)
```

Where:
- `<TITLE>` = composer's working title (in italics).
- `<USER-ATTACHMENTS-URL>` = the URL pasted by composer in Task 1.
- `OWNER/REPO` = canonical repo path (e.g. `noahfreelove/flow-sharp`).
- The "1.5 MB" size in the Download MP3 label should match the actual MP3 size from plan 34-05 Task 2 (round to nearest 0.1 MB).

Rules:
- NO emoji (CLAUDE.md Conventions).
- ASCII only.
- The `<video controls src="...">` tag is the ONLY HTML form GitHub honors here per RESEARCH Pitfall 1 — do NOT replace with `<audio>` or `<iframe>` or with a bare URL.
- DO NOT touch any section before `## Features` or after `## Install (Linux x64)`.
- Preserve the rest of the README byte-for-byte (run `git diff README.md` after edit and verify ONLY the new section appears in the diff).

Commit:
- Subject: `docs(34-03): add Showcase section to top-level README with inline symphony player`
- Body: 3 lines naming the inline `<video>` tag with user-attachments URL + the 4 supporting links + the position between Features and Install.
- Standard `Co-Authored-By` trailer.
  </action>
  <verify>
    <automated>grep -q '^## Showcase$' README.md &amp;&amp; grep -q 'user-attachments/assets/' README.md &amp;&amp; grep -q 'examples/symphony/symphony.flow' README.md &amp;&amp; grep -q 'examples/symphony/README.md' README.md &amp;&amp; grep -q 'releases/tag/v1.4.0' README.md &amp;&amp; grep -q 'releases/download/v1.4.0/flow-symphony-v1.4.0.mp3' README.md &amp;&amp; grep -B1 '^## Showcase$' README.md | grep -Eq '^$|FEATURES' &amp;&amp; grep -A1 '^## Showcase$' README.md | grep -Eq 'Listen|^[A-Z]' &amp;&amp; awk '/^## /{print NR, $0}' README.md | grep -B1 -A1 'Showcase' | head -10 &amp;&amp; git log -1 --pretty=format:%s README.md | grep -Eq 'docs\(34-03\)'</automated>
  </verify>
  <acceptance_criteria>
    - `## Showcase` H2 section is present in README.md — verified by grep.
    - `<video controls src="https://github.com/user-attachments/assets/...">` tag is present with a real UUID URL (not a placeholder) — verified by grep on `user-attachments/assets/`.
    - All 4 D-601 links are present:
      - `examples/symphony/symphony.flow` (source link).
      - `examples/symphony/README.md` (reproduction link).
      - `releases/download/v1.4.0/flow-symphony-v1.4.0.mp3` (MP3 download fallback).
      - `releases/tag/v1.4.0` (release page link).
    - The `## Showcase` heading appears BETWEEN `## Features` and `## Install (Linux x64)` in the file ordering — verified by inspecting the H2 sequence: `awk '/^## /{print NR, $0}' README.md` should show Features → Showcase → Install in that order.
    - Latest commit subject on README.md starts with `docs(34-03)`.
    - No emoji present (negative-grep on common emoji characters).
  </acceptance_criteria>
  <done>README.md updated with `## Showcase` section. The user-attachments URL renders as an inline player when viewed on GitHub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Composer verifies inline player renders on GitHub</name>
  <files>(no file changes — visual verification)</files>
  <read_first>(none — interactive verification)</read_first>
  <action>HUMAN CHECKPOINT — this is a manual verification step performed by the composer. See <how-to-verify> for the procedure and <acceptance_criteria> for the pass conditions. No automated action is taken by the executor; the executor pauses and waits for the resume-signal.</action>
  <verify>
    <human-check>Composer follows the procedure in &lt;how-to-verify&gt; and replies with the resume-signal text described in &lt;resume-signal&gt;.</human-check>
  </verify>
  <done>Composer provides resume-signal indicating acceptance criteria are satisfied (per &lt;acceptance_criteria&gt;).</done>
  <what-built>
    - README.md now has a `## Showcase` section with a `<video>` tag pointing at the user-attachments URL plus 4 supporting links.
    - The commit is pushed to dev (or wherever default branch sits).
  </what-built>
  <how-to-verify>
The user-attachments inline player ONLY renders on GitHub-rendered markdown (NOT in editor preview, NOT in local markdown viewers). Composer manually verifies:

1. Push the commit if not already pushed: `git push origin dev`.
2. Open the repo's README on GitHub in browser: `gh repo view --web` (or `xdg-open https://github.com/OWNER/REPO`).
3. Scroll to the `## Showcase` section.
4. Confirm:
   - An inline HTML5 video player is visible (NOT a plain link).
   - Pressing play streams the MP3 audio (the symphony plays in-browser).
   - All 4 markdown links resolve correctly when clicked (source link goes to symphony.flow; reproduction link goes to examples/symphony/README.md; MP3 download triggers download; release link goes to /releases/tag/v1.4.0).
5. (Optional) Verify on a logged-OUT browser to confirm the inline player works for unauthenticated visitors — the user-attachments URL is public CDN content per RESEARCH Pitfall 1.

If the inline player does NOT render (shows a plain link or nothing instead):
- Verify the URL is exactly the form `https://github.com/user-attachments/assets/<UUID>` (NOT `https://github.com/OWNER/REPO/releases/download/...` which would NOT render — that's the D-601 misunderstanding).
- Verify the tag is `<video controls src="...">` (the ONLY HTML form GitHub honors here).
- If still broken, the most likely cause is Task 1 drag-drop failed silently and the URL is stale; re-run Task 1 to capture a fresh URL.
  </how-to-verify>
  <acceptance_criteria>
    - Composer affirms the inline `<video>` player renders on the public GitHub README page.
    - Composer affirms pressing play streams the MP3 audio correctly.
    - Composer affirms all 4 supporting links resolve to their intended targets.
  </acceptance_criteria>
  <resume-signal>Composer types "approved — Showcase section renders" or describes the issue if the inline player did not render (in which case Task 1 may need re-execution).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local /tmp MP3 ↔ GitHub user-attachments CDN | Task 1 drag-drop uploads the MP3 to GitHub's user-attachments storage, which is publicly hosted forever once the asset URL is generated. Composer reviews the MP3 contents before drag-drop (composer rendered it themselves in plan 34-01 + 34-05). |
| README.md ↔ public repo HEAD | README is publicly visible immediately upon push; the user-attachments URL is permanent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-PUB | Information disclosure | user-attachments MP3 + README link | mitigate | Composer authored and rendered the MP3 themselves in plans 34-01 + 34-05; reviewed contents in plan 34-05 Task 5 manual sign-off. The user-attachments URL is publicly hosted by GitHub once embedded; composer confirms the MP3 contents are intended for public distribution before the Task 1 drag-drop. Severity: low (informational). |
| T-34-03-LINK | Tampering | 4 supporting links | accept | Plain Markdown links; no executable surface. Composer verifies all 4 resolve in Task 3 visual check. Severity: none. |
</threat_model>

<verification>
- README.md contains `## Showcase` section with inline `<video>` tag.
- The user-attachments URL is a real UUID URL (verified by grep + curl).
- Task 3 composer manual sign-off confirms inline player renders on GitHub.
</verification>

<success_criteria>
- README.md has a `## Showcase` section positioned between `## Features` and `## Install (Linux x64)`.
- Inline `<video controls src="https://github.com/user-attachments/assets/<UUID>">` player renders on GitHub-rendered markdown.
- All 4 D-601 links present (source / reproduction / MP3 download / release).
- Composer signed off on Task 3 visual verification.
</success_criteria>

<output>
Create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-03-SUMMARY.md` when done. SUMMARY should record: the captured user-attachments URL (for future audit), the README.md commit SHA, and confirmation of the Task 3 composer sign-off.
</output>
