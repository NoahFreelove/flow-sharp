---
phase: 34-symphony-showcase-v1-4-closer-pre-public-public-pivot
plan: 04
type: execute
wave: 3
depends_on: [34-02]
files_modified:
  - docs/announcements/v1.4.0.md
autonomous: true
requirements: [SYM-05]
must_haves:
  truths:
    - "D-603: announcement draft lives at docs/announcements/v1.4.0.md — NEW directory under existing docs/. Format: ~3 paragraphs Markdown."
    - "D-603 paragraph 1: elevator pitch — `Flow is a music-production language...` adapted from PROJECT.md lines 5-9."
    - "D-603 paragraph 2: highlight summary — one bullet per shipping Phase 28..34 user-facing feature (SFZ sampler + Scala tuning + sampled tonal instruments + articulation + polyphony + CLI binary + LSP polish)."
    - "D-603 paragraph 3: `Listen to the showcase: [link]. Try it yourself: [install link]. Source: [repo link].`"
    - "D-602 (out of scope): NO auto-posting. The draft is shipped as markdown; composer chooses platform + timing."
    - "Claude's Discretion: composer adapts per-platform from this single source (Reddit gets full 3 paragraphs; HN gets the top-line; X gets the 280-char compression; Discord gets the full text or a TL;DR depending on channel)."
    - "Plan 34-05 reads docs/announcements/v1.4.0.md via `gh release create --notes-file docs/announcements/v1.4.0.md` as the release body source — this plan ships the file BEFORE 34-05 runs (D-901 ordering)."
  artifacts:
    - path: docs/announcements/v1.4.0.md
      provides: "Public-facing v1.4.0 announcement draft — 3 paragraphs Markdown per D-603."
      min_lines: 20
      contains: 'v1.4'
  key_links:
    - from: docs/announcements/v1.4.0.md
      to: examples/symphony/symphony.flow
      via: "Paragraph 3 'Listen to the showcase' link points at the In Five Voices symphony"
      pattern: 'symphony'
    - from: docs/announcements/v1.4.0.md
      to: README.md
      via: "Paragraph 3 'Try it yourself' link points at top-level README install section"
      pattern: 'install'
---

<objective>
Draft the public-facing v1.4.0 announcement at `docs/announcements/v1.4.0.md` — the markdown source the composer adapts per-platform (Reddit / HN / X / Discord) AND the body source for the GitHub Release page (plan 34-05's `gh release create --notes-file` reads this file).

Purpose: SYM-05 (announcement draft ready as part of v1.4 milestone closure activities).

Output: a single new file `docs/announcements/v1.4.0.md` — 3 paragraphs Markdown, ~30 lines, no emoji, repo-link placeholders (`<user>`/`<repo>`) inlined as the actual `noahfreelove/flow-sharp` repo path (verify via `git remote -v` to extract the canonical owner/name).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md
@.planning/PROJECT.md
@docs/editor-setup/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/announcements/v1.4.0.md (3-paragraph public announcement)</name>
  <files>docs/announcements/v1.4.0.md</files>
  <read_first>
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 4 (announcement shape pattern + tone analog)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md D-603 (announcement draft contract + 3-paragraph contents)
    - .planning/PROJECT.md (extract elevator pitch from "What This Is" or top description section for paragraph 1)
    - docs/editor-setup/README.md (closest existing docs/ tone analog — composer-facing, link-heavy)
    - examples/symphony/symphony.flow (just-committed source — extract working title for the announcement)
  </read_first>
  <action>
Create the new directory `docs/announcements/` and the first file inside it.

Steps:
1. Run `mkdir -p docs/announcements` to ensure the parent exists.
2. Extract the canonical repo owner/name from `git remote get-url origin` — strip the `https://github.com/` or `git@github.com:` prefix and `.git` suffix; use the resulting `OWNER/REPO` string in all GitHub URLs below. If the remote URL is not a GitHub URL, halt and surface to composer.
3. Extract the symphony working title from the top comment of `examples/symphony/symphony.flow` (the title the composer signed off on in plan 34-01 — defaults to "In Five Voices" unless composer renamed during UAT).
4. Write the announcement body. Required structure:

```
# Flow v1.4 — Audio Fidelity, Distribution & Public Showcase

<paragraph 1: elevator pitch>
Flow is an interpreted, statically-typed music-production language. Composers write musical ideas as code — note streams, musical-context blocks (tempo / timesig / key), chord literals, transforms, articulation marks — and the interpreter renders them to WAV and MIDI through a full audio pipeline. <Adapt 2-3 more sentences from PROJECT.md "What This Is" or equivalent describing the flow operator, the music-specific types, and the goals/non-goals "ergonomics first, genre-agnostic, music-only" framing.>

<paragraph 2: highlight summary — one bullet per phase>
v1.4 ships the audio-fidelity rewrite and Flow's first formal distribution surface:

- Phase 28 — per-voice polyphony and 5 locked articulation envelopes (staccato / legato / accent / marcato / tenuto), with multi-track MIDI export and voice-pool allocation (default 32).
- Phase 29 — sampled tonal instruments (piano, brass, sax, strings, flute, bell) via a 3 MB CC-BY 4.0 University-of-Iowa MIS bundle.
- Phase 30 — self-contained `flow` Linux x64 binary + 11-subcommand CLI + `install.sh` + XDG config (`~/.config/flow/config.toml`) + bidirectional MIDI↔Flow round-trip.
- Phase 31 — LSP polish (4 closed gaps, semantic tokens) + JetBrains plugin scaffolding.
- Phase 32 — full Scala (`.scl`) microtonal tuning loader + `tuning t { ... }` musical-context block.
- Phase 33 — SFZ orchestral sampler (`use "@sfz"` opt-in; 19-entry GM dict; 14-opcode common-subset parser; 441-frame equal-power crossfade), with VSCO Community CE 1.1.0 as the blessed external library.
- Phase 34 — the headline artifact: a curated ~60s symphony rendered entirely from Flow source through the SFZ sampler against VSCO-CE.

<paragraph 3: listen + try + source>
Listen to the showcase: <SYMPHONY-TITLE> — ~60 seconds for 5 orchestral instruments (violin, cello, flute, horn, timpani), rendered entirely from `examples/symphony/symphony.flow`. [Audio + source links](https://github.com/OWNER/REPO#showcase). Try it yourself: [Install (Linux x64)](https://github.com/OWNER/REPO#install-linux-x64). Source: [github.com/OWNER/REPO](https://github.com/OWNER/REPO).
```

Substitute `OWNER/REPO` with the resolved repo path from step 2 (e.g. `noahfreelove/flow-sharp`). Substitute `<SYMPHONY-TITLE>` with the composer-signed-off working title (e.g. *In Five Voices* in italics).

Rules:
- NO emoji (CLAUDE.md Conventions).
- Single H1 + 3 paragraph H2-free structure (per D-603 "3 paragraphs Markdown").
- Composer voice: warm + plain — NOT marketing-speak (think the Reddit-r/programming style; not Product Hunt).
- ASCII only.
- ~30 lines total (D-603 sizing).
- Pre-public-language SCRUBBED: do not call Flow a "personal project" or "prototype" or "experimental" — v1.4 is the public release.

Commit:
- Subject: `docs(34-04): draft v1.4.0 public announcement`
- Body: 2-3 lines naming the 3-paragraph shape and noting plan 34-05 will read this file via `gh release create --notes-file`.
- Standard `Co-Authored-By` trailer.
  </action>
  <verify>
    <automated>test -f docs/announcements/v1.4.0.md &amp;&amp; test -d docs/announcements &amp;&amp; grep -q '^# Flow v1.4' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 28' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 29' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 30' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 31' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 32' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 33' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'Phase 34' docs/announcements/v1.4.0.md &amp;&amp; grep -q 'symphony.flow' docs/announcements/v1.4.0.md &amp;&amp; grep -Eq 'https://github.com/[^/]+/[^/)]+' docs/announcements/v1.4.0.md &amp;&amp; ! grep -Eq '[A-Z]+: |TODO|FIXME|TBD|<user>|<repo>' docs/announcements/v1.4.0.md &amp;&amp; ! grep -Eq '😀|🎵|🎉|🚀' docs/announcements/v1.4.0.md &amp;&amp; git log -1 --pretty=format:%s docs/announcements/v1.4.0.md | grep -Eq 'docs\(34-04\)'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/announcements/v1.4.0.md` exists with ≥ 20 lines.
    - Directory `docs/announcements/` is the parent (verified by `test -d`).
    - H1 starts with `# Flow v1.4` — verified by grep.
    - All 7 phase names referenced at least once (Phase 28 through Phase 34) — verified by grep.
    - References `symphony.flow` (the source link).
    - Contains at least one `https://github.com/OWNER/REPO`-style URL with the actual resolved owner/repo (NOT `<user>` / `<repo>` placeholder).
    - No placeholder tokens `TBD` / `TODO` / `FIXME` / `<user>` / `<repo>` remain — verified by the negative-grep.
    - No emoji characters present — verified by negative-grep on common emoji.
    - Latest commit subject on the file starts with `docs(34-04)`.
  </acceptance_criteria>
  <done>Public announcement draft committed to git at docs/announcements/v1.4.0.md. Plan 34-05 will use this file as the `--notes-file` argument for `gh release create`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-fs ↔ git history (public repo) | docs/announcements/v1.4.0.md will be public once committed; will also appear verbatim as the GitHub Release v1.4.0 body. Composer reviews the staged diff before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-NONE | — | docs/announcements/v1.4.0.md | accept | Pure prose markdown; no secrets, no untrusted input, no executable surface. Severity: none. |
</threat_model>

<verification>
- File exists at docs/announcements/v1.4.0.md.
- 3-paragraph structure + 7 phase references present.
- No emoji, no placeholder tokens.
- Plan 34-05 can read this file as `--notes-file` input.
</verification>

<success_criteria>
- `docs/announcements/v1.4.0.md` committed to git as a clean single new-file commit.
- File is ready to serve as both the per-platform adaptation source for the composer AND the GitHub Release body for plan 34-05.
</success_criteria>

<output>
Create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-04-SUMMARY.md` when done.
</output>
