---
phase: 34-symphony-showcase-v1-4-closer-pre-public-public-pivot
plan: 06
type: execute
wave: 6
depends_on: [34-03, 34-05]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - CLAUDE.md
  - .gitignore
autonomous: true
requirements: [SYM-05]
must_haves:
  truths:
    - "D-901 plan 34-06: v1.4 milestone closure docs — `PROJECT.md` Current State flipped to 'Shipped: v1.4 (YYYY-MM-DD)'; `ROADMAP.md` Phase 34 row marked Complete; `STATE.md` reset for next milestone; `REQUIREMENTS.md` v1.4 entries marked Complete; `.planning/MILESTONES.md` v1.4 entry added; `CLAUDE.md` 'Public as of v1.4' footnote added under § 'Goals'."
    - "Claude's Discretion 'CLAUDE.md Public as of v1.4 footnote text': single line under § Goals: `> **Note:** Flow is public as of v1.4 (2026-XX-XX). The pre-public scope-creep-without-deprecation latitude (project_pre_public_no_legacy_burden) no longer applies; breaking changes now go through a deprecation cycle.`"
    - "Claude's Discretion 'PROJECT.md Current State wording': `**Shipped:** v1.4 Audio Fidelity, Distribution & Public Showcase (YYYY-MM-DD)` replacing the current 'Shipped: v1.2 / In progress: v1.3' lines. Add single-line 'Next milestone: TBD — see `.planning/MILESTONES.md`' pointer."
    - "Memory file `~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md` body rewritten per RESEARCH Priority 8 + Claude's Discretion — flips from 'breaking changes are cheap' framing to 'breaking changes ship through deprecation'. YAML frontmatter (`name`/`description`/`type`/`originSessionId`) preserved verbatim per RESEARCH Assumption A5."
    - "D-903: this plan MUST run AFTER plan 34-05 (release tag exists) AND plan 34-03 (README showcase section landed). Closure docs link to both the release URL and the README showcase section."
    - "PATTERNS § 7: PROJECT.md gets a new collapsed `<details>` v1.4 block ABOVE the existing v1.2 block, mirroring the established v1.2 + v1.1 + v1.0 collapsed-summary pattern."
    - "PATTERNS § 8: ROADMAP.md edits are confined to (a) the milestones list line for v1.4 flipped from 🚧 to ✅, (b) the Phase 34 progress-table row line 456 flipped from `0/N Spec pending` to `6/6 Complete YYYY-MM-DD`, (c) Phase 34 section's `Plans: TBD` replaced with the 6-plan listing."
    - "PATTERNS § 10: REQUIREMENTS.md gets a new `## v1.4 Phase 34 — Symphony Showcase` cross-insert section after the existing Phase 33 cross-insert (line 253ish), before `## Notes`. SYM-01..05 table with status `Shipped <commit-hash>`."
    - "PATTERNS § 11: MILESTONES.md gets a new `## v1.4 Audio Fidelity, Distribution & Public Showcase — Shipped YYYY-MM-DD` entry inserted as the NEW topmost milestone, ABOVE the current v1.2 entry. Existing v1.2/v1.1/v1.0 entries preserved verbatim below."
    - "Defensive .gitignore rule: `examples/symphony/*.wav` and `examples/symphony/*.mp3` patterns added to prevent composer accidentally committing future renders (D-502 enforcement — preventative)."
  artifacts:
    - path: .planning/PROJECT.md
      provides: "Current State flipped to v1.4 shipped; collapsed v1.4 details block added above v1.2."
      contains: 'v1.4'
    - path: .planning/ROADMAP.md
      provides: "Phase 34 progress row marked Complete; v1.4 milestone flipped ✅; Phase 34 plan list expanded from TBD to 6 entries."
      contains: '6/6'
    - path: .planning/STATE.md
      provides: "Frontmatter status flipped to shipped; stopped_at = Phase 34 complete; next milestone pointer."
      contains: 'shipped'
    - path: .planning/REQUIREMENTS.md
      provides: "New `## v1.4 Phase 34 — Symphony Showcase` cross-insert with SYM-01..05 status table."
      contains: 'SYM-01'
    - path: .planning/MILESTONES.md
      provides: "New v1.4 milestone entry above v1.2 with full Stats/Delivered/Key-accomplishments/Patterns shape."
      contains: 'v1.4 Audio Fidelity'
    - path: CLAUDE.md
      provides: "Public as of v1.4 footnote under § Goals; one-line Symphony showcase reference under § Music-Specific."
      contains: 'Public as of v1.4'
    - path: .gitignore
      provides: "Defensive ignore for examples/symphony/*.{wav,mp3} preventing future render-commit footguns."
      contains: 'examples/symphony/'
  key_links:
    - from: .planning/PROJECT.md
      to: .planning/MILESTONES.md
      via: "Current State pointer 'Next milestone: TBD — see `.planning/MILESTONES.md`'"
      pattern: '\.planning/MILESTONES\.md'
    - from: .planning/REQUIREMENTS.md
      to: GitHub Release v1.4.0
      via: "SYM-05 row references the v1.4.0 release URL"
      pattern: 'v1.4.0'
    - from: .planning/MILESTONES.md
      to: examples/symphony/symphony.flow + GitHub Release v1.4.0
      via: "Phase 34 accomplishment bullet links to the symphony source and the v1.4.0 release URL"
      pattern: 'symphony'
    - from: CLAUDE.md
      to: memory/project_pre_public_no_legacy_burden.md
      via: "Footnote names the memory file that no longer applies post-v1.4"
      pattern: 'project_pre_public_no_legacy_burden'
---

<objective>
Land the v1.4 milestone closure docs in a single coordinated commit — PROJECT.md / ROADMAP.md / STATE.md / REQUIREMENTS.md / .planning/MILESTONES.md / CLAUDE.md flipped to v1.4-shipped + the memory file `project_pre_public_no_legacy_burden.md` rewritten to reflect Flow's post-public footing + a defensive `.gitignore` rule preventing future symphony-render commit footguns.

This is the bookkeeping plan that gates the next milestone discussion: after this lands, `/gsd-new-milestone` is the natural follow-up.

Purpose: SYM-05 (v1.4 milestone closure: ROADMAP/STATE/REQUIREMENTS/MILESTONES/PROJECT marked complete; public release tag referenced; "Public as of v1.4" footnote landed).

Output: 7 modified files (5 .planning/* + CLAUDE.md + .gitignore) + 1 modified external memory file (preserved per RESEARCH Priority 8 mechanics). Single coordinated commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-05-SUMMARY.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-03-SUMMARY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .planning/PROJECT.md + ROADMAP.md + STATE.md (top-level milestone state)</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (current — locate "Current State" lines and the existing v1.2/v1.1/v1.0 collapsed `<details>` blocks at lines 43-64 per PATTERNS § 7)
    - .planning/ROADMAP.md (current — locate (a) v1.4 milestone line in milestones list, (b) Phase 34 row in Progress table line 456, (c) Phase 34 section "Plans: TBD" line 415)
    - .planning/STATE.md (current — locate frontmatter status/stopped_at/last_updated/progress fields)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 7 + § 8 + § 9 (PROJECT/ROADMAP/STATE patterns)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-05-SUMMARY.md (v1.4.0 release URL for cross-references)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-03-SUMMARY.md (README showcase section URL fragment for cross-references)
  </read_first>
  <action>
Apply three coordinated edits per PATTERNS analogs:

**PROJECT.md edits** (per PATTERNS § 7 + Claude's Discretion):
1. Replace the "Current State" block (current lines 11-16 — `**Shipped:** v1.2` / `**In progress:** v1.3` form per PATTERNS) with:
   ```
   ## Current State

   **Shipped:** v1.4 Audio Fidelity, Distribution & Public Showcase (<TODAY-ISO>)

   **Next milestone:** TBD — see `.planning/MILESTONES.md`
   ```
   Substitute `<TODAY-ISO>` with today's date in ISO YYYY-MM-DD format.
2. Insert a NEW `<details>` block ABOVE the existing v1.2 `<details>` block (matching the v1.2 entry's shape at PROJECT.md:43-54 verbatim, scaled for v1.4 content):
   ```
   <details>
   <summary>v1.4 Audio Fidelity, Distribution & Public Showcase (shipped <TODAY-ISO>)</summary>

   Delivered: per-voice polyphony + Phase 28 articulation envelopes (staccato/legato/accent/marcato/tenuto), Phase 29 sampled tonal instruments (piano/brass/sax/strings/flute/bell), Phase 30 self-contained `flow` CLI binary + install + XDG config, Phase 31 LSP polish + JetBrains plugin scaffolding, Phase 32 full Scala (`.scl`) microtonal tuning loader, Phase 33 SFZ orchestral sampler (blessed: VSCO Community CE 1.1.0), Phase 34 curated symphony showcase ("<TITLE>") as the v1.4 closer + pre-public → public pivot.

   - <N> requirements Complete across the v1.4 phase set + SYM-01..05
   - <M> plans across Phases 28-34
   - Release: [v1.4.0](https://github.com/OWNER/REPO/releases/tag/v1.4.0)
   - See: `.planning/MILESTONES.md` and the per-phase SUMMARYs under `.planning/phases/`

   </details>
   ```
   Substitute `<TITLE>` with composer's symphony title; substitute `<N>` and `<M>` with the actual requirement + plan counts (resolved via grep/wc on REQUIREMENTS.md + planning state).

**ROADMAP.md edits** (per PATTERNS § 8):
1. Milestones-list line for v1.4: flip the `🚧` marker to `✅` and append ` (shipped <TODAY-ISO>)` to the description. Pattern (from PATTERNS): `✅ **v1.4 Audio Fidelity, Distribution & Public Showcase** — Phases 28-34 (shipped <TODAY-ISO>)`.
2. Progress table row for Phase 34 (line 456 in the file): change `| 34. Symphony Showcase (v1.4 closer) | v1.4 | 0/N | Spec pending | - |` to `| 34. Symphony Showcase (v1.4 closer) | v1.4 | 6/6 | Complete | <TODAY-ISO> |`.
3. Phase 34 section (line 415): replace `**Plans**: TBD` with the 6-plan listing:
   ```
   **Plans**:
   - [x] 34-01-PLAN.md — Symphony composition + iterative composer UAT
   - [x] 34-02-PLAN.md — Commit canonical symphony.flow + expand examples/symphony/README.md
   - [x] 34-03-PLAN.md — Top-level README.md ## Showcase section + user-attachments inline player
   - [x] 34-04-PLAN.md — docs/announcements/v1.4.0.md public announcement draft
   - [x] 34-05-PLAN.md — v1.4.0 annotated tag + GitHub Release with 3 assets
   - [x] 34-06-PLAN.md — v1.4 milestone closure docs (this plan)
   ```

**STATE.md edits** (per PATTERNS § 9):
1. Frontmatter flips:
   - `status: ready_to_plan` → `status: shipped`
   - `stopped_at:` line → `stopped_at: Phase 34 complete (6/6) — v1.4 shipped <TODAY-ISO>`
   - `last_updated:` → today's ISO timestamp
   - `last_activity:` → today's ISO date
   - `progress:` block — bump `completed_phases` by 1 (the count includes Phase 34); bump `total_plans` and `completed_plans` to reflect plan 34-06 completion; set `percent: 100` (all v1.4 phases now complete).
2. Body updates (per PATTERNS):
   - Add a `### Phase 34 highlights` section near the Phase 33 highlights section summarizing: the In Five Voices symphony render via VSCO-CE, the v1.4.0 GitHub Release with MP3+WAV+binary assets, the docs/announcements/v1.4.0.md draft, the top-level README ## Showcase section with inline user-attachments player, the milestone closure flip.
   - Update "Current Position" body to "v1.4 shipped <TODAY-ISO>; next milestone TBD pending `/gsd-new-milestone` invocation."
   - Update "Resume Instructions" to a brief "v1.4 closed. Next session starts with `/gsd-new-milestone` to discuss the v1.5+ direction."

Substitute `OWNER/REPO` from `git remote get-url origin` and `<TODAY-ISO>` everywhere they appear above.

Do NOT touch other sections (Performance Metrics, Out of Scope, Constraints, Requirements Validated list — those are stable).
  </action>
  <verify>
    <automated>grep -q '\*\*Shipped:\*\* v1.4' .planning/PROJECT.md &amp;&amp; grep -q 'Next milestone' .planning/PROJECT.md &amp;&amp; grep -Eq '<summary>v1.4 ' .planning/PROJECT.md &amp;&amp; grep -Eq '\| 34\. Symphony Showcase.*\| v1\.4 \| 6/6 \| Complete' .planning/ROADMAP.md &amp;&amp; grep -Eq '34-01-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -Eq '34-06-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -Eq '^status: shipped' .planning/STATE.md &amp;&amp; grep -q 'Phase 34 complete' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - PROJECT.md has `**Shipped:** v1.4` in Current State.
    - PROJECT.md has a new `<summary>v1.4 ` collapsed details block (verified by grep).
    - PROJECT.md has `Next milestone:` pointer.
    - ROADMAP.md Phase 34 row reads `6/6 | Complete | <TODAY-ISO>` (regex verified).
    - ROADMAP.md Phase 34 section lists all 6 plan files (34-01 through 34-06) with `[x]` checkboxes.
    - ROADMAP.md v1.4 milestones-list line uses `✅` instead of `🚧`.
    - STATE.md frontmatter has `status: shipped` (verified by grep on frontmatter line).
    - STATE.md body contains `Phase 34 complete`.
  </acceptance_criteria>
  <done>Top-level milestone state files (PROJECT/ROADMAP/STATE) reflect v1.4 shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/REQUIREMENTS.md + .planning/MILESTONES.md (requirements + history ledgers)</name>
  <files>.planning/REQUIREMENTS.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate the existing v1.4 Phase 30 cross-insert section at lines 204-222 and v1.4 Phase 33 cross-insert at lines 226-253 — these are the exact templates per PATTERNS § 10)
    - .planning/MILESTONES.md (locate v1.2 entry at lines 7-52 — the template per PATTERNS § 11)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-05-SUMMARY.md (release URL + commit SHAs for status references)
    - All 6 plan SUMMARY files (34-01 through 34-06) for commit-hash extraction.
  </read_first>
  <action>
**REQUIREMENTS.md edits** (per PATTERNS § 10):

Add a NEW cross-insert section after the existing Phase 33 cross-insert (after line 253), before `## Notes`. Mirror the Phase 33 cross-insert shape verbatim:

```
---

## v1.4 Phase 34 — Symphony Showcase (v1.4 closer — pre-public → public pivot)

Phase 34 ships the v1.4 headline artifact — a curated ~60 s minimalist-orchestral symphony for 5 VSCO-CE instruments rendered through the Phase 33 SFZ surface — plus the public-facing release machinery (v1.4.0 tag, GitHub Release with MP3 + WAV + Linux binary, top-level README showcase section, announcement draft) and v1.4 milestone closure docs.

REQ-IDs map 1:1 to the 5 ROADMAP Phase 34 success criteria, formalized as SYM-01..05.

| SPEC | Phase | Status |
|------|-------|--------|
| SYM-01 (Symphony renders end-to-end via SFZ sampler, two-run cmp-clean) | Phase 34 | Shipped <34-01-SHA> + <34-02-SHA> |
| SYM-02 (Composer "postable on GitHub" sign-off recorded in 34-HUMAN-UAT.md) | Phase 34 | Shipped <34-01-SHA> |
| SYM-03 (Code paired with audible features: articulation, polyphony, voicePool) | Phase 34 | Shipped <34-02-SHA> |
| SYM-04 (README.md showcase + user-attachments audio embed + examples/symphony/README.md reproduction) | Phase 34 | Shipped <34-02-SHA> + <34-03-SHA> |
| SYM-05 (v1.4.0 tag + GitHub Release + announcement draft + milestone closure) | Phase 34 | Shipped <34-04-SHA> + <34-05-SHA> + <34-06-SHA> |

Two-run byte-identical determinism contract (Phase 18/25/27/33 inheritance) preserved end-to-end through the real VSCO-CE library — verified manually by composer at release time per D-702.

Release: https://github.com/OWNER/REPO/releases/tag/v1.4.0

---
```

Substitute the actual commit SHAs from each plan's SUMMARY.md. For 34-06 (this plan), use the literal string `<34-06-SHA-pending>` in the initial commit; the SHA is unknown until the commit lands. After the commit lands, run a follow-up `git commit --amend` is NOT allowed per CLAUDE.md amendment-avoidance — instead leave the placeholder for honesty and note in the SUMMARY that the SHA resolves on commit (or use a separate trailing fix-up commit if the composer prefers).

Substitute `OWNER/REPO` from `git remote get-url origin`.

**MILESTONES.md edits** (per PATTERNS § 11):

Add a NEW entry as the topmost milestone (above current v1.2 entry at line 7), mirroring the v1.2 entry shape verbatim:

```
## v1.4 Audio Fidelity, Distribution & Public Showcase — Shipped <TODAY-ISO>

**Goal:** Ship the v1.4 audio-fidelity rewrite (per-voice polyphony + articulation envelopes + sampled tonal instruments), the distribution wedge (self-contained `flow` CLI + install + XDG config + MIDI↔Flow round-trip), LSP polish + JetBrains plugin scaffolding, full Scala (`.scl`) microtonal tuning loader, SFZ orchestral sampler (blessed library: VSCO Community CE 1.1.0), and the curated symphony showcase as the milestone closer — flipping Flow from pre-public to public.

**Delivered:** [3-5 sentences summarizing each Phase 28-34 delivery, mirroring v1.2 entry's "Delivered" shape; extract from the per-phase 33-VERIFICATION.md and earlier SUMMARYs.]

**Stats:**
- Phases: 7 (Phase 28 – Phase 34)
- Plans: <ACTUAL-COUNT> across all v1.4 phases (sum of completed_plans from each phase's SUMMARY or grep `\\.planning/phases/[23]\\d-` for *-PLAN.md files)
- Requirements: all v1.4 SPEC-* + REQ-* + SYM-01..05 marked Complete in REQUIREMENTS.md
- Git range: post-`v1.3` tag → `v1.4.0` tag (~5 weeks, dates extracted from `git log v1.3.0..v1.4.0 --format=%aI | head -1` / `tail -1`)
- Source files at close: <FILE-COUNT from `git ls-files | wc -l`>

**Key accomplishments:**
1. Phase 28 — articulation system (5 tokens: staccato / legato / accent / marcato / tenuto, locked envelope rules) + per-voice polyphony (voicePool default 32) + multi-track MIDI export
2. Phase 29 — sampled tonal instruments (piano + brass + sax + strings + flute + bell via SampledInstrumentRenderer, ≤5 MB CC-BY 4.0 University-of-Iowa MIS bundle)
3. Phase 30 — `flow` self-contained Linux x64 binary (~40 MB) + 11-subcommand CLI + install.sh + XDG config + MIDI↔Flow round-trip ±1 tick
4. Phase 31 — LSP polish (4 closed gaps) + JetBrains plugin scaffolding (stretch goal MET)
5. Phase 32 — full Scala (`.scl`) tuning loader + `tuning t { ... }` musical-context block, ±0.1¢ Carlos Alpha / Bohlen-Pierce, canonical fixtures shipped
6. Phase 33 — SFZ orchestral sampler (`use "@sfz"` gated; 19-entry GM dict; 14-opcode parser; 441-frame equal-power crossfade)
7. Phase 34 — symphony showcase ("<TITLE>") + v1.4.0 GitHub Release + public announcement + pre-public → public pivot ([release link](https://github.com/OWNER/REPO/releases/tag/v1.4.0))

**Patterns established:**
- Two-run cmp-clean determinism contract (replacing pre-Phase-28 pinned-bytes)
- HUMAN-UAT.md for subjective composer sign-off (Phase 17 + Phase 33 + Phase 34 precedent)
- Per-instrument render + sum-mix pattern for orchestral pieces (Phase 33 + Phase 34)
- GitHub user-attachments drag-drop for inline audio player in README (Phase 34 novel)

**Known deferred items at close:** [list any open items from any v1.4 phase's VERIFICATION.md / HUMAN-UAT.md; if none, write "None — all v1.4 phases closed clean."]

**Forward-deferred items (v1.5+ candidates):**
- Stereo panning across instruments (SfzRenderer stereo retrofit OR hand-stereo via dual-render-and-pan)
- A second showcase: jazz / EDM / other contrasting genre
- SFZ round-robin opcode parser extension (`seq_position` / `seq_length`)
- Per-articulation envelope multipliers for sampled instrument path (Phase 29 follow-up)
- Sampled drums with transient-preserving pitch shift (Phase 29 follow-up)
- More flute samples to close the D5 timbre-crossover gap (Phase 29 v1.5 backlog)
- GitHub-rendered video screen-recording demo
- `flow showcase` CLI subcommand
- Per-articulation A/B fixture as permanent example (`examples/symphony/symphony_no_articulation.flow`)

**Archives:**
- `.planning/phases/{28-34}/` (per-phase planning artifacts)
- `examples/symphony/symphony.flow` + `examples/symphony/sfz_smoke.flow` (canonical Flow source)
- GitHub Release: https://github.com/OWNER/REPO/releases/tag/v1.4.0
```

Preserve all existing entries below this new one unchanged.
  </action>
  <verify>
    <automated>grep -q '^## v1.4 Phase 34 — Symphony Showcase' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| SYM-01 ' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| SYM-02 ' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| SYM-03 ' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| SYM-04 ' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| SYM-05 ' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'releases/tag/v1.4.0' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^## v1.4 Audio Fidelity, Distribution &amp; Public Showcase' .planning/MILESTONES.md &amp;&amp; grep -Eq 'Phase 28 — articulation' .planning/MILESTONES.md &amp;&amp; grep -Eq 'Phase 34 — symphony' .planning/MILESTONES.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains a new `## v1.4 Phase 34 — Symphony Showcase` section.
    - All 5 SYM-* rows (SYM-01 through SYM-05) present in the new section's status table.
    - REQUIREMENTS.md references the v1.4.0 release URL (`releases/tag/v1.4.0`).
    - MILESTONES.md contains a new `## v1.4 Audio Fidelity, Distribution & Public Showcase` entry as the topmost milestone (verified by `head -50 .planning/MILESTONES.md | grep -q 'v1.4 Audio'`).
    - MILESTONES.md v1.4 entry mentions all 7 phases (Phase 28 through Phase 34) in the Key Accomplishments numbered list.
    - Existing v1.2 / v1.1 / v1.0 entries preserved unchanged below the new entry.
  </acceptance_criteria>
  <done>REQUIREMENTS.md + MILESTONES.md reflect v1.4 closure with full Phase 34 cross-insert + milestone-history entry.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md + .gitignore (project instructions + defensive ignore)</name>
  <files>CLAUDE.md, .gitignore</files>
  <read_first>
    - CLAUDE.md (locate § "Goals" — typically lines 11-19 — and § "Music-Specific Language Features" — locate the Phase 33 SFZ paragraph at the end of the locked-rules cluster)
    - .gitignore (verify what's already covered; usually `*.wav` is covered globally — but `examples/symphony/*.wav` may not be specifically called out)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 6 (CLAUDE.md two-insertion pattern)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md Claude's Discretion "CLAUDE.md 'Public as of v1.4' footnote text"
  </read_first>
  <action>
**CLAUDE.md edits** (per PATTERNS § 6):

Insertion 1 — under § "Goals", immediately after the 3rd Goals bullet ("Make the easy cases fast.") and BEFORE the blank line preceding `**Non-Goals**`:

```
> **Note:** Flow is public as of v1.4 (<TODAY-ISO>). The pre-public scope-creep-without-deprecation latitude (`project_pre_public_no_legacy_burden`) no longer applies; breaking changes now go through a deprecation cycle.
```

Insertion 2 — under § "Music-Specific Language Features", appended as a one-line paragraph at the END of the Phase 33 SFZ paragraph cluster (locate the line ending `v1.4 Phase 34 symphony showcase is the downstream consumer (Phase 33)`):

```
- **Symphony showcase:** `examples/symphony/symphony.flow` — see `README.md` § "Showcase". The v1.4 headline artifact rendering 5 VSCO-CE instruments through the Phase 33 SFZ surface; ~60 s ABA single-movement piece in <KEY> (Phase 34).
```

Substitute `<TODAY-ISO>` with today's ISO date. Substitute `<KEY>` with the actual key the composer settled on (read from `examples/symphony/symphony.flow`'s `key NAME { ... }` line — likely D minor per D-104).

DO NOT touch any other CLAUDE.md section. Specifically per PATTERNS § 6 NOT-CHANGE list:
- DO NOT touch § "Music Types Quick Reference" (no new music type from Phase 34).
- DO NOT touch § "Conventions" (two-run cmp-clean already documented Phase 28).
- DO NOT touch § "Locked articulation rules" or § "Multi-track MIDI export" or any other Phase 28 paragraph.

**.gitignore edits** (defensive — D-502 enforcement preventative per plan 34-02 Task 1 directive):

Add these two lines to `.gitignore` if not already present:
```
# Phase 34 D-502: never commit rendered symphony output into examples/symphony/
examples/symphony/*.wav
examples/symphony/*.mp3
```

Place them in a sensible location (near other `examples/` rules or under an "Audio output" comment block if one exists). Run `git check-ignore examples/symphony/test.wav` afterward to confirm the rule matches.
  </action>
  <verify>
    <automated>grep -q 'Public as of v1.4' CLAUDE.md &amp;&amp; grep -q 'project_pre_public_no_legacy_burden' CLAUDE.md &amp;&amp; grep -q 'Symphony showcase:.*examples/symphony/symphony.flow' CLAUDE.md &amp;&amp; grep -Eq '^examples/symphony/\*\.wav' .gitignore &amp;&amp; grep -Eq '^examples/symphony/\*\.mp3' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "Public as of v1.4" — verified by grep.
    - CLAUDE.md contains "project_pre_public_no_legacy_burden" reference (the memory file name) — verified by grep.
    - CLAUDE.md contains "Symphony showcase:" line under § "Music-Specific Language Features" — verified by grep.
    - `.gitignore` contains `examples/symphony/*.wav` line.
    - `.gitignore` contains `examples/symphony/*.mp3` line.
    - `git check-ignore examples/symphony/foo.wav` exits 0 (rule matches a hypothetical render).
    - No other CLAUDE.md sections changed beyond the 2 insertions — verified by `git diff CLAUDE.md` showing only addition lines in the right locations.
  </acceptance_criteria>
  <done>CLAUDE.md has the v1.4 public footnote + Symphony showcase reference; .gitignore defensively blocks future symphony renders.</done>
</task>

<task type="auto">
  <name>Task 4: Rewrite memory file project_pre_public_no_legacy_burden.md</name>
  <files>~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md</files>
  <read_first>
    - ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md (current — preserve YAML frontmatter `name`/`description`/`type`/`originSessionId` verbatim per RESEARCH Priority 8 + Assumption A5)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Priority 8 (Memory File Update Mechanics)" — full procedure
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 12 (memory file body-rewrite shape)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md Claude's Discretion (memory file body content per CONTEXT)
  </read_first>
  <action>
Read the existing memory file first (it lives outside the repo at `~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md`). Use the `Read` tool with the absolute path expanded.

Preserve the YAML frontmatter EXACTLY — the 4 fields (`name`, `description`, `type`, `originSessionId`) — only the `description` and `name` fields may be updated to reflect the flip. Per PATTERNS § 12, the safe update flips:
- `name: Flow is pre-public — no legacy compatibility burden` → `name: Flow is public as of v1.4`
- `description: No external users have written code in Flow yet. ...` → `description: Flow shipped publicly at v1.4 (<TODAY-ISO>). Breaking changes require deprecation windows.`
- `type: project` → unchanged
- `originSessionId: <UUID>` → unchanged (RESEARCH A5 confirms set-once)

Then rewrite the body prose with content drawn from PATTERNS § 12's "Body rewrite per CONTEXT D-901 plan 34-06 Claude's Discretion + RESEARCH Priority 8" template — see the full template in PATTERNS § 12 lines 605-645. The body should:
- State that Flow went public with v1.4.0 on <TODAY-ISO>.
- List the 4 public artifacts (CLI binary on Releases, announcement, symphony, v1.4.0 tag) with concrete `<URL>` references where possible (use the release URL from plan 34-05's SUMMARY).
- State how to apply the new rule post-v1.4: breaking changes through deprecation cycle, migration tools cover external `.flow` files, parser errors hint at migrations, builtin renames get `// DEPRECATED` markers, semver discipline.
- Preserve the historical pre-public latitude as context (do NOT delete — keep as "Original pre-public latitude (preserved for historical reference)" paragraph at the end).

Substitute `<TODAY-ISO>` and the actual release URL from plan 34-05's SUMMARY.

Use the Write tool (not Edit) to do the rewrite — this is a wholesale body replacement per PATTERNS § 12's "direct Write overwrite preserving YAML frontmatter verbatim" pattern. Make sure to preserve the YAML frontmatter delimiters (`---` at lines 1 + N).

After the file is written:
1. Verify the file is parseable: `head -10 ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md` should show YAML frontmatter intact.
2. Verify `grep -q 'public as of v1.4' ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md` — sanity-check the new content landed.
3. Verify `grep -q 'originSessionId' ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md` — frontmatter preserved.
  </action>
  <verify>
    <automated>test -f ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md &amp;&amp; head -10 ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md | grep -q '^---$' &amp;&amp; grep -q 'originSessionId' ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md &amp;&amp; grep -qi 'public as of v1.4\|v1.4.0' ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md &amp;&amp; grep -qi 'deprecation' ~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `~/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md`.
    - YAML frontmatter intact (verified by `head -10` returning the `---` delimiters + the 4 expected field names).
    - `originSessionId` field preserved verbatim (verified by grep).
    - Body contains "public as of v1.4" or "v1.4.0" reference (verified by grep — case-insensitive).
    - Body mentions "deprecation" (verified by grep — case-insensitive — confirms the new policy framing landed).
    - Body's `name:` field updated to reflect post-public framing (visible in YAML head).
  </acceptance_criteria>
  <done>Memory file rewritten to reflect Flow's post-public footing; YAML frontmatter + originSessionId preserved.</done>
</task>

<task type="auto">
  <name>Task 5: Commit all milestone closure changes in one coordinated commit</name>
  <files>(git commit — all files modified by Tasks 1-4)</files>
  <read_first>
    - All files modified by Tasks 1-4 (PROJECT.md / ROADMAP.md / STATE.md / REQUIREMENTS.md / MILESTONES.md / CLAUDE.md / .gitignore) — `git status` confirms the change set.
    - The memory file edit lives OUTSIDE the repo and is NOT included in this commit (it's external Claude state, not project source).
  </read_first>
  <action>
Stage exactly the 7 in-repo files modified by Tasks 1-3 (the memory file is external — not committed):
```
git add .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md .planning/MILESTONES.md CLAUDE.md .gitignore
```

Verify `git status` shows exactly these 7 files staged (no untracked + no unintended changes).

Commit with subject + body:
```
docs(34-06): close v1.4 milestone — Flow ships public

Phase 34 closure docs:
- PROJECT.md Current State flipped to "Shipped: v1.4 (<TODAY-ISO>)" + new v1.4 <details> block above v1.2.
- ROADMAP.md Phase 34 progress row marked 6/6 Complete; v1.4 milestone marker flipped to shipped; Phase 34 plan listing expanded from TBD.
- STATE.md frontmatter status = shipped; stopped_at + last_updated bumped; Phase 34 highlights body block added.
- REQUIREMENTS.md gains "## v1.4 Phase 34 — Symphony Showcase" cross-insert with SYM-01..05 status table.
- MILESTONES.md gains v1.4 entry as topmost milestone above v1.2 (full Stats/Delivered/Key-accomplishments shape).
- CLAUDE.md gains "Public as of v1.4" footnote under § Goals + Symphony showcase reference under § Music-Specific.
- .gitignore defensively blocks examples/symphony/*.{wav,mp3} (D-502 enforcement).

Memory file `~/.claude/projects/.../memory/project_pre_public_no_legacy_burden.md` rewritten externally per RESEARCH Priority 8 (not part of this commit — external Claude state).

Release: https://github.com/OWNER/REPO/releases/tag/v1.4.0

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Substitute `<TODAY-ISO>` and `OWNER/REPO`. Do NOT push (composer pushes separately).

After commit, run `git log -1 --stat` and confirm the file count is exactly 7 and all expected files appear.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:%s | grep -Eq 'docs\(34-06\)' &amp;&amp; git log -1 --pretty=format:%s | grep -q 'close v1.4 milestone' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^\.gitignore$' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^CLAUDE\.md$' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^\.planning/PROJECT\.md$' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^\.planning/ROADMAP\.md$' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^\.planning/STATE\.md$' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^\.planning/REQUIREMENTS\.md$' &amp;&amp; git diff HEAD~1 --name-only | sort -u | grep -Eq '^\.planning/MILESTONES\.md$'</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject starts with `docs(34-06)` and contains `close v1.4 milestone`.
    - Commit touches exactly the 7 expected files: `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONES.md`, `CLAUDE.md`, `.gitignore` (verified by `git diff HEAD~1 --name-only`).
    - No memory file appears in the commit (it lives outside the repo).
    - Commit body references the v1.4.0 release URL.
  </acceptance_criteria>
  <done>v1.4 milestone closure committed in a single coordinated commit. After push, the next session begins with `/gsd-new-milestone`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-fs ↔ git history | All 7 edited files commit to the public repo. Composer reviews `git diff` before pushing. |
| local Claude state ↔ memory dir | The memory file edit is external Claude state, not committed. Composer's local Claude installation reads the updated file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06-NONE | — | .planning/*.md + CLAUDE.md + .gitignore + memory file | accept | Internal planning artifacts + project convention notes + defensive ignore. No new attack surface. Severity: none. |
</threat_model>

<verification>
- All 7 in-repo files modified and committed in a single coordinated commit.
- Memory file rewritten externally (verified by automated grep + manual head inspection).
- v1.4 milestone is now closed in all 5 planning ledgers (PROJECT/ROADMAP/STATE/REQUIREMENTS/MILESTONES).
- CLAUDE.md reflects the v1.4 public flip.
- .gitignore defensively blocks future symphony renders.
</verification>

<success_criteria>
- 7 in-repo files committed in single `docs(34-06)` commit.
- v1.4 milestone closed across all 5 .planning/* ledgers.
- CLAUDE.md "Public as of v1.4" footnote landed; Symphony showcase reference landed.
- Memory file rewritten (external; verified separately).
- `.gitignore` defensively blocks symphony render commits.
- After this plan: `/gsd-new-milestone` is the natural next invocation for v1.5+ discussion.
</success_criteria>

<output>
Create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-06-SUMMARY.md` when done. SUMMARY should record: the 34-06 closure commit SHA, the canonical v1.4.0 release URL, links to the 6 plan SUMMARYs, and a one-line "v1.4 shipped — next milestone discussion via `/gsd-new-milestone`" pointer for the next session.
</output>
