---
phase: 34-symphony-showcase-v1-4-closer-pre-public-public-pivot
plan: 05
type: execute
wave: 4
depends_on: [34-02, 34-04]
files_modified: []
autonomous: false
requirements: [SYM-05]
must_haves:
  truths:
    - "D-503: rendered audio ships as GitHub Release assets attached to the v1.4.0 tag — `flow-symphony-v1.4.0.mp3` (~1.5 MB at 192 kbps CBR) + `flow-symphony-v1.4.0.wav` (~10 MB uncompressed). NOT committed to the repo."
    - "Claude's Discretion: 3 release assets total — MP3 + WAV + `flow-linux-x64.tar.gz` (the Phase 30 self-contained binary)."
    - "Claude's Discretion: tag is ANNOTATED (`git tag -a v1.4.0 -m \"...\"`), NOT lightweight — annotated tags ship release-note metadata to `git describe`."
    - "Claude's Discretion: tag the commit that lands plan 34-06 (milestone closure) — BUT plan 34-06 explicitly depends on this plan (34-05) per D-903 because the closure docs reference the release URL. So this plan tags the commit AT THE TIP of `dev` (which will be plan 34-04's announcement-draft commit), and plan 34-06 lands milestone closure AFTER the release exists. The intra-cycle ordering is: 34-04 commits announcement → 34-05 tags + creates release → 34-03 adds top-level README pointing at release → 34-06 commits milestone closure pointing at release URL. The v1.4.0 tag does NOT move; closure docs reference it without re-tagging."
    - "RESEARCH Priority 4: one-shot `gh release create v1.4.0 --title \"...\" --notes-file docs/announcements/v1.4.0.md --verify-tag <assets>` syntax with `#\"label\"` asset-label suffixes."
    - "RESEARCH Priority 9: direct commit on `dev` (no PR — `branching_strategy: none`). Tag `v1.4.0` lives on `dev`."
    - "RESEARCH Priority 4: `gh auth status` must succeed before `gh release create` — composer authenticated via prior `gh auth login`."
    - "D-803-style precedent: the release assets are externally hosted (GitHub CDN) — composer reviews `gh release view v1.4.0` after creation to confirm 3 assets are attached and the body renders correctly."
  artifacts:
    - path: /tmp/flow-symphony-v1.4.0.mp3
      provides: "192 kbps CBR MP3 encoded from examples/output/symphony.wav via ffmpeg libmp3lame. Uploaded as release asset; NOT committed."
    - path: /tmp/flow-symphony-v1.4.0.wav
      provides: "Uncompressed WAV copy of examples/output/symphony.wav, renamed for the release asset. Uploaded as release asset; NOT committed."
    - path: /tmp/flow-linux-x64.tar.gz
      provides: "Phase 30 self-contained Linux x64 binary tarballed. Uploaded as release asset; NOT committed."
  key_links:
    - from: v1.4.0 (annotated git tag)
      to: dev branch HEAD (post plan-34-04 commit)
      via: 'git tag -a v1.4.0 -m "..."'
      pattern: 'v1.4.0'
    - from: GitHub Release v1.4.0
      to: docs/announcements/v1.4.0.md
      via: 'gh release create --notes-file docs/announcements/v1.4.0.md'
      pattern: '--notes-file'
    - from: GitHub Release v1.4.0
      to: 3 asset URLs (MP3 + WAV + Linux tar.gz)
      via: 'gh release create v1.4.0 <asset paths>'
      pattern: 'gh release'
---

<objective>
Cut the annotated `v1.4.0` git tag on `dev`, then create the GitHub Release with the announcement-draft body and 3 release assets (symphony MP3 + symphony WAV + Phase 30 Linux binary tarball). This is the public-facing "v1.4 ships" moment — after this plan lands, the v1.4.0 release URL is live and downloadable; plans 34-03 (top-level README showcase section, depends on the release-asset URL existing) and 34-06 (milestone closure docs, references the release URL) follow.

Purpose: SYM-05 (v1.4.0 tag cut + GitHub Release published with assets ready for the showcase embed and announcement distribution).

Output:
1. Annotated git tag `v1.4.0` pushed to `origin`.
2. GitHub Release `v1.4.0` created with the announcement file as the body, the `--latest=true` default flag, and 3 labeled assets attached.
3. NO repo file changes — all artifacts live on GitHub. No commit on `dev`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-02-SUMMARY.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-04-SUMMARY.md
@docs/announcements/v1.4.0.md
@scripts/install.sh

<interfaces>
<!-- Environment commands (RESEARCH § Environment Availability — all verified present): -->
<!--   ffmpeg 7.1.1-1ubuntu4.2 (libmp3lame enabled) -->
<!--   gh CLI 2.46.0 -->
<!--   git system -->
<!--   tar (system) -->

<!-- gh release create one-shot syntax (RESEARCH Priority 4 verified): -->
<!--   gh release create v1.4.0 \ -->
<!--       --title "..." \ -->
<!--       --notes-file docs/announcements/v1.4.0.md \ -->
<!--       --verify-tag \ -->
<!--       file1#"Label" file2#"Label" file3#"Label" -->

<!-- Tag pattern (verified against prior tags v1.0.0, v1.1.0, v1.2.0, v1.3.0 — all annotated): -->
<!--   git tag -a v1.4.0 -m "v1.4 Audio Fidelity, Distribution & Public Showcase" -->
<!--   git push origin v1.4.0 -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Compose authenticates gh CLI + verifies release assets are ready</name>
  <files>(no file changes — preflight check)</files>
  <read_first>
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Priority 4" (gh CLI auth requirement)
    - examples/output/symphony.wav (the canonical render from plan 34-01 — must exist as the source for MP3 encoding)
    - docs/announcements/v1.4.0.md (the body source — must exist from plan 34-04)
  </read_first>
  <action>HUMAN CHECKPOINT — this is a manual verification step performed by the composer. See <how-to-verify> for the procedure and <acceptance_criteria> for the pass conditions. No automated action is taken by the executor; the executor pauses and waits for the resume-signal.</action>
  <verify>
    <human-check>Composer follows the procedure in &lt;how-to-verify&gt; and replies with the resume-signal text described in &lt;resume-signal&gt;.</human-check>
  </verify>
  <done>Composer provides resume-signal indicating acceptance criteria are satisfied (per &lt;acceptance_criteria&gt;).</done>
  <what-built>
This is a preflight checkpoint — no automation has run yet. The composer confirms the environment is ready BEFORE the release-creation steps fire.

Specifically:
- Plan 34-02 committed the canonical symphony.flow.
- Plan 34-04 committed docs/announcements/v1.4.0.md.
- The composer's local `examples/output/symphony.wav` is the post-UAT render from plan 34-01 (assumed still present; if not, re-render via `flow render examples/symphony/symphony.flow -o examples/output/symphony.wav`).
- The Phase 30 self-contained `flow` Linux binary exists locally (built via `dotnet publish flow-cli/flow-cli.csproj -c Release -r linux-x64 --self-contained` or already produced as part of the Phase 30 ship).
  </what-built>
  <how-to-verify>
Composer performs 4 checks before the autonomous release-creation tasks proceed:

1. **gh authentication:** `gh auth status` returns a green checkmark and lists the correct GitHub account (the one that owns the repo). If not, composer runs `gh auth login --web` and re-runs `gh auth status`. Confirm by visually inspecting the output.

2. **Required source files present:**
   - `test -f examples/output/symphony.wav && echo "WAV present"`
   - `test -f docs/announcements/v1.4.0.md && echo "announcement present"`
   - If either is missing, halt and re-render / re-commit.

3. **Tag does not already exist:** `git tag --list v1.4.0` returns EMPTY. If the tag already exists locally or on the remote (`git ls-remote --tags origin v1.4.0`), composer decides whether to (a) delete the existing tag (`git tag -d v1.4.0` + `git push origin :refs/tags/v1.4.0`) and retag, or (b) bump to v1.4.1 and update all references. Note: this plan assumes scenario (a) is acceptable for a re-execution; if v1.4.0 was already publicly published, do NOT delete the public tag (RESEARCH "Don't amend the v1.4.0 tag after publication") — open a discussion with composer first.

4. **Phase 30 binary available:** Either `which flow` returns a path (if installed system-wide via scripts/install.sh) OR `test -f flow-cli/bin/Release/net10.0/linux-x64/publish/flow` returns success (if built locally). If neither, run `dotnet publish flow-cli/flow-cli.csproj -c Release -r linux-x64 --self-contained -o /tmp/flow-bin/` first to produce the binary at `/tmp/flow-bin/flow`.
  </how-to-verify>
  <acceptance_criteria>
    - `gh auth status` output contains `Logged in to github.com` (composer's account, not a bot).
    - `test -f examples/output/symphony.wav` exits 0.
    - `test -f docs/announcements/v1.4.0.md` exits 0.
    - `git tag --list v1.4.0` produces no output (tag does not already exist) OR composer has confirmed the existing tag is safe to overwrite.
    - The Phase 30 self-contained `flow` binary is reachable by an absolute path the composer can name (e.g. `/usr/local/bin/flow` or `/tmp/flow-bin/flow` or `flow-cli/bin/Release/net10.0/linux-x64/publish/flow`).
  </acceptance_criteria>
  <resume-signal>Composer types "approved" and provides the absolute path to the `flow` binary (e.g. `/tmp/flow-bin/flow`). The autonomous tasks below will tarball that binary.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Encode the symphony MP3 + stage release assets in /tmp</name>
  <files>(release assets staged in /tmp — no repo files modified)</files>
  <read_first>
    - examples/output/symphony.wav (the canonical render from plan 34-01)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "MP3 encoding (composer-local one-shot)" + § "Priority 3 (MP3 Encoding Choice)"
    - The absolute path to the Phase 30 `flow` binary provided by composer in Task 1's resume-signal.
  </read_first>
  <action>
Produce the 3 release-asset files in `/tmp/` ready for upload:

1. **MP3 encode** (verbatim from RESEARCH Code Examples):
   ```bash
   ffmpeg -i examples/output/symphony.wav \
          -c:a libmp3lame \
          -b:a 192k \
          -y \
          /tmp/flow-symphony-v1.4.0.mp3
   ```
   Verify size is ≈1.0-2.0 MB (192 kbps × actual duration / 8 → typically 1.2-1.8 MB for a 60s piece).

2. **WAV copy**: `cp examples/output/symphony.wav /tmp/flow-symphony-v1.4.0.wav`. Verify size is ≈5-15 MB (uncompressed 44.1kHz 16-bit stereo or mono — depends on SfzRenderer output format).

3. **Linux binary tarball**: Tarball the binary directory the composer supplied in Task 1. If composer pointed at a single binary file (e.g. `/usr/local/bin/flow`), wrap it into a parent directory so the tarball extracts cleanly:
   ```bash
   mkdir -p /tmp/flow-linux-x64-stage/flow-linux-x64
   cp <COMPOSER-SUPPLIED-PATH> /tmp/flow-linux-x64-stage/flow-linux-x64/flow
   chmod +x /tmp/flow-linux-x64-stage/flow-linux-x64/flow
   # Also include README pointer + install hint
   printf '# Flow CLI (Linux x64, self-contained)\n\nMove `flow` onto your $PATH. See https://github.com/OWNER/REPO for source + docs.\n' > /tmp/flow-linux-x64-stage/flow-linux-x64/README.txt
   tar -czvf /tmp/flow-linux-x64.tar.gz -C /tmp/flow-linux-x64-stage flow-linux-x64
   ```
   Substitute the canonical `OWNER/REPO` from `git remote get-url origin` into the README.txt. Verify tarball size is ≈10-60 MB (Phase 30 self-contained binary is large).

After all 3 files exist in /tmp/, run `ls -lh /tmp/flow-symphony-v1.4.0.mp3 /tmp/flow-symphony-v1.4.0.wav /tmp/flow-linux-x64.tar.gz` and emit the sizes — these go into the release-asset display labels.

Do NOT commit anything; do NOT touch the repo. All staging is in `/tmp/`.
  </action>
  <verify>
    <automated>test -f /tmp/flow-symphony-v1.4.0.mp3 &amp;&amp; test -f /tmp/flow-symphony-v1.4.0.wav &amp;&amp; test -f /tmp/flow-linux-x64.tar.gz &amp;&amp; file /tmp/flow-symphony-v1.4.0.mp3 | grep -q 'Audio file' &amp;&amp; file /tmp/flow-symphony-v1.4.0.wav | grep -q 'WAVE audio' &amp;&amp; file /tmp/flow-linux-x64.tar.gz | grep -q 'gzip compressed'</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/flow-symphony-v1.4.0.mp3` exists and `file` identifies it as an audio file (libmp3lame output).
    - `/tmp/flow-symphony-v1.4.0.wav` exists and `file` identifies it as a WAVE audio file.
    - `/tmp/flow-linux-x64.tar.gz` exists and `file` identifies it as a gzip-compressed archive.
    - MP3 size is between 0.5 MB and 3 MB inclusive (sanity bound around the 1.5 MB target).
    - WAV size is between 3 MB and 30 MB inclusive (sanity bound around the ~10 MB target).
    - Tarball size is between 5 MB and 100 MB inclusive (sanity bound — Phase 30 self-contained .NET 10 binary is large).
  </acceptance_criteria>
  <done>3 release assets staged at /tmp/ paths ready for `gh release create`.</done>
</task>

<task type="auto">
  <name>Task 3: Create annotated v1.4.0 tag and push to origin</name>
  <files>(git tag — no repo file changes)</files>
  <read_first>
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md Claude's Discretion "v1.4.0 tag commit" (annotated, NOT lightweight)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Priority 9 (PR vs direct commit)" (direct commit on dev)
  </read_first>
  <action>
Create the annotated tag at the current `dev` HEAD (which is the commit landed by plan 34-04) and push it to `origin`:

```bash
git checkout dev    # ensure on dev; if branching policy changes, swap to main
git pull --rebase origin dev || true   # ensure local is up-to-date; allow failure if no remote changes
git tag -a v1.4.0 -m "v1.4 Audio Fidelity, Distribution & Public Showcase

Phases 28-34: per-voice polyphony + articulation envelopes (Phase 28),
sampled tonal instruments (Phase 29), self-contained CLI + install + XDG
config (Phase 30), LSP polish + JetBrains plugin scaffolding (Phase 31),
full Scala (.scl) microtonal tuning loader (Phase 32), SFZ orchestral
sampler with VSCO Community CE 1.1.0 (Phase 33), and the In Five Voices
symphony showcase (Phase 34) flipping Flow from pre-public to public."
git push origin v1.4.0
```

(The exact "In Five Voices" title comes from the composer-signed-off `examples/symphony/symphony.flow` top comment — substitute the composer's chosen title if different.)

Verify:
- `git tag --list v1.4.0` returns `v1.4.0`.
- `git show v1.4.0` shows the annotation body (annotated tags only — lightweight tags don't carry a body).
- `git ls-remote --tags origin v1.4.0` returns a hash matching the local tag's commit.

If the push fails (auth, network, or non-fast-forward), surface the exact stderr to composer.
  </action>
  <verify>
    <automated>git tag --list v1.4.0 | grep -q '^v1.4.0$' &amp;&amp; git for-each-ref refs/tags/v1.4.0 --format='%(objecttype)' | grep -q 'tag' &amp;&amp; git ls-remote --tags origin v1.4.0 | grep -q 'refs/tags/v1.4.0'</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v1.4.0` returns `v1.4.0`.
    - `git for-each-ref refs/tags/v1.4.0 --format='%(objecttype)'` returns `tag` (annotated; lightweight tags would return `commit`).
    - `git ls-remote --tags origin v1.4.0` returns a non-empty line (tag exists on remote).
    - `git log -1 --format='%s' v1.4.0` shows a meaningful commit subject (the commit being tagged is plan 34-04's announcement-draft commit).
  </acceptance_criteria>
  <done>v1.4.0 annotated tag pushed to origin. Ready for `gh release create --verify-tag`.</done>
</task>

<task type="auto">
  <name>Task 4: Create the GitHub Release with 3 labeled assets</name>
  <files>(GitHub Release — no repo file changes; release lives on GitHub)</files>
  <read_first>
    - docs/announcements/v1.4.0.md (the release body source — must exist from plan 34-04)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "GitHub Release creation" + "Priority 4 (gh CLI Release Create + Upload Mechanics)"
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § "Pattern G" (annotated tag + gh release with assets)
  </read_first>
  <action>
Create the GitHub Release in one shot via the verified RESEARCH Priority 4 command:

```bash
gh release create v1.4.0 \
    --title "v1.4 Audio Fidelity, Distribution & Public Showcase" \
    --notes-file docs/announcements/v1.4.0.md \
    --verify-tag \
    /tmp/flow-symphony-v1.4.0.mp3#"Symphony (MP3, 192 kbps, ~1.5 MB)" \
    /tmp/flow-symphony-v1.4.0.wav#"Symphony (WAV, uncompressed, ~10 MB)" \
    /tmp/flow-linux-x64.tar.gz#"Flow CLI binary (Linux x64, self-contained)"
```

The asset-label sizes in the `#"label"` suffixes should be the ACTUAL sizes from Task 2's `ls -lh` output, not the round-number defaults — composer reads the rendered release page expecting accurate file sizes.

The `--verify-tag` flag aborts if the v1.4.0 tag doesn't exist on the remote (catches "forgot to push" footgun — should not fire here because Task 3 pushed the tag).

Defaults that apply implicitly (DO NOT change unless composer requests):
- `--latest=true` — marks v1.4.0 as the repo's latest release in the GitHub UI header.
- Target branch = repo default (dev or main as configured).
- Not draft, not pre-release.

Verify after creation:
- `gh release view v1.4.0` shows the release body (the announcement-draft content) rendered.
- `gh release view v1.4.0 --json assets --jq '.assets | length'` returns `3`.
- `gh release view v1.4.0 --json assets --jq '.assets[].name'` returns the 3 asset filenames.
- `gh release view v1.4.0 --json isLatest --jq '.isLatest'` returns `true`.

If the release-create command fails (auth, conflict, network), surface the exact stderr to composer.
  </action>
  <verify>
    <automated>gh release view v1.4.0 --json assets --jq '.assets | length' | grep -q '^3$' &amp;&amp; gh release view v1.4.0 --json assets --jq '.assets[].name' | grep -q 'flow-symphony-v1.4.0.mp3' &amp;&amp; gh release view v1.4.0 --json assets --jq '.assets[].name' | grep -q 'flow-symphony-v1.4.0.wav' &amp;&amp; gh release view v1.4.0 --json assets --jq '.assets[].name' | grep -q 'flow-linux-x64.tar.gz' &amp;&amp; gh release view v1.4.0 --json isLatest --jq '.isLatest' | grep -q '^true$'</automated>
  </verify>
  <acceptance_criteria>
    - `gh release view v1.4.0 --json assets --jq '.assets | length'` returns exactly `3`.
    - All three asset filenames (`flow-symphony-v1.4.0.mp3`, `flow-symphony-v1.4.0.wav`, `flow-linux-x64.tar.gz`) appear in the release's asset list.
    - `gh release view v1.4.0 --json isLatest --jq '.isLatest'` returns `true`.
    - `gh release view v1.4.0 --json body --jq '.body'` returns a non-empty body string (the announcement-draft content was uploaded via `--notes-file`).
    - Manual: composer opens the release page in browser (`gh release view v1.4.0 --web`) and confirms the body renders correctly + all 3 assets show their human labels.
  </acceptance_criteria>
  <done>GitHub Release v1.4.0 published with 3 labeled assets and the announcement-draft body. The MP3 release-asset download URL is now a real public URL that plan 34-03 will reference.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Composer reviews the published release page</name>
  <files>(no file changes)</files>
  <read_first>(none — interactive verification)</read_first>
  <action>HUMAN CHECKPOINT — this is a manual verification step performed by the composer. See <how-to-verify> for the procedure and <acceptance_criteria> for the pass conditions. No automated action is taken by the executor; the executor pauses and waits for the resume-signal.</action>
  <verify>
    <human-check>Composer follows the procedure in &lt;how-to-verify&gt; and replies with the resume-signal text described in &lt;resume-signal&gt;.</human-check>
  </verify>
  <done>Composer provides resume-signal indicating acceptance criteria are satisfied (per &lt;acceptance_criteria&gt;).</done>
  <what-built>
    - v1.4.0 annotated tag pushed to origin.
    - GitHub Release v1.4.0 published with 3 labeled assets: `flow-symphony-v1.4.0.mp3` + `flow-symphony-v1.4.0.wav` + `flow-linux-x64.tar.gz`.
    - Release body = docs/announcements/v1.4.0.md verbatim.
  </what-built>
  <how-to-verify>
Composer performs a manual review of the public release:

1. Open the release page: `gh release view v1.4.0 --web` (opens in browser).
2. Confirm:
   - Title reads "v1.4 Audio Fidelity, Distribution & Public Showcase".
   - Body renders cleanly (3 paragraphs, no broken Markdown, no placeholder tokens).
   - 3 assets listed with the size + format labels (MP3 / WAV / Linux binary).
   - "Latest" badge appears.
3. Download the MP3 asset and listen — confirms upload integrity. (Optional but recommended.)
4. Click the symphony.flow source link in the body — confirms the link resolves.
5. Confirm the release tag URL `https://github.com/OWNER/REPO/releases/tag/v1.4.0` is stable (this is the URL plan 34-03 will embed in the top-level README and plan 34-06 will link from milestone closure docs).

If anything is wrong (typo in body, missing asset, incorrect labels, release marked as pre-release accidentally), the fix is either:
- Edit body in-place: `gh release edit v1.4.0 --notes-file docs/announcements/v1.4.0.md` (after fixing the file).
- Re-upload an asset: `gh release upload v1.4.0 /tmp/<file> --clobber`.
- Toggle latest: `gh release edit v1.4.0 --latest`.

The release is now publicly live — composer should NOT delete/recreate the tag after this checkpoint (RESEARCH "Don't amend the v1.4.0 tag after publication"). Any fix is via `gh release edit` (which preserves the tag).
  </how-to-verify>
  <acceptance_criteria>
    - Composer affirms the release page renders cleanly in the browser.
    - Composer affirms the 3 assets are listed with correct labels.
    - Composer affirms the release body renders as expected (no broken Markdown).
    - Composer optionally affirms the downloaded MP3 plays correctly.
  </acceptance_criteria>
  <resume-signal>Composer types "approved — release looks good" or describes any issues for follow-up `gh release edit` commands.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer's gh token ↔ GitHub API | `gh release create` uses the composer's pre-authenticated `gh auth login` token. The token scope is the composer's existing scope; no new credential is introduced. |
| local /tmp staged assets ↔ GitHub CDN | The 3 release assets become publicly downloadable once the release publishes. Composer reviews each asset's contents (Task 5) before the public read-out moment in plan 34-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05-CRED | Tampering | composer gh token | accept | `gh release create` uses pre-authenticated `gh auth login` token. No new secret. Composer's existing scope applies; no token persistence beyond gh CLI's normal credential store. Severity: low (informational). |
| T-34-05-LEAK | Information disclosure | release assets | mitigate | Task 5 manual review gate: composer confirms the MP3/WAV contents are intended for public distribution AND the tarballed binary contains only the Phase 30 `flow` executable (no leaked credentials, no /tmp paths, no .git directories). Composer downloads + inspects before the release page is shared publicly via plan 34-03's README link. Severity: low. |
| T-34-05-TAG | Tampering | v1.4.0 tag | accept | Annotated tag immutability convention (RESEARCH "Don't amend the v1.4.0 tag after publication"). Post-publication fixes go through `gh release edit` (preserves tag) or v1.4.1 follow-up. No force-push to the tag. Severity: low. |
</threat_model>

<verification>
- `git tag --list v1.4.0` returns the tag.
- `gh release view v1.4.0` returns the release with 3 assets and announcement body.
- Composer manual sign-off on Task 5 confirms public-facing rendering.
</verification>

<success_criteria>
- v1.4.0 annotated tag exists locally + on origin.
- GitHub Release v1.4.0 published with title "v1.4 Audio Fidelity, Distribution & Public Showcase", body from docs/announcements/v1.4.0.md, and 3 labeled assets (MP3 / WAV / Linux tar.gz).
- Release marked as `--latest=true`.
- Composer signed off on Task 5 manual review.
</success_criteria>

<output>
Create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-05-SUMMARY.md` when done. SUMMARY should record: the v1.4.0 tag SHA, the 3 release-asset download URLs (gh release view v1.4.0 --json assets --jq '.assets[].url'), and the canonical release page URL (used by plans 34-03 + 34-06).
</output>
