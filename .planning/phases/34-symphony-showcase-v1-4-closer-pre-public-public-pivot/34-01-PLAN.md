---
phase: 34-symphony-showcase-v1-4-closer-pre-public-public-pivot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/symphony/symphony.flow
  - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md
autonomous: false
requirements: [SYM-01, SYM-02, SYM-03]
must_haves:
  truths:
    - "D-101 / D-102 / D-103 / D-104: a ~60s ABA single-movement film-score/neo-classical minimalist piece rendered at tempo 100 / key D minor (or planner-chosen minor) / 4/4. Length may flex ±15s if the piece demands it."
    - "D-201 / D-202 / D-203: 5 instruments loaded via (loadSfz #symbol) — #violin (SViolinVib.sfz) + #cello (CelloEnsSusVib.sfz ensemble) + #flute (FluteSusVib.sfz) + #horn (FHornSus.sfz) + #timpani (Timpani.sfz). Flute carries A-section melody; violin enters A' octave-up via transpose; cello sustained bass; horn pads in B; timpani marks A→B and B→A' transitions with marcato hits."
    - "D-301: source exercises every criterion-#3 feature explicitly — tempo + timesig + key + voicePool 32 context blocks; ≥1 transform (transpose for violin A' octave-up); sampler:NAME dispatch on every instrument (5 calls); each Phase 28 articulation token used ≥1× (>, stacc, ten, leg, marc); ≥1 {voice ...}{voice ...} voice block."
    - "D-302: NO microtonal pragmas (enable justIntonation; / enable pythagorean;) and NO tuning t { ... } block — symphony stays in 12-TET."
    - "D-303: exactly one {3:2 ...}q tuplet bracket on the flute line in section B."
    - "D-304: humanizeGaussian(celloLine, 0.05, FIXED-INT-SEED) applied to the cello part (RESEARCH Pitfall 4 — seed MUST be a literal integer like 42, NEVER (randomInt ...) — that would break two-run determinism)."
    - "D-401: per-instrument volume(buf, linear) balancing — STARTING balance: flute 0.85, violin 1.0, cello 0.75, horn 0.65, timpani 0.40 — composer overrides during UAT iteration."
    - "D-402: master Second-decay reverb on summed mix: (reverb mix 0.3 2.5s). Stacks on top of SongRenderer's per-section reverb; Pitfall 3 warns muddiness — composer adjusts during UAT if combined wet exceeds clean concert-hall feel."
    - "D-403: soft master compress on top of reverb: (compress mix -12dB 4 100ms 200ms) — 4:1 above -12dB, 100ms attack / 200ms release."
    - "D-404: NO pan(...) calls — SfzRenderer mono-sums at v1.4; per-instrument pan is v1.5 deferred."
    - "D-504: render outputs to examples/output/symphony.wav + examples/output/symphony.mid (gitignored)."
    - "D-702: two-run byte-identical determinism — `flow render ... -o a.wav && flow render ... -o b.wav && cmp a.wav b.wav` exits 0."
    - "D-801: iterative UAT with no cap — composer listens, gives plain-English feedback, source is adjusted, re-render. Loop continues until composer signs off. This plan does NOT close until 34-HUMAN-UAT.md contains sign-off."
    - "D-802 condition 1: composer affirms 'I would publicly share this' / 'postable on GitHub quality' verbatim in 34-HUMAN-UAT.md."
    - "D-802 condition 2: composer A/B-listens canonical mix vs an all-articulations-stripped variant rendered alongside; canonical is audibly more expressive (audible articulation differentiation)."
    - "D-802 condition 3: composer picks out simultaneous voices in the {voice ...}{voice ...} A' section (audible polyphony)."
    - "D-803: sign-off recorded as `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md` with frontmatter mirroring 33-HUMAN-UAT.md shape — `status: partial` → `status: closed` on sign-off, three test rows for the three D-802 conditions."
    - "D-701: NO CI test for the rendered symphony WAV — symphony render requires VSCO-CE not present in CI; Phase 33 synthetic SFZ smoke fixture is the load-bearing CI surface for the SFZ subsystem. Two-run cmp-clean is composer-local + release-time verification only."
    - "D-703: NO RMS-windowed regression baseline for the symphony WAV — symphony WAV depends on VSCO-CE samples not in CI, so an RMS baseline would either need the library checked in (rejected per Phase 33 SPEC-2) or be skipped on CI (worse than no test — false signal of coverage). Leave it out."
  artifacts:
    - path: examples/symphony/symphony.flow
      provides: "Canonical Flow source for the ~60s symphony. ABA single-movement, 5 VSCO-CE SFZ instruments, full D-301 feature coverage."
      min_lines: 60
      contains: 'loadSfz #violin'
    - path: .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md
      provides: "Composer sign-off ledger — 3 D-802 test rows + frontmatter mirroring 33-HUMAN-UAT.md shape."
      contains: 'status: closed'
  key_links:
    - from: examples/symphony/symphony.flow
      to: flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
      via: 'renderSong piece "sampler:NAME" dispatches to SfzRenderer (Phase 33 D-13)'
      pattern: 'sampler:violin|sampler:cello|sampler:flute|sampler:horn|sampler:timpani'
    - from: examples/symphony/symphony.flow
      to: flow-lang/StandardLibrary/Audio/Sfz/SfzPatchRegistry.cs
      via: 'Sfz NAME = (loadSfz #symbol) registers patch by binding name'
      pattern: 'loadSfz #'
    - from: examples/symphony/symphony.flow
      to: flow-lang/StandardLibrary/Audio/SongRenderer.cs
      via: 'Phase 28 voicePool 32 + voice-block routing'
      pattern: 'voicePool 32|\{voice'
    - from: .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md
      to: .planning/phases/33-sfz-orchestral-sampler/33-HUMAN-UAT.md
      via: 'frontmatter and section shape mirrored verbatim per D-803'
      pattern: 'status: closed'
---

<objective>
Compose `examples/symphony/symphony.flow` — a curated ~60s minimalist-orchestral piece for 5 VSCO-CE SFZ instruments (violin, cello, flute, horn, timpani) — and iterate with the composer until sign-off lands in `34-HUMAN-UAT.md`. This is the long pole of Phase 34: the iteration loop (compose → render → composer listens → composer feedback → adjust → re-render) has NO arbitrary cap and the plan does not close until the composer signs off all three D-802 conditions.

Purpose: SYM-01 (symphony renders end-to-end via SFZ sampler with no runtime errors) + SYM-02 (composer "postable on GitHub" sign-off) + SYM-03 (audible articulation differentiation + audible polyphony).

Output:
1. `examples/symphony/symphony.flow` — iterated to composer satisfaction; not yet the final committed canonical version (plan 34-02 commits the final post-UAT source as a separate clean-history commit per D-902).
2. `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md` — composer sign-off record with status closed and 3 D-802 test rows resolved.
3. Local render artifacts at `examples/output/symphony.wav` + `examples/output/symphony.mid` (gitignored — not committed per D-502).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-VALIDATION.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md
@.planning/phases/33-sfz-orchestral-sampler/33-HUMAN-UAT.md
@examples/symphony/sfz_smoke.flow
@examples/showcase.flow

<interfaces>
<!-- Phase 33 SFZ surface the symphony consumes (already shipped, unchanged here): -->

From flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs:
  - (loadSfz #symbol) -> Sfz             # resolves 19-entry GM dict against sfz_root from ~/.config/flow/config.toml
  - (loadSfz "/abs/path.sfz") -> Sfz     # bypasses the dict for absolute paths

From flow-lang/Runtime/Value.cs:
  - SfzType is the music-type backing Sfz bindings (Phase 33; reference identity; not coercible to Double/Float).

From flow-lang/StandardLibrary/Audio/SongRenderer.cs:
  - (renderSong piece "sampler:NAME") -> Buffer
    # NAME matches a top-level `Sfz NAME = (loadSfz ...)` binding
    # Dispatches to SfzRenderer via Phase 33 D-13 prefix branch BEFORE bundled-sample dispatch
    # Throws UnknownSamplerNameError if NAME not registered (with `Did you forget Sfz {name} = (loadSfz #...)?` hint)

From flow-lang/StandardLibrary/Audio/BuiltInFunctions.cs (existing surface):
  - (volume buf 0.85)         # linear multiplier (Phase 26.2 ERG-03 — NOT gain; volume = linear)
  - (gain buf -6dB)            # decibel attenuation/amplification (NOT used here per D-401)
  - (reverb buf wet decay)     # decay accepts Second literal (e.g. 2.5s)
  - (compress buf thr ratio attack release)   # thr is Decibel literal (e.g. -12dB); attack/release are Millisecond literals (100ms / 200ms)
  - (writeWav "path" buf)      # writes 44.1kHz 16-bit WAV
  - (writeMidi "path" song)    # multi-track MIDI: one track per unique sequence name + conductor track (CLAUDE.md § "Multi-track MIDI export")
  - (transpose seq 12)         # semitone transpose
  - (humanizeGaussian seq amount seed)   # Phase 25 — seed MUST be literal int for two-run determinism

From flow-lang/Runtime/MusicalContext.cs (existing):
  - tempo 100 { ... }           # BPM
  - timesig 4/4 { ... }         # numerator/denominator
  - key Dminor { ... }          # roman numerals resolve from active key
  - voicePool 32 { ... }        # Phase 28 SPEC-7 — range 1..256, locked default 32; surfaced here for documentation value (D-301)
  - section NAME { ... }        # section declaration

Note-stream tokens this plan uses (verified surface):
  - Pitch + duration: D5q, A5h, F4w, E5e, _ (rest), D5q. (dotted), C4q~ (tied to next)
  - Cent offset: D5+50c                    # Phase 26.1 / 26.2 — not used in symphony per D-302
  - Articulation tokens (Phase 28 — D-301 requires ≥1 of each):
      > (after note)     -> Accent     (+0.30 velocity; clamped)
      stacc              -> Staccato   (25% duration + sustain=0 + release×0.5)
      ten                -> Tenuto     (100% duration + release×1.2)
      leg                -> Legato     (110% duration + crossfade)
      marc               -> Marcato    (25% duration + Accent's +0.30 velocity)
  - Voice block (Phase 28 polyphony — D-301 requires ≥1):
      | {voice C4w} {voice C5q D5q E5q F5q} |
  - Tuplet bracket (Phase 19 — D-303 requires exactly 1):
      | _ | {3:2 D5q E5q F5q}q stacc _ _ | _ | _ |

VSCO-CE pitch ranges (RESEARCH Priority 1 — verified by GitHub raw probes 2026-05-16):
  #violin  : MIDI 55-96 (G3-C7), 2 velocity layers (p / f)
  #cello   : MIDI 36-77 (C2-F5), 2 velocity layers — stay in C2-G3 for sustained-bass role per D-203
  #flute   : MIDI 60-96 (C4-C6), 15 samples chromatically (NO Phase 29 2-sample quirk — write D5 freely)
  #horn    : MIDI 33-77 (A1-F5), up to 4 velocity layers — stay in F2-A4 for warm-pad role per D-203
  #timpani : MIDI 36-60 (C2-C4), 24 samples, 3 velocity layers. RR opcodes (rr1/rr2) silently ignored — vary velocity/pitch between hits instead (RESEARCH Pitfall 2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the initial symphony.flow source from the RESEARCH skeleton</name>
  <files>examples/symphony/symphony.flow</files>
  <read_first>
    - examples/symphony/sfz_smoke.flow (Phase 33 tutorial — imports, loadSfz pattern, musical-context nesting)
    - examples/showcase.flow (v1.3 — per-section render + effect-chain + writeWav shape; note the bundled "strings" dispatch — replace with "sampler:NAME")
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Symphony skeleton" (lines 370-450)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 1 "examples/symphony/symphony.flow"
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md decisions D-101..D-404
    - flow-lang/StandardLibrary/Audio/SongRenderer.cs (verify sampler: prefix dispatch + identify the exact `mix` / `add` / `sum` builtin name for summing Buffers — RESEARCH Assumption A1)
    - CLAUDE.md § "Locked articulation rules" (the 5 token semantics) and § "Voice-block polyphony"
  </read_first>
  <action>
Create `examples/symphony/symphony.flow` adapting the RESEARCH § "Symphony skeleton" (lines 370-450 of 34-RESEARCH.md) into a working initial draft. Single file. ASCII-only comments (CLAUDE.md Conventions — no emoji). Brief `//` comments only; no `Note:` tutorial-narration prose (this is a music piece, not a tutorial). Working title `In Five Voices` as a top file-comment header (Claude's Discretion — composer renames if a better title surfaces during UAT).

Required shape:
- Imports: `use "@std"`, `use "@audio"`, `use "@sfz"`, `use "@composition"`.
- 5× `Sfz NAME = (loadSfz #symbol)` bindings — exactly the 5 D-202 instruments: violin, cello, flute, horn, timpani.
- Musical-context nesting `voicePool 32 { tempo 100 { timesig 4/4 { key Dminor { ... } } } }`. ALL FOUR blocks required by D-301. Composer may swap key Dminor for a different minor key during UAT if a sketch reads cleaner there (D-104 — "recommendation, not lock").
- 5 sections: themeA, transitionAB, themeB, transitionBAPrime, themeAPrime — ABA single-movement (D-102).
- D-203 role assignment: flute carries A theme; violin enters A' octave-up via `(transpose | ... | 12)`; cello sustained bass (with `humanizeGaussian` per D-304 — fixed integer seed 42, NEVER (randomInt ...)); horn chord pads in B; timpani marc hits at A→B and B→A' transitions.
- ≥1 of each Phase 28 articulation token spread across instruments per RESEARCH Pattern 3 (NOT stacked on one line): `>` accent on A-theme flute downbeats, `stacc` on B-section flute running line, `ten` on horn pad notes, `leg` on cello sustained passages, `marc` on the 2 timpani transition hits.
- Exactly 1 tuplet bracket `{3:2 D5q E5q F5q}q stacc` on the flute line in section B (D-303).
- ≥1 voice block — `Sequence celloVoiced = | {voice D2w} {voice A3h F3h} |` in section A' per RESEARCH Priority 6 recommendation (held bass + inner voices under transposed violin lead — most musically natural placement and satisfies D-802 condition 3).
- `Song piece = [themeA transitionAB themeB transitionBAPrime themeAPrime]`.
- 5× `Buffer rawX = (renderSong piece "sampler:NAME")` per-instrument render.
- 5× `Buffer balX = (volume rawX 0.XX)` with D-401 starting balance (flute 0.85, violin 1.0, cello 0.75, horn 0.65, timpani 0.40).
- Sum via the correct `@audio` builtin (Task 1 sub-step: grep `flow-lang/StandardLibrary/Audio/*.cs` for `Register.*"mix"` / `"sum"` / `"add"` on Buffer types — use whichever exists; RESEARCH Assumption A1 LOW risk). Document the chosen builtin in a `//` comment so plan 34-02 can copy verbatim.
- `Buffer wet = summed -> (reverb 0.3 2.5s)` (D-402) — passing the `2.5s` Second literal directly per Phase 26.2 ergonomics.
- `Buffer mastered = wet -> (compress -12dB 4 100ms 200ms)` (D-403) — using `-12dB` Decibel literal and `100ms` / `200ms` Millisecond literals per Phase 26.2.
- `(writeWav "examples/output/symphony.wav" mastered)` + `(writeMidi "examples/output/symphony.mid" piece)` per D-504.
- NO `pan(...)` calls (D-404). NO `enable justIntonation;` / `enable pythagorean;` / `tuning t { ... }` (D-302). NO Phase 29 piano (D-202).

Do NOT run the render yet — that is Task 2. Do NOT commit yet — plan 34-02 commits the canonical post-UAT version per D-902.
  </action>
  <verify>
    <automated>test -f examples/symphony/symphony.flow &amp;&amp; grep -q 'loadSfz #violin' examples/symphony/symphony.flow &amp;&amp; grep -q 'loadSfz #cello' examples/symphony/symphony.flow &amp;&amp; grep -q 'loadSfz #flute' examples/symphony/symphony.flow &amp;&amp; grep -q 'loadSfz #horn' examples/symphony/symphony.flow &amp;&amp; grep -q 'loadSfz #timpani' examples/symphony/symphony.flow &amp;&amp; grep -q 'voicePool 32' examples/symphony/symphony.flow &amp;&amp; grep -q 'tempo 100' examples/symphony/symphony.flow &amp;&amp; grep -q 'timesig 4/4' examples/symphony/symphony.flow &amp;&amp; grep -Eq 'key [A-G][#b]?(major|minor)' examples/symphony/symphony.flow &amp;&amp; grep -q 'sampler:violin' examples/symphony/symphony.flow &amp;&amp; grep -q 'sampler:cello' examples/symphony/symphony.flow &amp;&amp; grep -q 'sampler:flute' examples/symphony/symphony.flow &amp;&amp; grep -q 'sampler:horn' examples/symphony/symphony.flow &amp;&amp; grep -q 'sampler:timpani' examples/symphony/symphony.flow &amp;&amp; grep -q 'humanizeGaussian' examples/symphony/symphony.flow &amp;&amp; grep -q 'transpose' examples/symphony/symphony.flow &amp;&amp; grep -q '{voice' examples/symphony/symphony.flow &amp;&amp; grep -Eq '\{3:2' examples/symphony/symphony.flow &amp;&amp; grep -q ' stacc' examples/symphony/symphony.flow &amp;&amp; grep -Eq '[A-G][0-9] ten' examples/symphony/symphony.flow &amp;&amp; grep -q ' leg' examples/symphony/symphony.flow &amp;&amp; grep -q ' marc' examples/symphony/symphony.flow &amp;&amp; grep -Eq '[A-G][0-9][a-z]+&gt;' examples/symphony/symphony.flow &amp;&amp; grep -q 'reverb' examples/symphony/symphony.flow &amp;&amp; grep -q '2.5s' examples/symphony/symphony.flow &amp;&amp; grep -q '\-12dB' examples/symphony/symphony.flow &amp;&amp; grep -q '100ms' examples/symphony/symphony.flow &amp;&amp; grep -q '200ms' examples/symphony/symphony.flow &amp;&amp; grep -q 'writeWav .*examples/output/symphony.wav' examples/symphony/symphony.flow &amp;&amp; grep -q 'writeMidi .*examples/output/symphony.mid' examples/symphony/symphony.flow &amp;&amp; ! grep -q 'enable justIntonation' examples/symphony/symphony.flow &amp;&amp; ! grep -q 'enable pythagorean' examples/symphony/symphony.flow &amp;&amp; ! grep -Eq '^[^/]*\(pan ' examples/symphony/symphony.flow &amp;&amp; ! grep -q 'sampler:piano' examples/symphony/symphony.flow</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/symphony/symphony.flow` exists with ≥ 60 non-comment lines.
    - All 5 `(loadSfz #symbol)` bindings present (#violin, #cello, #flute, #horn, #timpani) — verified by grep.
    - All 4 musical-context blocks present: `voicePool 32`, `tempo 100`, `timesig 4/4`, `key {minor-key}` (D minor recommended; planner may pick another minor key) — verified by grep.
    - All 5 `sampler:NAME` dispatches present — verified by grep.
    - All 5 Phase 28 articulation tokens used at least once: `>` (after note), `stacc`, `ten`, `leg`, `marc` — verified by grep.
    - At least one `{voice ...}` block present — verified by grep.
    - Exactly one tuplet bracket matching `{3:2` present (composer may use a different tuplet ratio if the music reads better, but only ONE bracket total per D-303).
    - `humanizeGaussian` call present with a literal integer seed (NOT `randomInt`) — verified by grep + manual review confirming the third arg is a digit-only token.
    - Mix-stack ships `(reverb ... 2.5s)` and `(compress ... -12dB 4 100ms 200ms)` — verified by grep on the four Phase 26.2 music-type literals.
    - `(writeWav "examples/output/symphony.wav" ...)` and `(writeMidi "examples/output/symphony.mid" ...)` lines present — verified by grep.
    - Prohibited patterns absent: no `enable justIntonation;` / `enable pythagorean;` (D-302), no `(pan ...)` calls outside comments (D-404), no `sampler:piano` (D-202 exclusion).
  </acceptance_criteria>
  <done>Initial draft of symphony.flow committed in working state (not yet to git). Ready for Task 2 render attempt.</done>
</task>

<task type="auto">
  <name>Task 2: First-render smoke + two-run determinism check</name>
  <files>examples/output/symphony.wav, examples/output/symphony.mid</files>
  <read_first>
    - examples/symphony/symphony.flow (the draft from Task 1)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Pitfall 4" (humanizeGaussian seed) + § "Priority 7" (two-run reproduction script)
    - flow-cli/Config/FlowConfigPoco.cs (confirm `sfz_root` key the composer must populate per Phase 30 + Phase 33 SPEC-1)
    - examples/symphony/sfz_smoke.flow lines 11-17 (existing VSCO-CE setup steps composer must have completed)
  </read_first>
  <action>
Render the symphony twice back-to-back and verify byte-identical output, then debug to clean SYM-01 + D-702 status BEFORE composer UAT begins.

Steps:
1. Confirm `sfz_root` is set in `~/.config/flow/config.toml` and VSCO-CE 1.1.0 is installed at that path (run `cat ~/.config/flow/config.toml | grep sfz_root` — if missing, halt and surface to composer with the exact line to add). RESEARCH Open Question 3 noted this gate.
2. Build the CLI once: `dotnet build flow-cli/flow-cli.csproj -c Release`.
3. First render: `dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/symphony-a.wav` — capture stderr to a temp file. If render fails (non-zero exit), DEBUG the source per the error message and re-render. If render emits a stderr advisory about an unmapped patch or missing sample, capture it for the composer to review but do not halt unless exit is non-zero.
4. Second render: `dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/symphony-b.wav` — same flow.
5. Determinism check: `cmp /tmp/symphony-a.wav /tmp/symphony-b.wav && echo "OK: byte-identical"`. If `cmp` exits non-zero, the most likely cause is a non-literal seed in `humanizeGaussian` (RESEARCH Pitfall 4) — verify the seed is a digit-only literal; fix and re-render both.
6. Also render the MIDI: `dotnet run --project flow-cli -c Release -- flow2midi examples/symphony/symphony.flow -o examples/output/symphony.mid` — verify exit 0 (no behavioral check on the .mid contents at this stage).
7. Move the final accepted /tmp WAV into place: `cp /tmp/symphony-a.wav examples/output/symphony.wav`.
8. Verify approximate duration via `soxi -D examples/output/symphony.wav 2>/dev/null || ffprobe -v error -show_entries format=duration -of csv=p=0 examples/output/symphony.wav` — should be 45s ≤ duration ≤ 75s (D-101 ±15s window). If outside, adjust section lengths in symphony.flow and re-render until in range.

If all 4 gates pass (render exit 0, two-run cmp exit 0, MIDI export exit 0, duration in window), this task is done and Task 3 begins. If any gate fails after 3 debug iterations, halt and surface the specific failure to composer with the exact stderr captured.
  </action>
  <verify>
    <automated>test -f examples/output/symphony.wav &amp;&amp; test -f examples/output/symphony.mid &amp;&amp; dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/sym-verify-a.wav 2&gt;/tmp/sym-stderr-a &amp;&amp; dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/sym-verify-b.wav 2&gt;/tmp/sym-stderr-b &amp;&amp; cmp /tmp/sym-verify-a.wav /tmp/sym-verify-b.wav</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/sym-X.wav` exits 0 on TWO consecutive invocations.
    - `cmp /tmp/sym-verify-a.wav /tmp/sym-verify-b.wav` exits 0 (byte-identical determinism — SYM-01 D-702 satisfied).
    - `dotnet run --project flow-cli -c Release -- flow2midi examples/symphony/symphony.flow -o examples/output/symphony.mid` exits 0.
    - `examples/output/symphony.wav` duration is in the inclusive range [45.0, 75.0] seconds (D-101 ≈60s ±15s window).
    - No stderr advisory contains the strings "FATAL", "missing patch", or "sfz_root not set" in either render — those would indicate the VSCO-CE setup is incomplete.
  </acceptance_criteria>
  <done>Two-run cmp-clean render of symphony.wav + symphony.mid produced; duration in window; ready for composer UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Composer UAT iteration loop (D-801 — no cap)</name>
  <files>examples/symphony/symphony.flow, examples/output/symphony.wav, examples/output/symphony.mid, .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/33-sfz-orchestral-sampler/33-HUMAN-UAT.md (frontmatter + test-row shape to mirror verbatim per D-803)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 5 (HUMAN-UAT.md 3-row pattern)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md decisions D-801..D-803 (UAT loop semantics + 3 sign-off conditions)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Pitfall 3" (per-section reverb stacked with master reverb muddiness — common UAT feedback)
  </read_first>
  <action>HUMAN CHECKPOINT — this is a manual verification step performed by the composer. See <how-to-verify> for the procedure and <acceptance_criteria> for the pass conditions. No automated action is taken by the executor; the executor pauses and waits for the resume-signal.</action>
  <verify>
    <human-check>Composer follows the procedure in &lt;how-to-verify&gt; and replies with the resume-signal text described in &lt;resume-signal&gt;.</human-check>
  </verify>
  <done>Composer provides resume-signal indicating acceptance criteria are satisfied (per &lt;acceptance_criteria&gt;).</done>
  <what-built>
    - Initial `examples/symphony/symphony.flow` source (Task 1) — 5-instrument ABA piece with full D-301 feature coverage.
    - Two-run cmp-clean render at `examples/output/symphony.wav` (Task 2) — byte-identical determinism preserved.
    - MIDI export at `examples/output/symphony.mid` (Task 2).
    - Composer setup verified: VSCO-CE 1.1.0 installed at `sfz_root` from `~/.config/flow/config.toml`.
  </what-built>
  <how-to-verify>
This is the D-801 iterative UAT loop. NO arbitrary iteration cap. The plan does not close until composer signs off all 3 D-802 conditions in `34-HUMAN-UAT.md`.

**Step 0 — Create the UAT ledger:** Before the first listen, create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md` mirroring `33-HUMAN-UAT.md` frontmatter shape (status: partial, phase, source list, started, updated) with 3 test rows for the 3 D-802 conditions (see 34-PATTERNS.md § 5 for the exact body shape). Initialize all 3 rows as `result: [pending]`.

**Composer iteration loop:**

1. Play the rendered symphony for the composer:
   - `aplay examples/output/symphony.wav` (ALSA — RESEARCH Environment Availability)
   - OR `dotnet run --project flow-cli -c Release -- play examples/symphony/symphony.flow` (PulseAudio via Phase 30 CLI)

2. Composer gives plain-English feedback against the 3 D-802 conditions:
   - **D-802 condition 1 — "I would publicly share this":** Is the overall mix postable-on-GitHub quality?
   - **D-802 condition 2 — Audible articulation differentiation:** Render an A/B variant — copy symphony.flow to `/tmp/symphony_no_articulation.flow`, strip all `>` / `stacc` / `ten` / `leg` / `marc` tokens (replace with bare notes), render that variant to `/tmp/symphony_no_articulation.wav`. Play both: canonical vs stripped. Composer confirms canonical is audibly more expressive.
   - **D-802 condition 3 — Audible polyphony:** Play the A' section (or the bars containing the `{voice ...}{voice ...}` block). Composer confirms simultaneous voices are intelligible as distinct lines (not a muddied chord).

3. If composer flags any issue, treat it as a concrete request: adjust the relevant line(s) in `examples/symphony/symphony.flow` (common adjustments per RESEARCH Pitfall 3: lower master reverb wet from 0.3 to 0.2 / 0.15 if "sounds underwater"; shorten master decay 2.5s → 1.5s; lower per-instrument volumes 5-10% if buildup is muddy; transpose a section if a melody is in an awkward register; swap an articulation token if one doesn't read; etc.). Re-render via Task 2 sequence (verify two-run cmp stays clean after each iteration). Re-play.

4. The plan closes ONLY when composer affirms all 3 conditions in 34-HUMAN-UAT.md. At that moment:
   - Flip frontmatter `status: partial` → `status: closed`.
   - Update each of the 3 test rows from `result: [pending]` to `result: pass — <one-sentence composer affirmation in plain English>`.
   - Update `updated:` timestamp to ISO-8601 now.
   - Update the Summary block (passed: 3, pending: 0).
   - Optional: Delete `/tmp/symphony_no_articulation.flow` + `.wav` (D-802's UAT-only A/B fixture — Deferred Ideas notes it COULD become a permanent example as v1.5 docs polish; for v1.4 the A/B is a one-shot).

If the composer requests a different working title than `In Five Voices`, rename the file's top comment header to match (the filename `symphony.flow` stays per D-501).
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md` exists.
    - Frontmatter contains `status: closed` (verified by `grep -q '^status: closed' 34-HUMAN-UAT.md`).
    - All 3 test rows show `result: pass` lines (verified by `grep -c '^result: pass' 34-HUMAN-UAT.md` returning 3).
    - Summary block reports `passed: 3` and `pending: 0`.
    - `examples/output/symphony.wav` exists and is the final post-UAT render (its duration is still in [45, 75] seconds).
    - Two-run cmp on the FINAL source still passes: `dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/final-a.wav && dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/final-b.wav && cmp /tmp/final-a.wav /tmp/final-b.wav` returns 0.
  </acceptance_criteria>
  <resume-signal>Composer signs off in 34-HUMAN-UAT.md (status flipped to closed, all 3 conditions marked pass). Reply with "approved" to advance to plan 34-02.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer ↔ local workstation | All work in this plan is local — composer authors source, runs CLI, listens to local renders. No externally-reachable surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-NONE | — | local Flow source + local WAV renders | accept | No new attack surface. Composer is authoring music on their own workstation; no untrusted input crosses any trust boundary; the rendered WAV is not yet published. Severity: none. |
</threat_model>

<verification>
- Composer sign-off in 34-HUMAN-UAT.md is the load-bearing gate; without it, this plan does not close and Phase 34 does not proceed to plan 34-02.
- Final two-run cmp-clean render is the load-bearing technical gate; without it, D-702 fails and the symphony cannot ship.
- File `examples/symphony/symphony.flow` is left in the working directory (uncommitted) at plan end — plan 34-02 reads this file as the canonical source and commits it as the final clean-history shipment.
</verification>

<success_criteria>
- `examples/symphony/symphony.flow` exists, renders end-to-end with no runtime errors via `flow render`, satisfies all D-301 feature requirements (verified by Task 1 grep gates), and produces a 45-75s WAV.
- Two consecutive renders produce byte-identical WAV output (`cmp` exit 0) — SYM-01 + D-702 satisfied.
- `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md` contains composer sign-off with `status: closed` and 3/3 D-802 conditions marked pass — SYM-02 + SYM-03 satisfied.
- Local render artifacts `examples/output/symphony.wav` + `examples/output/symphony.mid` exist (not committed — `.gitignore` covers them per D-502).
</success_criteria>

<output>
Create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-01-SUMMARY.md` when done. Note: do NOT commit `examples/symphony/symphony.flow` in this plan — plan 34-02 owns the canonical ship commit per D-902.
</output>
