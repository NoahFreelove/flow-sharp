---
phase: 34-symphony-showcase-v1-4-closer-pre-public-public-pivot
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - examples/symphony/symphony.flow
  - examples/symphony/README.md
autonomous: true
requirements: [SYM-01, SYM-03, SYM-04]
must_haves:
  truths:
    - "D-501: symphony.flow lives at examples/symphony/symphony.flow — sibling to existing sfz_smoke.flow (the SFZ tutorial chapter). Both files coexist in a single directory; both reference the single shared README.md."
    - "D-502 / D-503: rendered WAV/MP3 NOT committed — release-asset only (uploaded in plan 34-05)."
    - "D-602: examples/symphony/README.md expanded to cover BOTH files — new top section `## The Symphony` describing symphony.flow's ABA arc + 5-instrument lineup + Phase 34 feature map + expected output + mix notes + reproduction steps; existing tutorial content demoted to `## Tutorial Chapter: sfz_smoke.flow` heading (preserved verbatim — D-602 is `expand, not replace`)."
    - "D-702 reproduction docs: README ships the two-run cmp-clean one-liner from RESEARCH Priority 7 verbatim, with the framing sentence 'Same inputs → same bytes. Two runs back-to-back must produce identical WAVs.' per Claude's Discretion."
    - "D-902: this plan is the canonical-source ship commit — committing the post-UAT symphony.flow as a clean-history single commit separate from the iterative composition work that lived in plan 34-01."
  artifacts:
    - path: examples/symphony/symphony.flow
      provides: "Final post-UAT canonical Flow source — same file the composer signed off on in plan 34-01, now committed to git."
      contains: 'loadSfz #violin'
    - path: examples/symphony/README.md
      provides: "Expanded composer-facing docs covering both symphony.flow (new headline) and sfz_smoke.flow (existing tutorial chapter)."
      contains: 'The Symphony'
  key_links:
    - from: examples/symphony/README.md
      to: examples/symphony/symphony.flow
      via: "README's `## The Symphony` section references the file as the canonical source"
      pattern: 'symphony.flow'
    - from: examples/symphony/README.md
      to: examples/symphony/sfz_smoke.flow
      via: "Existing tutorial reference preserved verbatim under demoted `## Tutorial Chapter` heading"
      pattern: 'sfz_smoke.flow'
    - from: examples/symphony/README.md
      to: examples/output/symphony.wav
      via: "Reproduction step instructs `flow render examples/symphony/symphony.flow -o examples/output/symphony.wav`"
      pattern: 'examples/output/symphony.wav'
---

<objective>
Commit the canonical post-UAT `examples/symphony/symphony.flow` (produced iteratively in plan 34-01) as a clean single ship commit per D-902, and expand `examples/symphony/README.md` per D-602 to cover both `symphony.flow` (new headline piece) and the existing `sfz_smoke.flow` (Phase 33 tutorial chapter, preserved under a demoted heading).

Purpose: SYM-01 closure (the canonical source enters git history as the renderable artifact) + SYM-03 (README documents the feature-to-audible-moment map for the source) + SYM-04 (README ships the local reproduction steps composers need).

Output:
1. `examples/symphony/symphony.flow` — committed to git, byte-identical to plan 34-01's signed-off final iteration.
2. `examples/symphony/README.md` — expanded with new `## The Symphony` section above the demoted `## Tutorial Chapter: sfz_smoke.flow` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md
@.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-01-SUMMARY.md
@examples/symphony/sfz_smoke.flow
@examples/symphony/README.md
@examples/symphony/symphony.flow

<interfaces>
<!-- Existing examples/symphony/README.md structure (Phase 33 form, ~79 lines): -->
<!--   Top H1: # Symphony Showcase + SFZ Tutorial (or similar — check current file) -->
<!--   ## Setup — VSCO-CE install + sfz_root config -->
<!--   ## Run — single `dotnet run --project flow-interpreter ...` line -->
<!--   ## What the tutorial demonstrates — Phase 33 SFZ tutorial bullets -->
<!--   ## Supported instruments — Phase 33 19-symbol GM dict listing -->
<!--   ## Loading non-GM patches — abs-path overload usage -->
<!--   ## Reference — CONTEXT/SPEC/RESEARCH links -->

<!-- D-602 target shape (post-expansion): -->
<!--   ## The Symphony       (NEW — first section after H1) -->
<!--   ## Setup              (KEEP from current — verbatim) -->
<!--   ## Tutorial Chapter: sfz_smoke.flow   (NEW heading — demotes the current Run/What-demo/Supported/Loading/Reference subsections beneath it) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the canonical post-UAT symphony.flow source</name>
  <files>examples/symphony/symphony.flow</files>
  <read_first>
    - examples/symphony/symphony.flow (the post-UAT working file from plan 34-01)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md (confirms composer sign-off status: closed)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-01-SUMMARY.md (confirms plan 34-01 closed cleanly)
  </read_first>
  <action>
Verify the post-UAT source is in good shape and commit it as the canonical ship.

Steps:
1. Confirm plan 34-01 sign-off: `grep -q '^status: closed' .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-HUMAN-UAT.md`. If false, HALT — plan 34-01 did not close; this plan cannot proceed (D-902 ordering enforcement).
2. Re-verify two-run cmp-clean on the current file:
   - `dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/sym-ship-a.wav`
   - `dotnet run --project flow-cli -c Release -- render examples/symphony/symphony.flow -o /tmp/sym-ship-b.wav`
   - `cmp /tmp/sym-ship-a.wav /tmp/sym-ship-b.wav` — exit 0 required.
3. Stage `examples/symphony/symphony.flow` ONLY (not the WAV or MID outputs — D-502 + .gitignore covers them, but be explicit). Run `git status examples/symphony/` and confirm only `symphony.flow` is the new tracked file.
4. Commit with message:
   `feat(34-02): add canonical In Five Voices symphony source` (subject — use the actual working title from the composer-signed-off file's top comment; if composer renamed during UAT iteration, use the renamed title here).
   Body: 3-line description naming the 5 instruments + ABA structure + the SFZ-sampler-via-VSCO-CE rendering path. Include the standard `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer.
5. Do NOT push (developer pushes separately when ready).

If `git status` shows ANY rendered WAV/MP3 staged or untracked under `examples/symphony/`, halt and surface — D-502 prohibits committing renders into the repo; `.gitignore` should already cover `examples/output/*.wav` but `examples/symphony/*.wav` is NOT in the default ignore list. Add a single-line ignore rule `examples/symphony/*.wav` + `examples/symphony/*.mp3` to `.gitignore` BEFORE committing if necessary (preventative — defends against composer accidentally dropping a render into the wrong directory).
  </action>
  <verify>
    <automated>test -f examples/symphony/symphony.flow &amp;&amp; git log -1 --pretty=format:%s examples/symphony/symphony.flow | grep -Eq 'feat\(34-02\)' &amp;&amp; ! git ls-files examples/symphony/ | grep -Eq '\.(wav|mp3)$'</automated>
  </verify>
  <acceptance_criteria>
    - `examples/symphony/symphony.flow` is tracked in git (present in `git ls-files examples/symphony/`).
    - The most recent commit touching `examples/symphony/symphony.flow` has a subject starting with `feat(34-02)`.
    - No `.wav` or `.mp3` files are tracked under `examples/symphony/` (D-502 enforcement — verified by `git ls-files examples/symphony/` not matching the audio extensions).
    - Two-run cmp-clean still holds at the committed SHA (re-verified by Task 1 step 2).
  </acceptance_criteria>
  <done>Canonical symphony.flow is in git history as a clean single ship commit. Ready for Task 2 to expand the README.</done>
</task>

<task type="auto">
  <name>Task 2: Expand examples/symphony/README.md per D-602</name>
  <files>examples/symphony/README.md</files>
  <read_first>
    - examples/symphony/README.md (current 79-line Phase 33 form — DO NOT delete content, only add + demote)
    - examples/symphony/symphony.flow (just-committed canonical source — extract the actual instrumentation, key, tempo, and any composer-final title for the README's framing)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-PATTERNS.md § 2 (section ordering pattern + code-fenced bash blocks)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-RESEARCH.md § "Two-run determinism reproduction" (lines 454-464)
    - .planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-CONTEXT.md decisions D-602 + D-702 + Claude's Discretion "Determinism reproduction step doc shape"
  </read_first>
  <action>
Expand the existing README. The contract per D-602 is "expand, not replace" — every section currently present stays, just gets repositioned beneath the new `## Tutorial Chapter: sfz_smoke.flow` heading.

Target final structure:

```
# Symphony Showcase + SFZ Tutorial  (or current H1 — preserve existing title)

(optional one-line repo intro from current README, preserved verbatim)

## The Symphony  (NEW — D-602)

  - Opening framing paragraph: working title (the title the composer signed off on in plan 34-01, e.g. "In Five Voices"), duration (~60s; quote exact rendered duration in seconds from the committed render), ABA single-movement, key (D minor or whatever composer settled), tempo (100 or composer's choice), 5 instruments listed with VSCO-CE patch file names.

  - Instrumentation table (one row per instrument): symbol (`#violin`), VSCO-CE patch (`SViolinVib.sfz`), role in the piece (e.g. "solo lead in A'", "sustained bass throughout"). Use the canonical roles from D-203 unless composer adjusted during UAT.

  - Phase 34 feature map: bullet list mapping each criterion-#3 feature (musical-context blocks, note streams, transform, sampler:NAME dispatch, every Phase 28 articulation, voice block, tuplet, humanizeGaussian) to the section / bar / instrument where it audibly fires. Composer / planner extracts from the final source.

  - Expected output: `examples/output/symphony.wav` (44.1kHz 16-bit, ~10 MB) + `examples/output/symphony.mid` (multi-track MIDI, 5 sequence tracks + conductor track per CLAUDE.md § "Multi-track MIDI export").

  - Mix notes: per-instrument volume balance numbers (the composer's final post-UAT settings — read from the committed source's volume() calls), master reverb (Phase 26.2 Second-decay 2.5s), master compress (4:1 @ -12dB, 100ms attack / 200ms release).

  - Reproduction (numbered list):
      1. Setup: see the `## Setup` section below — install VSCO Community CE 1.1.0 and populate `sfz_root` in `~/.config/flow/config.toml`.
      2. Render:
         ```bash
         flow render examples/symphony/symphony.flow -o examples/output/symphony.wav
         flow flow2midi examples/symphony/symphony.flow -o examples/output/symphony.mid
         ```
      3. Listen: `aplay examples/output/symphony.wav` (Linux ALSA) or `flow play examples/symphony/symphony.flow`.
      4. Two-run determinism check — VERBATIM the one-liner from RESEARCH Priority 7:
         ```bash
         flow render examples/symphony/symphony.flow -o /tmp/symphony-a.wav && \
         flow render examples/symphony/symphony.flow -o /tmp/symphony-b.wav && \
         cmp /tmp/symphony-a.wav /tmp/symphony-b.wav && \
         echo "OK: byte-identical determinism preserved"
         ```
         Frame it with the sentence from CONTEXT Claude's Discretion: "Same inputs → same bytes. Two runs back-to-back must produce identical WAVs."
      5. (Optional, composer-only) Encode to MP3 for sharing:
         ```bash
         ffmpeg -i examples/output/symphony.wav -c:a libmp3lame -b:a 192k -y flow-symphony-v1.4.0.mp3
         ```

## Setup  (KEEP existing — verbatim. This is the same VSCO-CE install for both files.)

## Tutorial Chapter: sfz_smoke.flow  (NEW heading — DEMOTE the current `## Run`, `## What the tutorial demonstrates`, `## Supported instruments`, `## Loading non-GM patches`, `## Reference` sections beneath it as H3 subsections via `### Run`, `### What the tutorial demonstrates`, etc. Preserve the body content verbatim.)
```

Rules:
- DO NOT delete any current content.
- DO NOT add emoji (CLAUDE.md Conventions).
- DO NOT change the `## Supported instruments` GM dict listing (still accurate per Phase 33).
- Preserve the H1 title — if the current H1 is `# Symphony Showcase + SFZ Tutorial` keep it; if it's `# SFZ Sampler Tutorial` rename to `# Symphony Showcase + SFZ Tutorial` to reflect the expanded scope.

Commit:
- `docs(34-02): expand examples/symphony/README.md to cover the symphony and demote tutorial`
- Body: 3-line description noting the new `## The Symphony` section + preserved-and-demoted tutorial content + the reproduction steps including the two-run cmp-clean snippet.
- Standard `Co-Authored-By` trailer.
  </action>
  <verify>
    <automated>grep -q '^## The Symphony$' examples/symphony/README.md &amp;&amp; grep -q '^## Tutorial Chapter: sfz_smoke.flow$' examples/symphony/README.md &amp;&amp; grep -q '^## Setup$' examples/symphony/README.md &amp;&amp; grep -q 'flow render examples/symphony/symphony.flow' examples/symphony/README.md &amp;&amp; grep -q 'flow flow2midi examples/symphony/symphony.flow' examples/symphony/README.md &amp;&amp; grep -q 'cmp /tmp/symphony-a.wav /tmp/symphony-b.wav' examples/symphony/README.md &amp;&amp; grep -q 'Same inputs' examples/symphony/README.md &amp;&amp; grep -q 'libmp3lame' examples/symphony/README.md &amp;&amp; grep -q 'SViolinVib.sfz' examples/symphony/README.md &amp;&amp; grep -q 'CelloEnsSusVib.sfz' examples/symphony/README.md &amp;&amp; grep -q 'FluteSusVib.sfz' examples/symphony/README.md &amp;&amp; grep -q 'FHornSus.sfz' examples/symphony/README.md &amp;&amp; grep -q 'Timpani.sfz' examples/symphony/README.md &amp;&amp; git log -1 --pretty=format:%s examples/symphony/README.md | grep -Eq 'docs\(34-02\)'</automated>
  </verify>
  <acceptance_criteria>
    - `## The Symphony` H2 section is present at the top (above `## Setup`) — verified by grep.
    - `## Tutorial Chapter: sfz_smoke.flow` H2 section is present (the demoted heading) — verified by grep.
    - All 5 VSCO-CE patch filenames present in the README — verified by grep (SViolinVib.sfz, CelloEnsSusVib.sfz, FluteSusVib.sfz, FHornSus.sfz, Timpani.sfz).
    - Reproduction render command `flow render examples/symphony/symphony.flow` present.
    - Two-run cmp-clean reproduction snippet present, including `cmp /tmp/symphony-a.wav /tmp/symphony-b.wav` exactly.
    - Framing sentence `Same inputs` present (verifies the exact CONTEXT Claude's Discretion phrasing landed).
    - ffmpeg libmp3lame encoding command present.
    - The previously-existing `## Supported instruments` GM dict content is preserved (verified by `grep -c '^- #' examples/symphony/README.md` returning at least the 19 entries the Phase 33 dict shipped — exact count depends on current README, just confirm preservation by checking the legacy section still appears as H3 under Tutorial Chapter).
    - Latest commit subject on `examples/symphony/README.md` starts with `docs(34-02)`.
  </acceptance_criteria>
  <done>examples/symphony/README.md expanded; both files (symphony.flow + sfz_smoke.flow) documented; reproduction steps with two-run cmp-clean snippet present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local-fs ↔ git history | symphony.flow + README.md will be committed to a public repo. Composer reviews the diff before staging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-NONE | — | examples/symphony/symphony.flow + examples/symphony/README.md | accept | No new attack surface. Pure Markdown docs + ASCII Flow source; no untrusted input; no secrets. Composer reviews the staged diff before commit. Severity: none. |
| T-34-02-LEAK | Information disclosure | examples/symphony/ directory | mitigate | D-502 prohibits committing renders into the repo. Task 1 step 5 adds a defensive ignore rule (`examples/symphony/*.wav` + `examples/symphony/*.mp3`) to `.gitignore` before commit if it's not already present, protecting against composer accidentally dropping a render into the wrong directory. Severity: none — preventative. |
</threat_model>

<verification>
- `examples/symphony/symphony.flow` tracked in git after Task 1.
- `examples/symphony/README.md` contains both `## The Symphony` and `## Tutorial Chapter: sfz_smoke.flow` after Task 2.
- The committed source still passes two-run cmp-clean — re-verified in Task 1 step 2.
</verification>

<success_criteria>
- Final post-UAT symphony.flow committed to git as the canonical ship.
- examples/symphony/README.md expanded with new `## The Symphony` section above demoted tutorial chapter.
- Reproduction steps documented including two-run cmp-clean snippet + ffmpeg MP3 encoding command + per-instrument mix notes from the actual committed source.
</success_criteria>

<output>
Create `.planning/phases/34-symphony-showcase-v1-4-closer-pre-public-public-pivot/34-02-SUMMARY.md` when done.
</output>
