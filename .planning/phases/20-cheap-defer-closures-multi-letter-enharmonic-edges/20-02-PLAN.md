---
phase: 20-cheap-defer-closures-multi-letter-enharmonic-edges
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
  - flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs
  - flow-lang.Tests/Unit/Phase20/EnharmonicEdgesTests.cs
  - tests/test_enharmonic_edges.flow
  - flow-lang.Tests/FlowScriptData.cs
autonomous: true
requirements:
  - DEFER-04

must_haves:
  truths:
    - "enharmonic(E4) returns Fb4 (no-key context)"
    - "enharmonic(F4) returns E#4 (no-key context)"
    - "enharmonic(B4) returns Cb5 (octave +1, no-key context)"
    - "enharmonic(C4) returns B#3 (octave -1, no-key context)"
    - "enharmonic(D4), enharmonic(G4), enharmonic(A4) return D4/G4/A4 unchanged (no edge for D/G/A naturals per D-USER-C)"
    - "When key is active and pitch is diatonic to key, enharmonic returns scale-diatonic spelling (preserves Phase 14 D-04 in-key rule per D-USER-B)"
    - "When key is active but pitch is chromatic (not in scale), enharmonic falls through to no-key edge respelling per D-USER-B"
    - "Round-trip pitch-equivalence: enharmonic(enharmonic(n)) returns a note with the same MIDI as n for every chromatic note"
    - "4 Phase14 NoKey_NaturalUnchanged_C4/E4/B4/F4 Facts MIGRATED (rename + re-pin, NOT delete) per D-USER-F"
    - "Existing 348 xUnit Facts (post-20-01) + ~70 .flow tests pass (zero regression beyond the 4 migrated Facts)"
    - "Phase 18 byte-identical regression gate (19/19 Facts) GREEN — DEFER-04 does not touch audio path"
  artifacts:
    - path: "flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs"
      provides: "Enharmonic naturals branch MODIFIED — 5-line natural-edge switch for E/F/B/C; D/G/A unchanged"
      contains: "DEFER-04"
    - path: "flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs"
      provides: "4 NoKey_NaturalUnchanged_C4/E4/B4/F4 Facts RENAMED to NoKey_NaturalEdgeRespells_* with INVERTED assertions"
      contains: "NoKey_NaturalEdgeRespells"
    - path: "flow-lang.Tests/Unit/Phase20/EnharmonicEdgesTests.cs"
      provides: "NEW Facts: 4 edge respells + round-trip Theory + non-edge naturals D/G/A unchanged + in-key diatonic preservation"
      min_lines: 100
    - path: "tests/test_enharmonic_edges.flow"
      provides: "NEW .flow integration test demonstrating edges + non-edges + in-key behavior"
      min_lines: 20
    - path: "flow-lang.Tests/FlowScriptData.cs"
      provides: "RequiredSentinels entry for test_enharmonic_edges.flow"
      contains: "test_enharmonic_edges.flow"
  key_links:
    - from: "HarmonyFunctions.Enharmonic naturals branch (line 44)"
      to: "NoteType.Format(letter, octave, alteration)"
      via: "letter switch returning Value.Note for E/F/B/C edges"
      pattern: "letter switch"
    - from: "EnharmonicEdgesTests.cs round-trip Theory"
      to: "NoteType.ToMidiNote"
      via: "MIDI-equality assertion (NOT string-equality per Pitfall 8)"
      pattern: "ToMidiNote"
    - from: "Phase14/EnharmonicTests.cs renamed Facts"
      to: "DEFER-04 acceptance"
      via: "Assert.Contains(\"Fb\"...) / Assert.Contains(\"B#\"...) / Assert.Contains(\"E#\"...) / Assert.Contains(\"Cb\"...)"
      pattern: "NoKey_NaturalEdgeRespells_"
---

<objective>
Ship DEFER-04: extend `HarmonyFunctions.Enharmonic` so the four "edge naturals" (E, F, B, C) respell to their multi-letter enharmonic neighbors. Existing flat/sharp flip behavior unchanged. Existing in-key diatonic respelling unchanged. D/G/A naturals continue to return unchanged (per D-USER-C).

Purpose: This closes a v1.2-deferred item that Phase 21 (H-as-B alias via pragma) explicitly depends on. Per binding pre-ordering #3, Phase 21 cannot start until DEFER-04 lands — H-sharp must respell to B# = C natural, which only works once edge respelling is in place.

Algorithm (5-line natural-edge switch):
- E -> Fb (same octave): MIDI 64 stays MIDI 64
- F -> E# (same octave): MIDI 65 stays MIDI 65
- B -> Cb (octave +1): B4 (MIDI 71) -> Cb5 (MIDI 71)
- C -> B# (octave -1): C4 (MIDI 60) -> B#3 (MIDI 60)
- D, G, A -> unchanged

The existing `ComputeFlippedSpelling` helper (HarmonyFunctions.cs:234-252) ALREADY handles inverse cases correctly — `enharmonic(Fb4) -> E4`, `enharmonic(E#4) -> F4`, etc. — so the new code is ONLY the 5-line edge switch in the natural branch.

Output:
- `HarmonyFunctions.Enharmonic` naturals branch (line 44) MODIFIED — switch on letter for E/F/B/C, fall through to unchanged for D/G/A
- 4 Phase14 EnharmonicTests `NoKey_NaturalUnchanged_*` Facts RENAMED to `NoKey_NaturalEdgeRespells_*` with INVERTED assertions (rename + re-pin per Pitfall 1 migration shape (a))
- New `Phase20/EnharmonicEdgesTests.cs` with edge Facts, round-trip Theory, non-edge unchanged, in-key diatonic preservation
- New `tests/test_enharmonic_edges.flow` Theory row with sentinels in FlowScriptData.cs
- ONE atomic commit per plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-RESEARCH.md
@.planning/phases/14-composer-dx-part-1/14-CONTEXT.md
@.planning/phases/14-composer-dx-part-1/deferred-items.md
@flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
@flow-lang/TypeSystem/SpecialTypes/NoteType.cs
@flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs
@flow-lang.Tests/FlowScriptData.cs
@flow-lang.Tests/Fixtures/FlowEngineRunner.cs
@CLAUDE.md

<interfaces>
Key existing types and patterns the executor needs.

From flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (CURRENT branch to MODIFY at lines 43-47):

    // D-05: naturals return unchanged, full stop — no edge respelling.
    if (alteration == 0)
    {
        return Value.Note(NoteType.Format(letter, octave, 0));
    }

From flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs:234-252 (UNCHANGED — handles inverse cases automatically):

    // ComputeFlippedSpelling handles non-natural inputs (Db, F#, Fb, E#, Cb, B#)
    // and via LetterUp/LetterDown ALREADY produces the correct inverse for edge cases.
    // Verified in 20-RESEARCH §Pattern 3 inverse-respell verification.

From flow-lang/TypeSystem/SpecialTypes/NoteType.cs (Format and ToMidiNote signatures):

    public static string Format(char letter, int octave, int alteration);
    public static int ToMidiNote(char letter, int octave, int alteration);
    public static (char letter, int octave, int alteration) Parse(string noteStr);

Format canonical emissions (verified at HEAD via Phase 14 plan 14-02):
    Format('F', 4, -1)  == "F4-"     (Fb4 — single-flat, run-based emitter)
    Format('E', 4, +1)  == "E4+"     (E#4 — single-sharp)
    Format('C', 5, -1)  == "C5-"     (Cb5 — single-flat at octave 5)
    Format('B', 3, +1)  == "B3+"     (B#3 — single-sharp at octave 3)

From flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs (FlowEngineRunner pattern — required because Enharmonic is RegisterContextDependent and reads MusicalContext.Key):

    [Collection("FlowScripts")]
    public class EnharmonicTests
    {
        [Fact]
        public void NoKey_FlatToSharp_Db4()
        {
            using var runner = new FlowEngineRunner();
            var (_, stdout, stderr, errorCount) = runner.RunSource(@"
    use ""@std""
    (print (str (enharmonic Db4)))
    ");
            Assert.Equal(0, errorCount);
            Assert.Contains("C4+", stdout);
        }
    }

From flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs (the 4 Facts that MIGRATE — currently lines 53-105):

    NoKey_NaturalUnchanged_C4: asserts Contains("C4") + DoesNotContain("B#") + DoesNotContain("Cb")
    NoKey_NaturalUnchanged_E4: asserts Contains("E4") + DoesNotContain("Fb")
    NoKey_NaturalUnchanged_B4: asserts Contains("B4") + DoesNotContain("Cb")
    NoKey_NaturalUnchanged_F4: asserts Contains("F4") + DoesNotContain("E#")

DEFER-04 INVERTS each of these. New shapes (rename + re-pin) become:
    NoKey_NaturalEdgeRespells_C4: asserts Contains("B3+") (B#3 = MIDI 60 = C4)
    NoKey_NaturalEdgeRespells_E4: asserts Contains("F4-") (Fb4 = MIDI 64 = E4)
    NoKey_NaturalEdgeRespells_B4: asserts Contains("C5-") (Cb5 = MIDI 71 = B4)
    NoKey_NaturalEdgeRespells_F4: asserts Contains("E4+") (E#4 = MIDI 65 = F4)
</interfaces>

<phase_context>
This plan (20-02) is wave 2 — DEFER-04 only. Plan 20-01 already shipped DEFER-01 (range stdlib) in wave 1. Plan 20-03 (DEFER-05 slice) follows in wave 3. Plan 20-04 closes the phase.

CRITICAL — DO NOT in this plan:
- Edit `Collections.cs` `SliceArray` / `SliceSequence` (plan 20-03's territory)
- Edit `FlowScriptData.cs:57` (`test_custom_oscillator.flow` entry — plan 20-04's territory)
- Edit `tests/test_enharmonic.flow` (the existing Phase 14 file — its `(enharmonic E4)` and `(enharmonic C4)` calls at lines 6-7 will silently print Fb4 / B#3 instead of E4 / C4 post-DEFER-04, but the Theory row has NO RequiredSentinels so it stays GREEN. RESEARCH Pitfall 2 documents this — the migration shape we adopt is `add a NEW test_enharmonic_edges.flow file` rather than touch the existing one. This preserves the audit trail.)
- Edit `14-deferred-items.md` (closure docs — plan 20-04's territory)

In-key behavior (D-USER-B): when `key` is active AND the input pitch matches a scale tone (existing `TryEnharmonicInKey` returns true), return the diatonic spelling — NOT the no-key edge. This preserves Phase 14 D-04. Edge-respelling fires only on the no-key fallback path. The existing branching at lines 53-61 already implements this correctly — the only modification is the natural-branch (line 44) switch.

Migration choice (D-USER-F): Per 20-RESEARCH Pitfall 1, two acceptable migration shapes — (a) rename + re-pin, (b) delete + replace. We choose (a) because it preserves the Phase14 directory's v1.2-deferred audit trail.
</phase_context>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modify Enharmonic naturals branch + author Phase20/EnharmonicEdgesTests.cs Facts</name>
  <files>flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs, flow-lang.Tests/Unit/Phase20/EnharmonicEdgesTests.cs</files>
  <read_first>
    - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (full read — focus on lines 27-67 Enharmonic public method, 234-252 ComputeFlippedSpelling, 254-272 LetterUp/Down/NaturalSemitoneOf helpers)
    - flow-lang/TypeSystem/SpecialTypes/NoteType.cs (Format / Parse / ToMidiNote signatures + canonical emission examples)
    - flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs (FlowEngineRunner Fact pattern + the 4 NoKey_NaturalUnchanged_* Facts to MIGRATE)
    - flow-lang.Tests/Fixtures/FlowEngineRunner.cs (RunSource API — returns tuple of (engine, stdout, stderr, errorCount))
  </read_first>
  <behavior>
    EnharmonicEdgesTests.cs Facts (NEW — under FlowLang.Tests.Unit.Phase20 namespace, [Collection("FlowScripts")]):

    - Test 1 (NoKey_E4_RespellsFb4): `(enharmonic E4)` returns a string whose Format emission contains "F4-" (Fb4 — MIDI 64)
    - Test 2 (NoKey_F4_RespellsEsharp4): `(enharmonic F4)` returns "E4+" (E#4 — MIDI 65)
    - Test 3 (NoKey_B4_RespellsCb5): `(enharmonic B4)` returns "C5-" (Cb5 — MIDI 71, octave +1)
    - Test 4 (NoKey_C4_RespellsBsharp3): `(enharmonic C4)` returns "B3+" (B#3 — MIDI 60, octave -1)
    - Test 5 (NoKey_D4_Unchanged): `(enharmonic D4)` returns "D4" (unchanged — D is not an edge per D-USER-C)
    - Test 6 (NoKey_G4_Unchanged): `(enharmonic G4)` returns "G4" (unchanged)
    - Test 7 (NoKey_A4_Unchanged): `(enharmonic A4)` returns "A4" (unchanged)
    - Test 8 (RoundTrip_PitchEquivalent — [Theory]): For each chromatic note in a representative set [C4, D4, E4, F4, G4, A4, B4, C5, Db4, Cs4, Ds4, Fs4, Bb4], `enharmonic(enharmonic(n))` produces a string parseable by NoteType.Parse whose ToMidiNote equals the input's ToMidiNote. Pitch-equivalent (NOT string-equivalent) per Pitfall 8.
    - Test 9 (NoKey_Fb4_RoundTripsToE4): `(enharmonic Fb4)` returns string whose MIDI == 64 (E4). Verifies the existing ComputeFlippedSpelling already handles edge inverses.
    - Test 10 (NoKey_Bsharp3_RoundTripsToC4): `(enharmonic B#3)` returns string whose MIDI == 60 (C4)
    - Test 11 (InKey_Fmajor_E4_PreservesDiatonic): `key Fmajor { (enharmonic E4) }` returns "E4" (E is diatonic in F major — preserves in-key spelling per D-USER-B)
    - Test 12 (InKey_Cmajor_E4_FallsThroughToEdge): `key Cmajor { (enharmonic E4) }` returns "F4-" (E is diatonic in Cmajor too, but the in-key branch returns it unchanged via the existing TryEnharmonicInKey path, so the natural-edge does NOT fire when in-key matches — this Fact actually pins that the IN-KEY path returns "E4". CORRECTION: redesign Fact to use a key where E is chromatic, e.g. `key Bbmajor { (enharmonic E4) }` — E is chromatic in Bb major (Bb scale: Bb C D Eb F G A), so falls through to no-key edge -> Fb4 / "F4-")

    Phase14/EnharmonicTests.cs migration (rename + re-pin per D-USER-F):
    - NoKey_NaturalUnchanged_C4 -> NoKey_NaturalEdgeRespells_C4: assert Contains("B3+") + DoesNotContain("C4") (well, "C4" appears in input — better: assert Contains("B3+") only, with explanatory comment that C4 input MIDI 60 == B#3 MIDI 60)
    - NoKey_NaturalUnchanged_E4 -> NoKey_NaturalEdgeRespells_E4: assert Contains("F4-") + (assertion that result is NOT plain "E4" — easiest is DoesNotContain via stdout exact-match, but stdout includes the input echo. Use stdout assertion shape from Phase14/EnharmonicTests existing patterns: Assert.Contains("F4-", stdout) sufficient since Format emits run-based notation deterministically)
    - NoKey_NaturalUnchanged_B4 -> NoKey_NaturalEdgeRespells_B4: assert Contains("C5-")
    - NoKey_NaturalUnchanged_F4 -> NoKey_NaturalEdgeRespells_F4: assert Contains("E4+")
  </behavior>
  <action>
    Step 1 — Modify `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` Enharmonic naturals branch (currently lines 43-47). Replace the 4-line "naturals return unchanged" block with the 5-line natural-edge switch. New code:

    Replace this current block (line 43-47):

        // D-05: naturals return unchanged, full stop — no edge respelling.
        if (alteration == 0)
        {
            return Value.Note(NoteType.Format(letter, octave, 0));
        }

    With:

        // DEFER-04 (Phase 20 plan 20-02): naturals at letter-boundary edges (E/F/B/C) respell
        // to their multi-letter neighbor. D/G/A naturals remain unchanged (no enharmonic edge
        // — they sit between two whole-step letters). In-key diatonic respelling fires earlier
        // via TryEnharmonicInKey on the alteration!=0 path; this branch handles only naturals.
        // E ↔ Fb (same octave): E4 (MIDI 64) → Fb4 (F=65, alt=-1, MIDI 64)
        // F ↔ E# (same octave): F4 (MIDI 65) → E#4 (E=64, alt=+1, MIDI 65)
        // B ↔ Cb (octave +1):   B4 (MIDI 71) → Cb5 (C5=72, alt=-1, MIDI 71)
        // C ↔ B# (octave -1):   C4 (MIDI 60) → B#3 (B3=59, alt=+1, MIDI 60)
        if (alteration == 0)
        {
            return letter switch
            {
                'E' => Value.Note(NoteType.Format('F', octave,     -1)),
                'F' => Value.Note(NoteType.Format('E', octave,     +1)),
                'B' => Value.Note(NoteType.Format('C', octave + 1, -1)),
                'C' => Value.Note(NoteType.Format('B', octave - 1, +1)),
                _   => Value.Note(NoteType.Format(letter, octave, 0)),  // D/G/A unchanged
            };
        }

    ALSO update the XML doc comment at line 27-36 to reflect the new behavior. The existing comment says "naturals (alteration == 0) always return unchanged (D-05: no E↔Fb, F↔E#, B↔Cb, C↔B# edges)". Change to: "naturals at edges (E/F/B/C) respell to multi-letter neighbor (DEFER-04 / Phase 20); D/G/A naturals return unchanged (no enharmonic edge)."

    Step 2 — Create `flow-lang.Tests/Unit/Phase20/EnharmonicEdgesTests.cs` (the directory was created in plan 20-01 Task 1). Namespace: `FlowLang.Tests.Unit.Phase20`. Decorate the class with `[Collection("FlowScripts")]` (FlowEngineRunner-based Facts must serialize per Phase 14 precedent — Enharmonic is context-dependent and tests the active MusicalContext). Author all 12 Facts described in `<behavior>` above.

    For Tests 1-7 and 9-12, use the FlowEngineRunner pattern (mirrors EnharmonicTests.cs lines 27-38 exactly):

        [Fact]
        public void NoKey_E4_RespellsFb4()
        {
            using var runner = new FlowEngineRunner();
            var (_, stdout, _, errorCount) = runner.RunSource(@"
    use ""@std""
    (print (str (enharmonic E4)))
    ");
            Assert.Equal(0, errorCount);
            Assert.Contains("F4-", stdout);
        }

    For Test 8 (RoundTrip_PitchEquivalent), use [Theory] + [InlineData] pattern. Each row asserts the round-trip via parsing the result string back through NoteType.Parse + ToMidiNote. Recommended row set (each row: input note string, expected MIDI):

        [Theory]
        [InlineData("C4", 60)]
        [InlineData("D4", 62)]
        [InlineData("E4", 64)]
        [InlineData("F4", 65)]
        [InlineData("G4", 67)]
        [InlineData("A4", 69)]
        [InlineData("B4", 71)]
        [InlineData("C5", 72)]
        [InlineData("Db4", 61)]
        [InlineData("C#4", 61)]
        [InlineData("D#4", 63)]
        [InlineData("F#4", 66)]
        [InlineData("Bb4", 70)]

    The Theory body must:
    - Run `(enharmonic <input>)` once to get result1 (extract from stdout — last token after canonical print)
    - Run `(enharmonic <result1>)` to get result2
    - Parse result2 via NoteType.Parse(result2)
    - Assert NoteType.ToMidiNote(letter, octave, alteration) == expectedMidi

    For helper extraction of the Format output string from RunSource stdout: split on whitespace, take the last non-empty token. Stdout will be something like `"F4-\n"` after a single `(print (str (enharmonic E4)))` call.

    Step 3 — Migrate the 4 Phase14/EnharmonicTests.cs Facts (rename + invert assertions). Replace the four `[Fact]` methods at lines 53-105 (`NoKey_NaturalUnchanged_C4`, `_E4`, `_B4`, `_F4`) with renamed `NoKey_NaturalEdgeRespells_C4`, `_E4`, `_B4`, `_F4` versions. Keep the `[Fact]` decorator and the runner.RunSource shape; only change:
    - Method name (rename)
    - Assert.Contains/DoesNotContain assertions (invert per the table in `<behavior>` Phase14 migration sub-section)
    - The XML doc/comment block above each Fact (mention DEFER-04 inversion)

    Add a class-level XML comment block above EnharmonicTests class noting the migration: "Phase 20 plan 20-02 (DEFER-04) MIGRATED 4 NoKey_NaturalUnchanged_* Facts into NoKey_NaturalEdgeRespells_* — naturals at edge letters (E/F/B/C) now respell to multi-letter neighbors. Phase 14 D-05 contract was deliberately scope-cut and reframed by REQUIREMENTS.md DEFER-04. Migration shape (a) per 20-RESEARCH Pitfall 1: rename + re-pin (preserves audit trail in Phase14 directory)."

    Step 4 — Bundle Steps 1-3 into ONE atomic commit. Pre-commit verification: `dotnet test --filter "FullyQualifiedName~Phase20.EnharmonicEdgesTests"` returns 12/12 GREEN; `dotnet test --filter "FullyQualifiedName~Phase14.EnharmonicTests"` returns 9/9 GREEN (4 migrated + 5 unchanged: NoKey_FlatToSharp_Db4, NoKey_SharpToFlat_Fsharp3, InKey_Dbmajor_CsharpRespells, InKey_Cmajor_FsharpFallsBack, DoubleSharp_NonInvolutive_FdoubleSharp). Commit message: `feat(20-02): DEFER-04 multi-letter enharmonic edges + 12 EnharmonicEdgesTests + Phase14 NoKey_NaturalEdgeRespells migration (4 Facts)`.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase20.EnharmonicEdgesTests|FullyQualifiedName~Phase14.EnharmonicTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` contains the literal string `DEFER-04`
    - File `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` contains the regex `'E' =&gt; Value\.Note\(NoteType\.Format\('F', octave,\s*-1\)\)` (Fb same octave)
    - File `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` contains the regex `'B' =&gt; Value\.Note\(NoteType\.Format\('C', octave \+ 1, -1\)\)` (Cb octave +1)
    - File `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` contains the regex `'C' =&gt; Value\.Note\(NoteType\.Format\('B', octave - 1, \+1\)\)` (B# octave -1)
    - File `flow-lang.Tests/Unit/Phase20/EnharmonicEdgesTests.cs` exists with namespace `FlowLang.Tests.Unit.Phase20;` and `[Collection("FlowScripts")]` attribute
    - `EnharmonicEdgesTests.cs` contains 11 `[Fact]` methods (Tests 1-7 + 9-12) AND 1 `[Theory]` method (Test 8 RoundTrip_PitchEquivalent)
    - File `flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs` contains the literal `NoKey_NaturalEdgeRespells_C4`, `NoKey_NaturalEdgeRespells_E4`, `NoKey_NaturalEdgeRespells_B4`, `NoKey_NaturalEdgeRespells_F4`
    - File `flow-lang.Tests/Unit/Phase14/EnharmonicTests.cs` does NOT contain `NoKey_NaturalUnchanged_C4` (renamed away)
    - `dotnet test --filter "FullyQualifiedName~Phase20.EnharmonicEdgesTests"` returns 12/12 GREEN
    - `dotnet test --filter "FullyQualifiedName~Phase14.EnharmonicTests"` returns 9/9 GREEN (4 migrated + 5 unchanged)
    - `dotnet test flow-sharp.sln` full suite count rises to 360 (348 prior + 12 new); zero regression
    - Phase 18 byte-identical regression Facts (filter `Phase18`) all GREEN — DEFER-04 does not touch DurationFraction or audio path
  </acceptance_criteria>
  <done>
    Enharmonic naturals branch implements 5-line edge switch for E/F/B/C and unchanged for D/G/A. 12 new Phase20/EnharmonicEdgesTests Facts (11 [Fact] + 1 [Theory]) all GREEN. 4 Phase14 NoKey_NaturalUnchanged_* Facts renamed + inverted to NoKey_NaturalEdgeRespells_* and GREEN. Full xUnit suite 348 -> 360. Atomic commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/test_enharmonic_edges.flow + RequiredSentinels pin</name>
  <files>tests/test_enharmonic_edges.flow, flow-lang.Tests/FlowScriptData.cs</files>
  <read_first>
    - tests/test_enharmonic.flow (template — existing Phase 14 file; do NOT edit it, just mirror shape)
    - flow-lang.Tests/FlowScriptData.cs (RequiredSentinels patterns; new entry placement before closing `};` of the dictionary)
    - 20-RESEARCH.md Pitfall 2 (rationale for separate file vs editing test_enharmonic.flow)
  </read_first>
  <behavior>
    - tests/test_enharmonic_edges.flow runs to completion with errorCount==0
    - tests/test_enharmonic_edges.flow stdout contains substring "F4-" (gates E4 -> Fb4)
    - tests/test_enharmonic_edges.flow stdout contains substring "E4+" (gates F4 -> E#4)
    - tests/test_enharmonic_edges.flow stdout contains substring "C5-" (gates B4 -> Cb5)
    - tests/test_enharmonic_edges.flow stdout contains substring "B3+" (gates C4 -> B#3)
    - tests/test_enharmonic_edges.flow stdout contains substring "DGA naturals unchanged: ok" (gates D/G/A unchanged path)
    - tests/test_enharmonic_edges.flow stdout contains substring "test_enharmonic_edges: PASSED" (whole-script gate)
    - FlowScriptData.cs RequiredSentinels has new `["test_enharmonic_edges.flow"]` entry pinning all 6 sentinels
  </behavior>
  <action>
    Step 1 — Create `tests/test_enharmonic_edges.flow`. Mirrors `tests/test_enharmonic.flow` shape but explicitly demonstrates DEFER-04 edges. File body must include literal sentinel strings exactly as listed in `<behavior>`. Recommended structure:
    - Line 1: `use "@std"`
    - Blank line
    - `Note: DEFER-04 multi-letter enharmonic edges (Phase 20 plan 20-02)`
    - Blank line
    - `Note: E ↔ Fb (same octave) — E4 MIDI 64 → Fb4 MIDI 64`
    - `(print (str (enharmonic E4)))`
    - `Note: F ↔ E# (same octave) — F4 MIDI 65 → E#4 MIDI 65`
    - `(print (str (enharmonic F4)))`
    - `Note: B ↔ Cb (octave +1) — B4 MIDI 71 → Cb5 MIDI 71`
    - `(print (str (enharmonic B4)))`
    - `Note: C ↔ B# (octave -1) — C4 MIDI 60 → B#3 MIDI 60`
    - `(print (str (enharmonic C4)))`
    - Blank line
    - `Note: D/G/A naturals unchanged (no edge respelling per D-USER-C)`
    - `(print (str (enharmonic D4)))`
    - `(print (str (enharmonic G4)))`
    - `(print (str (enharmonic A4)))`
    - `(print "DGA naturals unchanged: ok")`
    - Blank line
    - `Note: in-key diatonic preservation — Bbmajor scale: Bb C D Eb F G A.`
    - `Note: E is chromatic in Bbmajor → falls through to no-key edge → Fb4 / "F4-"`
    - `key Bbmajor {`
    - `    (print (str (enharmonic E4)))`
    - `}`
    - Blank line
    - `(print "test_enharmonic_edges: PASSED")`

    Step 2 — Add `RequiredSentinels` entry to `flow-lang.Tests/FlowScriptData.cs`. Locate the dictionary literal beginning at line 60. Add new entry before closing `};`. Entry shape:

        // Phase 20-02 (DEFER-04): pin multi-letter enharmonic edges (E↔Fb, F↔E#, B↔Cb, C↔B#)
        // and D/G/A naturals unchanged. Format canonical output: Fb4 → "F4-", E#4 → "E4+",
        // Cb5 → "C5-", B#3 → "B3+". If the natural-edge switch in HarmonyFunctions.Enharmonic
        // regresses, the corresponding sentinel does not print and this Theory row goes RED.
        ["test_enharmonic_edges.flow"] = new[]
        {
            "F4-",
            "E4+",
            "C5-",
            "B3+",
            "DGA naturals unchanged: ok",
            "test_enharmonic_edges: PASSED",
        },

    Step 3 — Manual sanity check: `dotnet run --project flow-interpreter tests/test_enharmonic_edges.flow`. Expect all 6 sentinels in stdout; exit 0.

    Bundle Steps 1-3 into the SAME atomic commit as Task 1 (one commit per plan). Final commit message: `feat(20-02): DEFER-04 multi-letter enharmonic edges + Phase14 NoKey_NaturalEdgeRespells migration + EnharmonicEdgesTests + test_enharmonic_edges.flow`.

    NOTE — DO NOT edit existing `tests/test_enharmonic.flow`. Lines 6-7 of that file print `(enharmonic E4)` and `(enharmonic C4)` which under DEFER-04 will produce "F4-" / "B3+" instead of "E4" / "C4". The Theory row has NO RequiredSentinels (verified at research time per 20-RESEARCH Assumption A4) so the row stays GREEN regardless of stdout. Editing it would conflate the audit trail. Per Pitfall 2, the migration shape we adopt is a NEW file (this plan's `tests/test_enharmonic_edges.flow`).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet run --project flow-interpreter tests/test_enharmonic_edges.flow && dotnet test flow-sharp.sln --filter "FullyQualifiedName~FlowScriptTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_enharmonic_edges.flow` exists and starts with `use "@std"`
    - File `tests/test_enharmonic_edges.flow` contains all 6 literal sentinel strings: `F4-`, `E4+`, `C5-`, `B3+`, `DGA naturals unchanged: ok`, `test_enharmonic_edges: PASSED`
    - File `tests/test_enharmonic_edges.flow` contains `key Bbmajor` block exercising in-key chromatic fall-through path
    - File `flow-lang.Tests/FlowScriptData.cs` contains the literal key `"test_enharmonic_edges.flow"`
    - `tests/test_enharmonic.flow` (the existing Phase 14 file) is UNCHANGED by this plan (verification: `git diff tests/test_enharmonic.flow` is empty)
    - Running `dotnet run --project flow-interpreter tests/test_enharmonic_edges.flow` returns exit code 0 and stdout contains all 6 sentinel substrings
    - `dotnet test flow-sharp.sln --filter "FullyQualifiedName~FlowScriptTests"` GREEN; new Theory row for test_enharmonic_edges.flow passes
  </acceptance_criteria>
  <done>
    `tests/test_enharmonic_edges.flow` runs end-to-end with all 6 sentinel-bearing stdout lines; FlowScriptData.cs RequiredSentinels entry committed; FlowScriptTests Theory row GREEN; `tests/test_enharmonic.flow` (existing Phase 14 file) untouched. Atomic commit complete.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (gates the atomic commit):**

1. `dotnet build flow-sharp.sln` — clean build
2. `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase20.EnharmonicEdgesTests"` — 12/12 GREEN
3. `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase14.EnharmonicTests"` — 9/9 GREEN (4 migrated NoKey_NaturalEdgeRespells_* + 5 unchanged: FlatToSharp, SharpToFlat, InKey_Dbmajor, InKey_Cmajor, DoubleSharp)
4. `dotnet test flow-sharp.sln --filter "FullyQualifiedName~FlowScriptTests"` — all Theory rows GREEN, including the new test_enharmonic_edges.flow row
5. `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase18"` — 19/19 byte-identical regression gate GREEN
6. `dotnet test flow-sharp.sln` — full suite GREEN at 348 + 12 = 360 Facts
7. `git diff tests/test_enharmonic.flow` returns no output (existing Phase 14 file untouched)

**Atomic-commit gate:** ONE commit ships all 5 modified files (HarmonyFunctions.cs, EnharmonicEdgesTests.cs, EnharmonicTests.cs migration, test_enharmonic_edges.flow, FlowScriptData.cs).
</verification>

<success_criteria>
- DEFER-04 acceptance criteria from REQUIREMENTS.md verbatim:
  - `enharmonic(E4) -> Fb4` — pinned by Test 1 (NoKey_E4_RespellsFb4) and tests/test_enharmonic_edges.flow E4 line
  - `enharmonic(F4) -> E#4` — pinned by Test 2 (NoKey_F4_RespellsEsharp4) and F4 line
  - `enharmonic(B4) -> Cb5` (octave +1) — pinned by Test 3 and B4 line
  - `enharmonic(C4) -> B#3` (octave -1) — pinned by Test 4 and C4 line
  - Round-trip pitch-equivalence — pinned by Test 8 ([Theory] across 13 chromatic notes)
- D/G/A naturals unchanged per D-USER-C — pinned by Tests 5-7 and tests/test_enharmonic_edges.flow
- In-key diatonic preservation per D-USER-B — pinned by Test 11 (key Bbmajor + E4 chromatic falls through)
- Phase 14 `NoKey_NaturalUnchanged_*` Facts MIGRATED (not deleted) per D-USER-F — preserves Phase14 audit trail
- `tests/test_enharmonic.flow` UNCHANGED (Pitfall 2 — silent stdout drift acceptable; new file is the additive migration)
- Full xUnit suite at 360 passing (348 + 12 new EnharmonicEdgesTests)
- Phase 18 byte-identical regression gate (19/19) GREEN
- Atomic commit landed with 5 file changes bundled
</success_criteria>

<output>
After completion, create `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-02-SUMMARY.md` summarizing:
- Commit hash of the atomic Task 1+2 commit
- Final test count (348 -> 360)
- Migration outcome: 4 Phase14 Facts renamed + assertions inverted (rename targets listed)
- Round-trip Theory coverage (13 chromatic notes)
- Any divergences encountered (Rule 1/2/3) — particularly around stdout extraction shape for the round-trip Theory body
- Confirmation that `tests/test_enharmonic.flow` is unchanged and that `FlowScriptData.cs:57` is preserved
- Phase 18 byte-identical regression Facts still GREEN (19/19)
- Hand-off note to plan 20-03 (DEFER-05 slice negative-from-end — independent file, can run parallel to this if both Wave 2)
</output>
