---
phase: 20-cheap-defer-closures-multi-letter-enharmonic-edges
plan: 04
type: execute
wave: 4
depends_on:
  - 20-01
  - 20-02
  - 20-03
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md
  - .planning/phases/14-composer-dx-part-1/deferred-items.md
  - .planning/phases/12-stability/deferred-items.md
  - flow-lang.Tests/FlowScriptData.cs
autonomous: true
requirements:
  - DEFER-01
  - DEFER-04
  - DEFER-05

must_haves:
  truths:
    - "REQUIREMENTS.md DEFER-01 row flipped from Pending to `Shipped <hash>` with the 20-01 commit hash"
    - "REQUIREMENTS.md DEFER-04 row flipped from Pending to `Shipped <hash>` with the 20-02 commit hash"
    - "REQUIREMENTS.md DEFER-05 row flipped from Pending to `Shipped <hash>` with the 20-03 commit hash"
    - "ROADMAP.md Phase 20 row marked complete with all 4 success criteria documented as met"
    - "ROADMAP.md Progress table row for Phase 20 updated to `4/4 Complete <date>`"
    - "STATE.md milestone progress incremented (v1.3: 2/9 -> 3/9 phases complete after Phase 20 closes)"
    - "20-VERIFICATION.md exists with criteria-to-artifact mapping + commit hash manifest + collision grep transcript"
    - "14-deferred-items.md DEFER-04 entry strikethrough applied per handling protocol §3 (preserve original + append closure note)"
    - "14-deferred-items.md DEFER-06 (slice negative-from-end — original origin) strikethrough applied per handling protocol §3 (DEFER-05 supersedes)"
    - "12-stability/deferred-items.md DEFER-01 entry strikethrough applied per handling protocol §3"
    - "FlowScriptData.cs:57 stale `[\"test_custom_oscillator.flow\"] = \"Function 'range' not found\"` ExpectedErrorScripts entry REMOVED — test_custom_oscillator.flow now flips to GREEN default branch (Test 4 unblocked by DEFER-01)"
    - "Full xUnit suite GREEN at 370/370 Facts post-removal (test_custom_oscillator.flow runs to completion and Theory row stays GREEN under errorCount==0 default gate)"
    - "Phase 21 (Pragma System + H-Alias) is unblocked — DEFER-04 closure documented as the binding pre-ordering #3 release gate"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table rows for DEFER-01/04/05 marked Shipped with hashes"
      contains: "Shipped"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 20 row marked [x] complete; Plans count 4/4; Progress table updated"
      contains: "20. Cheap DEFER Closures"
    - path: ".planning/STATE.md"
      provides: "Milestone v1.3 progress 3/9; current position cleared for Phase 21"
      contains: "Phase 20"
    - path: ".planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md"
      provides: "Phase 20 rollup: criteria-to-artifact map + commit manifest + collision grep transcript + Phase 18 regression gate confirmation"
      min_lines: 60
    - path: ".planning/phases/14-composer-dx-part-1/deferred-items.md"
      provides: "DEFER-04 + DEFER-06 (slice neg from end origin) strikethrough applied per §3"
      contains: "CLOSED"
    - path: ".planning/phases/12-stability/deferred-items.md"
      provides: "DEFER-01 strikethrough applied per §3"
      contains: "CLOSED"
    - path: "flow-lang.Tests/FlowScriptData.cs"
      provides: "Removed stale `[\"test_custom_oscillator.flow\"] = \"Function 'range' not found\"` line at :57"
      contains: ""
  key_links:
    - from: "REQUIREMENTS.md DEFER-01/04/05 rows"
      to: "20-01 / 20-02 / 20-03 atomic commit hashes"
      via: "Shipped <hash> markers"
      pattern: "Shipped [a-f0-9]+"
    - from: "20-VERIFICATION.md commit manifest"
      to: "git log --oneline output for plan 20-01..20-04"
      via: "explicit hash list with one-line per plan"
      pattern: "[a-f0-9]+"
    - from: "14-deferred-items.md DEFER-04 strikethrough"
      to: "20-02 SUMMARY"
      via: "explicit link to .planning/phases/20-.../20-02-SUMMARY.md"
      pattern: "20-02-SUMMARY"
---

<objective>
Close Phase 20. Plans 20-01, 20-02, 20-03 have shipped DEFER-01, DEFER-04, DEFER-05 as 3 atomic commits. This plan handles ONLY documentation and a single staleness-removal step — no production code changes.

Doc work:
1. Flip REQUIREMENTS.md traceability rows from `Pending` to `Shipped <hash>` for DEFER-01/04/05
2. Update ROADMAP.md Phase 20 row from `[ ]` to `[x]` with date + plan count + criteria status
3. Update STATE.md milestone progress, recent-decisions log, resume instructions for Phase 21
4. Author 20-VERIFICATION.md with criteria-to-artifact mapping + commit hash manifest + collision grep transcript + Phase 18 regression gate confirmation
5. Apply strikethrough to 14-deferred-items.md DEFER-04 and DEFER-06 entries per handling protocol §3 (preserve original + append closure note + link to 20-02-SUMMARY / 20-03-SUMMARY)
6. Apply strikethrough to 12-stability/deferred-items.md DEFER-01 entry per handling protocol §3 (link to 20-01-SUMMARY)

Single staleness-removal step:
7. Remove `["test_custom_oscillator.flow"] = "Function 'range' not found"` from `flow-lang.Tests/FlowScriptData.cs:57` (per RESEARCH Pitfall 5 — the entry is now stale post-DEFER-01; without removal, the Theory row asserts an expected error that will not appear since `range` is registered, producing FALSE-RED)

Why these are bundled into one plan rather than three: each item is docs-only and links the previous plans together. Phase 12-06, 13-04, 14-04, 15-07, 16-05, 17-08, 19-05 all followed this single closure-plan pattern. Atomic single commit closure per Phase 19-05 precedent.

Output:
- 1 atomic docs-only closure commit
- Phase 20 marked Closed in ROADMAP + STATE
- VERIFICATION.md as the rollup artifact
- DEFER-01/04/05 rows in REQUIREMENTS.md flipped
- Strikethrough handling applied to deferred-items files (audit trail preserved)
- FlowScriptData.cs:57 stale pin removed
- Phase 21 cleared to start
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-RESEARCH.md
@.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-01-PLAN.md
@.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-02-PLAN.md
@.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-03-PLAN.md
@.planning/phases/12-stability/deferred-items.md
@.planning/phases/14-composer-dx-part-1/deferred-items.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md
@flow-lang.Tests/FlowScriptData.cs
@CLAUDE.md

<interfaces>
Strikethrough handling protocol (per 14-deferred-items.md §Handling protocol — verbatim):

1. Copy the acceptance criterion into that phase's plan as a success criterion. (Already done in plans 20-01/02/03.)
2. Reference this file in the plan's `<context>` so reviewers can see the history and constraints. (Done.)
3. Once delivered, strike through the entry here (don't delete it) and link to the SUMMARY that closed it out, so the audit trail stays intact.

Strikethrough markdown shape (Phase 15-07 / Phase 19-05 precedent):

    > ~~**Original heading and body of the deferred entry, preserved verbatim**~~
    >
    > ~~Multi-line preserve — every line of the original gets `~~ ... ~~`.~~

    **CLOSED YYYY-MM-DD by Phase 20 plan 20-NN.** Implementation: <one-paragraph
    summary>. Commit hash: <hash>. See `.planning/phases/20-cheap-defer-closures-
    multi-letter-enharmonic-edges/20-NN-SUMMARY.md` for full divergences and
    verification matrix.

REQUIREMENTS.md traceability table (current Pending rows to flip):

    | DEFER-01 | Phase 20 | Pending |
    | DEFER-04 | Phase 20 | Pending |
    | DEFER-05 | Phase 20 | Pending |

After this plan:

    | DEFER-01 | Phase 20 | Shipped <hash from plan 20-01> |
    | DEFER-04 | Phase 20 | Shipped <hash from plan 20-02> |
    | DEFER-05 | Phase 20 | Shipped <hash from plan 20-03> |

ROADMAP.md Phase 20 row (currently):

    - [ ] **Phase 20: Cheap DEFER Closures + Multi-letter Enharmonic Edges** — `range(Int, Int[, Int])`, slice negative-from-end, multi-letter enharmonic edges (E↔Fb, F↔E#, B↔Cb, C↔B#)

After this plan:

    - [x] **Phase 20: Cheap DEFER Closures + Multi-letter Enharmonic Edges** — `range(Int, Int[, Int])`, slice negative-from-end, multi-letter enharmonic edges (E↔Fb, F↔E#, B↔Cb, C↔B#) — Shipped YYYY-MM-DD (commits <h1> + <h2> + <h3> + <h4>)

ROADMAP.md Progress table row (currently):

    | 20. Cheap DEFER Closures + Multi-letter Enharmonic Edges | v1.3 | 0/N | Not started | - |

After this plan:

    | 20. Cheap DEFER Closures + Multi-letter Enharmonic Edges | v1.3 | 4/4 | Complete | YYYY-MM-DD |

ROADMAP.md Phase 20 details `**Plans**: TBD` line (currently): replace with `**Plans**: 4 plans` and append the four bullets:

    - [x] 20-01-PLAN.md — DEFER-01 range stdlib (8 RangeTests Facts) — Shipped <h1>
    - [x] 20-02-PLAN.md — DEFER-04 multi-letter enharmonic edges + Phase14 NoKey_NaturalEdgeRespells migration — Shipped <h2>
    - [x] 20-03-PLAN.md — DEFER-05 slice negative-from-end (Python normalization) — Shipped <h3>
    - [x] 20-04-PLAN.md — Closure docs — Shipped <h4>

STATE.md updates:
- `progress.completed_phases` count: increment by 1 (was 2 = Phase 18 + 19; now 3 = + Phase 20)
- `progress.total_plans`: add 4 (this phase)
- `progress.completed_plans`: add 4
- `last_updated` + `last_activity`: today's date (2026-04-26)
- Current Position section: Phase 20 -> CLOSED; ready to plan Phase 21
- Resume Instructions: switch from "Phase 20 (next ROADMAP target)" to "Phase 21 (Pragma System + H-Alias) is the next ROADMAP target"
- Add a Decisions log entry summarizing Phase 20 plan-by-plan outcomes (Plan 20-01: DEFER-01, Plan 20-02: DEFER-04 + Phase14 migration, Plan 20-03: DEFER-05, Plan 20-04: closure)
- Performance Metrics By Phase: add new row `| 20 | 4 | - | - |`

FlowScriptData.cs:57 stale removal:

    // BEFORE (line 47-58 region):
    // Plan 12-05 status:
    //   - test_custom_oscillator.flow:42 if(Bool, String, String) overload — FIXED by 12-05
    //   - test_custom_oscillator.flow:57 if(Bool, Double, Double) overload — FIXED by 12-05
    //   - test_custom_oscillator.flow:86 `range` stdlib function — PRE-EXISTING, deferred to plan 12-06
    // ...
    // test_custom_oscillator stays as an expected-error row until plan 12-06 adds `range`.
    ["test_custom_oscillator.flow"] = "Function 'range' not found",

    // AFTER (DEFER-01 closure removes the entry; comment block is preserved with closure note):
    // Plan 12-05 status:
    //   - test_custom_oscillator.flow:42 if(Bool, String, String) overload — FIXED by 12-05
    //   - test_custom_oscillator.flow:57 if(Bool, Double, Double) overload — FIXED by 12-05
    //   - test_custom_oscillator.flow:86 `range` stdlib function — CLOSED by Phase 20 plan 20-01 (DEFER-01)
    //
    // test_custom_oscillator now flips to GREEN default branch (errorCount==0 gate).
    // (no entry — falls through to default GREEN branch)
</interfaces>

<phase_context>
This is the Phase 20 closure plan. Plans 20-01/02/03 must have already shipped before this plan runs. Verify with:

    git log --oneline | head -5  # confirm 20-01, 20-02, 20-03 commits present

Capture each commit hash for use in REQUIREMENTS.md / ROADMAP.md / VERIFICATION.md / strikethrough notes:
- HASH_2001 = git log of plan 20-01's atomic commit (`feat(20-01): DEFER-01 ...`)
- HASH_2002 = git log of plan 20-02's atomic commit (`feat(20-02): DEFER-04 ...`)
- HASH_2003 = git log of plan 20-03's atomic commit (`feat(20-03): DEFER-05 ...`)
- HASH_2004 = will be the commit hash THIS plan creates (use placeholder until commit lands; backfill via amend if needed — but per Phase 19-05 precedent, the closure VERIFICATION.md self-referential hash is omitted or backfilled in a no-op follow-up commit if needed)

CRITICAL — DO NOT in this plan:
- Touch any production code files (Collections.cs, HarmonyFunctions.cs, BuiltInFunctions.cs, collections.flow). All production code is frozen post 20-01/02/03.
- Touch any unit Fact files (Phase20/, Phase14/EnharmonicTests.cs). Frozen.
- Touch any new tests/test_*.flow files (test_range.flow, test_enharmonic_edges.flow, test_slice_negative.flow). Frozen.
- Run a `dotnet build` or `dotnet test` BEFORE the FlowScriptData.cs:57 removal — that removal will change the test_custom_oscillator.flow Theory row's branch (ExpectedErrors -> default GREEN). Run `dotnet test --filter "FullyQualifiedName~FlowScriptTests"` AFTER removal to confirm Theory row stays GREEN.

Phase 18 byte-identical regression gate (19/19) MUST stay GREEN after this plan — pure docs + one staleness removal that does not touch any audio path.
</phase_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture commit hashes + remove FlowScriptData.cs:57 stale pin + verify test_custom_oscillator.flow flips GREEN</name>
  <files>flow-lang.Tests/FlowScriptData.cs</files>
  <read_first>
    - flow-lang.Tests/FlowScriptData.cs (full read — focus lines 47-58, the test_custom_oscillator block)
    - 20-RESEARCH.md Pitfall 5 (rationale for removal)
    - .planning/phases/12-stability/deferred-items.md (DEFER-01 origin and the 3-step fix protocol)
  </read_first>
  <action>
    Step 1 — Capture commit hashes via:

        cd /home/noah/Desktop/projects/flow-sharp
        git log --oneline --grep="^feat(20-01)" -1
        git log --oneline --grep="^feat(20-02)" -1
        git log --oneline --grep="^feat(20-03)" -1

    Record the three short hashes (e.g. abcd123) for use in subsequent tasks. Save to a scratch note for tasks 2-4.

    Step 2 — Edit `flow-lang.Tests/FlowScriptData.cs`. Locate lines 47-58 (the `test_custom_oscillator` comment block + ExpectedErrorScripts entry).

    - Update the comment block at line 51 from `range` stdlib function — PRE-EXISTING, deferred to plan 12-06` to `range` stdlib function — CLOSED by Phase 20 plan 20-01 (DEFER-01)`
    - Update the comment block at lines 55-56 from `// test_custom_oscillator stays as an expected-error row until plan 12-06 adds range. // test_full_song entry removed: it now runs to completion after the auto-mkdir fix.` to `// test_custom_oscillator now flips to GREEN default branch (errorCount==0 gate) post-DEFER-01. // test_full_song entry removed: it now runs to completion after the auto-mkdir fix.`
    - DELETE line 57 entirely: `["test_custom_oscillator.flow"] = "Function 'range' not found",`

    Step 3 — Verify post-removal behavior:
    - `dotnet build flow-sharp.sln` — clean build
    - `dotnet run --project flow-interpreter tests/test_custom_oscillator.flow` — exit code 0 expected (Tests 1-4 all run; Test 4 was the previously-blocked range call, now legitimate)
    - `dotnet test flow-sharp.sln --filter "FullyQualifiedName~FlowScriptTests"` — full Theory suite GREEN; the test_custom_oscillator.flow Theory row now flows through the default `errorCount == 0` gate (was previously gated by ExpectedErrorScripts substring assertion).
    - `dotnet test flow-sharp.sln` — full suite GREEN at 370/370 (no count change since we are removing a Theory-row pin, not a Fact)

    Bundle Step 2 into the SAME atomic commit as tasks 2-4 below (one closure commit per Phase 19-05 precedent).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet run --project flow-interpreter tests/test_custom_oscillator.flow && dotnet test flow-sharp.sln --filter "FullyQualifiedName~FlowScriptTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-lang.Tests/FlowScriptData.cs` no longer contains the literal string `["test_custom_oscillator.flow"] = "Function 'range' not found"` (verify: `grep -c 'test_custom_oscillator.flow.*Function .range. not found' flow-lang.Tests/FlowScriptData.cs` returns 0)
    - File `flow-lang.Tests/FlowScriptData.cs` contains the closure-note string `CLOSED by Phase 20 plan 20-01`
    - Running `dotnet run --project flow-interpreter tests/test_custom_oscillator.flow` returns exit code 0
    - `dotnet test flow-sharp.sln --filter "FullyQualifiedName~FlowScriptTests"` GREEN; test_custom_oscillator.flow Theory row falls through to default `errorCount==0` branch and passes
    - Three commit hashes captured (HASH_2001, HASH_2002, HASH_2003) for later tasks; record in a scratch note (e.g. comment in FlowScriptData.cs comment block, or temporary file in `.planning/phases/20-.../`).
  </acceptance_criteria>
  <done>
    Stale `test_custom_oscillator.flow = "Function 'range' not found"` ExpectedErrorScripts entry removed. Comment block updated to reference Phase 20 closure. Three plan commit hashes captured. test_custom_oscillator.flow Theory row flips to default GREEN branch. Full suite GREEN at 370/370.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md + ROADMAP.md + STATE.md with shipped markers and progress increments</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (focus on traceability table at lines 113-145; also DEFER-01/04/05 active-requirements rows lines 49-51)
    - .planning/ROADMAP.md (focus on Phase 20 row line 66 and Progress table line 209; also Phase Details Phase 20 block lines 107-116)
    - .planning/STATE.md (full read — focus on `progress` frontmatter, Current Position, Decisions log, Resume Instructions)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md (template for VERIFICATION.md shape, used in Task 3)
  </read_first>
  <action>
    Step 1 — REQUIREMENTS.md traceability table updates (lines 113-145):
    - Find the row `| DEFER-01 | Phase 20 | Pending |` and replace `Pending` with `Shipped HASH_2001` (use captured hash from Task 1)
    - Find `| DEFER-04 | Phase 20 | Pending |` and replace `Pending` with `Shipped HASH_2002`
    - Find `| DEFER-05 | Phase 20 | Pending |` and replace `Pending` with `Shipped HASH_2003`

    Step 2 — REQUIREMENTS.md Active Requirements list (lines 47-51, the `### DEFER Closures from v1.2` section):
    - Find the three `- [ ] **DEFER-01...`, `**DEFER-04...`, `**DEFER-05...` checkboxes and flip each to `- [x]`. Acceptance criteria text preserved verbatim.

    Step 3 — ROADMAP.md Phase 20 row (line 66) — replace:

        - [ ] **Phase 20: Cheap DEFER Closures + Multi-letter Enharmonic Edges** — `range(Int, Int[, Int])`, slice negative-from-end, multi-letter enharmonic edges (E↔Fb, F↔E#, B↔Cb, C↔B#)

    With:

        - [x] **Phase 20: Cheap DEFER Closures + Multi-letter Enharmonic Edges** — `range(Int, Int[, Int])`, slice negative-from-end, multi-letter enharmonic edges (E↔Fb, F↔E#, B↔Cb, C↔B#) — Shipped 2026-04-26 (commits HASH_2001 + HASH_2002 + HASH_2003 + HASH_2004)

    HASH_2004 is the closure commit (this plan's commit) — use placeholder `<closure>` if not known until commit lands; per Phase 19-05 precedent, leave the actual hash to be backfilled in a no-op follow-up if needed, OR omit and document only the 3 production hashes.

    Step 4 — ROADMAP.md Phase 20 detail block (lines 107-116) — find the line `**Plans**: TBD` and replace with:

        **Plans**: 4 plans
        - [x] 20-01-PLAN.md — DEFER-01 range stdlib (8 RangeTests Facts + test_range.flow Theory row) — Shipped HASH_2001
        - [x] 20-02-PLAN.md — DEFER-04 multi-letter enharmonic edges (12 EnharmonicEdgesTests + Phase14 NoKey_NaturalEdgeRespells migration + test_enharmonic_edges.flow) — Shipped HASH_2002
        - [x] 20-03-PLAN.md — DEFER-05 slice negative-from-end Python normalization (10 SliceNegativeTests + test_slice_negative.flow) — Shipped HASH_2003
        - [x] 20-04-PLAN.md — Closure (REQUIREMENTS/ROADMAP/STATE/VERIFICATION + 14-deferred-items DEFER-04/06 strikethrough + 12-deferred-items DEFER-01 strikethrough + FlowScriptData.cs:57 removal)

    Step 5 — ROADMAP.md Progress table (line 209) — replace:

        | 20. Cheap DEFER Closures + Multi-letter Enharmonic Edges | v1.3 | 0/N | Not started | - |

    With:

        | 20. Cheap DEFER Closures + Multi-letter Enharmonic Edges | v1.3 | 4/4 | Complete | 2026-04-26 |

    Step 6 — STATE.md frontmatter:
    - Increment `progress.completed_phases` from 2 to 3
    - Increment `progress.total_plans` from 7 to 11 (7 + 4)
    - Increment `progress.completed_plans` from 7 to 11 (7 + 4)
    - Update `last_updated` to today's ISO timestamp (`2026-04-26TXX:XX:XX.000Z`)
    - Update `last_activity` to `2026-04-26`
    - Update `stopped_at` to `Completed 20-04-PLAN.md (Phase 20 closes — DEFER-01/04/05 shipped; commits HASH_2001 + HASH_2002 + HASH_2003 + closure)`

    Step 7 — STATE.md body:
    - Current Position section: change `Phase: 19 (Tuplets & Arbitrary Fractional Durations) — CLOSED 2026-04-26` to `Phase: 20 (Cheap DEFER Closures + Multi-letter Enharmonic Edges) — CLOSED 2026-04-26`; update Plan count line to `Plan: 4 of 4 complete`; update Status to `Phase 20 closed; ready to plan Phase 21`
    - Resume Instructions (top): replace Phase 20 forward-looking text with `Phase 20 closed 2026-04-26. v1.3 milestone now 3/9 phases complete (Phases 18, 19, 20). Phase 21 (Pragma System + H-Alias) is the next ROADMAP target.`
    - Performance Metrics By Phase table: add new row `| 20 | 4 | - | - |`
    - Performance Metrics: append 4 entries to the running per-plan timing list (Phase 20 P01..P04 with placeholder `-` for unknown timings — match Phase 19 P01..P05 entries' format)
    - Decisions log: append 4 entries summarizing Phase 20 outcomes:
      - `[Plan 20-01]: DEFER-01 range(Int, Int[, Int]) registered with both arities; Pythonic semantics; step==0 throws InvalidOperationException("range step cannot be zero"); 8 unit Facts + test_range.flow Theory row. FlowScriptData.cs:57 stale pin preserved for plan 20-04 closure (Pitfall 5 protocol).`
      - `[Plan 20-02]: DEFER-04 5-line natural-edge switch — E↔Fb / F↔E# / B↔Cb (oct+1) / C↔B# (oct-1); D/G/A unchanged per D-USER-C; in-key diatonic preservation per D-USER-B preserves Phase 14 D-04. Existing ComputeFlippedSpelling already produces correct inverses for non-natural edges. 4 Phase14/EnharmonicTests Facts MIGRATED (rename + invert per Pitfall 1 shape (a)) — audit trail preserved. 12 Phase20/EnharmonicEdgesTests + test_enharmonic_edges.flow.`
      - `[Plan 20-03]: DEFER-05 pre-clamp normalization — Python negative-from-end semantics in BOTH SliceArray and SliceSequence; Phase 14 D-01 silent-clamp tradition preserved post-normalization per D-USER-D. Existing 9 Phase14/SliceTests Facts UNCHANGED (cases coincide with old silent-clamp per RESEARCH verification matrix); zero migration. 10 Phase20/SliceNegativeTests + test_slice_negative.flow.`
      - `[Plan 20-04]: Closure — REQUIREMENTS DEFER-01/04/05 rows flipped to Shipped; ROADMAP Phase 20 marked complete; STATE milestone progress 2/9 -> 3/9; 14-deferred-items.md DEFER-04 + DEFER-06(slice neg origin) strikethrough applied per §3 (audit trail preserved); 12-deferred-items.md DEFER-01 strikethrough applied; FlowScriptData.cs:57 ExpectedErrorScripts stale pin removed (test_custom_oscillator.flow flips to default GREEN). Phase 21 (Pragma System + H-Alias) now unblocked per binding pre-ordering #3.`
    - Resume Instructions (next PC) at bottom: same content as Resume Instructions (top); also update the numbered steps (1-3) to point at Phase 21:
      - Step 2 changes from `/gsd-plan-phase 20` to `/gsd-plan-phase 21` with note "Phase 21 (Pragma System + H-Alias) depends on Phase 20 DEFER-04 (now shipped). Closes PRAG-01, PRAG-02, DEFER-02/03 (H-as-B alias inside note streams)."

    Bundle Steps 1-7 into the SAME atomic commit as Task 1's FlowScriptData.cs edit (one closure commit). Pre-commit verification: each file's diff reflects only the documented changes; no production code touched.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -E "DEFER-(01|04|05) \| Phase 20 \| Shipped" .planning/REQUIREMENTS.md && grep -E "Phase 20.*Shipped" .planning/ROADMAP.md && grep -E "completed_phases: 3" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md traceability table contains all three lines: `DEFER-01 | Phase 20 | Shipped <hash>`, `DEFER-04 | Phase 20 | Shipped <hash>`, `DEFER-05 | Phase 20 | Shipped <hash>` with real short hashes (NOT literal `<hash>`)
    - REQUIREMENTS.md Active Requirements list has all three DEFER-01/04/05 checkboxes marked `- [x]`
    - ROADMAP.md Phase 20 line contains `[x]` and `Shipped 2026-04-26`
    - ROADMAP.md Phase 20 details block contains `**Plans**: 4 plans` and 4 `- [x] 20-NN-PLAN.md` bullets with hashes
    - ROADMAP.md Progress table contains `| 20. Cheap DEFER Closures...| v1.3 | 4/4 | Complete | 2026-04-26 |`
    - STATE.md frontmatter `progress.completed_phases: 3`, `progress.total_plans: 11`, `progress.completed_plans: 11`, `last_activity: 2026-04-26`
    - STATE.md Decisions log contains 4 new `[Plan 20-XX]:` entries (one per plan)
    - STATE.md Resume Instructions point at `/gsd-plan-phase 21` for next action
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md, ROADMAP.md, STATE.md all reflect Phase 20 closure with hashes. Resume Instructions point at Phase 21. Atomic commit will bundle these edits with the FlowScriptData.cs:57 removal from Task 1 and the strikethroughs from Tasks 3-4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 20-VERIFICATION.md rollup with criteria-to-artifact map + commit manifest + collision grep transcript</name>
  <files>.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md (template — Phase 19 closure VERIFICATION)
    - .planning/phases/15-composer-dx-part-2/15-VERIFICATION.md (alternate template — re-surfacing collision grep transcripts)
    - .planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-RESEARCH.md (acceptance criteria for cross-reference)
    - 20-01-PLAN.md, 20-02-PLAN.md, 20-03-PLAN.md (must-haves for criteria-to-artifact mapping)
    - 20-01-SUMMARY.md, 20-02-SUMMARY.md, 20-03-SUMMARY.md (if exist; for divergence rollup)
  </read_first>
  <action>
    Create `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md` with the following structure (mirrors Phase 19 + Phase 15 closure VERIFICATION precedent):

    Section 1 — Header:
    - Title `# Phase 20 Verification — Cheap DEFER Closures + Multi-letter Enharmonic Edges`
    - Closed date `2026-04-26`
    - Commits: 4 lines (HASH_2001..HASH_2004) with the commit message titles

    Section 2 — ROADMAP Success Criteria mapping (per the 4 criteria at ROADMAP lines 111-115):
    - Criterion 1 (DEFER-01): cite Phase20/RangeTests.cs (8 Facts) + tests/test_range.flow (4 sentinels) + commit HASH_2001. Verified: ✅
    - Criterion 2 (DEFER-04): cite Phase20/EnharmonicEdgesTests.cs (12 Facts) + Phase14/EnharmonicTests.cs (4 migrated NoKey_NaturalEdgeRespells_*) + tests/test_enharmonic_edges.flow (6 sentinels) + commit HASH_2002. Verified: ✅
    - Criterion 3 (DEFER-05): cite Phase20/SliceNegativeTests.cs (10 Facts) + Phase14/SliceTests.cs (9 unchanged — coincidence verified) + tests/test_slice_negative.flow (5 sentinels) + commit HASH_2003. Verified: ✅
    - Criterion 4 (DEFER-05 collision grep): cite the empty grep transcript below. Verified: ✅

    Section 3 — REQ-ID Traceability:
    - DEFER-01: Phase20/RangeTests.cs all 8 Facts + tests/test_range.flow Theory row + Collections.Range registered as 2-arg + 3-arg + collections.flow 2 internal proc declarations. Shipped HASH_2001.
    - DEFER-04: Phase20/EnharmonicEdgesTests.cs 12 Facts + Phase14/EnharmonicTests.cs 4 renamed Facts + tests/test_enharmonic_edges.flow Theory row + HarmonyFunctions.Enharmonic naturals branch updated. Shipped HASH_2002.
    - DEFER-05: Phase20/SliceNegativeTests.cs 10 Facts + tests/test_slice_negative.flow Theory row + Collections.SliceArray + Collections.SliceSequence both updated with pre-clamp normalization. Shipped HASH_2003.

    Section 4 — Migration Items (per D-USER-F):
    - Phase14/EnharmonicTests.cs: 4 Facts MIGRATED (NoKey_NaturalUnchanged_C4/E4/B4/F4 -> NoKey_NaturalEdgeRespells_C4/E4/B4/F4 with inverted assertions). Migration shape (a) per RESEARCH Pitfall 1 — preserves audit trail in Phase14 directory.
    - FlowScriptData.cs:57: stale `test_custom_oscillator.flow = "Function 'range' not found"` ExpectedErrorScripts entry REMOVED. test_custom_oscillator.flow Theory row now flows through default `errorCount==0` branch.
    - 14-deferred-items.md: DEFER-04 + DEFER-06 (slice neg-from-end origin) strikethrough applied per handling protocol §3 (Task 4).
    - 12-stability/deferred-items.md: DEFER-01 strikethrough applied per handling protocol §3 (Task 4).

    Section 5 — Collision grep transcript (per Pitfall 6):
    - Command: `grep -rn "slice.*,.*,.*-" tests/`
    - Result: empty (verified at research time and re-verified at plan 20-03 commit time)
    - Significance: zero existing user scripts rely on the silent-clamp old behavior of negative slice indices. DEFER-05 is a clean cut-over.

    Section 6 — Phase 18 byte-identical regression gate:
    - Phase 18 19/19 Facts GREEN post Phase 20 closure (verified at each plan's atomic commit time)
    - Significance: DEFER-01/04/05 do not interact with `MusicalNoteData.DurationFraction` or any audio path; the byte-identical determinism contract for tutorial.flow + showcase.flow holds.

    Section 7 — Test count progression:
    - Pre-Phase-20 baseline: 340/340
    - Post-20-01: 348/348 (+8 RangeTests Facts)
    - Post-20-02: 360/360 (+12 EnharmonicEdgesTests Facts; 4 Phase14/EnharmonicTests renamed)
    - Post-20-03: 370/370 (+10 SliceNegativeTests Facts)
    - Post-20-04 (this commit): 370/370 (no Fact-count change — docs + 1 Theory-row pin removal)
    - Plus per-plan FlowScripts Theory rows for new test_*.flow files (test_range.flow + test_enharmonic_edges.flow + test_slice_negative.flow — 3 rows added).

    Section 8 — Phase 21 unblocking:
    - DEFER-04 closure satisfies binding pre-ordering #3 — Phase 21 (Pragma System + H-Alias) can now begin. PRAG-01 (file-scope `enable <pragma>;`), PRAG-02 (no propagation across `use` imports), and DEFER-02/03 (H-as-B alias inside note streams) are next.

    Section 9 — Deviations:
    - Aggregate any Rule 1 / Rule 2 / Rule 3 deviations recorded in 20-01-SUMMARY / 20-02-SUMMARY / 20-03-SUMMARY (if those SUMMARYs exist; otherwise document NONE)
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && test -f .planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md && grep -c "Verified: " .planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md` exists with at least 60 lines
    - 20-VERIFICATION.md contains 4 `Verified: ✅` lines (one per ROADMAP success criterion)
    - 20-VERIFICATION.md contains the literal string `grep -rn "slice.*,.*,.*-"` and the result `empty`
    - 20-VERIFICATION.md contains the test-count progression `340/340 → 348/348 → 360/360 → 370/370`
    - 20-VERIFICATION.md contains all three commit hashes (HASH_2001, HASH_2002, HASH_2003)
    - 20-VERIFICATION.md mentions Phase 18 byte-identical regression gate as GREEN
    - 20-VERIFICATION.md cites Phase 21 unblocking via binding pre-ordering #3
  </acceptance_criteria>
  <done>
    20-VERIFICATION.md authored as the Phase 20 rollup. Cross-references all four success criteria to artifacts and commit hashes. Migration items, collision grep transcript, and Phase 18 regression gate confirmation included.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply strikethrough handling to 14-deferred-items.md DEFER-04/06 and 12-stability/deferred-items.md DEFER-01 + commit closure</name>
  <files>.planning/phases/14-composer-dx-part-1/deferred-items.md, .planning/phases/12-stability/deferred-items.md</files>
  <read_first>
    - .planning/phases/14-composer-dx-part-1/deferred-items.md (full read — focus DEFER-04 lines 101-118 and DEFER-06 lines 158-168; also DEFER-05 closed example at lines 122-154 as the strikethrough template)
    - .planning/phases/12-stability/deferred-items.md (full read)
    - .planning/phases/15-composer-dx-part-2/15-VERIFICATION.md (Phase 15 strikethrough precedent for Phase 14 DEFER-05)
  </read_first>
  <action>
    Step 1 — Apply strikethrough to `.planning/phases/14-composer-dx-part-1/deferred-items.md` DEFER-04 entry (lines 101-118). Wrap the existing 7 content lines (heading + 5 body paragraphs + closing rationale) in `> ~~ ... ~~` block-quote-strikethrough markdown shape mirroring the existing DEFER-05 closure (lines 122-154 in the same file). Then append a closure note paragraph below:

        **CLOSED 2026-04-26 by Phase 20 plan 20-02 (DEFER-04).** Implementation: 5-line
        natural-edge switch in `HarmonyFunctions.Enharmonic` — E↔Fb / F↔E# (same octave) /
        B↔Cb (octave +1) / C↔B# (octave -1); D/G/A naturals remain unchanged (no enharmonic
        edge per CONTEXT D-USER-C). In-key diatonic preservation rule (D-USER-B) preserves
        Phase 14 D-04 — edge respelling fires only on no-key fallback. Existing
        `ComputeFlippedSpelling` already produces correct inverses for the non-natural
        edge cases (Fb→E, E#→F, Cb→B, B#→C). 4 Phase14/EnharmonicTests Facts migrated to
        `NoKey_NaturalEdgeRespells_*` per Pitfall 1 shape (a). Commit hash: HASH_2002. See
        `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-02-SUMMARY.md`
        for full divergences.

    Step 2 — Apply strikethrough to `.planning/phases/14-composer-dx-part-1/deferred-items.md` DEFER-06 entry (lines 158-168). Wrap the existing 9 content lines in `> ~~ ... ~~` shape. Append closure note:

        **CLOSED 2026-04-26 by Phase 20 plan 20-03 (DEFER-05).** Implementation: pre-clamp
        normalization (`idx < 0 ? idx + count : idx`) in BOTH `Collections.SliceArray` and
        `Collections.SliceSequence`, preserving Phase 14 D-01 silent-clamp tradition for
        post-normalization out-of-range values per CONTEXT D-USER-D. The original DEFER-06
        recommendation ("add a separate overload or `reverseSlice` / `sliceFromEnd` rather
        than overload existing semantics") was superseded by REQUIREMENTS.md DEFER-05 which
        explicitly framed this as a behavioral change. Existing 9 Phase14/SliceTests Facts
        UNCHANGED (verification matrix shows cases coincide between old silent-clamp and new
        Python normalization). Repository grep `slice.*,.*,.*-` over `tests/` empty —
        zero existing user scripts relied on the old clamp. Commit hash: HASH_2003. See
        `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-03-SUMMARY.md`.

    Step 3 — Apply strikethrough to `.planning/phases/12-stability/deferred-items.md` DEFER-01 entry. Read the file first to locate the entry (it should mirror the 14-deferred-items.md format). Wrap the existing entry content in `> ~~ ... ~~`. Append closure note:

        **CLOSED 2026-04-26 by Phase 20 plan 20-01 (DEFER-01).** Implementation: registered
        `range(Int, Int)` and `range(Int, Int, Int)` overloads in
        `BuiltInFunctions.RegisterCollections` mapping to new `Collections.Range` method;
        added 2 `internal proc range` declarations in `collections.flow` per Phase 14 plan
        14-02 stdlib-registration contract. Pythonic semantics — start inclusive, end
        exclusive, default step=1, negative step iterates backward, empty array when
        unsatisfiable, step==0 throws InvalidOperationException("range step cannot be zero").
        8 Phase20/RangeTests unit Facts + 1 tests/test_range.flow Theory row pin
        acceptance. FlowScriptData.cs:57 stale ExpectedErrorScripts entry
        (`test_custom_oscillator.flow = "Function 'range' not found"`) removed in plan 20-04
        closure — test_custom_oscillator.flow now flips to default GREEN branch. Commit
        hash: HASH_2001. See
        `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-01-SUMMARY.md`.

    Step 4 — Stage all 7 files modified across this closure plan (Tasks 1-4):
    - `flow-lang.Tests/FlowScriptData.cs` (Task 1)
    - `.planning/REQUIREMENTS.md` (Task 2)
    - `.planning/ROADMAP.md` (Task 2)
    - `.planning/STATE.md` (Task 2)
    - `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-VERIFICATION.md` (Task 3)
    - `.planning/phases/14-composer-dx-part-1/deferred-items.md` (Task 4)
    - `.planning/phases/12-stability/deferred-items.md` (Task 4)

    Step 5 — Single atomic closure commit. Commit message:

        docs(20-04): Phase 20 closure — DEFER-01/04/05 shipped, REQUIREMENTS/ROADMAP/STATE updated, 14+12 deferred-items strikethrough, FlowScriptData.cs:57 stale pin removed

    Step 6 — Final post-commit verification:
    - `dotnet test flow-sharp.sln` — full suite GREEN at 370/370 Facts
    - `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase18"` — 19/19 GREEN
    - `git log --oneline | head -5` — 4 plan commits visible (20-01, 20-02, 20-03, 20-04 closure)
    - `grep -c "test_custom_oscillator.flow.*Function" flow-lang.Tests/FlowScriptData.cs` returns 0
    - All three deferred-items entries show `CLOSED 2026-04-26` strikethrough notes
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -c "CLOSED 2026-04-26 by Phase 20" .planning/phases/14-composer-dx-part-1/deferred-items.md && grep -c "CLOSED 2026-04-26 by Phase 20" .planning/phases/12-stability/deferred-items.md && dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase18"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/14-composer-dx-part-1/deferred-items.md` contains TWO `CLOSED 2026-04-26 by Phase 20` strings (one for DEFER-04, one for DEFER-06)
    - File `.planning/phases/14-composer-dx-part-1/deferred-items.md` contains DEFER-04 + DEFER-06 entries wrapped in `> ~~ ... ~~` block-quote-strikethrough markdown (verification: grep for `> ~~\*\*DEFER-04` and `> ~~\*\*DEFER-06`)
    - File `.planning/phases/12-stability/deferred-items.md` contains ONE `CLOSED 2026-04-26 by Phase 20` string (DEFER-01)
    - All strikethrough notes link to the corresponding `20-NN-SUMMARY.md`
    - Single atomic commit lands all 7 modified files (verification: `git show --stat HEAD` shows 7 files)
    - `dotnet test flow-sharp.sln` returns 370/370 GREEN
    - `dotnet test --filter "FullyQualifiedName~Phase18"` returns 19/19 GREEN (byte-identical regression gate)
  </acceptance_criteria>
  <done>
    14-deferred-items.md DEFER-04 and DEFER-06 entries strikethrough applied with closure notes linking to 20-02-SUMMARY and 20-03-SUMMARY. 12-stability/deferred-items.md DEFER-01 strikethrough applied linking to 20-01-SUMMARY. Single atomic closure commit lands all 7 modified files. Full suite GREEN at 370/370. Phase 20 closes officially.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (gates the atomic closure commit):**

1. `git log --oneline | head -5` — 4 commits visible: 20-01, 20-02, 20-03, 20-04 closure
2. `grep -E "DEFER-(01|04|05) \| Phase 20 \| Shipped" .planning/REQUIREMENTS.md` — 3 lines
3. `grep "Phase 20.*Shipped 2026-04-26" .planning/ROADMAP.md` — 1 line
4. `grep "completed_phases: 3" .planning/STATE.md` — 1 line
5. `grep -c "CLOSED 2026-04-26 by Phase 20" .planning/phases/14-composer-dx-part-1/deferred-items.md` — 2 (DEFER-04 + DEFER-06)
6. `grep -c "CLOSED 2026-04-26 by Phase 20" .planning/phases/12-stability/deferred-items.md` — 1 (DEFER-01)
7. `grep -c 'test_custom_oscillator.flow.*Function .range. not found' flow-lang.Tests/FlowScriptData.cs` — 0 (stale pin removed)
8. `dotnet test flow-sharp.sln` — 370/370 GREEN
9. `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase18"` — 19/19 byte-identical GREEN
10. `dotnet run --project flow-interpreter tests/test_custom_oscillator.flow` — exit 0
11. `git show --stat HEAD` — 7 files in the closure commit (no production code)

**Atomic-commit gate:** ONE closure commit ships all 7 docs files + the FlowScriptData.cs:57 removal. No production code touched.
</verification>

<success_criteria>
- All 4 ROADMAP success criteria for Phase 20 marked Verified ✅ in 20-VERIFICATION.md with cited artifacts and hashes
- All 3 DEFER REQ-IDs (DEFER-01, DEFER-04, DEFER-05) flipped to `Shipped <hash>` in REQUIREMENTS.md traceability table
- ROADMAP.md Phase 20 row marked complete with date and 4 plan bullets
- STATE.md milestone progress 2/9 → 3/9 phases for v1.3
- 14-deferred-items.md DEFER-04 + DEFER-06 strikethrough applied per handling protocol §3 (audit trail preserved)
- 12-stability/deferred-items.md DEFER-01 strikethrough applied per handling protocol §3
- FlowScriptData.cs:57 stale `test_custom_oscillator.flow = "Function 'range' not found"` ExpectedErrorScripts entry removed; test_custom_oscillator.flow Theory row flips to default `errorCount==0` GREEN
- Full xUnit suite at 370/370 (no Fact-count change in this plan; +3 FlowScripts Theory rows from plans 20-01/02/03)
- Phase 18 byte-identical regression gate (19/19) GREEN
- Phase 21 explicitly unblocked per binding pre-ordering #3 — DEFER-04 closure documented as the gate
- Single atomic closure commit landed
</success_criteria>

<output>
After completion, create `.planning/phases/20-cheap-defer-closures-multi-letter-enharmonic-edges/20-04-SUMMARY.md` summarizing:
- Closure commit hash (HASH_2004)
- Files modified (7 total — 1 test infra removal + 6 docs)
- 14-deferred-items.md strikethrough verification (DEFER-04 + DEFER-06 both wrapped)
- 12-deferred-items.md strikethrough verification (DEFER-01 wrapped)
- FlowScriptData.cs:57 stale pin removal verified — test_custom_oscillator.flow Theory row default-GREEN
- Final test count: 370/370 with 3 new FlowScripts Theory rows
- Phase 18 byte-identical regression gate (19/19) GREEN
- Phase 20 closes officially; v1.3 milestone now 3/9 phases complete
- Hand-off note to Phase 21 (Pragma System + H-Alias) — depends on DEFER-04 (now shipped)
</output>
