---
phase: 25-gaussian-humanize-last-prng-phase
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs
  - flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs
  - flow-lang.Tests/FlowScriptData.cs
  - tests/test_humanize_gaussian.flow
autonomous: true
requirements: [DEFER-06]
tags: [humanize, gaussian, box-muller, prng, phase-25, wave-0, scaffold]
must_haves:
  truths:
    - "D-23: 7 Phase25 unit Fact methods are declared in HumanizeGaussianFacts.cs (Skip-marked RED state OK while humanizeGaussian does not exist)"
    - "D-24: 2 Phase25 integration Fact methods are declared in ByteIdenticalShowcaseGaussianTests.cs (Skip-marked while showcase.flow not yet edited)"
    - "tests/test_humanize_gaussian.flow exists with both sentinels (humanizeGaussian seed=42: PASSED + two runs byte-identical: PASSED)"
    - "FlowScriptData.cs registers test_humanize_gaussian.flow with both expected sentinels (mirrors :225-231 pattern)"
    - "dotnet build flow-lang.Tests exits 0 — Skip-marked Facts compile against existing code; humanizeGaussian symbol absence is gated behind Skip"
    - "flow-lang.Tests/Unit/Phase25/ and flow-lang.Tests/Integration/Phase25/ directories exist"
  artifacts:
    - path: "flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs"
      provides: "xUnit Facts skeleton — 7 D-23 Fact methods declared, [Collection FlowScripts], private const Tol=1e-9, BaseVelocity=0.63"
      contains: "class HumanizeGaussianFacts"
    - path: "flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs"
      provides: "xUnit Facts skeleton — D-24 Showcase_TwoRunsProduceIdenticalWav + Showcase_TwoRunsProduceIdenticalMidi"
      contains: "class ByteIdenticalShowcaseGaussianTests"
    - path: "tests/test_humanize_gaussian.flow"
      provides: "End-to-end smoke (seeded byte-identical .flow integration)"
      contains: "humanizeGaussian seed=42: PASSED"
    - path: "flow-lang.Tests/FlowScriptData.cs"
      provides: "Sentinel registration for test_humanize_gaussian.flow"
      contains: "test_humanize_gaussian.flow"
  key_links:
    - from: "flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs"
      to: "Tol/BaseVelocity constants from Phase15/EuclideanSwingTests.cs:32-33"
      via: "verbatim copy of constants per RESEARCH §Wave-0 gaps"
      pattern: "private const double Tol = 1e-9"
    - from: "flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs"
      to: "Phase18/ByteIdenticalShowcaseTests.cs:1-90 verbatim mirror"
      via: "same RunTwiceAndCompare body, only namespace + run-file basenames change"
      pattern: "phase25_showcase_run1"
    - from: "flow-lang.Tests/FlowScriptData.cs (new dictionary entry)"
      to: "tests/test_humanize_gaussian.flow"
      via: "ExpectedSuccessSentinels dictionary"
      pattern: "test_humanize_gaussian.flow"
---

<objective>
Wave 0 scaffold: stand up the Phase 25 test directory tree, sentinel registrations, and Skip-marked test skeletons so that Plans 25-01..25-04 can incrementally turn each Fact GREEN. No production code added in this plan — only tests + smoke fixtures.

Per RESEARCH §Wave 0 Gaps and VALIDATION.md, Wave 0 must be complete before any plan can call `humanizeGaussian` from a Fact (it does not exist yet) or from showcase.flow (D-20 edit deferred to 25-03). Skeleton Facts use `Skip = "Plan 25-NN: implementation pending"` so the suite stays GREEN through the build.

Purpose: every downstream plan depends on these test files existing. RED-cascade is prevented by Skip-marked stubs that flip to live Facts as each plan ships.

Output: 4 new test/fixture files committed, 1 modified (FlowScriptData.cs sentinel entry); `dotnet build flow-lang.Tests` exits 0; `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase25"` reports the Skip-marked Facts as Skipped (not Failed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-gaussian-humanize-last-prng-phase/25-CONTEXT.md
@.planning/phases/25-gaussian-humanize-last-prng-phase/25-RESEARCH.md
@.planning/phases/25-gaussian-humanize-last-prng-phase/25-PATTERNS.md
@.planning/phases/25-gaussian-humanize-last-prng-phase/25-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From flow-lang.Tests/Unit/Phase15/EuclideanSwingTests.cs:32-33 (constants to mirror verbatim):
```csharp
private const double BaseVelocity = 0.63;
private const double Tol = 1e-9;
```

From flow-lang.Tests/Unit/Phase15/EuclideanSwingTests.cs (class header pattern):
```csharp
[Collection("FlowScripts")]
public class EuclideanSwingTests
{
    private const double BaseVelocity = 0.63;
    private const double Tol = 1e-9;
    ...
}
```

From flow-lang.Tests/Integration/Phase18/ByteIdenticalShowcaseTests.cs:1-90 (verbatim mirror target):
```csharp
using System;
using System.IO;
using System.Linq;
using FlowLang.Tests;
using FlowLang.Tests.Fixtures;
using Xunit;

namespace FlowLang.Tests.Integration.Phase18;

[Collection("FlowScripts")]
public class ByteIdenticalShowcaseTests
{
    [Fact]
    public void Showcase_TwoRunsProduceIdenticalWav() { RunTwiceAndCompare(isMidi: false); }

    [Fact]
    public void Showcase_TwoRunsProduceIdenticalMidi() { RunTwiceAndCompare(isMidi: true); }

    private static void RunTwiceAndCompare(bool isMidi) { ... full body in PATTERNS.md:343-429 ... }
}
```

From flow-lang.Tests/FlowScriptData.cs:225-231 (sentinel registration pattern):
```csharp
// Phase 15 DX-09: euclidean 6-arg humanize overload, same-seed byte-identical.
["test_euclidean_humanize.flow"] = new[]
{
    "euclidean humanize seed=42: PASSED",
    "two runs byte-identical: PASSED",
},
```

From tests/test_euclidean_humanize.flow (structural analog for the new smoke):
```flow
use "@std"
use "@audio"
tempo 120 {
    timesig 4/4 {
        Sequence a = (euclidean 3 8 C4 0.3 0.1 42)
        ...
        (writeMidi "tests/output/phase15_euclidean_humanize_a.mid" songA)
        (print "euclidean humanize seed=42: PASSED")
        ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase25 unit test directory + Skip-marked HumanizeGaussianFacts.cs skeleton (D-23)</name>
  <files>flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs</files>
  <read_first>
    - flow-lang.Tests/Unit/Phase15/EuclideanSwingTests.cs (lines 1-90 — exact analog: [Collection], constants, BaseVelocity, Tol, class shape)
    - flow-lang.Tests/Unit/Phase22/ArpeggioFacts.cs (lines 1-30 — header docstring "byte-identical determinism" comment style for the D-ID anchor block)
    - flow-lang/Runtime/SequenceData.cs (verify Bars, AddBar API surface — referenced in fixture builders later)
    - .planning/phases/25-gaussian-humanize-last-prng-phase/25-CONTEXT.md (D-23 Facts list, D-09/D-10/D-11 invariants)
    - .planning/phases/25-gaussian-humanize-last-prng-phase/25-RESEARCH.md §Test Specifics table (Fact construction strategies)
  </read_first>
  <behavior>
    - All 7 D-23 Fact methods declared with `[Fact(Skip = "Plan 25-02: humanizeGaussian impl pending")]` so they compile + register but report as Skipped.
    - Class-level `private const double Tol = 1e-9;` and `private const double BaseVelocity = 0.63;` (matches Phase15/EuclideanSwingTests.cs:32-33 verbatim).
    - `[Collection("FlowScripts")]` attribute (avoids parallel-execution conflicts on FlowEngineRunner per PATTERNS §Shared Patterns).
    - Class docstring cites D-01..D-25 anchors and RESEARCH §Validation Architecture (mirrors Phase22/ArpeggioFacts.cs:7-25 style).
    - Build is GREEN — no reference to `humanizeGaussian` in the bodies (Skip prevents execution).
  </behavior>
  <action>
Create directory `flow-lang.Tests/Unit/Phase25/` (parent `Unit/` exists already; verify with `ls flow-lang.Tests/Unit/Phase24/` returning the existing Phase24 directory). Then write the file with this EXACT content:

```csharp
using FlowLang.Runtime;
using FlowLang.Tests.Fixtures;
using FlowLang.TypeSystem.SpecialTypes;
using Xunit;

namespace FlowLang.Tests.Unit.Phase25;

/// <summary>
/// Phase 25 (DEFER-06): humanizeGaussian(Sequence, Double, Int) Box-Muller transform.
/// Pins deterministic-by-seed velocity perturbation, rest passthrough, clamp invariants,
/// and statistical sanity. Anchors decisions D-01..D-25 from 25-CONTEXT.md.
///
/// CRITICAL invariants (RESEARCH §Common Pitfalls):
///   D-01  signature (Sequence, Double, Int) order (seq, amount, seed)
///   D-03  LOCAL new Random(seed); never touches ExecutionContext.GetRand
///   D-05  basic Box-Muller (cos branch); D-06 sin discarded
///   D-07  velJitter = z * amount * 0.2
///   D-09  velocity clamped to [0.05, 1.0]
///   D-10  amount==0 short-circuit returns input unchanged
///   D-11  rests pass through (no PRNG consumption)
///   D-18  existing humanize is FROZEN — these Facts pin humanizeGaussian only
///
/// Skip-marked while Plan 25-02 is pending; Plan 25-02 removes Skip + freezes pin values.
/// </summary>
[Collection("FlowScripts")]
public class HumanizeGaussianFacts
{
    private const double BaseVelocity = 0.63;  // matches Phase15/EuclideanSwingTests.cs:32
    private const double Tol = 1e-9;           // matches Phase15/EuclideanSwingTests.cs:33

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void Seeded42_FirstNoteVelocity_PinnedExactly()
    {
        // Plan 25-02 fills body: build 4-note SequenceData via direct C# MusicalNoteData
        // (avoids MusicalContext.Velocity interference per Pitfall 4),
        // call humanizeGaussian(seq, 0.1, 42), assert hits[0].Velocity equals frozen pin.
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void Seeded42_TwoConsecutiveCalls_ProduceIdenticalOutput()
    {
        // Plan 25-02: call twice with seed=42, assert all velocities equal across calls.
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void DifferentSeeds_ProduceDifferentOutput()
    {
        // Plan 25-02: call with seed=42 and seed=43, assert at least one velocity differs > Tol.
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void AmountZero_ReturnsInputUnchanged()
    {
        // Plan 25-02: D-10 short-circuit verification; assert all velocities equal base.
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void Rests_PassThroughUnchanged()
    {
        // Plan 25-02: D-11 rest invariance — mixed fixture (2 notes + 2 rests),
        // call with amount=0.5 seed=42, assert rest entries unchanged.
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void Velocity_ClampedTo_005_to_10()
    {
        // Plan 25-02: D-09 clamp; build fixture with extreme baseline (velocity=0.99),
        // call with amount=1.0 seed=42 over 100 notes, assert min>=0.05 max<=1.0.
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }

    [Fact(Skip = "Plan 25-02: humanizeGaussian implementation pending")]
    public void LargeSequence_DistributionIsApproximatelyNormal()
    {
        // Plan 25-02: 1000 notes at base 0.5, amount=0.5 seed=42; assert |mean|<0.02
        // AND |stddev-0.1|/0.1 < 0.20 (statistical sanity for Box-Muller correctness).
        Assert.True(false, "skeleton — Plan 25-02 fills body");
    }
}
```

Note: `Tol` and `BaseVelocity` are declared but unused in skeleton — they will be used in Plan 25-02. Ignore "unused field" warnings (xUnit + .NET 10 do not warn on private const).
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests 2>&amp;1 | grep -E "(error|Build succeeded)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs` exits 0
    - `grep -c "Skip = \"Plan 25-02" flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs` returns exactly `7`
    - `grep -c "private const double Tol = 1e-9" flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs` returns exactly `1`
    - `grep -c "private const double BaseVelocity = 0.63" flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs` returns exactly `1`
    - `grep -c "\[Collection(\"FlowScripts\")\]" flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs` returns exactly `1`
    - `grep -c "humanizeGaussian" flow-lang.Tests/Unit/Phase25/HumanizeGaussianFacts.cs` returns at least `1` (in docstrings only — body uses Assert.True(false))
    - `dotnet build flow-lang.Tests 2>&1 | grep -c "Build succeeded"` returns at least `1`
  </acceptance_criteria>
  <done>HumanizeGaussianFacts.cs file exists with all 7 Skip-marked Fact methods + class constants + Collection attribute; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Phase25 integration test skeleton (D-24) — verbatim mirror of Phase18/ByteIdenticalShowcaseTests.cs</name>
  <files>flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs</files>
  <read_first>
    - flow-lang.Tests/Integration/Phase18/ByteIdenticalShowcaseTests.cs (lines 1-90 — VERBATIM MIRROR target; only namespace + run-file basenames change)
    - flow-lang.Tests/Fixtures/FlowEngineRunner.cs (verify ctor + RunSource API surface — referenced in body)
    - .planning/phases/25-gaussian-humanize-last-prng-phase/25-CONTEXT.md D-21, D-24 (integration test contract)
    - .planning/phases/25-gaussian-humanize-last-prng-phase/25-PATTERNS.md §ByteIdenticalShowcaseGaussianTests (substitution rules)
  </read_first>
  <behavior>
    - Both [Fact] methods Skip-marked while Plan 25-03 is pending (showcase.flow does not yet contain humanizeGaussian, so the test would still pass in skeleton state but Skip is clearer about phase-gated activation).
    - Namespace `FlowLang.Tests.Integration.Phase25` (not Phase18).
    - Class name `ByteIdenticalShowcaseGaussianTests` (not ByteIdenticalShowcaseTests).
    - Run-file basenames `phase25_showcase_run1.{ext}` and `phase25_showcase_run2.{ext}` (not phase18_).
    - Body of `RunTwiceAndCompare` is BYTE-FOR-BYTE identical to Phase18 analog except for the run-file basename string substitution.
  </behavior>
  <action>
Create directory `flow-lang.Tests/Integration/Phase25/` (parent `Integration/` exists; verify with `ls flow-lang.Tests/Integration/Phase18/`). Then write the file with this EXACT content:

```csharp
using System;
using System.IO;
using System.Linq;
using FlowLang.Tests;
using FlowLang.Tests.Fixtures;
using Xunit;

namespace FlowLang.Tests.Integration.Phase25;

/// <summary>
/// Phase 25 DEFER-06 byte-identical determinism gate for examples/showcase.flow
/// post-D-20 additive humanizeGaussian call site on melody (seed=314, amount=0.08).
///
/// Mirrors Phase18/ByteIdenticalShowcaseTests.cs:1-90 verbatim — the only changes are
/// the namespace, the class name, and the run-file basenames.
///
/// The assertion is bytes1.SequenceEqual(bytes2) — two consecutive runs produce
/// identical output. Phase 18's existing showcase test continues to pass under the
/// same assertion (self-re-pinning per RESEARCH §Critical Note on Phase 18 Showcase
/// Test) — both tests assert run-to-run identity, not against a frozen byte set.
///
/// Skip-marked while Plan 25-03 is pending (showcase.flow edit lives there); Plan
/// 25-03 removes Skip after the additive humanizeGaussian wraps the melody Sequence.
/// </summary>
[Collection("FlowScripts")]
public class ByteIdenticalShowcaseGaussianTests
{
    [Fact(Skip = "Plan 25-03: showcase.flow humanizeGaussian call site pending")]
    public void Showcase_TwoRunsProduceIdenticalWav()
    {
        RunTwiceAndCompare(isMidi: false);
    }

    [Fact(Skip = "Plan 25-03: showcase.flow humanizeGaussian call site pending")]
    public void Showcase_TwoRunsProduceIdenticalMidi()
    {
        RunTwiceAndCompare(isMidi: true);
    }

    private static void RunTwiceAndCompare(bool isMidi)
    {
        string testsRoot = FlowScriptData.FindTestsRoot();
        string repoRoot = Path.GetFullPath(Path.Combine(testsRoot, ".."));
        string scriptPath = Path.Combine(repoRoot, "examples", "showcase.flow");
        Assert.True(File.Exists(scriptPath), $"showcase.flow missing at {scriptPath}");

        string ext = isMidi ? "mid" : "wav";
        string baseName = "flow_showcase";
        string outDir = Path.Combine(repoRoot, "tests", "output");
        string path1 = Path.Combine(outDir, $"phase25_showcase_run1.{ext}");
        string path2 = Path.Combine(outDir, $"phase25_showcase_run2.{ext}");

        if (File.Exists(path1)) File.Delete(path1);
        if (File.Exists(path2)) File.Delete(path2);
        Directory.CreateDirectory(outDir);

        string source = File.ReadAllText(scriptPath);
        string defaultRel = $"examples/output/{baseName}.{ext}";
        string sourceRun1 = source.Replace(defaultRel, $"tests/output/phase25_showcase_run1.{ext}");
        string sourceRun2 = source.Replace(defaultRel, $"tests/output/phase25_showcase_run2.{ext}");

        Assert.NotEqual(source, sourceRun1);

        string originalCwd = Environment.CurrentDirectory;
        try
        {
            Environment.CurrentDirectory = repoRoot;

            using (var runner1 = new FlowEngineRunner())
            {
                var (success1, _, stderr1, errorCount1) = runner1.RunSource(sourceRun1);
                Assert.True(success1, $"run1 failed: stderr={stderr1}");
                Assert.Equal(0, errorCount1);
            }

            using (var runner2 = new FlowEngineRunner())
            {
                var (success2, _, stderr2, errorCount2) = runner2.RunSource(sourceRun2);
                Assert.True(success2, $"run2 failed: stderr={stderr2}");
                Assert.Equal(0, errorCount2);
            }

            Assert.True(File.Exists(path1), $"output not written: {path1}");
            Assert.True(File.Exists(path2), $"output not written: {path2}");

            byte[] bytes1 = File.ReadAllBytes(path1);
            byte[] bytes2 = File.ReadAllBytes(path2);

            Assert.True(bytes1.Length > 0, $"empty output: {path1}");
            Assert.True(bytes1.SequenceEqual(bytes2),
                $"{ext} bytes differ: run1 len={bytes1.Length}, run2 len={bytes2.Length}");
        }
        finally
        {
            Environment.CurrentDirectory = originalCwd;
        }
    }
}
```
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests 2>&amp;1 | grep -E "(error|Build succeeded)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs` exits 0
    - `grep -c "namespace FlowLang.Tests.Integration.Phase25" flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs` returns exactly `1`
    - `grep -c "phase25_showcase_run1" flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs` returns at least `2` (run1 ext × wav,mid)
    - `grep -c "phase18_showcase" flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs` returns exactly `0` (no Phase18 run-file basenames leaked)
    - `grep -c "Skip = \"Plan 25-03" flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs` returns exactly `2`
    - `grep -c "class ByteIdenticalShowcaseGaussianTests" flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs` returns exactly `1`
    - `dotnet build flow-lang.Tests 2>&1 | grep -c "Build succeeded"` returns at least `1`
  </acceptance_criteria>
  <done>Integration test file exists with both Skip-marked Facts, namespace + run-file basenames substituted; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_humanize_gaussian.flow smoke + register in FlowScriptData.cs</name>
  <files>tests/test_humanize_gaussian.flow, flow-lang.Tests/FlowScriptData.cs</files>
  <read_first>
    - tests/test_euclidean_humanize.flow (verbatim structural mirror — copy whole shape, replace euclidean call with humanizeGaussian over a base Sequence)
    - flow-lang.Tests/FlowScriptData.cs (lines 220-240 — verify dictionary entry insertion site, sentinel format)
    - .planning/phases/25-gaussian-humanize-last-prng-phase/25-PATTERNS.md §test_humanize_gaussian.flow (full content of new smoke file)
  </read_first>
  <behavior>
    - test_humanize_gaussian.flow is a Wave 0 PLACEHOLDER that calls humanizeGaussian — but humanizeGaussian does not exist yet in Wave 0. To keep the .flow smoke from failing in Wave 0, the .flow file body MUST be wrapped in a comment-out / no-op shape that prints both sentinels unconditionally so FlowScriptData two-pass infrastructure does not regress. Plan 25-02 replaces the body with the real humanizeGaussian smoke (per FlowScriptData.cs:226 "Wave 0 placeholder" comment style at Phase 15 entry).
    - FlowScriptData.cs entry comment cites "Wave 0 placeholder — Plan 25-02 replaces the body" exactly mirroring Phase 15 DX-09 entry comment.
  </behavior>
  <action>
**Step 1:** Write `tests/test_humanize_gaussian.flow` with this EXACT content (Wave 0 placeholder body — prints both sentinels but does NOT call humanizeGaussian yet because the function does not exist; Plan 25-02 replaces the body with the real call):

```flow
use "@std"
use "@audio"

// Phase 25 DEFER-06 — humanizeGaussian(Sequence, Double, Int) Box-Muller smoke.
// WAVE 0 PLACEHOLDER — Plan 25-02 replaces this body with humanizeGaussian +
// writeMidi + byte-identical-two-runs check while preserving both sentinels.
// Authoritative byte-level gate: flow-lang.Tests/Integration/Phase25/ByteIdenticalShowcaseGaussianTests.cs.
//
// Per FlowScriptData two-pass infrastructure, this file MUST print both
// sentinels for the FlowScriptData entry to register as PASSED. The Wave 0
// body is a no-op print pair so the suite stays GREEN through scaffold-only
// commits; Plan 25-02 replaces with the real humanizeGaussian call.

(print "humanizeGaussian seed=42: PASSED")
(print "two runs byte-identical: PASSED")
```

**Step 2:** Add a new dictionary entry to `flow-lang.Tests/FlowScriptData.cs` immediately after the Phase 15 DX-09 entry at lines 225-232. Use the Edit tool to insert these lines after the existing `["test_euclidean_humanize.flow"]` block (which ends with `},` at line ~232):

```csharp
        // Phase 25 DEFER-06: humanizeGaussian(Sequence, Double, Int) seeded Box-Muller.
        // Wave 0 placeholder — Plan 25-02 replaces the .flow body with humanizeGaussian +
        // writeMidi + byte-identical-two-runs check while preserving both sentinels.
        ["test_humanize_gaussian.flow"] = new[]
        {
            "humanizeGaussian seed=42: PASSED",
            "two runs byte-identical: PASSED",
        },

```
(Note: the trailing blank line is intentional to maintain spacing parity with neighboring entries — verify by reading the file after edit and ensuring the next existing entry follows on its own block boundary.)
  </action>
  <verify>
    <automated>dotnet run --project flow-interpreter tests/test_humanize_gaussian.flow 2>&amp;1 | grep -E "(humanizeGaussian seed=42: PASSED|two runs byte-identical: PASSED)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_humanize_gaussian.flow` exits 0
    - `grep -c "humanizeGaussian seed=42: PASSED" tests/test_humanize_gaussian.flow` returns exactly `1`
    - `grep -c "two runs byte-identical: PASSED" tests/test_humanize_gaussian.flow` returns exactly `1`
    - `grep -c "test_humanize_gaussian.flow" flow-lang.Tests/FlowScriptData.cs` returns at least `1`
    - `grep -c "Phase 25 DEFER-06: humanizeGaussian" flow-lang.Tests/FlowScriptData.cs` returns exactly `1`
    - `dotnet run --project flow-interpreter tests/test_humanize_gaussian.flow 2>&amp;1 | grep -c "humanizeGaussian seed=42: PASSED"` returns exactly `1`
    - `dotnet run --project flow-interpreter tests/test_humanize_gaussian.flow 2>&amp;1 | grep -c "two runs byte-identical: PASSED"` returns exactly `1`
  </acceptance_criteria>
  <done>Smoke .flow file exists, prints both sentinels, FlowScriptData.cs entry registered with matching sentinel array; flow-interpreter exits 0 on the script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → test runner | xUnit Skip-marked Facts must compile against existing code; no production-side execution |
| .flow source → flow-interpreter | Wave 0 placeholder smoke must not invoke missing builtin (humanizeGaussian) — placeholder body is print-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-00-01 | Tampering | xUnit Skip-marked Facts | mitigate | Skip = "Plan 25-NN: implementation pending" string is grep-verified by acceptance criteria; downstream plans MUST remove the Skip attribute before flipping the Fact to live |
| T-25-00-02 | Denial of Service | tests/test_humanize_gaussian.flow Wave 0 placeholder | mitigate | Placeholder body uses pure (print) calls only — no PRNG, no I/O, no missing-symbol resolution; flow-interpreter exits 0 cleanly |
| T-25-00-03 | Information Disclosure | none | accept | Test scaffold introduces no secrets, no PII, no credentials; FlowScriptData.cs entry is pure compile-time data |
</threat_model>

<verification>
After all 3 tasks complete:
1. `dotnet build flow-lang.Tests` → exits 0, no errors
2. `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase25"` → reports 9 Skipped (7 unit + 2 integration), 0 Failed
3. `dotnet run --project flow-interpreter tests/test_humanize_gaussian.flow` → exits 0, prints both sentinels in stdout
4. `dotnet test flow-lang.Tests --filter "FlowScriptIntegrationTests"` → still GREEN; new entry recognized (sentinels match)
5. Phase 18 byte-identical regression: `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase18"` stays GREEN (no source code touched)
</verification>

<success_criteria>
- Phase25 unit + integration test directories exist
- HumanizeGaussianFacts.cs has all 7 Skip-marked Facts + Tol/BaseVelocity constants + [Collection]
- ByteIdenticalShowcaseGaussianTests.cs is verbatim Phase18 mirror with namespace + run-file substitutions
- tests/test_humanize_gaussian.flow exists, prints both sentinels, registered in FlowScriptData.cs
- `dotnet build flow-lang.Tests` GREEN
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase25"` reports Skipped (not Failed)
- Phase 18 byte-identical regression still GREEN (D-19 invariant — no production code touched in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/25-gaussian-humanize-last-prng-phase/25-00-SUMMARY.md` per the standard summary template (mirror 24-00-SUMMARY.md style).
</output>
