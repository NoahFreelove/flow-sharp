---
phase: 41-reach-v1-5-closer
plan: 07
type: execute
wave: 5
depends_on: [41-01, 41-03, 41-04, 41-05, 41-06]
files_modified:
  - examples/edm/pulse.flow
  - README.md
  - flow-lang.Tests/Integration/Phase41/Phase41ShowcaseRmsTests.cs
  - flow-lang.Tests/baselines/Phase41/showcase.wav
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SHOWCASE-01]
must_haves:
  truths:
    - "A ~60s curated third-genre piece at examples/<genre>/<piece>.flow exercises ALL five required primitives: Phase 35 pattern matching, a Phase 36 generative primitive (e.g. euclidean/markov), Phase 37 granular OR time-stretch DSP, a Phase 38 live block, and Phase 40 real-time MIDI midiOut (D-11/D-13, SHOWCASE-01)"
    - "The OFFLINE render path (writeWav + writeMidi) is fully seeded/deterministic and holds two-run cmp-clean; the live block + real-time midiOut live in a SEPARATE playback/demo section, NOT in the pinned render (Pitfall 5)"
    - "The showcase WAV holds RMS-windowed regression ±0.5 dB / 100 ms against a committed baseline under flow-lang.Tests/baselines/Phase41/ (D-13, SPEC-8)"
    - "README.md gains a ## Showcase v1.5 section embedding the showcase audio (D-13)"
    - "D-19 bookkeeping: REQUIREMENTS.md WASM-01/02/03 traceability flips Pending/Phase-41 -> Shipped (carved to 47-49); ROADMAP progress-table rows for Phase 39 (Not started -> shipped) + Phase 48 (In Progress 5/7 -> shipped) are corrected"
  artifacts:
    - path: "examples/edm/pulse.flow"
      provides: "third-genre showcase piece (genre composer-confirmable)"
    - path: "flow-lang.Tests/baselines/Phase41/showcase.wav"
      provides: "RMS regression baseline"
    - path: "README.md"
      provides: "## Showcase v1.5 section"
  key_links:
    - from: "examples/edm/pulse.flow"
      to: "flow-lang.Tests/baselines/Phase41/showcase.wav"
      via: "writeWav -> AssertRmsWithinTolerance baseline"
      pattern: "writeWav"
    - from: ".planning/REQUIREMENTS.md"
      to: "WASM-01/02/03 traceability rows"
      via: "Pending -> Shipped (carved to 47-49)"
      pattern: "carved to 47-49"
---

<objective>
Author the v1.5 third-genre showcase piece (~60s, genre = EDM is the D-11 recommended default but composer-confirmable), exercising all five required feature primitives, with a CLEAN split between the pinned offline render (seeded, deterministic writeWav/writeMidi) and the live/real-time-MIDI demo section (Pitfall 5). Pin the WAV with two-run cmp-clean + RMS-windowed regression against a committed baseline, add a README `## Showcase` section, and perform the D-19 bookkeeping reconciliation. This is the v1.5 closer artifact — it consumes Phases 35-40's surface and runs last.

This plan is `autonomous: false` ONLY for the genre-choice checkpoint (D-11 is the composer's creative call); everything after the choice is autonomous.

Purpose: SHOWCASE-01 — validate Flow's genre-agnostic claim alongside v1.4's symphony + ragtime; close v1.5's tracking-doc debt (D-19).
Output: examples/<genre>/<piece>.flow; baseline WAV; Phase41ShowcaseRmsTests GREEN; README ## Showcase; reconciled REQUIREMENTS.md + ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-reach-v1-5-closer/41-CONTEXT.md
@.planning/phases/41-reach-v1-5-closer/41-RESEARCH.md
@CLAUDE.md
@examples/generative/markov_jazz.flow

<interfaces>
<!-- Build against these — no codebase exploration needed. -->

Feature checklist syntax (SHOWCASE-01, all SHIPPED — exact forms):
  Pattern matching (Phase 35):      (match expr | (pattern) => ... | _ => ...)
  Generative primitive (Phase 36):  (euclidean pulses steps)  OR  (markov corpus order length SEED)  — SEED for determinism
  Granular / time-stretch (P37):    (granular buf grain=50ms density=20Hz)  OR  (stretch buf factor)  OR  (pitchShift buf cents)
  Live block (Phase 38):            live 1bar { ... }   // opts OUT of determinism (D-v1.5-07) — DEMO section only
  Real-time MIDI (Phase 40):        use "@midi" ; midiOut(song, port)   // play-path only — DEMO section only

Determinism harness: scripts/test_two_run_determinism.sh <script.flow>
  The script under test MUST emit a (writeWav "out.wav" buf) call; the harness renders twice + compares SHA-256.
  Seeded-determinism precedent: examples/generative/markov_jazz.flow uses (markov corpus 2 16 42) explicit seed,
  routes unseeded calls via PrngRegistry, reseeds at the writeWav boundary → byte-identical two renders.

RMS regression: flow-lang.Tests/Helpers/RmsRegressionTests.AssertRmsWithinTolerance(...)  (signature at :39)
  / AssertWavMatchesBaseline(tempPath, baselineWavPath, windowMs, toleranceDb, overrideReason)  (:72)
  SPEC-8 locked: ±0.5 dB / 100 ms window. Baselines committed under flow-lang.Tests/baselines/PhaseNN/
  (seeded dither RNG → byte-stable baseline).

D-19 reconciliation targets (exact lines):
  .planning/REQUIREMENTS.md :147-149  — WASM-01/02/03 [ ] checkbox lines (add "carved to 47-49" note)
  .planning/REQUIREMENTS.md :392-394  — traceability rows: | WASM-01 | Phase 41 | Pending |  ->  Shipped (carved to 47-49)
  .planning/ROADMAP.md :352           — | 39. Notation Citizenship | v1.5 | 0/0 | Not started | - |  -> shipped row
  .planning/ROADMAP.md :361           — | 48. WASM Runtime + WebAudioBackend | v1.5 | 5/7 | In Progress |  -> shipped row
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Confirm the third-genre showcase genre (EDM default, D-11)</name>
  <action>Pause for the composer's creative decision on the showcase genre before authoring the piece. D-11 recommends EDM; death-metal and jazz are equally valid alternatives. The scaffolding is genre-independent — only the file path (examples/<genre>/<piece>.flow) and musical content change. Present the options below and wait for the selection.</action>
  <decision>Which genre for the v1.5 third-genre showcase piece?</decision>
  <context>D-11 recommends EDM (maximally contrasting with v1.4's classical symphony + ragtime, strongest fit for the feature checklist: four-on-the-floor pattern matching + Euclidean generative + granular/stretch DSP + sidechain + live block + real-time MIDI). This is a genuine composer creative choice — the scaffolding is genre-independent (one .flow file). The composer should confirm before execution (CONTEXT D-11 / Open Q2).</context>
  <options>
    <option id="edm">
      <name>EDM (recommended default, D-11)</name>
      <pros>Maximal contrast with symphony+ragtime; four-on-the-floor maps cleanly to pattern matching + Euclidean; granular/stretch is the signature EDM texture; sidechain already in Flow.</pros>
      <cons>Most "expected" electronic choice.</cons>
    </option>
    <option id="death-metal">
      <name>Death metal (D-12 — boldest genre-agnostic proof)</name>
      <pros>Strongest validation of the genre-agnostic claim; tremolo-picked patterns + blast-beat drums + aggressive synthesis stress the engine differently.</pros>
      <cons>Drum/synthesis-heavy; granular fit is less idiomatic.</cons>
    </option>
    <option id="jazz">
      <name>Jazz (D-12 — leans on @improv jam + swing + Markov)</name>
      <pros>Showcases @improv jam + swing.</pros>
      <cons>Overlaps the existing examples/generative/markov_jazz.flow — showcases the LEAST new ground (D-12).</cons>
    </option>
  </options>
  <resume-signal>Select: edm, death-metal, or jazz (then the file lands at examples/<genre>/<piece>.flow).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author the showcase piece — pinned-render section + live/MIDI demo section</name>
  <files>examples/edm/pulse.flow</files>
  <read_first>
    - examples/generative/markov_jazz.flow (the established showcase style + the seeded-determinism header pattern + PrngRegistry/writeWav-boundary note)
    - examples/showcase.flow (the v1.4 showcase structure — section/song layout, writeWav target)
    - .planning/phases/41-reach-v1-5-closer/41-RESEARCH.md (Pitfall 5 — the render/demo split; Open Q3/A3 — real-time MIDI is play-path only)
    - CLAUDE.md (the exact syntax for each Phase 35-40 primitive in the feature checklist)
  </read_first>
  <action>
    Create `examples/<genre>/<piece>.flow` (path from the checkpoint choice; default `examples/edm/pulse.flow`). Structure it in TWO clearly-commented sections per Pitfall 5:

    PINNED OFFLINE RENDER (deterministic): build the ~60s piece using Phase 35 pattern matching (e.g. `(match ...)` over chord/numeral patterns to drive variation), a Phase 36 generative primitive with an EXPLICIT SEED (e.g. `(euclidean 5 8)` for the rhythm or `(markov corpus 2 N 42)` — seed all stochastic calls so PrngRegistry reseeds at the writeWav boundary), and Phase 37 granular OR time-stretch DSP (`(granular buf grain=50ms density=20Hz)` or `(stretch buf factor)`) on the rendered texture. End with `(writeWav "examples/<genre>/<piece>.wav" mix)` AND `(writeMidi "examples/<genre>/<piece>.mid" song)`. This section MUST be byte-identical across two renders.

    LIVE / REAL-TIME DEMO (NOT pinned, opts out of determinism per D-v1.5-07): a `live 1bar { ... }` block + a `use "@midi"` / `midiOut(song, port)` real-time MIDI demonstration, clearly commented as the interactive/playback demo that satisfies the SHOWCASE-01 feature checklist's "live block" + "real-time MIDI" items WITHOUT entering the pinned render (Pitfall 5 / A3). Guard it so a headless render of the file does not block on the live loop (e.g. gate behind a comment instruction or a separate entry the determinism harness does not execute — the harness only needs the writeWav path).

    Author a header comment block (mirroring markov_jazz.flow) documenting: the genre, the five feature-checklist items and where each appears, the render/demo split, and the seeded-determinism contract. Verify the file runs and emits the WAV.
  </action>
  <verify>
    <automated>dotnet run --project flow-cli -- render examples/edm/pulse.flow 2>&1 | tail -5; ls examples/edm/pulse.wav examples/edm/pulse.mid 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `examples/<genre>/<piece>.flow` exists and renders without error, emitting both a `.wav` and a `.mid`
    - The file contains all five primitives: a `(match ...)`, a seeded generative call, a granular/stretch DSP call, a `live` block, and a `midiOut` call (grep each)
    - The `live` block + `midiOut` are in a clearly-commented demo section, NOT inside the writeWav/writeMidi render path (Pitfall 5)
    - Every stochastic call in the render path carries an explicit seed
  </acceptance_criteria>
  <done>Showcase piece renders WAV+MIDI; all five primitives present; render/demo split clean; stochastic render path fully seeded.</done>
</task>

<task type="auto">
  <name>Task 2: Pin two-run cmp-clean + RMS regression baseline + README ## Showcase</name>
  <files>flow-lang.Tests/baselines/Phase41/showcase.wav, flow-lang.Tests/Integration/Phase41/Phase41ShowcaseRmsTests.cs, README.md</files>
  <read_first>
    - flow-lang.Tests/Helpers/RmsRegressionTests.cs (AssertRmsWithinTolerance :39 / AssertWavMatchesBaseline :72 — the SPEC-8 ±0.5 dB/100 ms gate)
    - flow-lang.Tests/Integration/Phase41/Phase41ShowcaseRmsTests.cs (the 41-01 stub: Showcase_RmsWithinTolerance)
    - scripts/test_two_run_determinism.sh (usage + the writeWav-path requirement)
    - README.md (existing section structure :22-71 — where ## Showcase slots)
  </read_first>
  <action>
    Run `bash scripts/test_two_run_determinism.sh examples/<genre>/<piece>.flow` and confirm the two renders are byte-identical (the seeded render path must hold two-run cmp-clean). Also render the `.mid` twice and confirm identical SHA-256 (offline MIDI is DryWetMidi-deterministic). If either diverges, fix the render path (an unseeded call leaked — Pitfall 5) before proceeding.

    Generate the baseline: render once and commit the resulting WAV to `flow-lang.Tests/baselines/Phase41/showcase.wav` (the seeded dither RNG makes the baseline byte-stable — same precedent as Phase 28/45 baselines). Implement the `Phase41ShowcaseRmsTests.cs` real assertion (remove the Skip from 41-01): render the showcase to a temp WAV and call `RmsRegressionTests.AssertRmsWithinTolerance` / `AssertWavMatchesBaseline` against `baselines/Phase41/showcase.wav` with the SPEC-8 ±0.5 dB / 100 ms window.

    Add a `## Showcase` section to `README.md` (after the Features section, mirroring how v1.4's symphony/ragtime would be presented): name the genre, describe the ~60s piece + the five primitives it exercises, link the `.flow` source + the rendered audio (the WAV path), and note it ships in the v1.5.0 GitHub Release alongside the cross-platform binaries (the Release itself is the D-04 human gate).
  </action>
  <verify>
    <automated>bash scripts/test_two_run_determinism.sh examples/edm/pulse.flow 2>&1 | tail -3; test -f flow-lang.Tests/baselines/Phase41/showcase.wav && dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase41ShowcaseRmsTests" 2>&1 | grep -E "Passed!|Failed!" && grep -q "## Showcase" README.md && echo SHOWCASE_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test_two_run_determinism.sh examples/<genre>/<piece>.flow` exits 0 (byte-identical WAV across two renders)
    - The showcase `.mid` is byte-identical across two renders (SHA-256 match)
    - `flow-lang.Tests/baselines/Phase41/showcase.wav` is committed
    - `Phase41ShowcaseRmsTests` GREEN (RMS within ±0.5 dB / 100 ms vs the baseline)
    - `README.md` contains a `## Showcase` section describing the piece + the five primitives + the v1.5.0 Release note
  </acceptance_criteria>
  <done>Two-run cmp-clean holds for WAV+MIDI; RMS baseline committed + test GREEN; README ## Showcase added.</done>
</task>

<task type="auto">
  <name>Task 3: D-19 bookkeeping reconciliation (REQUIREMENTS.md WASM + ROADMAP stale rows)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (WASM-01/02/03 checkboxes :147-149; traceability rows :392-394)
    - .planning/ROADMAP.md (progress table — Phase 39 row :352, Phase 48 row :361)
    - .planning/phases/41-reach-v1-5-closer/41-CONTEXT.md (D-19 — STATE frontmatter + phase-detail sections are authoritative)
  </read_first>
  <action>
    Per D-19 (a doc edit, NOT implementation — the WASM playground shipped via Phases 47-49 under a different architecture):

    In `.planning/REQUIREMENTS.md`: flip the three WASM traceability rows at :392-394 from `| WASM-NN | Phase 41 | Pending |` to reflect Shipped/carved-to-47-49 (e.g. `| WASM-01 | Phase 47-49 | Shipped (carved 2026-05-25) |`). Annotate the three WASM-01/02/03 checkbox bullets at :147-149 with a short "(carved to Phase 47-49 — shipped via Mono-WASM Web target + SvelteKit playground, NOT the original Blazor flow-wasm plan)" note. Do NOT mark them `[x]` as Phase 41 deliverables — they were carved OUT; the note records where they actually landed.

    In `.planning/ROADMAP.md` progress table: correct the Phase 39 row (:352) from `0/0 | Not started` to its actual shipped status (Phase 39 Notation Citizenship is shipped per STATE), and the Phase 48 row (:361) from `5/7 | In Progress` to shipped (Phase 48 SHIPPED 2026-06-05 per STATE + CLAUDE.md). Use STATE frontmatter + the phase-detail sections as authoritative for the exact plan counts + dates.

    This is a tracked-doc edit with no runtime footprint (RESEARCH §Runtime State Inventory). Do NOT touch any production code.
  </action>
  <verify>
    <automated>grep -q "carved" .planning/REQUIREMENTS.md && ! grep -E "WASM-01 \| Phase 41 \| Pending" .planning/REQUIREMENTS.md && ! grep -E "39\. Notation Citizenship.*Not started" .planning/ROADMAP.md && ! grep -E "48\. WASM Runtime.*In Progress" .planning/ROADMAP.md && echo RECONCILED</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md WASM-01/02/03 traceability rows no longer read `Phase 41 | Pending` — they read Shipped/carved-to-47-49
    - The WASM-01/02/03 checkbox bullets carry a "carved to Phase 47-49" annotation
    - ROADMAP.md Phase 39 row no longer reads "Not started"; Phase 48 row no longer reads "In Progress 5/7" — both reflect shipped
    - No production code changed (only .planning/ docs)
  </acceptance_criteria>
  <done>WASM-01/02/03 reconciled to Shipped (carved 47-49); ROADMAP Phase 39 + 48 progress rows corrected; no code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The showcase is a `.flow` source artifact + a baseline WAV + tracking-doc edits — no new runtime attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-DETERM | (correctness/integrity) | showcase offline render | mitigate | Every stochastic call in the writeWav/writeMidi path is explicitly seeded + routed through PrngRegistry; the live block + real-time midiOut are isolated to the demo section (Pitfall 5). Pinned by `test_two_run_determinism.sh` + `Phase41ShowcaseRmsTests`. |
| T-41-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs (a `.flow` showcase + a baseline WAV + doc edits — zero new dependencies). |
</threat_model>

<verification>
- `dotnet run --project flow-cli -- render examples/<genre>/<piece>.flow` emits WAV + MIDI
- `bash scripts/test_two_run_determinism.sh examples/<genre>/<piece>.flow` exits 0
- `.mid` byte-identical across two renders
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase41ShowcaseRmsTests"` → 0 FAILED
- README ## Showcase present; REQUIREMENTS.md + ROADMAP.md reconciled (D-19)
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase41"` → 0 FAILED (full Phase 41 subset green)
</verification>

<success_criteria>
- ~60s third-genre showcase exercising all five primitives (D-11/D-13)
- Clean render/demo split (live + real-time MIDI out of the pinned render — Pitfall 5)
- Two-run cmp-clean (WAV+MIDI) + RMS regression ±0.5 dB/100 ms (success criterion 6, SPEC-8)
- README ## Showcase section (D-13)
- D-19 reconciliation: WASM-01/02/03 → Shipped (carved 47-49); ROADMAP Phase 39 + 48 rows corrected
</success_criteria>

<output>
Create `.planning/phases/41-reach-v1-5-closer/41-07-SUMMARY.md` when done
</output>
