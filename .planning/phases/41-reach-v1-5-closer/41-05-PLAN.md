---
phase: 41-reach-v1-5-closer
plan: 05
type: execute
wave: 4
depends_on: [41-03, 41-04]
files_modified:
  - scripts/publish.sh
autonomous: false
requirements: [BIN-01]
must_haves:
  truths:
    - "scripts/publish.sh produces self-contained single-file binaries for all 5 RIDs — linux-x64, linux-arm64, osx-x64, osx-arm64, win-x64 — via dotnet publish -p:PublishSingleFile=true --self-contained (D-14)"
    - "No trimming: -p:PublishTrimmed=false on every RID (the reflection-heavy InternalFunctionRegistry would silently break — D-15)"
    - "Each archive is named flow-<rid>-v1.5.0.tar.gz (linux/osx) or flow-win-x64-v1.5.0.zip (windows), with a .sha256 sidecar per artifact (D-16, tampered-binary mitigation)"
    - "linux-x64 + linux-arm64 get a runtime `flow version` smoke; osx-x64/osx-arm64/win-x64 are cross-compiled here but their execution smoke is the 41-HUMAN-UAT.md gate (D-02, Pitfall 4 — never faked)"
  artifacts:
    - path: "scripts/publish.sh"
      provides: "5-RID self-contained publish + tar/zip + .sha256"
      contains: "linux-arm64"
  key_links:
    - from: "scripts/publish.sh"
      to: "publish/flow-<rid>-v1.5.0.{tar.gz,zip} + .sha256"
      via: "per-RID dotnet publish + package + sha256sum"
      pattern: "sha256"
---

<objective>
Generalize `scripts/publish.sh` from its single-RID (linux-x64) form to a 5-RID self-contained single-file publish loop (linux-x64, linux-arm64, osx-x64, osx-arm64, win-x64), packaging each as `flow-<rid>-v1.5.0.tar.gz` (linux/osx) or `flow-win-x64-v1.5.0.zip` (windows) with a `.sha256` sidecar. NO trimming (D-15). All 5 cross-compile from this Linux box because Flow's binaries are managed-only (audio backends P/Invoke SYSTEM libs that are never bundled). Only linux-x64/linux-arm64 get a runtime `flow version` smoke here; the osx/win execution smoke is the 41-HUMAN-UAT.md gate.

This plan is `autonomous: false`: all 5 binaries BUILD + checksum autonomously, but executing the osx/win artifacts is a human gate (Pitfall 4 — cross-compiled binaries can only be exercised on their target OS).

Purpose: BIN-01 — Mac + Windows users can finally `flow run script.flow` locally; staged for the v1.5.0 GitHub Release (which itself is the D-04 human gate).
Output: an extended `publish.sh` producing 5 RID archives + `.sha256` each, linux RIDs smoke-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-reach-v1-5-closer/41-CONTEXT.md
@.planning/phases/41-reach-v1-5-closer/41-RESEARCH.md
@CLAUDE.md
@scripts/publish.sh

<interfaces>
<!-- Build against these — no codebase exploration needed. -->

Current scripts/publish.sh — single RID (linux-x64), self-contained single-file:
  dotnet publish flow-cli/flow-cli.csproj -c Release -r linux-x64 --self-contained true \
    -p:PublishSingleFile=true -p:PublishTrimmed=false -p:DebugType=embedded \
    -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o "$OUT_DIR"
  Verifies $OUT_DIR/flow is executable; checks stdlib .flow files copied (std/collections/audio/bars/notation/composition);
  enforces a 120 MB SIZE_BUDGET_MB via du -sb; smoke `"$OUT_DIR/flow" version`. set -euo pipefail.

5-RID generalization (RESEARCH publish.sh section, VERIFIED cross-compile from Linux):
  for RID in linux-x64 linux-arm64 osx-x64 osx-arm64 win-x64; do
    OUT="$PROJECT_ROOT/publish/flow-$RID"; rm -rf "$OUT"; mkdir -p "$OUT"
    dotnet publish flow-cli/flow-cli.csproj -c Release -r "$RID" --self-contained true \
      -p:PublishSingleFile=true -p:PublishTrimmed=false -p:DebugType=embedded \
      -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o "$OUT"
    case "$RID" in
      win-x64) (cd "$OUT" && zip -r "../flow-win-x64-v1.5.0.zip" .) ;;
      *)       tar -czf "$PROJECT_ROOT/publish/flow-$RID-v1.5.0.tar.gz" -C "$OUT" . ;;
    esac
    # sha256sum the archive -> .sha256
  done
  # linux-x64 + linux-arm64: runtime `"$OUT/flow" version` smoke (arm64 = qemu OR skip-with-reason)
  # osx-*/win-x64: NO exec here — HUMAN-UAT exec smoke (the Linux box cannot run them)

Win binary name is `flow.exe`; linux/osx is `flow`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize publish.sh to a 5-RID self-contained loop with tar/zip + .sha256</name>
  <read_first>
    - scripts/publish.sh (the full current single-RID script — flag set, stdlib-copy check, size-budget check, version smoke)
    - .planning/phases/41-reach-v1-5-closer/41-RESEARCH.md (publish.sh 5-RID generalization; Anti-Pattern "Enabling PublishTrimmed" — keep -p:PublishTrimmed=false)
  </read_first>
  <action>
    Rewrite `scripts/publish.sh` to loop over the 5 RIDs `linux-x64 linux-arm64 osx-x64 osx-arm64 win-x64`. For each RID: clean `publish/flow-$RID`, run `dotnet publish flow-cli/flow-cli.csproj -c Release -r "$RID" --self-contained true -p:PublishSingleFile=true -p:PublishTrimmed=false -p:DebugType=embedded -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o "$OUT"` (keep the exact flag set; NEVER add PublishTrimmed=true — D-15). Verify the produced binary exists (`flow` for linux/osx, `flow.exe` for win-x64). Keep the stdlib `.flow`-copied check per output dir (std/collections/audio/bars/notation/composition + the Phase 36/38/39/40 modules that the engine ships — confirm the existing list; do not drop any). Keep a per-RID size-budget guard (the existing 120 MB SPEC-2 cap is a single-RID number — apply it per-RID, not summed).

    Package: `win-x64` to `zip -r publish/flow-win-x64-v1.5.0.zip .` from the output dir; all others to `tar -czf publish/flow-$RID-v1.5.0.tar.gz -C "$OUT" .`. After packaging each archive, emit a `.sha256` sidecar via `sha256sum <archive> > <archive>.sha256` (tampered-binary mitigation, D-16). Keep `set -euo pipefail`.

    Smoke ONLY the linux RIDs: run `"$OUT/flow" version` for linux-x64; for linux-arm64 attempt qemu-user emulation if available, otherwise skip-with-reason (echo a clear "linux-arm64 exec smoke skipped — no qemu; cross-compile artifact built + checksummed" message — do NOT fail the script for missing qemu). Do NOT attempt to execute the osx/win binaries (Pitfall 4) — echo that their exec smoke is the 41-HUMAN-UAT.md gate. Update the script header comment to describe the 5-RID behavior + the v1.5.0 artifact naming.

    Allow a generous timeout — 5 self-contained publishes is several minutes.
  </action>
  <verify>
    <automated>bash scripts/publish.sh 2>&1 | tail -20; ls publish/flow-linux-x64-v1.5.0.tar.gz publish/flow-linux-arm64-v1.5.0.tar.gz publish/flow-osx-x64-v1.5.0.tar.gz publish/flow-osx-arm64-v1.5.0.tar.gz publish/flow-win-x64-v1.5.0.zip 2>/dev/null && ls publish/*.sha256 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/publish.sh` loops over exactly the 5 RIDs and keeps `-p:PublishTrimmed=false` on every one (grep: no `PublishTrimmed=true`)
    - 5 archives exist: `flow-linux-x64-v1.5.0.tar.gz`, `flow-linux-arm64-v1.5.0.tar.gz`, `flow-osx-x64-v1.5.0.tar.gz`, `flow-osx-arm64-v1.5.0.tar.gz`, `flow-win-x64-v1.5.0.zip`
    - 5 `.sha256` sidecars exist (one per archive)
    - linux-x64 `flow version` smoke passes; linux-arm64 smoke runs or skips-with-reason (does not fail the script)
    - osx/win binaries are NOT executed (the script echoes they are the 41-HUMAN-UAT.md exec gate)
  </acceptance_criteria>
  <done>publish.sh builds + packages + checksums all 5 RID artifacts; linux smoke green; osx/win exec deferred to the human gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build artifacts → downstream consumers | The published binaries will be attached to a GitHub Release and downloaded by users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-TAMPER | Tampering | published binary archives | mitigate | A `.sha256` sidecar is emitted per archive (D-16); the human verifies each checksum before attaching to the v1.5.0 GitHub Release (D-04 gate). |
| T-41-05-TRIM | (correctness/integrity) | PublishTrimmed | mitigate | `-p:PublishTrimmed=false` on every RID (D-15) — trimming would silently break the reflection-heavy `InternalFunctionRegistry`. Grep gate forbids `PublishTrimmed=true`. |
| T-41-05-CROSSEXEC | (correctness) | osx/win binaries on Linux | accept (human gate) | Cross-compiled artifacts can only be exercised on their target OS (Pitfall 4); the script never executes them — osx/win exec smoke is the 41-HUMAN-UAT.md gate, never faked. |
| T-41-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (shell + dotnet SDK publish only). |
</threat_model>

<verification>
- `bash scripts/publish.sh` exits 0
- 5 archives + 5 `.sha256` sidecars under `publish/`
- linux-x64 `flow version` smoke passes; linux-arm64 runs-or-skips-with-reason
- No `PublishTrimmed=true` anywhere (grep)
- osx/win exec smoke recorded as the 41-HUMAN-UAT.md gate (D-05) — NOT run here
</verification>

<success_criteria>
- 5-RID self-contained single-file publish, no trimming (D-14/D-15)
- flow-<rid>-v1.5.0.tar.gz / flow-win-x64-v1.5.0.zip + .sha256 each (D-16)
- linux RIDs smoke-tested; osx/win exec honestly deferred to 41-HUMAN-UAT.md
</success_criteria>

<output>
Create `.planning/phases/41-reach-v1-5-closer/41-05-SUMMARY.md` when done
</output>
