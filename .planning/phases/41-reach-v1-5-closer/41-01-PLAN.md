---
phase: 41-reach-v1-5-closer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang.Tests/Integration/Phase41/DocCommentLexTests.cs
  - flow-lang.Tests/Integration/Phase41/DocCommentBindTests.cs
  - flow-lang.Tests/Integration/Phase41/FlowDocGenTests.cs
  - flow-lang.Tests/Integration/Phase41/DocCacheTests.cs
  - flow-lang.Tests/Integration/Phase41/DocExampleExecTests.cs
  - flow-lang.Tests/Integration/Phase41/WasapiBackendAvailabilityTests.cs
  - flow-lang.Tests/Integration/Phase41/CoreAudioBackendAvailabilityTests.cs
  - flow-lang.Tests/Integration/Phase41/Phase41ShowcaseRmsTests.cs
  - flow-lang.Tests/baselines/Phase41/.gitkeep
  - .planning/phases/41-reach-v1-5-closer/41-HUMAN-UAT.md
autonomous: true
requirements: [DOC-01, DOC-02, WASAPI-01, COREAUDIO-01, SHOWCASE-01]
must_haves:
  truths:
    - "Every Phase 41 requirement has a named xUnit test class that compiles and runs (RED until its feature lands) — the Nyquist validation contract (D-01..D-05 autonomous/human split is encoded by which tests are auto vs which behaviors route to 41-HUMAN-UAT.md)"
    - "41-HUMAN-UAT.md exists with one honest row per cross-platform/external gate, mirroring 40-HUMAN-UAT.md — never a faked pass (D-02/D-03/D-04/D-05)"
    - "The CoreAudio availability test is created here only if no prior duplicate exists (VALIDATION.md confirm-before-add note)"
  artifacts:
    - path: "flow-lang.Tests/Integration/Phase41/DocCommentLexTests.cs"
      provides: "DOC-01 lexer stub (/// vs // vs /* */)"
    - path: "flow-lang.Tests/Integration/Phase41/WasapiBackendAvailabilityTests.cs"
      provides: "WASAPI-01 Linux IsAvailable-false stub"
    - path: "flow-lang.Tests/Integration/Phase41/Phase41ShowcaseRmsTests.cs"
      provides: "SHOWCASE-01 RMS regression stub + baseline dir"
    - path: ".planning/phases/41-reach-v1-5-closer/41-HUMAN-UAT.md"
      provides: "Honest human-gate rows (D-03/D-04/D-05)"
  key_links:
    - from: "flow-lang.Tests/Integration/Phase41/*Tests.cs"
      to: "FullyQualifiedName~Phase41 filter"
      via: "namespace FlowLang.Tests.Integration.Phase41"
      pattern: "namespace.*Phase41"
---

<objective>
Seed the Wave 0 test scaffolding and the human-gate ledger for Phase 41. This plan writes NO production code — it creates the failing/skipped test classes named in 41-VALIDATION.md so every downstream task has a concrete `<automated>` target, and authors `41-HUMAN-UAT.md` so the cross-platform/external gates are recorded honestly before any of them is attempted.

Purpose: Establish the Nyquist validation contract (every requirement → a named test) and the "flag, don't fake" ledger up front, so downstream plans turn RED stubs GREEN rather than inventing verification ad hoc.
Output: 8 xUnit test classes under `flow-lang.Tests/Integration/Phase41/`, a `baselines/Phase41/` directory, and `41-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-reach-v1-5-closer/41-CONTEXT.md
@.planning/phases/41-reach-v1-5-closer/41-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the 8 Phase 41 xUnit test stubs + baseline dir</name>
  <read_first>
    - flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs (xUnit + namespace style, [Fact] shape)
    - flow-lang.Tests/Helpers/RmsRegressionTests.cs (AssertRmsWithinTolerance / AssertWavMatchesBaseline signatures, lines 39 + 72 — the showcase RMS stub calls this)
    - flow-lang/Audio/CoreAudioBackend.cs (IsAvailable() probe shape — line 19 sealed class; the availability stub asserts false on Linux)
    - flow-lang/Audio/AudioPlaybackManager.cs:82,164 (CoreAudioBackend.IsAvailable() call sites — confirm the public static probe exists)
  </read_first>
  <action>
    Confirm first whether a CoreAudio availability test already exists: grep the test project for `CoreAudioBackendAvailability`. If a duplicate exists, SKIP creating CoreAudioBackendAvailabilityTests.cs and note it in the summary; otherwise create it.

    Create these 8 test classes under namespace `FlowLang.Tests.Integration.Phase41` (one file each, matching the Phase47 xUnit + `[Fact]`/`[Trait]` style). Each test should compile and run today; for features not yet built, use `[Fact(Skip = "Wave 0 stub — turns RED/GREEN when {plan} lands {feature}")]` so the class is discoverable under `--filter "FullyQualifiedName~Phase41"` without failing the suite prematurely. Name the methods exactly so downstream `<automated>` filters resolve:

    - DocCommentLexTests.cs — DOC-01: `TripleSlash_LexesAsDocComment`, `DoubleSlash_Unchanged`, `BlockComment_Unchanged`. Skip-stub until 41-02.
    - DocCommentBindTests.cs — DOC-01: `DocComment_BindsToFollowingProc`, `OrphanDocComment_DroppedCharitably`. Skip-stub until 41-02.
    - FlowDocGenTests.cs — DOC-01: `FlowDoc_EmitsHtmlAndMarkdown`, `FlowDoc_OutPath_NormalizedNoTraversal` (T-41 path-traversal). Skip-stub until 41-03.
    - DocCacheTests.cs — DOC-01: `ContentHashCache_SkipsUnchanged`, `EditedEntry_Regenerates`. Skip-stub until 41-03.
    - DocExampleExecTests.cs — DOC-02: `PassingExample_NoAnnotation`, `FailingExample_AnnotatedExampleFailed`. Skip-stub until 41-03.
    - WasapiBackendAvailabilityTests.cs — WASAPI-01: `IsAvailable_FalseOnLinux_NoCrash`. Skip-stub until 41-04 (the class must exist now; the assertion `WasapiBackend.IsAvailable() == false` on Linux goes live when 41-04 adds the type).
    - CoreAudioBackendAvailabilityTests.cs (only if no duplicate) — COREAUDIO-01: `IsAvailable_FalseOnLinux_NoThrow` — this one can be LIVE (not skipped) since CoreAudioBackend already exists; assert `CoreAudioBackend.IsAvailable()` returns false on this Linux box and does not throw.
    - Phase41ShowcaseRmsTests.cs — SHOWCASE-01: `Showcase_RmsWithinTolerance` calling `RmsRegressionTests.AssertRmsWithinTolerance` against a baseline under `baselines/Phase41/`. Skip-stub until 41-07 (baseline WAV does not exist yet).

    Create `flow-lang.Tests/baselines/Phase41/.gitkeep` so the baseline directory is tracked (41-07 drops the real baseline WAV here). Tag every class with `[Trait("Category", "Phase41")]` for the wave-level subset run.

    Do NOT write any production code, lexer change, or backend in this task — stubs only.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase41" 2>&1 | grep -E "Passed!|Failed!|error" ; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build flow-lang.Tests` exits 0 (all 8 stub classes compile)
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase41"` runs with 0 FAILED (skipped stubs + the live CoreAudio availability test pass)
    - Each file declares `namespace FlowLang.Tests.Integration.Phase41` and carries `[Trait("Category", "Phase41")]`
    - `flow-lang.Tests/baselines/Phase41/.gitkeep` exists
    - CoreAudioBackendAvailabilityTests.cs is created ONLY if grep found no pre-existing duplicate (summary records which)
  </acceptance_criteria>
  <done>All 8 (or 7 if CoreAudio dup exists) Phase 41 test classes compile and run with 0 FAILED under the Phase41 filter; baseline dir tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Author 41-HUMAN-UAT.md with honest cross-platform/external gate rows</name>
  <read_first>
    - .planning/phases/40-studio-sync/40-HUMAN-UAT.md (the structural template to mirror — row format, machine-vs-human split)
    - .planning/phases/41-reach-v1-5-closer/41-CONTEXT.md (D-02/D-03/D-04/D-05 gate definitions)
    - .planning/phases/41-reach-v1-5-closer/41-VALIDATION.md (§Manual-Only Verifications — the 5 manual rows)
  </read_first>
  <action>
    Create `.planning/phases/41-reach-v1-5-closer/41-HUMAN-UAT.md` mirroring the table structure of `40-HUMAN-UAT.md` (Row #, What to verify, Requirement, How to verify, Status=pending). Write one row per human gate, all status `pending` (never `passed`):

    1. Windows WASAPI audible playback (WASAPI-01, D-05) — run `flow run examples/<genre>/<piece>.flow` on Windows; confirm audible stereo.
    2. macOS CoreAudio audible playback + <20 ms round-trip latency (COREAUDIO-01, D-05) — live-coding session on macOS; if >20 ms escalate to OwnAudioSharp swap per D-18.
    3. osx-x64 binary execution smoke (BIN-01, D-05) — `flow version` + a render on macOS x64.
    4. osx-arm64 binary execution smoke (BIN-01, D-05) — `flow version` + a render on Apple Silicon.
    5. win-x64 binary execution smoke (BIN-01, D-05) — `flow version` + a render on Windows.
    6. JetBrains Marketplace publish (JET-01, D-03) — `./gradlew signPlugin publishPlugin` with env-var `CERTIFICATE_CHAIN`/`PRIVATE_KEY`/`PRIVATE_KEY_PASSWORD`/`PUBLISH_TOKEN` (never committed) from the composer's JetBrains account.
    7. v1.5.0 GitHub Release cut (BIN-01/SHOWCASE-01, D-04) — composer verifies each `.sha256`, attaches the 5 binaries + showcase WAV/MIDI, pushes the Release.

    Add a top note stating these are the "flag, don't fake" gates per `feedback_autonomous_phase_execution` — the autonomous run lands code + cross-compiles + checksums, but real-hardware/external-account verification is the composer's action. Reference D-02 (code lands here, audible verify there) explicitly.
  </action>
  <verify>
    <automated>test -f .planning/phases/41-reach-v1-5-closer/41-HUMAN-UAT.md && grep -c -i "pending" .planning/phases/41-reach-v1-5-closer/41-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `41-HUMAN-UAT.md` exists with at least 7 gate rows
    - Every row's status is `pending` (zero rows marked passed/verified)
    - Rows cover: Windows WASAPI audible, macOS CoreAudio audible+latency, osx-x64 exec, osx-arm64 exec, win-x64 exec, JetBrains publish, GitHub Release
    - File references D-02/D-03/D-04/D-05 and the env-var-only secret handling for JET-01
  </acceptance_criteria>
  <done>41-HUMAN-UAT.md mirrors 40-HUMAN-UAT.md structure; 7 honest pending rows; no faked pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan writes test scaffolding + a planning doc only — no runtime, no input parsing, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-IDISCLOSE | Information Disclosure | 41-HUMAN-UAT.md JET-01 row | mitigate | Document signing cert / publish token as env-var-only (`CERTIFICATE_CHAIN`/`PRIVATE_KEY`/`PUBLISH_TOKEN`); the row explicitly states "never committed" so no secret enters VCS via the UAT doc. |
| T-41-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure test-stub + doc authoring). NAudio.Wasapi pin is handled in 41-04 with its own checkpoint. |
</threat_model>

<verification>
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase41"` → 0 FAILED
- `dotnet build flow-lang.Tests` exits 0
- 41-HUMAN-UAT.md present with 7 pending rows
</verification>

<success_criteria>
- 8 (or 7) Phase 41 test classes compile and are discoverable under the Phase41 filter with 0 FAILED
- baselines/Phase41/ directory tracked
- 41-HUMAN-UAT.md authored honestly (no faked pass) mirroring 40-HUMAN-UAT.md
</success_criteria>

<output>
Create `.planning/phases/41-reach-v1-5-closer/41-01-SUMMARY.md` when done
</output>
