---
phase: 49-flowlang-dev-site
plan: 07
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - flow-site/src/lib/showcase/pieces.ts
  - flow-site/src/lib/showcase/PieceCard.svelte
  - flow-site/src/routes/showcase/+page.svelte
  - flow-site/src/routes/showcase/[slug]/+page.ts
  - flow-site/src/routes/showcase/[slug]/+page.svelte
  - flow-site/static/showcase/
  - flow-site/tests/showcase.spec.ts
autonomous: true
requirements:
  - REQ-SITE-SHOWCASE-01

must_haves:
  truths:
    - "The /showcase gallery renders 6-12 curated piece cards"
    - "Each /showcase/[slug] detail shows gesture-gated audio + shiki source + composer notes"
    - "No audio autoplays; every piece has an explicit play button"
    - "Pieces whose .flow source is absent from the worktree link out charitably (no fabricated source)"
  artifacts:
    - path: "flow-site/src/lib/showcase/pieces.ts"
      provides: "curated piece manifest (title, genre, audio, source ref, notes)"
    - path: "flow-site/src/routes/showcase/+page.svelte"
      provides: "the gallery grid"
    - path: "flow-site/src/routes/showcase/[slug]/+page.svelte"
      provides: "piece detail with audio + source + notes + Open-in-playground"
  key_links:
    - from: "flow-site/src/routes/showcase/[slug]/+page.svelte"
      to: "shiki source block + Open-in-playground"
      via: "shiki.ts highlightFlow"
      pattern: "highlightFlow|shiki"
---

<objective>
Build the showcase gallery (`/showcase`) and per-piece detail (`/showcase/[slug]`) — 6–12 curated Flow pieces (D-49-24) with gesture-gated audio, shiki-highlighted source, and composer notes. Validates Flow's genre-agnostic claim with audible artifacts.

Purpose: The "hear what Flow makes" surface. Honor the worktree reality — some D-49-24 pieces (symphony/ragtime) had their `.flow` source deleted (STATE.md); link those out charitably instead of fabricating source.
Output: pieces manifest + gallery + detail pages + showcase E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-flowlang-dev-site/49-UI-SPEC.md
@.planning/phases/49-flowlang-dev-site/49-CONTEXT.md
@.planning/phases/49-flowlang-dev-site/49-02-PLAN.md

<interfaces>
<!-- D-49-24 curated set (6-12 pieces). Worktree source-file reality (verified): -->
  PRESENT .flow source in worktree: examples/generative/markov_jazz.flow (Markov jazz),
    examples/dsp/granular.flow (granular showpiece), examples/scala/intro.flow (Carlos Alpha microtonal
    + a Bohlen-Pierce variant lives in scala examples), flow-lang/improv/styles/{jazz,blues,classical}.flow.
  ABSENT in this worktree (deleted — STATE.md commits cd9f053/9990782): examples/symphony/*.flow ("In Five
    Voices"), examples/ragtime/*.flow ("Stride & Stomp") — link to GitHub source / v1.4 release audio assets
    charitably; do NOT fabricate the .flow source.
  v1.5 third-genre piece (Phase 41 SHOWCASE-01) is NOT yet built — mark it "coming with v1.5 closer" or omit
    until Phase 41 ships; do NOT invent it.

Components (Plan 49-02): $lib/components/skeuo/{Panel,Button,LedIndicator}.svelte; shiki via $lib/docs/shiki.ts.
Layout (UI-SPEC §Showcase): gallery = grid of <Panel framed> cards (title + genre tag + felt-grille play
  affordance); detail = hero <audio> behind explicit play <Button> + <LedIndicator> (gesture-gated, plays
  inline on mobile per D-49-01/09) + shiki source block + composer notes + "Open in playground" <Button secondary>.
No autoplay (D-49-01). Audio assets under static/showcase/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curated piece manifest + gallery grid</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-CONTEXT.md (D-49-24 piece list, D-49-01 no-autoplay)
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Showcase gallery layout)
    - examples/generative/markov_jazz.flow, examples/dsp/granular.flow, examples/scala/intro.flow (present source to embed)
  </read_first>
  <action>
    Write `flow-site/src/lib/showcase/pieces.ts` — a manifest of 6–12 curated pieces, each `{ slug, title, genre, audioSrc?, sourceFlow?|sourceRef, notes }`. For pieces with present `.flow` source (Markov jazz, granular, Carlos Alpha microtonal, Bohlen-Pierce, a jazz/blues improv style demo), embed the actual source text. For absent pieces (symphony, ragtime), set `sourceRef` to the GitHub source URL / v1.4 release audio asset and a clear "source in the repo / released audio" note — NO fabricated `.flow`. Curate to land in the 6–12 range using what actually exists. Write `flow-site/src/lib/showcase/PieceCard.svelte` (a `<Panel framed>` card: title, one-line genre tag, felt-grille play affordance, links to `/showcase/[slug]`). Write `flow-site/src/routes/showcase/+page.svelte` (`prerender = true`) rendering the gallery grid from the manifest inside a `<main>` landmark. Place any curated audio under `static/showcase/`.
  </action>
  <acceptance_criteria>
    - `pieces.ts` exports 6–12 pieces; pieces with present source embed real `.flow`; absent pieces carry a `sourceRef` (no fabricated source).
    - `/showcase` renders one card per manifest piece inside a single `<main>`.
    - `pnpm -C flow-site build` exits 0 and prerenders the gallery.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && node -e "const m=require('./src/lib/showcase/pieces.ts')||{};" 2>/dev/null; grep -cE "slug" src/lib/showcase/pieces.ts | grep -qE "[6-9]|1[0-2]" && grep -qi "main" src/routes/showcase/+page.svelte && pnpm build && echo GALLERY_OK</automated>
  </verify>
  <done>Curated 6–12-piece manifest + gallery grid; present pieces embed real source, absent pieces link out charitably.</done>
</task>

<task type="auto">
  <name>Task 2: Piece detail page + gesture-gated audio + shiki source + E2E</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Showcase /showcase/[slug] detail layout, §Copywriting "Press play to listen" / "Open in playground")
    - flow-site/src/lib/showcase/pieces.ts (Task 1 manifest)
    - flow-site/src/lib/docs/shiki.ts (highlightFlow)
    - flow-site/tests/showcase.spec.ts (the Plan 49-01 stub — note: VALIDATION lists this E2E under SHOWCASE coverage; create it if 49-01 named it differently)
  </read_first>
  <action>
    Write `flow-site/src/routes/showcase/[slug]/+page.ts` (`prerender = true`, `entries()` from the manifest slugs) + `+page.svelte`: hero `<audio>` behind an explicit play `<Button>` + `<LedIndicator>` (gesture-gated, NO autoplay, plays inline on mobile per D-49-01/09); the shiki-highlighted source block (`highlightFlow` for present source; a "view source on GitHub" link for absent pieces); composer notes ("why this piece, what Flow features show up"); and an "Open in playground" `<Button secondary>` deep-linking the source into `/playground#code=...` (only for pieces with embeddable source that runs on the Web target). Flesh out `flow-site/tests/showcase.spec.ts`: assert the gallery lists every piece, each detail page renders (200), audio has NO autoplay and a play control, and present-source pieces show a highlighted source block. Replace the stub.
  </action>
  <acceptance_criteria>
    - `/showcase/[slug]` prerenders one page per manifest piece.
    - Detail audio has no `autoplay`; play is behind an explicit `<Button>` + `<LedIndicator>`.
    - Present-source pieces render a shiki source block + "Open in playground"; absent pieces show a source link instead.
    - `flow-site/tests/showcase.spec.ts` covers gallery + details + no-autoplay, no `test.skip`.
    - `pnpm -C flow-site playwright test tests/showcase.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && ! grep -q "autoplay" src/routes/showcase/[slug]/+page.svelte && grep -qiE "highlightFlow|shiki" src/routes/showcase/[slug]/+page.svelte && ! grep -q "test.skip" tests/showcase.spec.ts && pnpm build && pnpm playwright test tests/showcase.spec.ts && echo DETAIL_OK</automated>
  </verify>
  <done>Per-piece detail pages with gesture-gated audio, shiki source (or charitable link), composer notes, Open-in-playground; showcase E2E green.</done>
</task>

</tasks>

<threat_model>
No new threat surface — showcase is static prerendered content with first-party audio + curated source. Audio embeds are gesture-gated (no autoplay). Source blocks render via shiki (escaped HTML), never `{@html}` of untrusted input. Open-in-playground deep-links carry only the site's own curated snippets; the playground performs the defensive decode (Plan 49-05/06).
</threat_model>

<verification>
- `pnpm -C flow-site build` exits 0; gallery + all detail pages prerender.
- `pnpm -C flow-site playwright test tests/showcase.spec.ts` passes.
- No autoplay anywhere; absent-source pieces link out (no fabrication).
</verification>

<success_criteria>
- /showcase gallery (6–12 pieces) + /showcase/[slug] detail (D-49-24).
- Gesture-gated audio (D-49-01); shiki source for present pieces; charitable links for absent ones.
- Showcase E2E green.
</success_criteria>

<output>
Create `.planning/phases/49-flowlang-dev-site/49-07-SUMMARY.md` when done.
</output>
