---
phase: 49-flowlang-dev-site
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - flow-site/src/lib/design/tokens.css
  - flow-site/src/app.css
  - flow-site/src/lib/components/skeuo/Button.svelte
  - flow-site/src/lib/components/skeuo/Knob.svelte
  - flow-site/src/lib/components/skeuo/Toggle.svelte
  - flow-site/src/lib/components/skeuo/Panel.svelte
  - flow-site/src/lib/components/skeuo/MetalRail.svelte
  - flow-site/src/lib/components/skeuo/LedIndicator.svelte
  - flow-site/src/lib/components/skeuo/Slider.svelte
  - flow-site/src/lib/components/skeuo/Tabs.svelte
  - flow-site/src/lib/design/theme.ts
  - flow-site/src/lib/design/surfaces.css
  - flow-site/scripts/optimize-textures.mjs
  - flow-site/static/textures/
  - flow-site/static/fonts/
  - flow-site/src/routes/design/+page.svelte
  - flow-site/tests/visual.spec.ts
autonomous: true
requirements:
  - REQ-SITE-DESIGN-01
  - REQ-SITE-DESIGN-02
  - REQ-SITE-DESIGN-03
  - REQ-SITE-DESIGN-04
  - REQ-SITE-DESIGN-05
  - REQ-SITE-A11Y-01
  - REQ-SITE-A11Y-03

must_haves:
  truths:
    - "All 8 skeuo components render in light + dark themes at /design"
    - "Theme toggle persists in localStorage and honors prefers-color-scheme first visit"
    - "Knob renders as a flat slider under prefers-reduced-motion; cards lose shadows for a 1px border"
    - "Every interactive component has a brass 2px focus-visible ring and full keyboard operation"
    - "Each .surface-* class degrades to a flat base color on texture failure"
  artifacts:
    - path: "flow-site/src/lib/design/tokens.css"
      provides: "all D-49-17 tokens as CSS custom properties + dark-theme overrides"
      contains: "--color-walnut"
      min_lines: 60
    - path: "flow-site/src/lib/design/surfaces.css"
      provides: "the four .surface-* material utility classes"
      contains: ".surface-wood"
    - path: "flow-site/src/lib/components/skeuo/Knob.svelte"
      provides: "role=slider rotary control + reduced-motion flat-slider fallback"
      contains: "role=\"slider\""
    - path: "flow-site/src/lib/components/skeuo/Tabs.svelte"
      provides: "5-tab nav with aria-current + external GitHub variant"
      contains: "aria-current"
    - path: "flow-site/src/routes/design/+page.svelte"
      provides: "component showcase for visual regression"
  key_links:
    - from: "flow-site/src/app.css"
      to: "tokens.css + surfaces.css"
      via: "@import + @theme"
      pattern: "@import.*tokens"
    - from: "flow-site/src/lib/components/skeuo/Toggle.svelte"
      to: "localStorage theme persistence"
      via: "theme.ts setTheme"
      pattern: "localStorage"
---

<objective>
Build the skeuomorphic design system: design tokens (D-49-17), four material surface classes (D-49-18), the ≤8 base components (D-49-19), the second dark theme (D-49-20), self-hosted fonts, and a `/design` showcase page that renders every component in light/dark/reduced-motion for visual-regression baselining. This is the visual vocabulary every page consumes.

Purpose: Restraint-driven skeuomorphism (Logic Pro / Reason / vintage synth — NOT glassmorphism). The UI-SPEC is the binding visual contract; this plan implements it exactly.
Output: tokens.css + surfaces.css + 8 components + theme.ts + /design page + visual.spec.ts baselines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-flowlang-dev-site/49-UI-SPEC.md
@.planning/phases/49-flowlang-dev-site/49-RESEARCH.md

<interfaces>
<!-- The UI-SPEC is the source of truth. Exact token values + component contracts: -->
Tokens (D-49-17, UI-SPEC §Color/Typography/Spacing/Motion):
  Light: --color-paper #F5F0E6, --color-walnut #5C3A21, --color-walnut-soft #7A5235,
         --color-brass #C9A567, --color-slate #2C2E33, --color-ink #241A12,
         --color-ink-muted #5A4B3C, --color-felt #6E2B2B, --color-danger #A8322B
  Dark ([data-theme="dark"]): paper #2A1F18, walnut #1C140E, walnut-soft #3A2A1E,
         brass #E0B45C (amber-LED), slate #15161A, ink #EDE3D3, ink-muted #B8A78F,
         felt #4A1E1E, danger #E0635A
  Spacing --space-1..16: 4/8/12/16/24/32/48/64. Type 12/14/16/18/24/32/48 w/ line-height+tracking.
  Shadow 1/3/8/16/32. Radius 2/4/8/12/24. --motion-press 50ms, --ease-overshoot cubic-bezier(0.16,1,0.3,1),
  --motion-slide 180ms, --motion-led 1200ms, --motion-hover 120ms.
  Fonts (self-hosted woff2, font-display swap): Inter (400/600) body/UI, Recoleta (700) wordmark/hero,
  JetBrains Mono (400) code.

8 components (UI-SPEC §Component Inventory — variant/state/a11y/reduced-motion contracts are MANDATORY):
  Button (primary brass/secondary metal/ghost/icon/danger; pressed=INSET 50ms; brass focus ring; real <button>)
  Knob (role=slider, aria-valuemin/max/now/valuetext, Arrow/Home/End/PageUp-Down keys; reduced-motion→flat Slider)
  Toggle (role=switch, aria-checked; theme toggle persists localStorage; slide overshoot ease)
  Panel (framed/inset/header; elevated 8-16px vs seated 1-3px; reduced-motion→1px walnut border, no shadow)
  MetalRail (aria-hidden decorative)
  LedIndicator (idle/rendering/playing/error; status via visually-hidden aria-live, NOT color alone)
  Slider (role=slider or native range; vertical fader + horizontal/knob-fallback)
  Tabs (5-tab nav; aria-current="page"; external GitHub variant target=_blank rel=noopener + "(opens in new tab)")

Brass accent reserved ONLY for: primary CTA fills, focus ring, active-tab indicator, LED playing glow,
  Knob pointer+value-arc. Never body text/large fills/plain links (links=walnut underline). >10% viewport brass = wrong.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens + Tailwind v4 @theme + material surfaces + self-hosted fonts</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Design System, §Spacing Scale, §Typography, §Color, §Material Surfaces, §Motion)
    - flow-site/src/app.css (the Tailwind v4 entry from Plan 49-01)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Code Examples Tailwind v4 tokens, Pitfall 5, D-49-32 image optimization)
  </read_first>
  <action>
    Write `flow-site/src/lib/design/tokens.css` declaring every token from the interfaces block as CSS custom properties under `:root` (light defaults) + a `[data-theme="dark"]` override block (D-49-20 amber-LED dark palette). Map the color/radius/motion tokens into Tailwind v4 via an `@theme` block in `src/app.css` (Pitfall 5 — `@theme`, NOT a `tailwind.config.js`); `@import "./lib/design/tokens.css"` and `@import "./lib/design/surfaces.css"` from app.css. Add the global `@media (prefers-reduced-motion: reduce)` rule zeroing animation/transition durations (RESEARCH code example). Write `flow-site/src/lib/design/surfaces.css` with the four `.surface-*` classes (UI-SPEC §Material Surfaces): `.surface-brushed-metal` (slate→satin linear-gradient base + inline-SVG feTurbulence horizontal-brush overlay), `.surface-paper` (paper base + inline-SVG paper-fiber speckle + optional staff-line ruling variant), `.surface-felt` (felt color + inline-SVG dot/weave + recessed inner shadow), `.surface-wood` (walnut gradient base + a `background-image` referencing the Sharp-optimized wood-grain raster); EACH class must define its flat-base fallback first so first paint is correct before the overlay (D-49-33) and a `prefers-reduced-data` degrade to flat. Self-host Inter (400/600), Recoleta (700), JetBrains Mono (400) as woff2 under `static/fonts/` with `@font-face` `font-display: swap` (UI-SPEC §Font loading — no Google Fonts CDN, keeps CSP `font-src 'self'`). Write `flow-site/scripts/optimize-textures.mjs` (Sharp) that emits AVIF (≥80% quality, D-49-32) + WebP + PNG for the wood-grain source into `static/textures/`; run it to generate the assets.
  </action>
  <acceptance_criteria>
    - `flow-site/src/lib/design/tokens.css` defines `--color-walnut`, `--color-brass`, `--space-16`, `--ease-overshoot`, and a `[data-theme="dark"]` block (grep, comment-filtered).
    - `flow-site/src/lib/design/surfaces.css` defines all four `.surface-brushed-metal|wood|paper|felt` classes.
    - `flow-site/src/app.css` imports tokens.css + surfaces.css and contains an `@theme` block.
    - `static/fonts/` contains the three self-hosted woff2 families; no Google Fonts URL appears in any CSS (`! grep -rq "fonts.googleapis" flow-site/src`).
    - `static/textures/` contains a wood-grain `.avif` + `.webp` + `.png`.
    - `pnpm -C flow-site build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && grep -v '^[[:space:]]*/\*' src/lib/design/tokens.css | grep -q -- "--color-walnut" && grep -q ".surface-wood" src/lib/design/surfaces.css && grep -q "@theme" src/app.css && ! grep -rq "fonts.googleapis" src && ls static/textures/*.avif >/dev/null && pnpm build && echo TOKENS_OK</automated>
  </verify>
  <done>Tokens, dark theme, four material surfaces (with flat fallbacks), self-hosted fonts, and optimized wood-grain textures all in place; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The 8 skeuomorphic components + theme persistence</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Component Inventory items 1-8 — full per-component variant/state/a11y/reduced-motion contract)
    - flow-site/src/lib/design/tokens.css (Task 1 output)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Code Examples accessible Knob skeleton; Don't Hand-Roll accessible rotary)
  </read_first>
  <behavior>
    - Button: renders a real `<button>`; `primary` variant has brass face + walnut label; pressed state toggles an inset class; carries a `:focus-visible` brass ring; icon variant requires `aria-label`.
    - Knob: `role="slider"` + `tabindex="0"` + aria-valuemin/max/now/valuetext; ArrowUp/Right +step, ArrowDown/Left −step, Home→min, End→max, PageUp/Down ±large; renders a flat Slider when `prefers-reduced-motion` matches.
    - Toggle: `role="switch"` + `aria-checked`; Space/Enter flips; theme variant writes `localStorage` and sets `[data-theme]`.
    - LedIndicator: state prop idle/rendering/playing/error; mirrors state in a visually-hidden `aria-live="polite"` text node (status not by color alone); LED node `aria-hidden`.
    - Tabs: 5 items; active route gets `aria-current="page"`; GitHub item is `target="_blank" rel="noopener noreferrer"` with visually-hidden "(opens in new tab)".
    - theme.ts: `getInitialTheme()` reads localStorage else `prefers-color-scheme`; `setTheme(t)` persists + applies `[data-theme]`.
  </behavior>
  <action>
    Implement the 8 Svelte 5 (runes) components under `flow-site/src/lib/components/skeuo/` exactly to the UI-SPEC §Component Inventory contracts: `Button.svelte` (5 variants, INSET pressed at `--motion-press`, brass focus ring, 44px min hit area), `Knob.svelte` (role=slider + the full keyboard arrow/Home/End/PageUp-Down model + `aria-valuetext` "{value} {unit}" + reduced-motion→flat `<Slider>` render), `Toggle.svelte` (role=switch, sun/moon `with-icons` variant for the theme switch), `Panel.svelte` (framed/inset/header variants; elevated vs seated shadows; reduced-motion swaps shadow → 1px walnut border per D-49-10), `MetalRail.svelte` (`aria-hidden`), `LedIndicator.svelte` (idle/rendering/playing/error + visually-hidden `aria-live` mirror), `Slider.svelte` (vertical fader + horizontal knob-fallback; same role=slider aria contract), `Tabs.svelte` (the 5-tab nav with `local`/`external` variants). Write `flow-site/src/lib/design/theme.ts` with `getInitialTheme()` + `setTheme()` (localStorage + prefers-color-scheme per D-49-20), consumed by the Toggle theme variant. Component-test the a11y-critical contracts via `@testing-library/svelte` (Knob keyboard model + aria attributes; Toggle role=switch + localStorage; LedIndicator aria-live text) so the behaviors are pinned before the visual layer.
  </action>
  <acceptance_criteria>
    - All 8 component files exist under `src/lib/components/skeuo/`.
    - `Knob.svelte` contains `role="slider"` and handles `ArrowUp`/`Home`/`End`/`PageUp`.
    - `Toggle.svelte` contains `role="switch"` and `theme.ts` writes `localStorage`.
    - `Tabs.svelte` contains `aria-current` and the external `rel="noopener noreferrer"`.
    - `LedIndicator.svelte` contains `aria-live`.
    - Component tests pass: `pnpm -C flow-site vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && ls src/lib/components/skeuo/*.svelte | wc -l | grep -q 8 && grep -q 'role="slider"' src/lib/components/skeuo/Knob.svelte && grep -q 'role="switch"' src/lib/components/skeuo/Toggle.svelte && grep -q 'aria-current' src/lib/components/skeuo/Tabs.svelte && grep -q 'aria-live' src/lib/components/skeuo/LedIndicator.svelte && pnpm vitest run && echo COMPONENTS_OK</automated>
  </verify>
  <done>All 8 components implemented to spec with mandatory a11y contracts; theme persistence works; component tests green.</done>
</task>

<task type="auto">
  <name>Task 3: /design showcase page + visual-regression baselines</name>
  <read_first>
    - flow-site/src/lib/components/skeuo/ (Task 2 components)
    - flow-site/tests/visual.spec.ts (the Plan 49-01 stub)
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Component Inventory, §Motion reduced-motion rules)
  </read_first>
  <action>
    Write `flow-site/src/routes/design/+page.svelte` (NOT prerendered for the runtime parts, but static is fine here) that renders every one of the 8 components in all documented states grouped by component, with a theme toggle and a "reduced-motion preview" wrapper. Flesh out `flow-site/tests/visual.spec.ts` to capture Playwright screenshot baselines of `/design` in three configurations — light theme, dark theme (`[data-theme="dark"]`), and `prefers-reduced-motion: reduce` (Playwright `colorScheme`/`reducedMotion` + a theme set via `addInitScript`) — establishing the visual-regression baselines for REQ-SITE-DESIGN-01..04. Replace the `test.skip` placeholder.
  </action>
  <acceptance_criteria>
    - `/design` route renders all 8 components (page references each component import).
    - `pnpm -C flow-site build` exits 0 with the /design route.
    - `flow-site/tests/visual.spec.ts` captures light + dark + reduced-motion screenshots and no longer contains `test.skip`.
    - `pnpm -C flow-site playwright test tests/visual.spec.ts` runs (baselines written on first run; pass on second).
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && grep -c "skeuo/" src/routes/design/+page.svelte | grep -qE "[8-9]|1[0-9]" && pnpm build && ! grep -q "test.skip" tests/visual.spec.ts && pnpm playwright test tests/visual.spec.ts --update-snapshots && pnpm playwright test tests/visual.spec.ts && echo DESIGN_OK</automated>
  </verify>
  <done>/design renders the full component inventory; visual-regression baselines captured for light/dark/reduced-motion.</done>
</task>

</tasks>

<threat_model>
No new threat surface — static prerendered design-system content, no user input, no secrets, no network calls. Self-hosted fonts keep `font-src 'self'` tight (closes the CSP font-CDN hole). Inline-SVG textures carry no external fetches.
</threat_model>

<verification>
- `pnpm -C flow-site build` exits 0.
- `pnpm -C flow-site vitest run` exits 0 (component a11y tests).
- `pnpm -C flow-site playwright test tests/visual.spec.ts` passes against baselines.
- All 8 components + tokens + dark theme + /design present.
</verification>

<success_criteria>
- D-49-17 tokens + D-49-18 surfaces + D-49-19 components (exactly 8) + D-49-20 dark theme all implemented to the UI-SPEC.
- Reduced-motion + keyboard + focus-ring contracts pinned by tests.
- /design showcase + visual baselines for downstream visual regression.
</success_criteria>

<output>
Create `.planning/phases/49-flowlang-dev-site/49-02-SUMMARY.md` when done.
</output>
