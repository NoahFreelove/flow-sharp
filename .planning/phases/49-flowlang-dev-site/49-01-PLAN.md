---
phase: 49-flowlang-dev-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-site/package.json
  - flow-site/pnpm-lock.yaml
  - flow-site/svelte.config.js
  - flow-site/vite.config.ts
  - flow-site/tsconfig.json
  - flow-site/src/app.css
  - flow-site/src/app.html
  - flow-site/src/routes/+layout.svelte
  - flow-site/src/routes/+page.svelte
  - flow-site/src/routes/playground/+page.svelte
  - flow-site/static/wasm/
  - flow-site/scripts/sync-runtime.sh
  - flow-site/_headers
  - flow-site/.gitignore
  - flow-site/vitest.config.ts
  - flow-site/playwright.config.ts
  - flow-site/lighthouserc.cjs
  - flow-site/tests/
  - flow-site/src/lib/docs/transform.test.ts
  - flow-site/src/lib/share/encode.test.ts
  - flow-site/workers/gist-auth.test.ts
autonomous: false
requirements:
  - REQ-SITE-IA-02
  - REQ-SITE-IA-03
  - REQ-SITE-DEPLOY-01
user_setup:
  - service: cloudflare-pages
    why: "Create the CF Pages project + connect the git repo for deploys (composer's CF account; project name flow-music or flow-music-playground per D-49-36)"
    dashboard_config:
      - task: "Create a Pages project, set build command `pnpm -C flow-site build`, output dir `flow-site/.svelte-kit/cloudflare`, and note the assigned `<project>.pages.dev` URL"
        location: "Cloudflare dashboard -> Workers & Pages -> Create -> Pages"

must_haves:
  truths:
    - "flow-site/ is a SvelteKit 2 + Svelte 5 + TypeScript + Tailwind v4 project that builds clean"
    - "The Phase 48 AppBundle is committed under flow-site/static/wasm/ with flow-runtime.js at root + _framework/ sibling intact"
    - "Per-route render strategy is set: Home prerendered, /playground client-only SPA"
    - "The entire greenfield test stack (vitest + playwright + lhci + axe) is installed and configured"
    - "A `<project>.pages.dev` URL serves the placeholder site"
  artifacts:
    - path: "flow-site/svelte.config.js"
      provides: "adapter-cloudflare + mdsvex preprocess wiring"
      contains: "adapter-cloudflare"
    - path: "flow-site/vite.config.ts"
      provides: "@tailwindcss/vite plugin + Monaco worker config foundation"
      contains: "tailwindcss"
    - path: "flow-site/static/wasm/flow-runtime.js"
      provides: "Phase 48 runtime entry, copied verbatim"
    - path: "flow-site/scripts/sync-runtime.sh"
      provides: "dotnet publish FlowTarget=Web + cp AppBundle -> static/wasm/"
    - path: "flow-site/_headers"
      provides: "CSP + Permissions-Policy + (scoped) COOP/COEP for CF Pages"
      contains: "Permissions-Policy"
    - path: "flow-site/vitest.config.ts"
      provides: "unit/component test runner config"
    - path: "flow-site/playwright.config.ts"
      provides: "E2E test runner config with desktop + mobile (375px) + mobile-narrow (320px) projects"
    - path: "flow-site/lighthouserc.cjs"
      provides: "Lighthouse CI thresholds ≥90 on 4 axes"
  key_links:
    - from: "flow-site/src/routes/playground/+page.svelte"
      to: "ssr=false / csr=true"
      via: "page options export"
      pattern: "ssr = false"
    - from: "flow-site/scripts/sync-runtime.sh"
      to: "flow-lang/bin/Release/net10.0/browser-wasm/AppBundle"
      via: "dotnet publish -p:FlowTarget=Web then cp"
      pattern: "FlowTarget=Web"
---

<objective>
Scaffold the greenfield `flow-site/` SvelteKit 2 / Svelte 5 / TypeScript / Tailwind v4 project, wire it to `adapter-cloudflare`, commit the Phase 48 WASM AppBundle into `static/wasm/`, install the entire Nyquist test stack (Wave 0), set the security `_headers`, and stand up a placeholder deploy on Cloudflare Pages. Everything else in Phase 49 depends on this plan.

Purpose: This is the foundation Wave. No design, no docs, no playground logic yet — just a building, deployable, test-instrumented shell with the runtime in place and render strategy declared.
Output: A `flow-site/` directory that `pnpm -C flow-site build` compiles, a live `<project>.pages.dev` URL, the committed AppBundle, and green (empty/stub) vitest + playwright + lhci configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-flowlang-dev-site/49-RESEARCH.md
@.planning/phases/49-flowlang-dev-site/49-VALIDATION.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md

<interfaces>
<!-- Phase 48 runtime contract the playground will later consume. This plan only COMMITS the bundle + declares the SPA route; the actual wiring is Plan 49-05. -->
From the published AppBundle (HANDOFF §1, layout MUST be preserved on copy):
  flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/
    flow-runtime.js        (import target — sits at AppBundle root)
    _framework/dotnet.js    (flow-runtime.js does `import './_framework/dotnet.js'`)
    _framework/dotnet.boot.js, dotnet.native.wasm, flow-lang.wasm, System.*.wasm

flow-runtime.js exports (HANDOFF §4 — frozen, do NOT edit):
  loadFlowRuntime(): Promise<Runtime>
  Runtime: { run(src)->Promise<RunResult>, play(wav,sr?,ch?), stop(), dispose(), resumeAudio()->Promise<void> }

Verified versions to pin (RESEARCH §Standard Stack, npm-verified 2026-06-05):
  @sveltejs/kit 2.63.0, svelte 5.56.2, @sveltejs/adapter-cloudflare 7.2.8,
  vite 8.0.16, tailwindcss 4.3.0, @tailwindcss/vite 4.3.0, mdsvex 0.12.7,
  shiki 4.2.0, monaco-editor 0.55.1, fflate 0.8.3, sharp 0.34.5,
  @playwright/test 1.60.0, vitest 4.1.8
  @lhci/cli + @axe-core/playwright + @testing-library/svelte — VERIFY latest at install (RESEARCH A1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold flow-site + adapter-cloudflare + Tailwind v4 + render strategy</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Standard Stack, §Recommended Project Structure, Pattern 1, Pattern 5, Pitfall 5)
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Design System table — Tailwind v4 CSS-first, no tailwind.config.js)
  </read_first>
  <action>
    Run `npx sv create flow-site` from the repo root selecting: SvelteKit minimal, TypeScript, add-ons tailwindcss + prettier + eslint + playwright + vitest. Use pnpm (Claude's-discretion per RESEARCH §Claude's Discretion; verified 11.12.1). Then `pnpm -C flow-site add -D @sveltejs/adapter-cloudflare@7.2.8 mdsvex@0.12.7 shiki@4.2.0` and `pnpm -C flow-site add monaco-editor@0.55.1 fflate@0.8.3` and `pnpm -C flow-site add -D sharp@0.34.5`. In `svelte.config.js` swap the default adapter for `adapter-cloudflare()` and add the `mdsvex` preprocess + `.md`/`.svx` to `extensions` (highlight wiring is Plan 49-04 — leave a passthrough now). In `vite.config.ts` add the `@tailwindcss/vite` plugin (Tailwind v4 path — NO `tailwind.config.js`; if `sv create` emitted one, delete it per Pitfall 5). In `src/app.css` use `@import "tailwindcss";` (token `@theme` block is Plan 49-02 — leave a placeholder comment). Create `src/routes/+page.svelte` with `export const prerender = true;` and a minimal placeholder "Flow — coming soon" h1. Create `src/routes/playground/+page.svelte` with `export const prerender = false; export const ssr = false; export const csr = true;` and a placeholder "Playground" h1 (NO Monaco/WASM import yet — that is Plan 49-05; importing them here would SSR-crash). Create `src/routes/+layout.svelte` with an `<a href="/">Flow</a>` placeholder nav (real nav is Plan 49-02/03). Add `flow-site/.gitignore` ignoring `src/docs/wiki/`, `.dev.vars`, `node_modules`, `.svelte-kit`.
  </action>
  <acceptance_criteria>
    - `pnpm -C flow-site build` exits 0 and produces `flow-site/.svelte-kit/cloudflare/` output.
    - `grep -q "adapter-cloudflare" flow-site/svelte.config.js` succeeds.
    - `grep -q "tailwindcss" flow-site/vite.config.ts` succeeds.
    - No `flow-site/tailwind.config.js` exists (`test ! -f flow-site/tailwind.config.js`).
    - `flow-site/src/routes/playground/+page.svelte` contains `ssr = false` and `csr = true`.
    - `flow-site/src/routes/+page.svelte` contains `prerender = true`.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && pnpm build && grep -q "adapter-cloudflare" svelte.config.js && grep -q "ssr = false" src/routes/playground/+page.svelte && test ! -f tailwind.config.js && echo SCAFFOLD_OK</automated>
  </verify>
  <done>flow-site builds clean with adapter-cloudflare + Tailwind v4; Home prerenders; /playground is a client-only SPA shell.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the Phase 48 AppBundle + sync-runtime.sh + _headers</name>
  <read_first>
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md (§1 AppBundle layout, §2 integration, §3 COOP/COEP, §8 what NOT to change)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Runtime State Inventory "decide commit-vs-rebuild" → COMMIT; Open Q1 headers, Open Q2 commit, A4 _headers location)
    - .planning/phases/49-flowlang-dev-site/49-CONTEXT.md (D-49-38 header set)
  </read_first>
  <action>
    Write `flow-site/scripts/sync-runtime.sh` that runs `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` then `cp -r` the resulting `flow-lang/bin/Release/net10.0/browser-wasm/AppBundle/`* contents into `flow-site/static/wasm/`, PRESERVING `flow-runtime.js`-at-root + `_framework/`-sibling (HANDOFF §1/§8 — do not flatten). Exclude `index.html` (the dev harness — not shipped). Run the script once to populate `static/wasm/`. COMMIT the published bundle into the repo (RESEARCH Open Q2 recommendation — keeps the CF Pages build pure-Node, no .NET SDK in the CF container). Do NOT add `static/wasm/` to `.gitignore`. Write `flow-site/_headers` at the PROJECT ROOT (RESEARCH A4 — official adapter-cloudflare docs place `_headers` in project root, NOT static/; charitable correction of HANDOFF §3): a global block setting `Permissions-Policy: microphone=(), camera=(), geolocation=()` + a `Content-Security-Policy` allowing `script-src 'self' 'wasm-unsafe-eval'` (the WASM runtime needs `wasm-unsafe-eval`; self-hosted Monaco per RESEARCH Open Q1 means no CDN allowance needed) + `connect-src 'self' https://api.github.com https://github.com` + `font-src 'self'` + `style-src 'self' 'unsafe-inline'`; and a `/playground/*` block carrying `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` scoped to playground only (RESEARCH Open Q1 — scoped so marketing/docs fonts/assets are unaffected; if friction surfaces at Plan 49-05/08, the two COOP/COEP lines may be removed per HANDOFF §3 sanction — keep CSP + Permissions-Policy regardless). Do NOT modify flow-runtime.js, WasmEntry.cs, or FlowRuntimeInterop.cs (HANDOFF §8).
  </action>
  <acceptance_criteria>
    - `flow-site/static/wasm/flow-runtime.js` exists at the wasm root.
    - `flow-site/static/wasm/_framework/dotnet.js` exists (sibling layout preserved).
    - `flow-site/_headers` exists at project root and contains `Permissions-Policy` + `Content-Security-Policy` + `wasm-unsafe-eval`.
    - `flow-site/scripts/sync-runtime.sh` exists, is executable, and contains `FlowTarget=Web`.
    - `flow-lang/wasm/flow-runtime.js` is byte-identical to the committed `flow-site/static/wasm/flow-runtime.js` (the runtime was NOT edited).
  </acceptance_criteria>
  <verify>
    <automated>test -f flow-site/static/wasm/flow-runtime.js && test -f flow-site/static/wasm/_framework/dotnet.js && grep -q "wasm-unsafe-eval" flow-site/_headers && grep -q "Permissions-Policy" flow-site/_headers && grep -q "FlowTarget=Web" flow-site/scripts/sync-runtime.sh && diff -q flow-lang/wasm/flow-runtime.js flow-site/static/wasm/flow-runtime.js && echo BUNDLE_OK</automated>
  </verify>
  <done>AppBundle committed under static/wasm/ with layout intact; sync-runtime.sh regenerates it; security _headers at project root; runtime source untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Install + configure the Nyquist Wave 0 test stack (stubs + configs)</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-VALIDATION.md (§Test Infrastructure, §Per-Task Verification Map, §Wave 0 Requirements — the full greenfield test stack list)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Validation Architecture, Wave 0 Gaps; A1 verify @lhci/cli + @axe-core/playwright versions)
  </read_first>
  <action>
    Install `pnpm -C flow-site add -D @playwright/test@1.60.0 vitest@4.1.8 @lhci/cli @axe-core/playwright @testing-library/svelte` (verify the latest published versions of the last three at install per RESEARCH A1; pin what installs). Write `flow-site/vitest.config.ts` (jsdom or browser env; include `src/**/*.test.ts` + `workers/**/*.test.ts`). Write `flow-site/playwright.config.ts` with three projects — `desktop` (1280×800), `mobile` (375×667, the <768px read-only viewport), and `mobile-narrow` (320×568, the narrowest supported target per ROADMAP acceptance criterion #7) — `webServer` running `pnpm preview`, `testDir: 'tests'`. Write `flow-site/lighthouserc.cjs` asserting `categories:performance|accessibility|best-practices|seo >= 0.9` on `/`, `/docs`, `/playground` (D-49-31). Create the `flow-site/tests/` directory with stub spec files (each a `test.skip` placeholder that names its REQ so they are discoverable but green): `nav.spec.ts`, `render-strategy.spec.ts`, `docs-render.spec.ts`, `docs-toc.spec.ts`, `wasm-boot.spec.ts`, `playground-run.spec.ts`, `playground-audio.spec.ts`, `playground-export.spec.ts`, `playground-mobile.spec.ts`, `a11y.spec.ts`, `visual.spec.ts`, `responsive.spec.ts`. Create the three unit-test stubs that downstream plans flesh out — `src/lib/docs/transform.test.ts`, `src/lib/share/encode.test.ts`, `workers/gist-auth.test.ts` — each importing its (not-yet-existing) module with a `test.todo`/`it.skip` placeholder so `vitest run` exits 0 today. Add package.json scripts: `"test": "vitest run"`, `"test:e2e": "playwright test"`, `"lh": "lhci autorun"`.
  </action>
  <acceptance_criteria>
    - `pnpm -C flow-site vitest run` exits 0 (stubs skip, no failures).
    - `flow-site/vitest.config.ts`, `flow-site/playwright.config.ts`, `flow-site/lighthouserc.cjs` all exist.
    - `flow-site/lighthouserc.cjs` references all three routes `/`, `/docs`, `/playground` and the four-axis ≥0.9 thresholds.
    - `flow-site/playwright.config.ts` defines a `mobile` project at a <768px width AND a `mobile-narrow` project at 320px width.
    - The 12 E2E stub specs + 3 unit stubs exist under `tests/` and `src/lib/` / `workers/`.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && pnpm vitest run && test -f playwright.config.ts && test -f lighthouserc.cjs && grep -q "playground" lighthouserc.cjs && grep -q "0.9" lighthouserc.cjs && grep -q "mobile-narrow" playwright.config.ts && grep -q "320" playwright.config.ts && ls tests/*.spec.ts | wc -l | grep -qE "1[0-9]" && echo TESTSTACK_OK</automated>
  </verify>
  <done>Greenfield test infra installed; vitest green on stubs; playwright + lhci configs present with mobile (375px) + mobile-narrow (320px) projects and 4-axis ≥90 thresholds across the three routes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The flow-site/ scaffold builds clean locally and is ready to deploy. CF Pages project creation requires the composer's Cloudflare account (D-49-36) — Claude cannot create the dashboard project or read back the assigned `*.pages.dev` URL.</what-built>
  <how-to-verify>
    1. In the Cloudflare dashboard: Workers & Pages -> Create -> Pages -> connect this git repo (or "Direct Upload" of `flow-site/.svelte-kit/cloudflare/` for a first smoke).
    2. Set project name to `flow-music` (or `flow-music-playground` if taken — D-49-36).
    3. Build command: `pnpm -C flow-site build`. Output directory: `flow-site/.svelte-kit/cloudflare`. Root: repo root.
    4. Deploy. Confirm `https://<project>.pages.dev/` serves the "Flow — coming soon" placeholder HTML.
    5. Report back the exact `<project>.pages.dev` URL so Plan 49-06 (OAuth redirect URI) + Plan 49-09 (runbook) can reference it.
  </how-to-verify>
  <resume-signal>Paste the live `<project>.pages.dev` URL, or describe the deploy error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CF Pages static assets | No user input; static prerendered HTML + opaque WASM bundle served over the edge |
| CF Pages build container → git repo | Build pulls committed code + the committed AppBundle (no .NET SDK in container — pure Node) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-HDR | Information Disclosure | `_headers` CSP | mitigate | CSP `script-src 'self' 'wasm-unsafe-eval'` (no CDN), `connect-src` limited to self + GitHub; Permissions-Policy denies mic/camera/geo explicitly (D-49-38) |
| T-49-01-COEP | Tampering | COOP/COEP scope | accept | Scoped to `/playground/*` only; v1 single-threaded WASM needs no isolation — removable per HANDOFF §3 if it breaks subresources; no security loss (foundation-only) |
| T-49-01-BUNDLE | Tampering | committed AppBundle | mitigate | Bundle copied byte-verbatim from Phase 48 publish; sync-runtime.sh + a diff-against-flow-lang/wasm/flow-runtime.js check detect drift; runtime files never edited (HANDOFF §8) |
| T-49-01-SC | Tampering | npm/pnpm installs | mitigate | All packages npm-verified with healthy download counts + first-party repos (RESEARCH §Package Legitimacy Audit, npm ecosystem — no [SLOP]/[SUS]); lockfile committed; versions pinned. No `[ASSUMED]`/`[SUS]` packages requiring a blocking checkpoint (the three "verify-version" packages @lhci/cli + @axe-core/playwright + @testing-library/svelte are first-party Lighthouse/Deque/Testing-Library, OK disposition) |
</threat_model>

<verification>
- `pnpm -C flow-site build` exits 0.
- `pnpm -C flow-site vitest run` exits 0 (Wave 0 stubs).
- AppBundle present + layout intact under `static/wasm/`; `_headers` at project root.
- A live `<project>.pages.dev` URL serves the placeholder (human-confirmed in the checkpoint).
</verification>

<success_criteria>
- flow-site/ is a building SvelteKit 2 / Svelte 5 / TS / Tailwind-v4 / adapter-cloudflare project.
- Render strategy declared (Home prerender, /playground SPA).
- Phase 48 AppBundle committed under static/wasm/ unmodified; sync-runtime.sh regenerates it.
- Full Nyquist test stack installed + configured (vitest + playwright[mobile 375px + mobile-narrow 320px] + lhci[4-axis ≥90] + axe).
- Security _headers in place.
- CF Pages placeholder live (human checkpoint).
</success_criteria>

<output>
Create `.planning/phases/49-flowlang-dev-site/49-01-SUMMARY.md` when done.
</output>
