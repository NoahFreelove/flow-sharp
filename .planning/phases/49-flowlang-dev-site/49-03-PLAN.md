---
phase: 49-flowlang-dev-site
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02", "49-04"]
files_modified:
  - flow-site/src/routes/+layout.svelte
  - flow-site/src/routes/+page.svelte
  - flow-site/src/lib/home/examples.ts
  - flow-site/src/lib/home/CodeCard.svelte
  - flow-site/src/lib/home/AudioEmbed.svelte
  - flow-site/static/audio/
  - flow-site/tests/nav.spec.ts
  - flow-site/tests/render-strategy.spec.ts
autonomous: true
requirements:
  - REQ-SITE-IA-01
  - REQ-SITE-HOME-01
  - REQ-SITE-A11Y-02

must_haves:
  truths:
    - "The 5-tab top nav renders on every route with the active tab marked and GitHub external"
    - "Home renders the six D-49-21 sections using Plan 49-02 components"
    - "Hero 'Play in playground' cards deep-link to /playground#code=... and auto-run on arrival"
    - "No audio autoplays anywhere — every embed is behind an explicit play button"
    - "Home is prerendered (static HTML, no WASM load)"
  artifacts:
    - path: "flow-site/src/routes/+layout.svelte"
      provides: "the 5-tab brushed-metal nav + theme toggle + ARIA landmarks"
      contains: "Tabs"
    - path: "flow-site/src/routes/+page.svelte"
      provides: "the six-section Home"
      contains: "Hero"
    - path: "flow-site/src/lib/home/CodeCard.svelte"
      provides: "shiki code card + Play-in-playground deep link"
    - path: "flow-site/src/lib/home/AudioEmbed.svelte"
      provides: "gesture-gated audio embed with LedIndicator (no autoplay)"
  key_links:
    - from: "flow-site/src/lib/home/CodeCard.svelte"
      to: "/playground#code=..."
      via: "encode + deep-link href + auto-run param"
      pattern: "playground#code"
    - from: "flow-site/src/routes/+layout.svelte"
      to: "skeuo Tabs + Toggle"
      via: "component import"
      pattern: "components/skeuo"
---

<objective>
Build the marketing Home page (six D-49-21 sections) and the persistent 5-tab top nav (D-49-07) using the Plan 49-02 component library. Hero examples deep-link into the playground with an auto-run gesture chain (D-49-08); all audio embeds are explicit-play, no autoplay (D-49-01); Home is prerendered.

Purpose: The landing surface — Flow's value prop, sound, and a code-first taste, in the skeuomorphic vocabulary.
Output: +layout.svelte nav + +page.svelte Home + CodeCard + AudioEmbed + nav/render-strategy E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-flowlang-dev-site/49-UI-SPEC.md
@.planning/phases/49-flowlang-dev-site/49-CONTEXT.md
@.planning/phases/49-flowlang-dev-site/49-02-PLAN.md

<interfaces>
<!-- Plan 49-02 components consumed here: -->
  $lib/components/skeuo/{Tabs,Toggle,Button,Panel,LedIndicator,MetalRail}.svelte
  $lib/design/theme.ts (getInitialTheme/setTheme)

Home sections (D-49-21, UI-SPEC §Home /):
  1. Hero — .surface-paper inlay framed by .surface-wood; "Flow" Recoleta wordmark (clamp 48→72px) +
     lead tagline + 3 "Play in playground" shiki code cards (each brass primary Button → /playground#code=...
     auto-clicks Run on arrival, one-gesture per D-49-08) + Phase 34 symphony audio embed (explicit play, NO autoplay).
  2. Value-prop trio — 3 <Panel framed> cards: Ergonomics-first / Genre-agnostic / Music-notation roots
     (sourced from PROJECT.md Goals + CLAUDE.md "## Goals & Non-Goals").
  3. "How it sounds" — <audio> embeds (v1.4 symphony + v1.5 generative) behind play Button + felt LedIndicator.
  4. Code-first explanation — single ~20-line shiki Flow snippet with margin annotations (-> / note streams / musical context).
  5. CTAs — "Install" copy-command Button + brass "Try in browser" Button → /playground.
  6. Footer — .surface-wood band: license / repo / wiki / community links. <footer> landmark.

Nav (D-49-07, UI-SPEC §Top nav + §Tabs): brushed-metal 56px desktop / 48px mobile; Recoleta wordmark
  (home link) + 5-tab <Tabs> (Home · Docs · Playground · Showcase · GitHub↗) + <Toggle> theme switch.
  Mobile <768px: tabs collapse to a hamburger <Button icon> slide-down. <nav aria-label="Primary">.
  aria-current="page" on active route; GitHub target=_blank rel=noopener + "(opens in new tab)".

Copywriting (UI-SPEC §Copywriting): "Play in playground" / "Try in browser" / "Press play to listen" / "Copy".
Audio assets: place curated mp3/wav under static/audio/ (symphony + generative samples). If the worktree
  lacks symphony source audio (examples/symphony was deleted — STATE.md), use an available rendered sample
  or link to the GitHub release asset charitably; do NOT fabricate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5-tab top nav (+layout.svelte) + theme toggle + ARIA landmarks</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Top nav, §Tabs component, §Accessibility landmarks)
    - flow-site/src/lib/components/skeuo/Tabs.svelte + Toggle.svelte (Plan 49-02)
    - flow-site/src/routes/+layout.svelte (Plan 49-01 placeholder)
  </read_first>
  <action>
    Rewrite `flow-site/src/routes/+layout.svelte` as the persistent chrome: a `.surface-brushed-metal` `<nav aria-label="Primary">` (56px desktop / 48px mobile) with the Recoleta "Flow" wordmark (links home, embossed `text-shadow` per UI-SPEC, NOT a gradient) + the `<Tabs>` 5-tab nav (Home/Docs/Playground/Showcase/GitHub — GitHub as the `external` variant) + the `<Toggle>` theme switch (initializes from `theme.ts getInitialTheme`, persists on change). Collapse tabs to a hamburger `<Button icon>` slide-down panel <768px (wordmark + theme toggle stay visible). Wrap the route slot in a single `<main>` landmark per page is handled by each page — here ensure the layout provides `<nav>` + `<footer>` slots and sets `aria-current="page"` wiring via `$page.url.pathname`. Flesh out `flow-site/tests/nav.spec.ts`: assert the 5 tabs render, the 4 local tabs navigate to their routes, GitHub is `target="_blank"` with `rel` set, and `aria-current` lands on the active route — replacing the stub.
  </action>
  <acceptance_criteria>
    - `+layout.svelte` imports `Tabs` + `Toggle` from `$lib/components/skeuo` and renders `<nav aria-label="Primary">`.
    - The 5 tab labels (Home/Docs/Playground/Showcase/GitHub) are present; GitHub carries `target="_blank"`.
    - Theme toggle initializes from `theme.ts` and persists.
    - `flow-site/tests/nav.spec.ts` asserts nav + routing + aria-current, no `test.skip`.
    - `pnpm -C flow-site build` exits 0; `pnpm -C flow-site playwright test tests/nav.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && grep -q "components/skeuo" src/routes/+layout.svelte && grep -q 'aria-label="Primary"' src/routes/+layout.svelte && grep -q "Playground" src/routes/+layout.svelte && ! grep -q "test.skip" tests/nav.spec.ts && pnpm build && pnpm playwright test tests/nav.spec.ts && echo NAV_OK</automated>
  </verify>
  <done>Persistent 5-tab skeuo nav with theme toggle, active-tab + external-link a11y, mobile hamburger; nav E2E green.</done>
</task>

<task type="auto">
  <name>Task 2: Home page six sections + CodeCard + AudioEmbed</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Home / — all six sections, §Copywriting, §Color brass-accent rules)
    - .planning/phases/49-flowlang-dev-site/49-CONTEXT.md (D-49-21, D-49-08, D-49-01)
    - CLAUDE.md (## Goals & Non-Goals — value-prop copy source)
    - flow-site/src/lib/docs/shiki.ts (the Plan 49-04 highlighter — 49-03 depends_on 49-04, so shiki.ts already exists; consume `highlightFlow`, do not recreate it)
  </read_first>
  <action>
    Write `flow-site/src/lib/home/examples.ts` exporting the 3 hero "Play in playground" snippets + the single ~20-line code-first snippet (curated Flow that runs on the Web target — no `@sfz`/`@osc`). Write `flow-site/src/lib/home/CodeCard.svelte` rendering a shiki-highlighted Flow snippet (server-side via the Plan 49-04 `shiki.ts` `highlightFlow`) inside a `.surface-paper`/`<Panel>` card with a brass `primary` "Play in playground" `<Button>` whose href is `/playground#code=<encoded>&run=1` (use the Plan 49-06 `encode` if present, else a plain `/playground` link as a graceful pre-49-06 fallback; the `run=1` param is the auto-run signal the playground honors). Write `flow-site/src/lib/home/AudioEmbed.svelte` — an explicit-play `<audio>` wrapper with a play `<Button>` + felt-grille `<LedIndicator>` + "Press play to listen" caption, NO `autoplay` attribute (D-49-01). Write `flow-site/src/routes/+page.svelte` (keep `prerender = true`) composing the six sections (UI-SPEC §Home) inside a single `<main>` landmark: Hero (wordmark + tagline + 3 CodeCards + symphony AudioEmbed), value-prop trio (3 `<Panel framed>` cards from CLAUDE.md Goals), "How it sounds" (AudioEmbeds), code-first annotated snippet, CTAs ("Install" copy `<Button>` + brass "Try in browser" → /playground), and the `<footer>` band. Place curated audio under `static/audio/` (or link release assets if worktree source-audio is absent — charitable, no fabrication).
  </action>
  <acceptance_criteria>
    - `+page.svelte` renders all six sections (Hero, value-prop trio, how-it-sounds, code-first, CTAs, footer) inside one `<main>`.
    - `CodeCard.svelte` deep-links to `/playground#code=` and carries the auto-run signal.
    - `AudioEmbed.svelte` has NO `autoplay` attribute and pairs a play Button with an LedIndicator.
    - `pnpm -C flow-site build` exits 0; Home prerenders to static HTML (no `/wasm/flow-runtime.js` reference in the Home HTML).
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && grep -q "playground#code" src/lib/home/CodeCard.svelte && ! grep -q "autoplay" src/lib/home/AudioEmbed.svelte && grep -qi "footer" src/routes/+page.svelte && pnpm build && ! grep -rq "flow-runtime.js" .svelte-kit/output/prerendered/pages/index.html 2>/dev/null; pnpm build && echo HOME_OK</automated>
  </verify>
  <done>Home renders the six sections with deep-linking code cards + no-autoplay audio embeds; prerendered, no WASM on Home.</done>
</task>

<task type="auto">
  <name>Task 3: Render-strategy E2E (prerender vs SPA) + a11y label sweep</name>
  <read_first>
    - flow-site/tests/render-strategy.spec.ts (Plan 49-01 stub)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (Pattern 1 per-route render strategy)
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Accessibility — SR labels on every interactive element)
  </read_first>
  <action>
    Flesh out `flow-site/tests/render-strategy.spec.ts`: assert Home/Docs/Showcase ship server-rendered content in the initial HTML response (prerendered — content present before hydration) while `/playground` ships a minimal SPA shell (content appears only after client mount). Sweep the Home + nav surface for the a11y contract (REQ-SITE-A11Y-02): every icon-only `<Button>` has an `aria-label`, the GitHub external link announces "opens in new tab" (visually-hidden text), audio embeds have accessible names — fix any gaps found. Add these assertions to `nav.spec.ts`/`render-strategy.spec.ts` (axe coverage of all routes is the Plan 49-08 gate; this is the Home-specific label check). Replace the stub.
  </action>
  <acceptance_criteria>
    - `render-strategy.spec.ts` asserts prerendered content on /, /docs, /showcase and SPA-shell behavior on /playground, no `test.skip`.
    - Every icon button on Home/nav has an `aria-label`; GitHub link has the "opens in new tab" affordance.
    - `pnpm -C flow-site playwright test tests/render-strategy.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && ! grep -q "test.skip" tests/render-strategy.spec.ts && pnpm build && pnpm playwright test tests/render-strategy.spec.ts && echo RENDER_OK</automated>
  </verify>
  <done>Per-route render strategy proven by E2E; Home interactive-element labels verified.</done>
</task>

</tasks>

<threat_model>
No new threat surface — Home + nav are static prerendered marketing content, no user input, no secrets. Audio embeds reference first-party static assets; deep-link hrefs to `/playground#code=...` carry only the site's own curated snippets (the playground does the defensive decode, Plan 49-05/06). No `{@html}` of user content.
</threat_model>

<verification>
- `pnpm -C flow-site build` exits 0; Home + nav prerender.
- `pnpm -C flow-site playwright test tests/nav.spec.ts tests/render-strategy.spec.ts` passes.
- No autoplay; deep-link auto-run signal present.
</verification>

<success_criteria>
- 5-tab persistent nav (D-49-07) + theme toggle + landmarks.
- Home six sections (D-49-21) in the skeuo vocabulary; hero auto-run deep links (D-49-08); no autoplay (D-49-01).
- Render-strategy + a11y-label E2E green.
</success_criteria>

<output>
Create `.planning/phases/49-flowlang-dev-site/49-03-SUMMARY.md` when done.
</output>
