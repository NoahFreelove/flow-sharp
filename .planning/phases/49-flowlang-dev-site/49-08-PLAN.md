---
phase: 49-flowlang-dev-site
plan: 08
type: execute
wave: 5
depends_on: ["49-03", "49-04", "49-05", "49-06", "49-07"]
files_modified:
  - flow-site/tests/a11y.spec.ts
  - flow-site/tests/responsive.spec.ts
  - flow-site/lighthouserc.cjs
  - flow-site/src/
  - .planning/phases/49-flowlang-dev-site/49-HUMAN-UAT.md
autonomous: false
requirements:
  - REQ-SITE-A11Y-01
  - REQ-SITE-A11Y-02
  - REQ-SITE-A11Y-03
  - REQ-SITE-PERF-01
  - REQ-SITE-RESPONSIVE-01

must_haves:
  truths:
    - "axe reports 0 critical violations on /, /docs, /playground"
    - "Lighthouse ≥90 on Performance/Accessibility/Best-Practices/SEO for /, /docs, /playground (mobile + desktop)"
    - "Layout collapses to a single column <768px across all routes, with no horizontal overflow down to the narrowest 320px target"
    - "Audible audio confirmed across Chrome/Firefox/Safari + mobile (human UAT)"
    - "Skeuomorphic look matches the references with no glassmorphism (human review)"
  artifacts:
    - path: "flow-site/tests/a11y.spec.ts"
      provides: "axe-core 0-critical-violations gate on the three routes"
      contains: "axe"
    - path: "flow-site/tests/responsive.spec.ts"
      provides: "single-column <768px collapse assertions across routes, including a 320px no-horizontal-overflow check"
    - path: ".planning/phases/49-flowlang-dev-site/49-HUMAN-UAT.md"
      provides: "cross-browser audible-audio + visual-fidelity sign-off record"
  key_links:
    - from: "flow-site/lighthouserc.cjs"
      to: "/, /docs, /playground ≥90 ×4 axes"
      via: "lhci assertions"
      pattern: "0.9"
---

<objective>
The validation gate: drive Lighthouse ≥90 on all four axes for the three high-traffic routes (mobile + desktop), prove 0 critical axe violations, prove the <768px single-column collapse, and run the cross-browser HUMAN-UAT (audible audio + skeuomorphic visual fidelity). Tune any failing scores. This is the phase quality gate before the closer.

Purpose: D-49-31 Lighthouse ≥90; D-49-10 a11y; D-49-09 responsive. Phase 48 left Chrome/Safari audio unverified (HANDOFF §7) — re-smoke here.
Output: a11y.spec + responsive.spec green; lhci green; 49-HUMAN-UAT.md signed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-flowlang-dev-site/49-VALIDATION.md
@.planning/phases/49-flowlang-dev-site/49-UI-SPEC.md
@.planning/phases/49-flowlang-dev-site/49-RESEARCH.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md

<interfaces>
<!-- Validation targets (D-49-31, D-49-10, D-49-09): -->
  Lighthouse ≥90: Performance / Accessibility / Best-Practices / SEO on /, /docs, /playground — mobile + desktop.
  axe (RESEARCH §A11y, @axe-core/playwright): 0 critical violations on /, /docs, /playground.
  Responsive (<768px): playground stacks controls→editor→console (Monaco read-only); docs sidebar →
    Contents disclosure; nav → hamburger; showcase audio inline.
  Perf lever (RESEARCH Pitfall, D-49-34): WASM lazy-load keeps the runtime off LCP on marketing/docs;
    eager-load the above-fold hero, lazy-load only below-fold textures.

<!-- Manual-only (VALIDATION §Manual-Only): cross-browser AUDIBLE audio (Chrome 120+/Firefox 121+/Safari 17+
     + mobile) per HANDOFF §7; skeuomorphic visual fidelity vs D-49-06 references (no glassmorphism). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: axe a11y gate + responsive collapse E2E (+ fix failures)</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-UI-SPEC.md (§Accessibility Contract — full a11y requirements)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§A11y, @axe-core/playwright)
    - flow-site/tests/a11y.spec.ts + tests/responsive.spec.ts (Plan 49-01 stubs)
  </read_first>
  <action>
    Flesh out `flow-site/tests/a11y.spec.ts` using `@axe-core/playwright`: run axe on `/`, `/docs` (an index + one `[slug]` page), and `/playground` (post-mount), asserting 0 critical/serious violations; cover keyboard-only nav (Tab order matches visual order, no traps, visible focus) and the `aria-current`/landmark/`aria-live` contracts. Flesh out `flow-site/tests/responsive.spec.ts`: at 375px assert each route collapses to a single column — playground stacks controls→editor→console with Monaco read-only + the mobile banner; docs sidebar becomes a Contents disclosure; nav becomes a hamburger; showcase cards single-column. ALSO assert at 320px (the narrowest supported target per ROADMAP acceptance criterion #7) that every route stays single-column with NO horizontal overflow (`document.documentElement.scrollWidth <= clientWidth`). Where axe or the responsive assertions surface real gaps, FIX the underlying component/page (this is the cleanup pass — touch `flow-site/src/` as needed: missing labels, focus order, contrast, missing landmarks). Replace both stubs.
  </action>
  <acceptance_criteria>
    - `a11y.spec.ts` runs axe on /, /docs, /playground and asserts 0 critical violations, no `test.skip`.
    - `responsive.spec.ts` asserts single-column collapse + Monaco read-only on mobile across routes AND a 320px no-horizontal-overflow check on every route, no `test.skip`.
    - `pnpm -C flow-site playwright test tests/a11y.spec.ts tests/responsive.spec.ts` passes (after any fixes).
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && grep -q "axe" tests/a11y.spec.ts && ! grep -q "test.skip" tests/a11y.spec.ts && ! grep -q "test.skip" tests/responsive.spec.ts && pnpm build && pnpm playwright test tests/a11y.spec.ts tests/responsive.spec.ts && echo A11Y_RESP_OK</automated>
  </verify>
  <done>axe 0-critical gate + responsive single-column collapse (down to 320px, no horizontal overflow) green across routes; gaps fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse ≥90 ×4 axes on /, /docs, /playground (+ tune)</name>
  <read_first>
    - flow-site/lighthouserc.cjs (Plan 49-01 thresholds)
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Lighthouse scoring weights, D-49-34 lazy-load, D-49-33 critical CSS, Pitfall "lazy-loading the LCP hero image")
    - .planning/phases/49-flowlang-dev-site/49-CONTEXT.md (D-49-31, D-49-32 image opt, D-49-33 critical CSS)
  </read_first>
  <action>
    Run `pnpm -C flow-site lhci autorun` against a production preview of /, /docs, /playground (mobile + desktop). For any axis below 90: tune per the established levers — confirm the WASM runtime is NOT fetched on / or /docs (D-49-34 lazy-load keeps it off LCP), eager-load the above-fold hero image while lazy-loading below-fold textures (RESEARCH Pitfall), confirm critical CSS is inlined (D-49-33), confirm AVIF/WebP texture serving (D-49-32), add any missing SEO meta (title/description/canonical/lang) and Best-Practices items (HTTPS-only resources, no console errors). /playground will be the hardest on Performance (WASM weight) — the D-49-34 lazy-load (runtime fetched only in `onMount`, off the LCP path) is the planned mitigation; document the measured score and confirm it clears 90. Do NOT add an acceptance carve-out — ≥90 is unconditional per AC-6 + D-49-31 (the pre-execution risk is recorded in `## Risks` below). Ensure `lighthouserc.cjs` asserts the four-axis ≥0.9 thresholds across all three routes.
  </action>
  <acceptance_criteria>
    - `pnpm -C flow-site lhci autorun` reports ≥0.90 on Performance / Accessibility / Best-Practices / SEO for /, /docs, /playground (mobile + desktop) — unconditional, no carve-out (ROADMAP AC-6, locked D-49-31).
    - The runtime WASM is confirmed NOT requested on / or /docs.
  </acceptance_criteria>
  <verify>
    <automated>cd flow-site && pnpm build && pnpm lhci autorun && echo LHCI_OK</automated>
  </verify>
  <done>Lighthouse ≥90 ×4 axes on the three routes (mobile + desktop), unconditional — D-49-34 lazy-load is the planned mitigation; the /playground-Perf risk is tracked in ## Risks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full automated suite (vitest + playwright + lhci + axe) is green. The two things automation cannot confirm — AUDIBLE audio across browsers (headless only checks AudioContext state; Phase 48 left Chrome/Safari unverified per HANDOFF §7) and the SKEUOMORPHIC visual fidelity (a subjective "Logic Pro / Reason / vintage-synth, not glassmorphism" target) — need the composer.</what-built>
  <how-to-verify>
    1. Open the deployed `/playground` in Chrome 120+, Firefox 121+, Safari 17+, and mobile (Safari + Chrome). Run the default 440 Hz tone via the Run button. Confirm AUDIBLE sound in each (the D-48-09 gesture chain should satisfy even Safari's strict autoplay policy — confirm).
    2. Run a `writeMidi` snippet; confirm the MIDI download works.
    3. Open a shared `#code=` link in a private window (no login) and confirm it loads + runs.
    4. Walk /, /docs, /playground, /showcase and confirm the skeuomorphic look matches the D-49-06 references (wood/brushed-metal/paper/felt, embossed buttons, brass accents used sparingly) with NO glassmorphism / AI-template feel. Toggle dark mode; confirm the amber-LED vintage-gear aesthetic.
    5. Smoke a screen reader (VoiceOver or NVDA) over the nav + playground controls; confirm labels + LED status announce.
    6. Record each browser row PASS/DEFER/SKIP in 49-HUMAN-UAT.md (mirroring the Phase 48 UAT format).
  </how-to-verify>
  <resume-signal>Type "approved" with the per-browser audio results + visual sign-off, or list the blockers.</resume-signal>
</task>

</tasks>

<threat_model>
No new threat surface — this plan adds tests and tunes performance/a11y; it introduces no new inputs, secrets, or network endpoints. The axe gate + the Lighthouse Best-Practices axis (no mixed content, CSP present, no console errors) are themselves defense-in-depth confirmations of the Plan 49-01/05/06 threat mitigations.
</threat_model>

## Risks
- **/playground Lighthouse Performance vs WASM weight (pre-execution risk).** /playground ships the Phase 48 WASM runtime, which threatens the Performance axis. Mitigation is PLANNED, not optional: the D-49-34 lazy-load fetches the runtime only inside `onMount` (off the LCP path) so /, /docs, and the above-fold /playground shell never block on it; D-49-33 critical-CSS inlining + D-49-32 AVIF/WebP textures further protect LCP. The ≥90 ×4-axis bar (ROADMAP AC-6, locked D-49-31) is UNCONDITIONAL — there is no acceptance carve-out. If, after applying every planned lever, /playground Performance still measures below 90, that is a genuine blocker to surface to the composer for a scope/decision call (e.g. revisiting the v1.6 lazy-load split) — it is NOT a self-sanctioned exception to the gate.

<verification>
- `pnpm -C flow-site vitest run && pnpm -C flow-site playwright test && pnpm -C flow-site lhci autorun` all green.
- axe 0 critical on /, /docs, /playground; responsive single-column; Lighthouse ≥90 ×4.
- 49-HUMAN-UAT.md signed (cross-browser audio + visual fidelity).
</verification>

<success_criteria>
- Full automated suite green; Lighthouse ≥90 ×4 axes (D-49-31); axe 0-critical (D-49-10); responsive collapse (D-49-09).
- Cross-browser audible audio + skeuomorphic fidelity human-confirmed.
</success_criteria>

<output>
Create `.planning/phases/49-flowlang-dev-site/49-08-SUMMARY.md` + `49-HUMAN-UAT.md` when done.
</output>
