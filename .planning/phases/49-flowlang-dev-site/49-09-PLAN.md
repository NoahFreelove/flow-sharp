---
phase: 49-flowlang-dev-site
plan: 09
type: execute
wave: 5
depends_on: ["49-08"]
files_modified:
  - .planning/phases/49-flowlang-dev-site/49-VERIFICATION.md
  - .planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - CLAUDE.md
  - flow-site/README.md
autonomous: true
requirements:
  - REQ-SITE-IA-01
  - REQ-SITE-DEPLOY-01

must_haves:
  truths:
    - "49-VERIFICATION.md records per-REQ closure evidence for every REQ-SITE-*"
    - "A composer deployment runbook documents CF Pages + OAuth App + env-var setup end to end"
    - "ROADMAP/STATE/REQUIREMENTS/MILESTONES/CLAUDE all flip Phase 49 to its final state"
    - "Every REQ-SITE-* checkbox is reconciled (closed or explicitly deferred with reason)"
  artifacts:
    - path: ".planning/phases/49-flowlang-dev-site/49-VERIFICATION.md"
      provides: "per-REQ closure + D-49-NN decision trace + caveats"
    - path: ".planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md"
      provides: "CF Pages dashboard tour + env vars + OAuth App + custom-domain CNAME"
    - path: "flow-site/README.md"
      provides: "build/dev/test/deploy quickstart for the new flow-site project"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "Phase 49 row + plan list"
      via: "status flip + plan checkboxes"
      pattern: "Phase 49"
---

<objective>
Close Phase 49: write the per-REQ VERIFICATION, the composer-facing deployment runbook, a flow-site README, and sweep the tracking artifacts (ROADMAP / STATE / REQUIREMENTS / MILESTONES / CLAUDE.md) to Phase 49's final state. This is the v1.5 reach-track closer.

Purpose: Make the work auditable and the site re-deployable by the composer; flip the milestone bookkeeping.
Output: 49-VERIFICATION.md + 49-DEPLOYMENT-RUNBOOK.md + flow-site/README.md + tracking flips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/phases/49-flowlang-dev-site/49-CONTEXT.md
@.planning/phases/49-flowlang-dev-site/49-VALIDATION.md
@.planning/phases/49-flowlang-dev-site/49-08-PLAN.md

<interfaces>
<!-- Closer mirrors the Phase 48 closer shape (Plan 48-07): VERIFICATION + HANDOFF-style docs + a 5-file
     tracking sweep. The flow-site/ project is greenfield TS — CLAUDE.md gets a new section noting the
     C# conventions do NOT apply inside flow-site/ (greenfield web conventions do), plus build/deploy commands. -->
All 27 REQ-SITE-* IDs (from REQUIREMENTS.md "### flowlang.dev Site (Phase 49)") must be reconciled.
The 38 D-49-NN locked decisions must each trace to a plan/artifact in the VERIFICATION decision trace.
Phase 49 row in ROADMAP "### Phase 49: flowlang.dev Site" + the Progress table row + the per-plan checklist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 49-VERIFICATION.md (per-REQ closure + D-49-NN trace + caveats) + flow-site README</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-VALIDATION.md (the per-REQ test map to cite as evidence)
    - .planning/REQUIREMENTS.md (the 27 REQ-SITE-* IDs)
    - .planning/phases/49-flowlang-dev-site/49-CONTEXT.md (the 38 D-49-NN decisions to trace)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md (shape reference)
  </read_first>
  <action>
    Write `.planning/phases/49-flowlang-dev-site/49-VERIFICATION.md`: a per-REQ closure table (each REQ-SITE-* → the test/command/artifact that proves it, citing the VALIDATION map), a D-49-01..38 decision trace (each decision → where it landed; note the three RESEARCH corrections honored — grammar path, no real wiki [[links]], `_headers` at project root — and the discretionary calls: pnpm, self-hosted Monaco, Monarch tokenizer, COOP/COEP scoping), and a Known Caveats section (e.g. /playground Performance ceiling if any; absent symphony/ragtime source linked out; Chrome/Safari audio per the HUMAN-UAT outcome; gist live-write is manual). Write `flow-site/README.md`: project intro + the build/dev/test/deploy commands (`pnpm -C flow-site dev`, `build`, `vitest run`, `playwright test`, `lhci autorun`, `scripts/sync-wiki.sh`, `scripts/sync-runtime.sh`) + the "this is greenfield TS — C# conventions don't apply here" note.
  </action>
  <acceptance_criteria>
    - `49-VERIFICATION.md` has a row for every REQ-SITE-* ID and a trace entry for every D-49-01..38 decision.
    - The three RESEARCH corrections + the discretionary calls are documented.
    - `flow-site/README.md` lists the dev/build/test/deploy commands.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && test -f .planning/phases/49-flowlang-dev-site/49-VERIFICATION.md && reqs=$(grep -oE "REQ-SITE-[A-Z]+-[0-9]+" .planning/REQUIREMENTS.md | sort -u | wc -l) && hit=$(grep -oE "REQ-SITE-[A-Z]+-[0-9]+" .planning/phases/49-flowlang-dev-site/49-VERIFICATION.md | sort -u | wc -l) && [ "$hit" -ge "$reqs" ] && grep -q "D-49-38" .planning/phases/49-flowlang-dev-site/49-VERIFICATION.md && grep -qi "sync-runtime" flow-site/README.md && echo VERIFICATION_OK</automated>
  </verify>
  <done>VERIFICATION closes every REQ + traces every D-49 decision; flow-site README documents the workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Deployment runbook (CF Pages + OAuth App + env vars + custom domain)</name>
  <read_first>
    - .planning/phases/49-flowlang-dev-site/49-RESEARCH.md (§Runtime State Inventory — CF Pages project + GitHub OAuth App manual setup; Environment Availability table)
    - .planning/phases/49-flowlang-dev-site/49-CONTEXT.md (D-49-36 project name, D-49-37 custom domain, D-49-38 headers, D-49-28 OAuth env)
    - .planning/phases/49-flowlang-dev-site/49-01-SUMMARY.md + 49-06-SUMMARY.md (the live URL + OAuth app the composer set up)
  </read_first>
  <action>
    Write `.planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md` — a step-by-step composer guide: (1) CF Pages project creation (name per D-49-36, build command `pnpm -C flow-site build`, output `flow-site/.svelte-kit/cloudflare`, git integration), (2) env vars in the CF dashboard (`WIKI_REPO_URL`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` as a secret), (3) GitHub OAuth App registration (callback `https://<project>.pages.dev/api/auth/github`, scope `gist`), (4) the committed-AppBundle deploy model (pure-Node CF build, regenerate via `scripts/sync-runtime.sh` on the dev machine + commit), (5) the `_headers` model (CSP/Permissions-Policy always; COOP/COEP scoped or deferred), (6) custom-domain CNAME steps for the deferred post-v1.5 domain (D-49-37), (7) the wiki re-sync model (push to flow-sharp main triggers rebuild; v1.6 webhook). Reference the actual `<project>.pages.dev` URL from the Plan 49-01 checkpoint.
  </action>
  <acceptance_criteria>
    - `49-DEPLOYMENT-RUNBOOK.md` covers CF Pages creation, env vars, OAuth App, the AppBundle commit model, `_headers`, custom-domain CNAME, and wiki re-sync.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && test -f .planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md && grep -qi "GITHUB_CLIENT_SECRET" .planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md && grep -qi "pages.dev" .planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md && grep -qi "sync-runtime" .planning/phases/49-flowlang-dev-site/49-DEPLOYMENT-RUNBOOK.md && echo RUNBOOK_OK</automated>
  </verify>
  <done>A complete composer deployment runbook for the site + OAuth + env vars + domain.</done>
</task>

<task type="auto">
  <name>Task 3: Tracking sweep — ROADMAP / STATE / REQUIREMENTS / MILESTONES / CLAUDE.md</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 49 section + Progress table row 355)
    - .planning/STATE.md (frontmatter + Current Position + phase map)
    - .planning/REQUIREMENTS.md (the REQ-SITE-* block — flip checkboxes for closed REQs)
    - .planning/MILESTONES.md (v1.5 progress + v1.6 backlog)
    - CLAUDE.md (add the flow-site/ section)
  </read_first>
  <action>
    Update ROADMAP.md: flip the Phase 49 row + Progress-table row to its final status (shipped / shipped-with-followup per the HUMAN-UAT outcome), set the plan count to 9, and list the 9 plans with their objectives. Update STATE.md frontmatter (`stopped_at`, `last_activity`, progress counters) + Current Position + the v1.5 phase map row for Phase 49 + add a "Phase 49 highlights" block (mirroring the Phase 48 highlights style). In REQUIREMENTS.md flip the closed REQ-SITE-* checkboxes to `[x]` with the proving artifact/commit, leaving any genuinely-deferred item `[ ]` with a v1.6 note (e.g. live-gist-write confirmed manually; any /playground-Perf exception). Update MILESTONES.md v1.5 progress (Phase 49 added) + log any v1.6 backlog items (custom domain, Monaco full-LSP, COOP/COEP if deferred, AnalyserNode waveform, etc.). Add a `## flowlang.dev Site` section to CLAUDE.md documenting the new `flow-site/` top-level (greenfield SvelteKit/TS — C# conventions do NOT apply inside it), the build/deploy commands, the committed-AppBundle model, and the playground's frozen-runtime consumption (pointer to the HANDOFF). Use the Write/Edit-equivalent (full-file rewrite where needed) — do not heredoc.
  </action>
  <acceptance_criteria>
    - ROADMAP Phase 49 row + Progress row updated to final status with 9 plans listed.
    - STATE.md frontmatter + Current Position + phase map reflect Phase 49 closure.
    - REQUIREMENTS.md REQ-SITE-* checkboxes reconciled (closed `[x]` or deferred `[ ]` + reason).
    - MILESTONES.md v1.5 progress updated.
    - CLAUDE.md has a `## flowlang.dev Site` (or similarly named) section for flow-site/.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -qiE "Phase 49.*(SHIPPED|shipped)" .planning/STATE.md && grep -qiE "flow-site|flowlang.dev" CLAUDE.md && grep -q "REQ-SITE-IA-01" .planning/REQUIREMENTS.md && grep -qiE "Phase 49" .planning/MILESTONES.md && echo SWEEP_OK</automated>
  </verify>
  <done>All five tracking artifacts flipped to Phase 49's final state; REQ-SITE-* reconciled; CLAUDE.md documents flow-site/.</done>
</task>

</tasks>

<threat_model>
No new threat surface — this plan writes documentation + flips planning/tracking artifacts. No code, no inputs, no secrets, no network. The deployment runbook explicitly documents the secret-handling model (GITHUB_CLIENT_SECRET as a CF secret, never committed) reinforcing the Plan 49-06 mitigations.
</threat_model>

<verification>
- 49-VERIFICATION.md closes every REQ-SITE-* + traces every D-49 decision.
- 49-DEPLOYMENT-RUNBOOK.md is complete.
- ROADMAP / STATE / REQUIREMENTS / MILESTONES / CLAUDE.md all reflect Phase 49 closure.
</verification>

<success_criteria>
- Auditable per-REQ closure + decision trace + caveats.
- Composer deployment runbook.
- flow-site README + CLAUDE.md flow-site section.
- Full tracking sweep to Phase 49 final state.
</success_criteria>

<output>
Create `.planning/phases/49-flowlang-dev-site/49-09-SUMMARY.md` when done.
</output>
