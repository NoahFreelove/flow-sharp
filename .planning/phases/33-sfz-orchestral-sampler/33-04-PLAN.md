---
phase: 33
plan: 04
type: execute
wave: 2
depends_on:
  - 33-01
  - 33-02
files_modified:
  - flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs
  - flow-lang.Tests/Unit/Phase33/SfzParserTests.cs
autonomous: true
requirements: [SPEC-3, SPEC-4, SPEC-5]
must_haves:
  truths:
    - "SfzParser.Parse(content, filePath, patchDescription) returns an SfzData containing all parsed regions plus the 128×128 grid plus the SortedByPitch[] side index (the v1.4 signature drops `diagnosticsSink` — RenderingDiagnostics.WarnOnce uses the global per-process sentinel set, so the parameter has no consumer in v1.4)"
    - "All 13 whitelist opcodes (sample, lokey, hikey, pitch_keycenter, lovel, hivel, loop_mode, loop_start, loop_end, ampeg_attack, ampeg_release, volume, pan) parse with `StringComparer.Ordinal` (rejects unicode tricks per T-33-OPCODE-01)"
    - "Unknown opcodes silently ignored + RenderingDiagnostics.WarnOnce keyed on `sfz:opcode:{patchDescription}:{opcodeName}` (charitable interpretation per CLAUDE.md memory)"
    - "Header inheritance applied AT PARSE TIME: <global> opcodes cascade into every <group>, <group> opcodes cascade into every <region> (runtime never traverses headers per CONTEXT Claude's Discretion)"
    - "MaxRegionCount = 10000 DoS cap mirroring Phase 32 ScalaParser.MaxStepCount = 10000 (T-33-PARSE-01)"
    - "Strict numeric: NumberStyles.Float & ~AllowExponent & ~AllowThousands + CultureInfo.InvariantCulture rejects `1.5e2`, `100,5` (T-33-NUM-01 + Phase 32 D-18 precedent)"
    - "Volume opcode dB→linear conversion (`Math.Pow(10.0, dB / 20.0)`) applied at parse time per Pitfall 8 (SfzRegion.Volume stored as linear)"
    - "Pan opcode ÷ 100.0 conversion applied at parse time per Pitfall 7 (SfzRegion.Pan stored in Flow's [-1.0, +1.0] range)"
    - "Last-declared-wins grid build per D-02: build loop iterates regions in declaration order, writes `grid[k, v] = region` for every (k ∈ [lokey..hikey], v ∈ [lovel..hivel]) cell — later writes overwrite earlier ones structurally"
    - "D-04: Region grid storage is `SfzRegion?[128, 128]` — 131 KB per patch plus ~13 KB region records. For a 6-patch symphony total memory is under 1 MB, roughly 1% of the audio sample data the same patches load."
    - "D-05: `loadSfz` performs parsing + region-grid build only — zero `.wav` files hit disk during `loadSfz`. The returned `Sfz` value carries region metadata + sample-file-path strings; sample IO is deferred to the SfzSampleCache eager-load in plan 33-06."
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs"
      provides: "Hand-rolled INI-style SFZ parser (13-opcode whitelist + 3 headers + inheritance flattening)"
      contains: "MaxRegionCount = 10000"
      contains_pattern: "KnownOpcodes"
    - path: "flow-lang.Tests/Unit/Phase33/SfzParserTests.cs"
      provides: "Parser fact suite (10+ facts covering 13-opcode whitelist, advisory dedup, header inheritance, strict numeric, MaxRegionCount cap, grid last-declared-wins, dB-to-linear conversion, pan normalization)"
      contains: "AllKnownOpcodes_Parse"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs"
      to: "flow-lang/Diagnostics/RenderingDiagnostics.cs"
      via: "RenderingDiagnostics.WarnOnce calls for unknown opcodes"
      pattern: "WarnOnce"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      via: "Parse returns SfzData"
      pattern: "return new SfzData"
    - from: "flow-lang.Tests/Unit/Phase33/SfzParserTests.cs"
      to: "flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz"
      via: "LoadFixture helper reads the Plan 33-01 fixture for one end-to-end parse fact"
      pattern: "smoke.sfz"
---

<objective>
Wave 2: ship the hand-rolled SFZ parser (the largest single code surface in Phase 33, ~250 LOC). Single-pass line walker, header inheritance flattened at parse time, 13-opcode whitelist with one-shot advisories for everything else, strict numeric (mirrors Phase 32 ScalaParser), MaxRegionCount cap, grid built by D-02 last-declared-wins write-order.

Purpose: SPEC-3 (opcode whitelist + advisory dedup + strict numeric), SPEC-4 (region grid is the lookup index at render time), SPEC-5 (loop_mode + loop_start + loop_end are the gates for the crossfade math) — three SPECs satisfied here. The parser is the longest single new file in the phase; isolating it in a dedicated plan keeps the context cost under 35%.

Output: 1 new parser file + 1 fact suite (~10 facts). Test files in flow-lang.Tests/Unit/Phase33/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-sfz-orchestral-sampler/33-SPEC.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md
@.planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md
@.planning/phases/33-sfz-orchestral-sampler/33-VALIDATION.md
@.planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md
@flow-lang/StandardLibrary/Audio/Tuning/ScalaParser.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzParseException.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs
@flow-lang.Tests/Unit/Phase32/ScalaParserFacts.cs
@flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz

<interfaces>
<!-- SfzParser public surface (the contract):
public static class SfzParser
{
    public const int MaxRegionCount = 10000;

    public static SfzData Parse(
        string content,
        string filePath,
        string patchDescription);
}
Note (v1.4): `diagnosticsSink` was dropped — RenderingDiagnostics.WarnOnce
uses the global per-process sentinel set, so the parameter had no consumer.
`patchDescription` stays — it is the dedup key for advisory sentinels.
-->

<!-- KnownOpcodes whitelist (13 entries, StringComparer.Ordinal, MUST be exact):
"sample", "lokey", "hikey", "pitch_keycenter", "lovel", "hivel",
"loop_mode", "loop_start", "loop_end",
"ampeg_attack", "ampeg_release", "volume", "pan"
-->

<!-- SfzLoopMode mapping (strict — unknown string falls back to NoLoop + advisory):
"no_loop"          → SfzLoopMode.NoLoop
"one_shot"         → SfzLoopMode.OneShot
"loop_continuous"  → SfzLoopMode.LoopContinuous
"loop_sustain"     → SfzLoopMode.LoopSustain
-->

<!-- SFZ opcode defaults from sfzformat.com (locked in 33-RESEARCH § Pattern 2):
lokey=0, hikey=127, pitch_keycenter=60, lovel=1, hivel=127
loop_mode="no_loop" (or "loop_continuous" if loop_start/end > 0)
loop_start=0, loop_end=0
ampeg_attack=0, ampeg_release=0.001
volume=0 dB (stored as linear 1.0)
pan=0 (stored as Flow 0.0)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SfzParser implementation</name>
  <files>flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs</files>
  <behavior>
    - Parse a 2-region smoke.sfz: returns SfzData with Regions.Count == 2, Description non-null, BasePath equal to Path.GetDirectoryName(filePath)
    - All 13 opcodes on a single <region> populate the corresponding SfzRegion fields with parsed values (NOT defaults)
    - Unknown opcode on a region (e.g. `fil_type=lpf_2p`) is silently ignored + one-shot advisory fired with sentinel `sfz:opcode:{patchDescription}:fil_type`
    - Re-loading the same patch (same patchDescription) does NOT fire a second advisory for the same opcode (dedup contract)
    - <global> + <group> + <region> headers cascade: a `<global>` opcode propagates into ALL regions of ALL groups; a `<group>` opcode propagates into all regions inside that group; region-specific opcodes override inherited values
    - Strict numeric: `volume=1.5e2` throws SfzParseException OR fires advisory (parser choice — but result MUST NOT be 150 as a parsed value)
    - Strict numeric: `pan=100,5` throws SfzParseException OR fires advisory (same)
    - DoS cap: synthetic content with 10001 `<region>` headers throws SfzParseException citing "region count <= 10000"
    - Grid lookup: a region declaring lokey=48 hikey=71 with pitch_keycenter=60 sets grid[48..71, 1..127] = region; outside that range grid cell is null
    - Last-declared-wins: two regions both covering midi 60, vel 64 → grid[60, 64] returns the SECOND-declared region (declaration order = write order)
    - SortedByPitch is the ascending unique pitch list with any region coverage; for the smoke fixture (48..71 + 72..127), values 48..127 appear in ascending order
    - Volume opcode conversion: `volume=0` (dB) → SfzRegion.Volume = 1.0 (linear); `volume=-6` → SfzRegion.Volume ≈ 0.501; `volume=-12` → SfzRegion.Volume ≈ 0.251
    - Pan opcode conversion: `pan=0` → SfzRegion.Pan = 0.0; `pan=100` → SfzRegion.Pan = 1.0; `pan=-100` → SfzRegion.Pan = -1.0
    - Adjacent opcodes on the header line: `<region> sample=foo.wav lokey=60 hikey=72` parses ALL three opcodes (NOT just the first); per Pitfall 11
    - Comment stripping: `//` line comments removed before tokenization; an `opcode=value // commentary` line parses only the opcode and value
    - loop_mode unknown value (e.g. `loop_mode=bogus`) → SfzLoopMode.NoLoop + advisory keyed on `sfz:opcode_value:{patchDescription}:loop_mode:bogus`
  </behavior>
  <read_first>
    - .planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md (REQUIRED FIRST — answers Q3; if its decision is FOUND, extend the opcode whitelist from 13 to 14 entries by adding `default_path` AND parse `<control>` as a fourth header that cascades `default_path=` into every region's `sample=` lookup. If NOT FOUND or NETWORK_FAIL, ship the 13-opcode whitelist as currently specified.)
    - flow-lang/StandardLibrary/Audio/Tuning/ScalaParser.cs (read in full — it is the exact pattern to mirror per 33-PATTERNS.md, including: single-pass line walker, NumberStyles mask + InvariantCulture, MaxStepCount cap shape, StripCr helper, ScalaParseException construction site at line 103)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs (verify the SfzData ctor signature shipped by merged Plan 33-02 Task 1)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs (verify the 13-field record shipped by merged Plan 33-02 Task 1 — field order matters for the positional constructor)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs (verify the 4 enum values shipped by merged Plan 33-02 Task 1)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzParseException.cs (verify the 5-arg constructor shipped by merged Plan 33-02 Task 1)
    - flow-lang/Diagnostics/RenderingDiagnostics.cs (read WarnOnce signature at line 29 + ResetForTesting at line 47)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Pattern 2: SFZ Parser" + §"Common Pitfalls" 7, 8, 9, 11 + §"Security Domain" T-33-PARSE-01, T-33-NUM-01, T-33-OPCODE-01)
    - .planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md (§"flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs" — full divergence list)
  </read_first>
  <action>Create `flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs` (namespace `FlowLang.StandardLibrary.Audio.Sfz`). Public static class. Public constant `MaxRegionCount = 10000`. Private static readonly `KnownOpcodes` HashSet&lt;string&gt; with StringComparer.Ordinal containing the 13 whitelist opcodes (exact strings per the `<interfaces>` block). Private static readonly `FloatStyle = NumberStyles.Float & ~NumberStyles.AllowExponent & ~NumberStyles.AllowThousands` (mirrors Phase 32 ScalaParser.cs:54). Public static `Parse(string content, string filePath, string patchDescription) -> SfzData` method (the v1.4 signature drops the `diagnosticsSink` parameter — RenderingDiagnostics.WarnOnce uses the global per-process sentinel set so the parameter had no consumer). Algorithm: (1) Split content by '\n'; (2) Strip trailing '\r' using a StripCr helper copied from ScalaParser.cs:300; (3) Per line: trim, strip everything after `//` (Pitfall: handle the case where `//` appears inside a quoted sample path — for the 13-opcode subset, paths are bareword so this is safe; document the simplification), skip empty lines; (4) State machine: track three accumulators — `currentGlobal: Dictionary<string,string>`, `currentGroup: Dictionary<string,string>`, plus a `regions: List<SfzRegion>` output; (5) On `<global>` token: clear currentGlobal + currentGroup; subsequent opcodes until the next header land in currentGlobal; (6) On `<group>` token: clear currentGroup; subsequent opcodes land in currentGroup; (7) On `<region>` token: flatten — start with a fresh `regionOpcodes` dict, copy currentGlobal entries in, then copy currentGroup entries in (overwriting); subsequent opcodes on the same line AND on following lines until the next header land in regionOpcodes (overwriting); when the next header or EOF is hit, build the SfzRegion from regionOpcodes by reading each of the 13 known opcodes (default if absent per the spec defaults list in `<interfaces>`); append to `regions`; (8) DoS cap: when `regions.Count > MaxRegionCount` throw `new SfzParseException(filePath, lineNumber, 1, "region count <= 10000", regions.Count.ToString())`; (9) Multiple opcodes per line: AFTER recognizing a header (which is a token starting with `<` and ending with `>`), continue scanning whitespace-separated remainder for `key=value` pairs and apply them to whichever accumulator the header just selected — per Pitfall 11; (10) Strict numeric using `int.TryParse(token, NumberStyles.None, CultureInfo.InvariantCulture, out var iv)` for int opcodes (lokey/hikey/lovel/hivel/loop_start/loop_end/pitch_keycenter) and `double.TryParse(token, FloatStyle, CultureInfo.InvariantCulture, out var dv)` for double opcodes (ampeg_attack/ampeg_release/volume/pan); on TryParse failure call `RenderingDiagnostics.WarnOnce($"sfz:opcode_value:{patchDescription}:{opcodeName}:{token}", $"[sfz] invalid value for {opcodeName} in '{patchDescription}' — '{token}' rejected, using default")` and use the spec default; (11) Unknown opcode (not in KnownOpcodes): `RenderingDiagnostics.WarnOnce($"sfz:opcode:{patchDescription}:{opcodeName}", $"[sfz] unrecognized opcode '{opcodeName}' in '{patchDescription}' — ignoring");` and continue; (12) loop_mode value mapping: switch on the value string with StringComparer.Ordinal (case-sensitive — SFZ spec is lowercase); unknown value → NoLoop + advisory keyed on `sfz:opcode_value:{patchDescription}:loop_mode:{value}`; (13) Volume conversion at read time: `linearVolume = Math.Pow(10.0, dbValue / 20.0)` BEFORE storing on SfzRegion (Pitfall 8); default if absent is `0 dB` → linear `1.0`; (14) Pan conversion at read time: `flowPan = sfzPan / 100.0` BEFORE storing on SfzRegion (Pitfall 7); default if absent is 0; (15) After region list assembled, build the grid: allocate `var grid = new SfzRegion?[128, 128]`; iterate `regions` in declaration order (D-02): for each region, loop `for (int k = region.LoKey; k <= region.HiKey; k++) for (int v = region.LoVel; v <= region.HiVel; v++) grid[k, v] = region;` — last write wins structurally; (16) Build SortedByPitch: collect a HashSet<int> of all `k` values where any grid[k, v] is non-null, sort ascending, materialize as `int[]`; (17) Resolve `description`: the first non-comment non-blank line if present and not a header, else `Path.GetFileName(filePath)`; resolve `basePath = Path.GetDirectoryName(filePath) ?? ""`; (18) Return `new SfzData(description, basePath, regions, grid, sortedByPitch)`. Document the global-WarnOnce contract with an XML-doc comment on the Parse method.</action>
  <verify>
    <automated>dotnet build flow-sharp.sln --logger "console;verbosity=minimal" &amp;&amp; grep -q "MaxRegionCount = 10000" flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs &amp;&amp; grep -q "KnownOpcodes" flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs &amp;&amp; grep -q "NumberStyles.Float" flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs &amp;&amp; grep -q "CultureInfo.InvariantCulture" flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs</automated>
  </verify>
  <done>SfzParser.cs compiles; MaxRegionCount + KnownOpcodes + NumberStyles.Float constants present; strict-numeric posture verified by grep gate; behavior facts in Task 2 prove the parser body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SfzParserTests fact suite</name>
  <files>flow-lang.Tests/Unit/Phase33/SfzParserTests.cs</files>
  <behavior>
    - AllKnownOpcodes_Parse: a synthetic SFZ string with all 13 opcodes populates SfzRegion correctly
    - UnknownOpcode_AdvisoryOnce: feed a synthetic SFZ with 5 unknown opcodes; assert all 13 known opcodes parsed; assert WarnOnce sentinel set has 5 entries; assert a second parse with same patchDescription does NOT add to the sentinel set
    - HeaderInheritance: a <global> opcode propagates into all regions; a <group> opcode overrides global for its regions; a <region> opcode overrides both
    - StrictNumeric_RejectsExponent: `volume=1.5e2` is rejected (advisory + spec default 0 dB → linear 1.0)
    - StrictNumeric_RejectsThousands: `lokey=1,500` is rejected
    - MaxRegionCount_Caps: synthetic content with 10001 `<region>` headers throws SfzParseException
    - GridBuild_LastDeclaredWins: two regions both covering (60, 64) → grid[60, 64] is the second-declared region
    - SortedByPitch_AscendingUnique: smoke fixture grid coverage produces a SortedByPitch array starting with 48 ending with 127
    - VolumeOpcode_DbToLinear: `volume=0` → SfzRegion.Volume == 1.0; `volume=-6` → ≈ 0.501; `volume=-12` → ≈ 0.251
    - PanOpcode_NormalizedToFlowRange: `pan=100` → 1.0; `pan=-100` → -1.0; `pan=50` → 0.5
    - SmokeFixture_ParsesCleanly: load `flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz`, assert Regions.Count == 2, grid[60, 64] non-null pointing at C4_sine.wav region, grid[79, 64] non-null pointing at G5_sine.wav region
    - MultipleOpcodesOnHeaderLine: `<region> sample=foo.wav lokey=60 hikey=72` parses all three opcodes
    - LoopMode_UnknownValue_FallsBackToNoLoop: `loop_mode=bogus` → SfzLoopMode.NoLoop + advisory
  </behavior>
  <read_first>
    - flow-lang.Tests/Unit/Phase32/ScalaParserFacts.cs (entire file — copy FindRepoRoot/LoadFixture helpers, [Collection("FlowScripts")] attribute usage, RenderingDiagnostics.ResetForTesting in ctor + Dispose per 33-PATTERNS.md)
    - flow-lang/Diagnostics/RenderingDiagnostics.cs (verify ResetForTesting clears the internal HashSet — Task 1 of this plan depends on this for the dedup test)
    - flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz (read content — facts assert on Regions.Count == 2 and specific grid cells)
  </read_first>
  <action>Create `flow-lang.Tests/Unit/Phase33/SfzParserTests.cs` (namespace `FlowLang.Tests.Unit.Phase33`). `[Collection("FlowScripts")] public class SfzParserTests : IDisposable`. Constructor calls `RenderingDiagnostics.ResetForTesting();` Dispose method calls the same. Implement the 13 facts listed in `<behavior>`. For each fact: construct an inline string for the synthetic SFZ content (no fixture files needed except SmokeFixture_ParsesCleanly which uses FindRepoRoot to locate `flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz` via the helper lifted from ScalaParserFacts.cs). Use `Assert.Equal`, `Assert.NotNull`, `Assert.True` with descriptive messages. For the WarnOnce dedup test, after the parse calls assert by re-parsing the same content with the same patchDescription and verifying the sentinel HashSet count is unchanged from the first run. For the MaxRegionCount test, generate the SFZ content programmatically (`var sb = new StringBuilder(); for (int i = 0; i &lt;= 10000; i++) sb.AppendLine("&lt;region&gt; sample=x.wav");`) and assert `Assert.Throws&lt;SfzParseException&gt;(...)`. For strict-numeric tests, parse the content, then verify the affected SfzRegion field equals the spec default (not the malformed value). For dB conversion tests, use `Assert.Equal(expected, actual, precision: 3)` for double comparisons.</action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzParserTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>13 SfzParserTests facts green. Full suite still passes. Two-run determinism preserved (RenderingDiagnostics.ResetForTesting in ctor/dispose isolates state per Pitfall 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer .sfz file → SfzParser.Parse | Untrusted file content (size, region count, numeric formats, opcode names) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-PARSE-01 | DoS | unbounded region count | mitigate | `MaxRegionCount = 10000` constant + early-throw SfzParseException (mirrors Phase 32 MaxStepCount per 33-RESEARCH §"Security Domain") |
| T-33-NUM-01 | Tampering | malformed numerics (`1.5e2`, `100,5`) | mitigate | `NumberStyles.Float & ~AllowExponent & ~AllowThousands` + `CultureInfo.InvariantCulture` (Phase 32 D-18 precedent); failed parses log advisory + fall back to spec default |
| T-33-OPCODE-01 | Tampering | unicode opcode names attempting to slip past whitelist | mitigate | `KnownOpcodes` HashSet&lt;string&gt; with `StringComparer.Ordinal` rejects case mismatch and unicode tricks |
| T-33-LOOP-01 | DoS | loop_end &gt; sample length | accept (deferred) | Parser stores the raw value; render-time clamp happens in Plan 33-06 (the sample isn't loaded yet at parse time per Pitfall 3) |
| T-33-PATH-01 | InfoDisclosure | absolute sample= paths | accept | Composer-controlled .sfz; loadSfz with absolute path is the documented contract for the String overload. For symbol-resolved loads, Plan 33-05 documents the path-containment posture |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~Phase33.SfzParserTests"` exits 0 with 13/13 green
- `dotnet test flow-sharp.sln --logger "console;verbosity=minimal"` exits 0 (full suite green; Phase 29 byte-identical contract preserved)
</verification>

<success_criteria>
- SfzParser.cs ~250 LOC, all behavior facts green
- 13-opcode whitelist enforced with StringComparer.Ordinal
- All numeric parsing uses InvariantCulture + Float mask
- Header inheritance flattened at parse time
- Grid built last-declared-wins per D-02
- MaxRegionCount cap enforced
- All security threat mitigations in place
</success_criteria>

<output>
Create `.planning/phases/33-sfz-orchestral-sampler/33-04-SUMMARY.md` when done.
</output>
