---
phase: 33
plan: 08
type: execute
wave: 5
depends_on:
  - 33-01
  - 33-05
  - 33-06
  - 33-07
files_modified:
  - flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs
  - flow-lang.Tests/Integration/Phase33/SfzArticulationTests.cs
  - flow-lang.Tests/Integration/Phase33/SfzDeterminismTests.cs
  - examples/symphony/sfz_smoke.flow
  - examples/symphony/README.md
  - CLAUDE.md
autonomous: true
requirements: [SPEC-1, SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6, SPEC-7, SPEC-8]
must_haves:
  truths:
    - "SfzSmokeTests renders the Plan 33-01 fixture end-to-end via FlowEngineRunner; asserts exit code 0, output WAV non-empty, RMS > -40 dBFS, per-sample discontinuity check on the sustained body (SPEC-7 full acceptance)"
    - "SfzArticulationTests renders the smoke fixture under all 6 Phase 28 articulations (Staccato, Tenuto, Legato, Accent, Marcato, Sforzando); asserts each produces a distinct buffer; asserts RMS-thresholded audible duration matches the Phase 28 locked rules within ±5% (SPEC-8 full acceptance)"
    - "SfzArticulationTests also covers the ampeg_attack=0.5 vs 0.005 override case end-to-end via FlowEngineRunner (SPEC-8 acceptance criterion explicit)"
    - "SfzDeterminismTests runs the smoke fixture render TWICE in sequence; asserts cmp -clean byte-identical WAV output (Phase 18/25/27 contract preserved end-to-end through the SFZ surface)"
    - "examples/symphony/sfz_smoke.flow is a runnable composer-facing 4-bar example demonstrating the full `use \"@sfz\"; Sfz violin = (loadSfz #violin); renderSong song \"sampler:violin\"; writeWav` pipeline"
    - "examples/symphony/README.md documents how to download VSCO Community CE 1.1.0, configure sfz_root in ~/.config/flow/config.toml, and run sfz_smoke.flow"
    - "CLAUDE.md gains a new row in the Music Types Quick Reference table for `Sfz` (alongside the existing Tuning row); a new bullet under Language Features documenting the @sfz surface; the v1.4 backlog cites Phase 34 symphony showcase as the consumer"
    - "Full suite green: `dotnet test flow-sharp.sln` exits 0"
  artifacts:
    - path: "flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs"
      provides: "SPEC-7 acceptance — end-to-end render of smoke fixture"
      contains_pattern: "class Phase33SfzSmokeTests"
    - path: "flow-lang.Tests/Integration/Phase33/SfzArticulationTests.cs"
      provides: "SPEC-8 acceptance — 6-articulation distinctness + ampeg_attack override"
      contains_pattern: "SixArticulations_(ProduceDistinctBuffers|AudibleDuration)"
    - path: "flow-lang.Tests/Integration/Phase33/SfzDeterminismTests.cs"
      provides: "Two-run byte-identical determinism contract preserved through SFZ surface"
      contains_pattern: "TwoRun_(CmpClean|SameEngine)"
    - path: "examples/symphony/sfz_smoke.flow"
      provides: "Composer-facing 4-bar runnable example"
      contains: "loadSfz"
    - path: "examples/symphony/README.md"
      provides: "Composer setup instructions: download VSCO-CE, configure sfz_root, run sfz_smoke.flow"
      contains: "sfz_root"
    - path: "CLAUDE.md"
      provides: "Sfz row in Music Types Quick Reference + @sfz surface documentation"
      contains: "Sfz"
  key_links:
    - from: "flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs"
      to: "flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz"
      via: "Tests load this fixture and render through SongRenderer.RenderSong(\"sampler:smoke\")"
      pattern: "smoke.sfz"
    - from: "examples/symphony/sfz_smoke.flow"
      to: "flow-lang/sfz.flow"
      via: "use \"@sfz\" import gates the surface"
      pattern: "use \"@sfz\""
---

<objective>
Wave 5: ship the end-to-end SPEC-7 + SPEC-8 acceptance integration tests, the determinism regression net, the composer-facing example, and the documentation updates that pin Phase 33's behavior. This is the closure plan — once green, every one of the 8 SPEC requirements has a passing test gate and the Phase 34 symphony showcase has a working foundation to build on.

Purpose: SPEC-7 (CI smoke test renders fixture without errors + non-zero RMS + discontinuity check), SPEC-8 (all 6 articulations distinct + ampeg_attack override), all-SPEC two-run byte-identical determinism, composer-facing docs.

Output: 3 new integration test files + 2 new example files + 1 modified CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-sfz-orchestral-sampler/33-SPEC.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md
@.planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md
@.planning/phases/33-sfz-orchestral-sampler/33-VALIDATION.md
@flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz
@flow-lang.Tests/Integration/Phase29/SampledInstrumentSmokeTests.cs
@CLAUDE.md
@flow-lang/sfz.flow
@flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs

<interfaces>
<!-- The composer-facing example shape (per 33-RESEARCH § Example 5):
// examples/symphony/sfz_smoke.flow
use "@audio"
use "@sfz"

tempo 100 {
  key Cmajor {
    Sfz violin = (loadSfz #violin)

    section opening {
      | C4q D4q E4q F4q G4h G4h C5w |
    }

    Song song = [opening]
    Buffer mix = (renderSong song "sampler:violin")
    (writeWav "sfz_smoke.wav" mix)
  }
}
-->

<!-- The CLAUDE.md Music Types Quick Reference row to add (after the Tuning row):
| `(loadSfz #violin)` | `Sfz`         | strict (reference identity; no `Double`/`Float`) | `Sfz` variable binding for `renderSong song "sampler:NAME"` dispatch (Phase 33) |
-->

<!-- Phase 28 articulation expected duration ratios (locked rules from CLAUDE.md):
Staccato:  25% of authored duration + sustain=0 + release × 0.5
Tenuto:    100% of authored duration + release × 1.2 soft
Legato:    110% of authored duration + crossfade overlap
Accent:    no duration change, +0.30 velocity (clamped)
Marcato:   25% of authored duration + Accent's velocity boost
Sforzando: 100% duration + 1.5× → 1.0× envelope spike over first 15% of frames
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SfzSmokeTests + SfzArticulationTests + SfzDeterminismTests</name>
  <files>flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs, flow-lang.Tests/Integration/Phase33/SfzArticulationTests.cs, flow-lang.Tests/Integration/Phase33/SfzDeterminismTests.cs</files>
  <behavior>
    SfzSmokeTests (class name: `Phase33SfzSmokeTests` to match `dotnet test --filter "FullyQualifiedName~Phase33SfzSmoke"`):
    - SmokeFixture_Renders_NonEmpty_Above40dBFS: render the smoke fixture through `(loadSfz "{abs-path}")` + `renderSong song "sampler:smoke"`; assert WAV file non-empty, RMS > -40 dBFS (linear ~0.01)
    - SmokeFixture_Renders_DiscontinuityCheck: on the sustained-body portion of the render, max consecutive-sample amplitude jump ≤ 0.05 (SPEC-5 + SPEC-7 acceptance gate)
    - SmokeFixture_ExitCode_Zero: the FlowEngineRunner reports Success == true (no errors during parse/render)

    SfzArticulationTests:
    - SixArticulations_ProduceDistinctBuffers: render the same `C4q` note 6 times through the smoke fixture under each of {Staccato, Tenuto, Legato, Accent, Marcato, Sforzando}; assert the 6 resulting buffers are pairwise distinct (compute byte hash of each, assert 6 unique hashes)
    - SixArticulations_AudibleDuration_WithinTolerance: for each articulation, compute RMS-thresholded audible duration (consecutive frames above -40 dBFS) and assert it matches the Phase 28 locked rules within tolerance. Use the ratios in `<interfaces>`. Per-articulation tolerance: ±5% for Staccato, Tenuto, Legato, Marcato (clean duration ratios); ±10% for Sforzando and Accent (envelope shaping doesn't map to a clean duration ratio — Sforzando's 1.5× envelope spike + Accent's velocity boost both shift the -40 dBFS crossing window beyond ±5%).
    - AmpegAttack_Override_TakesEffect: two synthetic SFZ patches differing only in ampeg_attack (0.005 vs 0.5); render the same note under each; assert the 0.5 patch's time-to-50%-peak-RMS exceeds 200 ms while the 0.005 patch's is well under 50 ms (SPEC-8 explicit acceptance)

    SfzDeterminismTests:
    - TwoRun_CmpClean_SmokeFixture: render the smoke fixture to a temp WAV file TWICE (each render uses a fresh FlowEngineRunner instance to prove cache-cold determinism); read both files; assert `Assert.Equal(bytesA, bytesB)` (byte-identical per Phase 18/25/27 contract)
    - TwoRun_SameEngine_CmpClean: render TWICE within a SINGLE FlowEngineRunner instance (cache-warm path); assert byte-identical (proves the warm-cache path is also deterministic)
    - SortedIteration_LoadOrderStable: this is implicit in the above two — but add a debug assertion via the existing SfzSampleCache internals (if accessible) that the sorted iteration shipped in Plan 33-06 holds
  </behavior>
  <read_first>
    - flow-lang.Tests/Integration/Phase29/SampledInstrumentSmokeTests.cs (entire file — the FlowEngineRunner + cwd-guard + repo-root-find pattern per 33-PATTERNS.md)
    - flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz (the fixture content shipped by Plan 33-01)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs (verify the Render signature + the way articulation/ampeg_attack flow through)
    - flow-lang/StandardLibrary/Audio/SynthUtils.cs (verify SignalAndNoise + ResetNoiseRng — relevant for the two-run determinism contract; SongRenderer.RenderSong calls SynthUtils.ResetNoiseRng at entry per Phase 33-07 SongRenderer divergence)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Validation Architecture" — per-requirement test map)
    - .planning/phases/33-sfz-orchestral-sampler/33-VALIDATION.md (Wave 0 gaps that this plan closes)
  </read_first>
  <action>Create `flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs` (namespace `FlowLang.Tests.Integration.Phase33`). Class name MUST be `Phase33SfzSmokeTests` so `dotnet test --filter "FullyQualifiedName~Phase33SfzSmoke"` matches. [Collection("FlowScripts")] + IDisposable + RenderingDiagnostics.ResetForTesting. Implement the 3 SfzSmokeTests facts. The render script uses the String overload of loadSfz to bypass VSCO-CE dependency: `Sfz smoke = (loadSfz "{abs-path-to-smoke.sfz}")`. Use the FindTestsRoot helper from Phase29 to locate the fixture. For the discontinuity check on the sustained body: render a `C4w` (whole note at the test's tempo) which exercises the loop_continuous path of region 1, then iterate consecutive samples from frame ~22050 onward (past the initial attack) computing `Math.Abs(samples[i+1] - samples[i])`. Create `flow-lang.Tests/Integration/Phase33/SfzArticulationTests.cs` with the 3 facts. The SixArticulations test renders the SAME script 6 times, mutating only `note.Articulation` (this requires either driving via 6 separate scripts that use the Phase 28 articulation note-stream tokens `>`/`stacc`/`ten`/`marc`/`leg` per CLAUDE.md, or driving directly through SfzRenderer.Render at the unit level — pick whichever is simpler given the FlowEngineRunner shape). For AudibleDuration_Within5Percent: compute the expected duration in frames per articulation using the Phase 28 rules in `<interfaces>` (e.g. Staccato: 0.25 * authoredFrames; Legato: 1.10 * authoredFrames), threshold the output at -40 dBFS linear, count consecutive frames above threshold, assert `Math.Abs(actualFrames - expectedFrames) / expectedFrames &lt; 0.05`. Create `flow-lang.Tests/Integration/Phase33/SfzDeterminismTests.cs` with the 2 facts. TwoRun_CmpClean_SmokeFixture: instantiate FlowEngineRunner, render to `Path.GetTempFileName() + ".wav"`, dispose the runner, instantiate a NEW runner, render to a second temp file, read both via `File.ReadAllBytes`, assert equal. TwoRun_SameEngine_CmpClean: same but reuse the same FlowEngineRunner — the second call must hit the warm cache via the idempotency check shipped in Plan 33-06. Use the [Collection("FlowScripts")] serialization to ensure the two facts don't race RenderingDiagnostics state.</action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33SfzSmoke|FullyQualifiedName~Phase33.SfzArticulationTests|FullyQualifiedName~Phase33.SfzDeterminismTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>All 8 integration facts green. SPEC-7 + SPEC-8 acceptance criteria fully testable via CI. Two-run byte-identical determinism contract preserved end-to-end through the SFZ surface (Phase 18/25/27 gate).</done>
</task>

<task type="auto">
  <name>Task 2: Composer-facing example + README</name>
  <read_first>
    - examples/ (browse to verify directory layout + the `examples/scala/intro.flow` style from Phase 32 Plan 32-07)
    - examples/tutorial.flow (the canonical composer-facing voice — read the first 50 lines for tone/convention)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Code Examples" Example 5 — the locked example body)
    - flow-lang/sfz.flow (verify the 19-entry dict — the README's "supported symbols" list must match)
    - flow-cli/Config/ (verify the exact TOML key path for sfz_root config — Plan 33-02 added SfzRoot to FlowConfigPoco with snake_case_lower mapping)
  </read_first>
  <action>Create `examples/symphony/` directory (the parent directory must not exist yet — verify with `ls examples/` first). Create `examples/symphony/sfz_smoke.flow` with the exact body from `<interfaces>`. Create `examples/symphony/README.md`. Body sections: (1) Overview — what the example demonstrates (4 bars of single-violin melody rendered through VSCO-CE via the @sfz surface); (2) Setup — link to https://github.com/sgossner/VSCO-2-CE/releases/tag/1.1.0 (the blessed orchestral library per 33-RESEARCH); download and extract instructions; configure `sfz_root` in `~/.config/flow/config.toml` with a one-liner example: `sfz_root = "/home/user/.flow/samples/VSCO-CE"`; (3) Run — `dotnet run --project flow-interpreter examples/symphony/sfz_smoke.flow` produces `sfz_smoke.wav`; play with `play sfz_smoke.wav` (or any system player); (4) Supported instruments — the 19-symbol GM list from sfz.flow, formatted as a markdown bullet list; reference the audit doc at `.planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md` for the exact VSCO-CE paths each symbol resolves to; (5) Absolute paths — note that `loadSfz "/abs/path.sfz"` bypasses the dict for non-GM instruments or custom SFZ patches; (6) Phase 34 forward reference — "This example is the foundation for the v1.4 symphony showcase shipping in Phase 34". Keep the README under 80 lines.</action>
  <verify>
    <automated>test -d examples/symphony &amp;&amp; test -f examples/symphony/sfz_smoke.flow &amp;&amp; test -f examples/symphony/README.md &amp;&amp; grep -q "use \"@sfz\"" examples/symphony/sfz_smoke.flow &amp;&amp; grep -q "loadSfz #violin" examples/symphony/sfz_smoke.flow &amp;&amp; grep -q "sfz_root" examples/symphony/README.md &amp;&amp; grep -q "VSCO" examples/symphony/README.md</automated>
  </verify>
  <done>examples/symphony/ shipped with runnable Flow script + composer-facing README explaining VSCO-CE download + config + run. The script does NOT need to run successfully in CI (it requires VSCO-CE on disk) — manual UAT per the SPEC-2 acceptance criterion.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md updates + closure docs</name>
  <read_first>
    - CLAUDE.md (read the Music Types Quick Reference table around the Tuning row + the Language Features section for the bulleted-feature pattern + the bottom-of-file "Phase NN" sections used by prior phases)
    - .planning/REQUIREMENTS.md (the per-phase requirement catalog; Phase 33 SPEC-1..SPEC-8 need to be ingested here per the planning_context "use SPEC-N format throughout (these are not yet in REQUIREMENTS.md — they live in 33-SPEC.md)" — Plan 33-08 is the closure plan that ingests them)
    - .planning/phases/32-full-scala-scl-tuning-loader/32-07-PLAN.md (the Phase 32 closure-plan analog — same shape of CLAUDE.md weave + REQUIREMENTS ingestion)
  </read_first>
  <action>Patch `CLAUDE.md`: (1) In the "Music Types Quick Reference" table, add ONE new row immediately after the Tuning row per the `<interfaces>` block (`| (loadSfz #violin) | Sfz | strict (reference identity; no Double/Float) | Sfz variable binding for renderSong song "sampler:NAME" dispatch (Phase 33) |`). (2) In the "Language Features" → "Music-Specific" bulleted list, add a new bullet near the existing Tuning bullet: `**SFZ orchestral sampler (opt-in):** `use "@sfz"` activates the surface. `(loadSfz #violin)` resolves a 19-entry GM symbol dict against `sfz_root` from `~/.config/flow/config.toml` (Phase 30 config); `(loadSfz "/abs/path.sfz")` bypasses the dict. Bind via `Sfz violin = (loadSfz #violin)` and render via `renderSong song "sampler:violin"`. Common-subset SFZ parser (13 opcodes + <region>/<group>/<global>); per-region sustain looping with 441-frame equal-power crossfade; Phase 28 articulation envelope applies on top. Phase 29 bundled-sample path stays byte-identical (Phase 33).`. (3) Update the prose "Special Types" list in CLAUDE.md (the line under heading `### Special Types (\`TypeSystem/SpecialTypes/\`)` ending with `Tuning (Phase 32 — Scala \`.scl\` tuning loader output, reference identity)`). Append `, Sfz (Phase 33 — SFZ orchestral sampler patch, reference identity)` to the end of that single-line comma-separated list. Do NOT create a new section — the line is the canonical list and just gets one more entry. (4) NEW: ingest SPEC-1..SPEC-8 into `.planning/REQUIREMENTS.md` under a new Phase 33 section (read the existing per-phase shape in REQUIREMENTS.md before writing; mirror the Phase 32 SPEC-1..SPEC-7 row format). Each SPEC row: ID, brief description, status (locked vs deferred), pointer to 33-SPEC.md for the full acceptance criterion. Status of SPEC-1..SPEC-8 = locked (all 8 ship in this phase).</action>
  <verify>
    <automated>grep -q "Sfz" CLAUDE.md &amp;&amp; grep -q "loadSfz" CLAUDE.md &amp;&amp; grep -q "@sfz" CLAUDE.md &amp;&amp; grep -q "SPEC-1\|SFZ" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>CLAUDE.md has the Sfz row in Music Types Quick Reference + the @sfz feature bullet. REQUIREMENTS.md ingests SPEC-1..SPEC-8 as the canonical Phase 33 requirement catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer downloads VSCO-CE → sfz_root path | External library; composer trust posture documented in README |
| Phase 33 fixture render → byte-identical determinism gate | Internal regression net; preserves Phase 18/25/27 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-DOC-01 | Tampering | composer follows README instructions to download VSCO-CE from untrusted mirror | accept | Documented to use the canonical GitHub release; composer responsibility |
| T-33-DET-FINAL-01 | Tampering | Phase 33 surface introduces nondeterminism in the render path | mitigate | SfzDeterminismTests is the final gate; if either fact fails, the phase cannot ship |
| T-33-REGR-FINAL-01 | Tampering | Phase 33 surface regresses Phase 28 or Phase 29 byte-identical contracts | mitigate | `dotnet test flow-sharp.sln` full-suite green is the closure gate; Phase28 + Phase29 RmsBaselineTests are the canaries |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~Phase33"` exits 0 — full Phase 33 suite green
- `dotnet test flow-sharp.sln --logger "console;verbosity=minimal"` exits 0 — full suite green; Phase 28 + Phase 29 byte-identical contracts preserved
- All 8 SPEC acceptance criteria from 33-SPEC.md mapped to passing tests (cross-check against 33-VALIDATION.md table)
- examples/symphony/sfz_smoke.flow exists + composer can run it with a VSCO-CE install (manual UAT)
- CLAUDE.md has Sfz row + @sfz bullet
- REQUIREMENTS.md ingests SPEC-1..SPEC-8
</verification>

<success_criteria>
- 3 integration test classes green (SfzSmokeTests + SfzArticulationTests + SfzDeterminismTests)
- examples/symphony/ shipped with runnable Flow script + composer README
- CLAUDE.md updated with Sfz music type + @sfz feature bullet
- REQUIREMENTS.md ingests Phase 33's 8 SPECs
- Full test suite green (Phase 28 + Phase 29 byte-identical contracts intact)
- Phase 34 has a working SFZ surface to build the symphony showcase on
</success_criteria>

<output>
Create `.planning/phases/33-sfz-orchestral-sampler/33-08-SUMMARY.md` when done. Include: SPEC coverage matrix (8 rows, each mapped to a green test), full-suite test count before/after, byte-identical regression confirmation for Phase 28 + Phase 29.
</output>
