---
phase: 33
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzParseException.cs
  - flow-lang/TypeSystem/SpecialTypes/SfzType.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang/Runtime/FlowConfig.cs
  - flow-lang/Runtime/Value.cs
  - flow-lang.Tests/Unit/Phase33/SfzTypeFacts.cs
autonomous: true
requirements: [SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6]
must_haves:
  truths:
    - "flow-lang/StandardLibrary/Audio/Sfz/ subdirectory exists with the four data-model files (SfzLoopMode, SfzRegion, SfzData, SfzParseException)"
    - "SfzLoopMode enum has exactly four members: NoLoop, OneShot, LoopContinuous, LoopSustain — matching SFZ spec valid values"
    - "SfzRegion is an immutable sealed record carrying 13 positional fields (per CONTEXT Claude's Discretion SfzRegion field set); Volume stored as LINEAR amplitude (after dB→linear conversion at parse time per Pitfall 8); Pan stored in Flow's [-1.0, +1.0] range (after ÷100.0 at parse time per Pitfall 7)"
    - "SfzData record contains Description, BasePath, Regions list, Grid (SfzRegion?[128, 128] per D-01), and SortedByPitch int[] (per D-03)"
    - "SfzParseException extends System.Exception (mirroring ScalaParseException's actual base) and formats the `{file}:{line}:{col} — expected X got 'Y'` message (Claude's Discretion error hierarchy)"
    - "SfzType is a sealed singleton extending FlowType with specificity 150 (above all existing music types per 33-RESEARCH Example 1)"
    - "SfzType.IsCompatibleWith and CanConvertTo are strict — return true ONLY when target is SfzType (no numeric coercion per CONTEXT Claude's Discretion, mirrors TuningType)"
    - "ExecutionContext gains FIVE new auto-init fields (SfzEnabled, SfzInstruments, SfzPatchRegistry, SfzDiagnostics, ResolvedSfzRoot) clustered beneath SymbolInternTable with a `// Phase 33 — SFZ surface` section header; SfzPatchRegistry is strongly typed `Dictionary<string, FlowLang.StandardLibrary.Audio.Sfz.SfzData>` (no forward-reference workaround — SfzData ships in this same plan, Task 1)"
    - "FlowConfigPoco gains a nullable string SfzRoot init-only property; Tomlyn's existing JsonNamingPolicy.SnakeCaseLower auto-maps the TOML key `sfz_root` to it (zero edit needed in flow-cli/Config/FlowConfigLoader.cs)"
    - "Value.cs gains a public static `Value Sfz(SfzData data)` factory immediately below the existing Value.Tuning factory"
    - "Final compile-verify task asserts `dotnet build flow-sharp.sln` exits 0 — proves the merged plan compiles without forward-reference workarounds"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs"
      provides: "Enum: NoLoop, OneShot, LoopContinuous, LoopSustain"
      contains: "LoopContinuous"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs"
      provides: "13-field record (per CONTEXT Claude's Discretion 'SfzRegion field set')"
      contains: "PitchKeycenter"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      provides: "Top-level patch record (Description, BasePath, Regions, Grid[128,128], SortedByPitch[])"
      contains: "Grid"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzParseException.cs"
      provides: "Parse-time error with file:line:col format"
      contains: "expected"
    - path: "flow-lang/TypeSystem/SpecialTypes/SfzType.cs"
      provides: "First-class Sfz value type (sealed singleton, strict, reference identity)"
      contains: "GetSpecificity() => 150"
    - path: "flow-lang/Runtime/ExecutionContext.cs"
      provides: "Five new fields gated by Phase 33 SFZ surface section header"
      contains: "SfzPatchRegistry"
    - path: "flow-lang/Runtime/FlowConfig.cs"
      provides: "FlowConfigPoco.SfzRoot nullable string property"
      contains: "SfzRoot"
    - path: "flow-lang/Runtime/Value.cs"
      provides: "Value.Sfz(SfzData) factory"
      contains: "SfzType.Instance"
    - path: "flow-lang.Tests/Unit/Phase33/SfzTypeFacts.cs"
      provides: "Type-system fact suite — specificity, strict compatibility, Value.Sfz round-trip"
      contains_pattern: "SfzType_GetSpecificity_Is150"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs"
      via: "SfzData.Regions: IReadOnlyList<SfzRegion>"
      pattern: "SfzRegion"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs"
      via: "SfzData.Grid: SfzRegion?[,] (128x128)"
      pattern: "SfzRegion\\?\\[,\\]"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs"
      via: "SfzRegion.LoopMode: SfzLoopMode"
      pattern: "SfzLoopMode"
    - from: "flow-lang/Runtime/Value.cs"
      to: "flow-lang/TypeSystem/SpecialTypes/SfzType.cs"
      via: "Value.Sfz factory returns Value typed as SfzType.Instance"
      pattern: "SfzType\\.Instance"
    - from: "flow-lang/Runtime/ExecutionContext.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      via: "SfzPatchRegistry: Dictionary<string, SfzData> directly references SfzData type from Task 1 of this same plan"
      pattern: "SfzPatchRegistry"
---

<objective>
Wave 1 (merged): ship the immutable SFZ data model PLUS the type-system + runtime-state foundation in one plan. The data model (Task 1) and the runtime surface (Task 2) have a mutual compile-time dependency (`ExecutionContext.SfzPatchRegistry: Dictionary<string, SfzData>` requires SfzData; `Value.Sfz(SfzData)` factory requires SfzData), so they must ship together. A final compile-verify task (Task 3) proves the project builds cleanly with no forward-reference workarounds.

Purpose: SPEC-3/4/5 consume the parser output shape (SfzRegion, SfzData, SfzLoopMode, SfzParseException). SPEC-2/6 consume the typed-Sfz binding surface (SfzType, Value.Sfz, ExecutionContext.SfzPatchRegistry, FlowConfigPoco.SfzRoot). Every other Phase 33 plan compiles against the symbols defined here.

Output: 4 new data-model files + 1 new SpecialType file + 3 modified runtime files (5 new ExecutionContext fields total) + 1 new fact suite + 1 build gate. Type-only — no audio rendering, no parsing, no I/O.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-sfz-orchestral-sampler/33-SPEC.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md
@.planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md
@flow-lang/StandardLibrary/Audio/Tuning/ScalaParser.cs
@flow-lang/StandardLibrary/Audio/Tuning/ScalaParseException.cs
@flow-lang/TypeSystem/SpecialTypes/TuningType.cs
@flow-lang/Runtime/Value.cs
@flow-lang/Runtime/FlowConfig.cs
@flow-lang/Runtime/ExecutionContext.cs

<interfaces>
<!-- ParsedScala record (the model SfzData mirrors), from ScalaParser.cs:22-28:
public sealed record ParsedScala(
    string Description,
    double[] StepCents,
    double PeriodCents,
    IReadOnlyDictionary<int, (int Num, int Den)> Ratios,
    string FilePath);
-->

<!-- ScalaParseException (the analog for SfzParseException):
Constructor signature `(string filePath, int line, int column, string expected, string got)`
yields a message of the form `"{filePath}:{line}:{column} — expected {expected} got '{got}'"`.
-->

<!-- SFZ opcode defaults from sfzformat.com (cited in 33-RESEARCH § Pattern 2 quirks):
lokey=0, hikey=127, pitch_keycenter=60, lovel=1, hivel=127
loop_mode="no_loop" (or "loop_continuous" if loop_start/end present)
loop_start=0, loop_end=0
ampeg_attack=0, ampeg_release=0.001 (spec) — parser populates as-authored
volume=0 dB → linear 1.0 (parser converts at read time per Pitfall 8)
pan=0 (Flow [-1.0, +1.0]) — parser ÷ 100.0 at read time per Pitfall 7
-->

<!-- TuningType.cs (the exact pattern to mirror for SfzType):
public sealed class TuningType : FlowType
{
    private TuningType() { }
    public static TuningType Instance { get; } = new();
    public override string Name => "Tuning";
    public override int GetSpecificity() => 137;
    public override bool IsCompatibleWith(FlowType target) => target is TuningType;
    public override bool CanConvertTo(FlowType target) => target is TuningType;
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SFZ data-model files (SfzLoopMode + SfzParseException + SfzRegion + SfzData)</name>
  <read_first>
    - flow-lang/StandardLibrary/Audio/Tuning/ScalaParseException.cs (entire file — the exact `{file}:{line}:{col} — expected X got 'Y'` shape; per 33-PATTERNS.md analog match — copy its actual base class)
    - flow-lang/StandardLibrary/Audio/Tuning/ScalaParser.cs (read lines 1-30 — verify `ParsedScala` record shape that 33-PATTERNS.md cites as the SfzData analog)
    - .planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md (§"Claude's Discretion" — `SfzData` internal model field list + `SfzRegion` field set)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Common Pitfalls" 7 + 8 — Pan ÷ 100, Volume dB→linear at parse time; §"Pattern 2 quirks" — loop_mode valid values verified at sfzformat.com/opcodes/loop_mode)
    - .planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md (§"flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs + SfzRegion.cs")
  </read_first>
  <action>Create `flow-lang/StandardLibrary/Audio/Sfz/` subdirectory with four files (all under namespace `FlowLang.StandardLibrary.Audio.Sfz`, file-scoped).

(1) `SfzLoopMode.cs` — Public enum `SfzLoopMode` with four members in this exact order: `NoLoop`, `OneShot`, `LoopContinuous`, `LoopSustain`. XML-doc on the enum citing sfzformat.com/opcodes/loop_mode + the pitfall that unknown string values from SFZ fall back to NoLoop with a one-shot advisory (the actual fallback logic lives in the parser, Plan 33-04).

(2) `SfzParseException.cs` — Mirror `flow-lang/StandardLibrary/Audio/Tuning/ScalaParseException.cs` structurally: derive from System.Exception (verify by reading ScalaParseException.cs's actual derivation — copy that exact base). Constructor signature `(string filePath, int line, int column, string expected, string got)` formatting the base message as `$"{filePath}:{line}:{column} — expected {expected} got '{got}'"`. Expose the five params as public init-only properties so test code can assert on individual fields.

(3) `SfzRegion.cs` — Public sealed record `SfzRegion` with these 13 positional fields IN THIS ORDER: `string SamplePath`, `int PitchKeycenter`, `int LoKey`, `int HiKey`, `int LoVel`, `int HiVel`, `SfzLoopMode LoopMode`, `int LoopStart`, `int LoopEnd`, `double AmpegAttack`, `double AmpegRelease`, `double Volume`, `double Pan`. XML-doc on the record explaining: (a) Volume is LINEAR amplitude (parser converts the SFZ `volume` dB value via `Math.Pow(10.0, db / 20.0)` at parse time per Pitfall 8 — runtime never sees dB); (b) Pan is Flow's `[-1.0, +1.0]` range (parser divides the SFZ `pan` [-100..+100] value by 100.0 at parse time per Pitfall 7 — runtime never sees the SFZ range); (c) AmpegAttack and AmpegRelease are in SECONDS (SFZ-native units; SPEC-8 says these override the Phase 28 baseline only when > 0); (d) LoopStart/LoopEnd are SOURCE-FRAME indices into the loaded WAV (not seconds); (e) inheritance from `<global>`/`<group>` is flattened at parse time — runtime never traverses headers (per CONTEXT Claude's Discretion).

(4) `SfzData.cs` — Public sealed record `SfzData` with positional fields IN THIS ORDER: `string Description`, `string BasePath`, `IReadOnlyList<SfzRegion> Regions`, `SfzRegion?[,] Grid`, `int[] SortedByPitch`. XML-doc explaining: (a) Description is the .sfz filename or the first non-comment line (parser decides); (b) BasePath is the directory containing the .sfz file — used to resolve relative sample paths at load time; (c) Regions preserves SFZ declaration order (matters for D-02 last-declared-wins grid build AND for deterministic eager-load iteration); (d) Grid is `[128, 128]` keyed by `(midiPitch, midiVelocity)` per D-01 — null cell means no region covers that (pitch, vel) pair; (e) SortedByPitch is the deduplicated ascending list of MIDI pitches with ANY region coverage — used by SPEC-4 nearest-pitch fallback (D-03).</action>
  <verify>
    <automated>test -f flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs &amp;&amp; test -f flow-lang/StandardLibrary/Audio/Sfz/SfzParseException.cs &amp;&amp; test -f flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs &amp;&amp; test -f flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs &amp;&amp; grep -q "LoopContinuous" flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs &amp;&amp; grep -q "LoopSustain" flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs &amp;&amp; grep -q "expected" flow-lang/StandardLibrary/Audio/Sfz/SfzParseException.cs &amp;&amp; grep -q "PitchKeycenter" flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs &amp;&amp; grep -q "AmpegAttack" flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs &amp;&amp; grep -q "SfzRegion?\[,\]" flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs &amp;&amp; grep -q "SortedByPitch" flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs</automated>
  </verify>
  <done>All four data-model files exist with the specified shapes. SfzRegion has 13 ordered fields; SfzData has 5 fields including the `SfzRegion?[,]` grid + `int[] SortedByPitch`. All records are sealed and immutable. The data-model files compile in isolation (no dependencies on Task 2 yet).</done>
</task>

<task type="auto">
  <name>Task 2: SfzType + ExecutionContext SFZ-surface fields + FlowConfigPoco.SfzRoot + Value.Sfz factory</name>
  <read_first>
    - flow-lang/TypeSystem/SpecialTypes/TuningType.cs (entire file — 27 LOC; the exact pattern to copy per 33-PATTERNS.md)
    - flow-lang/Runtime/Value.cs (read lines 1-80 — locate Value.Tuning factory at line ~60 for insertion site)
    - flow-lang/TypeSystem/SpecialTypes/HertzType.cs (verify the specificity slot map: HertzType=144, SfzType=150 above all music types)
    - flow-lang/Runtime/ExecutionContext.cs (read lines 1-100 — locate SymbolInternTable at line ~85 for cluster-below insertion site; the existing "Random Number Generation State" block lines 31-72 is the template for a Phase 33 section header)
    - flow-lang/Runtime/FlowConfig.cs (read lines 1-50 — locate FlowConfigPoco record at lines 19-26 for the single-line SfzRoot insertion)
    - flow-cli/Config/FlowConfigLoader.cs (verify lines 34-37 — JsonNamingPolicy.SnakeCaseLower is already configured, so `sfz_root` TOML key auto-maps to `SfzRoot` POCO property; this file requires NO edit)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Code Examples" Example 1 — SfzType body; Example 2 — the 5 ExecutionContext fields including ResolvedSfzRoot per Pitfall 2)
    - .planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md (§"flow-lang/TypeSystem/SpecialTypes/SfzType.cs" + §"flow-lang/Runtime/ExecutionContext.cs" + §"flow-lang/Runtime/FlowConfig.cs" + §"flow-lang/Runtime/Value.cs")
  </read_first>
  <action>(1) Create `flow-lang/TypeSystem/SpecialTypes/SfzType.cs` (namespace `FlowLang.TypeSystem.SpecialTypes`). Sealed class extending FlowType. Private no-arg constructor. Public static Instance singleton. Override `Name => "Sfz"`. Override `GetSpecificity() => 150`. Override `IsCompatibleWith(FlowType target) => target is SfzType`. Override `CanConvertTo(FlowType target) => target is SfzType`. XML-doc per 33-RESEARCH Example 1: explain specificity-slot rationale (Tuning=137, Section=138, Beat=139, Song=140, Hertz=144, SfzType=150 keeps clearance above all music types) and reference-identity contract (two `(loadSfz #violin)` calls produce distinct Sfz values even with identical resolved paths).

(2) Patch `flow-lang/Runtime/Value.cs`: immediately below the existing `Value.Tuning(ResolvedTuning resolved)` factory (around line 61), add `public static Value Sfz(StandardLibrary.Audio.Sfz.SfzData data) => new(data, SfzType.Instance);`. Add `using FlowLang.TypeSystem.SpecialTypes;` if not already present. SfzData ships in Task 1 of this same plan — no forward-reference workaround needed.

(3) Patch `flow-lang/Runtime/ExecutionContext.cs`: immediately below the existing `SymbolInternTable` property (around line 85), insert a section header comment `// ===== Phase 33 — SFZ surface =====` followed by FIVE properties: (a) `public bool SfzEnabled { get; set; } = false;` with XML-doc "flipped true by __enableSfzModule when `use \"@sfz\"` runs"; (b) `public Dictionary<Value, string> SfzInstruments { get; } = new();` with XML-doc "Symbol Value → relative path; populated by __enableSfzModule from the sfz.flow dict"; (c) `public Dictionary<string, FlowLang.StandardLibrary.Audio.Sfz.SfzData> SfzPatchRegistry { get; } = new();` strongly typed (SfzData ships in Task 1) with XML-doc "variable name → SfzData; written by Interpreter.ExecuteVariableDeclaration when varDecl.Type is SfzType (Plan 33-07); read by SongRenderer.RenderSong sampler: branch"; (d) `public HashSet<string> SfzDiagnostics { get; } = new();` with XML-doc "one-shot stderr-advisory dedup keyed by sentinel strings of the form `sfz:opcode:{patch}:{name}`, `sfz:missing:{patch}:{midi}:{vel}`, `sfz:config:sfz_root_missing`"; (e) `public string? ResolvedSfzRoot { get; set; } = null;` with XML-doc "first-read cache for FlowConfig.Active.SfzRoot per Pitfall 2 — prevents singleton-pollution test flakes and prevents script-time config edits from affecting an in-flight render".

(4) Patch `flow-lang/Runtime/FlowConfig.cs`: add one property `public string? SfzRoot { get; init; }` to `FlowConfigPoco` (positioned alphabetically or grouped with related path keys; XML-doc "XDG `sfz_root` key — composer-configured VSCO-CE library root; read at first loadSfz call per Pitfall 2"). Do NOT edit FlowConfigPoco.Defaults — record-init defaults to null automatically.

(5) Create `flow-lang.Tests/Unit/Phase33/SfzTypeFacts.cs` (namespace `FlowLang.Tests.Unit.Phase33`) with seven facts: `SfzType_GetSpecificity_Is150`, `SfzType_IsCompatibleWith_SfzType_True`, `SfzType_IsCompatibleWith_TuningType_False` (strict — no cross-music-type compatibility), `SfzType_IsCompatibleWith_StringType_False` (strict — no numeric/string coercion), `SfzType_Name_IsSfz`, `ExecutionContext_FreshInstance_HasEmptySfzFields` (asserts SfzEnabled == false; SfzInstruments.Count == 0; SfzPatchRegistry.Count == 0; SfzDiagnostics.Count == 0; ResolvedSfzRoot == null), `FlowConfigPoco_Default_SfzRootIsNull`. Use the existing test-file naming convention `*Facts.cs`. Construct ExecutionContext via `new ExecutionContext()` (verify constructor exists — check the file directly).</action>
  <verify>
    <automated>test -f flow-lang/TypeSystem/SpecialTypes/SfzType.cs &amp;&amp; grep -q "GetSpecificity() =&gt; 150" flow-lang/TypeSystem/SpecialTypes/SfzType.cs &amp;&amp; grep -q "Name =&gt; \"Sfz\"" flow-lang/TypeSystem/SpecialTypes/SfzType.cs &amp;&amp; grep -q "public static Value Sfz" flow-lang/Runtime/Value.cs &amp;&amp; grep -q "Phase 33 — SFZ surface" flow-lang/Runtime/ExecutionContext.cs &amp;&amp; grep -q "SfzPatchRegistry" flow-lang/Runtime/ExecutionContext.cs &amp;&amp; grep -q "ResolvedSfzRoot" flow-lang/Runtime/ExecutionContext.cs &amp;&amp; grep -q "string? SfzRoot" flow-lang/Runtime/FlowConfig.cs &amp;&amp; test -f flow-lang.Tests/Unit/Phase33/SfzTypeFacts.cs</automated>
  </verify>
  <done>SfzType.cs exists with sealed singleton shape mirroring TuningType. Value.Sfz factory exists in Value.cs right below Value.Tuning. ExecutionContext has 5 new fields under "Phase 33 — SFZ surface" section (including ResolvedSfzRoot per Pitfall 2). FlowConfigPoco has SfzRoot. SfzTypeFacts.cs exists with 7 facts.</done>
</task>

<task type="auto">
  <name>Task 3: Compile-verify gate + SfzTypeFacts test run</name>
  <read_first>
    - flow-sharp.sln (verify which projects are included so `dotnet build` exercises all dependent projects)
  </read_first>
  <action>Run `dotnet build flow-sharp.sln --logger "console;verbosity=minimal"` and assert exit code 0. This is the Nyquist gate for Wave 1: it proves Task 1 + Task 2 compose without forward-reference workarounds. Then run `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzTypeFacts" --logger "console;verbosity=minimal"` and assert all 7 facts green. This task has no file edits — it is a pure verification gate to prove the merged plan ships cleanly.</action>
  <verify>
    <automated>dotnet build flow-sharp.sln --logger "console;verbosity=minimal" &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzTypeFacts" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>`dotnet build flow-sharp.sln` exits 0 — proves the merged data-model + type-system + runtime-state surface compiles end-to-end. All 7 SfzTypeFacts pass. Phase 29 + Phase 32 byte-identical contract preserved (no behavioral edits to existing surface — verified by the full build succeeding without regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser → SfzData record | Type definition only — no untrusted-input crossing here; the parser (Plan 33-04) is responsible for input validation before constructing records |
| TOML config file → FlowConfigPoco | Untrusted disk content; sfz_root could be any string |
| Variable assignment → SfzPatchRegistry | Composer-controlled names; no validation needed (variable names are already filtered by parser) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-PARSE-01 | DoS | SfzData.Regions list size + Grid allocation | mitigate | The cap (`MaxRegionCount = 10000`) is enforced by SfzParser (Plan 33-04); the data model itself imposes no cap. Grid is fixed 128×128 — cannot inflate beyond ~144 KB per patch (CONTEXT D-04) |
| T-33-LOOP-01 | DoS | LoopStart / LoopEnd out-of-bounds vs sample length | mitigate | SfzRegion stores raw declared values; render-time clamp (Plan 33-06) applies `Math.Min(LoopEnd, sourceBuffer.Length - 1)` per Pitfall 3 |
| T-33-NUM-01 | Tampering | malformed sfz_root path | mitigate | Path validation happens at loadSfz call time (Plan 33-05) — this plan ships only the POCO surface |
| T-33-PATH-01 | InfoDisclosure | sfz_root pointing to system directory | accept | Phase 32 precedent (loadScala accepts absolute paths). Composer-controlled config; same posture as writeWav("/etc/foo") |
</threat_model>

<verification>
- `dotnet build flow-sharp.sln` exits 0 (Task 3 gate)
- `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzTypeFacts"` exits 0, all 7 facts green (Task 3 gate)
- Phase 29 + Phase 32 byte-identical contract preserved (no behavioral edits to existing surface)
</verification>

<success_criteria>
- 4 new data-model files in flow-lang/StandardLibrary/Audio/Sfz/
- SfzType.cs created with specificity 150 + strict compatibility
- ExecutionContext gains 5 new fields under "Phase 33 — SFZ surface" section (including ResolvedSfzRoot)
- FlowConfigPoco gains nullable SfzRoot init property
- Value.Sfz factory added immediately below Value.Tuning
- SfzTypeFacts.cs with 7 facts
- `dotnet build flow-sharp.sln` green at end-of-plan (compile-verify gate)
- Phase 29 + Phase 32 byte-identical contract preserved
</success_criteria>

<output>
Create `.planning/phases/33-sfz-orchestral-sampler/33-02-SUMMARY.md` when done.
</output>
