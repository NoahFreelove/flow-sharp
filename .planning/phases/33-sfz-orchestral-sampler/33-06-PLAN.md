---
phase: 33
plan: 06
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-02
files_modified:
  - flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
  - flow-lang.Tests/Unit/Phase33/SfzRegionMatchTests.cs
  - flow-lang.Tests/Unit/Phase33/SfzLoopCrossfadeTests.cs
autonomous: true
requirements: [SPEC-4, SPEC-5, SPEC-8]
must_haves:
  truths:
    - "SfzSampleCache.EagerLoad(SongData song, SfzData patch) walks the song's note set, dereferences patch.Grid for each (pitch, velocity) cell, collects distinct regions, then iterates the resulting set in `.OrderBy(r => r.SamplePath, StringComparer.Ordinal).ThenBy(r => r.PitchKeycenter)` order (Pitfall 5 — preserves two-run byte-identical determinism)"
    - "SfzSampleCache lives on FlowEngine (parallel to Phase 29's SampleCache); lifetime = engine disposal (D-07)"
    - "Idempotency: repeated EagerLoad calls for the same (song, patch) are no-ops (mirror SampleCache.cs:77 pattern)"
    - "SfzRenderer.Render(note, sampleRate, durationBeats, bpm, patch, cache) looks up patch.Grid[midi, vel]; on null walks SortedByPitch[] for nearest pitch + varispeed-shifts via FileIO.VarispeedResample(buffer, Math.Pow(2.0, semitonesShift/12.0)) (zero new resample code per RESEARCH §Don't Hand-Roll)"
    - "Velocity clamp `Math.Clamp((int)Math.Round(note.Velocity * 127.0), 1, 127)` prevents Pitfall 9 (lovel=1 spec default)"
    - "Equal-power 441-frame sin/cos crossfade fires only when region.LoopMode ∈ {LoopContinuous, LoopSustain}; weights are `wA = cos(πt/2N); wB = sin(πt/2N)` per RESEARCH Pattern 5 (constant-power cos² + sin² = 1)"
    - "loop_end clamped at render time via `effectiveLoopEnd = Math.Min(region.LoopEnd, sourceBuffer.Length - 1)` per Pitfall 3 (T-33-LOOP-01 mitigation)"
    - "Phase 28 articulation envelope applied per SPEC-8: SynthUtils.GenerateArticulationADSR with baseAttack = `region.AmpegAttack > 0 ? region.AmpegAttack : 0.005`, baseRelease = `region.AmpegRelease > 0 ? region.AmpegRelease : 0.05`, baseDecay=0.05, baseSustain=1.0, isPercussion=false"
    - "region.Volume (linear, parser-converted) and region.Pan (Flow [-1.0, +1.0], parser-converted) applied to the rendered buffer before the articulation envelope"
    - "On still-null after fallback (no region covers ANY pitch): RenderingDiagnostics.WarnOnce(`sfz:missing:{patch.Description}:{midi}:{vel}`) + return silence buffer (charitable interpretation per CLAUDE.md memory; the song doesn't die from one missing region)"
    - "D-06: When `renderSong song \"sampler:NAME\"` runs, the cache walks the song's note set first, dereferences `Grid[k, v]` for each unique `(pitch, velocity)` cell, collects the set of distinct regions actually needed, then eager-loads only those regions' `.wav` files into a new `SfzSampleCache`. Mirrors Phase 29 `SampleCache` D-13/14/15 pattern."
    - "D-08: Lazy-on-first-use is explicitly rejected — eager-load only. Mid-render disk IO causes stutter (Phase 29 D-14 ruled it out for the same reason). Phase 33 follows the same precedent."
    - "D-19: Pre-computing a 5-second looped buffer per region at sample-load time is rejected (memory cost ~44 MB per patch dominates the ~30 ns/frame compute savings of the per-note crossfade math). Crossfade runs inline inside `SfzRenderer.Render` per D-18."
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs"
      provides: "Per-FlowEngine cache mirroring Phase 29 SampleCache shape"
      contains: "EagerLoad"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      provides: "Sample-based renderer with region match + nearest-pitch fallback + equal-power crossfade + Phase 28 envelope hook"
      contains: "CrossfadeFrames = 441"
    - path: "flow-lang.Tests/Unit/Phase33/SfzRegionMatchTests.cs"
      provides: "SPEC-4 facts: (pitch, vel) routing + nearest-pitch fallback + velocity overlap"
      contains: "TwoRegionOverlap"
    - path: "flow-lang.Tests/Unit/Phase33/SfzLoopCrossfadeTests.cs"
      provides: "SPEC-5 facts: 4-second sustained-note discontinuity ≤ 0.05 + equal-power vs linear spectral centroid"
      contains: "DiscontinuityCheck"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      to: "flow-lang/StandardLibrary/Audio/FileIO.cs"
      via: "VarispeedResample for nearest-pitch fallback (verbatim reuse per RESEARCH §Don't Hand-Roll)"
      pattern: "VarispeedResample"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      to: "flow-lang/StandardLibrary/Audio/SynthUtils.cs"
      via: "Phase 28 articulation envelope (Pattern 6)"
      pattern: "GenerateArticulationADSR"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs"
      to: "flow-lang/StandardLibrary/Audio/FileIO.cs"
      via: "LoadWavInternal for each region's WAV at eager-load time"
      pattern: "LoadWavInternal"
---

<objective>
Wave 3: ship the audio-path core. SfzSampleCache mirrors Phase 29's SampleCache exactly for per-engine WAV memoization. SfzRenderer combines grid lookup (D-01), nearest-pitch fallback (SPEC-4), the equal-power crossfade (SPEC-5, the failure-analyst's worst-case for Phase 34), and the Phase 28 articulation envelope hook (SPEC-8). These two classes are isolated from the broader system — they take SfzData + a note + a cache and return an AudioBuffer. Plan 33-07 wires them into SongRenderer and FlowEngine.

Purpose: SPEC-4 (region matching + nearest-pitch fallback via VarispeedResample), SPEC-5 (441-frame equal-power loop crossfade, the symphony-killing failure mode), SPEC-8 (Phase 28 articulation envelope ON TOP of the SFZ render path).

Output: 2 new audio-path files + 2 new fact suites. SongRenderer integration deferred to Plan 33-07; this plan exercises the renderer via direct method calls in the fact suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-sfz-orchestral-sampler/33-SPEC.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md
@.planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md
@flow-lang/StandardLibrary/Audio/SampleCache.cs
@flow-lang/StandardLibrary/Audio/SampledInstrumentRenderer.cs
@flow-lang/StandardLibrary/Audio/FileIO.cs
@flow-lang/StandardLibrary/Audio/SynthUtils.cs
@flow-lang/StandardLibrary/Audio/PitchConversion.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzLoopMode.cs

<interfaces>
<!-- SfzSampleCache (public surface):
public class SfzSampleCache
{
    public AudioBuffer? GetSample(SfzData patch, string samplePath);
    public AudioBuffer? GetVarispeed(SfzData patch, string samplePath, int semitonesShift);
    public void EagerLoad(SongData song, SfzData patch);  // idempotent
}
-->

<!-- SfzRenderer (public surface):
public class SfzRenderer
{
    public SfzRenderer(SfzSampleCache cache);
    public AudioBuffer Render(MusicalNoteData note, int sampleRate, double durationBeats, double bpm, SfzData patch);
}
-->

<!-- Equal-power crossfade math (verified at RESEARCH Pattern 5):
const int CrossfadeFrames = 441;  // 10 ms at 44.1 kHz, locked by SPEC-5
int loopLen = effectiveLoopEnd - region.LoopStart;
// For each output frame:
//   Map dst → absSrc by wrapping (dst - loop_end) % loopLen + loop_start once past loop_end
//   Within the last 441 frames before effectiveLoopEnd:
//     int crossIndex = CrossfadeFrames - (effectiveLoopEnd - absSrc);
//     float t = (float)crossIndex / CrossfadeFrames;
//     float wA = MathF.Cos(MathF.PI * t / 2.0f);
//     float wB = MathF.Sin(MathF.PI * t / 2.0f);
//     output[dst] = wA * source[absSrc] + wB * source[region.LoopStart + crossIndex];
//   Else: output[dst] = source[Math.Min(absSrc, source.Length - 1)];
-->

<!-- Phase 28 envelope baseline (from SampledInstrumentRenderer.cs:126-130):
float[] envelope = SynthUtils.GenerateArticulationADSR(
    note.Articulation,
    baseAttack:  region.AmpegAttack  > 0 ? region.AmpegAttack  : 0.005,
    baseDecay:                                                   0.05,
    baseSustain:                                                 1.0,
    baseRelease: region.AmpegRelease > 0 ? region.AmpegRelease : 0.05,
    frames: targetFrames, sampleRate: sampleRate, isPercussion: false);
SynthUtils.ApplyEnvelope(fitted, envelope);
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: SfzSampleCache + build-verify gate</name>
  <files>flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs</files>
  <behavior>
    - SfzSampleCache.EagerLoad(song, patch) calls FileIO.LoadWavInternal once per distinct region.SamplePath needed by the song
    - Repeated EagerLoad with the same (song, patch) is a no-op (idempotency)
    - Cache iteration order is sorted by `(SamplePath ordinal, PitchKeycenter ascending)` — deterministic across runs (Pitfall 5)
    - GetSample returns the cached raw buffer; GetVarispeed returns the varispeed-shifted buffer + memoizes the result
    - Cache compiles in isolation (no SfzRenderer dependency)
  </behavior>
  <read_first>
    - flow-lang/StandardLibrary/Audio/SampleCache.cs (entire file — the exact shape to mirror per 33-PATTERNS.md, including: per-engine lifetime, _rawCache + _shiftedCache + _eagerLoadedKeys triple, deterministic OrderBy iteration at lines 87-92, GetVarispeed at lines 149-163)
    - flow-lang/StandardLibrary/Audio/SampledInstrumentRenderer.cs (entire file — the exact Render-method shape to mirror, including: rest short-circuit at lines 73-76, frame-count calc at lines 78-83, targetMidi via PitchConversion.GetMidiNote at line 84, trim-to-duration at lines 116-119, Phase 28 envelope hook at lines 120-132)
    - flow-lang/StandardLibrary/Audio/FileIO.cs (verify LoadWavInternal at line 362 + VarispeedResample at line 338 signatures)
    - flow-lang/StandardLibrary/Audio/SynthUtils.cs (verify GenerateArticulationADSR + ApplyEnvelope + CreateSilence + ToMonoBuffer signatures)
    - flow-lang/StandardLibrary/Audio/PitchConversion.cs (verify GetMidiNote signature)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Pattern 4: Region Matching" + §"Pattern 5: Equal-Power Sustain Loop Crossfade" + §"Pattern 6: Articulation Envelope Hook" + §"Pattern 7: Determinism via Sorted Iteration" + §"Common Pitfalls" 3, 5, 7, 8, 9)
    - .planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md (§"flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs" + §"flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs")
  </read_first>
  <action>Create `flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs` (namespace `FlowLang.StandardLibrary.Audio.Sfz`). Public class with these private fields: `Dictionary<(SfzData patch, string samplePath), AudioBuffer> _rawCache`, `Dictionary<(SfzData patch, string samplePath, int semitonesShift), AudioBuffer> _shiftedCache`, `HashSet<string> _eagerLoadedKeys`. Public method `GetSample(SfzData patch, string samplePath) -> AudioBuffer?`: look up in _rawCache; return null if not loaded. Public method `GetVarispeed(SfzData patch, string samplePath, int semitonesShift) -> AudioBuffer?`: check _shiftedCache first; if absent, look up raw; if raw missing return null; else `FileIO.VarispeedResample(raw, Math.Pow(2.0, semitonesShift / 12.0))` (shift==0 returns raw); cache the shifted result; return it. Public method `EagerLoad(SongData song, SfzData patch) -> void`: build the idempotency key `$"sfz:{patch.GetHashCode()}:{song.GetHashCode()}"`; if already in _eagerLoadedKeys return; walk every section's every sequence's every note in song, compute (midi, vel) per note via PitchConversion, dereference `patch.Grid[midi, vel]`; collect distinct non-null regions into a HashSet; sort the result via `.OrderBy(r => r.SamplePath, StringComparer.Ordinal).ThenBy(r => r.PitchKeycenter)` (Pitfall 5 — critical for two-run determinism); for each region in the sorted enumeration, if not already in _rawCache, load via `FileIO.LoadWavInternal(Path.Combine(patch.BasePath, region.SamplePath))` and insert; finally `_eagerLoadedKeys.Add(idempotencyKey)`. Use the existing `flow-lang/StandardLibrary/Audio/SampleCache.cs` as the structural template — class-doc, field layout, EagerLoad shape, sorted-iteration convention all copied with the appropriate substitutions.</action>
  <verify>
    <automated>dotnet build flow-sharp.sln --logger "console;verbosity=minimal" &amp;&amp; test -f flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs &amp;&amp; grep -q "OrderBy.*SamplePath.*StringComparer.Ordinal" flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs</automated>
  </verify>
  <done>SfzSampleCache.cs compiles in isolation. OrderBy sorted-iteration in cache (Pitfall 5). Build green at end-of-task. SfzRenderer ships in Task 1b.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: SfzRenderer + SfzRegionMatchTests + SfzLoopCrossfadeTests</name>
  <files>flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs, flow-lang.Tests/Unit/Phase33/SfzRegionMatchTests.cs, flow-lang.Tests/Unit/Phase33/SfzLoopCrossfadeTests.cs</files>
  <behavior>
    - SfzRenderer.Render(note=rest) returns a silence buffer of the correct duration
    - SfzRenderer.Render on a note covered by exactly one region returns a buffer whose RMS > 0
    - SfzRenderer.Render on a note OUTSIDE all region coverage triggers WarnOnce(`sfz:missing:...`) + returns silence
    - Velocity 0.0 maps to MIDI velocity 1 (Pitfall 9 clamp)
    - region.Volume linear gain applied (linear 0.5 ≈ -6 dB SFZ → output amplitude halves)
    - region.Pan within Flow [-1.0, +1.0] is respected (pan=1.0 sends all energy to right channel via constant-power; pan=-1.0 to left)
    - region.LoopMode == NoLoop produces a buffer that doesn't loop past the sample body
    - region.LoopMode == LoopContinuous extends the body across the full authored duration AND applies the 441-frame crossfade math
    - loop_end > sourceBuffer.Length is clamped to sourceBuffer.Length - 1 at render time (T-33-LOOP-01)
    - Phase 28 envelope: Staccato articulation produces a measurably shorter audible body than Legato on the same note
    - ampeg_attack=0.5 produces a measurably slower attack than ampeg_attack=0.005 on the same note (SPEC-8)
  </behavior>
  <read_first>
    - flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs (shipped by Task 1a — verify constructor + GetSample + GetVarispeed signatures)
    - (the other reads inherited from Task 1a / the plan-level context block remain in scope; do not re-read identical files)
  </read_first>
  <action>Create `flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs` (namespace `FlowLang.StandardLibrary.Audio.Sfz`). Public class. Constructor `SfzRenderer(SfzSampleCache cache)` stores the cache. Constant `private const int CrossfadeFrames = 441;`. Public method `Render(MusicalNoteData note, int sampleRate, double durationBeats, double bpm, SfzData patch) -> AudioBuffer`. Algorithm: (1) `if (note.IsRest) return SynthUtils.CreateSilence(sampleRate, durationBeats, bpm);` (mirrors SampledInstrumentRenderer.cs:73-76); (2) Frame count via `SynthUtils.BeatsToSeconds(durationBeats, bpm)` + multiplication by sampleRate (mirrors lines 78-83); (3) `int targetMidi = PitchConversion.GetMidiNote(note.NoteName, note.Octave, note.Alteration);` (mirrors line 84); (4) Pitfall 9 clamp: `int vel = Math.Clamp((int)Math.Round(note.Velocity * 127.0), 1, 127);`; (5) Region match: `SfzRegion? region = patch.Grid[targetMidi, vel];`; (6) `int semitonesShift = 0;` — on `region is null`: walk `patch.SortedByPitch[]` for the nearest pitch via a linear scan (acceptable — SortedByPitch is typically &lt; 128 entries); set `int nearestPitch = ...`; `region = patch.Grid[nearestPitch, vel] ?? FindAnyRegionAtPitch(patch, nearestPitch)` (helper that scans grid[nearestPitch, *] for any non-null cell — return the first hit, or null if no velocity slot has coverage at this pitch); on still-null: `RenderingDiagnostics.WarnOnce($"sfz:missing:{patch.Description}:{targetMidi}:{vel}", $"[sfz] no region for ({targetMidi}, {vel}) in '{patch.Description}' — rendered as rest"); return SynthUtils.CreateSilence(sampleRate, durationBeats, bpm);`; otherwise `semitonesShift = targetMidi - nearestPitch;`; (7) Load the sample via `_cache.GetSample(patch, region.SamplePath)` — if null (sample not eager-loaded, shouldn't happen in production but defensive), advisory + silence; (8) Apply varispeed via `_cache.GetVarispeed(patch, region.SamplePath, semitonesShift)`; (9) Sustain loop branch — if `region.LoopMode == SfzLoopMode.LoopContinuous || region.LoopMode == SfzLoopMode.LoopSustain`: run the 441-frame equal-power sin/cos crossfade per the `<interfaces>` block math. Defensive `int effectiveLoopEnd = Math.Min(region.LoopEnd, sourceBuffer.Frames - 1);` per Pitfall 3. NoLoop / OneShot: just copy + zero-pad as SampledInstrumentRenderer.cs:116-119 does; (10) Apply region.Volume (already linear from parser per Pitfall 8) by multiplying every sample; apply region.Pan (already in Flow [-1.0, +1.0] from parser per Pitfall 7) via constant-power stereo split — for a mono source: `leftSample = sample * Math.Cos((pan + 1) * Math.PI / 4); rightSample = sample * Math.Sin((pan + 1) * Math.PI / 4);` — wrap the result in a stereo AudioBuffer if pan != 0, else mono; (11) Phase 28 envelope per the `<interfaces>` block: `SynthUtils.GenerateArticulationADSR(note.Articulation, baseAttack: region.AmpegAttack > 0 ? region.AmpegAttack : 0.005, baseDecay: 0.05, baseSustain: 1.0, baseRelease: region.AmpegRelease > 0 ? region.AmpegRelease : 0.05, frames: targetFrames, sampleRate: sampleRate, isPercussion: false);` then `SynthUtils.ApplyEnvelope(fitted, envelope);`; (12) Return the resulting AudioBuffer via `SynthUtils.ToMonoBuffer` or the stereo equivalent depending on pan. Then create the two fact suites described in the next sub-action.

Create `flow-lang.Tests/Unit/Phase33/SfzRegionMatchTests.cs` (namespace `FlowLang.Tests.Unit.Phase33`). [Collection("FlowScripts")] class with IDisposable + RenderingDiagnostics.ResetForTesting() in ctor + Dispose. Six facts: TwoRegionOverlap (regions covering C4..F4 vs G4..C5; A4 routes to second, D4 to first); VelocityOverlap (lovel 0..63 vs 64..127 at C4); NearestPitchFallback (B5 outside fixture range; renderer falls back + varispeed-shifts; assert RMS > 0 and FFT peak ≈ nearestPitchFreq * 2^(shift/12) within ±5%); MissingRegion_RendersSilence (empty velocity row → silence + advisory once; second call dedupes); VelocityZero_ClampsToOne (Pitfall 9); VolumeOpcode_HalvesAmplitude (-6 dB → output RMS half of 0 dB).

Create `flow-lang.Tests/Unit/Phase33/SfzLoopCrossfadeTests.cs` (same namespace + Collection). Six facts: DiscontinuityCheck (4-second sustained `C4w` on `loop_continuous loop_start=22050 loop_end=44100`; max |output[i+1]-output[i]| ≤ 0.05 across body — SPEC-5 gate); EqualPowerSpectralCheck (compare against in-test linear-crossfade reference; spectral centroid within ±2% — SPEC-5 gate); LoopEndBeyondSampleLength_Clamped (loop_end=999999 on 22050-frame sample does not throw + non-empty output — T-33-LOOP-01); NoLoopMode_DoesNotExtend (4-second render on 1-second sample with no_loop produces 4-second buffer zero past 1s); Articulation_Staccato_ShorterThanLegato (Phase 28 ratio); AmpegAttack_Overrides_Baseline (0.5 vs 0.005; time-to-50%-peak-RMS &gt; 200 ms for 0.5).</action>
  <verify>
    <automated>dotnet build flow-sharp.sln --logger "console;verbosity=minimal" &amp;&amp; test -f flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs &amp;&amp; grep -q "CrossfadeFrames = 441" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs &amp;&amp; grep -q "Math.Min(region.LoopEnd" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzRegionMatchTests|FullyQualifiedName~Phase33.SfzLoopCrossfadeTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>SfzRenderer.cs compiles. CrossfadeFrames = 441 (SPEC-5 locked). loop_end clamp at render time (Pitfall 3). SfzRegionMatchTests (6 facts) + SfzLoopCrossfadeTests (6 facts) all green. SPEC-4 + SPEC-5 + partial SPEC-8 acceptance verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .wav file → FileIO.LoadWavInternal → SfzSampleCache | Untrusted binary content; FileIO handles RIFF/WAVE parsing — existing surface, no new mitigations needed here |
| SfzData.Grid + composer note → SfzRenderer.Render | Already-validated data crosses here; renderer is responsible for bounds clamps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-LOOP-01 | DoS | loop_end &gt; sample length → IndexOutOfRangeException | mitigate | `effectiveLoopEnd = Math.Min(region.LoopEnd, sourceBuffer.Frames - 1)` at the top of the sustain-loop branch in SfzRenderer.Render (Pitfall 3) |
| T-33-MISSING-01 | DoS | hostile SFZ pointing at every (pitch, vel) cell as null → flood of WarnOnce calls | mitigate | RenderingDiagnostics.WarnOnce dedupes per sentinel key (per-patch, per-(midi,vel)); HashSet membership check is O(1) |
| T-33-DET-01 | Tampering | nondeterministic HashSet iteration in eager-load | mitigate | `.OrderBy(r => r.SamplePath, StringComparer.Ordinal).ThenBy(r => r.PitchKeycenter)` before iterating (Pitfall 5) — preserves Phase 18/25/27 two-run byte-identical contract |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~Phase33.SfzRegionMatchTests|FullyQualifiedName~Phase33.SfzLoopCrossfadeTests"` exits 0
- `dotnet test flow-sharp.sln` exits 0 (full suite green; Phase 29 byte-identical contract preserved)
- Per-sample discontinuity check on a 4-second sustained note ≤ 0.05 (SPEC-5 locked)
- Equal-power vs linear spectral centroid within ±2% (SPEC-5 locked)
- All SPEC-4 + SPEC-5 + most of SPEC-8 acceptance criteria green
</verification>

<success_criteria>
- SfzSampleCache mirrors Phase 29 SampleCache shape with idempotent eager-load
- Sorted iteration preserves two-run determinism (Pitfall 5)
- SfzRenderer: grid lookup + nearest-pitch fallback + varispeed (zero new resample code)
- 441-frame equal-power sin/cos crossfade on LoopContinuous + LoopSustain
- loop_end clamp at render time (T-33-LOOP-01)
- Phase 28 articulation envelope ON TOP per SPEC-8
- All 6 SfzRegionMatchTests + all 6 SfzLoopCrossfadeTests green
- Full suite stays green
</success_criteria>

<output>
Create `.planning/phases/33-sfz-orchestral-sampler/33-06-SUMMARY.md` when done.
</output>
