---
phase: 33
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz
  - flow-lang.Tests/fixtures/sfz-smoke/C4_sine.wav
  - flow-lang.Tests/fixtures/sfz-smoke/G5_sine.wav
  - flow-lang.Tests/fixtures/sfz-smoke/LICENSE.md
  - flow-lang.Tests/Tools/Phase33FixtureGenerator.cs
  - flow-lang.Tests/Integration/Phase33/RepoSizeTests.cs
  - .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md
  - .planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md
autonomous: true
requirements: [SPEC-2, SPEC-7]
user_setup: []
must_haves:
  truths:
    - "flow-lang.Tests/fixtures/sfz-smoke/ contains a 2-region synthetic .sfz fixture plus two synthetic sine-burst WAVs totaling < 100 KB on disk"
    - "Phase33FixtureGenerator regenerates the C4_sine.wav and G5_sine.wav bytes from a documented seeded sine-burst recipe (deterministic across runs per Phase 18/25/27 contract)"
    - "RepoSizeTests asserts total fixture-directory size < 100 KB and fails CI if exceeded"
    - "33-VSCO-PATH-AUDIT.md lists the 19 GM orchestral symbols with the VSCO-CE 1.1.0 SFZ-release relative paths (per Assumption A1 from 33-RESEARCH.md), one row per symbol, with the actual on-disk filename verified or explicitly flagged as TBD"
    - "33-VSCO-CONTROL-DECISION.md records whether VSCO-CE 1.1.0 uses the `<control>` header / `default_path=` opcode (RESEARCH Open Question Q3 + Assumption A7); decision rule: if FOUND in the audit, Plan 33-04 extends its opcode whitelist to 14 entries and parses `<control>` as a fourth header; if NOT FOUND, the v1.4 13-opcode subset stands"
    - "D-20: Smoke fixture is a 2-region SFZ — region 1 (MIDI 48..71, C4) with `loop_mode=loop_continuous` + `loop_start=2205` + `loop_end=4410` exercises the crossfade path; region 2 (MIDI 72..127, G5) exercises the nearest-pitch fallback. Stays under SPEC-7 < 100 KB fixture budget."
  artifacts:
    - path: "flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz"
      provides: "2-region synthetic SFZ fixture exercising <global>/<group>/<region> headers, all 13 known opcodes, loop_mode=loop_continuous on region 1, loop_mode=no_loop on region 2"
      contains: "loop_continuous"
    - path: "flow-lang.Tests/fixtures/sfz-smoke/C4_sine.wav"
      provides: "100ms 261.63 Hz sine burst at 44.1 kHz 16-bit mono — region 1 sample (pitch_keycenter=60)"
    - path: "flow-lang.Tests/fixtures/sfz-smoke/G5_sine.wav"
      provides: "100ms 783.99 Hz sine burst at 44.1 kHz 16-bit mono — region 2 sample (pitch_keycenter=79)"
    - path: "flow-lang.Tests/fixtures/sfz-smoke/LICENSE.md"
      provides: "Attribution stub — fixtures are synthetic, generated by Phase33FixtureGenerator from a known seed; no third-party content"
    - path: "flow-lang.Tests/Tools/Phase33FixtureGenerator.cs"
      provides: "Regenerator helper. Pure C# sine-burst synthesis at fixed frequencies + 16-bit PCM WAV writer. Idempotent — second invocation produces byte-identical output"
      contains: "GenerateC4Sine"
    - path: "flow-lang.Tests/Integration/Phase33/RepoSizeTests.cs"
      provides: "CI gate — asserts new fixtures/sfz-smoke/ directory < 100 KB"
      contains: "Phase33"
    - path: ".planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md"
      provides: "VSCO-CE 1.1.0 relative-path audit (Assumption A1) — feeds Plan 33-05's sfz.flow dict entries"
      contains: "violin"
    - path: ".planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md"
      provides: "VSCO-CE 1.1.0 `<control>` / `default_path=` usage decision (Q3 + A7) — feeds Plan 33-04 opcode-whitelist sizing"
      contains_pattern: "(FOUND|NOT FOUND)"
  key_links:
    - from: "flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz"
      to: "flow-lang.Tests/fixtures/sfz-smoke/C4_sine.wav"
      via: "sample= relative path"
      pattern: "sample=C4_sine.wav"
    - from: "flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz"
      to: "flow-lang.Tests/fixtures/sfz-smoke/G5_sine.wav"
      via: "sample= relative path"
      pattern: "sample=G5_sine.wav"
---

<objective>
Wave 0 establishes the test infrastructure every downstream Phase 33 plan needs: the < 100 KB synthetic SFZ smoke fixture (SPEC-7), the regenerator helper that proves byte-identical determinism across runs, the repo-size gate, and the VSCO-CE 1.1.0 relative-path audit that unblocks Plan 33-05's 19-entry GM symbol dict.

Purpose: SPEC-7's CI smoke test renders this fixture; SPEC-3's parser tests load it; SPEC-5's discontinuity test exercises a loop_continuous region from it; SPEC-2's dict in Plan 33-05 needs verified VSCO-CE paths or this phase ships with broken `loadSfz #violin` calls (Assumption A1 is the highest-risk research item).

Output: 3 binary fixture files + 1 .sfz fixture + 1 LICENSE stub + 1 regenerator helper + 1 RepoSize gate test + 1 path-audit artifact under `.planning/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-sfz-orchestral-sampler/33-SPEC.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md
@.planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md
@.planning/phases/33-sfz-orchestral-sampler/33-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- The smoke.sfz contract — every downstream plan's tests reference it. -->
<!-- Region 1: <region> sample=C4_sine.wav pitch_keycenter=60 lokey=48 hikey=71 lovel=1 hivel=127 loop_mode=loop_continuous loop_start=2205 loop_end=4410 -->
<!-- Region 2: <region> sample=G5_sine.wav pitch_keycenter=79 lokey=72 hikey=127 lovel=1 hivel=127 loop_mode=no_loop -->
<!-- <global> sets ampeg_attack=0.005 ampeg_release=0.05; <group> sets volume=0 pan=0 (defaults, exercises inheritance flattening). -->

<!-- Phase33FixtureGenerator exposes: -->
<!-- public static void GenerateC4Sine(string outPath) — 100ms 261.63 Hz sine burst, 44.1 kHz, 16-bit mono PCM -->
<!-- public static void GenerateG5Sine(string outPath) — 100ms 783.99 Hz sine burst, 44.1 kHz, 16-bit mono PCM -->
<!-- Sine formula: sample[i] = (short)(Math.Sin(2 * Math.PI * freq * i / 44100.0) * 0.5 * short.MaxValue) — amplitude 0.5 to leave envelope headroom -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VSCO-CE 1.1.0 path audit (Assumption A1)</name>
  <read_first>
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Assumptions Log" A1, §"Open Questions" Q1; the canonical sgossner/VSCO-2-CE 1.1.0 SFZ-release URL)
    - .planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md (D-09 19-symbol list)
    - .planning/phases/33-sfz-orchestral-sampler/33-SPEC.md (SPEC-2 acceptance criterion — 19 GM symbols listed in target)
  </read_first>
  <action>Two deliverables in this task.

(1) Create `.planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md`. Body: a single Markdown table with columns Symbol | VSCO-CE Relative Path | Confidence | Source. Rows for all 19 GM orchestral symbols locked in SPEC-2 (`#violin`, `#viola`, `#cello`, `#contrabass`, `#flute`, `#oboe`, `#clarinet`, `#bassoon`, `#trumpet`, `#horn`, `#trombone`, `#tuba`, `#piano`, `#harp`, `#timpani`, `#choir`, `#guitar`, `#harpsichord`, `#celeste`). For each row: attempt to verify the actual on-disk filename from the GitHub VSCO-2-CE 1.1.0 SFZ-release tree (https://github.com/sgossner/VSCO-2-CE/tree/SFZ — use WebFetch to list the relevant directory, e.g. `Strings/Violin/`, `Woodwinds/Flute/`, etc.). If verified: Confidence=verified, Source=URL of directory listing. If the file does not exist or the directory naming has changed since 1.1.0: Confidence=TBD, Source="needs composer download". Use the "sustain" variant when multiple articulations exist (e.g. `Strings/Solo Violin/SViolin sus.sfz` if that is the canonical sustain patch). Document a fallback default at the bottom: if a row cannot be verified, Plan 33-05 should ship the best-effort relative path AND register an advisory in the test harness so `(loadSfz #symbol)` errors clearly on a real install. DO NOT make up paths — TBD is the correct answer if verification fails.

(2) Create `.planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md` — answers RESEARCH Open Question Q3 (and Assumption A7) before Plan 33-04 locks its opcode whitelist. Body: (a) Methodology — name the representative VSCO-CE 1.1.0 `.sfz` file(s) probed (pick at least three across categories: e.g. one strings, one brass, one keys); use WebFetch on the raw GitHub `?raw=true` URL OR list the file content via `https://github.com/sgossner/VSCO-2-CE/blob/SFZ/{path}`. (b) Findings — paste any `<control>` block discovered, OR record explicitly that NONE of the probed files contain `<control>` or `default_path=`. (c) Decision — exactly ONE of: **FOUND** (Plan 33-04 MUST extend the opcode whitelist to 14 entries, add `default_path` to the known list, AND parse `<control>` as a fourth header whose `default_path=` cascades into every region's `sample=` lookup at parse time; SPEC-3's "13 listed opcodes" wording is conditionally relaxed) OR **NOT FOUND** (the v1.4 13-opcode subset stands; `<control>` is silently ignored by the existing unknown-header skip path; Plan 33-04 ships as currently specified). (d) Citations — full URL of each probed file. DO NOT skip the probe; if the WebFetch fails, record `NETWORK_FAIL` as the decision and Plan 33-04 must escalate before locking the whitelist.</action>
  <verify>
    <automated>test -f .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md &amp;&amp; test -f .planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md &amp;&amp; grep -E '\b(violin|viola|cello|contrabass|flute|oboe|clarinet|bassoon|trumpet|horn|trombone|tuba|piano|harp|timpani|choir|guitar|harpsichord|celeste)\b' .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md | wc -l | awk '{exit ($1 &gt;= 19) ? 0 : 1}' &amp;&amp; grep -E '(FOUND|NOT FOUND|NETWORK_FAIL)' .planning/phases/33-sfz-orchestral-sampler/33-VSCO-CONTROL-DECISION.md</automated>
  </verify>
  <done>Audit file exists and contains all 19 GM symbols with either a verified path or an explicit TBD marker. Plan 33-05 consumes this artifact to populate the sfz.flow dict. The control-decision file resolves Q3 with one of FOUND/NOT FOUND/NETWORK_FAIL; if FOUND, Plan 33-04 MUST extend its opcode whitelist to 14 entries before locking the parser.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic smoke fixture + regenerator helper</name>
  <read_first>
    - flow-lang.Tests/Tools/ (note: directory may not exist yet — create it if needed; check siblings for existing analog helpers in flow-lang.Tests/Helpers/ for namespace conventions)
    - flow-lang.Tests/flow-lang.Tests.csproj (verify xunit.v3 + .NET 10 target so the helper compiles cleanly)
    - flow-lang/StandardLibrary/Audio/FileIO.cs (verify the WAV-write byte layout this fixture must round-trip through — RIFF/WAVE/fmt/data chunks, 16-bit PCM, 44.1 kHz mono)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Code Examples" Example 4 — the smoke.sfz body)
    - .planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md (smoke-fixture analog table row)
  </read_first>
  <action>Create `flow-lang.Tests/Tools/Phase33FixtureGenerator.cs` (namespace `FlowLang.Tests.Tools`) exposing two public static methods `GenerateC4Sine(string outPath)` and `GenerateG5Sine(string outPath)`. Each method writes a 4410-frame (100 ms at 44.1 kHz) 16-bit mono PCM WAV. Sample formula: `short sample = (short)(Math.Sin(2.0 * Math.PI * freq * i / 44100.0) * 0.5 * short.MaxValue)` with `freq=261.6256` for C4 and `freq=783.9909` for G5 (equal-tempered A=440). Write a standard 44-byte RIFF/WAVE header (RIFF + WAVE + fmt subchunk (16 bytes, PCM=1, channels=1, sampleRate=44100, byteRate=88200, blockAlign=2, bitsPerSample=16) + data subchunk (8 bytes + 8820 bytes payload)). Use `BinaryWriter` over `FileStream`. The generator must be byte-deterministic — run twice, same bytes. Then run the generator once from a top-level `[Fact]` named `Phase33FixtureGenerator_Smoke_GeneratesFixtures` that writes both WAVs to `flow-lang.Tests/fixtures/sfz-smoke/` and asserts each output exceeds 8 KB and is under 20 KB; that fact also writes `smoke.sfz` using the exact content from 33-RESEARCH.md §"Code Examples" Example 4 (verbatim — `<global> ampeg_attack=0.005 ampeg_release=0.05` + `<group> volume=0 pan=0` + region 1 with `sample=C4_sine.wav pitch_keycenter=60 lokey=48 hikey=71 lovel=1 hivel=127 loop_mode=loop_continuous loop_start=2205 loop_end=4410` + region 2 with `sample=G5_sine.wav pitch_keycenter=79 lokey=72 hikey=127 lovel=1 hivel=127 loop_mode=no_loop`); run the fact ONCE manually with `dotnet test --filter "Phase33FixtureGenerator_Smoke_GeneratesFixtures"` and commit the resulting .wav + .sfz files. Create `flow-lang.Tests/fixtures/sfz-smoke/LICENSE.md` with a single-paragraph attribution: "Synthetic test fixtures generated by `flow-lang.Tests/Tools/Phase33FixtureGenerator.cs` from documented sine-burst recipe. No third-party content. Regenerate via `dotnet test --filter Phase33FixtureGenerator_Smoke_GeneratesFixtures` — output is byte-deterministic across runs."</action>
  <verify>
    <automated>test -f flow-lang.Tests/Tools/Phase33FixtureGenerator.cs &amp;&amp; test -f flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz &amp;&amp; test -f flow-lang.Tests/fixtures/sfz-smoke/C4_sine.wav &amp;&amp; test -f flow-lang.Tests/fixtures/sfz-smoke/G5_sine.wav &amp;&amp; test -f flow-lang.Tests/fixtures/sfz-smoke/LICENSE.md &amp;&amp; grep -q "loop_continuous" flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz &amp;&amp; grep -q "sample=C4_sine.wav" flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz</automated>
  </verify>
  <done>Five fixture-directory files exist (smoke.sfz + 2 WAVs + LICENSE.md + the generator at flow-lang.Tests/Tools/). The generator compiles under `dotnet build flow-lang.Tests/flow-lang.Tests.csproj`. The .sfz contains the verbatim 2-region body from 33-RESEARCH Example 4.</done>
</task>

<task type="auto">
  <name>Task 3: RepoSizeTests gate</name>
  <read_first>
    - flow-lang.Tests/Integration/Phase29/ (check for existing RepoSizeTests-style fact for the Samples/ directory — the 5 MB cap pattern referenced in CLAUDE.md and 33-SPEC.md)
    - .planning/phases/33-sfz-orchestral-sampler/33-SPEC.md (constraints: "fixtures < 100 KB" + acceptance criterion "Phase 33 in-repo artifacts < 100 KB")
    - .planning/phases/33-sfz-orchestral-sampler/33-VALIDATION.md (Wave 0 RepoSizeTests entry)
  </read_first>
  <action>Create `flow-lang.Tests/Integration/Phase33/RepoSizeTests.cs` (namespace `FlowLang.Tests.Integration.Phase33`). Single `[Fact] public void SfzSmokeFixturesDirectory_TotalBytes_Under100Kb()`. Logic: locate the fixture directory via the same `FindRepoRoot` helper pattern lifted from `flow-lang.Tests/Unit/Phase32/ScalaParserFacts.cs` (walk up from `AppContext.BaseDirectory` looking for `flow-lang.Tests/fixtures`), join with `sfz-smoke`, enumerate files recursively, sum `.Length`. Assert total &lt; 102_400 bytes (100 KB). Helpful failure message: `"sfz-smoke fixtures total {n} bytes — exceeds 100 KB cap per SPEC-7. Trim or compress."`. Do NOT use `Directory.GetFiles` without a recursive option; use `Directory.EnumerateFiles(dir, "*", SearchOption.AllDirectories)`.</action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.RepoSizeTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>RepoSizeTests fact exists and passes (fixture directory under 100 KB). The fact runs in &lt; 1 second.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer .sfz file → SfzParser | Untrusted file content crosses here at parse time (Task 2's smoke.sfz is the canonical safe input; downstream plans will exercise the same gate with hostile inputs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-PARSE-01 | DoS | smoke.sfz fixture size | accept | Fixture is hand-authored at 2 regions; not a vector for the 10000-region cap until Plan 33-04 ships the cap |
| T-33-FIXTURE-01 | Tampering | committed binary WAVs | mitigate | Regenerator helper is deterministic; CI can verify the bytes match a known-seeded reproduction if the determinism contract is ever in doubt |
| T-33-SIZE-01 | DoS | committed fixture directory size | mitigate | RepoSizeTests fact in Task 3 fails CI if the directory exceeds 100 KB |
</threat_model>

<verification>
- `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.RepoSizeTests"` exits 0
- Manual: `du -sh flow-lang.Tests/fixtures/sfz-smoke/` reports &lt; 100 KB
- Manual: re-run `dotnet test --filter "Phase33FixtureGenerator_Smoke_GeneratesFixtures"` on a clean checkout and verify the WAV bytes match — the generator is the regression net for Phase 18/25/27's two-run byte-identical contract
- 33-VSCO-PATH-AUDIT.md is a planning artifact; not validated by tests, but Plan 33-05 will read it
</verification>

<success_criteria>
- 5 files in flow-lang.Tests/fixtures/sfz-smoke/ totaling &lt; 100 KB
- Phase33FixtureGenerator.cs compiles + the seed fact runs successfully
- RepoSizeTests fact exists and passes
- 33-VSCO-PATH-AUDIT.md exists with 19 rows
</success_criteria>

<output>
Create `.planning/phases/33-sfz-orchestral-sampler/33-01-SUMMARY.md` when done. Include: files created, .sfz content checksum, .wav byte counts, audit-row count (verified vs TBD).
</output>
