---
phase: 33
plan: 07
type: execute
wave: 4
depends_on:
  - 33-02
  - 33-05
  - 33-06
files_modified:
  - flow-lang/Interpreter/Interpreter.cs
  - flow-lang/Core/FlowEngine.cs
  - flow-lang/StandardLibrary/Audio/SongRenderer.cs
  - flow-lang/StandardLibrary/Audio/MidiExport.cs
  - flow-lang.Tests/Integration/Phase33/SfzBindingTests.cs
  - flow-lang.Tests/Integration/Phase33/SfzMidiExportTests.cs
autonomous: true
requirements: [SPEC-1, SPEC-6]
must_haves:
  truths:
    - "Interpreter.ExecuteVariableDeclaration writes (name, sfzData) into ExecutionContext.SfzPatchRegistry when `varDecl.Type is SfzType && value.Data is SfzData` — single branch BEFORE the existing _context.DeclareVariable call (D-12)"
    - "FlowEngine.CurrentSfzSampleCache static accessor and FlowEngine.CurrentExecutionContext static accessor both ship (mirrors Phase 29's FlowEngine.CurrentSampleCache pattern per RESEARCH Assumption A4)"
    - "FlowEngine constructor instantiates an SfzSampleCache + assigns both statics; Dispose clears both statics if they still point at this engine's instances"
    - "Plan 33-05 already shipped the `SfzBuiltins.Register(internalRegistry, _context);` call in FlowEngine.cs (immediately after `ScalaBuiltins.Register(internalRegistry);`). Plan 33-07 verifies presence via grep ONLY — it does NOT re-add this line."
    - "SongRenderer.RenderSong recognizes `instrument.StartsWith(\"sampler:\")` BEFORE the existing per-instrument dispatch; strips the prefix; reads ExecutionContext.SfzPatchRegistry[name]; on miss throws UnknownSamplerNameError with known-names list + a `Did you forget Sfz {name} = (loadSfz #...)?` hint (D-13)"
    - "Sampler dispatch calls FlowEngine.CurrentSfzSampleCache.EagerLoad(song, patch) BEFORE walking sections; the per-note render goes through new SfzRenderer(cache).Render(...) and reuses the existing per-section mixing pipeline"
    - "MidiExport.ResolveGmProgram strips the `sampler:` prefix as the FIRST statement after the empty-name check (Pitfall 6 — prevents bleeding through to GM-0 fallback)"
    - "MidiExport ships 12 new GM-program entries per CONTEXT D-16: violin→40, viola→41, cello→42, contrabass→43, oboe→68, clarinet→71, bassoon→70, horn→60, trombone→57, tuba→58, timpani→47 (channel 9), choir→52, harp→46, guitar→24, harpsichord→6, celeste→8"
    - "MidiExport track-name meta-event emits the prefix-stripped name (e.g. `violin`, NOT `sampler:violin`) per D-17"
    - "Phase 29 byte-identical contract preserved: existing `renderSong song \"piano\"` produces identical output before vs after Phase 33 (regression test passes)"
    - "SamplerDispatch_WithoutImport_Errors now passes (the fact deferred from Plan 33-05 lands here in SfzBindingTests.cs)"
    - "D-14: `ExecutionContext.SfzPatchRegistry` is a flat name→patch map keyed by variable name (top-level / file-scope Sfz bindings only). Cross-frame lookups for proc-local Sfz bindings are out of scope per CONTEXT decision; anonymous `(loadSfz #violin) -> renderSong \"...\"` flow is explicitly deferred per SPEC out-of-scope."
    - "D-15: `MidiExport` strips the `sampler:` prefix from the instrument string and looks up the remaining name in the existing GM-program dict — MIDI export of a symphony works without VSCO-CE installed on the receiver (they get a GM-compatible file with sensible instrument programs)."
  artifacts:
    - path: "flow-lang/Interpreter/Interpreter.cs"
      provides: "Single branch in ExecuteVariableDeclaration populating SfzPatchRegistry on SfzType binding"
      contains: "SfzPatchRegistry"
    - path: "flow-lang/Core/FlowEngine.cs"
      provides: "SfzSampleCache field + CurrentSfzSampleCache static + CurrentExecutionContext static + Dispose cleanup. (SfzBuiltins.Register was shipped by Plan 33-05 — this plan verifies it via grep only.)"
      contains: "CurrentSfzSampleCache"
    - path: "flow-lang/StandardLibrary/Audio/SongRenderer.cs"
      provides: "sampler: prefix branch BEFORE existing instrument dispatch"
      contains: "sampler:"
    - path: "flow-lang/StandardLibrary/Audio/MidiExport.cs"
      provides: "sampler: prefix strip + 12 new GM-program entries"
      contains: "violin"
    - path: "flow-lang.Tests/Integration/Phase33/SfzBindingTests.cs"
      provides: "SPEC-6 facts: typed binding + sampler dispatch + unknown-name error + non-zero RMS render"
      contains: "Render_NonEmpty"
    - path: "flow-lang.Tests/Integration/Phase33/SfzMidiExportTests.cs"
      provides: "MIDI export prefix-strip + GM-program lookup facts"
      contains: "SamplerPrefix_Stripped"
  key_links:
    - from: "flow-lang/Interpreter/Interpreter.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "ExecuteVariableDeclaration writes to ExecutionContext.SfzPatchRegistry on SfzType binding"
      pattern: "SfzPatchRegistry\\["
    - from: "flow-lang/StandardLibrary/Audio/SongRenderer.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      via: "sampler: branch dispatches to SfzRenderer.Render"
      pattern: "SfzRenderer"
    - from: "flow-lang/Core/FlowEngine.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs"
      via: "Engine owns the cache; static accessor exposes to SongRenderer"
      pattern: "CurrentSfzSampleCache"
---

<objective>
Wave 4: thread the SFZ surface into the existing audio + MIDI pipelines. After this plan lands, `Sfz violin = (loadSfz #violin)` followed by `renderSong song "sampler:violin"` actually produces audio (SPEC-6 end-to-end), and `writeMidi` of a sampler-instrument song emits the correct GM-program track (D-15/D-16/D-17). The Phase 29 byte-identical contract holds because every change is an additive branch BEFORE the existing dispatch — no edits to the bundled-sample path.

Purpose: SPEC-6 (typed-Sfz binding + sampler:NAME dispatch + unknown-name error), SPEC-1 closure (the SamplerDispatch_WithoutImport_Errors fact deferred from Plan 33-05 lands here).

Output: 4 modified production files (each gets ONE narrowly-scoped branch) + 2 new integration test files. Note: the `SfzBuiltins.Register(internalRegistry, _context);` call in FlowEngine.cs was already shipped by Plan 33-05 — Plan 33-07 ADDS the cache field + statics + Dispose cleanup to the same file and verifies the registration line is present via grep but does NOT re-add it (duplicate-edit avoidance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-sfz-orchestral-sampler/33-SPEC.md
@.planning/phases/33-sfz-orchestral-sampler/33-CONTEXT.md
@.planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md
@.planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md
@flow-lang/Interpreter/Interpreter.cs
@flow-lang/Core/FlowEngine.cs
@flow-lang/StandardLibrary/Audio/SongRenderer.cs
@flow-lang/StandardLibrary/Audio/MidiExport.cs
@flow-lang/StandardLibrary/Audio/SampleCache.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
@flow-lang/Runtime/ExecutionContext.cs
@flow-lang/Runtime/Value.cs
@flow-lang/TypeSystem/SpecialTypes/SfzType.cs

<interfaces>
<!-- Interpreter typed-binding hook insertion site (Interpreter.cs ~line 638-646, per 33-PATTERNS.md):
public void ExecuteVariableDeclaration(VariableDeclaration varDecl)
{
    ...
    // Convert if needed
    if (!value.Type.Equals(varDecl.Type) && value.Type.CanConvertTo(varDecl.Type))
    {
        value = value.ConvertTo(varDecl.Type);
    }

    // ===== Phase 33 D-12: insert this branch BEFORE _context.DeclareVariable =====
    if (varDecl.Type is FlowLang.TypeSystem.SpecialTypes.SfzType &&
        value.Data is FlowLang.StandardLibrary.Audio.Sfz.SfzData sfzData)
    {
        _context.SfzPatchRegistry[varDecl.Name] = sfzData;
    }
    // ============================================================================

    _context.DeclareVariable(varDecl.Name, value);
}
-->

<!-- FlowEngine.cs divergences (per 33-PATTERNS.md analog):
- Add `private SfzSampleCache _sfzSampleCache;` field
- Add `public static SfzSampleCache? CurrentSfzSampleCache { get; private set; }`
- Add `public static ExecutionContext? CurrentExecutionContext { get; private set; }`
- In constructor (after _context = new RuntimeContext(...)):
    _sfzSampleCache = new SfzSampleCache();
    CurrentSfzSampleCache = _sfzSampleCache;
    CurrentExecutionContext = _context;
- DO NOT re-add `SfzBuiltins.Register(internalRegistry, _context);` — Plan 33-05 already shipped it.
  This plan VERIFIES presence via grep only.
- Dispose: mirror lines 213-216 for both new statics
-->

<!-- SongRenderer.RenderSong sampler: branch (per 33-RESEARCH Example 3):
public static Value RenderSong(IReadOnlyList<Value> args)
{
    var song = args[0].As<SongData>();
    string synthType = (string)args[1].Data!;

    SynthUtils.ResetNoiseRng();

    // ===== Phase 33 D-13: sampler: dispatch BEFORE Phase 29 bundled path =====
    if (synthType.StartsWith("sampler:", StringComparison.Ordinal))
    {
        string patchName = synthType.Substring("sampler:".Length);
        var ctx = FlowEngine.CurrentExecutionContext;
        if (ctx is null || !ctx.SfzPatchRegistry.TryGetValue(patchName, out var patch))
            throw new InvalidOperationException(
                $"Unknown sampler patch '{patchName}'. " +
                $"Known: [{string.Join(", ", ctx?.SfzPatchRegistry.Keys ?? Enumerable.Empty<string>())}]. " +
                $"Did you forget `Sfz {patchName} = (loadSfz #...)`?");

        var cache = FlowEngine.CurrentSfzSampleCache!;
        cache.EagerLoad(song, patch);

        var renderer = new SfzRenderer(cache);
        AudioBuffer result = new AudioBuffer(0, StereoChannels, DefaultSampleRate);
        foreach (var sectionRef in song.Sections)
        {
            if (!song.SectionRegistry.TryGetValue(sectionRef.Name, out var sectionData))
                throw new InvalidOperationException(...);
            var sectionBuffer = RenderSectionWithSfz(sectionData, renderer, patch);
            result = AudioMix(result, sectionBuffer);  // same mix path as Phase 29
        }
        return Value.Buffer(result);
    }
    // ==========================================================================

    FlowEngine.CurrentSampleCache?.EagerLoad(song, synthType);
    // ... existing Phase 29 path
}
-->

<!-- MidiExport.ResolveGmProgram divergence (per Pitfall 6):
private static (int gmProgram, int channel) ResolveGmProgram(string seqName)
{
    if (string.IsNullOrEmpty(seqName)) return (0, 0);
    string lower = seqName.ToLowerInvariant();

    // ===== Phase 33 D-15: strip sampler: BEFORE any StartsWith check =====
    if (lower.StartsWith("sampler:")) lower = lower.Substring("sampler:".Length);
    // ====================================================================

    // ===== Phase 33 D-16: 12 new entries — MORE-SPECIFIC NAMES FIRST =====
    if (lower.StartsWith("violin"))      return (40, 0);
    if (lower.StartsWith("viola"))       return (41, 0);
    if (lower.StartsWith("cello"))       return (42, 0);
    if (lower.StartsWith("contrabass"))  return (43, 0);
    if (lower.StartsWith("oboe"))        return (68, 0);
    if (lower.StartsWith("clarinet"))    return (71, 0);
    if (lower.StartsWith("bassoon"))     return (70, 0);
    // horn → 60 (French horn) MUST come BEFORE the existing brass/horn → 56 entry
    if (lower.StartsWith("horn"))        return (60, 0);
    if (lower.StartsWith("trombone"))    return (57, 0);
    if (lower.StartsWith("tuba"))        return (58, 0);
    if (lower.StartsWith("timpani"))     return (47, 9);  // channel 9 — percussion
    if (lower.StartsWith("choir"))       return (52, 0);
    if (lower.StartsWith("harp"))        return (46, 0);
    if (lower.StartsWith("guitar"))      return (24, 0);
    if (lower.StartsWith("harpsichord")) return (6, 0);
    if (lower.StartsWith("celeste"))     return (8, 0);
    // ====================================================================

    // existing Phase 28 entries (UNCHANGED ordering)
    if (lower.StartsWith("piano"))                                  return (0, 0);
    if (lower.StartsWith("brass"))                                  return (56, 0);
    if (lower.StartsWith("sax"))                                    return (65, 0);
    if (lower.StartsWith("flute"))                                  return (73, 0);
    if (lower.StartsWith("string"))                                 return (48, 0);
    if (lower.StartsWith("organ"))                                  return (19, 0);
    if (lower.StartsWith("bell"))                                   return (14, 0);
    if (lower.StartsWith("drum"))                                   return (0, 9);
    return (0, 0);
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interpreter typed-binding + FlowEngine cache + statics + Dispose (NOT SfzBuiltins.Register — that ships in Plan 33-05)</name>
  <read_first>
    - flow-lang/Interpreter/Interpreter.cs (read ExecuteVariableDeclaration body — lines 588-647 per 33-PATTERNS.md; the insertion site is line ~646, BEFORE the `_context.DeclareVariable(varDecl.Name, value);` call)
    - flow-lang/Core/FlowEngine.cs (read lines 30-90 — CurrentSampleCache static at line ~54 + ctor body where _sampleCache is created + the existing `ScalaBuiltins.Register(internalRegistry);` and `SfzBuiltins.Register(internalRegistry, _context);` lines around line ~74 (the second was shipped by Plan 33-05); also read lines 200-220 for Dispose pattern)
    - flow-lang/Runtime/ExecutionContext.cs (verify SfzPatchRegistry field type from merged Plan 33-02 is already strongly typed `Dictionary<string, FlowLang.StandardLibrary.Audio.Sfz.SfzData>`; no tightening needed)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs (verify default-ctor + EagerLoad signature)
    - .planning/phases/33-sfz-orchestral-sampler/33-PATTERNS.md (§"flow-lang/Interpreter/Interpreter.cs" + §"flow-lang/Core/FlowEngine.cs")
  </read_first>
  <action>Patch `flow-lang/Interpreter/Interpreter.cs` ExecuteVariableDeclaration: insert the D-12 typed-binding branch per the `<interfaces>` block — BEFORE `_context.DeclareVariable(varDecl.Name, value);`. Use the exact pattern shown: `if (varDecl.Type is FlowLang.TypeSystem.SpecialTypes.SfzType && value.Data is FlowLang.StandardLibrary.Audio.Sfz.SfzData sfzData) { _context.SfzPatchRegistry[varDecl.Name] = sfzData; }`. Add an inline comment `// Phase 33 D-12: register typed-Sfz bindings in the patch registry so renderSong song "sampler:violin" can find the bound patch by name.`. Reassignment-overwrite is naturally handled by Dictionary indexer semantics (Pitfall 10's last-bound-wins contract). Patch `flow-lang/Core/FlowEngine.cs` — ADDITIVE edits only (Plan 33-05 already shipped `SfzBuiltins.Register(internalRegistry, _context);` immediately after `ScalaBuiltins.Register(internalRegistry);`; verify the line is present via grep BEFORE making any further edits — if grep `-q "SfzBuiltins.Register(internalRegistry, _context);" flow-lang/Core/FlowEngine.cs` returns non-zero, STOP and inspect what shipped — do NOT re-add the line under any circumstance): (1) Add `private readonly SfzSampleCache _sfzSampleCache;` field; (2) Add `public static SfzSampleCache? CurrentSfzSampleCache { get; private set; }` and `public static ExecutionContext? CurrentExecutionContext { get; private set; }` static properties next to the existing CurrentSampleCache; (3) In the constructor, right after `_context = new RuntimeContext(...)` (verify exact var name): add `_sfzSampleCache = new SfzSampleCache(); CurrentSfzSampleCache = _sfzSampleCache; CurrentExecutionContext = _context;`; (4) In Dispose, mirror the existing CurrentSampleCache cleanup: `if (ReferenceEquals(CurrentSfzSampleCache, _sfzSampleCache)) CurrentSfzSampleCache = null; if (ReferenceEquals(CurrentExecutionContext, _context)) CurrentExecutionContext = null;`. ExecutionContext.SfzPatchRegistry is already strongly typed as `Dictionary<string, FlowLang.StandardLibrary.Audio.Sfz.SfzData>` from merged Plan 33-02 — no tightening needed here.</action>
  <verify>
    <automated>dotnet build flow-sharp.sln --logger "console;verbosity=minimal" &amp;&amp; grep -q "SfzPatchRegistry\[varDecl.Name\]" flow-lang/Interpreter/Interpreter.cs &amp;&amp; grep -q "CurrentSfzSampleCache" flow-lang/Core/FlowEngine.cs &amp;&amp; grep -q "CurrentExecutionContext" flow-lang/Core/FlowEngine.cs &amp;&amp; grep -q "SfzBuiltins.Register" flow-lang/Core/FlowEngine.cs &amp;&amp; [ $(grep -c "SfzBuiltins.Register" flow-lang/Core/FlowEngine.cs) -eq 1 ]</automated>
  </verify>
  <done>Interpreter writes to SfzPatchRegistry on SfzType binding. FlowEngine has both new statics + cache lifecycle. The SfzBuiltins.Register line that Plan 33-05 added is still present (grep gate asserts EXACTLY ONE occurrence — duplicate-edit prevention). Build green. (SfzPatchRegistry is already strongly typed from merged Plan 33-02.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SongRenderer sampler: dispatch + SfzBindingTests</name>
  <files>flow-lang/StandardLibrary/Audio/SongRenderer.cs, flow-lang.Tests/Integration/Phase33/SfzBindingTests.cs (NOTE: flow-lang.Tests/Integration/Phase29/RmsBaselineTests.cs is verified-against in &lt;verify&gt; but NOT modified by this plan — it is the Phase 29 byte-identical regression gate)</files>
  <behavior>
    - Render_NonEmpty: with `use "@sfz"` + sfz_root set + a valid .sfz file (using flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz as the source via the String overload to bypass VSCO-CE dependency), the script `Sfz v = (loadSfz "/abs/path/to/smoke.sfz"); Song s = [...]; Buffer mix = (renderSong s "sampler:v");` produces a Buffer with frames > 0 and RMS > 0
    - UnknownName_Errors: `renderSong song "sampler:doesnotexist"` throws an InvalidOperationException whose message contains `Unknown sampler patch` AND `Did you forget Sfz doesnotexist`
    - SamplerDispatch_WithoutImport_Errors: without `use "@sfz"`, `renderSong song "sampler:violin"` errors. **This assertion lives HERE in SfzBindingTests.cs (Plan 33-07).** Plan 33-05 deliberately omitted any stub for this fact — single-location ownership.
    - Phase29_BundledPath_Unchanged: render an existing Phase 29 song with `renderSong song "piano"`; compare output bytes to a captured pre-Plan-07 baseline (the existing Phase29.RmsBaselineTests is the regression net — assert it stays GREEN; this fact runs that suite as part of the verify command)
    - LastBoundWins: declare `Sfz v = (loadSfz "smoke.sfz")` then `Sfz v = (loadSfz "other.sfz")`; render `sampler:v` → uses second patch (Pitfall 10 documented contract)
  </behavior>
  <read_first>
    - flow-lang/StandardLibrary/Audio/SongRenderer.cs (read entire RenderSong method body lines 97-260; locate the RenderSection helper + the AudioMix / stereo accumulation pattern that the sampler branch must mirror)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs (verify Render signature from Plan 33-06)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs (verify EagerLoad signature from Plan 33-06)
    - flow-lang.Tests/Integration/Phase29/SampledInstrumentSmokeTests.cs (the integration-test pattern with FlowEngineRunner + cwd guards per 33-PATTERNS.md)
    - flow-lang.Tests/Integration/Phase29/RmsBaselineTests.cs (the Phase 29 byte-identical regression gate — read to understand the assertion shape; this test file is verified-against in the automated step but is NOT modified by this plan)
    - flow-lang.Tests/fixtures/sfz-smoke/smoke.sfz (Plan 33-01 fixture used by the Render_NonEmpty fact)
  </read_first>
  <action>Patch `flow-lang/StandardLibrary/Audio/SongRenderer.cs` RenderSong method: insert the D-13 sampler: dispatch BEFORE the existing `FlowEngine.CurrentSampleCache?.EagerLoad(song, synthType);` line — exactly per the `<interfaces>` block. Add a private static helper `RenderSectionWithSfz(SectionData sectionData, SfzRenderer renderer, SfzData patch)` that mirrors the existing `RenderSection(sectionData, synthType)` shape but invokes `renderer.Render(note, sampleRate, durationBeats, bpm, patch)` instead of the SynthesizerFactory dispatch. Read the existing RenderSection body carefully — copy the mixing math, the per-voice accumulation, the panning, anything section-level. The sampler path produces stereo `AudioBuffer`s; verify stereo handling matches what the renderer emits (Plan 33-06 returns a stereo buffer when pan != 0, mono otherwise; SongRenderer's downstream mix expects stereo). Add inline comment `// Phase 33 D-13: sampler:NAME dispatch — reads ExecutionContext.SfzPatchRegistry; eager-loads via FlowEngine.CurrentSfzSampleCache; per-note render via SfzRenderer. Phase 29 bundled-sample path below stays untouched (byte-identical contract).`. Create `flow-lang.Tests/Integration/Phase33/SfzBindingTests.cs` (namespace `FlowLang.Tests.Integration.Phase33`). [Collection("FlowScripts")] with IDisposable + RenderingDiagnostics.ResetForTesting. Implement the 5 facts listed in `<behavior>`. Render_NonEmpty uses FlowEngineRunner with a script body: `use "@sfz"` import + `tempo 120 { Sfz v = (loadSfz "{absPathToSmokeSfz}") section demo { Sequence main = | C4q D4q E4q F4q | } Song s = [demo] Buffer mix = (renderSong s "sampler:v") }` — substitute the absolute path to the Plan 33-01 smoke fixture for `{absPathToSmokeSfz}`. Then via the runner's variable inspection API (check FlowEngineRunner for the analog of Plan 33-05's tests) verify `mix.Frames > 0` and compute RMS. UnknownName_Errors expects the runner to surface the InvalidOperationException OR translate it into the FlowEngine error surface — verify how Phase 32's UnknownSamplerNameError style is currently reported and assert on the message contents. Phase29_BundledPath_Unchanged uses `Assert.Equal` on the bytes of an existing Phase 29 .wav baseline OR delegates to a one-liner `Assert.True(BaselineMatches("renderSong-piano", sampleScript))` against a captured baseline — minimum viable assertion is that `dotnet test --filter "FullyQualifiedName~Phase29.RmsBaselineTests"` stays green after this plan lands (run that test as part of the verify step).</action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzBindingTests|FullyQualifiedName~Phase29.RmsBaselineTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>SfzBindingTests green (5 facts including SamplerDispatch_WithoutImport_Errors). Phase29.RmsBaselineTests still green (Phase 29 bundled-sample path byte-identical). SongRenderer has the D-13 sampler branch BEFORE the existing Phase 29 path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MidiExport prefix-strip + 12 new GM entries</name>
  <files>flow-lang/StandardLibrary/Audio/MidiExport.cs, flow-lang.Tests/Integration/Phase33/SfzMidiExportTests.cs</files>
  <behavior>
    - SamplerPrefix_Stripped: ResolveGmProgram("sampler:violin") returns (40, 0); without the strip it would route to (0, 0) — proves the strip is at the TOP of the function (Pitfall 6)
    - SamplerPrefix_Flute_RoutesCorrectly: ResolveGmProgram("sampler:flute") returns (73, 0) — proves the strip happens BEFORE the existing flute check
    - NewEntry_Violin: ResolveGmProgram("violin") returns (40, 0)
    - NewEntry_Cello: ResolveGmProgram("cello") returns (42, 0)
    - NewEntry_Timpani_Channel9: ResolveGmProgram("timpani") returns (47, 9) — timpani uses MIDI channel 9 (drum channel) per CONTEXT D-16
    - NewEntry_Horn_BeatsBrass: ResolveGmProgram("horn") returns (60, 0) — the more-specific `horn` MUST come BEFORE the existing `brass` check (which also matches `horn` historically per Phase 28's brass/horn → 56 group)
    - ExistingEntry_Brass_Unchanged: ResolveGmProgram("brass") still returns (56, 0)
    - ExistingEntry_Piano_Unchanged: ResolveGmProgram("piano") still returns (0, 0)
    - TrackName_StripsSamplerPrefix: a written MIDI file's track-name meta-event for `sampler:violin` reads `"violin"` (D-17)
    - Phase28_MidiExport_ByteIdentical: an existing Phase 28 writeMidi test produces byte-identical output before vs after this plan (regression — Phase 28's MIDI suite must stay green)
  </behavior>
  <read_first>
    - flow-lang/StandardLibrary/Audio/MidiExport.cs (read entire file — locate ResolveGmProgram at lines 60-73 per 33-PATTERNS.md; locate track-name meta-event emission site that uses the seqName — search for `MetaEventType.SequenceTrackName` or similar)
    - flow-lang.Tests/Unit/Phase28/ (search for existing MidiExport unit tests for the writeMidi regression baseline)
    - .planning/phases/33-sfz-orchestral-sampler/33-RESEARCH.md (§"Common Pitfalls" 6 — MIDI Export Prefix-Strip Order; D-15, D-16, D-17)
  </read_first>
  <action>Patch `flow-lang/StandardLibrary/Audio/MidiExport.cs` ResolveGmProgram per the `<interfaces>` block. The prefix-strip MUST be the first statement after the empty-name check (Pitfall 6). The 12 new entries MUST come BEFORE the existing entries because `horn` would otherwise be swallowed by the existing `brass` check (Phase 28 brass→56 entry covered both `brass` and `horn` — Phase 33 reassigns `horn → 60`/French horn per D-16). Verify ordering by reading the existing entries carefully — the new entries that have specific names (violin, viola, etc.) just slot in alphabetically; `horn` is the only entry that shadows an existing pattern. Locate the track-name meta-event emission site (search for `SequenceTrackName` or `track.Events.Add(new TextEvent(...))` patterns — DryWetMidi uses `SequenceTrackNameEvent`); update the value passed to the event to be the prefix-stripped name. The simplest implementation: a private helper `private static string StripSamplerPrefix(string name) => name.StartsWith("sampler:", StringComparison.Ordinal) ? name.Substring("sampler:".Length) : name;` and use it both inside ResolveGmProgram AND at the track-name emission site. Create `flow-lang.Tests/Integration/Phase33/SfzMidiExportTests.cs` (namespace FlowLang.Tests.Integration.Phase33). [Collection("FlowScripts")]. Implement the 10 facts listed in `<behavior>`. For ResolveGmProgram facts: invoke the (currently private) method via either making it `internal` and adding [InternalsVisibleTo("flow-lang.Tests")] to flow-lang.csproj OR via a public test-hook method like `MidiExport.ResolveGmProgramForTesting(seqName)`. For TrackName_StripsSamplerPrefix: write a MIDI file via the FlowEngineRunner with a sampler instrument, read the file back via DryWetMidi or a minimal manual MIDI track-name extractor, assert the meta-event payload is the stripped name. For Phase28_MidiExport_ByteIdentical: locate the existing Phase 28 byte-identical MIDI test (search Phase28ByteIdenticalPragmaTests.cs or similar from STATE.md timeline) and ensure it stays green.</action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase33.SfzMidiExportTests" --logger "console;verbosity=minimal" &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase28" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>SfzMidiExportTests all green. Phase 28 MIDI tests stay green (byte-identical contract preserved). MidiExport.cs has the prefix-strip at the top + 12 new entries before the existing entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer flow script → SongRenderer instrument string | Already-validated string; only the `sampler:` prefix path is new |
| ExecutionContext.SfzPatchRegistry → SongRenderer dispatch | Internal state crossing; no external untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-DISPATCH-01 | Information Disclosure | UnknownSamplerNameError listing registry keys | accept | Composer-controlled variable names; same posture as Phase 32 listing TuningStack keys in error messages |
| T-33-MIDI-01 | Tampering | sampler: prefix leaking through to GM-0 fallback | mitigate | Prefix-strip is the FIRST statement after empty-name check (Pitfall 6); test SamplerPrefix_Flute_RoutesCorrectly proves the order is correct |
| T-33-REG-01 | Tampering | Phase 29 byte-identical regression | mitigate | All Phase 33 changes are additive branches BEFORE the existing dispatch; Phase29.RmsBaselineTests is the gate (verified in Task 2's automated check) |
| T-33-DUP-01 | Tampering | duplicate `SfzBuiltins.Register` insertion across Plan 33-05 and Plan 33-07 | mitigate | Task 1's verify gate asserts `grep -c "SfzBuiltins.Register" flow-lang/Core/FlowEngine.cs` equals 1 — duplicate insertion is caught by the build-step grep |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~Phase33.SfzBindingTests|FullyQualifiedName~Phase33.SfzMidiExportTests"` exits 0
- `dotnet test --filter "FullyQualifiedName~Phase29.RmsBaselineTests"` exits 0 (Phase 29 byte-identical regression net)
- `dotnet test --filter "FullyQualifiedName~Phase28"` exits 0 (Phase 28 MIDI export byte-identical)
- `dotnet test flow-sharp.sln --logger "console;verbosity=minimal"` exits 0 (full suite)
- `grep -c "SfzBuiltins.Register" flow-lang/Core/FlowEngine.cs` returns 1 (duplicate-edit prevention; the line ships from Plan 33-05 and this plan must not re-add it)
</verification>

<success_criteria>
- Interpreter writes SfzPatchRegistry on SfzType binding
- FlowEngine ships CurrentSfzSampleCache + CurrentExecutionContext statics with proper lifecycle
- Plan 33-05's `SfzBuiltins.Register(internalRegistry, _context);` call is still present in FlowEngine.cs as a SINGLE occurrence (no duplicate from this plan)
- SongRenderer has sampler: branch BEFORE existing path
- MidiExport strips sampler: prefix + 12 new GM entries with correct ordering
- All facts green: SfzBindingTests + SfzMidiExportTests
- Phase 28 + Phase 29 byte-identical contracts preserved
- After this plan: SPEC-1 + SPEC-6 acceptance criteria all green
</success_criteria>

<output>
Create `.planning/phases/33-sfz-orchestral-sampler/33-07-SUMMARY.md` when done.
</output>
