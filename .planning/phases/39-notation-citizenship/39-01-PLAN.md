---
plan: 39-01
phase: 39
wave: 0
depends_on: []
files_modified:
  - flow-lang/Vendor/MusicXmlSchemas/
  - flow-lang/Vendor/MusicXmlSchemas/LICENSE
  - flow-lang/Vendor/MusicXmlSchemas/VENDORED-FROM.md
  - flow-lang/StandardLibrary/Notation/InstrumentRouting.cs
  - flow-lang/StandardLibrary/Notation/NotationIoBuiltins.cs
  - flow-lang/StandardLibrary/Notation/MusicXmlExport.cs
  - flow-lang/StandardLibrary/Notation/ArticulationEmit.cs
  - flow-lang/StandardLibrary/Audio/MidiExport.cs
  - flow-lang/notation-io.flow
  - flow-lang/flow-lang.csproj
  - flow-lang/Core/FlowEngine.cs
  - flow-lang.Tests/Integration/Phase39/VendoredSourceLicenseTests.cs
  - flow-lang.Tests/Integration/Phase39/MusicXmlExportTests.cs
  - flow-lang.Tests/Integration/Phase39/MusicXmlRoundTripTests.cs
autonomous: true
requirements:
  - XML-01
  - XML-02
---

<objective>
Wave 0 dependency root. Ship the `@notation-io` stdlib module skeleton, vendor
`sightreader/musicxml-schemas` POCOs (or skip the vendor if XDocument suffices for the
round-trip CI gate), extract `InstrumentRouting` from `MidiExport.cs` per D-39-20, and
implement `(writeMusicXML song "piece.musicxml")` builtin per XML-01 with the round-trip
CI gate per XML-02 (charitable skip when `mscore` absent per D-39-08).
</objective>

<must_haves>
- M1: `flow-lang/Vendor/MusicXmlSchemas/LICENSE` exists with verbatim MIT text + `VENDORED-FROM.md` carries upstream URL + commit SHA + date OR the entire vendor is skipped (if XDocument alone suffices). License-audit xUnit test (`VendoredSourceLicenseTests`) GREEN in both cases.
- M2: `flow-lang/StandardLibrary/Notation/InstrumentRouting.cs` exists with public `ResolveGmProgram(string)` matching `MidiExport.ResolveGmProgram` 17-entry table byte-for-byte. `MidiExport.cs` delegates to it (no logic duplication). Existing Phase 28 / Phase 33 MIDI byte-identical tests still PASS.
- M3: `flow-lang/StandardLibrary/Notation/NotationIoBuiltins.cs` registers `writeMusicXML(String, Song) → Void` builtin via `Register(InternalFunctionRegistry)` called from `FlowEngine.cs`. The `@notation-io` activation gate (`ExecutionContext.NotationIoEnabled`) flips on `use "@notation-io"` import.
- M4: `MusicXmlExport.WriteMusicXml(filepath, songData)` emits MusicXML 3.1 partwise via `XmlWriter` (NOT `XmlSerializer` — Pitfall 6 determinism). Output is two-run cmp-clean: same input → byte-identical output across runs.
- M5: Multi-track `Song` produces multi-`<part>` MusicXML; voice blocks within a sequence produce per-`<note><voice>N</voice>` tagging; articulation emit consumes Phase 35 pattern match per D-39-21 + Pitfall 5 (explicit enumeration of all 7 `Articulation` values, NO `_` wildcard).
- M6: Round-trip CI gate test (`MusicXmlRoundTripTests`) runs `mscore --convert-to mxl` when binary in PATH; fires `[xml] mscore not found — round-trip gate skipped` stderr advisory and returns Pass when absent (D-39-08).
- M7: `XML-01` and `XML-02` test files exist and all GREEN under `dotnet test --filter "FullyQualifiedName~Phase39"`.

**Truths (cross-cutting constraints):**
- D-v1.5-05: charitable interpretation — `mscore` absence MUST NOT fail the round-trip gate.
- D-v1.5-08: articulation decision table — Accent→`<accent/>`, Marcato→`<strong-accent/>`, Staccato→`<staccato/>`, Tenuto→`<tenuto/>`, Sforzando→`<dynamics><sfz/></dynamics>`, Legato→slur spans NOT per-note.
- D-39-07: Legato slur grouping — runs of ≥2 consecutive Legato notes within the same voice become one `<slur number="N">` span (per-voice incrementing); single Legato notes get no slur.
- D-39-09: each `Sequence` in a `Song` becomes one `<part>`; voice blocks → `<voice>N</voice>`.
- D-39-10: `.musicxml` extension default; no `.mxl` compression.
- D-39-20: extract `InstrumentRouting` from `MidiExport.ResolveGmProgram` — single source of truth for all notation formats.
- D-39-21: articulation emit uses Phase 35 `(match articulation | ...)` pattern matching site.
- Two-run cmp-clean determinism: `XmlWriter` with fixed `NewLineChars = "\n"` + `UTF8Encoding(false)` no-BOM.
</must_haves>

<tasks>
  <task id="39-01-T0" type="execute">
    <action>
      Create directory `flow-lang.Tests/Integration/Phase39/` (mkdir; tracked by git via the first test file added in T7).
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs
    </read_first>
    <acceptance_criteria>
      - `ls flow-lang.Tests/Integration/Phase39/` succeeds (directory exists).
    </acceptance_criteria>
  </task>

  <task id="39-01-T1" type="execute">
    <action>
      Decide on MusicXML POCO vendoring. Open Question OQ1 in 39-RESEARCH.md asks: do we need the POCOs at all, or is `XDocument` structural diff enough for the XML-02 round-trip test? **DECISION CRITERIA:** if the round-trip test only checks note-count + duration-sum + articulation presence preservation, `XDocument` is sufficient and we skip the vendor entirely (saves 1MB + 1 license-audit gate). Vendor the POCOs only if we plan to deserialize MuseScore's output into typed objects.

      For Phase 39 v1.5, **SKIP THE VENDOR** — `XDocument.Load(filepath)` walking `<score-partwise>/part/measure/note` is sufficient for the round-trip gate. Document this decision in `flow-lang/Vendor/README.md` (new file) explaining why the directory is currently empty (it stays as a placeholder for ABCSharp in Plan 39-03).

      Create `flow-lang/Vendor/README.md` with the rationale:
      - Header: "Vendored Sources"
      - One paragraph explaining the discipline (`LICENSE` + `VENDORED-FROM.md` per source)
      - Note: "MusicXmlSchemas: NOT vendored (Plan 39-01 decision — XDocument suffices for round-trip diff)"
      - Note: "ABCSharp: vendored in Plan 39-03 — see flow-lang/Vendor/ABCSharp/"

      Create `flow-lang/Vendor/.gitkeep` to ensure the directory is tracked.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/.planning/phases/39-notation-citizenship/39-RESEARCH.md (Open Questions section, OQ1)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs (vendor-discipline analog)
    </read_first>
    <acceptance_criteria>
      - `flow-lang/Vendor/README.md` exists with the rationale.
      - `flow-lang/Vendor/.gitkeep` exists.
      - `flow-lang/Vendor/MusicXmlSchemas/` does NOT exist (decision: skip).
    </acceptance_criteria>
  </task>

  <task id="39-01-T2" type="execute">
    <action>
      Extract `ResolveGmProgram` (and `StripSamplerPrefix`) from `flow-lang/StandardLibrary/Audio/MidiExport.cs:99-140` into a new file `flow-lang/StandardLibrary/Notation/InstrumentRouting.cs` (namespace `FlowLang.StandardLibrary.Notation`). Preserve the EXACT 17-entry table + ordering (Phase 33 D-16 horn-before-brass + Phase 28 entries unchanged). Modify `MidiExport.ResolveGmProgram` to delegate: `public static (int, int) ResolveGmProgram(string s) => InstrumentRouting.ResolveGmProgram(s);` (preserves public method signature for backwards compat). Same delegation for `StripSamplerPrefix`.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Audio/MidiExport.cs (lines 1-140, esp. the ResolveGmProgram + StripSamplerPrefix methods + their XML doc comments which document D-15/D-16 contract)
    </read_first>
    <acceptance_criteria>
      - `flow-lang/StandardLibrary/Notation/InstrumentRouting.cs` exists.
      - Contains `public static (int gmProgram, int channel) ResolveGmProgram(string seqName)` with all 17 entries.
      - Contains `public static string StripSamplerPrefix(string name)`.
      - `MidiExport.ResolveGmProgram` delegates: source-grep `grep "InstrumentRouting.ResolveGmProgram" flow-lang/StandardLibrary/Audio/MidiExport.cs` returns at least one match.
      - `dotnet build flow-lang/flow-lang.csproj` succeeds clean.
      - All existing Phase 28 / Phase 33 MIDI tests still PASS: `dotnet test --filter "FullyQualifiedName~Phase28|FullyQualifiedName~Phase33" --no-restore`.
    </acceptance_criteria>
  </task>

  <task id="39-01-T3" type="execute">
    <action>
      Create `flow-lang/StandardLibrary/Notation/ArticulationEmit.cs` (namespace `FlowLang.StandardLibrary.Notation`). Provides static `string ToMusicXml(Articulation a)` returning the inner-tag content per D-v1.5-08:
        - `Articulation.Accent` → `"<accent/>"`
        - `Articulation.Marcato` → `"<strong-accent/>"`
        - `Articulation.Staccato` → `"<staccato/>"`
        - `Articulation.Tenuto` → `"<tenuto/>"`
        - `Articulation.Sforzando` → null (Sforzando emits `<dynamics><sfz/></dynamics>` separately, NOT inside `<articulations>` — caller handles)
        - `Articulation.Legato` → null (slurs are NOT per-note; D-39-07 grouping handles spans)
        - `Articulation.Normal` → null

      Implementation MUST be a C# `switch` expression (Pitfall 5 — exhaustiveness guaranteed by the compiler; NO `_` wildcard). If a new `Articulation` value is added later, the compiler raises a warning forcing manual update.

      Also expose `static bool RequiresSlur(Articulation a) => a == Articulation.Legato;` for Plan 39-01's slur grouping logic + Plan 39-02's LilyPond reuse.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/NoteType.cs (lines 200-215, Articulation enum definition)
      - /home/noah/Desktop/projects/flow-sharp-phase39/.planning/phases/39-notation-citizenship/39-CONTEXT.md (D-v1.5-08 articulation table reference; D-39-21 Phase 35 match contract; D-39-07 Legato slur grouping)
    </read_first>
    <acceptance_criteria>
      - File `flow-lang/StandardLibrary/Notation/ArticulationEmit.cs` exists.
      - Source contains `public static string? ToMusicXml(Articulation a)`.
      - Source contains `public static bool RequiresSlur(Articulation a)`.
      - Implementation uses C# `switch` expression (no `_` wildcard) — verify with `grep "_ =>" flow-lang/StandardLibrary/Notation/ArticulationEmit.cs` returns no matches.
      - `dotnet build flow-lang/flow-lang.csproj` succeeds clean.
    </acceptance_criteria>
  </task>

  <task id="39-01-T4" type="execute">
    <action>
      Create `flow-lang/StandardLibrary/Notation/MusicXmlExport.cs` (namespace `FlowLang.StandardLibrary.Notation`) implementing `public static void WriteMusicXml(string filepath, SongData song)`. Walks the same `SongData → Sections → Sequences → Bars → Notes` tree as `MidiExport.ExportMidiInternal`.

      Output format: MusicXML 3.1 partwise (DOCTYPE `score-partwise PUBLIC "-//Recordare//DTD MusicXML 3.1 Partwise//EN" "http://www.musicxml.org/dtds/partwise.dtd"`).

      Use `System.Xml.XmlWriter` with these `XmlWriterSettings`:
        - `Indent = true`, `IndentChars = "  "`, `NewLineChars = "\n"`, `NewLineHandling = NewLineHandling.Replace`
        - `OmitXmlDeclaration = false`
        - `Encoding = new UTF8Encoding(encoderShouldEmitUTF8Identifier: false)` (no BOM)

      Element structure (skeleton):
        - `<score-partwise version="3.1">`
        - `<part-list>` containing one `<score-part id="P{N}"><part-name>{seqName}</part-name></score-part>` per unique Sequence
        - one `<part id="P{N}">` per Sequence; first `<measure number="1">` carries `<attributes>` with `<divisions>N</divisions>` (use `MidiExport.ComputeRequiredTpqn(song)` via reflection or expose the method as `internal`), `<key><fifths>{N}</fifths><mode>{major|minor}</mode></key>` (reuse `MidiExport.KeySignatureMap` — expose as `internal`), `<time><beats>{n}</beats><beat-type>{d}</beat-type></time>`, `<clef><sign>G</sign><line>2</line></clef>`
        - subsequent `<measure number="K">` blocks (one per Bar)
        - each `<note>` carries `<pitch><step>{NoteName}</step><alter>{alter|cents/100.0}</alter><octave>{octave}</octave></pitch>` (or `<rest/>` for rests), `<duration>{ticks}</duration>`, `<voice>{N}</voice>`, `<type>{quarter|eighth|...}</type>`, `<notations><articulations>...</articulations></notations>` per ArticulationEmit
        - Sforzando: emit `<direction placement="above"><direction-type><dynamics><sfz/></dynamics></direction-type></direction>` BEFORE the `<note>`
        - Legato slur grouping per D-39-07: scan each voice's notes; runs of ≥2 consecutive `Articulation.Legato` → one `<slur number="N" type="start"/>` on the first note + `<slur number="N" type="stop"/>` on the last; N increments per-voice per-part.

      Make `MidiExport.ComputeRequiredTpqn` + `MidiExport.KeySignatureMap` `internal` (or expose via `internal` accessor methods) so the new file can reach them without duplicating logic. If they're already `private`, change to `internal` (add `[InternalsVisibleTo("flow-lang.Tests")]` already exists per csproj).

      Voice blocks: when `bar.ParallelVoices != null && bar.ParallelVoices.Count > 0`, emit each voice's notes with `<voice>N</voice>` (N = parallel voice index + 1); otherwise `<voice>1</voice>` for all notes.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Audio/MidiExport.cs (lines 142-179 for ComputeRequiredTpqn; lines 182-225 for KeySignatureMap; lines 342-622 for the SongData walk pattern)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/SongType.cs
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/SectionType.cs
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/SequenceType.cs
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/BarType.cs
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/NoteType.cs (MusicalNoteData + Articulation enum)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Notation/InstrumentRouting.cs (T2 output)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Notation/ArticulationEmit.cs (T3 output)
    </read_first>
    <acceptance_criteria>
      - File `flow-lang/StandardLibrary/Notation/MusicXmlExport.cs` exists.
      - Public method `public static void WriteMusicXml(string filepath, SongData song)`.
      - Source uses `XmlWriter` (not `XmlSerializer`) — verify with `grep "XmlSerializer" flow-lang/StandardLibrary/Notation/MusicXmlExport.cs` returns nothing.
      - Source uses `NewLineChars = "\n"` for cross-platform determinism — verify with `grep 'NewLineChars = "\\\\n"' flow-lang/StandardLibrary/Notation/MusicXmlExport.cs` returns at least one match.
      - `dotnet build flow-lang/flow-lang.csproj` succeeds clean.
    </acceptance_criteria>
  </task>

  <task id="39-01-T5" type="execute">
    <action>
      Create `flow-lang/StandardLibrary/Notation/NotationIoBuiltins.cs` (namespace `FlowLang.StandardLibrary.Notation`). Modeled on `flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs`. Provides:

      - `public static void Register(InternalFunctionRegistry registry, ExecutionContext context)` — wires:
        - `writeMusicXML(String, Song) → Void` builtin (calls `MusicXmlExport.WriteMusicXml(args[0].As<string>(), args[1].As<SongData>())`; checks `context.NotationIoEnabled` at call time, throws clear error "writeMusicXML requires `use \"@notation-io\"`" when unset)
        - `__enableNotationIoModule()` marker builtin: sets `context.NotationIoEnabled = true`
      - Plan 39-02 / 39-03 / 39-04 add `writeLilyPond`, `abc`, `mml` registrations to the same `Register` method.

      Add `public bool NotationIoEnabled` property to `flow-lang/Runtime/ExecutionContext.cs` (default `false`). Mirror `SfzEnabled` pattern.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs (full file, registration pattern + module gate)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/Runtime/ExecutionContext.cs (find SfzEnabled, add NotationIoEnabled alongside)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Notation/MusicXmlExport.cs (T4 output, called from writeMusicXML)
    </read_first>
    <acceptance_criteria>
      - `flow-lang/StandardLibrary/Notation/NotationIoBuiltins.cs` exists.
      - Contains `public static void Register(InternalFunctionRegistry, ExecutionContext)`.
      - `flow-lang/Runtime/ExecutionContext.cs` has new `public bool NotationIoEnabled` field/property.
      - `dotnet build flow-lang/flow-lang.csproj` succeeds clean.
    </acceptance_criteria>
  </task>

  <task id="39-01-T6" type="execute">
    <action>
      Create `flow-lang/notation-io.flow` (NEW stdlib module file). Modeled on `flow-lang/sfz.flow`:
        - Header `Note: Notation IO — opt-in via use "@notation-io"`
        - `use "@std"` (for forward decls)
        - `internal proc __enableNotationIoModule ()` — marker builtin
        - `internal proc writeMusicXML (String: path, Song: song)`
        - Forward decls for `writeLilyPond`, `abc`, `mml` (these get implementations in Plans 39-02/39-03/39-04; declaring them here keeps the module surface stable)
        - Trailing `(__enableNotationIoModule)` call to flip the runtime gate at import time

      Modify `flow-lang/flow-lang.csproj` to add `<None Update="notation-io.flow"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None>` so the file is published to the build output (matches the `sfz.flow` / `notation.flow` / etc. entries).

      Modify `flow-lang/Core/FlowEngine.cs` to call `NotationIoBuiltins.Register(internalRegistry, _context);` in the constructor between the existing `SfzBuiltins.Register(...)` (line 173) and `StyleRegistry.RegisterBuiltinsOnly(...)` (line 184). Add an XML-doc comment block above the call documenting the Phase 39 D-39-01 module activation pattern.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/sfz.flow (full file, layout template)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/flow-lang.csproj (current `<None Update>` entries)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/Core/FlowEngine.cs (lines 160-200, registration site)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Notation/NotationIoBuiltins.cs (T5 output)
    </read_first>
    <acceptance_criteria>
      - `flow-lang/notation-io.flow` exists with `__enableNotationIoModule` marker proc + 4 forward decls + trailing init call.
      - `flow-lang/flow-lang.csproj` contains `<None Update="notation-io.flow">` block.
      - `flow-lang/Core/FlowEngine.cs` contains `NotationIoBuiltins.Register(internalRegistry, _context);` call.
      - `dotnet build flow-lang/flow-lang.csproj` succeeds clean.
      - Smoke: `dotnet run --project flow-interpreter -- -e 'use "@notation-io"; (print "ok")'` outputs `ok` cleanly (verifies module loads).
    </acceptance_criteria>
  </task>

  <task id="39-01-T7" type="execute">
    <action>
      Create `flow-lang.Tests/Integration/Phase39/VendoredSourceLicenseTests.cs`. Modeled on `LicenseAuditTests.cs`. Asserts:

      - `[Fact] VendorReadme_Exists()`: `flow-lang/Vendor/README.md` exists.
      - `[Fact] MusicXmlSchemas_IntentionallyNotVendored_DocumentedInReadme()`: README content contains `"MusicXmlSchemas: NOT vendored"` substring (sentinel for Plan 39-01 T1 decision).
      - Reserve `[Fact] ABCSharp_HasLicense()` and `[Fact] ABCSharp_HasVendoredFromMd()` stubs marked `[Fact(Skip="Plan 39-03")]` — these light up when ABCSharp ships.

      Use the `FlowScriptData.FindTestsRoot()` helper as `LicenseAuditTests` does.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs (full file, pattern + helper usage)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang.Tests/FlowScriptData.cs (find the FindTestsRoot helper signature)
    </read_first>
    <acceptance_criteria>
      - File `flow-lang.Tests/Integration/Phase39/VendoredSourceLicenseTests.cs` exists.
      - Contains `[Fact] public void VendorReadme_Exists()`.
      - Contains `[Fact] public void MusicXmlSchemas_IntentionallyNotVendored_DocumentedInReadme()`.
      - Contains `[Fact(Skip = "Plan 39-03")] public void ABCSharp_HasLicense()`.
      - `dotnet test --filter "FullyQualifiedName~Phase39.VendoredSourceLicenseTests" --no-restore` returns exit 0 with 2 PASS + 1 SKIP.
    </acceptance_criteria>
  </task>

  <task id="39-01-T8" type="execute">
    <action>
      Create `flow-lang.Tests/Integration/Phase39/MusicXmlExportTests.cs`. xUnit tests gating XML-01 emit correctness. Cover:

      1. `[Fact] EmitsWellFormedXml_FromTrivialSong()`: build a minimal `SongData` programmatically (one section, one sequence, 4 quarter notes C4 D4 E4 F4 in 4/4 at 120 BPM in C major), call `MusicXmlExport.WriteMusicXml(tempPath, song)`, assert the file exists and parses as XML via `XDocument.Load(tempPath)` without throwing.
      2. `[Fact] EmitsMultipartScore_FromMultiSequenceSong()`: build a `SongData` with 2 sequences ("piano", "violin") each with notes; assert the output `XDocument` contains exactly 2 `<score-part>` elements under `<part-list>` and exactly 2 `<part>` elements at the root.
      3. `[Fact] TwoRunCmpClean_ByteIdentical()`: write the same `SongData` to two tempfiles in succession; assert `File.ReadAllBytes(a).SequenceEqual(File.ReadAllBytes(b))`.
      4. `[Fact] ArticulationsEmitPerD_v1_5_08_Table()`: build a SongData with one note per `Articulation` value (Accent, Marcato, Staccato, Tenuto, Sforzando — Legato handled separately by Test 5); assert each expected XML tag (`<accent/>`, `<strong-accent/>`, `<staccato/>`, `<tenuto/>`, `<sfz/>`) appears at least once in the output text.
      5. `[Fact] LegatoSlurGrouping_PerD_39_07()`: build a SongData with 3 consecutive Legato notes followed by 1 non-Legato; assert `<slur number="1" type="start"/>` appears AT MOST once (on the first Legato) and `<slur number="1" type="stop"/>` appears AT MOST once (on the third Legato). A SongData with a single isolated Legato note → assert NO `<slur` tag at all (single-Legato gets no slur per D-39-07).
      6. `[Fact] VoiceBlocks_EmitVoiceTags()`: build a SongData with one bar containing two parallel voices (use `bar.ParallelVoices`); assert the output contains both `<voice>1</voice>` and `<voice>2</voice>`.
      7. `[Fact] InstrumentRoutingDelegatesToShared()`: simple smoke that `InstrumentRouting.ResolveGmProgram("violin")` returns `(40, 0)` and `MidiExport.ResolveGmProgram("violin")` returns the same — both go through the shared table.

      Use `Path.GetTempFileName()` + `File.Delete` in `try`/`finally` for tempfile lifecycle. All tests in `public class MusicXmlExportTests`, namespace `FlowLang.Tests.Integration.Phase39`.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Notation/MusicXmlExport.cs (T4 output, public surface)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/SongType.cs (SongData constructors)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/SectionType.cs (SectionData constructors)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/SequenceType.cs (SequenceData constructors)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/BarType.cs (BarData constructors + ParallelVoices)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/TypeSystem/SpecialTypes/NoteType.cs (MusicalNoteData constructors + Articulation values)
    </read_first>
    <acceptance_criteria>
      - File `flow-lang.Tests/Integration/Phase39/MusicXmlExportTests.cs` exists with 7 `[Fact]` methods.
      - `dotnet test --filter "FullyQualifiedName~Phase39.MusicXmlExportTests" --no-restore` exits 0 with all 7 PASS.
    </acceptance_criteria>
  </task>

  <task id="39-01-T9" type="execute">
    <action>
      Create `flow-lang.Tests/Integration/Phase39/MusicXmlRoundTripTests.cs`. xUnit tests gating XML-02 round-trip per D-39-08.

      - `[Fact] CharitableSkipWhenMscoreAbsent()`: write a MusicXML file via `MusicXmlExport.WriteMusicXml`. Probe `mscore` availability via `Process.Start("which", "mscore")` (Linux) / `command -v mscore` (cross-platform via `where.exe` on Windows — use `RuntimeInformation.IsOSPlatform(OSPlatform.Windows)` to dispatch). When ABSENT, capture stderr (Console.SetError redirect) and assert the `[xml] mscore not found — round-trip gate skipped` advisory fires; test PASSES.
      - `[Fact(Skip = "requires mscore in PATH")] StructuralPreservation_NoteCountMatches()`: when mscore IS available, run `mscore --convert-to mxl <flow-output.musicxml> <converted.mxl>`, unzip the .mxl (`System.IO.Compression`), read the inner `.musicxml`, parse via `XDocument`, count `<note>` elements; assert the count matches the original Flow-emitted file's count. This test is marked `[Fact(Skip=...)]` for now since CI on the dev box doesn't have mscore; it lights up automatically when CI provisions one.
      - Both tests in `public class MusicXmlRoundTripTests` in namespace `FlowLang.Tests.Integration.Phase39`.

      For the stderr capture, use the standard xUnit pattern: `var originalErr = Console.Error; var sw = new StringWriter(); Console.SetError(sw); try { ... assert sw.ToString().Contains(advisory) ... } finally { Console.SetError(originalErr); }`. Since `RenderingDiagnostics.WarnOnce` writes to `Console.Error`, the test catches the advisory.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/StandardLibrary/Notation/MusicXmlExport.cs (T4 — write-target for the round-trip)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/Diagnostics/RenderingDiagnostics.cs (find WarnOnce signature + stderr output behavior)
    </read_first>
    <acceptance_criteria>
      - File `flow-lang.Tests/Integration/Phase39/MusicXmlRoundTripTests.cs` exists.
      - Contains `[Fact] CharitableSkipWhenMscoreAbsent()`.
      - Contains `[Fact(Skip = "requires mscore in PATH")] StructuralPreservation_NoteCountMatches()`.
      - `dotnet test --filter "FullyQualifiedName~Phase39.MusicXmlRoundTripTests" --no-restore` exits 0 with 1 PASS + 1 SKIP (on dev box without mscore).
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  <vc id="V1">All 39-01 acceptance criteria met (T0-T9 each verified individually).</vc>
  <vc id="V2">`dotnet build` clean across entire solution: `dotnet build` (no `--no-restore`) returns 0 with no warnings beyond pre-existing baseline.</vc>
  <vc id="V3">All Phase 39 xUnit tests PASS: `dotnet test --filter "FullyQualifiedName~Phase39" --no-restore` returns 0.</vc>
  <vc id="V4">All pre-existing Phase 28 / Phase 33 MIDI byte-identical tests still PASS (M2 backwards-compat invariant): `dotnet test --filter "FullyQualifiedName~Phase28|FullyQualifiedName~Phase33" --no-restore` returns 0.</vc>
  <vc id="V5">Smoke: composer-facing single-line `use "@notation-io"; (print "ok")` runs cleanly via `dotnet run --project flow-interpreter`.</vc>
</verification>
