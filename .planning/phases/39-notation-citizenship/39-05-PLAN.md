---
plan: 39-05
phase: 39
wave: 2
depends_on: [39-01, 39-02, 39-03, 39-04]
files_modified:
  - examples/notation/README.md
  - examples/notation/to_musicxml.flow
  - examples/notation/to_lilypond.flow
  - examples/notation/from_abc.flow
  - examples/notation/from_mml.flow
  - tests/test_notation_to_musicxml_example.flow
  - tests/test_notation_to_lilypond_example.flow
  - tests/test_notation_from_abc_example.flow
  - tests/test_notation_from_mml_example.flow
  - CLAUDE.md
  - .planning/phases/39-notation-citizenship/39-VERIFICATION.md
autonomous: true
requirements:
  - XML-01
  - XML-02
  - LILY-01
  - ABC-01
  - ABC-02
  - MML-01
---

<objective>
Closer plan. Ship the 4 tutorial chapters under `examples/notation/` per D-39-22, the 4
composer-facing regression tests under `tests/test_notation_*_example.flow`, the
documentation sweep in `CLAUDE.md` to add `@notation-io` to the Standard Library Modules
table, and the phase `39-VERIFICATION.md` confirming all 6 REQs delivered.

Per the parent task brief, this plan **does NOT update `.planning/STATE.md`** — the
worktree-isolation posture is that STATE.md is reconciled by hand on merge to dev.
ROADMAP.md and REQUIREMENTS.md updates are also deferred to the merge step (the parent
agent will sweep them).
</objective>

<must_haves>
- M1: `examples/notation/README.md` introduces the 4 chapters + lists `mscore` / `lilypond` as optional reproduction prerequisites.
- M2: Each of the 4 tutorial chapters (`to_musicxml.flow`, `to_lilypond.flow`, `from_abc.flow`, `from_mml.flow`) is a self-contained runnable demo that exercises the respective builtin and `(print)`s a success message.
- M3: Each tutorial doubles as a regression test under `tests/test_notation_*_example.flow` using the Phase 35 `(test "name" lazy(...))` framework (TEST-01 / TEST-02). Each test is two-run cmp-clean deterministic (same input → byte-identical or RMS-within-tolerance output).
- M4: `CLAUDE.md` "Standard Library Modules (.flow files)" section adds the line `- notation-io.flow — single-module surface for MusicXML/LilyPond export + ABC/MML import (Phase 39; use "@notation-io")`. The "Language Features → Music-Specific" section adds a brief Phase 39 paragraph mentioning the 4 builtins + the vendoring decision (ABCSharp hand-rolled, MusicXmlSchemas skipped per Plan 39-01/03 decisions).
- M5: `39-VERIFICATION.md` exists with status `passed`, REQs ✅, test counts, and the merge-guidance one-liner per parent task brief.
- M6: All 6 REQ IDs (XML-01, XML-02, LILY-01, ABC-01, ABC-02, MML-01) covered by at least one test that PASSES.

**Truths:**
- D-39-22: `examples/notation/` chapter pattern + double as `tests/test_notation_*_example.flow`.
- Two-run cmp-clean: deterministic by construction (no PRNG in notation IO).
- Phase 35 test framework: `(test "name" lazy(body))` + `(assert)` / `(assertEq)` / `(assertNotesMatch)` — see `flow-lang/test.flow`.
- D-v1.5-05: charitable interpretation — examples NEVER hit an error path (they use well-formed input).
</must_haves>

<tasks>
  <task id="39-05-T1" type="execute">
    <action>
      Create `examples/notation/README.md`. Structure:
        - Header: "Notation IO — Phase 39 chapters"
        - Intro paragraph: "Flow speaks the music-notation ecosystem's languages. Phase 39 adds 4 builtins under `use \"@notation-io\"`: `writeMusicXML`, `writeLilyPond`, `abc`, `mml`."
        - "## Reproduction prerequisites" subsection:
          - `dotnet 10.x` (required — all chapters)
          - `mscore` / `musescore4` (optional — only needed if you want to render `to_musicxml.flow`'s output to PDF; D-39-08 skip when absent)
          - `lilypond` 2.24+ (optional — only needed for `to_lilypond.flow`'s engraver compile; Flow emits the `.ly` correctly without it)
        - "## Chapters" list:
          1. `to_musicxml.flow` — composer writes a 4-bar piece, exports it as MusicXML, opens in MuseScore
          2. `to_lilypond.flow` — same 4-bar piece, exports as LilyPond `.ly`, compiles to engraver PDF via `lilypond` if available
          3. `from_abc.flow` — composer imports a public-domain reel from thesession.org corpus (verbatim string embedded), renders to WAV
          4. `from_mml.flow` — composer imports a PC-98 chiptune snippet (Mario-bros-style melody), renders to WAV
        - "## Running" one-liner: `dotnet run --project flow-interpreter examples/notation/{chapter}.flow`
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/examples/symphony/README.md (sibling chapter README pattern)
    </read_first>
    <acceptance_criteria>
      - File `examples/notation/README.md` exists.
      - Contains substring `"writeMusicXML"`, `"writeLilyPond"`, `"abc"`, `"mml"`.
      - Contains "Reproduction prerequisites" and "Chapters" subsections.
    </acceptance_criteria>
  </task>

  <task id="39-05-T2" type="execute">
    <action>
      Create `examples/notation/to_musicxml.flow`. Self-contained ~25-line script:

      ```
      use "@notation-io"
      use "@composition"

      Note: A simple 4-bar piano piece in C major, exported as MusicXML for MuseScore.
      tempo 120 {
          timesig 4/4 {
              key Cmajor {
                  section intro {
                      Sequence piano = | C4q D4q E4q F4q | G4q A4q B4q C5q | C5q B4q A4q G4q | F4q E4q D4q C4w |
                  }
                  Song song = [intro]
                  (writeMusicXML song "examples/notation/output/to_musicxml.musicxml")
                  (print "MusicXML written to examples/notation/output/to_musicxml.musicxml")
              }
          }
      }
      ```

      Ensure `examples/notation/output/` directory exists (create + add a `.gitkeep`).
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/examples/showcase.flow (note-stream + section + song idioms)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/notation-io.flow (Plan 39-01 T6 — confirm writeMusicXML forward decl)
    </read_first>
    <acceptance_criteria>
      - File `examples/notation/to_musicxml.flow` exists.
      - Smoke: `dotnet run --project flow-interpreter examples/notation/to_musicxml.flow` exits 0 + emits "MusicXML written".
      - `examples/notation/output/to_musicxml.musicxml` created on disk.
      - File parses as XML: `dotnet run --project flow-interpreter -- -e 'Note: stub'` not needed — just check via shell: `file examples/notation/output/to_musicxml.musicxml | grep -i xml` or `head -1 examples/notation/output/to_musicxml.musicxml | grep -i xml`.
    </acceptance_criteria>
  </task>

  <task id="39-05-T3" type="execute">
    <action>
      Create `examples/notation/to_lilypond.flow`. Mirror the structure of `to_musicxml.flow` but call `(writeLilyPond song "examples/notation/output/to_lilypond.ly")`. Same 4-bar piece.

      Echo a note in `(print)` about optional `lilypond` engraver: "LilyPond source written. Run `lilypond -o examples/notation/output/to_lilypond examples/notation/output/to_lilypond.ly` to render PDF (requires lilypond 2.24+)."
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/examples/notation/to_musicxml.flow (T2 — pattern)
    </read_first>
    <acceptance_criteria>
      - File `examples/notation/to_lilypond.flow` exists.
      - Smoke: `dotnet run --project flow-interpreter examples/notation/to_lilypond.flow` exits 0 + emits the print message.
      - `examples/notation/output/to_lilypond.ly` created on disk.
      - File starts with `\version "2.24.0"`: `head -1 examples/notation/output/to_lilypond.ly | grep -F '\\version "2.24.0"'`.
    </acceptance_criteria>
  </task>

  <task id="39-05-T4" type="execute">
    <action>
      Create `examples/notation/from_abc.flow`. Imports a verbatim ABC string (4-bar Irish reel — public-domain melody such as a fragment of "Drowsy Maggie" simplified to 4 bars; or just compose a charitable 4-bar Cmajor Sequence). Calls `(abc str)` and then renders the returned Section to WAV via existing `renderSong` + `writeWav` if practical, OR just verifies the parsed Section's bar count via `(print)`.

      Practical shape:
      ```
      use "@notation-io"

      String tune = "X:1\nT:Demo Reel\nM:4/4\nL:1/4\nK:Cmaj\nC D E F | G A B c |"
      Section parsed = (abc tune)
      (print "ABC imported: 1 Section parsed")
      ```

      No WAV rendering needed for the example chapter — just demonstrate the import surface. The composer-facing test (T7) will assert more deeply.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/notation-io.flow (Plan 39-01 T6 — abc forward decl)
    </read_first>
    <acceptance_criteria>
      - File `examples/notation/from_abc.flow` exists.
      - Smoke: `dotnet run --project flow-interpreter examples/notation/from_abc.flow` exits 0 + emits print message.
    </acceptance_criteria>
  </task>

  <task id="39-05-T5" type="execute">
    <action>
      Create `examples/notation/from_mml.flow`. Imports a verbatim MML string (PC-98 melody). Calls `(mml str)` and prints success.

      ```
      use "@notation-io"

      String chiptune = "T120 L8 O4 cdefga>cb<a>c"
      Sequence parsed = (mml chiptune)
      (print "MML imported: 1 Sequence parsed")
      ```
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/notation-io.flow
    </read_first>
    <acceptance_criteria>
      - File `examples/notation/from_mml.flow` exists.
      - Smoke: `dotnet run --project flow-interpreter examples/notation/from_mml.flow` exits 0 + emits print message.
    </acceptance_criteria>
  </task>

  <task id="39-05-T6" type="execute">
    <action>
      Create `tests/test_notation_to_musicxml_example.flow`. Uses Phase 35 framework:

      ```
      use "@test"
      use "@notation-io"
      use "@composition"

      Note: Smoke test — composer-facing MusicXML export round-trip.
      Note: Mirrors examples/notation/to_musicxml.flow but writes to a temp path and
      Note: asserts the file exists + starts with the XML declaration.

      (test "writeMusicXML emits a well-formed XML file" lazy(
          tempo 120 {
              timesig 4/4 {
                  key Cmajor {
                      section intro {
                          Sequence piano = | C4q D4q E4q F4q |
                      }
                      Song song = [intro]
                      (writeMusicXML song "/tmp/flow_test_to_musicxml.musicxml")
                      (assert true)
                      Note: Existence + XML prefix check is enforced by xUnit.
                      Note: This .flow test asserts the no-throw path.
                  }
              }
          }
      ))
      ```

      The deep file-format assertion is owned by `MusicXmlExportTests.cs` (Plan 39-01 T8); this `.flow` test gates the "composer-facing call doesn't crash" surface.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/test.flow (Phase 35 test framework — (test ...) + (assert ...) signatures)
      - /home/noah/Desktop/projects/flow-sharp-phase39/tests/test_test_library.flow (Phase 35 framework usage examples)
      - /home/noah/Desktop/projects/flow-sharp-phase39/examples/notation/to_musicxml.flow (T2 — same surface)
    </read_first>
    <acceptance_criteria>
      - File `tests/test_notation_to_musicxml_example.flow` exists.
      - `dotnet run --project flow-cli -- test tests/test_notation_to_musicxml_example.flow` exits 0 with PASS reported.
    </acceptance_criteria>
  </task>

  <task id="39-05-T7" type="execute">
    <action>
      Create `tests/test_notation_to_lilypond_example.flow`, `tests/test_notation_from_abc_example.flow`, and `tests/test_notation_from_mml_example.flow` mirroring T6's pattern for the other 3 builtins. Each test asserts no-throw on the composer-facing call + writes to a `/tmp/` temp path.

      For `from_abc`: assert the returned Section has at least 1 bar (use `(assert (== (length (getSections section)) 1))` or similar — adapt to actual API).

      For `from_mml`: assert the returned Sequence is non-null (use `(assert true)` post-call if richer assertions are not trivially expressible).
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/tests/test_notation_to_musicxml_example.flow (T6 — template)
      - /home/noah/Desktop/projects/flow-sharp-phase39/flow-lang/test.flow (assertion primitives)
    </read_first>
    <acceptance_criteria>
      - Files `tests/test_notation_to_lilypond_example.flow`, `tests/test_notation_from_abc_example.flow`, `tests/test_notation_from_mml_example.flow` exist.
      - `dotnet run --project flow-cli -- test tests/` exits 0 with all 4 `test_notation_*_example.flow` reporting PASS.
    </acceptance_criteria>
  </task>

  <task id="39-05-T8" type="execute">
    <action>
      Modify `CLAUDE.md`:
        1. In the "Standard Library Modules (.flow files)" section (search for "## Standard Library Modules (.flow files)"), insert this entry after the existing `improv.flow` line, keeping alphabetical-ish order:
           `- notation-io.flow — single-module surface for MusicXML/LilyPond export + ABC/MML import (Phase 39; opt-in via use "@notation-io"). Distinct from notation.flow (musical-notation primitives — note durations, rests, bar/sequence building). Hand-rolled emit (XmlWriter for MusicXML, StringBuilder for LilyPond); hand-rolled imports (ABC + MML) — no vendored sources, zero new NuGets.`

        2. In the "Music-Specific" Language Features bullet list (search for "### Music-Specific"), add ONE concise bullet after the existing Phase 36 entries:
           `- **Notation IO surface** (Phase 39): \`(writeMusicXML song "file.musicxml")\` emits MusicXML 3.1 partwise (MuseScore-compatible per D-v1.5-08 articulation table; multi-track Song → multi-\`<part>\`; voice blocks → \`<voice>N</voice>\`; microtonal as decimal \`<alter>\` cents). \`(writeLilyPond song "file.ly")\` emits LilyPond 2.24 text (per-Sequence \`\\new Staff\`; voice blocks → \`\\new Voice\` siblings inside \`<< { } \\\\ { } >>\`; microtonal as \`% +Nc\` comments; nested tuplets flattened by ratio-multiplication). \`(abc "X:1\\n...")\` imports ABC 2.1 subset + abc2midi extensions (modal keys + Q: tempo; multi-tune files return \`Array[Section]\`; charitable per D-v1.5-05 — unknown ornaments dropped with \`[abc]\` stderr advisory). \`(mml "T120 L4 O4 cdefga>c")\` imports PC-98 MML common core (notes / accidentals / octave / length / tempo / loops with depth cap 16; dialect-specific opcodes ignored with \`[mml]\` advisory). All 4 surfaces opt-in via \`use "@notation-io"\`; round-trip CI gate (\`mscore --convert-to mxl\`) charitable-skips when binary absent per D-39-08.`

        3. In the "Vendoring" or "Adding New" sections (or create a "## Vendored Sources" section if absent), add a short note: "Phase 39 evaluated vendoring `sightreader/musicxml-schemas` + `matthewcpp/ABCSharp` per D-39-03/04. After research, BOTH were dropped in favor of XDocument structural diff (MusicXML round-trip) and hand-rolled parser (ABC). No `flow-lang/Vendor/` source-code dependencies ship in v1.5 — only the existing CC-BY 4.0 audio samples under `flow-lang/Samples/` (Phase 29)."
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/CLAUDE.md (lines containing "Standard Library Modules" and "Music-Specific")
    </read_first>
    <acceptance_criteria>
      - `CLAUDE.md` contains substring `"notation-io.flow"`.
      - `CLAUDE.md` contains substring `"Notation IO surface"` or `"writeMusicXML"`.
      - `CLAUDE.md` documents the no-vendor decision.
    </acceptance_criteria>
  </task>

  <task id="39-05-T9" type="execute">
    <action>
      Create `.planning/phases/39-notation-citizenship/39-VERIFICATION.md` with status `passed`, full REQ-coverage table, test counts, and merge guidance. Structure:

      ```
      # Phase 39: Notation Citizenship - Verification

      **Verified:** 2026-05-23
      **Status:** passed

      ## REQ Coverage

      | REQ ID | Requirement | Plan | Status |
      |--------|-------------|------|--------|
      | XML-01 | (writeMusicXML) emits MuseScore-compatible MusicXML 3.1 | 39-01 | ✅ |
      | XML-02 | mscore round-trip CI gate (charitable skip when absent) | 39-01 | ✅ |
      | LILY-01 | (writeLilyPond) emits lilypond 2.24-compatible text | 39-02 | ✅ |
      | ABC-01 | (abc str) → Section | Array[Section] for ABC 2.1 + abc2midi | 39-03 | ✅ |
      | ABC-02 | Unknown ornaments dropped with [abc] advisory | 39-03 | ✅ |
      | MML-01 | (mml str) → Sequence for PC-98 common core | 39-04 | ✅ |

      ## Test Counts

      - xUnit Phase39: {EXACT_COUNT_FILLED_BY_EXECUTOR} tests, all PASS
      - Composer .flow tests (tests/test_notation_*_example.flow): 4 tests, all PASS
      - Backwards-compat: Phase 28 + Phase 33 MIDI byte-identical tests still PASS (Plan 39-01 M2 invariant)

      ## Decisions Made (from CONTEXT Claude's Discretion)

      - Vendor/ directory naming: `flow-lang/Vendor/` (PascalCase, sibling to `Samples/`)
      - MusicXML emit path: hand-rolled `XmlWriter` (NOT `XmlSerializer`) for two-run cmp-clean
      - LilyPond `\midi { }` block: KEEP (matches LilyPond user expectation)
      - MML nested loop semantics: inner-each-time (PC-98 PMD/MUCOM convention)
      - `flow notation convert` CLI: deferred to v1.6
      - Plan breakdown: 5 plans, 3 waves
      - **MusicXmlSchemas POCO vendoring: SKIPPED** — XDocument structural diff sufficient for round-trip CI gate (Plan 39-01 T1 decision)
      - **ABCSharp source vendoring: SKIPPED** — hand-rolled parser fits Flow's narrow needs (ABC 2.1 core + abc2midi modal/Q:) better than ingesting a 5000-line dep (Plan 39-03 revised D-39-04)

      ## Notes for Merge to dev

      - This worktree (`phase-39-notation` branch at SHA {FILLED_AT_VERIFY}) is ISOLATED from the concurrent Phase 37 work in the main tree (`dev` branch).
      - `.planning/STATE.md` was NOT updated in this worktree per the parent task brief; reconcile manually on merge.
      - `.planning/ROADMAP.md` Phase 39 entry NOT updated — mark COMPLETE during merge.
      - `.planning/REQUIREMENTS.md` REQ-ID statuses NOT updated — mark XML-01/02/LILY-01/ABC-01/02/MML-01 as IMPLEMENTED during merge.
      - One-line merge guidance: `merge phase-39-notation into dev; resolve STATE.md/ROADMAP.md/REQUIREMENTS.md by hand`.
      ```

      Test counts to be replaced at verify time with actual numbers from `dotnet test --filter "FullyQualifiedName~Phase39"` output and `flow test tests/test_notation_*` output.
    </action>
    <read_first>
      - /home/noah/Desktop/projects/flow-sharp-phase39/.planning/phases/35-language-foundation/35-VERIFICATION.md (sibling phase format)
    </read_first>
    <acceptance_criteria>
      - File `.planning/phases/39-notation-citizenship/39-VERIFICATION.md` exists.
      - Status `passed`.
      - All 6 REQ IDs listed with ✅.
      - "Notes for Merge to dev" section present.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  <vc id="V1">All 39-05 acceptance criteria met (T1-T9).</vc>
  <vc id="V2">`dotnet build` clean across entire solution.</vc>
  <vc id="V3">All Phase 39 xUnit tests still GREEN.</vc>
  <vc id="V4">All 4 composer-facing .flow tests GREEN: `dotnet run --project flow-cli -- test tests/test_notation_to_musicxml_example.flow tests/test_notation_to_lilypond_example.flow tests/test_notation_from_abc_example.flow tests/test_notation_from_mml_example.flow` exits 0.</vc>
  <vc id="V5">All 4 example chapters run clean: each `dotnet run --project flow-interpreter examples/notation/{chapter}.flow` exits 0.</vc>
</verification>
