---
phase: 42-type-system-stdlib-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/StdlibAuditor/StdlibAuditor.csproj
  - scripts/StdlibAuditor/Program.cs
  - flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs
  - flow-sharp.sln
autonomous: true
requirements:
  - REQ-AUDIT-01
  - REQ-AUDIT-02
  - REQ-AUDIT-03
  - REQ-AUDIT-06
user_setup: []

must_haves:
  truths:
    - "Audit harness enumerates every FlowType subclass and every registered FunctionSignature without throwing"
    - "Harness emits machine-readable JSON describing producer/consumer adjacency for every type"
    - "Harness surfaces orphaned coercible types — Beat appears in the orphan list (anchor regression)"
    - "Harness surfaces asymmetric IsCompatibleWith pairs across the conversion graph"
    - "Existing flow-lang.Tests suite remains green — zero production code touched"
  artifacts:
    - path: "scripts/StdlibAuditor/StdlibAuditor.csproj"
      provides: "Standalone .NET 10 console project referencing flow-lang.csproj"
      contains: "OutputType>Exe"
    - path: "scripts/StdlibAuditor/Program.cs"
      provides: "Reflective audit extractor: types + signatures + orphans + asymmetries → JSON"
      min_lines: 80
    - path: "flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs"
      provides: "xUnit self-check fixture: harness runs without throwing, Beat is orphaned, expected asymmetries present"
      min_lines: 60
    - path: "flow-sharp.sln"
      provides: "Solution registers the new StdlibAuditor project (Migrate26 precedent)"
      contains: "StdlibAuditor"
  key_links:
    - from: "scripts/StdlibAuditor/Program.cs"
      to: "flow-lang/StandardLibrary/BuiltInFunctions.cs:90 RegisterSignaturesOnly"
      via: "static call wires every Register* path including Sfz/NotationIO/Osc"
      pattern: "BuiltInFunctions\\.RegisterSignaturesOnly"
    - from: "scripts/StdlibAuditor/Program.cs"
      to: "flow-lang/StandardLibrary/InternalFunctionRegistry.cs:133 EnumerateSignatures"
      via: "iteration entry point for the (name, signature) graph"
      pattern: "EnumerateSignatures\\(\\)"
    - from: "scripts/StdlibAuditor/Program.cs"
      to: "flow-lang/TypeSystem/FlowType.cs"
      via: "Assembly.GetTypes() reflection over FlowType subclasses"
      pattern: "typeof\\(FlowType\\)\\.Assembly"
    - from: "flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs"
      to: "scripts/StdlibAuditor/Program.cs"
      via: "shares core extraction logic OR shells out to dotnet run for self-check"
      pattern: "AuditExtractor|StdlibAuditor"
---

<objective>
Stand up the read-only audit harness that powers Phase 42 — a standalone .NET console program under `scripts/StdlibAuditor/` (Migrate26 sibling) plus a reflective xUnit self-check fixture under `flow-lang.Tests/Integration/Phase42/`. The harness consumes the existing `InternalFunctionRegistry.EnumerateSignatures` API + `Assembly.GetTypes()` reflection over `FlowType` to build the type↔signature adjacency table, the conversion-asymmetry list, and the orphan list. It emits both JSON (machine-readable, consumed by Plan 03) and a stub Markdown skeleton (the AUDIT.md author hook).

Purpose: Plan 03 cannot author AUDIT.md without raw structured data; Plan 02 covers the clamp/advisory inventory in parallel. This plan ships the reflective half.
Output: Working `dotnet run --project scripts/StdlibAuditor -- --emit-json <path>` command + xUnit self-check that passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-type-system-stdlib-audit/42-RESEARCH.md
@.planning/phases/42-type-system-stdlib-audit/42-PATTERNS.md
@.planning/phases/42-type-system-stdlib-audit/42-VALIDATION.md

<interfaces>
<!-- Key APIs the harness consumes — extracted from the codebase, no further exploration needed. -->

From flow-lang/StandardLibrary/InternalFunctionRegistry.cs:133-140:
```csharp
public IEnumerable<KeyValuePair<string, IReadOnlyList<FunctionSignature>>> EnumerateSignatures()
```

From flow-lang/StandardLibrary/BuiltInFunctions.cs:90-124:
```csharp
public static void RegisterSignaturesOnly(InternalFunctionRegistry registry)
```
PITFALL (RESEARCH.md §Pattern 2 line 193): RegisterAllImplementations does NOT wire SfzBuiltins / NotationIoBuiltins / Network.OscFunctions — those are wired only via FlowEngine. MUST use RegisterSignaturesOnly (StubbingRegistryProxy path) to capture every signature.

From flow-lang/TypeSystem/FlowType.cs:
```csharp
public abstract class FlowType : IEquatable<FlowType>
{
    public abstract string Name { get; }
    public virtual bool IsCompatibleWith(FlowType target) => Equals(target);
    public virtual bool CanConvertTo(FlowType target) => IsCompatibleWith(target);
    public virtual int GetSpecificity() => 100;
    public virtual bool IsHashable() => false;
}
```

From flow-lang/TypeSystem/FunctionSignature.cs:
- Has `InputTypes` (IReadOnlyList<FlowType>) and `ParameterNames` (string[]? — Phase 36 D-36-11 extension)
- Has NO `ReturnType` field (RESEARCH Open Question 1) — producer half of graph is inferred manually in Plan 03, not enumerated by harness

Reference-identity types (RESEARCH Pitfall 2): TuningType, SfzType, MarkovModelType, LsystemModelType, OscHandleType — strict equality only; NOT orphans even with 1 producer + 1 consumer site.
</interfaces>

<analogs>
- scripts/Migrate26/Program.cs — exact-shape analog for standalone console program (RESEARCH §Code Examples + PATTERNS.md §`Program.cs`)
- scripts/Migrate26/Migrate26.csproj — exact-shape analog for .csproj (PATTERNS.md §`StdlibAuditor.csproj`); copy verbatim with two renames
- flow-lang.Tests/Phase36/PrngRegistryNewRandomGateTests.cs — exact-shape analog for reflective xUnit fixture with FindRepoRoot walker + [Theory]+[InlineData] iteration (PATTERNS.md §`StdlibAuditTests.cs`)
- flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs — sibling reflective audit pattern under Integration/PhaseNN
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up StdlibAuditor console project + sln registration</name>
  <files>scripts/StdlibAuditor/StdlibAuditor.csproj, scripts/StdlibAuditor/Program.cs, flow-sharp.sln</files>
  <read_first>
    - scripts/Migrate26/Migrate26.csproj (exact-shape analog — copy verbatim, two renames)
    - scripts/Migrate26/Program.cs (entry-point shape: usage banner, args parsing, summary line, int return)
    - flow-lang/StandardLibrary/BuiltInFunctions.cs lines 85-125 (RegisterSignaturesOnly — the wiring entry point)
    - flow-lang/StandardLibrary/InternalFunctionRegistry.cs lines 125-145 (EnumerateSignatures API surface)
    - flow-lang/TypeSystem/FlowType.cs (base class — Name + IsCompatibleWith + CanConvertTo signatures)
    - flow-sharp.sln (existing project registrations to mirror the Migrate26 entry shape)
    - .planning/phases/42-type-system-stdlib-audit/42-RESEARCH.md §Code Examples lines 300-357 (Audit Harness Skeleton)
    - .planning/phases/42-type-system-stdlib-audit/42-PATTERNS.md §`Program.cs` and §`StdlibAuditor.csproj` sections
  </read_first>
  <action>
Create the standalone audit project paralleling `scripts/Migrate26/`.

**`scripts/StdlibAuditor/StdlibAuditor.csproj`:** Copy Migrate26.csproj verbatim. Change two values: `<RootNamespace>FlowLang.Migrate26</RootNamespace>` → `<RootNamespace>FlowLang.StdlibAuditor</RootNamespace>`. Keep `<OutputType>Exe</OutputType>`, `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>`. ProjectReference path stays `..\..\flow-lang\flow-lang.csproj` (same nesting depth — scripts/StdlibAuditor and scripts/Migrate26 are siblings).

**`scripts/StdlibAuditor/Program.cs`:** File-scoped namespace `FlowLang.StdlibAuditor`. `internal static class Program` with `static int Main(string[] args)`. Usage banner mirrors Migrate26: `dotnet run --project scripts/StdlibAuditor -- [--emit-json PATH] [--emit-markdown-skeleton PATH]`. Both flags optional but at least one required. Default JSON output: `.planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/type-signature-graph.json`. Default markdown skeleton: `.planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/AUDIT-skeleton.md`.

Body uses the reflective pipeline from RESEARCH §Code Examples lines 302-357:
- Construct `var registry = new InternalFunctionRegistry();` then `BuiltInFunctions.RegisterSignaturesOnly(registry);` — NOT `RegisterAllImplementations` (per PITFALL above; misses Sfz/NotationIO/OSC signatures).
- Reflect over `typeof(FlowType).Assembly.GetTypes()` filtered by `typeof(FlowType).IsAssignableFrom(t) && !t.IsAbstract && !t.IsGenericType`, fetch the public static `Instance` property per Pattern 1.
- Build `consumers: Dictionary<string, List<string>>` keyed by `FlowType.Name`, populated by iterating `registry.EnumerateSignatures()` and recording every `sig.InputTypes[i].Name`.
- Compute orphan list: coercible types (those that override `IsCompatibleWith` per RESEARCH Pitfall 2 — use the `OverridesIsCompatible` helper from §Code Examples line 354-356) with zero consumers. Reference-identity types (TuningType / SfzType / MarkovModelType / LsystemModelType / OscHandleType) classified separately; harness flags them but tags `category: "reference-identity"` so Plan 03 can suppress them.
- Compute conversion asymmetry: iterate all (A, B) pairs where A != B; record `{a, b, aToB, bToA}` whenever `a.IsCompatibleWith(b) != b.IsCompatibleWith(a)` OR `a.CanConvertTo(b) != b.CanConvertTo(a)`.
- Compute overload-gap candidates (REQ-AUDIT-06): for each function name with at least one signature accepting `DoubleType` or `FloatType`, check whether a sibling signature accepts a music-type companion (Decibel/Cent/Hertz/Millisecond/Second/Semitone). Emit a candidate row `{name, accepts_double: true, missing_music_type: ["Decibel", ...]}`.

JSON schema (one top-level object): `{ types: [{ name, kind: "coercible"|"reference-identity", consumer_count }], signatures: [{ name, input_types, parameter_names }], orphans: [{ name, kind }], asymmetries: [{ a, b, is_compat: [bool, bool], can_convert: [bool, bool] }], overload_gap_candidates: [{ function, accepts_double, missing_music_types }] }`.

Markdown skeleton: emits an AUDIT.md template with `## Orphaned Types`, `## Missing Conversions`, `## Asymmetric Pairs`, `## Dead-End Builtins`, `## Overload Gaps`, `## Clamp & Advisory Inventory`, `## Prioritization & Phase Routing` headings (all 7 sections empty bodies, ready for Plan 03 to fill in). The skeleton is NOT the final AUDIT.md — Plan 03 authors that with priorities + routing.

Atomic-write pattern: write JSON to temp path then `File.Move(temp, final, overwrite: true)` (RESEARCH Security §V12 — atomic-rename to avoid partial-state corruption mid-write).

Summary line at end: `Done. {n_types} types ({n_coercible} coercible, {n_ref_identity} ref-identity), {n_signatures} signatures, {n_orphans} orphans, {n_asymmetries} asymmetric pairs, {n_overload_gaps} overload-gap candidates.`. Return 0 on success.

**`flow-sharp.sln`:** Add a Project entry for StdlibAuditor mirroring the Migrate26 entry shape (find the Migrate26 `Project(...) = "Migrate26"` line and add a sibling entry with `"StdlibAuditor"` + the StdlibAuditor.csproj relative path + a new GUID). Also add the Build.0/ActiveCfg lines under GlobalSection(ProjectConfigurationPlatforms) for the new GUID.

No production code touched — `flow-lang/StandardLibrary/**`, `flow-lang/TypeSystem/**`, and all `*.flow` files remain unchanged. Per `<scope_reduction_prohibition>` invariant: zero edits under those paths.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build scripts/StdlibAuditor/StdlibAuditor.csproj -c Debug 2>&1 | tail -5 && dotnet run --project scripts/StdlibAuditor -- --emit-json /tmp/phase42-graph.json && test -s /tmp/phase42-graph.json && jq '.types | length >= 20 and .orphans | length >= 1 and .signatures | length >= 200' /tmp/phase42-graph.json</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `scripts/StdlibAuditor/StdlibAuditor.csproj`
    - File exists: `scripts/StdlibAuditor/Program.cs`
    - `scripts/StdlibAuditor/StdlibAuditor.csproj` contains `<OutputType>Exe</OutputType>` AND `<TargetFramework>net10.0</TargetFramework>` AND `<RootNamespace>FlowLang.StdlibAuditor</RootNamespace>`
    - `scripts/StdlibAuditor/Program.cs` contains `BuiltInFunctions.RegisterSignaturesOnly` (NOT `RegisterAllImplementations`)
    - `scripts/StdlibAuditor/Program.cs` contains `typeof(FlowType).Assembly`
    - `dotnet build scripts/StdlibAuditor/StdlibAuditor.csproj` exits 0
    - `dotnet run --project scripts/StdlibAuditor -- --emit-json /tmp/phase42-graph.json` exits 0
    - `/tmp/phase42-graph.json` contains top-level keys `types`, `signatures`, `orphans`, `asymmetries`, `overload_gap_candidates`
    - `jq '.types | length' /tmp/phase42-graph.json` returns a value >= 20 (29 expected per RESEARCH)
    - `jq '.signatures | length' /tmp/phase42-graph.json` returns a value >= 200 (~370 expected per RESEARCH)
    - `jq '.orphans | map(select(.name == "BeatType")) | length' /tmp/phase42-graph.json` returns >= 1 (Beat is the anchor regression per RESEARCH §Summary)
    - `flow-sharp.sln` contains `StdlibAuditor` project entry
    - Zero files modified under `flow-lang/StandardLibrary/`, `flow-lang/TypeSystem/`, or matching `flow-lang/*.flow`
  </acceptance_criteria>
  <done>
    Standalone audit harness builds and runs; emits structured JSON with 5 top-level result sections. Beat orphan finding reproduced as anchor regression. Solution file registers the new project. No production code touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reflective xUnit self-check fixture under flow-lang.Tests/Integration/Phase42/</name>
  <files>flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs</files>
  <read_first>
    - flow-lang.Tests/Phase36/PrngRegistryNewRandomGateTests.cs (primary analog — FindRepoRoot walker + [Theory]+[InlineData] + per-fact offender accumulation)
    - flow-lang.Tests/Integration/Phase29/LicenseAuditTests.cs (sibling reflective-audit pattern under Integration/PhaseNN)
    - scripts/StdlibAuditor/Program.cs (FRESHLY CREATED in Task 1 — fixture mirrors its extraction logic in-process)
    - flow-lang/StandardLibrary/InternalFunctionRegistry.cs lines 125-145 (EnumerateSignatures)
    - flow-lang/StandardLibrary/BuiltInFunctions.cs lines 85-125 (RegisterSignaturesOnly)
    - flow-lang/TypeSystem/FlowType.cs (Name / IsCompatibleWith)
    - .planning/phases/42-type-system-stdlib-audit/42-PATTERNS.md §`StdlibAuditTests.cs` section (xUnit class shape + offender pattern)
    - .planning/phases/42-type-system-stdlib-audit/42-VALIDATION.md (per-task verification map row 42-01-01)
  </read_first>
  <action>
Create `flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs` mirroring PrngRegistryNewRandomGateTests + LicenseAuditTests shape.

Namespace `FlowLang.Tests.Integration.Phase42` (matches LicenseAuditTests convention). Class `AuditHarnessTests`. xmldoc summary explains: Phase 42 read-only audit harness self-check; mirrors PrngRegistryNewRandomGateTests' source-scanning + offender-accumulation pattern; asserts the harness extracts the expected gap classes from a freshly-constructed `InternalFunctionRegistry`.

Static helper builds the registry in-process: `private static (InternalFunctionRegistry registry, List<(Type type, FlowType instance)> types) BuildGraph()`. Inside: `var registry = new InternalFunctionRegistry(); BuiltInFunctions.RegisterSignaturesOnly(registry);` then reflect over `typeof(FlowType).Assembly.GetTypes()` filtered exactly as in Task 1's Program.cs. Cached behind a `Lazy<>` so each fact reuses the result.

Facts:

1. `[Fact] public void Harness_EnumeratesWithoutThrowing()` — calls `BuildGraph()`, asserts types.Count >= 20 and `registry.EnumerateSignatures().SelectMany(kvp => kvp.Value).Count() >= 200`.

2. `[Fact] public void OrphanList_ContainsBeatType()` — anchor regression per RESEARCH §Summary high-signal finding. Computes coercible-orphan list (types that override `IsCompatibleWith` and have zero consumer signatures); asserts `"BeatType"` is in the list. Failure message: `"BeatType must appear in the orphan list — see RESEARCH.md §Summary. If a producer/consumer for Beat shipped, update the test."`

3. `[Theory] [InlineData("TuningType")] [InlineData("SfzType")] [InlineData("MarkovModelType")] [InlineData("LsystemModelType")] [InlineData("OscHandleType")] public void RefIdentityTypes_NotFlaggedAsCoercibleOrphans(string typeName)` — per RESEARCH Pitfall 2. Asserts the type either overrides `IsCompatibleWith` to do strict equality (`Equals(target)`) OR is recognized by the harness as `kind: "reference-identity"`. Implementation: reflect on the type's `IsCompatibleWith` MethodInfo and verify `DeclaringType == typeofthis OR Body returns Equals(...)`. Charitable: if neither check passes, still allow the test to pass with a warning written to stdout — this is a *guard against false-positive orphans*, not a strict gate.

4. `[Fact] public void AsymmetricConversions_NonEmpty()` — iterates the (A, B) pair grid; asserts `count >= 1` (there will be many — Beat↔Double is canonical, plus Decibel/Cent/Hertz/Ms/Sec all have A→Double but not Double→A).

5. `[Fact] public void Registry_WiresSfzAndNotationIoAndOsc()` — guards the PITFALL from RESEARCH §Pattern 2 line 193. Asserts the registry contains AT LEAST the following names (proves RegisterSignaturesOnly wired the context-bound paths, not just RegisterAllImplementations): `loadSfz`, `writeMusicXML`, `oscSend`. Failure message names the pitfall explicitly.

Use the `FindRepoRoot()` private helper copied from PrngRegistryNewRandomGateTests lines 84-92 — even though this fixture is in-process and doesn't grep, the helper is the standard analog idiom and may be reused for future Phase 42 follow-up tests.

No `Trait("Category", "...")` needed — facts run in default xUnit category, picked up by `dotnet test` and `dotnet test --filter "FullyQualifiedName~Phase42"`.

No edits to production code paths.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42.AuditHarnessTests" --logger "console;verbosity=minimal" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs`
    - File contains namespace `FlowLang.Tests.Integration.Phase42`
    - File contains class `AuditHarnessTests`
    - File contains at least 5 `[Fact]` or `[Theory]` declarations
    - File contains the test method `OrphanList_ContainsBeatType`
    - File contains the test method `Registry_WiresSfzAndNotationIoAndOsc`
    - File contains `BuiltInFunctions.RegisterSignaturesOnly`
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42.AuditHarnessTests"` exits 0 with all facts PASS
    - Full suite still green: `dotnet test flow-lang.Tests --logger "console;verbosity=minimal"` exits 0 (no production regressions — Phase 42 invariant)
    - Zero files modified under `flow-lang/StandardLibrary/`, `flow-lang/TypeSystem/`, or `flow-lang/*.flow`
  </acceptance_criteria>
  <done>
    xUnit fixture under flow-lang.Tests/Integration/Phase42/ pins the harness behavior with 5+ facts including the Beat-orphan anchor regression and the Sfz/NotationIO/OSC wiring guard. Full test suite remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-harness → flow-lang assembly | Reflection over `typeof(FlowType).Assembly` — fixed in-repo build artifact, not user-supplied |
| audit-harness → .planning/ output | Writes JSON + markdown skeleton to `.planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/` only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | StdlibAuditor reflection load | accept | Loads only `typeof(FlowType).Assembly` — fixed in-repo build, not user-supplied (severity: none per planning_context security_threat_model) |
| T-42-02 | Information Disclosure | JSON output | accept | Output contains class names + signature shapes + grep counts only; no secrets / paths-outside-repo (severity: none per planning_context) |
| T-42-03 | DoS via partial state | Atomic JSON write | mitigate | Write to temp path then `File.Move(temp, final, overwrite: true)` per RESEARCH Security §V12 — never leaves AUDIT-data in a torn state |
</threat_model>

<verification>
- `dotnet build scripts/StdlibAuditor/StdlibAuditor.csproj` succeeds
- `dotnet run --project scripts/StdlibAuditor -- --emit-json /tmp/phase42-graph.json` exits 0 and emits valid JSON with 5 top-level sections (types/signatures/orphans/asymmetries/overload_gap_candidates)
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42"` exits 0 — all new facts PASS
- `dotnet test flow-lang.Tests --logger "console;verbosity=minimal"` exits 0 — Phase 42 read-only invariant verified
- `git status` shows zero changes under `flow-lang/StandardLibrary/`, `flow-lang/TypeSystem/`, or `flow-lang/*.flow`
</verification>

<success_criteria>
- Audit harness builds and runs end-to-end
- Beat orphan finding reproduced as anchor regression in xUnit
- Sfz/NotationIO/OSC wiring guard fact passes (proves RegisterSignaturesOnly is the correct entry point)
- Full test suite green (zero production regressions)
- Plan 03 has structured JSON data to consume for AUDIT.md authoring
</success_criteria>

<output>
Create `.planning/phases/42-type-system-stdlib-audit/42-01-SUMMARY.md` when done with the standard summary template (objective recap, artifacts created, verification results, downstream consumers).
</output>
