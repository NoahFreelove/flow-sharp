---
phase: 42-type-system-stdlib-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 42-01
  - 42-02
  - 42-03
files_modified:
  - .planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-AUDIT-03
  - REQ-AUDIT-09

must_haves:
  truths:
    - "42-VERIFICATION.md documents every REQ-AUDIT-NN closure with verification evidence"
    - "ROADMAP.md Phase 42 row updated to 4/4 Complete with the v1.5 phase-map line acknowledging audit closure"
    - "STATE.md Current Position advances past Phase 42 with the audit deliverable named in 'Last activity'"
    - "REQUIREMENTS.md gains a Phase 42 cross-insert listing REQ-AUDIT-01..09"
    - "Full flow-lang.Tests suite remains green"
    - "Every tests/test_*.flow script still passes (zero production regressions — Phase 42 invariant)"
    - "42-AUDIT.md is committed and referenced by ROADMAP.md as feeding Phase 43 + Phase 44"
  artifacts:
    - path: ".planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md"
      provides: "Per-REQ closure evidence + Nyquist sampling log"
      min_lines: 50
    - path: ".planning/ROADMAP.md"
      provides: "Phase 42 row marked Complete; v1.5 Phase Map updated"
    - path: ".planning/STATE.md"
      provides: "Current Position advanced; Phase 42 closure noted"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 42 cross-insert with REQ-AUDIT-01..09"
  key_links:
    - from: ".planning/ROADMAP.md Phase 42 row"
      to: ".planning/phases/42-type-system-stdlib-audit/42-AUDIT.md"
      via: "Plans table marks 4/4 Complete + deliverable filename cited"
      pattern: "42-AUDIT\\.md"
    - from: ".planning/STATE.md"
      to: "Phase 43 + 44 next-action pointer"
      via: "Resume Instructions block updated to reference 42-AUDIT.md as the input artifact"
      pattern: "42-AUDIT\\.md"
---

<objective>
Phase 42 closer — author `42-VERIFICATION.md`, sweep `ROADMAP.md` / `STATE.md` / `REQUIREMENTS.md` to reflect Phase 42 completion, and run the final full-suite gate (existing flow-lang.Tests stays green + every `tests/test_*.flow` script still passes). After this plan ships, Phase 43 plan-phase can spawn with `42-AUDIT.md` as its authoritative input.

Purpose: Closes the v1.5 phase-tracking loop and makes Phase 42's output discoverable from the top-level planning artifacts. Per planning_context, Phase 42 has the strict invariant "ships AUDIT.md only, zero production code changes" — this closer plan enforces that invariant one last time via the full-suite + .flow-test pass.
Output: Updated tracking files + green test gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT.md
@.planning/phases/42-type-system-stdlib-audit/42-RESEARCH.md
@.planning/phases/42-type-system-stdlib-audit/42-VALIDATION.md
@.planning/phases/42-type-system-stdlib-audit/42-01-SUMMARY.md
@.planning/phases/42-type-system-stdlib-audit/42-02-SUMMARY.md
@.planning/phases/42-type-system-stdlib-audit/42-03-SUMMARY.md

<analogs>
- .planning/phases/39-notation-citizenship/39-VERIFICATION.md — analog for Phase NN VERIFICATION.md closer template (per-REQ closure block + Nyquist sampling log)
- ROADMAP.md Phase 38 row + Plans table — example of a 4/4 Complete sweep with v1.5 milestone tracking
- STATE.md last_activity update pattern — see line 8 for the canonical shape
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 42-VERIFICATION.md with per-REQ closure evidence</name>
  <files>.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/42-type-system-stdlib-audit/42-VALIDATION.md (per-REQ → test map)
    - .planning/phases/42-type-system-stdlib-audit/42-01-SUMMARY.md (Plan 01 outputs)
    - .planning/phases/42-type-system-stdlib-audit/42-02-SUMMARY.md (Plan 02 outputs)
    - .planning/phases/42-type-system-stdlib-audit/42-03-SUMMARY.md (Plan 03 outputs)
    - .planning/phases/42-type-system-stdlib-audit/42-AUDIT.md (the deliverable being verified)
    - .planning/phases/39-notation-citizenship/39-VERIFICATION.md (closest in-tree analog — closer-VERIFICATION shape under .planning/phases/NN/)
  </read_first>
  <action>
Author `.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md` mirroring `.planning/phases/39-notation-citizenship/39-VERIFICATION.md` structure (or the nearest existing VERIFICATION.md if 39 is unavailable — fall back to any `*/NN-VERIFICATION.md` under .planning/phases/).

Frontmatter:
```yaml
---
phase: 42-type-system-stdlib-audit
status: CLOSED
nyquist_compliant: true
ships: audit-deliverable-only
production_code_changes: 0
date: <today>
---
```

Body sections:

**§Closure Summary** — One paragraph. Phase 42 shipped a read-only type-system + stdlib audit (`42-AUDIT.md`) feeding Phase 43 (module/naming targets) + Phase 44 (strict-mode Axis B sites). Zero production code touched. Anchor finding (Beat orphan) confirmed.

**§Requirements Closure** — Table per REQ-AUDIT-NN. Columns: `REQ | Behavior | Evidence | Status`. Rows:
- REQ-AUDIT-01 — Harness enumerates types + signatures — `scripts/StdlibAuditor/Program.cs` runs; JSON emitted; AuditHarnessTests.Harness_EnumeratesWithoutThrowing PASS — CLOSED
- REQ-AUDIT-02 — AUDIT.md emitted with 5 gap sections + severity — `42-AUDIT.md` §1-§5 + §7 prioritization; AuditReportShapeTests theory PASS — CLOSED
- REQ-AUDIT-03 — Full flow-lang.Tests suite green + every .flow test passes — see §Test Gate below — CLOSED
- REQ-AUDIT-04 — Asymmetric pairs surfaced — `42-AUDIT.md` §3 + AuditReportShapeTests row — CLOSED
- REQ-AUDIT-05 — Dead-end candidates pruned via .flow caller cross-ref — `42-AUDIT.md` §4 cites `flow-call-sites.txt` from Plan 02 — CLOSED
- REQ-AUDIT-06 — Overload gaps surfaced via JSON `overload_gap_candidates` — `42-AUDIT.md` §5 — CLOSED
- REQ-AUDIT-07 — Clamp/advisory inventory complete — `42-AUDIT-data/all-clamps.txt` + `advisory-sites.txt` + `charitable-sites.txt` + `42-AUDIT.md` §6 — CLOSED
- REQ-AUDIT-08 — Composer-approved prioritization — Plan 03 Task 3 checkpoint signed off — CLOSED
- REQ-AUDIT-09 — AUDIT.md committed — `git log` shows the commit; ROADMAP.md updated — CLOSED

**§Test Gate** — Final regression bars. Cite verbatim:
- `dotnet test flow-lang.Tests` → <N>/<N> PASS (record actual count from final run)
- `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t"; done` → all PASS
- Phase 42 specific filter: `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42"` → all PASS (lists: AuditHarnessTests, ClampGrepConsistencyTests, AuditReportShapeTests)

**§Nyquist Sampling Log** — Per VALIDATION.md sampling rate (audit ~10s · full suite ~90s). Confirm sampling was applied after every plan wave (Wave 1 = Plans 01+02; Wave 2 = Plan 03; Wave 3 = Plan 04). Record final-gate timestamps if known.

**§Production Code Diff** — `git diff --stat <wave-1-start-sha>..HEAD -- flow-lang/StandardLibrary/ flow-lang/TypeSystem/ flow-lang/*.flow` MUST be empty. If the diff is non-empty, Phase 42 invariant was violated and this section blocks closure. Record the empty-diff result here.

**§Carryover** — Anything surfaced during the audit that wasn't a routable finding: e.g., RESEARCH Open Question 1 (`FunctionSignature.ReturnType` field addition is v1.6-backlog), Open Question 5 (test-coverage gap section was out-of-scope for v1 — possible v1.6 stretch).

**§Downstream Consumers** — Phase 43 plan-phase consumes §1 + §4 + §5 of AUDIT.md; Phase 44 plan-phase consumes §6 (Axis B sites) + §2 (explicit-conversion builtins).

No production code touched.
  </action>
  <verify>
    <automated>test -s /home/noah/Desktop/projects/flow-sharp/.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md && grep -q "status: CLOSED" /home/noah/Desktop/projects/flow-sharp/.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md && grep -c "REQ-AUDIT-" /home/noah/Desktop/projects/flow-sharp/.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md | awk '{ if ($1 >= 9) exit 0; else { print "Expected >= 9 REQ-AUDIT- references, found " $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md`
    - File frontmatter contains `status: CLOSED`
    - File frontmatter contains `production_code_changes: 0`
    - File mentions every REQ-AUDIT-NN ID from REQ-AUDIT-01 through REQ-AUDIT-09
    - File contains heading `Requirements Closure` (or equivalent §Requirements Closure section)
    - File contains heading `Test Gate` (or equivalent)
    - File contains heading `Production Code Diff` (or equivalent)
    - File cites the empty `git diff --stat ... -- flow-lang/StandardLibrary/ flow-lang/TypeSystem/ flow-lang/*.flow` result
    - Zero files modified under `flow-lang/StandardLibrary/`, `flow-lang/TypeSystem/`, or `flow-lang/*.flow`
  </acceptance_criteria>
  <done>
    Closer VERIFICATION.md documents every REQ-AUDIT-NN closure with concrete evidence, pins the empty production diff, and points downstream phases at the AUDIT.md sections they consume.
  </done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP + STATE + REQUIREMENTS sweep + final regression bars</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 340-390 (Phase 42-44 entries + Progress table rows 344-346)
    - .planning/STATE.md lines 1-50 (frontmatter + Current Position + Resume Instructions block)
    - .planning/REQUIREMENTS.md (look for the existing per-phase cross-insert blocks; mirror that shape for Phase 42)
    - .planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md (FRESHLY CREATED in Task 1)
    - .planning/phases/42-type-system-stdlib-audit/42-AUDIT.md (deliverable to cross-reference)
  </read_first>
  <action>
**`.planning/ROADMAP.md` updates:**

1. Phase 42 entry (line 348 region): Update `**Plans:** 0 plans` → `**Plans:** 4 plans`. Replace the `- [ ] TBD (run /gsd-plan-phase 42 to break down)` placeholder with the four plan rows:
   ```
   - [x] 42-01-PLAN.md — Reflective audit harness + xUnit self-check (REQ-AUDIT-01/02/03/06)
   - [x] 42-02-PLAN.md — Clamp/advisory grep extractor + .flow caller index (REQ-AUDIT-04/05/07)
   - [x] 42-03-PLAN.md — AUDIT.md synthesis + composer review checkpoint (REQ-AUDIT-02/04/05/06/07/08/09)
   - [x] 42-04-PLAN.md — Closer: VERIFICATION + ROADMAP/STATE/REQUIREMENTS sweep + full-suite gate (REQ-AUDIT-03/09)
   ```
2. Phase 42 Requirements line: `**Requirements**: TBD (defined at plan-phase)` → `**Requirements**: REQ-AUDIT-01, REQ-AUDIT-02, REQ-AUDIT-03, REQ-AUDIT-04, REQ-AUDIT-05, REQ-AUDIT-06, REQ-AUDIT-07, REQ-AUDIT-08, REQ-AUDIT-09`.
3. Add a one-line `**Deliverable**:` row referencing `42-AUDIT.md`.
4. Progress table row 344 (`| 42. Type System & Stdlib Audit | v1.5 | 0/0 | Not started | - |`) → `| 42. Type System & Stdlib Audit | v1.5 | 4/4 | Complete | <today> |`.
5. v1.5 milestone progress line (top of file or v1.5 summary block, ~line 10 or in STATE.md): leave numbers alone here; STATE.md owns the milestone-rollup updates.

**`.planning/STATE.md` updates:**

1. Frontmatter (lines 1-15):
   - `stopped_at: Phase 38 Live Coding 2.0 SHIPPED` → `stopped_at: Phase 42 Type System & Stdlib Audit SHIPPED`
   - `last_updated:` → today's ISO date
   - `last_activity:` → `"<today> — Phase 42 Type System & Stdlib Audit SHIPPED — read-only audit harness + 42-AUDIT.md gap list across 7 sections; 9/9 REQ-AUDIT-NN CLOSED per 42-VERIFICATION.md; zero production code touched (invariant gate-enforced)"`
   - `progress.completed_phases`: bump by 1 (5 → 6)
   - `progress.total_plans` + `progress.completed_plans`: bump by 4
   - `progress.percent`: recompute (e.g., from 71 → 81 for a +10pp bump or recompute exactly)

2. Current Position block (~line 28): update Phase, Plan, Next step, Status, Last activity to reflect Phase 42 closure. Next step: `/clear` then `/gsd:context-phase 43` (the natural next consumer of AUDIT.md) OR `/gsd:context-phase 40` if Phase 40 Studio Sync is still the user's preferred next move per existing roadmap.

3. v1.5 Phase Map table (line 34-44): leave Phase 35-41 rows alone; add or update the Phase 42 row noting REQ-AUDIT-01..09 (9 REQs) + new total.

4. Resume Instructions block: append a one-paragraph "Phase 42 SHIPPED" note explaining AUDIT.md is the gap-list input for the upcoming Phase 43 + 44 plan-phase spawns. Cite `42-AUDIT.md` path.

**`.planning/REQUIREMENTS.md` updates:**

Add a new section at the appropriate v1.5 milestone position (mirror the shape used for previous phases — search the file for existing phase cross-inserts like `## v1.5 Phase 38` to use as the template):

```
## v1.5 Phase 42 — Type System & Stdlib Audit

| ID | Requirement | Status | Plan(s) |
|----|-------------|--------|---------|
| REQ-AUDIT-01 | Audit harness enumerates every FlowType + FunctionSignature via reflection over `EnumerateSignatures` + `Assembly.GetTypes()` | CLOSED | 42-01 |
| REQ-AUDIT-02 | AUDIT.md emitted with 5 gap-class sections (orphans / missing-conversion / asymmetric-pair / dead-end / overload-gap) + severity | CLOSED | 42-01, 42-03 |
| REQ-AUDIT-03 | Existing flow-lang.Tests suite + tests/test_*.flow scripts all remain green (zero production regressions) | CLOSED | 42-04 |
| REQ-AUDIT-04 | AUDIT.md §3 surfaces asymmetric-pair findings (with Pitfall 5 false-positive guard) | CLOSED | 42-02, 42-03 |
| REQ-AUDIT-05 | AUDIT.md §4 dead-end candidates cross-referenced against .flow callers (Pitfall 1 false-positive guard) | CLOSED | 42-02, 42-03 |
| REQ-AUDIT-06 | AUDIT.md §5 overload gaps from JSON `overload_gap_candidates` + ergonomics test | CLOSED | 42-01, 42-03 |
| REQ-AUDIT-07 | AUDIT.md §6 Clamp & Advisory Inventory (load-bearing for Phase 44 Axis B) | CLOSED | 42-02, 42-03 |
| REQ-AUDIT-08 | §7 Prioritization & Phase Routing — every gap routed to Phase 43 / 44 / v1.6-backlog | CLOSED | 42-03 |
| REQ-AUDIT-09 | 42-AUDIT.md committed; ROADMAP.md/STATE.md updated | CLOSED | 42-04 |
```

**Final regression gates** (run as part of this task before committing the sweep):

- `dotnet test flow-lang.Tests` → must exit 0; record PASS count in 42-VERIFICATION.md if not already done.
- `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" > /dev/null 2>&1 || echo "FAIL: $t"; done` → must produce zero `FAIL:` lines (count is recorded in 42-VERIFICATION.md).
- `git diff --stat HEAD~10..HEAD -- flow-lang/StandardLibrary/ flow-lang/TypeSystem/ "flow-lang/*.flow"` → must be empty (Phase 42 invariant).

If any gate fails, do NOT proceed with the sweep — surface the failure to the executor as a blocking issue.

No production code touched.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -q "42-AUDIT.md" .planning/ROADMAP.md && grep -q "4/4 | Complete" .planning/ROADMAP.md && grep -q "Phase 42 Type System" .planning/STATE.md && grep -q "REQ-AUDIT-01" .planning/REQUIREMENTS.md && grep -q "REQ-AUDIT-09" .planning/REQUIREMENTS.md && dotnet test flow-lang.Tests --logger "console;verbosity=minimal" 2>&1 | tail -3 && PASS=0; FAIL=0; for t in tests/test_*.flow; do if dotnet run --project flow-interpreter "$t" > /dev/null 2>&1; then PASS=$((PASS+1)); else FAIL=$((FAIL+1)); echo "FAIL: $t"; fi; done; echo "flow tests: $PASS PASS, $FAIL FAIL"; [ "$FAIL" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 42 entry shows `**Plans:** 4 plans` (NOT `0 plans`) AND lists 4 `[x]` plan rows
    - `.planning/ROADMAP.md` Progress table Phase 42 row shows `4/4 | Complete`
    - `.planning/ROADMAP.md` Phase 42 Requirements line lists REQ-AUDIT-01 through REQ-AUDIT-09
    - `.planning/STATE.md` frontmatter `stopped_at` mentions Phase 42
    - `.planning/STATE.md` frontmatter `last_activity` cites today's date + Phase 42 closure
    - `.planning/STATE.md` `progress.completed_phases` incremented by 1
    - `.planning/STATE.md` `progress.completed_plans` incremented by 4
    - `.planning/REQUIREMENTS.md` contains `REQ-AUDIT-01` AND `REQ-AUDIT-09` (full table inserted)
    - `dotnet test flow-lang.Tests` exits 0 — final regression gate passes
    - Every `tests/test_*.flow` script exits 0 — final regression gate passes
    - `git diff --stat HEAD~10..HEAD -- flow-lang/StandardLibrary/ flow-lang/TypeSystem/ "flow-lang/*.flow"` is empty (Phase 42 invariant gate-enforced)
    - Zero files modified under `flow-lang/StandardLibrary/`, `flow-lang/TypeSystem/`, or `flow-lang/*.flow`
  </acceptance_criteria>
  <done>
    Tracking files reflect Phase 42 closure; full xUnit + .flow regression suites green; Phase 42 invariant (zero production code changes) verified via empty git diff. Phase 43 + 44 spawning is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closer plan → planning artifacts | Writes to `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, and `.planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md` only |
| closer plan → flow-lang/ production paths | NO writes — gate-enforced via final `git diff --stat` check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | Phase 42 invariant (zero production code changes) | mitigate | Final `git diff --stat HEAD~10..HEAD -- flow-lang/StandardLibrary/ flow-lang/TypeSystem/ "flow-lang/*.flow"` must be empty; blocks closure if not |
| T-42-02 | Information Disclosure | tracking-file content | accept | Public-repo planning artifacts; no secrets (severity: none per planning_context security_threat_model) |
</threat_model>

<verification>
- `42-VERIFICATION.md` exists with `status: CLOSED` + every REQ-AUDIT-NN cited
- `.planning/ROADMAP.md` Phase 42 entry shows 4/4 Complete with all four plan rows
- `.planning/STATE.md` Current Position advanced; frontmatter last_activity cites Phase 42
- `.planning/REQUIREMENTS.md` Phase 42 cross-insert table added
- `dotnet test flow-lang.Tests` exits 0
- Every `tests/test_*.flow` script exits 0
- `git diff --stat HEAD~10..HEAD -- flow-lang/StandardLibrary/ flow-lang/TypeSystem/ "flow-lang/*.flow"` is empty (Phase 42 invariant)
</verification>

<success_criteria>
- Phase 42 fully closed; all 9 REQ-AUDIT-NN documented
- Full test suite + .flow scripts green
- ROADMAP / STATE / REQUIREMENTS sweep complete
- Phase 43 + Phase 44 spawning is unblocked with 42-AUDIT.md as canonical input
</success_criteria>

<output>
Create `.planning/phases/42-type-system-stdlib-audit/42-04-SUMMARY.md` when done.
</output>
