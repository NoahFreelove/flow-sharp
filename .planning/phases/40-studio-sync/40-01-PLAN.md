---
phase: 40-studio-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/flow-lang.csproj
  - flow-lang/Audio/IMidiBackend.cs
  - flow-lang/Audio/NullMidiBackend.cs
  - flow-lang/Audio/RtMidiMidiBackend.cs
  - flow-lang/Audio/MidiPlaybackManager.cs
  - flow-lang/StandardLibrary/Audio/AudioCore.cs
  - flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs
  - flow-lang/StandardLibrary/Midi/MidiFunctions.cs
  - flow-lang/StandardLibrary/Midi/MidiDeviceData.cs
  - flow-lang/TypeSystem/SpecialTypes/MidiDeviceType.cs
  - flow-lang/Runtime/Value.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang/Runtime/ModuleLoader.cs
  - flow-lang/Core/FlowEngine.cs
  - flow-lang/midi.flow
  - flow-lang.Tests/Integration/Phase40/RtMidiInternalAccessSpikeTests.cs
  - flow-lang.Tests/Integration/Phase40/CaptureMidiBackend.cs
  - flow-lang.Tests/Integration/Phase40/MidiBackendTests.cs
  - flow-lang.Tests/Integration/Phase40/VirtualMidiTests.cs
  - flow-lang.Tests/Integration/Phase40/OfflineRenderDeterminismTests.cs
  - flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs
autonomous: true
requirements: [MIDI-RT-01, MIDI-RT-02, MIDI-RT-04, LINK-02]
user_setup:
  - service: RtMidi.Core native lib (librtmidi.so)
    why: "RtMidi.Core NuGet ships Win/Mac natives only; Linux real-time MIDI (MIDI-RT-02) needs a system librtmidi.so. Absent on this dev box. Code probes + falls back to NullMidiBackend, so the BUILD + all autonomous tests pass without it (CaptureMidiBackend seam needs no native lib); only real-hardware HUMAN-UAT needs it."
    dashboard_config:
      - task: "Install librtmidi (provides librtmidi.so + pulls libasound2)"
        location: "sudo apt-get install librtmidi-dev"

must_haves:
  truths:
    - "Composer can call (midiPorts) and (openMidiOutput \"name\") via use \"@midi\"; absent device returns a charitable Void/dead handle, never throws (MIDI-RT-01)"
    - "(midiNoteOn dev ch p v) / midiNoteOff / midiCC / midiSysex send correct in-range MIDI bytes to a virtual/captured port (MIDI-RT-02)"
    - "(midiOut song \"port\") routes each sequence to the SAME GM program/channel as writeMidi (D-40-02), via InstrumentRouting.ResolveGmProgram verbatim, with a ToRtChannel 0-based→1-based helper"
    - "Out-of-range channel/pitch/velocity/CC are charitably clamped at the builtin boundary (channel 0..15, pitch/vel/CC 0..127) + advisory; oversized sysex is length-capped — never passed unvalidated to native (T-40-01, T-40-04)"
    - "AudioBuffer carries a nullable PlaybackStartTime origin set the instant backend.Play begins; MIDI dispatch is buffer-relative ms-aligned best-effort, NOT sample-accurate (MIDI-RT-04)"
    - "writeWav output is byte-identical regardless of any sync/MIDI state — no MIDI path touches offline render (LINK-02 determinism intent, shippable even though Link itself is deferred to Plan 03)"
    - "dotnet build flow-lang -p:FlowTarget=Web exits 0 and RtMidi.Core is absent from the Web flow-lang.dll (T-40-03)"
  artifacts:
    - path: "flow-lang/Audio/IMidiBackend.cs"
      provides: "IMidiBackend + IMidiOutputHandle interfaces (ListPorts/OpenOutput/SendNoteOn/SendNoteOff/SendControlChange/SendProgramChange/SendSysex/SendRaw/Close + PortChanged)"
      contains: "interface IMidiBackend"
    - path: "flow-lang/Audio/RtMidiMidiBackend.cs"
      provides: "RtMidi.Core impl, #if !FLOW_WEB guarded, IsAvailable probe catching DllNotFoundException"
      contains: "RtMidiMidiBackend"
    - path: "flow-lang/Audio/NullMidiBackend.cs"
      provides: "silent no-op fallback so a live session never dies on missing librtmidi.so"
      contains: "NullMidiBackend"
    - path: "flow-lang/Audio/MidiPlaybackManager.cs"
      provides: "probe + lifecycle + DetectBackend returning NullMidiBackend (NOT throwing) + IsMidiAvailable"
      contains: "MidiPlaybackManager"
    - path: "flow-lang/StandardLibrary/Midi/MidiFunctions.cs"
      provides: "midiPorts/openMidiOutput/midiOut/midiNoteOn/midiNoteOff/midiCC/midiSysex + MidiEnabled gate"
      exports: ["Register"]
    - path: "flow-lang/midi.flow"
      provides: "opt-in @midi module with trailing (__enableMidiModule) marker"
      contains: "module midi"
    - path: "flow-lang.Tests/Integration/Phase40/CaptureMidiBackend.cs"
      provides: "in-process loopback IMidiBackend test seam recording sent byte arrays (no real ALSA)"
      contains: "CaptureMidiBackend"
  key_links:
    - from: "flow-lang/StandardLibrary/Midi/MidiFunctions.cs"
      to: "flow-lang/StandardLibrary/Notation/InstrumentRouting.cs"
      via: "ResolveGmProgram call in midiOut (D-40-02 verbatim)"
      pattern: "ResolveGmProgram"
    - from: "flow-lang/Core/FlowEngine.cs"
      to: "flow-lang/StandardLibrary/Midi/MidiFunctions.cs"
      via: "#if !FLOW_WEB MidiFunctions.Register call at the OSC register site"
      pattern: "MidiFunctions.Register"
    - from: "flow-lang/Runtime/ModuleLoader.cs"
      to: "@midi"
      via: "IsStrippedOnWeb predicate extension → charitable Web advisory"
      pattern: "@midi"
    - from: "flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs"
      to: "flow-lang/StandardLibrary/Audio/AudioCore.cs"
      via: "set AudioBuffer.PlaybackStartTime at backend.Play start (MIDI-RT-04 origin)"
      pattern: "PlaybackStartTime"
---

<objective>
Build the must-ship MIDI-out spine: the `IMidiBackend` abstraction (parallel to `IAudioBackend`), an RtMidi.Core ALSA-seq backend with charitable `NullMidiBackend` fallback, the `@midi` builtin surface (`midiPorts`/`openMidiOutput`/`midiOut`/`midiNoteOn`/`midiNoteOff`/`midiCC`/`midiSysex`), Web-strip discipline, the `AudioBuffer.PlaybackStartTime` alignment seam, and the Wave-0 test scaffold (`CaptureMidiBackend` loopback seam + the MIDI-RT-01/02/04 + LINK-02 tests).

**The FIRST task is a bounded SPIKE** resolving Open Q1 — how to send/receive the raw clock bytes (0xF8/0xFA/0xFB/0xFC) through RtMidi.Core whose raw-byte paths are `internal`. This MUST land before Plan 02 writes any clock send/receive code against it.

Purpose: Make Flow a real-time MIDI source (MIDI-RT-01/02/04, D-40-01/02/04/05), pin the clock-access strategy (Open Q1), and ship the LINK-02 determinism invariant — all while keeping the Web build clean (T-40-03).
Output: New backend + manager + handle + type + builtins + module + 5 test files; edits to csproj, AudioCore, PlaybackFunctions, ExecutionContext, ModuleLoader, FlowEngine, Value, and AssemblyReferenceScanTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-studio-sync/40-CONTEXT.md
@.planning/phases/40-studio-sync/40-RESEARCH.md
@.planning/phases/40-studio-sync/40-PATTERNS.md
@.planning/phases/40-studio-sync/40-VALIDATION.md

<interfaces>
<!-- Contracts extracted from the live tree this session. Use directly — no codebase exploration needed. -->

From flow-lang/Audio/IAudioBackend.cs (the interface-for-interface template, 72 lines):
  public interface IAudioBackend : IDisposable {
      string Name { get; }
      bool IsInitialized { get; }
      IReadOnlyList<string> GetDevices();   // "May be empty" — never throws
      bool SetDevice(string deviceName);
      // + Play(float[], sampleRate, channels, CancellationToken)
  }

From flow-lang/StandardLibrary/Notation/InstrumentRouting.cs:45 (D-40-02 VERBATIM REUSE — do NOT write a second table):
  public static (int gmProgram, int channel) ResolveGmProgram(string seqName)
  // drum* → (0, 9 percussion); piano* → (0,0); brass*/horn* → (56,0); sax* → (65,0);
  // flute* → (73,0); string* → (48,0); organ* → (19,0); bell* → (14,0)
  public static string StripSamplerPrefix(string name)   // line 31

From flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs (sealed-singleton ref-identity template, 39 lines):
  public sealed class OscHandleType : FlowType {
      private OscHandleType() { }
      public static OscHandleType Instance { get; } = new();
      public override string Name => "OscHandle";
      public override int GetSpecificity() => 151;
      public override bool IsCompatibleWith(FlowType target) => target is OscHandleType;
      public override bool CanConvertTo(FlowType target) => target is OscHandleType;
  }

From flow-lang/Runtime/Value.cs:122-124 (factory template, #if !FLOW_WEB guarded):
  public static Value OscHandle(StandardLibrary.Network.OscHandleData handle)
      => new(handle, OscHandleType.Instance);

From flow-lang/StandardLibrary/Network/OscHandleData.cs:31-60 (required-init record-shape):
  public sealed class OscHandleData {
      public required int Port { get; init; }
      public required Rug.Osc.OscReceiver? Receiver { get; init; }   // null = charitable dead handle
      public required CancellationTokenSource Cts { get; init; }
      public required Task ListenerTask { get; init; }
  }

From flow-lang/StandardLibrary/Audio/AudioCore.cs:10-42 (AudioBuffer — add PlaybackStartTime alongside these):
  public class AudioBuffer {
      public float[] Data { get; }        // line 16
      public int SampleRate { get; }      // line 21
      public int Channels { get; }        // line 26
      public int Frames { get; }          // line 32
  }

From flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs:340-348 (the alignment seam):
  private static void PlaySamples(float[] samples, int sampleRate, int channels, AudioPlaybackManager manager) {
      var ct = manager.StartPlayback();     // :342
      // backend.Play(samples, sampleRate, channels, ct);  // :347 (blocks until drain)
  }

From flow-lang/Runtime/MusicalContext.cs:43:
  public double? Tempo { get; set; }     // master reads (Plan 02); slave drives (Plan 02)

From flow-lang/StandardLibrary/Network/OscFunctions.cs (Register + gate template):
  Register(InternalFunctionRegistry registry, FlowLang.Runtime.ExecutionContext context)
    - sigMarker "__enableOscModule" → context.OscEnabled = true
    - each builtin body opens with RequireModuleActivated(context, "name")
    - ParameterNames: passed on FunctionSignature for D-36-11 named args

From flow-lang/Diagnostics/RenderingDiagnostics.cs:29:
  public static void WarnOnce(string sentinelKey, string message)   // [midi] prefix convention
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE — resolve RtMidi.Core internal raw-byte clock access (Open Q1) + add RtMidi.Core dep + Web-strip wiring</name>
  <read_first>
    - flow-lang/flow-lang.csproj:103-141 (existing PackageReference + Web-strip ItemGroups — Rug.Osc entry at :110-112 is the model for the new dep)
    - flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs:27-32 (ForbiddenTypeRefPrefixes — RtMidi.Core ALREADY present at :30; do NOT duplicate)
    - 40-RESEARCH.md §Open Questions Q1 (lines 479-482) + §Pitfall 1 (348-353) + §Pitfall 7 (384-388) + §Clock finding (line 68)
    - 40-PATTERNS.md §No Analog Found (lines 365-372) — clock raw-byte send is net-new
  </read_first>
  <action>
    Add `<PackageReference Include="RtMidi.Core" Version="1.0.53" />` to the EXISTING `<ItemGroup Condition="'$(FlowTarget)' != 'Web'">` (flow-lang.csproj, beside the Rug.Osc entry at :111). Do NOT add it to the unconditional group at :103-104.

    Create `flow-lang.Tests/Integration/Phase40/RtMidiInternalAccessSpikeTests.cs` — a bounded spike that resolves Open Q1: how to reach RtMidi.Core's `internal` raw-byte paths (`IRtMidiOutputDevice.SendMessage(byte[])` + `IRtMidiInputDevice.Message : EventHandler<byte[]>`) to send/receive clock bytes 0xF8/0xFA/0xFB/0xFC. Per RESEARCH Q1 recommendation, attempt strategy (a) reflection-into-internal FIRST (fastest, no extra native coupling). Write a Fact `RtMidiInternalSendMessage_Reachable` that reflectively locates the internal `SendMessage(byte[])` member on the RtMidi.Core output-device type at v1.0.53 and asserts the member signature exists (a GUARD test per Pitfall 7 — fails loudly if a future bump renames internals). If reflection proves unreachable, the spike must instead pin strategy (c): a minimal `[DllImport("rtmidi")] rtmidi_out_send_message` declaration — document whichever strategy succeeded in a top-of-file XML comment block so Plan 02 implements clock against the resolved path. The spike test MUST charitable-skip (probe + `return` PASS) when `librtmidi.so` is absent (this dev box lacks it — Environment Availability table) so it is green in CI; the reflection-signature assertion runs against the MANAGED assembly metadata which is present regardless of the native lib.

    Extend `AssemblyReferenceScanTests.ForbiddenTypeRefPrefixes` (AssemblyReferenceScanTests.cs:29-31) — add `"JackSharp"` ONLY (RtMidi.Core already at :30 per D-47-14; do not duplicate). This is the T-40-03 enforcement seam.
  </action>
  <verify>
    <automated>dotnet build flow-lang -p:FlowTarget=Desktop && dotnet test flow-lang.Tests --filter FullyQualifiedName~RtMidiInternalAccessSpike</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build flow-lang -p:FlowTarget=Desktop` exits 0 (RtMidi.Core resolves on Desktop)
    - `dotnet build flow-lang -p:FlowTarget=Web` exits 0 (RtMidi.Core NOT in the Web closure — under the `!= 'Web'` ItemGroup)
    - `dotnet test flow-lang.Tests --filter FullyQualifiedName~RtMidiInternalAccessSpike` passes (green or charitable-skip)
    - RtMidiInternalAccessSpikeTests.cs has a top-of-file comment recording WHICH access strategy (reflection vs C-API P/Invoke) Plan 02 must use for clock bytes
    - AssemblyReferenceScanTests.ForbiddenTypeRefPrefixes now contains both "RtMidi.Core" and "JackSharp"
  </acceptance_criteria>
  <done>Open Q1 resolved + recorded for Plan 02; RtMidi.Core added Desktop-only; JackSharp added to the forbidden-prefix gate; both builds green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IMidiBackend + RtMidiMidiBackend + NullMidiBackend + MidiPlaybackManager + AudioBuffer.PlaybackStartTime + Wave-0 test seam</name>
  <read_first>
    - flow-lang/Audio/IAudioBackend.cs (full, 72 lines — interface-for-interface template; doc-comment density to carry)
    - flow-lang/Audio/WebAudioBackend.cs:1-60 (impl + static IsAvailable probe + class-level design doc; Compile-Remove'd-on-Web model for RtMidiMidiBackend)
    - flow-lang/Audio/AudioPlaybackManager.cs (full, 183 lines — exact manager template: GetBackend lock+lazy at :54-67, DetectBackend at :108-115, IsAudioAvailable at :73-99, Dispose at :134-144)
    - flow-lang/StandardLibrary/Audio/AudioCore.cs:10-42 (AudioBuffer fields — add nullable PlaybackStartTime alongside)
    - flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs:340-348 (PlaySamples seam — set the origin here)
    - 40-RESEARCH.md §Pattern 1 (213-241) for the IMidiBackend/IMidiOutputHandle target signatures incl. SendProgramChange + SendRaw; §Pattern 2 (243-264) for RtMidi.Core Send mapping; §Pitfall 2/3/5
    - 40-PATTERNS.md §RtMidiMidiBackend (62-87), §MidiPlaybackManager (91-118), §MIDI-RT-04 alignment seam (330-344), §In-process loopback test seam (361)
  </read_first>
  <behavior>
    - MidiBackendEnumeratesPorts: MidiPlaybackManager.GetBackend() never throws; when no native device is present it returns a NullMidiBackend whose ListPorts() is empty (charitable, MIDI-RT-01)
    - MidiHotPlugNeverThrows: OpenOutput on an absent port returns null (or a dead handle) + WarnOnce, never throws (MIDI-RT-04 hot-plug charitable rule, T-40-04)
    - VirtualMidiNoteBytes (via CaptureMidiBackend seam): SendNoteOn(ch, pitch, vel) records the correct 3-byte status/data tuple; ToRtChannel maps 0-based ch9 drum → RtMidi 1-based correctly (Pitfall 3)
    - AudioBuffer.PlaybackStartTime is null on a freshly-constructed buffer and is set to a Stopwatch tick origin only when playback begins
  </behavior>
  <action>
    Create `flow-lang/Audio/IMidiBackend.cs` with `IMidiBackend : IDisposable` (`Name`, `IsInitialized`, `IReadOnlyList<string> ListPorts()` — "may be empty, never throws", `IMidiOutputHandle? OpenOutput(string port)` — "null = charitable failure", `event Action<IReadOnlyList<string>>? PortChanged` — poll-based per Pitfall 1/A7) and `IMidiOutputHandle : IDisposable` (`SendNoteOn(int ch, int pitch, int vel)`, `SendNoteOff(int ch, int pitch)`, `SendControlChange(int ch, int ctrl, int val)`, `SendProgramChange(int ch, int program)` — for D-40-02, `SendSysex(byte[] data)` — best-effort queue, `SendRaw(byte[] bytes)` — clock bytes Plan 02 calls, `Close()`). Carry IAudioBackend's per-member XML-doc density. NOT Web-stripped (pure interface, no RtMidi.Core ref).

    Create `flow-lang/Audio/RtMidiMidiBackend.cs` (`#if !FLOW_WEB` whole-file guard) implementing `IMidiBackend` over RtMidi.Core per RESEARCH Pattern 2: `MidiDeviceManager.Default.OutputDevices`, `info.CreateDevice()` → `IMidiOutputDevice`; map note/CC/program onto typed `Send(in NoteOnMessage/...)`. Add `public static bool IsAvailable()` modeled on AudioPlaybackManager.IsAudioAvailable (:73-99) — a cheap enumerate inside `try { } catch (DllNotFoundException) { return false; }` (Pitfall 2). Add a single `private static int ToRtChannel(int zeroBased)` helper (0-based→`Channel.Channel1..16`, Pitfall 3). `SendRaw` routes through the access strategy resolved by Task 1's spike. NEVER throw on send failure — WarnOnce + continue.

    Create `flow-lang/Audio/NullMidiBackend.cs` (NOT Web-stripped — pure managed no-op, model WebAudioBackend's stub branch shape): `ListPorts()` → empty, `OpenOutput()` → null, all sends no-op. This is the charitable fallback so a live session never dies on a missing librtmidi.so.

    Create `flow-lang/Audio/MidiPlaybackManager.cs` (NOT Web-stripped — but its `DetectBackend` references RtMidiMidiBackend only inside `#if !FLOW_WEB`) modeling AudioPlaybackManager exactly: lock + lazy `GetBackend()`, `DetectBackend()` that returns `new RtMidiMidiBackend()` if `RtMidiMidiBackend.IsAvailable()` (`#if !FLOW_WEB`) ELSE **returns `new NullMidiBackend()` — DOES NOT throw** (the one deviation from the audio manager per RESEARCH:118), `IsMidiAvailable()` probe in the IsAudioAvailable shape, `Dispose()` closing the device.

    Edit `flow-lang/StandardLibrary/Audio/AudioCore.cs` — add `public long? PlaybackStartTime { get; set; }` to `AudioBuffer` (nullable Stopwatch-tick origin, alongside Data/SampleRate/Channels/Frames). Default null.

    Edit `flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs:340-348` (`PlaySamples`) — record the Stopwatch tick origin into a buffer-relative scheduling concept at the instant before `backend.Play` (MIDI-RT-04 origin seam). Honesty: buffer-relative ms-alignment only; add NO claim of sample accuracy. (The actual MIDI dispatch thread keyed off this origin is wired in Task 3 with midiOut.)

    Create `flow-lang.Tests/Integration/Phase40/CaptureMidiBackend.cs` — an in-process `IMidiBackend`/`IMidiOutputHandle` test seam (models OSC `HandlerInvokeOverride` + `PulseAudioCaptureBackend.CaptureOverride`) that records every sent byte array into a public list so byte/rate assertions need NO real ALSA. This is the load-bearing seam for VirtualMidiTests + Plan 02's ClockMaster/Slave tests.

    Create `flow-lang.Tests/Integration/Phase40/MidiBackendTests.cs` — Facts `MidiBackendEnumeratesPorts` (charitable null/empty) + `MidiHotPlugNeverThrows` (OpenOutput absent port → no throw). Create `flow-lang.Tests/Integration/Phase40/VirtualMidiTests.cs` — Fact `VirtualMidiNoteBytes` asserting note/CC bytes via CaptureMidiBackend + the explicit drum→ch9 ToRtChannel mapping test (Pitfall 3); charitable-skip the real-ALSA end-to-end portion when librtmidi.so/snd-virmidi absent (model MusicXmlRoundTripTests.CharitableSkipWhenMscoreAbsent).
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "FullyQualifiedName~MidiBackend|FullyQualifiedName~VirtualMidi" && dotnet build flow-lang -p:FlowTarget=Web</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter MidiBackendEnumeratesPorts` passes (charitable null on absent device, MIDI-RT-01)
    - `dotnet test --filter MidiHotPlugNeverThrows` passes (T-40-04 — never throws)
    - `dotnet test --filter VirtualMidiNoteBytes` passes (correct bytes + drum→ch9 mapping via CaptureMidiBackend, MIDI-RT-02 + Pitfall 3)
    - `dotnet build flow-lang -p:FlowTarget=Web` exits 0 (RtMidiMidiBackend `#if !FLOW_WEB`-guarded; IMidiBackend/NullMidiBackend/MidiPlaybackManager compile clean on Web with no RtMidi.Core ref)
    - AudioBuffer has a nullable PlaybackStartTime field defaulting to null
  </acceptance_criteria>
  <done>IMidiBackend abstraction + RtMidi.Core backend + Null fallback + manager + alignment seam exist; Wave-0 test seam + MIDI-RT-01/02/04 tests green; Web build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: @midi builtin surface (handle type + data + builtins + module + 3-site opt-in gate) + GM routing reuse + input clamping + LINK-02 determinism test</name>
  <read_first>
    - flow-lang/StandardLibrary/Network/OscFunctions.cs (full — Register + marker + RequireModuleActivated gate at :96-156; named-arg ParameterNames at :115-118)
    - flow-lang/StandardLibrary/Network/OscHandleData.cs (full, 60 lines — required-init handle record shape)
    - flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs (full, 39 lines — sealed-singleton ref-identity type)
    - flow-lang/Runtime/Value.cs:122-124 (Value.OscHandle factory, #if-guarded — model Value.MidiDevice)
    - flow-lang/Runtime/ExecutionContext.cs:437 (OscEnabled), :1164 + :1259 (snapshot/restore — mirror for MidiEnabled)
    - flow-lang/Core/FlowEngine.cs:251-255 (OscFunctions.Register #if !FLOW_WEB site)
    - flow-lang/Runtime/ModuleLoader.cs:56-93 (IsStrippedOnWeb predicate + IsWebTarget advisory)
    - flow-lang/osc.flow (full, 52 lines — module skeleton with trailing marker)
    - flow-lang/StandardLibrary/Notation/InstrumentRouting.cs:45 (ResolveGmProgram — D-40-02 VERBATIM)
    - 40-RESEARCH.md §midi.flow skeleton (435-451), §Pattern 5 GM reuse (302-312), §Security V5 input validation (569, 576-577), §Open Q4 override shape (494-497)
    - 40-PATTERNS.md §MidiFunctions (122-169), §Opt-in module gating 3 edits (298-317), §Web-strip discipline (319-328)
  </read_first>
  <behavior>
    - midiOut routing: each sequence resolves to the SAME (gmProgram, channel) as writeMidi via ResolveGmProgram; drum* → ch9 percussion (asserted via CaptureMidiBackend program-change + channel capture)
    - input clamp: midiNoteOn with pitch 200 or velocity -5 clamps to 0..127 + emits one WarnOnce advisory, never passes the raw Int to native (T-40-01)
    - sysex length-cap: an oversized sysex byte array is capped at the builtin boundary, never handed unbounded to native (T-40-04)
    - module gate: calling a midi builtin without (__enableMidiModule) active → RequireModuleActivated error (mirrors @osc)
    - OfflineRenderIgnoresSync: writeWav output is byte-identical with vs without any MIDI/sync state present (LINK-02 determinism intent)
  </behavior>
  <action>
    Create `flow-lang/TypeSystem/SpecialTypes/MidiDeviceType.cs` (`#if !FLOW_WEB`) — sealed-singleton ref-identity FlowType modeling OscHandleType: `Name => "MidiDevice"`, `GetSpecificity() => 152` (above OscHandle=151, RESEARCH Pattern 3 / D-40-03 discretion), IsCompatibleWith/CanConvertTo `is MidiDeviceType`.

    Create `flow-lang/StandardLibrary/Midi/MidiDeviceData.cs` (`#if !FLOW_WEB`) — required-init record modeling OscHandleData holding the opened `IMidiOutputHandle?` (null = charitable dead handle) + the port Name.

    Edit `flow-lang/Runtime/Value.cs` — add `Value.MidiDevice(StandardLibrary.Midi.MidiDeviceData handle)` factory (`#if !FLOW_WEB`, model :122-124).

    Create `flow-lang/StandardLibrary/Midi/MidiFunctions.cs` (`#if !FLOW_WEB`) with `Register(InternalFunctionRegistry registry, ExecutionContext context)` modeling OscFunctions.Register: a `__enableMidiModule` marker flipping `context.MidiEnabled = true`; `RequireModuleActivated(context, name)` gate (checks `MidiEnabled`); builtins `midiPorts()`, `openMidiOutput(String port) → MidiDevice`, `midiOut(Song song, String port)` + `midiOut(Sequence seq, String port)`, `midiNoteOn(MidiDevice dev, Int ch, Int pitch, Int vel)`, `midiNoteOff(MidiDevice dev, Int ch, Int pitch)`, `midiCC(MidiDevice dev, Int ch, Int ctrl, Int val)`, `midiSysex(MidiDevice dev, Buffer data)`. Each body opens with RequireModuleActivated. `midiOut` calls `InstrumentRouting.ResolveGmProgram(seq.Name)` per sequence (D-40-02 VERBATIM — do NOT write a second table) → `SendProgramChange(ToRtChannel(channel), gmProgram)` then streams notes; dispatch keyed off the AudioBuffer.PlaybackStartTime origin from Task 2 on a sibling thread (best-effort ms). Add the per-sequence override as a NAMED ARG `overrides=` (D-40-02, Open Q4 minimal form: `Dict<String, Int>` name→channel, program still derives from name) via `ParameterNames:` on the signature (D-36-11). **Input validation (T-40-01/T-40-04, Security V5):** charitably clamp channel 0..15, pitch/vel/CC 0..127, length-cap sysex — clamp+WarnOnce, NEVER throw to native (Phase 44 input-perimeter precedent). All [midi] prefix advisories via RenderingDiagnostics.WarnOnce.

    Create `flow-lang/midi.flow` modeling osc.flow: `module midi`, `use "@std"`, `internal proc` decls for `__enableMidiModule`/`midiPorts`/`openMidiOutput`/`midiOut`/`midiNoteOn`/`midiNoteOff`/`midiCC`/`midiSysex`, leading `Note:` doc comments, trailing `(__enableMidiModule)`. (Clock decls — `clockMaster`/`clockSlave` — added by Plan 02.)

    Apply the THREE coordinated opt-in edits (PATTERNS §298-317): (1) `ExecutionContext.cs` — add `public bool MidiEnabled { get; set; } = false;` at :437 + mirror in snapshot :1164 + restore :1259; (2) `FlowEngine.cs:251-255` — add `MidiFunctions.Register(internalRegistry, _context);` inside the existing `#if !FLOW_WEB` block; (3) `ModuleLoader.cs:61` — extend `IsStrippedOnWeb` predicate to `|| requestedPath == "@midi"` (the existing IsWebTarget advisory at :86-93 fires, no new code).

    Apply Web-strip csproj edits (`<ItemGroup Condition="'$(FlowTarget)' == 'Web'">`): `<Compile Remove="Audio\RtMidiMidiBackend.cs" />`, `<Compile Remove="StandardLibrary\Midi\**\*.cs" />`, `<Compile Remove="TypeSystem\SpecialTypes\MidiDeviceType.cs" />`, `<None Remove="midi.flow" />`.

    Create `flow-lang.Tests/Integration/Phase40/OfflineRenderDeterminismTests.cs` — Fact `OfflineRenderIgnoresSync` asserting `writeWav` output is byte-identical with vs without any MIDI/sync state present (LINK-02 invariant — writable now even though Link itself is deferred to Plan 03). Add a `.flow` smoke + GM-routing assertions to VirtualMidiTests for midiOut drum→ch9.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase40" && dotnet build flow-lang -p:FlowTarget=Web</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter OfflineRenderIgnoresSync` passes (LINK-02 byte-identical writeWav)
    - `dotnet test --filter VirtualMidiNoteBytes` passes including midiOut drum* → ch9 program/channel via ResolveGmProgram (D-40-02)
    - A clamp test asserts midiNoteOn(dev, 0, 200, -5) clamps to 0..127 + one advisory, no throw, no out-of-range byte reaches the capture seam (T-40-01)
    - A sysex length-cap test asserts an oversized array is capped at the builtin (T-40-04)
    - `dotnet build flow-lang -p:FlowTarget=Web` exits 0 AND `dotnet test --filter AssemblyReferenceScan` passes (RtMidi.Core/JackSharp absent from Web dll, T-40-03)
    - `dotnet test flow-lang.Tests --filter FullyQualifiedName~Phase40` all green; full Desktop suite has no regressions
    - A `.flow` smoke `use "@midi"` followed by `(midiPorts)` runs without throwing on this lib-absent box (charitable NullMidiBackend)
  </acceptance_criteria>
  <done>@midi surface (high-level midiOut + low-level event builtins) ships with GM routing reuse, named-arg override, input clamping, 3-site opt-in gate, Web-strip, and the LINK-02 determinism test — all green; Web build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flow builtin → RtMidi.Core native send | Composer-supplied Int channel/pitch/vel/CC + Buffer sysex cross into a native P/Invoke layer — untrusted-shape input |
| flow-lang.dll → Web (WASM) closure | Native MIDI deps must NOT cross into the browser-shipped assembly |
| Offline render (writeWav/writeMidi) → sync state | Any clock/sync tempo must NEVER cross into deterministic offline render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | midiNoteOn/midiCC/midiNoteOff/midiOut builtin boundary | mitigate | Clamp channel 0..15, pitch/vel/CC 0..127 at the builtin before any native call (Task 3); charitable clamp+WarnOnce, never pass raw Int to RtMidi `Channel`/`Key` enums (Phase 44 input-perimeter precedent) |
| T-40-03 | Tampering | flow-lang.csproj Web ItemGroup + AssemblyReferenceScanTests | mitigate (HIGH/block) | RtMidi.Core PackageReference under `!= 'Web'` group; all RtMidi-referencing files `#if !FLOW_WEB` + `Compile Remove` on Web; AssemblyReferenceScanTests forbids `RtMidi.Core` (present) + `JackSharp` (added Task 1) — `dotnet build -p:FlowTarget=Web` green is the gate |
| T-40-04 | Denial of Service | midiSysex builtin + device hot-plug / DllNotFound | mitigate | Length-cap sysex at the builtin (Task 3); probe + NullMidiBackend + WarnOnce on missing librtmidi.so (Task 2); OpenOutput on absent port returns null, never throws — MidiHotPlugNeverThrows pins it |
| T-40-02 | License compromise | (Ableton Link GPL) | n/a this plan | Link is deferred (Plan 03 records it); no GPL binary is referenced here. The AssemblyReferenceScan seam stands ready |
</threat_model>

<verification>
- `dotnet build flow-lang -p:FlowTarget=Desktop` exits 0
- `dotnet build flow-lang -p:FlowTarget=Web` exits 0 (RtMidi.Core absent from Web closure)
- `dotnet test flow-lang.Tests --filter FullyQualifiedName~Phase40` all green (MIDI-RT-01/02/04 + LINK-02 + spike)
- `dotnet test flow-lang.Tests --filter AssemblyReferenceScan` green (T-40-03)
- Full Desktop suite (`dotnet test flow-lang.Tests`) — no regression in prior phases
</verification>

<success_criteria>
- Composer can `use "@midi"` then `(midiPorts)` / `(openMidiOutput "name")` / `(midiOut song "port")` / `(midiNoteOn ...)` — charitable on this lib-absent box (MIDI-RT-01/02, SC1)
- (midiOut song "port") routes via ResolveGmProgram identically to writeMidi (D-40-02)
- Out-of-range args clamped, oversized sysex capped, hot-plug never throws (T-40-01/04, MIDI-RT-04 SC2)
- AudioBuffer.PlaybackStartTime origin seam exists; MIDI dispatch is buffer-relative ms-aligned best-effort (honest, MIDI-RT-04)
- writeWav byte-identical regardless of sync state (LINK-02 intent)
- Open Q1 clock-access strategy resolved + recorded for Plan 02
- Web build clean; RtMidi.Core/JackSharp forbidden-prefix gate green (T-40-03)
</success_criteria>

<output>
Create `.planning/phases/40-studio-sync/40-01-SUMMARY.md` when done. Record the resolved Open Q1 access strategy explicitly so Plan 02 implements clock against it.
</output>
