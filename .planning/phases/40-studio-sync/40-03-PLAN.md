---
phase: 40-studio-sync
plan: 03
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - flow-lang/flow-lang.csproj
  - flow-lang/StandardLibrary/Midi/JackFunctions.cs
  - flow-lang/StandardLibrary/Midi/JackHandleData.cs
  - flow-lang/TypeSystem/SpecialTypes/JackHandleType.cs
  - flow-lang/Runtime/Value.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang/Runtime/ModuleLoader.cs
  - flow-lang/Core/FlowEngine.cs
  - flow-lang/jack.flow
  - flow-lang.Tests/Integration/Phase40/JackTransportTests.cs
  - flow-lang.Tests/Integration/Phase40/LinkDeferralTests.cs
  - .planning/phases/40-studio-sync/40-HUMAN-UAT.md
  - .planning/phases/40-studio-sync/40-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements: [JACK-01, LINK-01, LINK-02, MIDI-RT-03]

must_haves:
  truths:
    - "(jackSync) opt-in via use \"@jack\" reads JACK transport position/tempo and drives MusicalContext.Tempo + bar/beat when a JACK server is present (JACK-01)"
    - "Absence of a JACK server does not affect non-JACK workflows — (jackSync) with no server → charitable no-op + advisory, never throws (JACK-01)"
    - "Ableton Link (LINK-01) is DEFERRED to community/v1.6 per D-40-06 (GPLv2+ contamination) — NO Link implementation ships; the defer is recorded honestly (NOT registering the builtin + documenting, or a community stub + charitable advisory if (linkEnable) is somehow invoked)"
    - "LINK-02 determinism intent is SHIPPED: writeWav/writeMidi byte-identical regardless of any sync state — the OfflineRenderDeterminismTests gate (Plan 01) covers it; this plan adds a LinkDeferralTests assertion that no Link/sync path touches offline render"
    - "MIDI-RT-03 (CoreMIDI/WinMM) is recorded as deferred to Phase 41 cross-platform binary work (no Phase 40 work) — same IMidiBackend abstraction covers it later"
    - "40-HUMAN-UAT.md exists with real-hardware + DAW + Link-peer rows (model Phase 48/49 HUMAN-UAT), splitting machine-proven from human-confirmed (D-40-07)"
    - "JackSharp (if shipped) stays Desktop-only + in the AssemblyReferenceScan forbidden-prefix list (T-40-03); if the net10 compat spike fails, JACK hand-rolls a minimal libjack P/Invoke OR defers cleanly with Link"
  artifacts:
    - path: "flow-lang/StandardLibrary/Midi/JackFunctions.cs"
      provides: "jackSync builtin (best-effort, charitable absent-server) + @jack gate"
      exports: ["Register"]
    - path: "flow-lang/jack.flow"
      provides: "opt-in @jack module"
      contains: "module jack"
    - path: ".planning/phases/40-studio-sync/40-HUMAN-UAT.md"
      provides: "hardware/DAW/Link-peer manual verification checklist"
      contains: "HUMAN-UAT"
    - path: ".planning/phases/40-studio-sync/40-VERIFICATION.md"
      provides: "per-requirement closure trace (MIDI-RT-01..04, CLOCK-01/02, LINK-01/02, JACK-01) with honest covered/deferred/human-needed status"
      contains: "MIDI-RT-01"
    - path: "flow-lang.Tests/Integration/Phase40/LinkDeferralTests.cs"
      provides: "asserts no Link/sync path touches offline render (LINK-02) + records LINK-01 deferral"
      contains: "LinkDeferral"
  key_links:
    - from: "flow-lang/StandardLibrary/Midi/JackFunctions.cs"
      to: "flow-lang/Runtime/MusicalContext.cs"
      via: "jackSync drives Tempo + bar/beat from transport query"
      pattern: "Tempo"
    - from: "flow-lang/Runtime/ModuleLoader.cs"
      to: "@jack"
      via: "IsStrippedOnWeb predicate extension → charitable Web advisory"
      pattern: "@jack"
    - from: "flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs"
      to: "JackSharp"
      via: "forbidden-prefix gate (added in Plan 01) keeps JackSharp out of the Web closure"
      pattern: "JackSharp"
---

<objective>
Close Phase 40: ship JACK transport best-effort (JACK-01), record the Ableton Link deferral honestly (LINK-01 deferred per D-40-06; LINK-02 determinism already shipped in Plan 01), and author the phase closer — `40-HUMAN-UAT.md` (real-hardware + DAW + Link-peer rows) + `40-VERIFICATION.md` (per-requirement closure trace) + the tracking-file sweep (ROADMAP/STATE/REQUIREMENTS/CLAUDE.md).

JACK is best-effort (D-40-05): a first compat task confirms JackSharp 0.4.0 (net35) loads under net10 and exposes `jack_transport_query` (Open Q3); if it loads → ship `(jackSync)` with charitable absent-server behavior; if it does not → hand-roll a minimal `libjack` P/Invoke for transport state + BPM, OR defer JACK cleanly alongside Link (document the verdict). LINK gets NO implementation — only an honest recorded deferral + the LINK-02 determinism assertion.

Purpose: Best-effort JACK for Linux pro-audio composers (JACK-01, SC5); honest Link/MIDI-RT-03 deferral; human-verifiable closure (D-40-06/07).
Output: JACK builtin + module + handle type/data (if shipped); LinkDeferralTests; JackTransportTests; 40-HUMAN-UAT.md; 40-VERIFICATION.md; tracking-file sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-studio-sync/40-CONTEXT.md
@.planning/phases/40-studio-sync/40-RESEARCH.md
@.planning/phases/40-studio-sync/40-PATTERNS.md
@.planning/phases/40-studio-sync/40-VALIDATION.md
@.planning/phases/40-studio-sync/40-01-SUMMARY.md
@.planning/phases/40-studio-sync/40-02-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md
@.planning/phases/49-flowlang-dev-site/49-HUMAN-UAT.md

<interfaces>
<!-- From Plans 01/02 + the live tree. -->

The @midi opt-in 3-site gate is already wired (Plan 01). @jack needs its OWN parallel gate:
  - ExecutionContext.cs:437 — add `public bool JackEnabled { get; set; } = false;` + mirror snapshot :1164 / restore :1259 (model the MidiEnabled additions from Plan 01)
  - FlowEngine.cs:251-255 — add `JackFunctions.Register(...)` inside the existing #if !FLOW_WEB block
  - ModuleLoader.cs:61 — extend IsStrippedOnWeb to `|| requestedPath == "@jack"` (Plan 01 already added "@midi")

From flow-lang/Runtime/MusicalContext.cs:43 + :180:
  public double? Tempo { get; set; }   // jackSync drives this from transport query
  public static bool IsValidTempo(double tempo)

From flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs + ClockHandleType (Plan 02, specificity 153):
  JackHandleType → specificity 154 (distinct; D-40-03 discretion)

From flow-lang/StandardLibrary/Network/OscFunctions.cs (Register + charitable bind-failure dead-handle model)
From flow-lang/Diagnostics/RenderingDiagnostics.cs:29: WarnOnce(key, msg)   // [jack] prefix

JackSharp 0.4.0: net35, MIT, zero deps, archived repo. net10 compat via net4x shim is UNVERIFIED
(RESEARCH Open Q3, A3). `libjack.so.0` present on dev box. Transport API: jack_transport_query.

Web-strip for JACK (csproj `== 'Web'` group): Compile-Remove JackFunctions.cs (covered by the
StandardLibrary\Midi\**\*.cs wildcard from Plan 01) + JackHandleType.cs; <None Remove="jack.flow" />.
PackageReference JackSharp under the `!= 'Web'` group (beside RtMidi.Core from Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JACK best-effort — JackSharp net10 compat spike + jackSync builtin (charitable absent-server) OR clean defer</name>
  <read_first>
    - flow-lang/StandardLibrary/Network/OscFunctions.cs (Register + charitable bind-failure dead-handle path at :397-405)
    - flow-lang/TypeSystem/SpecialTypes/OscHandleType.cs (full — JackHandleType template) + Plan 02's ClockHandleType.cs (specificity precedent 153)
    - flow-lang/Runtime/Value.cs:122-124 (factory model — Value.JackHandle)
    - flow-lang/Runtime/ExecutionContext.cs:437,1164,1259 (gate-bool 3 sites — JackEnabled mirrors MidiEnabled from Plan 01)
    - flow-lang/Core/FlowEngine.cs:251-255 + flow-lang/Runtime/ModuleLoader.cs:56-93 (register + Web advisory)
    - flow-lang/osc.flow (full — jack.flow skeleton model) + flow-lang/flow-lang.csproj:107-141 (Web-strip groups)
    - flow-lang/Runtime/MusicalContext.cs:43,180 (Tempo write + IsValidTempo)
    - 40-RESEARCH.md §JACK-01 row (line 59), §Stack JackSharp (line 101), §Open Q3 (489-492), §A3 (line 471)
    - 40-PATTERNS.md §JackFunctions (line 22), §Web-strip discipline (319-328)
  </read_first>
  <action>
    First confirm the JackSharp net10 compat question (Open Q3). Add `<PackageReference Include="JackSharp" Version="0.4.0" />` to the existing `<ItemGroup Condition="'$(FlowTarget)' != 'Web'">` (beside RtMidi.Core from Plan 01) and run a build. If `dotnet build flow-lang -p:FlowTarget=Desktop` resolves JackSharp and a reflective probe confirms `jack_transport_query`-equivalent transport access is reachable → proceed with JackSharp. If the net35 shim does NOT load on net10 → either hand-roll a minimal `[DllImport("jack")]` for `jack_transport_query` (transport state + BPM only) OR defer JACK cleanly alongside Link (D-40-05 best-effort latitude) — record the verdict in the SUMMARY + VERIFICATION. Do NOT spend deep budget; JACK is best-effort.

    If shipping: create `flow-lang/TypeSystem/SpecialTypes/JackHandleType.cs` (`#if !FLOW_WEB`, specificity 154, model OscHandleType); `flow-lang/StandardLibrary/Midi/JackHandleData.cs` (`#if !FLOW_WEB`, required-init handle record); `Value.JackHandle(...)` factory in Value.cs (`#if !FLOW_WEB`). Create `flow-lang/StandardLibrary/Midi/JackFunctions.cs` (`#if !FLOW_WEB`) with `Register(registry, context)` exposing `jackSync()`: query JACK transport, drive `MusicalContext.Tempo` + bar/beat (validate via IsValidTempo), return a JackHandle. **Charitable absent-server (JACK-01):** no JACK server → WarnOnce("jack-absent", "[jack] no JACK server — (jackSync) is a no-op") + return a dead handle, NEVER throw; non-JACK workflows are unaffected. Each body opens with a `RequireModuleActivated(context, "jackSync")` gate against a NEW `JackEnabled` bool.

    Apply the @jack opt-in 3-site gate (parallel to Plan 01's @midi): (1) `ExecutionContext.cs` JackEnabled at :437 + snapshot :1164 + restore :1259; (2) `FlowEngine.cs:251-255` add `JackFunctions.Register(...)` in the `#if !FLOW_WEB` block; (3) `ModuleLoader.cs:61` extend IsStrippedOnWeb with `|| requestedPath == "@jack"`. Create `flow-lang/jack.flow` modeling osc.flow (`module jack`, `use "@std"`, `internal proc jackSync ()`, trailing marker if a `__enableJackModule` marker is used). Apply Web-strip csproj: `<Compile Remove="TypeSystem\SpecialTypes\JackHandleType.cs" />` + `<None Remove="jack.flow" />` (JackFunctions.cs/JackHandleData.cs covered by the StandardLibrary\Midi\**\*.cs wildcard from Plan 01).

    Create `flow-lang.Tests/Integration/Phase40/JackTransportTests.cs` — Fact `JackAbsentServerNoOp` asserting `(jackSync)` with no JACK server present → charitable no-op + advisory, no throw, MusicalContext.Tempo unchanged (charitable-skip the real-transport-drives-tempo portion when no server; model MusicXmlRoundTripTests charitable-skip). If JACK was deferred instead, JackTransportTests records the deferral as a documented skip (still asserts non-JACK workflows are unaffected).
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter FullyQualifiedName~JackTransport && dotnet build flow-lang -p:FlowTarget=Web</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter JackAbsentServerNoOp` passes (absent server → no-op + advisory, no throw, Tempo unchanged — JACK-01 charitable rule)
    - The JackSharp-net10 verdict (shipped / hand-rolled P/Invoke / deferred) is recorded in the SUMMARY
    - If shipped: `(jackSync)` via `use "@jack"` resolves on Desktop; `dotnet build flow-lang -p:FlowTarget=Web` exits 0 (JackFunctions/JackHandleType Compile-Removed; JackSharp under `!= 'Web'`)
    - `dotnet test --filter AssemblyReferenceScan` passes (JackSharp absent from Web dll — T-40-03; the forbidden-prefix entry was added in Plan 01)
    - Non-JACK workflows demonstrably unaffected by JACK absence
  </acceptance_criteria>
  <done>JACK transport ships best-effort with charitable absent-server behavior (JACK-01), OR is cleanly deferred with a recorded verdict; @jack gate + Web-strip applied; AssemblyReferenceScan green.</done>
</task>

<task type="auto">
  <name>Task 2: Record Ableton Link deferral (LINK-01) honestly + LINK-02 determinism assertion + MIDI-RT-03 deferral note</name>
  <read_first>
    - flow-lang.Tests/Integration/Phase40/OfflineRenderDeterminismTests.cs (Plan 01 — the LINK-02 gate this task reinforces)
    - 40-CONTEXT.md D-40-06 (Link license posture CONSERVATIVE) + 40-RESEARCH.md §Link defer (lines 57-58, 72, 102, 132-134, 462-463), §LINK-02 row (line 58)
    - 40-VALIDATION.md rows for LINK-01 (deferred) + LINK-02 (line 51, 47)
    - .planning/REQUIREMENTS.md lines 136-139 (LINK-01/02 text) + D-v1.5-04 (line 13)
  </read_first>
  <action>
    Record the Ableton Link deferral honestly per D-40-06 (GPLv2+ contamination → defer to community/v1.6). Do NOT implement Link, do NOT P/Invoke libabl_link, do NOT add any GPL binary reference. The honest record = NOT registering a Link builtin + documenting the defer (preferred), OR a tiny community-contribution stub that emits a charitable advisory `[link] Ableton Link deferred to community/v1.6 (GPL license gate) — see D-40-06` if `(linkEnable)` is somehow invoked. Pick the simpler honest option; there is no `@link` module shipped this phase.

    Create `flow-lang.Tests/Integration/Phase40/LinkDeferralTests.cs` — Fact `LinkDeferral_NoGplReference` (asserts no `libabl_link`/Ableton-Link symbol is referenced anywhere — the AssemblyReferenceScan forbidden-prefix gate is the structural enforcement; this is a documentation-anchored assertion) + Fact `OfflineRenderIgnoresSync_LinkDeferred` reinforcing LINK-02: writeWav byte-identical regardless of any sync state — restating that no Link/clock/JACK path touches offline render. This makes LINK-02's determinism intent SHIPPED even though Link itself ships nothing.

    Confirm MIDI-RT-03 (CoreMIDI/WinMM) is recorded as deferred to Phase 41 cross-platform binary work — the same IMidiBackend abstraction (Plan 01) covers it later. No Phase 40 implementation; this is a recorded note for VERIFICATION (Task 3).
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "FullyQualifiedName~LinkDeferral|FullyQualifiedName~OfflineRender" && dotnet build flow-lang -p:FlowTarget=Web</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter OfflineRenderIgnoresSync` (Plan 01) + `dotnet test --filter LinkDeferral` both pass (LINK-02 determinism shipped; LINK-01 deferral recorded)
    - No `libabl_link` / Ableton-Link / GPL reference exists in the tree (AssemblyReferenceScan + LinkDeferralTests assert it)
    - `dotnet build flow-lang -p:FlowTarget=Web` exits 0
    - LINK-01 (deferred), LINK-02 (shipped-determinism), MIDI-RT-03 (deferred Phase 41) statuses are ready for the VERIFICATION trace (Task 3)
  </acceptance_criteria>
  <done>Ableton Link deferred honestly per D-40-06 (no GPL reference); LINK-02 determinism shipped + asserted; MIDI-RT-03 deferral recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Phase closer — author 40-HUMAN-UAT.md + 40-VERIFICATION.md + tracking-file sweep (ROADMAP/STATE/REQUIREMENTS/CLAUDE.md)</name>
  <read_first>
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md + .planning/phases/49-flowlang-dev-site/49-HUMAN-UAT.md (HUMAN-UAT format models — row structure, pass/fail/skip, honest split)
    - .planning/phases/49-flowlang-dev-site/49-VERIFICATION.md (per-REQ closure trace format — status closed/human_needed/deferred, decision trace, caveats)
    - 40-VALIDATION.md §Manual-Only Verifications (69-76) + §Per-Task Verification Map (40-51)
    - .planning/phases/40-studio-sync/40-01-SUMMARY.md + 40-02-SUMMARY.md (what shipped) + this plan's Task 1/2 SUMMARY content (JACK verdict + Link defer)
    - .planning/ROADMAP.md:269-282 (Phase 40 section + Plans line) + :383-391 (requirement-status table) + :127 (phase checklist line)
    - .planning/REQUIREMENTS.md:124-143 (MIDI-RT/CLOCK/LINK/JACK rows) + :383-391 (status table)
    - .planning/STATE.md frontmatter + Current Position
    - CLAUDE.md (where Phase 38 @osc + Phase 47/48 entries live — add a Phase 40 studio-sync section in the same register)
  </read_first>
  <action>
    Author `.planning/phases/40-studio-sync/40-HUMAN-UAT.md` modeling Phase 48/49 HUMAN-UAT: rows for (a) real hardware synth produces sound on `(midiOut song "port")` — connect synth, run example, confirm audible (MIDI-RT-01/02); (b) DAW follows Flow as clock master + Flow follows DAW as slave — set DAW external sync, confirm BPM lock (CLOCK-01/02); (c) MIDI-audio alignment is "tight enough" perceptually — honest "best-effort ms-aligned, NOT sample-accurate" framing (MIDI-RT-04); (d) JACK transport drives tempo if shipped (JACK-01); (e) Ableton Link peer row marked DEFERRED (no test, records the D-40-06 verdict). Split machine-proven (CaptureMidiBackend tests) from human-confirmed (real hardware/DAW) honestly. Include the `librtmidi.so` + `snd-virmidi` prerequisite + `apt install librtmidi-dev` setup note.

    Author `.planning/phases/40-studio-sync/40-VERIFICATION.md` modeling 49-VERIFICATION.md: a per-requirement closure trace for ALL 9 IDs — MIDI-RT-01/02/04 (closed, automated + HUMAN-UAT for hardware), MIDI-RT-03 (deferred Phase 41), CLOCK-01/02 (closed, automated + HUMAN-UAT for DAW), LINK-01 (deferred — GPL, D-40-06), LINK-02 (closed — determinism shipped), JACK-01 (closed best-effort / or deferred per Task 1 verdict). Map each to its test name + status (closed / human_needed / deferred) + D-40-NN decision trace + honest caveats (best-effort ms-align, Link deferred, librtmidi.so native prerequisite, JackSharp net10 verdict).

    Sweep tracking files: (1) ROADMAP.md — set Phase 40 `**Plans:**` to "3 plans" + the plan list (40-01/02/03 with brief objectives), update the requirement-status table rows (:383-391) MIDI-RT-01/02/04 + CLOCK-01/02 + LINK-02 → done-or-as-closed, MIDI-RT-03 + LINK-01 → deferred, JACK-01 → per verdict, mark the phase checklist line :127; (2) STATE.md — advance Current Position to Phase 40 complete/verifying, update progress counts + a Phase 40 highlights entry in the established register; (3) REQUIREMENTS.md status table (:383-391) — same status updates; (4) CLAUDE.md — add a "Studio Sync (Phase 40)" section in the same register as the Phase 38 @osc / Phase 47/48 entries (the `@midi` surface + clock + JACK best-effort + Link deferred + librtmidi.so prerequisite + Web-strip note). Honest language throughout: "best-effort ms-aligned not sample-accurate", "Link deferred to community/v1.6 (GPL)", "JACK best-effort".
  </action>
  <verify>
    <automated>test -f .planning/phases/40-studio-sync/40-HUMAN-UAT.md && test -f .planning/phases/40-studio-sync/40-VERIFICATION.md && grep -q "MIDI-RT-01" .planning/phases/40-studio-sync/40-VERIFICATION.md && grep -q "LINK-01" .planning/phases/40-studio-sync/40-VERIFICATION.md && dotnet test flow-lang.Tests --filter FullyQualifiedName~Phase40</automated>
  </verify>
  <acceptance_criteria>
    - 40-HUMAN-UAT.md exists with real-hardware + DAW-master + DAW-slave + MIDI-audio-alignment + JACK + Link-deferred rows (D-40-07), splitting machine-proven from human-confirmed
    - 40-VERIFICATION.md exists with a per-requirement trace for ALL 9 IDs (grep confirms MIDI-RT-01 + LINK-01 present); each ID maps to a status (closed / human_needed / deferred) + D-40-NN trace
    - ROADMAP.md Phase 40 Plans line reads "3 plans" + lists 40-01/02/03; requirement-status table + phase checklist line updated
    - STATE.md Current Position advanced to Phase 40 + a Phase 40 highlights entry added in the established register
    - REQUIREMENTS.md status table updated (MIDI-RT-01/02/04 + CLOCK-01/02 + LINK-02 closed; MIDI-RT-03 + LINK-01 deferred; JACK-01 per verdict)
    - CLAUDE.md has a Studio Sync (Phase 40) section in the same register as the Phase 38/47/48 entries
    - `dotnet test flow-lang.Tests --filter FullyQualifiedName~Phase40` all green; honest language throughout (best-effort ms-align, Link deferred GPL, JACK best-effort)
  </acceptance_criteria>
  <done>Phase 40 closer authored: 40-HUMAN-UAT.md + 40-VERIFICATION.md cover all 9 requirements honestly; ROADMAP/STATE/REQUIREMENTS/CLAUDE.md swept; full Phase 40 test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JACK server → jackSync transport read | External transport position/tempo crosses into Flow's MusicalContext |
| flow-lang.dll → Web (WASM) closure | JackSharp native dep must NOT cross into the browser-shipped assembly |
| Flow MIT distribution → GPL binary | Ableton Link GPLv2+ must never link into MIT flow-lang.dll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | License compromise / legal | Ableton Link (libabl_link) GPLv2+ | mitigate (HIGH/block) | DEFER Link entirely (Task 2, D-40-06) — no libabl_link reference, no GPL binary; AssemblyReferenceScan forbidden-prefix gate + LinkDeferralTests are the structural enforcement; default to not-shipping over MIT contamination |
| T-40-03 | Tampering | JackSharp in Web closure | mitigate (HIGH/block) | JackSharp PackageReference under `!= 'Web'` group; JackFunctions/JackHandleType `#if !FLOW_WEB` + Compile-Removed on Web; AssemblyReferenceScanTests forbids `JackSharp` (added Plan 01) — `dotnet build -p:FlowTarget=Web` green is the gate |
| T-40-04 | Denial of Service | jackSync on missing JACK server | mitigate | Charitable no-op + WarnOnce on absent server (Task 1); JackAbsentServerNoOp pins it never throws; non-JACK workflows unaffected |
| T-40-01 | Tampering | JACK transport tempo → MusicalContext.Tempo | mitigate | Derived transport tempo validated via IsValidTempo before write; out-of-range rejected, not written |
</threat_model>

<verification>
- `dotnet test flow-lang.Tests --filter FullyQualifiedName~Phase40` all green (JACK + Link-defer + all prior Plan 01/02 tests)
- `dotnet build flow-lang -p:FlowTarget=Web` exits 0; `dotnet test --filter AssemblyReferenceScan` green (RtMidi.Core + JackSharp absent from Web dll — T-40-02/03)
- Full Desktop suite (`dotnet test flow-lang.Tests`) — no regression across all prior phases
- 40-HUMAN-UAT.md + 40-VERIFICATION.md exist and cover all 9 requirement IDs
</verification>

<success_criteria>
- Composer can opt into JACK transport (Linux) via `use "@jack"` + `(jackSync)`; absent server → no-op, non-JACK workflows unaffected (JACK-01, SC5) — OR JACK cleanly deferred with a recorded verdict (D-40-05 best-effort)
- Ableton Link deferred honestly per D-40-06 (no GPL reference); LINK-01 recorded as community/v1.6 defer
- LINK-02 determinism shipped + asserted (writeWav byte-identical regardless of sync state)
- MIDI-RT-03 (CoreMIDI/WinMM) recorded as deferred to Phase 41
- 40-HUMAN-UAT.md (hardware/DAW/Link rows, machine-vs-human split) + 40-VERIFICATION.md (all-9-ID trace) authored (D-40-07)
- Tracking files swept; Web build clean; no GPL contamination (T-40-02/03)
</success_criteria>

<output>
Create `.planning/phases/40-studio-sync/40-03-SUMMARY.md` when done. Record the JACK verdict (shipped / hand-rolled / deferred) and confirm the Link deferral + all-9-ID VERIFICATION trace.
</output>
