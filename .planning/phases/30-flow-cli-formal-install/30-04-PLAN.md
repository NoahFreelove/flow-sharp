---
phase: 30-flow-cli-formal-install
plan: 04
type: execute
wave: 3
depends_on: [30-01, 30-02, 30-03]
files_modified:
  - flow-cli/flow-cli.csproj
  - flow-cli/Properties/PublishProfiles/linux-x64.pubxml
  - scripts/publish.sh
  - flow-lang/flow-lang.csproj
autonomous: true
requirements: [REQ-2]

must_haves:
  truths:
    - "scripts/publish.sh runs dotnet publish with the locked flag set (per RESEARCH dotnet publish Profile) and produces a self-contained Linux x64 single-file binary at publish/flow-linux-x64/flow"
    - "The published binary requires NO .NET runtime install on the target machine (self-contained)"
    - "Stdlib .flow files (std.flow, collections.flow, audio.flow, bars.flow, notation.flow, composition.flow) are present in the publish output directory alongside the flow binary (so use \"@audio\" resolves post-install)"
    - "du -sh on the publish directory reports a total size <= 120 MB (SPEC-2 constraint)"
    - "publish/flow-linux-x64/flow version exits 0 and prints the version string"
    - "publish/flow-linux-x64/flow run /tmp/<scaffold>.flow exits 0 (validates stdlib resolution works post-publish)"
    - "publish/flow-linux-x64/flow eval 'Int x = 5; (print (str x))' prints 5"
  artifacts:
    - path: "flow-cli/Properties/PublishProfiles/linux-x64.pubxml"
      provides: "Visual-Studio-style publish profile committing the locked publish flags so devs can dotnet publish flow-cli -p:PublishProfile=linux-x64"
    - path: "scripts/publish.sh"
      provides: "One-command dotnet publish wrapper; CI calls this"
      contains: "dotnet publish flow-cli, -c Release, -r linux-x64, --self-contained true, -p:PublishSingleFile=true, -p:PublishTrimmed=false, -p:IncludeNativeLibrariesForSelfExtract=true, -p:EnableCompressionInSingleFile=true"
    - path: "flow-lang/flow-lang.csproj"
      provides: "Stdlib .flow files marked CopyToPublishDirectory=PreserveNewest (in addition to existing CopyToOutputDirectory) — so they ride along into publish output"
  key_links:
    - from: "scripts/publish.sh"
      to: "flow-cli/Properties/PublishProfiles/linux-x64.pubxml"
      via: "dotnet publish flow-cli -p:PublishProfile=linux-x64 -o publish/flow-linux-x64"
      pattern: "PublishProfile=linux-x64"
    - from: "flow-cli published binary"
      to: "publish/flow-linux-x64/std.flow"
      via: "FlowLang.Runtime.ModuleLoader.ResolveStdlibPath — already resolves relative to the assembly directory"
      pattern: "ResolveStdlibPath"
---

<objective>
Implement REQ-2: scripts/publish.sh + a PublishProfile lock the dotnet publish command to a known-good flag set, producing a self-contained Linux x64 single-file binary named 'flow' with stdlib .flow files copied into the publish output. Verify the published binary can run a scaffold script without .NET SDK installed (proxy: run from a clean env, do not invoke dotnet at runtime).

Purpose: REQ-2 acceptance — running ./flow from the publish dir works on a clean Linux system with PulseAudio and no .NET runtime. The 120 MB cap is enforced.

Output: publish/flow-linux-x64/flow self-contained binary; helper script for CI; stdlib bundled into the publish dir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-flow-cli-formal-install/30-SPEC.md
@.planning/phases/30-flow-cli-formal-install/30-RESEARCH.md
@CLAUDE.md
@flow-cli/flow-cli.csproj
@flow-lang/flow-lang.csproj

<interfaces>
RESEARCH-locked dotnet publish flag set (per 30-RESEARCH.md dotnet publish Profile section):
```
dotnet publish flow-cli/flow-cli.csproj \
  -c Release \
  -r linux-x64 \
  --self-contained true \
  -p:PublishSingleFile=true \
  -p:PublishTrimmed=false \
  -p:DebugType=embedded \
  -p:IncludeNativeLibrariesForSelfExtract=true \
  -p:EnableCompressionInSingleFile=true \
  -o publish/flow-linux-x64
```

Each flag's rationale is in 30-RESEARCH.md offset=504 limit=20 (flag-by-flag rationale table). DO NOT add PublishReadyToRun=true (known regression with EnableCompressionInSingleFile per dotnet/runtime#101866). DO NOT change PublishTrimmed (reflection in OverloadResolver/InternalFunctionRegistry/ModuleLoader requires trim=false per RESEARCH Assumption A2).

flow-lang/flow-lang.csproj already has CopyToOutputDirectory entries for std.flow, collections.flow, audio.flow, bars.flow, notation.flow, composition.flow, test.flow:
```
<None Update="std.flow"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None>
...
```

For publish, CopyToOutputDirectory ALONE is not enough — must also add CopyToPublishDirectory=PreserveNewest so the files ride into the publish output directory. Per Microsoft Learn (dotnet publish documentation), CopyToOutputDirectory only copies to bin/, not to the publish/ directory.

flow-lang/Runtime/ModuleLoader.ResolveStdlibPath (existing): resolves "@name" -> {assembly-dir}/{name}.flow. Confirmed in 30-SPEC.md Background: "ResolveStdlibPath already resolves stdlib relative to the loaded assembly directory, so a dotnet publish that copies stdlib alongside the DLL works without resolver changes."

PublishProfile .pubxml format (standard Microsoft pattern):
```xml
<?xml version="1.0" encoding="utf-8"?>
<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <Platform>Any CPU</Platform>
    <PublishDir>publish/flow-linux-x64/</PublishDir>
    <PublishProtocol>FileSystem</PublishProtocol>
    <TargetFramework>net10.0</TargetFramework>
    <RuntimeIdentifier>linux-x64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <PublishTrimmed>false</PublishTrimmed>
    <DebugType>embedded</DebugType>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
  </PropertyGroup>
</Project>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CopyToPublishDirectory to stdlib .flow files in flow-lang.csproj</name>
  <files>flow-lang/flow-lang.csproj</files>

  <read_first>
    - flow-lang/flow-lang.csproj (full file — find the existing ItemGroup with None Update entries for std.flow, collections.flow, audio.flow, bars.flow, notation.flow, composition.flow, test.flow)
    - ls flow-lang/*.flow (confirm the 6 stdlib files plus test.flow — test.flow may not be needed in publish; skip it)
  </read_first>

  <action>
Edit the existing ItemGroup containing the None Update entries for stdlib .flow files. For EACH of the 6 stdlib files (std.flow, collections.flow, audio.flow, bars.flow, notation.flow, composition.flow), the entry must contain BOTH:
- `<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>` (existing — keeps dev builds working)
- `<CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>` (NEW — makes them ride into publish output)

Skip test.flow — it is dev-test material, not stdlib. Either leave it as CopyToOutputDirectory-only OR remove the test.flow entry entirely if it was a leftover.

After edit, the ItemGroup must have 6 stdlib entries with BOTH copy directives, plus the test.flow entry untouched (or removed).
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang 2>&amp;1 | tail -3 &amp;&amp; grep -c 'CopyToPublishDirectory' flow-lang/flow-lang.csproj &amp;&amp; grep -B1 'CopyToPublishDirectory' flow-lang/flow-lang.csproj | grep -E 'std\.flow|collections\.flow|audio\.flow|bars\.flow|notation\.flow|composition\.flow' | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-lang exits 0
    - grep -c 'CopyToPublishDirectory' flow-lang/flow-lang.csproj returns at least 6 (one per stdlib file)
    - grep -c 'CopyToOutputDirectory' flow-lang/flow-lang.csproj returns at least 6 (existing entries preserved)
    - The 6 stdlib files (std, collections, audio, bars, notation, composition) each appear in an ItemGroup with BOTH copy directives — verify by reading the modified ItemGroup and counting paired entries
    - Existing dotnet run --project flow-interpreter examples/showcase.flow still exits 0 (CopyToOutputDirectory unchanged — dev workflow unaffected)
  </acceptance_criteria>

  <done>
    All 6 stdlib .flow files will be copied into publish output by dotnet publish. dev builds unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write PublishProfile + publish.sh + size budget check + smoke test</name>
  <files>flow-cli/Properties/PublishProfiles/linux-x64.pubxml, scripts/publish.sh</files>

  <read_first>
    - .planning/phases/30-flow-cli-formal-install/30-RESEARCH.md offset=486 limit=40 (dotnet publish profile section with exact flag rationale)
    - flow-cli/flow-cli.csproj (confirm net10.0 + AssemblyName=flow from Plan 30-01)
    - scripts/ directory contents — confirm scripts/lsp-smoke.sh exists as a bash-script style reference
  </read_first>

  <action>
Create flow-cli/Properties/PublishProfiles/linux-x64.pubxml with this exact content:

```
<?xml version="1.0" encoding="utf-8"?>
<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <Platform>Any CPU</Platform>
    <PublishDir>publish/flow-linux-x64/</PublishDir>
    <PublishProtocol>FileSystem</PublishProtocol>
    <TargetFramework>net10.0</TargetFramework>
    <RuntimeIdentifier>linux-x64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <PublishTrimmed>false</PublishTrimmed>
    <DebugType>embedded</DebugType>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
  </PropertyGroup>
</Project>
```

Create scripts/publish.sh (POSIX bash, executable):
```
#!/usr/bin/env bash
set -euo pipefail

# Phase 30 REQ-2: self-contained Linux x64 single-file publish of flow-cli.
# Flag set locked in .planning/phases/30-flow-cli-formal-install/30-RESEARCH.md.
# Size budget: 120 MB total (du -sh on the publish dir).

PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
OUT_DIR="$PROJECT_ROOT/publish/flow-linux-x64"
SIZE_BUDGET_MB=120

cd "$PROJECT_ROOT"

# Clean previous publish to avoid stale artifacts.
rm -rf "$OUT_DIR"
mkdir -p "$OUT_DIR"

echo "==> Publishing flow-cli (self-contained linux-x64, single-file)..."
dotnet publish flow-cli/flow-cli.csproj \
  -c Release \
  -r linux-x64 \
  --self-contained true \
  -p:PublishSingleFile=true \
  -p:PublishTrimmed=false \
  -p:DebugType=embedded \
  -p:IncludeNativeLibrariesForSelfExtract=true \
  -p:EnableCompressionInSingleFile=true \
  -o "$OUT_DIR"

# Verify the binary exists and is executable.
if [[ ! -x "$OUT_DIR/flow" ]]; then
  echo "ERROR: $OUT_DIR/flow was not produced or is not executable." >&2
  exit 1
fi

# Verify stdlib .flow files made it in.
for f in std.flow collections.flow audio.flow bars.flow notation.flow composition.flow; do
  if [[ ! -f "$OUT_DIR/$f" ]]; then
    echo "ERROR: stdlib file $OUT_DIR/$f missing from publish output." >&2
    echo "  Hint: confirm CopyToPublishDirectory=PreserveNewest on flow-lang.csproj for $f." >&2
    exit 1
  fi
done

# Enforce SPEC-2 size budget.
SIZE_BYTES=$(du -sb "$OUT_DIR" | awk '{print $1}')
SIZE_MB=$(( SIZE_BYTES / 1024 / 1024 ))
echo "==> Publish size: ${SIZE_MB} MB (budget: ${SIZE_BUDGET_MB} MB)"
if [[ $SIZE_MB -gt $SIZE_BUDGET_MB ]]; then
  echo "ERROR: Publish output exceeds SPEC-2 budget (${SIZE_MB} MB > ${SIZE_BUDGET_MB} MB)." >&2
  exit 1
fi

# Smoke: run the published binary's version subcommand.
echo "==> Smoke test: ./flow version"
"$OUT_DIR/flow" version

echo "==> Publish OK. Binary: $OUT_DIR/flow"
```

Make scripts/publish.sh executable: chmod +x scripts/publish.sh
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; ls -la scripts/publish.sh flow-cli/Properties/PublishProfiles/linux-x64.pubxml &amp;&amp; test -x scripts/publish.sh &amp;&amp; grep -c 'PublishSingleFile' flow-cli/Properties/PublishProfiles/linux-x64.pubxml &amp;&amp; grep -c 'PublishTrimmed.false' flow-cli/Properties/PublishProfiles/linux-x64.pubxml &amp;&amp; bash scripts/publish.sh 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - flow-cli/Properties/PublishProfiles/linux-x64.pubxml exists
    - scripts/publish.sh exists AND is executable (test -x returns 0)
    - The .pubxml contains all 9 locked flags (Configuration, RuntimeIdentifier, SelfContained, PublishSingleFile, PublishTrimmed, DebugType, IncludeNativeLibrariesForSelfExtract, EnableCompressionInSingleFile, PublishDir)
    - grep -c '<PublishTrimmed>false</PublishTrimmed>' flow-cli/Properties/PublishProfiles/linux-x64.pubxml returns 1 (NEVER true per RESEARCH)
    - grep -c 'PublishReadyToRun' scripts/publish.sh returns 0 AND grep -c 'PublishReadyToRun' flow-cli/Properties/PublishProfiles/linux-x64.pubxml returns 0 (known compression regression)
    - bash scripts/publish.sh exits 0
    - After publish: publish/flow-linux-x64/flow exists AND is executable
    - All 6 stdlib files in publish/flow-linux-x64/: ls publish/flow-linux-x64/{std,collections,audio,bars,notation,composition}.flow returns no error
    - du -sm publish/flow-linux-x64 | awk '{print $1}' returns a value <= 120
    - publish/flow-linux-x64/flow version exits 0 AND stdout matches regex ^flow [0-9]+\.[0-9]+
  </acceptance_criteria>

  <done>
    publish.sh is the single CI-callable entrypoint; the .pubxml mirrors the same flags so dotnet publish flow-cli -p:PublishProfile=linux-x64 works equivalently. Size budget enforced. Stdlib files validated. Smoke version test runs from the published binary.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end published-binary smoke — eval, scaffold, run</name>
  <files>(no new files; validates Tasks 1 and 2 produce a working binary via the existing flow new scaffold from Plan 30-02)</files>

  <read_first>
    - publish/flow-linux-x64/ directory contents (from Task 2)
    - flow-cli/Scaffold/Templates/default.flow (from Plan 30-02)
  </read_first>

  <action>
This task is verification-only. No file edits.

Run the following sequence to validate that the published binary's run + eval + new subcommands all work end-to-end with stdlib resolution:

1. From a CLEAN temp dir (cd /tmp; rm -rf /tmp/30-04-smoke; mkdir /tmp/30-04-smoke), invoke the absolute path to the published binary:
   `/home/noah/Desktop/projects/flow-sharp/publish/flow-linux-x64/flow version`
   Must exit 0 and print a version string.

2. From the SAME temp dir:
   `/home/noah/Desktop/projects/flow-sharp/publish/flow-linux-x64/flow eval 'Int x = 5; (print (str x))'`
   Must exit 0 and stdout must contain `5`.

3. From the SAME temp dir:
   `/home/noah/Desktop/projects/flow-sharp/publish/flow-linux-x64/flow new smoke30 --dir /tmp/30-04-smoke/smoke30`
   Must exit 0 and create /tmp/30-04-smoke/smoke30/smoke30.flow.

4. From the SAME temp dir:
   `/home/noah/Desktop/projects/flow-sharp/publish/flow-linux-x64/flow run /tmp/30-04-smoke/smoke30/smoke30.flow`
   Must exit 0. This validates that the published binary resolves `use "@audio"`, `use "@std"`, `use "@notation"` correctly from the publish/flow-linux-x64/ directory (not the dev tree).

5. PROOF that .NET runtime is NOT in scope: confirm no dotnet command appears in stack traces. The single-file extracted binary IS self-contained — it extracts to ~/.net/ on first run per IncludeNativeLibrariesForSelfExtract=true and never references a separately-installed runtime.

If any of these fail, the failure mode is one of:
- Stdlib file missing -> revisit Task 1
- Binary not found -> revisit Task 2
- Reflection trim regression -> confirm PublishTrimmed=false (RESEARCH Assumption A2)

Document the result in summary.
  </action>

  <verify>
    <automated>BIN=/home/noah/Desktop/projects/flow-sharp/publish/flow-linux-x64/flow ; test -x "$BIN" &amp;&amp; rm -rf /tmp/30-04-smoke &amp;&amp; mkdir -p /tmp/30-04-smoke &amp;&amp; cd /tmp/30-04-smoke &amp;&amp; "$BIN" version &amp;&amp; "$BIN" eval 'Int x = 5; (print (str x))' 2>/dev/null | grep -E '^5$' &amp;&amp; "$BIN" new smoke30 --dir /tmp/30-04-smoke/smoke30 &amp;&amp; ls /tmp/30-04-smoke/smoke30/smoke30.flow &amp;&amp; "$BIN" run /tmp/30-04-smoke/smoke30/smoke30.flow ; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - $BIN exists and is executable
    - $BIN version exits 0 and matches regex '^flow [0-9]'
    - $BIN eval 'Int x = 5; (print (str x))' exits 0 AND stdout contains a line matching ^5$
    - $BIN new smoke30 --dir /tmp/30-04-smoke/smoke30 exits 0
    - /tmp/30-04-smoke/smoke30/smoke30.flow exists
    - $BIN run /tmp/30-04-smoke/smoke30/smoke30.flow exits 0 (this is the load-bearing assertion — stdlib resolves correctly from publish/)
    - The scaffold's use "@std" / "@audio" / "@notation" all resolve (no 'Module not found' errors in stderr)
  </acceptance_criteria>

  <done>
    Published binary runs end-to-end from a non-source directory. Stdlib resolution post-publish validated. REQ-2 is satisfied end-to-end and ready to flow into Plan 30-05's install script.
  </done>
</task>

</tasks>

<verification>
- bash scripts/publish.sh produces publish/flow-linux-x64/flow
- Size budget <= 120 MB enforced by script
- All 6 stdlib files in publish output
- Published binary's run + eval + new + version all work
- Existing dotnet build solution unaffected
</verification>

<success_criteria>
- REQ-2 acceptance: 'Publish produces a self-contained directory. Running ./flow run script.flow from the publish dir works on a clean Linux x64 system with PulseAudio installed and no .NET runtime'
- Single-file binary with embedded native libs
- Reproducible: scripts/publish.sh is CI-callable and idempotent (cleans previous publish first)
- Locked publish flags committed in two forms (script + .pubxml)
- 120 MB cap holds
</success_criteria>

<output>
After completion, create .planning/phases/30-flow-cli-formal-install/30-04-SUMMARY.md summarizing:
- Actual measured publish size in MB (vs 120 MB cap)
- The 9 publish flags locked in
- Stdlib bundling mechanism (CopyToPublishDirectory on flow-lang.csproj)
- The smoke-test command sequence and its results
- Any gotchas encountered (e.g. if Tomlyn or System.CommandLine surfaced unexpected trim warnings)
</output>
