---
phase: 30-flow-cli-formal-install
plan: 08
type: execute
wave: 3
depends_on: [30-07]
files_modified:
  - flow-midi/Conversion/FlowGenerator.cs
  - flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests.cs
autonomous: true
requirements: [REQ-5, REQ-6]

must_haves:
  truths:
    - "FlowGenerator.Generate accepts an explicit roundTrip bool parameter (or equivalent FlowGenerator.GenerateRoundTrip method) that defaults to false (preserves existing flow-midi CLI behavior — backward compat) and when true: (a) omits the (play output) trailer line, (b) disables CanAutoFit elision (every note carries an explicit duration suffix), (c) names sequences track1, track2, ... per source track index rather than channel-derived `track_ch1` naming"
    - "When roundTrip == true, generated source contains NO substring '(play output)'"
    - "When roundTrip == true, every note token inside `| ... |` streams carries an explicit duration suffix (q/h/w/e/s/t with optional dot/tie) — no bare `C4` tokens"
    - "When roundTrip == true, sequence names are `track1_seq`, `track2_seq`, ... (no `_rh` / `_lh` suffixes, no channel-derived `_ch2` suffixes)"
    - "When roundTrip == false (default), existing FlowGenerator behavior is preserved byte-for-byte for backward compat with the current `flow-midi` CLI"
    - "Generated source still wraps content in `tempo N { timesig N/M { key X { section roundtrip { ... } } } }` per SPEC-5 — but section name is `roundtrip` (not `song_part`) when roundTrip==true; section name `song_part` preserved when roundTrip==false"
    - "Single Song expression `Song s = [roundtrip]` (when roundTrip==true) — no `(renderSong s \"piano\")` automatic buffer emission, no `(writeWav ...)` automatic emission, no `(play output)` automatic emission. The midi2flow output is a pure structural translation; the composer adds render/play/write boilerplate via `flow new`-style scaffolding if they want audio."
    - "flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests facts all GREEN"
    - "When roundTrip==false, `flow-midi examples/ragtime.mid` continues to produce a file structure identical to the pre-Plan-30-07-but-post-Quantizer-fix output (Quantizer changes in Plan 30-07 already affect the input; FlowGenerator output for non-roundTrip mode should be functionally equivalent to pre-Plan-30-08 behavior for that already-improved input)"
  artifacts:
    - path: "flow-midi/Conversion/FlowGenerator.cs"
      provides: "Updated Generate method (with roundTrip parameter) — overload or single method with default-false parameter, your choice"
      contains: "if (roundTrip) ... structural differences for SPEC-5 compliance"
  key_links:
    - from: "flow-midi/Conversion/FlowGenerator.cs::Generate"
      to: "QuantizedTrack -> Sequence name"
      via: "if roundTrip, sequence names use track index; else use SanitizeVarName(track.Name)"
      pattern: "track\\d+_seq"
    - from: "flow-midi/Conversion/FlowGenerator.cs::WriteSequence"
      to: "CanAutoFit"
      via: "if roundTrip, bypass CanAutoFit entirely (useAutoFit = false)"
      pattern: "if .*roundTrip.*useAutoFit"
    - from: "flow-midi/Conversion/FlowGenerator.cs::Generate"
      to: "(play output) emit"
      via: "if (!roundTrip) AppendLine the (play output) line; else skip"
      pattern: "!roundTrip"
---

<objective>
Implement Bug B Layer 3 (FlowGenerator emit-format adjustments) per RESEARCH.md → Bug B Required Rework Scope → Layer 3. Add a `roundTrip` flag (parameter on Generate or new overload Generate(...) accepting bool) defaulting to false for backward compat. When true: drop the `(play output)` trailer, disable auto-fit elision, use track-index naming, change the section name to `roundtrip`, drop the `Buffer output = (renderSong ...)` line and `(play output)` line.

The non-roundTrip path is preserved EXACTLY — only the roundTrip path differs. This decouples the existing flow-midi CLI binary's output style from the new Phase 30 midi2flow CLI's round-trip-friendly output.

Plan 30-09 then wires `flow midi2flow` to call Generate(..., roundTrip: true).

Purpose: REQ-5 (flat output structure) + REQ-6 (round-trip fidelity). The structural changes here are necessary because auto-fit elision drops duration info — explicit durations on every note guarantee that flow2midi → midi2flow → flow2midi produces note-count + pitch + duration parity.

Output: FlowGenerator with dual-mode output. RED FlowGeneratorStructureTests facts GREEN. Existing flow-midi CLI behavior preserved for non-roundTrip default callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-flow-cli-formal-install/30-SPEC.md
@.planning/phases/30-flow-cli-formal-install/30-RESEARCH.md
@CLAUDE.md
@flow-midi/Conversion/FlowGenerator.cs
@flow-midi/Conversion/Quantizer.cs
@flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests.cs

<interfaces>
Current FlowGenerator API (line 33):
```
public static string Generate(MidiFile midi, QuantizeResult quantizeResult, string sourceFileName);
```

Post-Plan-30-08 API options (executor decides — recommendation: add bool parameter with default false):

Option A — extra parameter with default:
```
public static string Generate(MidiFile midi, QuantizeResult quantizeResult, string sourceFileName, bool roundTrip = false);
```

Option B — separate method (explicit, no risk of caller accidentally invoking the wrong mode):
```
public static string Generate(MidiFile midi, QuantizeResult quantizeResult, string sourceFileName);  // unchanged default
public static string GenerateRoundTrip(MidiFile midi, QuantizeResult quantizeResult, string sourceFileName);  // new
```

RESEARCH-Layer-3 specific changes when roundTrip==true:
- Drop auto-fit elision: `bool useAutoFit = false;` (pass to WriteSequence; existing WriteSequence already accepts this — see line 145 "bool useAutoFit = CanAutoFit(track);")
- Drop the `(play output)` trailer line at FlowGenerator.cs:123
- Drop the `Buffer output = (renderSong song "piano")` emission at line 122 (pure structural emit; renderSong/play are render-time concerns not source-translation concerns)
- Change section name from `song_part` to `roundtrip` at line 100
- Change `Song song = [song_part]` to `Song s = [roundtrip]` at line 121
- Change sequence names from `track.Name`-derived (via SanitizeVarName) to `track1_seq`, `track2_seq`, ... in source-track-order

Existing FlowGenerator.cs structure (read for the pre-state):
- line 33: Generate entrypoint
- line 100: `section song_part {`
- line 104: foreach playableTrack — emits Sequence per track via WriteSequence
- line 106-114: uniqueName + suffix-deduping for track names
- line 121: `Song song = [song_part]`
- line 122: `Buffer output = (renderSong song "piano")`
- line 123: `(play output)`
- line 145: `bool useAutoFit = CanAutoFit(track);` in WriteSequence
- line 235-281: CanAutoFit method

RED-on-HEAD facts from Plan 30-06 to flip GREEN:
- FlowGeneratorStructureTests.Generated_Output_Has_No_Play_Output_Trailer_When_Round_Trip_Mode
- FlowGeneratorStructureTests.One_Sequence_Per_Track_Channel_No_RH_LH_Suffix
- FlowGeneratorStructureTests.No_Auto_Fit_When_Track_Has_Mixed_Durations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add roundTrip mode to FlowGenerator.Generate</name>
  <files>flow-midi/Conversion/FlowGenerator.cs</files>

  <read_first>
    - flow-midi/Conversion/FlowGenerator.cs (full file — 309 lines, all of it)
    - .planning/phases/30-flow-cli-formal-install/30-RESEARCH.md offset=420 limit=20 (Layer 3 prescriptive guidance)
    - flow-midi/Conversion/Quantizer.cs offset=55 limit=15 (QuantizedTrack record shape — Name field used for sequence naming)
  </read_first>

  <action>
Choose Option A (add `bool roundTrip = false` parameter to Generate). Update the signature:
```
public static string Generate(MidiFile midi, QuantizeResult quantizeResult, string sourceFileName, bool roundTrip = false)
```

Inside the method body make the following gated changes — every behavior change is wrapped in `if (roundTrip) ... else ...` so the non-roundTrip path is byte-identical to the pre-edit behavior:

1. **Section name (line 100):**
```
string sectionName = roundTrip ? "roundtrip" : "song_part";
sb.AppendLine($"{indent}section {sectionName} {{");
```

2. **Sequence naming (line 106 / WriteSequence call ~line 114):**
   Pass an explicit name to WriteSequence based on mode. Easiest: introduce a track-index counter and the desired name BEFORE the WriteSequence call:
```
int trackIdx = 0;
foreach (var track in playableTracks)
{
    trackIdx++;
    string seqName;
    if (roundTrip)
    {
        seqName = $"track{trackIdx}_seq";
    }
    else
    {
        // Existing logic: SanitizeVarName + uniqueness suffix
        string baseName = SanitizeVarName(track.Name);
        string uniqueName = baseName;
        int suffix = 2;
        while (seqNames.Contains(uniqueName))
            uniqueName = $"{baseName}_{suffix++}";
        seqNames.Add(uniqueName);
        seqName = uniqueName + "_seq";
    }
    WriteSequence(sb, sectionIndent, seqName, track, forceExplicitDurations: roundTrip);
}
```

3. **Song expression (lines 121-123) — wrap in roundTrip check:**
```
if (roundTrip)
{
    sb.AppendLine($"{indent}Song s = [{sectionName}]");
    // No renderSong / play emission — keep midi2flow output as pure structural translation
}
else
{
    sb.AppendLine($"{indent}Song song = [{sectionName}]");
    sb.AppendLine($"{indent}Buffer output = (renderSong song \"piano\")");
    sb.AppendLine($"{indent}(play output)");
}
```

4. **WriteSequence signature change (line 140):**
Add a parameter `bool forceExplicitDurations = false` to WriteSequence. Inside WriteSequence (line 145), change:
```
bool useAutoFit = forceExplicitDurations ? false : CanAutoFit(track);
```
This single change disables auto-fit elision when roundTrip==true.

DO NOT modify CanAutoFit's body itself — it's still called from the non-roundTrip path.

5. Add a top-of-file comment to FlowGenerator.cs noting the dual-mode contract:
```
// Generate(..., roundTrip: false) — default; preserves existing flow-midi CLI output (with (play output) trailer + auto-fit elision)
// Generate(..., roundTrip: true)  — Plan 30-08; emits SPEC-5 round-trip-friendly source (no (play output), explicit durations, trackN naming, section "roundtrip")
```
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi 2>&amp;1 | tail -3 &amp;&amp; dotnet build flow-midi.Tests 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-midi.Tests --filter 'FullyQualifiedName~FlowGeneratorStructure' --logger 'console;verbosity=minimal' 2>&amp;1 | tail -10 &amp;&amp; dotnet run --project flow-midi examples/ragtime.mid -o /tmp/30-08-default.flow &amp;&amp; grep -c '(play output)' /tmp/30-08-default.flow</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi exits 0
    - dotnet build flow-midi.Tests exits 0
    - grep -c 'bool roundTrip' flow-midi/Conversion/FlowGenerator.cs returns at least 1 (signature)
    - grep -c 'if (roundTrip)' flow-midi/Conversion/FlowGenerator.cs returns at least 3 (section name, Song expression, sequence naming)
    - grep -c 'forceExplicitDurations' flow-midi/Conversion/FlowGenerator.cs returns at least 2 (WriteSequence parameter + use)
    - All FlowGeneratorStructureTests facts GREEN: dotnet test --filter FlowGeneratorStructure reports 0 failures
    - All QuantizerSnapDurationTests + QuantizerRoundingTests still GREEN (Plan 30-07 work preserved)
    - HarnessSmokeFacts still GREEN
    - flow-lang.Tests full suite still GREEN
    - **Backward compat**: dotnet run --project flow-midi examples/ragtime.mid -o /tmp/30-08-default.flow produces output that STILL CONTAINS `(play output)` (grep returns at least 1) — proves roundTrip=false default preserves existing behavior
    - Direct call test of round-trip mode: writing a small C# program (or test) that calls `FlowGenerator.Generate(midi, qr, name, roundTrip: true)` on a fixture produces a string with ZERO occurrences of `(play output)` AND ZERO occurrences of `_rh`/`_lh`
  </acceptance_criteria>

  <done>
    FlowGenerator supports dual emit modes. roundTrip=true output is SPEC-5/6 compliant. roundTrip=false output preserves the existing flow-midi CLI behavior. RED FlowGeneratorStructureTests are GREEN. Plan 30-09 can now call Generate(..., roundTrip: true) from the new `flow midi2flow` CLI subcommand.
  </done>
</task>

</tasks>

<verification>
- dotnet build flow-midi + flow-midi.Tests + flow-lang.Tests + flow-cli all exit 0
- All Plan 30-06 RED facts GREEN: QuantizerSnapDuration + QuantizerRounding + FlowGeneratorStructure
- HarnessSmokeFacts still GREEN
- flow-lang.Tests still GREEN
- Backward compat: existing flow-midi CLI binary output for examples/ragtime.mid still contains (play output) (default mode unchanged)
- Round-trip mode produces structurally-different output ((play output) gone, sequence names trackN, no auto-fit elision)
</verification>

<success_criteria>
- FlowGenerator dual-mode (roundTrip param) implemented
- All structural Bug B defects closed at the generator layer
- Plan 30-09 has a Generate(midi, qr, name, roundTrip: true) entrypoint to wrap
- Zero regressions: existing flow-midi behavior preserved when caller doesn't opt into roundTrip
- All Plan 30-06 RED facts GREEN
</success_criteria>

<output>
After completion, create .planning/phases/30-flow-cli-formal-install/30-08-SUMMARY.md summarizing:
- The Generate signature change (Option A: added bool roundTrip=false parameter)
- The 5 gated behavior differences when roundTrip==true
- WriteSequence's new forceExplicitDurations parameter and its single use site
- Backward-compat proof: pre-vs-post output for examples/ragtime.mid (default mode unchanged)
- Note the round-trip mode is dormant until Plan 30-09 wires the CLI surface
</output>
