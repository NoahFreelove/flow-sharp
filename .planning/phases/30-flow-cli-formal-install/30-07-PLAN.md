---
phase: 30-flow-cli-formal-install
plan: 07
type: execute
wave: 2
depends_on: [30-06]
files_modified:
  - flow-midi/Conversion/Quantizer.cs
  - flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs
  - flow-midi.Tests/Unit/Phase30/QuantizerRoundingTests.cs
autonomous: true
requirements: [REQ-5, REQ-6]

must_haves:
  truths:
    - "SnapDurationCapped allows grid values up to capTicks + tolerance (tolerance = tpqn / 32 = 15 ticks at TPQN=480), not strict-cap. A 470-tick available window still snaps a 480-tick quarter to (q, false)."
    - "QuantizeSpans no longer emits a leading bar when the first note onset is after the first bar boundary — first emitted bar's BarNumber equals firstNoteOnsetBarIdx; trailing-trim continues to work; existing comment in Quantizer.cs:474-476 stays accurate after the symmetric leading-trim is added."
    - "AddRests never emits more than 4 RestElement entries for a single gap. When a gap is smaller than 1/8 of TPQN (60 ticks at TPQN=480), AddRests emits exactly 1 rest at the closest grid-snapped duration."
    - "AddSplitTracks RH/LH heuristic is REMOVED (the entire 47-line method including the 24-semitone split test, the median + middle-C bias, and the upperBars/lowerBars partition). Tracks with > 2 octave range still produce exactly 1 QuantizedTrack."
    - "Format-0 multi-channel files still split by channel (SplitByChannel logic is preserved — that's per-MIDI-channel, not per-RH/LH pitch heuristic)."
    - "Format-1 files still emit one QuantizedTrack per source track (no _rh/_lh suffixes)."
    - "Channel 9 (drum) tracks still route via the IsDrumTrack=true path (existing behavior preserved)."
    - "flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests and QuantizerRoundingTests Defects-1-and-2-and-3 facts all GREEN after this plan ships."
  artifacts:
    - path: "flow-midi/Conversion/Quantizer.cs"
      provides: "Bug B closure for Quantizer-layer defects (1, 2, 3 of 3). Rewrite is incremental, not full-file."
      contains: "Method SnapDurationCapped with tolerance band; method QuantizeSpans with leading-bar trim; method AddRests with count cap; method AddSplitTracks DELETED; QuantizeSpans calls preserved for both Format 0 + Format 1 paths but bypass the deleted AddSplitTracks (call result.Add(new QuantizedTrack(...)) directly)."
  key_links:
    - from: "flow-midi/Conversion/Quantizer.cs::SnapDurationCapped"
      to: "DurationGrid table (lines 73-85)"
      via: "tolerance-band comparison instead of strict gridTicks <= capTicks"
      pattern: "capTicks \\+ tolerance"
    - from: "flow-midi/Conversion/Quantizer.cs::QuantizeSpans"
      to: "spans.Min(s => s.StartTick) for leading-trim"
      via: "compute firstNoteOnsetBarIdx; emit bars only from that index onward"
      pattern: "firstNoteOnsetBarIdx"
    - from: "flow-midi/Conversion/Quantizer.cs::AddRests"
      to: "RestElement count cap"
      via: "if elements would exceed 4, emit 1 grid-snapped rest"
      pattern: "if .*count > 4"
---

<objective>
Implement Bug B Layer 2 fixes per RESEARCH.md Bug B Scope Assessment → Required Rework Scope. Three targeted edits to flow-midi/Conversion/Quantizer.cs:

1. SnapDurationCapped — replace strict cap with tolerance band (tpqn / 32 ticks default; verify 15 ticks at TPQN=480) so 480-tick quarters snap to (q, false) even when availableTicks is 470 due to channel grouping arithmetic noise.

2. QuantizeSpans — add leading-empty-bar trim symmetric to the existing trailing-trim. Compute firstNoteOnsetBarIdx from spans.Min(StartTick); emit bars from that index onward (not from 0).

3. AddRests — cap emit count at ≤ 4 rests per gap. When a gap is smaller than 1/8 of TPQN, emit exactly 1 grid-snapped rest. Stops the 5+ thirty-second-rest cascade reported in Bug B Defect 1.

4. AddSplitTracks — DELETE the entire 47-line method (lines 201-247). Replace its 2 call sites (lines 152 + 182) with direct `result.Add(new QuantizedTrack(baseName, bars, channel, false))` after `var bars = QuantizeSpans(...)`. SPEC-5 says "one Sequence per MIDI track", which means we honor the MIDI's authored channel/track structure, not a pitch heuristic.

Make the RED facts in flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs + QuantizerRoundingTests.cs (Plan 30-06) GREEN. New tests proving the GREEN state may be added; existing fact bodies/signatures should not change.

Purpose: REQ-5 + REQ-6 — fix the quantization algorithm so SPEC-5 (flat one-Sequence-per-track output) and SPEC-6 (round-trip fidelity) can hold. WITHOUT this plan, Plan 30-08's structural changes to FlowGenerator are insufficient because the upstream quantized data is wrong.

Output: 3 fact classes from Plan 30-06 GREEN; existing flow-midi behavior on simple round-trip cases is correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-flow-cli-formal-install/30-SPEC.md
@.planning/phases/30-flow-cli-formal-install/30-RESEARCH.md
@.planning/debug/midi-import-quarter-quantize.md
@CLAUDE.md
@flow-midi/Conversion/Quantizer.cs
@flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs
@flow-midi.Tests/Unit/Phase30/QuantizerRoundingTests.cs
@flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs

<interfaces>
Existing Quantizer.cs structure (read for the exact pre-state before editing):

```
namespace FlowMidi.Conversion;
public static class Quantizer
{
    static readonly (double Multiplier, string Suffix, bool IsDotted)[] DurationGrid = { ... };  // lines 73-85
    public static long SuffixToTicks(string suffix, bool isDotted, int tpqn);  // line 87
    public static QuantizeResult Quantize(MidiFile midi);  // line 103 — calls AddSplitTracks at lines 152 + 182
    static void AddSplitTracks(List<QuantizedTrack> result, string baseName, List<NoteSpan> spans, int channel, int tpqn, int timeSigNum, int timeSigDen, bool useFlats);  // line 201 — DELETE
    static (int Num, int Den) PickPrimaryTimeSig(...);  // line 253
    static Dictionary<int, List<NoteSpan>> SplitByChannel(MidiTrack track);  // line 291
    static List<NoteSpan> PairNotes(List<MidiEvent> events);  // line 320
    static List<QuantizedBar> QuantizeSpans(List<NoteSpan> spans, int tpqn, int timeSigNum, int timeSigDen, bool useFlats);  // line 346 — MODIFY: add leading-bar trim
    static List<List<NoteSpan>> GroupSimultaneous(...);  // line 481
    static (string Suffix, bool IsDotted) SnapDuration(long ticks, int tpqn);  // line 508
    static (string Suffix, bool IsDotted) SnapDurationCapped(long ticks, long capTicks, int tpqn);  // line 568 — MODIFY: tolerance band
    static void AddRests(List<IBarElement> elements, long ticks, int tpqn);  // line 604 — MODIFY: cap emit count
    static string MidiPitchToFlowNote(int midiPitch, bool useFlats);  // line 647
}
```

The CURRENT SnapDurationCapped at lines 568-602 (the strict cap defect):
```
static (string Suffix, bool IsDotted) SnapDurationCapped(long ticks, long capTicks, int tpqn)
{
    if (capTicks <= 0) return ("t", false);

    // STRICT cap — no tolerance band.
    double bestDistance = double.MaxValue;
    string bestSuffix = "t";
    bool bestDotted = false;

    foreach (var (mult, suffix, isDotted) in DurationGrid)
    {
        double gridTicks = mult * tpqn;
        if (gridTicks > capTicks)
            continue;  // <-- THE DEFECT: rejects 480.0 > 479.9999 = "capTicks - epsilon" combinations
        double distance = Math.Abs(ticks - gridTicks);
        if (distance < bestDistance) { bestDistance = distance; bestSuffix = suffix; bestDotted = isDotted; }
    }
    return (bestSuffix, bestDotted);
}
```

The CURRENT QuantizeSpans bar-emission loop (lines 359-472):
```
long maxTick = spans.Max(s => s.EndTick);
int totalBars = (int)((maxTick + barTicks - 1) / barTicks);
// ...
for (int barIdx = 0; barIdx < totalBars; barIdx++)  // <-- DEFECT: starts from 0 regardless of firstNoteOnset
{
    long barStart = barIdx * barTicks;
    long barEnd = barStart + barTicks;
    // ... emit bar ...
}
// Trailing-trim already exists at lines 475-476:
while (bars.Count > 0 && bars[^1].Elements.All(e => e is RestElement))
    bars.RemoveAt(bars.Count - 1);
```

The CURRENT AddRests at lines 604-637 (over-emission defect):
```
static void AddRests(List<IBarElement> elements, long ticks, int tpqn)
{
    if (ticks <= 0) return;
    double[] gridMultipliers = { 4.0, 2.0, 1.0, 0.5, 0.25, 0.125 };
    foreach (double mult in gridMultipliers)
    {
        long unitTicks = (long)(mult * tpqn);
        if (unitTicks <= 0) continue;
        int count = (int)Math.Round((double)ticks / unitTicks);
        // <-- DEFECT: count can be up to 16 — too many small rests
        if (count > 0 && count <= 16 && Math.Abs(ticks - count * unitTicks) <= tpqn * 0.1)
        {
            var (suffix, isDotted) = SnapDuration(unitTicks, tpqn);
            for (int i = 0; i < count; i++)
                elements.Add(new RestElement(suffix, isDotted));
            return;
        }
    }
    elements.Add(new RestElement("q", false));  // fallback
}
```

RED-on-HEAD facts to flip GREEN (from Plan 30-06):
- QuantizerSnapDurationTests: FourQuarterNotes_In_4_4_Produce_Four_Q_Tokens, Quarter_Note_With_One_Tick_Gap_Still_Snaps_To_Q, Quarter_Eighth_Eighth_Pattern_Produces_Q_E_E_In_Order
- QuantizerRoundingTests: Empty_Leading_Bars_Are_Trimmed, Rest_Of_Half_Bar_Is_One_Half_Rest_Not_Eight_Sixteenths, Two_Octave_Range_Does_Not_Split_RH_LH
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quantizer.cs — SnapDurationCapped tolerance + AddRests count-cap</name>
  <files>flow-midi/Conversion/Quantizer.cs</files>

  <read_first>
    - flow-midi/Conversion/Quantizer.cs (full file — entire structure)
    - .planning/debug/midi-import-quarter-quantize.md (Bug B symptoms)
    - .planning/phases/30-flow-cli-formal-install/30-RESEARCH.md offset=400 limit=30 (RESEARCH Layer 2 prescriptive guidance for these two methods)
  </read_first>

  <action>
Edit Quantizer.cs lines 568-602 (SnapDurationCapped):

Replace the strict cap with a tolerance band. Add a `tolerance` variable equal to `Math.Max(tpqn / 32, 1)` (15 ticks at TPQN=480, never less than 1). Change the rejection condition from `if (gridTicks > capTicks) continue;` to `if (gridTicks > capTicks + tolerance) continue;`. Update the method's XML doc comment to reflect: "Tolerance band (tpqn/32 ticks, ~3% of a quarter at TPQN=480) lets exact-quarter notes snap to q even when availableTicks is off by a few ticks due to upstream channel-grouping arithmetic noise. The caller's `cursor >= barEnd` guard still bounds any total bar-fit overshoot."

Edit Quantizer.cs lines 604-637 (AddRests):

Implement the count cap. After computing `count` for each grid multiplier, ADD an upper bound of 4:
```
if (count > 0 && count <= 4 && Math.Abs(ticks - count * unitTicks) <= tpqn * 0.1)
{
    // ... existing emit loop
}
```
For gaps smaller than `tpqn / 8` (60 ticks at TPQN=480), short-circuit BEFORE the foreach loop: emit exactly 1 rest at the closest grid-snapped duration via SnapDuration(ticks, tpqn). Add an early-return:
```
if (ticks > 0 && ticks < tpqn / 8)
{
    var (s, d) = SnapDuration(ticks, tpqn);
    elements.Add(new RestElement(s, d));
    return;
}
```
The remaining `elements.Add(new RestElement("q", false));` fallback at end-of-method becomes the catch-all for unusual gap sizes (e.g. 5/8 of a beat that doesn't snap to a single grid).

Both edits preserve method signatures and the existing comment about the bar-overflow-rh-desync invariant. The bar-fit-invariant guard at line 400 (`if (cursor >= barEnd) break;`) is still in force.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi 2>&amp;1 | tail -3 &amp;&amp; dotnet build flow-midi.Tests 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-midi.Tests --filter 'FullyQualifiedName~QuantizerSnapDuration' --logger 'console;verbosity=minimal' 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi exits 0
    - dotnet build flow-midi.Tests exits 0
    - grep -c 'capTicks + tolerance' flow-midi/Conversion/Quantizer.cs returns at least 1 (tolerance band)
    - grep -c 'tpqn / 32' flow-midi/Conversion/Quantizer.cs returns at least 1 (concrete tolerance value)
    - grep -c 'count <= 4' flow-midi/Conversion/Quantizer.cs returns at least 1 (rest count cap)
    - grep -c 'ticks < tpqn / 8' flow-midi/Conversion/Quantizer.cs returns at least 1 (small-gap shortcut)
    - All 3 facts in QuantizerSnapDurationTests are GREEN: dotnet test --filter QuantizerSnapDuration reports 3 passed / 0 failed
    - At least 1 fact in QuantizerRoundingTests is GREEN — specifically Rest_Of_Half_Bar_Is_One_Half_Rest_Not_Eight_Sixteenths
  </acceptance_criteria>

  <done>
    Defect 1 closed (quarter snapping). Half of Defect 2 closed (rest count cap). Quantizer still produces the same shape outputs for cases the algorithm was already getting right.
  </done>
</task>

<task type="auto">
  <name>Task 2: Quantizer.cs — QuantizeSpans leading-bar trim</name>
  <files>flow-midi/Conversion/Quantizer.cs</files>

  <read_first>
    - flow-midi/Conversion/Quantizer.cs offset=346 limit=140 (the QuantizeSpans method body, from line 346 to 479)
  </read_first>

  <action>
Edit QuantizeSpans (lines 346-479):

After `long maxTick = spans.Max(s => s.EndTick);` add:
```
long firstNoteTick = spans.Min(s => s.StartTick);
int firstBarIdx = (int)(firstNoteTick / barTicks);
```

Change the bar-emission loop start condition from `for (int barIdx = 0; barIdx < totalBars; barIdx++)` to `for (int barIdx = firstBarIdx; barIdx < totalBars; barIdx++)`.

Add an XML doc comment block above QuantizeSpans documenting: "Leading-trim symmetry: bars before the first note's onset are not emitted. Trailing-trim at lines 475-476 already handles the back end. This prevents the Bug B Defect 2 cascade where 4 silent bars at the start of a track became 4 whole-bar rests of `| _ |` in the generated .flow output."

DO NOT change anything else in QuantizeSpans. The bar-fit clamp, cursor arithmetic, group emission, trailing-trim, the bar-overflow-rh-desync invariant comment all stay.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-midi.Tests --filter 'FullyQualifiedName~QuantizerRoundingTests.Empty_Leading_Bars_Are_Trimmed' --logger 'console;verbosity=minimal' 2>&amp;1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi exits 0
    - grep -c 'firstBarIdx' flow-midi/Conversion/Quantizer.cs returns at least 2 (declaration + use in for-loop)
    - grep -c 'spans.Min(s =>' flow-midi/Conversion/Quantizer.cs returns at least 1
    - QuantizerRoundingTests.Empty_Leading_Bars_Are_Trimmed is GREEN
    - QuantizerRoundingTests.Rest_Of_Half_Bar_Is_One_Half_Rest_Not_Eight_Sixteenths still GREEN (from Task 1)
    - All flow-lang.Tests still GREEN: dotnet test flow-lang.Tests 2>&amp;1 | grep -E 'Failed: 0|Failed!.*0' (no regressions to other phases)
  </acceptance_criteria>

  <done>
    Defect 2 fully closed. Leading + trailing bar emission is symmetric.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete AddSplitTracks (RH/LH heuristic removal) — Defect 3 closure</name>
  <files>flow-midi/Conversion/Quantizer.cs</files>

  <read_first>
    - flow-midi/Conversion/Quantizer.cs (current state after Tasks 1 + 2)
    - flow-midi/Conversion/Quantizer.cs offset=140 limit=110 (the two AddSplitTracks call sites in the Format-0 + Format-1 paths)
    - flow-midi/Conversion/Quantizer.cs offset=201 limit=50 (the AddSplitTracks method body)
  </read_first>

  <action>
1. DELETE the entire AddSplitTracks method (currently lines 201-247 or near there after Tasks 1+2 edits). It is a 47-line static method with the RH/LH pitch-split heuristic that SPEC-5 prohibits.

2. Replace the call site in the Format-0 branch (around line 152 after Quantize's `if (!isDrum)`):
   FROM:
   ```
   AddSplitTracks(result, baseName, spans, channel, tpqn, timeSigNum, timeSigDen, useFlats);
   ```
   TO:
   ```
   var bars = QuantizeSpans(spans, tpqn, timeSigNum, timeSigDen, useFlats);
   result.Add(new QuantizedTrack(baseName, bars, channel, false));
   ```

3. Replace the call site in the Format-1 branch (around line 182):
   Same transformation as step 2 — replace AddSplitTracks call with the QuantizeSpans + result.Add pair.

4. Add a code comment above each of the 2 replacement sites: `// SPEC-5: one Sequence per MIDI track/channel. No pitch-split heuristic (Bug B Defect 3 closure).`

5. Verify the file compiles — there should be no remaining references to AddSplitTracks anywhere in flow-midi/.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi 2>&amp;1 | tail -3 &amp;&amp; grep -c 'AddSplitTracks' flow-midi/Conversion/Quantizer.cs &amp;&amp; dotnet test flow-midi.Tests --filter 'FullyQualifiedName~QuantizerSnapDuration|FullyQualifiedName~QuantizerRounding' --logger 'console;verbosity=minimal' 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi exits 0
    - grep -c 'AddSplitTracks' flow-midi/Conversion/Quantizer.cs returns 0 (method fully deleted; no leftover references)
    - grep -c '_rh\|_lh' flow-midi/Conversion/Quantizer.cs returns 0 (suffix conventions removed)
    - grep -c 'SPEC-5' flow-midi/Conversion/Quantizer.cs returns at least 2 (the 2 replacement-site comments)
    - QuantizerRoundingTests.Two_Octave_Range_Does_Not_Split_RH_LH is GREEN
    - ALL facts in QuantizerSnapDurationTests + QuantizerRoundingTests are GREEN (6+ facts total): dotnet test --filter 'QuantizerSnapDuration|QuantizerRounding' reports 0 failures
    - HarnessSmokeFacts still GREEN
    - flow-lang.Tests full suite still GREEN: dotnet test flow-lang.Tests reports 0 failures
    - flow-midi still works on examples/ragtime.mid: dotnet run --project flow-midi examples/ragtime.mid -o /tmp/30-07-ragtime.flow exits 0 AND the generated .flow file contains NO `_rh` or `_lh` substrings (grep -c '_rh\|_lh' /tmp/30-07-ragtime.flow returns 0)
  </acceptance_criteria>

  <done>
    Defect 3 closed. Quantizer.cs is now SPEC-5-compliant at the data layer. FlowGenerator.cs still has its own structural defects (auto-fit + (play output) trailer) — Plan 30-08's job.
  </done>
</task>

</tasks>

<verification>
- dotnet build flow-midi + flow-midi.Tests + flow-lang.Tests + flow-cli all exit 0
- All Plan 30-06 RED facts targeted at Quantizer GREEN: QuantizerSnapDurationTests (3+), QuantizerRoundingTests (3+)
- HarnessSmokeFacts still GREEN
- flow-lang.Tests full suite still GREEN (no cross-project regressions)
- examples/ragtime.mid still parses and generates a .flow file (with the new flat structure; no _rh/_lh)
</verification>

<success_criteria>
- Bug B Defects 1, 2, 3 closed at the Quantizer layer
- SnapDurationCapped tolerance band (tpqn/32 ticks)
- AddRests count cap (≤ 4 per gap; 1 for small gaps)
- QuantizeSpans leading-trim symmetric to trailing-trim
- AddSplitTracks DELETED — no pitch-split heuristic
- RED-on-HEAD facts from Plan 30-06 GREEN
- ZERO regressions to flow-lang or other test projects
</success_criteria>

<output>
After completion, create .planning/phases/30-flow-cli-formal-install/30-07-SUMMARY.md summarizing:
- The 4 algorithm changes (with line-number deltas where readable)
- Why tolerance = tpqn/32 (concrete justification — ~3% of a quarter is the inherent timing slop of channel-grouping arithmetic)
- The deleted AddSplitTracks method's full signature for the record
- The fact-count delta (HEAD: 6+ RED → after this plan: 6+ GREEN)
- A note on Plan 30-08's remaining work (FlowGenerator structural changes)
</output>
