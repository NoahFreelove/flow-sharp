---
phase: 30-flow-cli-formal-install
plan: 02
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - flow-cli/Commands/RunCommand.cs
  - flow-cli/Commands/EvalCommand.cs
  - flow-cli/Commands/ReplCommand.cs
  - flow-cli/Commands/WatchCommand.cs
  - flow-cli/Commands/PlayCommand.cs
  - flow-cli/Commands/RenderCommand.cs
  - flow-cli/Commands/Flow2MidiCommand.cs
  - flow-cli/Commands/CheckCommand.cs
  - flow-cli/Commands/NewCommand.cs
  - flow-cli/Commands/Midi2FlowStubCommand.cs
  - flow-cli/Commands/CommandRegistry.cs
  - flow-cli/Scaffold/Templates/default.flow
  - flow-cli/Scaffold/ScaffoldEmitter.cs
  - flow-cli/flow-cli.csproj
autonomous: true
requirements: [REQ-1]

must_haves:
  truths:
    - "flow run examples/showcase.flow exits 0 and produces the same console output as dotnet run --project flow-interpreter examples/showcase.flow"
    - "flow eval prints expected stdout for simple snippets (e.g. '5' for an Int x = 5; print snippet)"
    - "flow render examples/showcase.flow -o /tmp/test30.wav produces a non-empty WAV file"
    - "flow flow2midi examples/showcase.flow -o /tmp/test30.mid produces a non-empty MIDI file"
    - "flow check examples/showcase.flow exits 0; broken file exits non-zero"
    - "flow new <name> writes a runnable .flow file; flow run on it exits 0"
    - "flow midi2flow STILL prints 'not yet implemented' (deferred to Plan 30-09)"
  artifacts:
    - path: "flow-cli/Commands/RunCommand.cs"
      provides: "run <script.flow> subcommand — wraps FlowInterpreter.ScriptRunner.RunScript"
    - path: "flow-cli/Commands/EvalCommand.cs"
      provides: "eval <code> subcommand — runs FlowEngine.Execute(code, '<eval>')"
    - path: "flow-cli/Commands/ReplCommand.cs"
      provides: "repl subcommand — wraps FlowInterpreter.Repl.Run()"
    - path: "flow-cli/Commands/WatchCommand.cs"
      provides: "watch <script.flow> subcommand — wraps FlowInterpreter.LiveReloadManager"
    - path: "flow-cli/Commands/PlayCommand.cs"
      provides: "play <script.flow> — forwards to ScriptRunner (script itself owns play call)"
    - path: "flow-cli/Commands/RenderCommand.cs"
      provides: "render <script.flow> -o out.wav — wraps ScriptRunner + verifies output path written"
    - path: "flow-cli/Commands/Flow2MidiCommand.cs"
      provides: "flow2midi <script.flow> -o out.mid — wraps ScriptRunner + verifies output path written"
    - path: "flow-cli/Commands/CheckCommand.cs"
      provides: "check <script.flow> — parse-and-execute with stdout-muted; exit 0 if no parse errors"
    - path: "flow-cli/Commands/NewCommand.cs"
      provides: "new <name> — scaffolds <name>/<name>.flow from embedded template"
    - path: "flow-cli/Commands/Midi2FlowStubCommand.cs"
      provides: "Explicit stub returning exit code 2 with message 'midi2flow ships in Plan 30-09'"
    - path: "flow-cli/Scaffold/Templates/default.flow"
      provides: "Embedded resource — minimum-viable .flow file"
    - path: "flow-cli/Scaffold/ScaffoldEmitter.cs"
      provides: "Reads embedded resource, writes to disk"
  key_links:
    - from: "flow-cli/Commands/RunCommand.cs"
      to: "FlowInterpreter.ScriptRunner.RunScript"
      via: "new ScriptRunner().RunScript(path, device, verbose)"
      pattern: "ScriptRunner"
    - from: "flow-cli/Commands/ReplCommand.cs"
      to: "FlowInterpreter.Repl.Run"
      via: "new Repl().Run()"
      pattern: "new Repl"
    - from: "flow-cli/Commands/WatchCommand.cs"
      to: "FlowInterpreter.LiveReloadManager"
      via: "using var manager = new LiveReloadManager(...); manager.Run()"
      pattern: "LiveReloadManager"
    - from: "flow-cli/Commands/NewCommand.cs"
      to: "flow-cli/Scaffold/ScaffoldEmitter.cs"
      via: "ScaffoldEmitter.WriteScaffold(name)"
      pattern: "ScaffoldEmitter"
---

<objective>
Implement 9 real subcommand handlers (run, eval, repl, watch, play, render, flow2midi, check, new) by wrapping existing logic from flow-interpreter and flow-lang. midi2flow stays an explicit stub (returns exit code 2 with message referencing Plan 30-09). Replace the placeholders in CommandRegistry.cs with real Build() calls. Verify each subcommand exits 0 on valid input via the existing examples/showcase.flow fixture.

Purpose: REQ-1 — make 10 of 11 subcommands work end-to-end (midi2flow shipped in Plan 30-09 after flow-midi rewrite). No new functionality — these are thin wrappers over existing entrypoints.

Output: flow binary that runs, evaluates, REPLs, watches, plays, renders, flow2midis, checks, and scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-flow-cli-formal-install/30-SPEC.md
@.planning/phases/30-flow-cli-formal-install/30-RESEARCH.md
@CLAUDE.md
@flow-interpreter/Program.cs
@flow-interpreter/ScriptRunner.cs
@flow-interpreter/Repl.cs
@flow-interpreter/LiveReloadManager.cs
@flow-lang/Core/FlowEngine.cs

<interfaces>
Existing entrypoints this plan wraps. Extracted from flow-interpreter and flow-lang.

From flow-interpreter/ScriptRunner.cs:
```
namespace FlowInterpreter;
public class ScriptRunner
{
    public int RunScript(string filePath, string? deviceName = null, bool verbose = false);
}
```

From flow-interpreter/Repl.cs:
```
namespace FlowInterpreter;
public class Repl
{
    public void Run();
}
```

From flow-interpreter/LiveReloadManager.cs (usage pattern from Program.cs:128-131):
```
namespace FlowInterpreter;
// constructor: LiveReloadManager(string fullPath, string? deviceName)
// usage: using var manager = new LiveReloadManager(fullPath, deviceName); manager.Run();
```

From flow-lang/Core/FlowEngine.cs:
```
namespace FlowLang.Core;
public class FlowEngine : IDisposable
{
    public FlowEngine(bool verbose = false);
    public bool Execute(string source, string filePath);
    public ErrorReporter ErrorReporter { get; }
    public AudioPlaybackManager AudioManager { get; }
}
```

System.CommandLine 2.0.7 argument/option patterns (per 30-RESEARCH.md Pattern 2):
```
var scriptArg  = new Argument<FileInfo>("script") { Description = "Path to .flow script" };
var outputOpt  = new Option<FileInfo>("--output", "-o") { Description = "Output file path" };
var deviceOpt  = new Option<string?>("--device") { Description = "PulseAudio device name" };
var verboseOpt = new Option<bool>("--verbose", "-v") { Description = "Diagnostic output" };
cmd.Add(scriptArg);
cmd.Add(outputOpt);
cmd.SetAction(parseResult =>
{
    var script = parseResult.GetValue(scriptArg)!;
    var output = parseResult.GetValue(outputOpt);
    var verbose = parseResult.GetValue(verboseOpt);
    return /* int exit code */;
});
```

CommandRegistry.cs (from Plan 30-01): order is run, eval, repl, watch, play, render, flow2midi, midi2flow, check, version, new. Replace placeholders for indexes 0..8 and 10; version (index 9) is already real.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Run, Eval, Repl, Watch, Check — 5 core-language commands</name>
  <files>flow-cli/Commands/RunCommand.cs, flow-cli/Commands/EvalCommand.cs, flow-cli/Commands/ReplCommand.cs, flow-cli/Commands/WatchCommand.cs, flow-cli/Commands/CheckCommand.cs, flow-cli/Commands/CommandRegistry.cs</files>

  <read_first>
    - flow-cli/Commands/CommandRegistry.cs (from Plan 30-01)
    - flow-interpreter/Program.cs (full file — eval/stdin/watch handling logic to mirror)
    - flow-interpreter/ScriptRunner.cs (full file — RunScript signature is the wrap target)
    - flow-interpreter/Repl.cs (first 30 lines)
    - flow-interpreter/LiveReloadManager.cs (first 30 lines — constructor and Run method)
    - flow-lang/Core/FlowEngine.cs (search for Parse and Execute — confirm whether parse-only method exists; if not, the check command invokes engine.Execute but redirects stdout to TextWriter.Null and inspects ErrorReporter.HasErrors)
  </read_first>

  <action>
Create 5 files in flow-cli/Commands/. Each is a static class XxxCommand with a public static Command Build() method using namespace FlowCli.Commands.

RunCommand.cs:
- Arguments: script (Argument FileInfo, required)
- Options: --device (string?), --verbose / -v (bool)
- Action: validates script.FullName exists (if not, write 'Error: File not found: {path}' to stderr and return 1); calls new FlowInterpreter.ScriptRunner().RunScript(script.FullName, device, verbose); returns its exit code.

EvalCommand.cs:
- Arguments: code (Argument string, required, description 'Flow source string')
- Options: --device, --verbose
- Action: instantiate using var engine = new FlowLang.Core.FlowEngine(verbose: verbose); if device provided and engine.AudioManager.IsAudioAvailable(), call engine.AudioManager.GetBackend().SetDevice(device); call engine.Execute(code, '<eval>'); if !success, print engine.ErrorReporter.FormatErrors() to stderr in red (mirror flow-interpreter/Program.cs RunFromString); return 0 on success, 1 on failure.

ReplCommand.cs:
- No arguments, no options
- Action: new FlowInterpreter.Repl().Run(); return 0;

WatchCommand.cs:
- Arguments: script (Argument FileInfo, required)
- Options: --device, --verbose
- Action: mirror flow-interpreter/Program.cs RunWithWatch: var fullPath = Path.GetFullPath(script.FullName); using var manager = new FlowInterpreter.LiveReloadManager(fullPath, device); manager.Run(); return 0;

CheckCommand.cs:
- Arguments: script (Argument FileInfo, required)
- Action: read source via File.ReadAllText; save original Console.Out reference; do Console.SetOut(TextWriter.Null) inside a try/finally so that Console.SetOut(originalOut) is restored unconditionally; create using var engine = new FlowEngine(verbose: false); call engine.Execute(source, script.FullName); after restoring stdout: if engine.ErrorReporter.HasErrors (or !success), print formatted errors to stderr and return 1; else print 'OK: {script.FullName}' to stdout and return 0. Add code comment noting this is parse-AND-execute; RESEARCH Open Question 2 deferred parse-only mode. If FlowEngine exposes a separate Parse-only method, prefer it (search the class first).

CommandRegistry.cs edit: replace the 5 placeholder lines for run, eval, repl, watch, check with calls to RunCommand.Build(), EvalCommand.Build(), ReplCommand.Build(), WatchCommand.Build(), CheckCommand.Build(). Leave the placeholders for play, render, flow2midi, midi2flow, new in place (handled in Tasks 2 and 3).
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-cli 2>&amp;1 | tail -5 &amp;&amp; dotnet run --project flow-cli -- eval 'Int x = 5; (print (str x))' 2>/dev/null | grep -E '^5$' &amp;&amp; dotnet run --project flow-cli -- run examples/showcase.flow &amp;&amp; dotnet run --project flow-cli -- check examples/showcase.flow</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-cli exits 0
    - dotnet run --project flow-cli -- eval 'Int x = 5; (print (str x))' exits 0 AND stdout contains '5' (regex ^5$ on a line)
    - dotnet run --project flow-cli -- run examples/showcase.flow exits 0 (matches dotnet run --project flow-interpreter examples/showcase.flow exit code)
    - dotnet run --project flow-cli -- check examples/showcase.flow exits 0
    - Broken-file negative test: write 'this is (((( unclosed' to /tmp/30-02-broken.flow; dotnet run --project flow-cli -- check /tmp/30-02-broken.flow exits non-zero
    - Nonexistent-file negative test: dotnet run --project flow-cli -- run /nonexistent/path.flow exits non-zero with stderr mentioning the path
    - All 5 new files exist: ls flow-cli/Commands/RunCommand.cs flow-cli/Commands/EvalCommand.cs flow-cli/Commands/ReplCommand.cs flow-cli/Commands/WatchCommand.cs flow-cli/Commands/CheckCommand.cs returns no error
    - grep -c 'RunCommand.Build\|EvalCommand.Build\|ReplCommand.Build\|WatchCommand.Build\|CheckCommand.Build' flow-cli/Commands/CommandRegistry.cs returns at least 5
    - grep -c 'BuildPlaceholder' flow-cli/Commands/CommandRegistry.cs returns at most 5 (only play, render, flow2midi, midi2flow, new still placeholders)
  </acceptance_criteria>

  <done>
    5/9 real subcommands wired. flow run, flow eval, flow repl (manual smoke only — interactive), flow watch (manual smoke only — interactive), flow check work end-to-end with backward-compatible behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Play, Render, Flow2Midi + Midi2Flow explicit-stub</name>
  <files>flow-cli/Commands/PlayCommand.cs, flow-cli/Commands/RenderCommand.cs, flow-cli/Commands/Flow2MidiCommand.cs, flow-cli/Commands/Midi2FlowStubCommand.cs, flow-cli/Commands/CommandRegistry.cs</files>

  <read_first>
    - flow-cli/Commands/RunCommand.cs (from Task 1 — copy its structure)
    - flow-cli/Commands/CommandRegistry.cs (current state)
    - .planning/phases/30-flow-cli-formal-install/30-SPEC.md (REQ-1 acceptance bullets)
    - flow-lang/StandardLibrary/Audio/FileIO.cs (first 50 lines — confirm WAV write is initiated by .flow source calling (writeWav ...))
    - flow-lang/StandardLibrary/Audio/MidiExport.cs (first 30 lines — confirm (writeMidi ...) is .flow-facing; CLI cannot auto-inject for Phase 30)
  </read_first>

  <action>
Create 4 command files in flow-cli/Commands/. All use namespace FlowCli.Commands.

PlayCommand.cs:
- Arguments: script (Argument FileInfo)
- Options: --device, --verbose
- Action: identical to RunCommand — call new ScriptRunner().RunScript(script.FullName, device, verbose); return its exit code. The script itself is responsible for calling (play output). Top-of-file comment must document this forwarding semantic.

RenderCommand.cs:
- Arguments: script (Argument FileInfo)
- Options: --output / -o (Option FileInfo, REQUIRED), --device, --verbose
- Action:
  1. Call new ScriptRunner().RunScript(script.FullName, device, verbose) — let the script's own (writeWav path output) write the file.
  2. After the runner returns, check File.Exists(output.FullName). If not, emit a charitable stderr warning: 'Warning: --output={output.FullName} but the script did not write to that path. For Phase 30, the .flow source must contain (writeWav "...") — auto-injection deferred (ROADMAP v1.5+).'
  3. Return the script's exit code regardless.
  4. Top-of-file comment documents the charitable interpretation (per CLAUDE.md feedback_charitable_interpretation memory).

Flow2MidiCommand.cs:
- Same shape as RenderCommand but with --output / -o for a .mid file. After execution, File.Exists check; warn if missing. The script's (writeMidi ...) does the actual work.

Midi2FlowStubCommand.cs:
- Arguments: input (Argument FileInfo) — accept but ignore
- Options: --output / -o (Option FileInfo) — accept but ignore
- Action: print to stderr 'flow midi2flow: This subcommand ships in Plan 30-09 after the flow-midi quantizer/generator rewrite (Phase 30 Bug B closure). See .planning/phases/30-flow-cli-formal-install/30-09-PLAN.md.' Return exit code 2.

CommandRegistry.cs edit: replace play, render, flow2midi, midi2flow placeholders with PlayCommand.Build(), RenderCommand.Build(), Flow2MidiCommand.Build(), Midi2FlowStubCommand.Build(). Only new remains as placeholder.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-cli 2>&amp;1 | tail -3 &amp;&amp; rm -f /tmp/test30-render.wav /tmp/test30-flow2midi.mid &amp;&amp; dotnet run --project flow-cli -- render examples/showcase.flow -o /tmp/test30-render.wav 2>&amp;1 | tail -3 &amp;&amp; dotnet run --project flow-cli -- flow2midi examples/showcase.flow -o /tmp/test30-flow2midi.mid 2>&amp;1 | tail -3 ; dotnet run --project flow-cli -- midi2flow examples/ragtime.mid -o /tmp/test30-out.flow ; echo "midi2flow-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-cli exits 0
    - dotnet run --project flow-cli -- render examples/showcase.flow -o /tmp/test30-render.wav exits 0 (subject to examples/showcase.flow writing somewhere — if it writes to examples/output/showcase.wav, the charitable warning fires but exit is still success)
    - dotnet run --project flow-cli -- flow2midi examples/showcase.flow -o /tmp/test30-flow2midi.mid exits 0
    - dotnet run --project flow-cli -- midi2flow examples/ragtime.mid -o /tmp/test30-out.flow exits with code 2 AND stderr contains 'Plan 30-09'
    - All 4 new files exist: ls flow-cli/Commands/PlayCommand.cs flow-cli/Commands/RenderCommand.cs flow-cli/Commands/Flow2MidiCommand.cs flow-cli/Commands/Midi2FlowStubCommand.cs returns no error
    - grep -c 'PlayCommand.Build\|RenderCommand.Build\|Flow2MidiCommand.Build\|Midi2FlowStubCommand.Build' flow-cli/Commands/CommandRegistry.cs returns at least 4
    - grep -c 'BuildPlaceholder' flow-cli/Commands/CommandRegistry.cs returns at most 1 (only 'new' remains placeholder)
  </acceptance_criteria>

  <done>
    9/11 subcommands wired (4 added here + 5 from Task 1; only 'new' and the explicit-stub 'midi2flow' remain). flow render and flow flow2midi forward to ScriptRunner; midi2flow returns exit 2 with explicit message.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement New (scaffold) — embedded template + scaffold emitter</name>
  <files>flow-cli/Scaffold/Templates/default.flow, flow-cli/Scaffold/ScaffoldEmitter.cs, flow-cli/Commands/NewCommand.cs, flow-cli/Commands/CommandRegistry.cs, flow-cli/flow-cli.csproj</files>

  <read_first>
    - flow-cli/flow-cli.csproj (must add an EmbeddedResource ItemGroup)
    - .planning/phases/30-flow-cli-formal-install/30-RESEARCH.md offset=760 limit=50 ('flow new Scaffold Template' section — locks ~30-line single-file scaffold)
    - examples/tutorial.flow (look at the opening 20-30 lines for canonical use / tempo / timesig / key / section idiom — match style)
    - flow-cli/Commands/RunCommand.cs (Build() shape to mirror)
    - flow-cli/Commands/CommandRegistry.cs (replace the final placeholder)
    - CLAUDE.md (ergonomics-first; the scaffold must produce a working musical fragment, not Hello World)
  </read_first>

  <action>
Create flow-cli/Scaffold/Templates/default.flow with this exact content (the {{PIECE_NAME}} token is substituted at emit time):

```
// {{PIECE_NAME}}.flow — generated by flow new
//
// Run with:    flow run {{PIECE_NAME}}.flow
// Render to:   flow render {{PIECE_NAME}}.flow -o {{PIECE_NAME}}.wav
// Play live:   flow play {{PIECE_NAME}}.flow

use "@std"
use "@audio"
use "@notation"

tempo 120 {
    timesig 4/4 {
        key Cmajor {

            section main {
                Sequence melody = | C4q D4q E4q F4q | G4q F4q E4q D4q | C4w |
            }

            Song song = [main]
            Buffer output = (renderSong song "piano")
            (writeWav "{{PIECE_NAME}}.wav" output)
        }
    }
}
```

Add to flow-cli/flow-cli.csproj an ItemGroup:
```
<ItemGroup>
  <EmbeddedResource Include="Scaffold\Templates\default.flow" />
</ItemGroup>
```

Create flow-cli/Scaffold/ScaffoldEmitter.cs:
```
namespace FlowCli.Scaffold;
using System.Reflection;

public static class ScaffoldEmitter
{
    // Writes a scaffold .flow file to <targetDir>/<pieceName>.flow.
    // If targetDir does not exist, creates it. If <targetDir>/<pieceName>.flow exists, returns false (no overwrite).
    public static bool WriteScaffold(string pieceName, string targetDir, out string writtenPath, out string? error)
    {
        writtenPath = string.Empty;
        error = null;

        if (string.IsNullOrWhiteSpace(pieceName) || pieceName.Any(c => !char.IsLetterOrDigit(c) && c != '_' && c != '-'))
        {
            error = $"Invalid piece name '{pieceName}': must contain only letters, digits, underscore, hyphen.";
            return false;
        }

        Directory.CreateDirectory(targetDir);
        writtenPath = Path.Combine(targetDir, $"{pieceName}.flow");

        if (File.Exists(writtenPath))
        {
            error = $"File already exists: {writtenPath} (not overwriting).";
            return false;
        }

        // Find the embedded resource. The resource name follows .NET convention:
        // {RootNamespace}.{Path with dots}.{filename}. RootNamespace=FlowCli, path=Scaffold/Templates, file=default.flow
        // Expected: 'FlowCli.Scaffold.Templates.default.flow'
        var asm = Assembly.GetExecutingAssembly();
        var resourceName = "FlowCli.Scaffold.Templates.default.flow";
        using var stream = asm.GetManifestResourceStream(resourceName);
        if (stream == null)
        {
            // Defensive: list available resources and surface in error message
            var avail = string.Join(", ", asm.GetManifestResourceNames());
            error = $"Embedded resource not found: {resourceName}. Available: {avail}";
            return false;
        }

        using var reader = new StreamReader(stream);
        var template = reader.ReadToEnd();
        var content = template.Replace("{{PIECE_NAME}}", pieceName);
        File.WriteAllText(writtenPath, content);
        return true;
    }
}
```

Create flow-cli/Commands/NewCommand.cs:
```
using System.CommandLine;
using FlowCli.Scaffold;

namespace FlowCli.Commands;

static class NewCommand
{
    public static Command Build()
    {
        var nameArg = new Argument<string>("name") { Description = "Piece name (becomes <name>/<name>.flow)" };
        var dirOpt = new Option<DirectoryInfo?>("--dir") { Description = "Target directory (default: ./<name>/)" };

        var cmd = new Command("new", "Scaffold a new Flow piece");
        cmd.Add(nameArg);
        cmd.Add(dirOpt);
        cmd.SetAction(parseResult =>
        {
            var name = parseResult.GetValue(nameArg)!;
            var dir = parseResult.GetValue(dirOpt);
            var targetDir = dir?.FullName ?? Path.Combine(Directory.GetCurrentDirectory(), name);

            if (!ScaffoldEmitter.WriteScaffold(name, targetDir, out var path, out var err))
            {
                Console.Error.WriteLine($"flow new: {err}");
                return 1;
            }

            Console.WriteLine($"Created {path}");
            Console.WriteLine($"Next: flow run {path}");
            return 0;
        });
        return cmd;
    }
}
```

CommandRegistry.cs edit: replace the remaining placeholder for 'new' with NewCommand.Build(). After this edit, grep -c 'BuildPlaceholder' returns 0.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-cli 2>&amp;1 | tail -3 &amp;&amp; rm -rf /tmp/test30-new &amp;&amp; dotnet run --project flow-cli -- new test30 --dir /tmp/test30-new &amp;&amp; ls /tmp/test30-new/test30.flow &amp;&amp; cd /tmp/test30-new &amp;&amp; /home/noah/Desktop/projects/flow-sharp/flow-cli/bin/Debug/net10.0/flow run /tmp/test30-new/test30.flow 2>&amp;1 | tail -5 ; cd - &gt;/dev/null</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-cli exits 0
    - dotnet run --project flow-cli -- new test30 --dir /tmp/test30-new exits 0
    - File /tmp/test30-new/test30.flow exists after the command
    - grep -c 'tempo 120' /tmp/test30-new/test30.flow returns at least 1 (template was emitted)
    - grep -c 'test30.wav' /tmp/test30-new/test30.flow returns at least 1 (PIECE_NAME substitution worked)
    - dotnet run --project flow-cli -- run /tmp/test30-new/test30.flow exits 0
    - dotnet run --project flow-cli -- new test30 --dir /tmp/test30-new exits non-zero (no overwrite of existing file) AND stderr contains 'already exists'
    - dotnet run --project flow-cli -- new 'bad name with spaces' --dir /tmp/test30-badname exits non-zero (invalid piece name)
    - grep -c 'BuildPlaceholder' flow-cli/Commands/CommandRegistry.cs returns 0 (all 11 wired)
    - grep -c 'EmbeddedResource' flow-cli/flow-cli.csproj returns at least 1
  </acceptance_criteria>

  <done>
    11/11 subcommands fully wired (10 real handlers + 1 explicit deferral-stub for midi2flow). flow new creates a working scaffold; flow run on the scaffold exits 0. CommandRegistry contains zero BuildPlaceholder calls.
  </done>
</task>

</tasks>

<verification>
- dotnet build flow-cli exits 0
- dotnet build flow-interpreter exits 0 (REQ-8 backward compat)
- dotnet run --project flow-cli -- --help shows all 11 subcommand names
- 9 subcommands exit 0 on examples/showcase.flow or generated scaffold; midi2flow exits 2 with deferral message
- flow new scaffold produces a runnable .flow file
</verification>

<success_criteria>
- 10 real subcommand handlers + 1 explicit stub committed
- CommandRegistry contains zero BuildPlaceholder calls
- Embedded default.flow scaffold renders end-to-end through flow run
- REQ-1 acceptance bullet list 'flow run script.flow ... flow new my-piece — all 11 subcommands exit 0 on valid input' is satisfied (10/11 here; midi2flow at Plan 30-09)
- flow-interpreter dotnet run unchanged in behavior
</success_criteria>

<output>
After completion, create .planning/phases/30-flow-cli-formal-install/30-02-SUMMARY.md summarizing:
- The 10 real Build() implementations (run/eval/repl/watch/play/render/flow2midi/check/new — and version from Plan 01)
- The explicit midi2flow stub with its deferral message
- The embedded default.flow scaffold and substitution mechanism
- Backward-compat status: flow-interpreter unchanged
- The 'render without auto-inject writeWav' charitable warning design tradeoff (RESEARCH.md SPEC-3 Background — script self-writes)
</output>
