---
phase: 30-flow-cli-formal-install
plan: 06
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - flow-midi.Tests/flow-midi.Tests.csproj
  - flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs
  - flow-midi.Tests/Unit/Phase30/HarnessSmokeFacts.cs
  - flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs
  - flow-midi.Tests/Unit/Phase30/QuantizerRoundingTests.cs
  - flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests.cs
  - flow-midi/flow-midi.csproj
  - flow-sharp.sln
autonomous: true
requirements: [REQ-6]

must_haves:
  truths:
    - "A new flow-midi.Tests xUnit project exists in flow-sharp.sln (absent on HEAD before this plan)"
    - "flow-midi.Tests.csproj uses xunit.v3 3.2.2 + Microsoft.NET.Test.Sdk 17.13.0; references flow-midi and flow-lang"
    - "MidiFixtureBuilder fluent helper exists in flow-midi.Tests/Fixtures/ and constructs MidiFile instances at TPQN=480 without external .mid files"
    - "HarnessSmokeFacts is GREEN-on-HEAD (proves harness works)"
    - "Three RED-on-HEAD fact classes pin Bug B defects 1, 2, 3: QuantizerSnapDurationTests + QuantizerRoundingTests + FlowGeneratorStructureTests"
    - "RED facts are ACTIVE (not Skip-attributed) — Plans 30-07 and 30-08 turn them GREEN by changing production code; this plan does NOT touch Quantizer.cs or FlowGenerator.cs"
    - "dotnet test flow-midi.Tests exits non-zero on HEAD (RED facts fail by design); after Plans 30-07 + 30-08 ship, exits 0"
  artifacts:
    - path: "flow-midi.Tests/flow-midi.Tests.csproj"
      provides: "xUnit project: net10.0; references flow-midi and flow-lang"
    - path: "flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs"
      provides: "Fluent builder: AddNote, AddTempoEvent, AddTimeSignatureEvent, AddKeySignatureEvent, WithFormat, WithTpqn, Build"
    - path: "flow-midi.Tests/Unit/Phase30/HarnessSmokeFacts.cs"
      provides: "2 GREEN facts proving Quantizer.SuffixToTicks is reachable and MidiFixtureBuilder produces valid MidiFile instances"
    - path: "flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs"
      provides: "RED facts pinning Bug B Defect 1 — 480-tick quarter must snap to ('q', false) end-to-end via Quantize, NOT to ('s', true) or any dotted-sixteenth"
    - path: "flow-midi.Tests/Unit/Phase30/QuantizerRoundingTests.cs"
      provides: "RED facts pinning Bug B Defect 2 (AddRests over-emission + leading-empty-bar trim) and Defect 3 (RH/LH pitch-split heuristic produces 2 sequences for a single >2-octave-range track — must produce 1)"
    - path: "flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests.cs"
      provides: "RED facts on FlowGenerator emit shape — no (play output) trailer in midi2flow path, one Sequence per source track (no _rh/_lh suffixes), no auto-fit elision when explicit-durations is requested"
  key_links:
    - from: "flow-midi.Tests/Unit/Phase30/*.cs"
      to: "flow-midi/Conversion/Quantizer.cs and FlowGenerator.cs"
      via: "Quantizer.Quantize(MidiFile) + FlowGenerator.Generate(midi, qr, name)"
      pattern: "Quantizer\\.|FlowGenerator\\."
    - from: "flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs"
      to: "flow-midi/Midi/* types (MidiFile, NoteOnEvent, NoteOffEvent, TempoEvent, TimeSignatureEvent, KeySignatureEvent)"
      via: "new MidiFile { Format, TicksPerQuarterNote, Tracks }"
      pattern: "new MidiFile"
---

<objective>
Stand up flow-midi.Tests as the FIRST gate of Bug B closure (RESEARCH Layer 1 — test infrastructure precedes rewrites). Add the project to the solution. Build MidiFixtureBuilder so Plans 30-07 + 30-08 can synthesize MIDI without external files. Write 3 RED-on-HEAD test classes pinning Bug B defects 1/2/3 so subsequent plans have a clear GREEN target.

Purpose: REQ-6 infrastructure. SPEC-6 round-trip needs a test target before quantizer/generator rewrites can be verified incrementally.

Output: flow-midi.Tests in solution; MidiFixtureBuilder + 3 RED fact classes + 1 GREEN smoke fact class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-flow-cli-formal-install/30-SPEC.md
@.planning/phases/30-flow-cli-formal-install/30-RESEARCH.md
@.planning/debug/midi-import-quarter-quantize.md
@CLAUDE.md
@flow-sharp.sln
@flow-midi/flow-midi.csproj
@flow-midi/Conversion/Quantizer.cs
@flow-midi/Conversion/FlowGenerator.cs
@flow-midi/Midi/MidiParser.cs
@flow-lang.Tests/flow-lang.Tests.csproj

<interfaces>
flow-midi public types — REQUIRES VERIFICATION before writing tests. Read flow-midi/Midi/MidiParser.cs to confirm exact accessibility (public vs internal) and field/property shape (mutable property vs init-only vs constructor). Based on the typical flow-midi pattern:

```
namespace FlowMidi.Midi;
// MidiFile, MidiTrack, MidiEvent + subclasses (NoteOnEvent, NoteOffEvent, TempoEvent, TimeSignatureEvent, KeySignatureEvent)
// If they're internal, this plan adds InternalsVisibleTo to flow-midi.csproj.
```

From flow-midi/Conversion/Quantizer.cs (confirmed public at lines 67, 87, 103):
```
namespace FlowMidi.Conversion;
public static class Quantizer
{
    public static long SuffixToTicks(string suffix, bool isDotted, int tpqn);
    public static QuantizeResult Quantize(MidiFile midi);
}
public record QuantizeResult(List<QuantizedTrack> Tracks, int TimeSigNumerator, int TimeSigDenominator);
public record QuantizedTrack(string Name, List<QuantizedBar> Bars, int Channel, bool IsDrumTrack);
public record QuantizedBar(List<IBarElement> Elements, int BarNumber);
// IBarElement subtypes: NoteElement, ChordElement, RestElement (each with DurationSuffix + IsDotted)
```

From flow-midi/Conversion/FlowGenerator.cs (confirmed public):
```
public static class FlowGenerator
{
    public static string Generate(MidiFile midi, QuantizeResult quantizeResult, string sourceFileName);
}
```

flow-lang.Tests.csproj is the template — copy its structure but change RootNamespace to FlowMidi.Tests and references to flow-midi + flow-lang.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create flow-midi.Tests project + add to solution + reach internal types if needed</name>
  <files>flow-midi.Tests/flow-midi.Tests.csproj, flow-midi/flow-midi.csproj, flow-sharp.sln</files>

  <read_first>
    - flow-sharp.sln (Plan 30-01 added flow-cli — mirror that pattern)
    - flow-lang.Tests/flow-lang.Tests.csproj (template)
    - flow-midi/flow-midi.csproj (current — may need InternalsVisibleTo)
    - flow-midi/Midi/MidiParser.cs (grep 'public class\|internal class\|public abstract' to confirm accessibility of MidiFile/Track/Event types)
  </read_first>

  <action>
1. Create flow-midi.Tests/flow-midi.Tests.csproj mirroring flow-lang.Tests.csproj structure. RootNamespace=FlowMidi.Tests. Both ProjectReferences: flow-midi + flow-lang. Versions: Microsoft.NET.Test.Sdk 17.13.0, xunit.v3 3.2.2, xunit.runner.visualstudio 3.1.5, coverlet.collector 6.0.2.

2. If any flow-midi/Midi types are `internal class`, edit flow-midi/flow-midi.csproj to add:
   ```
   <ItemGroup>
     <InternalsVisibleTo Include="flow-midi.Tests" />
   </ItemGroup>
   ```

3. Edit flow-sharp.sln to add the flow-midi.Tests project. Generate a unique GUID (different from the 7 existing ones and from Plan 30-01's flow-cli GUID). Add:
   - Project(...) = "flow-midi.Tests", "flow-midi.Tests\flow-midi.Tests.csproj", "{NEW-GUID}" + EndProject line
   - 12 corresponding ProjectConfigurationPlatforms lines (Debug/Release x AnyCPU/x64/x86 x ActiveCfg/Build.0)
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi.Tests 2>&amp;1 | tail -5 &amp;&amp; dotnet sln list | grep -c '^flow-midi.Tests' &amp;&amp; ls flow-midi.Tests/flow-midi.Tests.csproj</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi.Tests exits 0
    - dotnet sln list output contains exactly 1 line starting with 'flow-midi.Tests'
    - grep -c 'flow-midi.Tests' flow-sharp.sln returns at least 2
    - grep -c 'xunit.v3.*3.2.2' flow-midi.Tests/flow-midi.Tests.csproj returns 1
    - grep -c 'ProjectReference.*flow-midi.csproj' flow-midi.Tests/flow-midi.Tests.csproj returns 1
    - grep -c 'ProjectReference.*flow-lang.csproj' flow-midi.Tests/flow-midi.Tests.csproj returns 1
    - If flow-midi types are internal: grep -c 'InternalsVisibleTo' flow-midi/flow-midi.csproj returns at least 1
    - Existing dotnet build flow-midi still exits 0
  </acceptance_criteria>

  <done>
    flow-midi.Tests is in the solution, builds clean, can construct flow-midi data types from test code. Plans 30-07 + 30-08 can immediately add facts here.
  </done>
</task>

<task type="auto">
  <name>Task 2: MidiFixtureBuilder helper + HarnessSmokeFacts (GREEN)</name>
  <files>flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs, flow-midi.Tests/Unit/Phase30/HarnessSmokeFacts.cs</files>

  <read_first>
    - flow-midi/Midi/MidiParser.cs (full file — confirm exact constructor + property shape of MidiFile, MidiTrack, and the 5 event subtypes)
    - flow-midi/Conversion/Quantizer.cs offset=1 limit=70 (public API + records)
  </read_first>

  <action>
Create flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs — fluent builder. Provides at minimum these methods:
- WithTpqn(int tpqn) — default 480
- WithFormat(int format) — default 0
- WithTrackName(string name) — applies to current track
- StartNewTrack() — adds a new MidiTrack, makes it current
- AddTempoEvent(double bpm, long tick = 0)
- AddTimeSignatureEvent(int num, int den, long tick = 0)
- AddKeySignatureEvent(int sharpsFlats, bool isMinor, long tick = 0)
- AddNote(int channel, int pitch, long startTick, long endTick, int velocity = 100) — adds matched NoteOn + NoteOff
- AddFourQuarterNotes(int channel, int pitch) — convenience: pitch, pitch+2, pitch+4, pitch+5 at tick 0/tpqn/2tpqn/3tpqn (4/4 fits in 1 bar at TPQN=480)
- Build() — sorts events by AbsoluteTick within each track, returns MidiFile

CRITICAL: read flow-midi/Midi/MidiParser.cs BEFORE writing this file to confirm whether the event/track types use:
- Public mutable properties (use object initializer syntax: new NoteOnEvent { AbsoluteTick=..., Channel=..., Pitch=..., Velocity=... })
- Constructors (use positional: new NoteOnEvent(tick, channel, pitch, velocity))
- Record types (positional)
Adapt the builder to whatever shape exists. If types are internal AND InternalsVisibleTo was added in Task 1, this file can use them directly.

Create flow-midi.Tests/Unit/Phase30/HarnessSmokeFacts.cs with 2 GREEN-on-HEAD facts:
- Fact 1: SuffixToTicks_Quarter_At_Tpqn_480_Returns_480 — asserts Quantizer.SuffixToTicks("q", false, 480) == 480
- Fact 2: Builder_Constructs_MidiFile_With_Expected_Events — constructs a fixture, asserts TicksPerQuarterNote == 480 AND track count == 1 AND event count == expected (2 meta + 2 note events for 1 note)
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi.Tests 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-midi.Tests --filter 'FullyQualifiedName~HarnessSmokeFacts' --logger 'console;verbosity=minimal' 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi.Tests exits 0
    - dotnet test flow-midi.Tests --filter HarnessSmokeFacts reports 2 passed / 0 failed
    - File flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs exists
    - File flow-midi.Tests/Unit/Phase30/HarnessSmokeFacts.cs exists
    - grep -c 'public MidiFixtureBuilder\|public MidiFile Build' flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs returns at least 2 (class + Build)
    - grep -c 'AddTempoEvent\|AddTimeSignatureEvent\|AddKeySignatureEvent\|AddNote' flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs returns at least 4
  </acceptance_criteria>

  <done>
    Harness validated. MidiFixtureBuilder reachable. Plans 30-07 + 30-08 can use it to synthesize tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 3 RED-on-HEAD fact classes pinning Bug B defects 1, 2, 3</name>
  <files>flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs, flow-midi.Tests/Unit/Phase30/QuantizerRoundingTests.cs, flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests.cs</files>

  <read_first>
    - .planning/debug/midi-import-quarter-quantize.md (full file — Bug B symptoms + 3 defects)
    - .planning/phases/30-flow-cli-formal-install/30-RESEARCH.md offset=359 limit=80 (Bug B Scope Assessment + 3 defect breakdown + Layer 2/3 rework prescriptive guidance)
    - flow-midi/Conversion/Quantizer.cs (full file — defects 1+2+3 live here at lines 201-247 RH/LH split, 346-479 QuantizeSpans + bar emission, 568-602 SnapDurationCapped, 604-637 AddRests)
    - flow-midi/Conversion/FlowGenerator.cs (full file — defect: 100 always 1 section, 123 always (play output), 235-281 CanAutoFit elision)
    - flow-midi.Tests/Fixtures/MidiFixtureBuilder.cs (from Task 2)
  </read_first>

  <action>
Write 3 fact files. Each fact is ACTIVE (not [Skip]). Each fact's assertion fails on current HEAD by design, with the failure message citing the SPEC requirement that the failing behavior violates.

**QuantizerSnapDurationTests.cs (pins Defect 1: quarter-note mis-snap to dotted-16th):**

At minimum these facts, each calling Quantizer.Quantize(MidiFile) end-to-end (NOT testing the internal SnapDurationCapped directly):

1. `FourQuarterNotes_In_4_4_Produce_Four_Q_Tokens`: build a fixture with 4 quarter notes at ticks 0/480/960/1440 in 4/4 TPQN=480; call Quantize; assert the resulting track has 4 NoteElement instances; assert ALL 4 NoteElements have DurationSuffix == "q" AND IsDotted == false. On HEAD this fails because Bug B Defect 1 snaps some quarters to dotted-sixteenth + rests.

2. `Quarter_Note_With_One_Tick_Gap_Still_Snaps_To_Q`: build a fixture with 4 quarter notes but with the 2nd note starting at tick 479 (1 tick early); call Quantize; assert the first NoteElement is still ("q", false). The strict-cap algorithm currently rejects this — fix in Plan 30-07 must add a small tolerance to SnapDurationCapped.

3. `Quarter_Eighth_Eighth_Pattern_Produces_Q_E_E_In_Order`: build a Bug-B-shaped fixture (one quarter at tick 0 ending tick 480, then two eighths at tick 480/720 ending tick 720/960); call Quantize; assert the resulting elements in order are NoteElement{q,false}, NoteElement{e,false}, NoteElement{e,false} (3 notes total in the bar, no rests). On HEAD this fails — the Bug B reported pattern.

For each Fact, in the test message reference: `// Pins Bug B Defect 1 — see .planning/debug/midi-import-quarter-quantize.md and 30-RESEARCH.md Bug B Scope.`

**QuantizerRoundingTests.cs (pins Defect 2 + Defect 3):**

1. `Empty_Leading_Bars_Are_Trimmed`: build a fixture where the FIRST note is at tick 1920 (start of bar 2 at 4/4 TPQN=480 means tick 0/480/960/1440 in bar 1 are silent); call Quantize; assert the resulting track has Bars[0].BarNumber == 1 (zero-indexed bar 1, i.e. the second bar), NOT BarNumber == 0 with all rest elements. On HEAD this fails: QuantizeSpans emits a bar for tick 0 with only rest elements.

2. `Rest_Of_Half_Bar_Is_One_Half_Rest_Not_Eight_Sixteenths`: build a fixture with a quarter note at tick 0-480, then silence to tick 1920 (3 quarter-rests of gap); call Quantize; assert that between the note and end-of-bar the number of RestElement entries is small (at most 3 — one per beat) AND not 8+ thirty-seconds. The exact target depends on Plan 30-07's AddRests fix; the assertion can be "RestElement.Count <= 4 in the bar's gap region" as a generous upper bound on HEAD failure (HEAD emits 5+ rests for sub-grid gaps).

3. `Two_Octave_Range_Does_Not_Split_RH_LH`: build a fixture with notes spanning C2 (MIDI 36) to C5 (MIDI 72) — 36 semitones, >24 — all on channel 0; call Quantize; assert that result.Tracks.Count == 1 AND result.Tracks[0].Name does NOT end in "_rh" or "_lh". On HEAD: AddSplitTracks splits this into two tracks. Plan 30-08 removes that heuristic.

**FlowGeneratorStructureTests.cs (pins SPEC-5 emit structure):**

1. `Generated_Output_Has_No_Play_Output_Trailer_When_Round_Trip_Mode`: build a fixture, call Quantize, call FlowGenerator.Generate; assert the resulting string does NOT contain "(play output)" as a substring. On HEAD this fails (FlowGenerator.cs:123 always emits it). Plan 30-08 must add a round-trip mode flag (or always drop it — RESEARCH recommends always drop and let composer add (play output) manually after editing).

2. `One_Sequence_Per_Track_Channel_No_RH_LH_Suffix`: build a 2-channel Format-0 fixture, each channel with notes; call Quantize + Generate; assert the generated source contains exactly 2 `Sequence ` declarations (use regex match count) AND neither sequence name ends in `_rh` or `_lh`. On HEAD this fails when the channels are pitch-wide.

3. `No_Auto_Fit_When_Track_Has_Mixed_Durations`: per SPEC-5 and RESEARCH "drop auto-fit suffix-elision when round-trip fidelity matters" — build a fixture with mixed q + e durations; call Generate; assert ALL note tokens in the generated `| ... |` stream carry an explicit duration suffix (`q`, `e`, etc.). On HEAD: when all notes share a duration, CanAutoFit returns true and the suffix is elided. This fact is parameterized to TWO inputs: (a) all-quarter (auto-fit triggers on HEAD), (b) mixed q+e (no auto-fit on HEAD, GREEN). The (a) case is the RED-on-HEAD pin until Plan 30-08 forces explicit durations.

Each fact includes a code comment referencing the defect: `// Pins Bug B Defect 2 — empty-leading-bar emission (Quantizer.cs:355-359 emits bars from idx 0)`.

NOTE: These facts test PRODUCTION code that this plan does NOT change. They are designed to fail on HEAD. The verify command below CONFIRMS they fail — that's the goal.
  </action>

  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-midi.Tests 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-midi.Tests --filter 'FullyQualifiedName~QuantizerSnapDuration|FullyQualifiedName~QuantizerRounding|FullyQualifiedName~FlowGeneratorStructure' --logger 'console;verbosity=minimal' 2>&amp;1 | tail -25 ; echo "test-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - dotnet build flow-midi.Tests exits 0
    - All 3 fact files exist: ls flow-midi.Tests/Unit/Phase30/QuantizerSnapDurationTests.cs flow-midi.Tests/Unit/Phase30/QuantizerRoundingTests.cs flow-midi.Tests/Unit/Phase30/FlowGeneratorStructureTests.cs
    - Each file has at least 3 [Fact] attributes (or [Theory] with cases): grep -c '\[Fact\]\|\[Theory\]' on each file returns at least 3
    - dotnet test on the 3 filtered fact classes reports FAILURES on HEAD (exit code non-zero) — this is the GOAL. The failures form the GREEN target for Plans 30-07 + 30-08.
    - Failure count: at least 5 facts fail on HEAD (covering defects 1/2/3 + flat-output + no-play-trailer)
    - HarnessSmokeFacts STILL pass: dotnet test flow-midi.Tests --filter HarnessSmokeFacts reports 2 passed / 0 failed (RED facts do not pollute GREEN smoke)
    - Each test class has a top-of-file comment referencing .planning/debug/midi-import-quarter-quantize.md
    - No fact uses [Fact(Skip = ...)] — Skip attribute count: grep -rc 'Skip\s*=' flow-midi.Tests/Unit/Phase30/ returns 0
  </acceptance_criteria>

  <done>
    Bug B's 3 defects are now codified as failing xUnit facts. Plan 30-07 must turn the QuantizerSnapDurationTests + QuantizerRoundingTests defect-1/2/3 facts GREEN. Plan 30-08 must turn the FlowGeneratorStructureTests facts GREEN. The harness fails today by design — failure is the point of this plan.
  </done>
</task>

</tasks>

<verification>
- dotnet build flow-midi.Tests exits 0
- HarnessSmokeFacts: 2 GREEN
- QuantizerSnapDurationTests + QuantizerRoundingTests + FlowGeneratorStructureTests: at least 5 RED on HEAD
- flow-lang.Tests still GREEN (no cross-project regression)
- Solution build: dotnet build (whole solution) exits 0 even though tests fail when run
</verification>

<success_criteria>
- REQ-6 test infrastructure provisioned
- flow-midi.Tests added to solution
- MidiFixtureBuilder fluent helper available
- Bug B defects 1/2/3 codified as failing xUnit facts pointing at the exact code lines responsible
- Plans 30-07 and 30-08 have a clear GREEN target — these tests SHOULD NOT BE EDITED by those plans except via production-code changes
</success_criteria>

<output>
After completion, create .planning/phases/30-flow-cli-formal-install/30-06-SUMMARY.md summarizing:
- flow-midi.Tests project structure
- The MidiFixtureBuilder API surface
- The 3 RED-on-HEAD fact classes and which Bug B defect each pins
- The 2 GREEN smoke facts
- The exact failing fact count on HEAD (for Plans 30-07/08 to track green-progress)
</output>
