---
phase: 32
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-lang.Tests/fixtures/scala/partch_43.scl
  - flow-lang.Tests/fixtures/scala/slendro.scl
  - flow-lang.Tests/fixtures/scala/carlos_alpha.scl
  - flow-lang.Tests/fixtures/scala/pythagorean_12.scl
  - flow-lang.Tests/fixtures/scala/just_5limit.scl
  - flow-lang.Tests/fixtures/scala/malformed_step_count.scl
  - flow-lang.Tests/fixtures/scala/malformed_cents.scl
  - flow-lang.Tests/fixtures/scala/malformed_kbm.kbm
  - flow-lang.Tests/fixtures/scala/LICENSE.md
autonomous: true
requirements: [SPEC-3, SPEC-6, SPEC-7]
must_haves:
  truths:
    - "All 5 canonical Scala archive .scl fixtures exist in flow-lang.Tests/fixtures/scala/"
    - "3 hand-authored malformed fixtures exist for error-path tests"
    - "LICENSE.md cites Huygens-Fokker attribution + documents pyth_12.scl → pythagorean_12.scl and ji_12.scl → just_5limit.scl renames"
  artifacts:
    - path: "flow-lang.Tests/fixtures/scala/partch_43.scl"
      provides: "Harry Partch 43-tone fixture; 43 step lines ending 2/1 (verified content)"
      contains: "Harry Partch's 43-tone pure scale"
    - path: "flow-lang.Tests/fixtures/scala/slendro.scl"
      provides: "Javanese slendro fixture; 5 steps cents+ratio mix"
      contains: "Observed Javanese Slendro scale"
    - path: "flow-lang.Tests/fixtures/scala/carlos_alpha.scl"
      provides: "Non-octave fixture; period = 1404.00000¢"
      contains: "1404.00000"
    - path: "flow-lang.Tests/fixtures/scala/pythagorean_12.scl"
      provides: "12-tone Pythagorean fixture (renamed from pyth_12.scl)"
      contains: "12-tone Pythagorean scale"
    - path: "flow-lang.Tests/fixtures/scala/just_5limit.scl"
      provides: "5-limit JI fixture (renamed from ji_12.scl; 7-limit tritone at step 6)"
      contains: "Basic JI with 7-limit tritone"
    - path: "flow-lang.Tests/fixtures/scala/malformed_step_count.scl"
      provides: "Negative step count for SPEC-7 error tests"
    - path: "flow-lang.Tests/fixtures/scala/malformed_cents.scl"
      provides: "Non-numeric token on step line for SPEC-7 error tests"
    - path: "flow-lang.Tests/fixtures/scala/malformed_kbm.kbm"
      provides: "Negative reference frequency for SPEC-7 error tests"
    - path: "flow-lang.Tests/fixtures/scala/LICENSE.md"
      provides: "Attribution + rename audit trail per D-17"
      contains: "Huygens-Fokker"
  key_links:
    - from: "flow-lang.Tests/fixtures/scala/LICENSE.md"
      to: "flow-lang.Tests/fixtures/scala/pythagorean_12.scl"
      via: "documented rename from pyth_12.scl"
      pattern: "pyth_12.scl"
    - from: "flow-lang.Tests/fixtures/scala/LICENSE.md"
      to: "flow-lang.Tests/fixtures/scala/just_5limit.scl"
      via: "documented rename from ji_12.scl"
      pattern: "ji_12.scl"
---

<objective>
Wave 0: commit the fixture battery + LICENSE attribution that every other Phase 32 plan depends on.

Purpose: unblock parser, builtin, AST, and acceptance plans by providing byte-verified Scala-archive content in-repo. Renames (`pyth_12.scl` → `pythagorean_12.scl`, `ji_12.scl` → `just_5limit.scl`) honor SPEC fixture names per D-16; original archive filenames are documented in LICENSE.md AND in each renamed file's first `!` comment line so the audit trail is intact.

Output: 5 canonical fixtures + 3 hand-authored malformed fixtures + 1 LICENSE.md under `flow-lang.Tests/fixtures/scala/`. Total file budget < 100 KB per SPEC constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md
@flow-lang/Samples/CREDITS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch and commit the 5 canonical Scala archive fixtures</name>
  <files>
    flow-lang.Tests/fixtures/scala/partch_43.scl,
    flow-lang.Tests/fixtures/scala/slendro.scl,
    flow-lang.Tests/fixtures/scala/carlos_alpha.scl,
    flow-lang.Tests/fixtures/scala/pythagorean_12.scl,
    flow-lang.Tests/fixtures/scala/just_5limit.scl
  </files>
  <read_first>
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (sections "Fixture Sourcing & Reference Values" and "Verified contents" — verbatim file contents are documented there for content cross-check)
    .planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md (D-16: filename renames; D-17: license wording)
  </read_first>
  <action>
    Fetch the 5 fixtures from the narenratan GitHub mirror raw URLs and save under `flow-lang.Tests/fixtures/scala/`. Use the SPEC-mandated in-repo names (renaming the two archive originals):

    - `partch_43.scl` ← `https://raw.githubusercontent.com/narenratan/scala_scale_archive/main/scl/partch_43.scl` (no rename; final step `2/1`)
    - `slendro.scl` ← `https://raw.githubusercontent.com/narenratan/scala_scale_archive/main/scl/slendro.scl` (no rename; 5 steps, mixed cents + final `2/1`)
    - `carlos_alpha.scl` ← `https://raw.githubusercontent.com/narenratan/scala_scale_archive/main/scl/carlos_alpha.scl` (no rename; 18 cents-only steps ending `1404.00000`)
    - `pythagorean_12.scl` ← content from `pyth_12.scl` (rename per D-16; 12-tone Pythagorean)
    - `just_5limit.scl` ← content from `ji_12.scl` (rename per D-16; "Basic JI with 7-limit tritone")

    Content is byte-verbatim from the archive except for ONE line addition to the two renamed files: prepend a `! ORIGINAL ARCHIVE FILENAME: pyth_12.scl` (or `ji_12.scl`) line as the very first line of those two files so the rename audit trail is visible inside the file itself. The first existing line in the archive (`! pyth_12.scl` / `! ji_12.scl`) is preserved as the second line of the renamed file. Do not touch step values, ratios, cents, or step counts.

    Use `curl -fsSL <url> -o <path>` (curl 8.x confirmed available per RESEARCH §Environment Availability). If curl fails, fall back to `wget -q -O <path> <url>`. Verify each fetched file is not empty and contains the description line documented in RESEARCH §"Verified contents" (e.g. partch_43.scl contains `Harry Partch's 43-tone pure scale`).

    Total file size MUST stay under 100 KB combined (per SPEC constraint "5 in-repo fixtures + 3 negative-case fixtures total ≤ 100 KB"). Actual fixture sizes are < 5 KB each, so this budget is generous.
  </action>
  <verify>
    <automated>test -f flow-lang.Tests/fixtures/scala/partch_43.scl && test -f flow-lang.Tests/fixtures/scala/slendro.scl && test -f flow-lang.Tests/fixtures/scala/carlos_alpha.scl && test -f flow-lang.Tests/fixtures/scala/pythagorean_12.scl && test -f flow-lang.Tests/fixtures/scala/just_5limit.scl && grep -q "Harry Partch" flow-lang.Tests/fixtures/scala/partch_43.scl && grep -q "1404.00000" flow-lang.Tests/fixtures/scala/carlos_alpha.scl && grep -q "ORIGINAL ARCHIVE FILENAME: pyth_12.scl" flow-lang.Tests/fixtures/scala/pythagorean_12.scl && grep -q "ORIGINAL ARCHIVE FILENAME: ji_12.scl" flow-lang.Tests/fixtures/scala/just_5limit.scl</automated>
  </verify>
  <acceptance_criteria>
    - `flow-lang.Tests/fixtures/scala/partch_43.scl` exists; grep returns `Harry Partch's 43-tone pure scale`; tail of file contains `2/1` as last non-blank non-comment line
    - `flow-lang.Tests/fixtures/scala/slendro.scl` exists; grep returns `Observed Javanese Slendro scale`; declared step count `5`; final value `2/1`
    - `flow-lang.Tests/fixtures/scala/carlos_alpha.scl` exists; grep returns `Wendy Carlos' Alpha scale`; final value `1404.00000`; NO `2/1` line
    - `flow-lang.Tests/fixtures/scala/pythagorean_12.scl` first line is `! ORIGINAL ARCHIVE FILENAME: pyth_12.scl`; grep returns `12-tone Pythagorean scale`; final value `2/1`
    - `flow-lang.Tests/fixtures/scala/just_5limit.scl` first line is `! ORIGINAL ARCHIVE FILENAME: ji_12.scl`; grep returns `Basic JI with 7-limit tritone`; step 6 contains `7/5`
    - Combined size of the 5 files: `du -cb flow-lang.Tests/fixtures/scala/*.scl | tail -1` reports total < 102400 (100 KB)
  </acceptance_criteria>
  <done>5 fixtures committed with verified content; renames carry an in-file audit line; size budget met.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-author 3 malformed fixtures + write LICENSE.md</name>
  <files>
    flow-lang.Tests/fixtures/scala/malformed_step_count.scl,
    flow-lang.Tests/fixtures/scala/malformed_cents.scl,
    flow-lang.Tests/fixtures/scala/malformed_kbm.kbm,
    flow-lang.Tests/fixtures/scala/LICENSE.md
  </files>
  <read_first>
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (section "Wave 0 Gaps" — malformed fixture content hints; section "License attribution" — LICENSE.md template with mapping table; Pitfall 5 — rename audit trail)
    .planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md (D-17: softened community-use license wording)
    .planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md (Requirement 7: error format `{file}:{line}:{col} — {expected} got '{found}'`)
    flow-lang/Samples/CREDITS.md (Phase 29 in-repo attribution precedent)
  </read_first>
  <action>
    Author three minimal repro fixtures each isolating ONE class of error so SPEC-7 `ScalaParserErrorFacts` can assert per-error-class line:column messages without ambiguity:

    1. `malformed_step_count.scl` — first non-comment line after the description is a NEGATIVE integer (`-5`). Skeleton: a `! comment`, a `!` blank-comment, the description line `Malformed: negative step count`, then on line 4 the token `-5`, then a `!` and a `2/1` step value. The error will surface as `malformed_step_count.scl:4:1 — expected step count (positive integer), got '-5'` when the parser reads line 4.

    2. `malformed_cents.scl` — a structurally-valid header with step count `3` followed by step values where the SECOND step is a non-numeric token (`foo`). Skeleton: description `Malformed: non-numeric step`, count `3`, step 1 `100.0`, step 2 `foo`, step 3 `2/1`. Expected error: `malformed_cents.scl:N:1 — expected cents value or ratio, got 'foo'` where N is the line of `foo`.

    3. `malformed_kbm.kbm` — a structurally-valid header with reference frequency (field 6) set to a NEGATIVE value (`-50.0`). Lines: `! malformed kbm`, `0` (size), `0` (first MIDI), `127` (last MIDI), `60` (middle note), `69` (reference note), `-50.0` (reference frequency — the offender), `0` (formal octave). Expected error: `malformed_kbm.kbm:7:1 — expected reference frequency (positive Hz), got '-50.0'`.

    Place each error token at column 1 (no leading whitespace) so the column number in the expected error message is unambiguous and the parser column-tracking is testable.

    Then write `flow-lang.Tests/fixtures/scala/LICENSE.md` per D-17 softened-community-use wording. The file must contain:
    - Title: `# Scala Tuning Fixtures — Credits`
    - Attribution paragraph naming Manuel Op de Coul (coul@huygens-fokker.org) + Huygens-Fokker Foundation
    - The `Released for free use per the long-standing community understanding` wording (D-17)
    - A markdown table mapping each in-repo fixture to its archive original filename + source raw URL (5 rows; mark pyth_12.scl and ji_12.scl as "renamed for clarity; content verbatim")
    - Note that the 3 negative-case fixtures (`malformed_step_count.scl`, `malformed_cents.scl`, `malformed_kbm.kbm`) are hand-authored under Flow's own license

    Use the template laid out in RESEARCH §"License attribution (Phase 29 precedent)" as the structural reference (do not include code fences inside LICENSE.md output — it's a plain markdown doc).
  </action>
  <verify>
    <automated>test -f flow-lang.Tests/fixtures/scala/malformed_step_count.scl && test -f flow-lang.Tests/fixtures/scala/malformed_cents.scl && test -f flow-lang.Tests/fixtures/scala/malformed_kbm.kbm && test -f flow-lang.Tests/fixtures/scala/LICENSE.md && grep -q "Huygens-Fokker" flow-lang.Tests/fixtures/scala/LICENSE.md && grep -q "long-standing community understanding" flow-lang.Tests/fixtures/scala/LICENSE.md && grep -q "pyth_12.scl" flow-lang.Tests/fixtures/scala/LICENSE.md && grep -q "ji_12.scl" flow-lang.Tests/fixtures/scala/LICENSE.md && grep -q "^-5$" flow-lang.Tests/fixtures/scala/malformed_step_count.scl && grep -q "^foo$" flow-lang.Tests/fixtures/scala/malformed_cents.scl && grep -q "^-50.0$" flow-lang.Tests/fixtures/scala/malformed_kbm.kbm</automated>
  </verify>
  <acceptance_criteria>
    - `flow-lang.Tests/fixtures/scala/malformed_step_count.scl` contains the literal `-5` at column 1 of a line; grep `^-5$` matches
    - `flow-lang.Tests/fixtures/scala/malformed_cents.scl` contains the literal `foo` at column 1 of a step line; grep `^foo$` matches
    - `flow-lang.Tests/fixtures/scala/malformed_kbm.kbm` contains the literal `-50.0` at column 1 of the reference-frequency line; grep `^-50.0$` matches
    - `flow-lang.Tests/fixtures/scala/LICENSE.md` contains the strings `Huygens-Fokker`, `Manuel Op de Coul`, `long-standing community understanding`, `pyth_12.scl`, `ji_12.scl` (case-sensitive)
    - Combined size of 3 malformed fixtures + LICENSE.md: `du -cb flow-lang.Tests/fixtures/scala/malformed_*.scl flow-lang.Tests/fixtures/scala/malformed_kbm.kbm flow-lang.Tests/fixtures/scala/LICENSE.md | tail -1` < 20480 (20 KB)
  </acceptance_criteria>
  <done>3 malformed fixtures isolate one error class each; LICENSE.md attributes Huygens-Fokker + documents the 2 renames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→test-suite | Fixture content is consumed verbatim by the parser at test time; tampering would surface as parser test failures (acceptance asserts numeric values, not file bytes — covered by Plan 32-02) |
| supply-chain→repo | Initial fetch happens once during Wave 0 from a GitHub mirror raw URL; once committed, content is pinned in git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-FIX-01 | Tampering | `flow-lang.Tests/fixtures/scala/*.scl` content drift across commits | mitigate | Acceptance tests in plans 32-02 / 32-04 / 32-06 assert numeric values (step counts, period cents, specific ratios like `1404.00000`, `7/5`) — tampering surfaces immediately. No silent dependency on byte hashes; integrity is structural. |
| T-32-FIX-02 | Information Disclosure | LICENSE.md misattribution (failure to credit Huygens-Fokker / Op de Coul) | mitigate | LICENSE.md acceptance criteria explicitly require strings `Huygens-Fokker`, `Manuel Op de Coul`, source URLs in the rename mapping table. |
| T-32-FIX-03 | Repudiation | Filename rename audit trail | mitigate | Original archive filenames (`pyth_12.scl`, `ji_12.scl`) appear BOTH in LICENSE.md table AND as the first `!` comment line inside each renamed file. Future readers can verify content provenance against the upstream archive without consulting LICENSE.md. |
</threat_model>

<verification>
- 5 canonical fixtures + 3 malformed fixtures + LICENSE.md exist
- LICENSE.md cites Huygens-Fokker + documents both renames
- Renamed fixtures carry an in-file `! ORIGINAL ARCHIVE FILENAME: ...` audit line as their first line
- Total fixture directory size < 120 KB (well under SPEC's 100 KB excluding LICENSE.md)
- No code touched — Wave 0 is pure data + attribution
</verification>

<success_criteria>
- `flow-lang.Tests/fixtures/scala/` contains exactly 9 files: 5 canonical .scl + 3 malformed + 1 LICENSE.md
- Every canonical fixture's description line and final-step value match RESEARCH §"Verified contents"
- Every malformed fixture isolates ONE error class at column 1 for unambiguous line:col assertion in Plan 32-02
- LICENSE.md follows D-17 softened-community-use wording
</success_criteria>

<output>
After completion, create `.planning/phases/32-full-scala-scl-tuning-loader/32-01-SUMMARY.md`.
</output>
