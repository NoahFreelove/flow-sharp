---
phase: 32
plan: 06
type: execute
wave: 3
depends_on: [32-04, 32-05]
files_modified:
  - flow-lang/Lexing/TokenType.cs
  - flow-lang/Lexing/SimpleLexer.cs
  - flow-lang/Ast/Statements/TuningContextStatement.cs
  - flow-lang/Parsing/Parser.cs
  - flow-lang/Interpreter/Interpreter.cs
  - flow-lang.Tests/Unit/Phase32/TuningContextStatementFacts.cs
  - flow-lang.Tests/Integration/Phase32/LastWinsTuningTests.cs
  - flow-lang.Tests/Integration/Phase32/ScalaTuningDeterminismTests.cs
autonomous: true
requirements: [SPEC-2, SPEC-6]
must_haves:
  truths:
    - "`tuning` is a reserved keyword token; lexer recognizes it; parser dispatches to ParseTuningContextStatement"
    - "Three composer surfaces for `tuning <expr> { ... }` all parse: identifier, inline `(loadScala \"x\")`, string-literal sugar `tuning \"x.scl\" { ... }`"
    - "TuningContextStatement is a `record` extending Statement (AST conventions per CLAUDE.md)"
    - "Interpreter push/pop pairs the TuningStack via try/finally per ExecuteMusicalContext pattern"
    - "Last-wins: `enable justIntonation; tuning partch { section a {...} } section b {...}` renders section a under Partch, section b under JI"
    - "Two-run determinism: same .flow + same .scl + same git SHA -> byte-identical WAV (SPEC-6 constraint)"
    - "String-literal sugar preserves source location: desugared FunctionCallExpression carries the SourceLocation of the user's typed `tuning \"x.scl\"` line, not a synthetic frame (T-32-AST mitigation)"
  artifacts:
    - path: "flow-lang/Ast/Statements/TuningContextStatement.cs"
      provides: "record TuningContextStatement(SourceLocation, Expression TuningExpr, IReadOnlyList Body)"
      contains: "public record TuningContextStatement"
    - path: "flow-lang.Tests/Unit/Phase32/TuningContextStatementFacts.cs"
      provides: "Parser+AST Facts for all 3 D-15 expression forms"
      contains: "public class TuningContextStatementFacts"
    - path: "flow-lang.Tests/Integration/Phase32/LastWinsTuningTests.cs"
      provides: "SPEC-6 last-wins pragma+block interaction"
      contains: "public class LastWinsTuningTests"
    - path: "flow-lang.Tests/Integration/Phase32/ScalaTuningDeterminismTests.cs"
      provides: "SPEC-6 two-run byte-identical WAV gate"
      contains: "public class ScalaTuningDeterminismTests"
  key_links:
    - from: "flow-lang/Parsing/Parser.cs"
      to: "flow-lang/Ast/Statements/TuningContextStatement.cs"
      via: "ParseTuningContextStatement produces TuningContextStatement; string-literal form desugars to (loadScala) FunctionCallExpression per D-15"
      pattern: "TuningContextStatement|loadScala"
    - from: "flow-lang/Interpreter/Interpreter.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "ExecuteTuningContext calls PushTuning/PopTuning via try/finally"
      pattern: "PushTuning|PopTuning"
---

<objective>
Land the composer-facing `tuning <expr> { ... }` musical-context block. Lexer recognizes the `tuning` keyword (Pitfall 9 — fully reserved per Claude's Discretion). Parser dispatches a new AST node `TuningContextStatement` per D-13. Interpreter executes via push/pop on the TuningStack (Plan 32-05's API). Three composer surface forms parse per D-15: identifier, inline call, string-literal sugar.

Purpose: this is SPEC-2 ("`tuning t { section ... }` musical-context block applies a Tuning") landing in code. Builds the bridge from .flow source -> ExecutionContext.PushTuning. The two-run byte-identical determinism gate (SPEC-6 constraint, Pitfall 8) ships here as a dedicated test class — once the AST + interpreter wire push/pop correctly AND the parser is deterministic AND ResolvedTuning's MidiToHz math is deterministic, the determinism Fact passes.

Output: lexer keyword + AST record + parser dispatch + interpreter case + 3 test classes covering AST parsing, last-wins interaction, two-run determinism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md
@flow-lang/Lexing/TokenType.cs
@flow-lang/Lexing/SimpleLexer.cs
@flow-lang/Ast/Statements/MusicalContextStatement.cs
@flow-lang/Parsing/Parser.cs
@flow-lang/Interpreter/Interpreter.cs
@flow-lang.Tests/Integration/Phase23/TuningDeterminismTests.cs

<interfaces>
New token type: `TokenType.Tuning` (added to the enum, after `VoicePool` per RESEARCH §"Codebase Integration Map > Parser dispatch")

Lexer keyword table: add `"tuning" => TokenType.Tuning` (SimpleLexer.cs lines 855-887 keyword table)

New AST node: `flow-lang/Ast/Statements/TuningContextStatement.cs` — `public record TuningContextStatement(SourceLocation Location, Expression TuningExpr, IReadOnlyList<Statement> Body) : Statement(Location);` (parallel shape to MusicalContextStatement.cs:14-20 per D-13)

Parser dispatch: `Parser.cs` musical-context dispatch table (lines 102-153) adds a 12th case for `TokenType.Tuning` -> `ParseTuningContextStatement()`. This method:
1. Consumes `Tuning` token; captures `tuningTokenLocation` from the consumed token's SourceLocation (used as the desugar's anchor location per T-32-AST mitigation)
2. If next token is `StringLiteral`: desugar to `FunctionCallExpression(loadScala, [stringLit])` per D-15 string-literal sugar — the synthetic FunctionCallExpression's SourceLocation MUST be `tuningTokenLocation` (NOT a synthetic frame), so runtime errors point at the user's `tuning "x.scl"` line
3. Else: call `ParseExpression()` to capture identifier or inline call form
4. Expect `{`, parse body statements until `}`, return `TuningContextStatement(location, tuningExpr, body)`

Interpreter dispatch: `Interpreter.cs:97-131` `ExecuteStatement` switch adds case `TuningContextStatement tctx => ExecuteTuningContext(tctx)`. The `ExecuteTuningContext` method:
1. Evaluate `tctx.TuningExpr` -> expect a Value with Type == TuningType.Instance
2. Extract underlying ResolvedTuning, wrap into RenderTuning (with Custom set, keeping system defaults from current ActiveTuning's tonic/mode/letter)
3. `_context.PushTuning(rt)` (the API Plan 32-05 added)
4. try { execute each statement in tctx.Body } finally { _context.PopTuning(); }
5. Per D-14: if exception escapes during body, the finally clause still pops. Mirrors `ExecuteMusicalContext` try/finally shape at lines 137-322.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lexer keyword + AST record + Parser dispatch for `tuning ... { ... }`</name>
  <files>
    flow-lang/Lexing/TokenType.cs,
    flow-lang/Lexing/SimpleLexer.cs,
    flow-lang/Ast/Statements/TuningContextStatement.cs,
    flow-lang/Parsing/Parser.cs,
    flow-lang.Tests/Unit/Phase32/TuningContextStatementFacts.cs
  </files>
  <read_first>
    flow-lang/Lexing/TokenType.cs (lines 1-50 — locate VoicePool entry; add Tuning after it)
    flow-lang/Lexing/SimpleLexer.cs (lines 855-887 — keyword recognition table)
    flow-lang/Ast/Statements/MusicalContextStatement.cs (lines 1-25 — record shape to mirror per D-13)
    flow-lang/Parsing/Parser.cs (lines 100-160 — musical-context dispatch; lines 240-260 — keyword-as-proc-name allowlist at line 247 per RESEARCH)
    flow-lang/Parsing/Parser.cs (find ParseMusicalContextStatement implementation — read body for shape pattern)
    .planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md (D-13 parallel AST node rationale; D-15 three expression forms; Claude's Discretion: tuning fully reserved, NOT added to allowlist at line 247)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pitfall 9 — keyword shadow grep)
  </read_first>
  <behavior>
    - Source `tuning partch { (print "inside") }` lexes to tokens including `Tuning`, identifier `partch`, `LBrace`, ... , `RBrace`
    - Source `tuning partch { }` parses to a `TuningContextStatement` whose TuningExpr is a `VariableExpression("partch")` and Body is empty
    - Source `tuning (loadScala "x.scl") { }` parses to a TuningContextStatement whose TuningExpr is a `FunctionCallExpression` for loadScala
    - Source `tuning "x.scl" { }` parses to a TuningContextStatement whose TuningExpr is desugared to `(loadScala "x.scl")` — i.e. a FunctionCallExpression with name `loadScala` and one String arg `"x.scl"` (D-15 string-literal sugar)
    - **T-32-AST source-location preservation:** the desugared FunctionCallExpression's `SourceLocation.Line` matches the line of the original `tuning` keyword in the source — NOT a synthetic frame. Verified by asserting `desugared.Location.Line == tuningKeywordLine` in a dedicated Fact, and by a negative-path Fact that produces a runtime error from the desugar (e.g. nonexistent .scl path) and asserts the error message references the user's tuning line, not an internal synthetic frame.
    - Source `tuning partch { (print "inner") } (print "outer")` parses with the inner print INSIDE Body and the outer print as a sibling statement (block scoping respected)
    - Grep audit: `rg "\btuning\b" --type-add 'flow:*.flow' -t flow` (or equivalent recursive grep over `.flow` files in repo) shows zero pre-existing user identifier usages OR the existing usages are documented in this plan's summary for renaming (Pitfall 9 mitigation)
    - Negative case: `tuning { }` (missing expression after `tuning`) raises a parser error
  </behavior>
  <action>
    Run the Pitfall 9 grep audit FIRST:
    ```
    grep -rn "\btuning\b" --include="*.flow" examples/ tests/ flow-lang/ flow-lang.Tests/ 2>/dev/null
    ```
    Document findings inline in this plan's eventual 32-06-SUMMARY.md. If any `.flow` file uses `tuning` as an identifier (variable/proc name), rename it BEFORE the keyword lands to avoid a wave of parser errors mid-task. If no usages found, proceed.

    Modify `flow-lang/Lexing/TokenType.cs`: add `Tuning` to the enum after `VoicePool` (the position matters less than the name; just keep alphabetical ordering consistent with what's already there if the enum is alphabetized; otherwise append at the end of the musical-context block of tokens).

    Modify `flow-lang/Lexing/SimpleLexer.cs:855-887`: add an entry `"tuning" => TokenType.Tuning` to the keyword table. The lexer recognizes the keyword at column boundaries — same shape as `"tempo"` / `"timesig"` / `"voicePool"` already there.

    Per Claude's Discretion: do NOT add `Tuning` to the keyword-as-proc-name allowlist at `Parser.cs:247`. The keyword is fully reserved per SPEC line 139 pre-public lean.

    Create `flow-lang/Ast/Statements/TuningContextStatement.cs`:
    File-scoped namespace `FlowLang.Ast.Statements`. Single record:
    `public record TuningContextStatement(SourceLocation Location, Expression TuningExpr, IReadOnlyList<Statement> Body) : Statement(Location);`
    Mirror the constructor and inheritance pattern of `MusicalContextStatement.cs:14-20` — read that file first to copy the exact base-class constructor call pattern.

    Modify `flow-lang/Parsing/Parser.cs:102-153` (musical-context dispatch):
    Add a 12th case in the dispatch table for `TokenType.Tuning` -> call `ParseTuningContextStatement()`. Implement `ParseTuningContextStatement` as a new private method:
    1. Record start `SourceLocation` from the current token position (before consuming `Tuning`). This becomes both the `TuningContextStatement.Location` AND the anchor for any synthetic FunctionCallExpression created in step 3 (T-32-AST source-location-preservation requirement).
    2. Consume the `Tuning` keyword token
    3. Peek the next token:
       - If `TokenType.StringLiteral`: consume it; construct a desugared `FunctionCallExpression` whose function name is the identifier `loadScala` and whose single argument is a `LiteralExpression(stringValue)`. **The desugared FunctionCallExpression's SourceLocation MUST be the start location captured in step 1 (the `tuning` keyword's location), NOT `SourceLocation.None` or a synthetic frame** — so runtime errors thrown during the desugared call (e.g. file-not-found) surface at the user's `tuning "x.scl"` line. The desugar happens at parse time per D-15 — the same parse-time-transform pattern as the flow operator `->` (read Parser.cs to see how `->` desugars FunctionCallExpression; note its location-propagation pattern and mirror it here).
       - Else: call `ParseExpression()` to capture the identifier or inline-call form. ParseExpression handles `partch` as a VariableExpression and `(loadScala "x.scl")` as a FunctionCallExpression.
    4. Expect and consume `{` (`TokenType.LBrace`); on missing brace raise a parser error per existing parser conventions.
    5. Parse body statements until `}` is encountered. Use the same body-parsing loop pattern as `ParseMusicalContextStatement` (read that method's body for the exact loop structure — likely something like `while (!Check(TokenType.RBrace)) body.Add(ParseStatement())`).
    6. Expect and consume `}`.
    7. Return `new TuningContextStatement(startLocation, tuningExpr, body.ToList())`.

    Write `flow-lang.Tests/Unit/Phase32/TuningContextStatementFacts.cs` with parser-level Facts (operate at the AST shape level, not interpreter execution). Test harness: invoke the lexer + parser directly (read Phase 23 parser tests to find the pattern — likely `Lexer.Tokenize(source)` followed by `new Parser(tokens).Parse()`). Facts:
    1. `Parse_TuningWithIdentifier_ProducesVariableExpression` — `tuning partch { }` produces TuningContextStatement whose TuningExpr is VariableExpression("partch")
    2. `Parse_TuningWithInlineCall_ProducesFunctionCallExpression` — `tuning (loadScala "x.scl") { }` produces TuningContextStatement whose TuningExpr is FunctionCallExpression named loadScala
    3. `Parse_TuningWithStringLiteral_DesugarsToLoadScalaCall` — `tuning "x.scl" { }` produces TuningContextStatement whose TuningExpr is FunctionCallExpression named loadScala with one String arg "x.scl" (D-15 string sugar verified)
    4. `Parse_TuningStringLiteralDesugar_PreservesSourceLocation` — T-32-AST Fact. Source `\n\ntuning "x.scl" { }\n` (tuning on line 3); assert `desugaredCall.Location.Line == 3` (the user's tuning line, NOT line 1 or a synthetic 0). Also assert the LiteralExpression for `"x.scl"` carries the same or adjacent line.
    5. `Parse_TuningWithBody_CollectsBodyStatements` — `tuning partch { Int x = 5 }` produces TuningContextStatement with Body.Count == 1 (a VariableDeclaration)
    6. `Parse_NestedTuningInsideTempo_BothNodesPresent` — `tempo 120 { tuning partch { } }` parses to a MusicalContextStatement(Tempo) with Body containing one TuningContextStatement (verifies block nesting)
    7. `Parse_TuningWithoutExpr_RaisesError` — `tuning { }` throws a parser exception (missing expression)

    Total: ≥ 7 Facts. File-scoped namespace `FlowLangTests.Unit.Phase32`.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "ClassName~TuningContextStatementFacts" -v minimal &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase23" --no-build -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Tuning' flow-lang/Lexing/TokenType.cs` returns ≥ 1 match for the new enum entry
    - `grep -n '"tuning"' flow-lang/Lexing/SimpleLexer.cs` returns ≥ 1 match (keyword recognition)
    - `flow-lang/Ast/Statements/TuningContextStatement.cs` contains `public record TuningContextStatement` AND `Expression TuningExpr` AND `IReadOnlyList<Statement> Body` (all 3 via grep)
    - `grep -n 'ParseTuningContextStatement\|TuningContextStatement' flow-lang/Parsing/Parser.cs` returns ≥ 2 matches (dispatch + construction)
    - Pitfall 9 grep audit recorded — either zero pre-existing usages OR documented renames in plan summary
    - `dotnet test --filter "ClassName~TuningContextStatementFacts" -v minimal` exits 0; reports ≥ 7 Facts passed
    - T-32-AST Fact `Parse_TuningStringLiteralDesugar_PreservesSourceLocation` passes (desugar carries user's `tuning` line, not synthetic frame)
    - Phase 23 regression sweep `dotnet test --filter "FullyQualifiedName~Phase23" --no-build -v minimal` exits 0
  </acceptance_criteria>
  <done>tuning keyword reserved; AST node + parser dispatch parse all 3 D-15 forms with preserved source locations; Phase 23 sub-suite still GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interpreter ExecuteTuningContext + LastWinsTuningTests integration</name>
  <files>
    flow-lang/Interpreter/Interpreter.cs,
    flow-lang.Tests/Integration/Phase32/LastWinsTuningTests.cs
  </files>
  <read_first>
    flow-lang/Interpreter/Interpreter.cs (lines 97-131 ExecuteStatement switch; lines 135-323 ExecuteMusicalContext implementation — the try/finally shape Phase 32's new method mirrors)
    flow-lang/Runtime/ExecutionContext.cs (post-Plan-32-05 PushTuning/PopTuning API)
    flow-lang/Ast/Statements/TuningContextStatement.cs (Task 1 product)
    flow-lang/StandardLibrary/Audio/Tuning/ResolvedTuning.cs (Plan 32-03 product)
    flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs (Plan 32-03 extension with Custom field)
    flow-lang/TypeSystem/SpecialTypes/TuningType.cs (Plan 32-03 product; for type check on evaluated TuningExpr)
    flow-lang.Tests/Integration/Phase23/TuningDeterminismTests.cs (Pattern D + Pattern E reference for FlowEngineRunner+ResetForTesting test structure)
    flow-lang.Tests/Unit/Phase23/WriteMidiWarningFacts.cs (last-wins pattern reference)
    flow-lang.Tests/Helpers/Phase29Fft.cs (FFT-based dominant-frequency extraction used by Fact 3 — see usage in Phase 29 sampled-instrument tests)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pattern E — last-wins test pattern at line ~554; section "Codebase Integration Map > Interpreter dispatch")
  </read_first>
  <behavior>
    - Source `Tuning t = (loadScala "...partch_43.scl") tuning t { section a { ... } }` executes section a with ActiveTuning.Custom == partch ResolvedTuning
    - After the `tuning t { ... }` block closes, ActiveTuning reverts to whatever was active before (file-scope pragma frame OR Default)
    - `enable justIntonation; tuning partch { section a { ... } } section b { ... }` — section a renders under Partch (Custom != null), section b renders under JI (Custom == null AND System == JI). Verified by rendering both sections to separate WAVs and asserting the BYTES DIFFER.
    - If `tuning` body throws an exception, PopTuning still fires (try/finally guarantee per D-14 graceful unwinding)
    - REPL eval boundary: a script with an unclosed `tuning partch {` (artificial — would normally be a parser error) is academic since the parser requires the closing brace. The real REPL test is: a script `tuning partch { section a { ... } }` completes; subsequent eval in the same engine has ActiveTuning == Default (block popped per D-14).
  </behavior>
  <action>
    Modify `flow-lang/Interpreter/Interpreter.cs:97-131` (ExecuteStatement switch): add a new case that dispatches `TuningContextStatement tctx` to a private `ExecuteTuningContext(tctx)` method. (Executor reads the file and applies the equivalent C# addition in the switch — same shape as the existing `MusicalContextStatement` arm.)

    Implement `ExecuteTuningContext(TuningContextStatement tctx)` as a new private method paralleling `ExecuteMusicalContext` at lines 135-323. Algorithm:
    1. Evaluate `tctx.TuningExpr` via the existing expression evaluator (`_evaluator.Evaluate(tctx.TuningExpr)` or however the established API name is — read Interpreter.cs to find).
    2. Verify the resulting Value has `Type == TuningType.Instance`; if not, raise a runtime error `"tuning block expects a Tuning value, got <typeName>"` (mirror Flow's existing type-mismatch error style — look at how ExecuteMusicalContext checks types for tempo / timesig / etc.).
    3. Extract underlying ResolvedTuning via `(ResolvedTuning)value.RawValue` (or whatever accessor Value.cs exposes — Plan 32-04 Task 1 added `Value.Tuning(ResolvedTuning)` factory; mirror that on the unwrap path).
    4. Construct the RenderTuning to push. Per Plan 32-03 Pitfall 3 mutual-exclusion: when Custom is set, the System/Mode/Tonic fields are effectively ignored by PitchConversion. So just construct `new RenderTuning(TuningSystem.EqualTemperament, Mode.Major, 'C', 0, custom: resolved)` — the wedge fields are placeholders. (Alternative: inherit tonic/mode/letter from current ActiveTuning so that if PitchConversion ever needs them for something the Custom branch doesn't override, they remain consistent. Either path works; pick the simpler one — fixed placeholders — since the Custom-takes-priority contract is now defense-in-depth-asserted in Plan 32-03 Task 2.)
    5. Call `_context.PushTuning(renderTuning)` (the API Plan 32-05 added).
    6. try { foreach (var stmt in tctx.Body) ExecuteStatement(stmt); } finally { _context.PopTuning(); }

    This is the entry point that ties together Plans 32-03 (RenderTuning.Custom + ResolvedTuning), 32-04 (Value wrapping + builtin), and 32-05 (PushTuning/PopTuning API).

    Write `flow-lang.Tests/Integration/Phase32/LastWinsTuningTests.cs`. xUnit + `[Collection("FlowScripts")]` + FlowEngineRunner per Pattern E (RESEARCH §"Pattern E"). Mandatory `RenderingDiagnostics.ResetForTesting()` in test class ctor/Dispose per Pattern D guidance.

    Facts:
    1. `TuningBlock_BodyExecutesUnderCustomTuning` — RunSource with a script that loads partch_43, opens `tuning t { section a { ... } }`, renders section a to WAV. Run the SAME source but with the body's section a OUTSIDE the tuning block (default 12-TET). Assert the two WAVs have DIFFERENT bytes (`!File.ReadAllBytes(wavA).SequenceEqual(File.ReadAllBytes(wavB))`). Proves the tuning block actually changes the render.

    2. `LastWins_JIPragmaWithPartchBlock_InsideOutsideDiffer` — the SPEC-6 acceptance Fact. RunSource with `enable justIntonation; Tuning t = (loadScala "...partch_43.scl") tempo 120 { tuning t { section a { Sequence m = | C4q D4q | } } section b { Sequence n = | C4q D4q | } } Song song = [a b] Buffer buf = (renderSong song "sine") (writeWav "/tmp/p32_lastwins.wav" buf)`. Then re-render two variants in isolation: variant A renders ONLY section a from this script (force Partch inside the block); variant B renders ONLY section b (force JI outside the block). Compare the two WAVs byte-for-byte: must differ. Bonus: assert variant A and variant B both differ from a pure 12-TET render of the same notes (no pragma, no tuning block).

    3. `TuningBlock_AfterClose_ActiveTuningReverts` — **commit to the Phase29Fft path** (the checker explicitly rejected the speculative `_context.ActiveTuning` test-hatch option because ExecutionContext is not part of the public FlowEngineRunner API). Approach:
       - RunSource: `enable justIntonation; Tuning t = (loadScala "...partch_43.scl") tuning t { } (writeWav "/tmp/p32_after_close_ji.wav" (renderSong [section z { Sequence m = | C4w | }] "sine"))`. The tuning block is empty; immediately after it closes, a C4 whole-note is rendered. The JI file-scope pragma should be active again (Pitfall 2 — pragmas sticky; blocks ephemeral per D-14).
       - For the baseline, RunSource (separate engine, ResetForTesting between) the SAME script but with `enable justIntonation;` and NO tuning block at all — pure JI render of the same C4 whole-note to `/tmp/p32_baseline_ji.wav`.
       - Run Phase29Fft's dominant-frequency extraction on both WAVs (read flow-lang.Tests/Helpers/Phase29Fft.cs to find the helper signature; the established pattern in Phase 29 sampled-instrument tests is something like `Phase29Fft.DominantFrequency(File.ReadAllBytes(wavPath), sampleRate: 44100)`).
       - Assert `Math.Abs(afterCloseHz - baselineJiHz) < 0.5` Hz. Both renders should produce the SAME dominant frequency because both render C4 under JI; if the tuning block had failed to pop, afterCloseHz would have stayed at Partch's C4 frequency, which is detectably different from JI's C4 frequency.
       - Bonus assertion (sanity check): a THIRD render under Partch (without the after-close revert — just plain `tuning t { ... C4w ... }`) produces a dominant freq distinguishably different from `baselineJiHz`. This proves the test's discrimination power.

       This Fact piggybacks on existing test infrastructure (Phase29Fft helper, FlowEngineRunner, WAV I/O) and does NOT require a new test-hatch on FlowEngineRunner or ExecutionContext.

    4. `TuningBlock_BodyThrows_StackStillPops` — synthesize a body that throws (e.g. a divide-by-zero or any controlled runtime error). Catch the runtime error in the test; after the failed run, render a follow-up C4 note in a SEPARATE engine eval (within the same FlowEngineRunner instance if it persists across RunSource calls, or via a second-render-inside-the-same-script-after-a-`try`-catch — pick whichever fits the FlowEngineRunner API best). The follow-up C4's dominant freq via Phase29Fft should match the pre-block state (e.g. Default 12-TET or whatever pragma frame was active), proving PopTuning fired via finally despite the exception.

    Total: ≥ 4 Facts. File-scoped namespace `FlowLangTests.Integration.Phase32`.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "ClassName~LastWinsTuningTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'TuningContextStatement\|ExecuteTuningContext' flow-lang/Interpreter/Interpreter.cs` returns ≥ 2 matches
    - `grep -n 'PushTuning\|PopTuning' flow-lang/Interpreter/Interpreter.cs` returns ≥ 2 matches (push + finally pop)
    - `grep -n 'try\|finally' flow-lang/Interpreter/Interpreter.cs | head` shows the ExecuteTuningContext method uses try/finally (defensive unwinding per D-14)
    - `grep -n 'Phase29Fft' flow-lang.Tests/Integration/Phase32/LastWinsTuningTests.cs` returns ≥ 1 match (Fact 3 commits to Phase29Fft path — NOT to `_context.ActiveTuning` test-hatch)
    - `dotnet test --filter "ClassName~LastWinsTuningTests" -v minimal` exits 0; reports ≥ 4 Facts passed
    - SPEC-6 last-wins acceptance Fact `LastWins_JIPragmaWithPartchBlock_InsideOutsideDiffer` passes (Partch-bytes != JI-bytes)
    - Phase 23 sub-suite stays GREEN
  </acceptance_criteria>
  <done>tuning block executes; last-wins pragma-vs-block interaction proven via WAV byte-differ; revert-after-close proven via Phase29Fft frequency comparison; exception unwind preserves stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ScalaTuningDeterminismTests — two-run byte-identical determinism gate</name>
  <files>
    flow-lang.Tests/Integration/Phase32/ScalaTuningDeterminismTests.cs
  </files>
  <read_first>
    flow-lang.Tests/Integration/Phase23/TuningDeterminismTests.cs (Pattern D template — two FlowEngineRunner instances; ResetForTesting between; File.ReadAllBytes + SequenceEqual)
    flow-lang/Diagnostics/RenderingDiagnostics.cs (ResetForTesting hatch)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pattern D — byte-identical determinism test; Pitfall 8 — two-run-cmp-clean contract)
    .planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md (constraint "Two-run byte-identical determinism: same script + same `.scl`/`.kbm` files + same git SHA → byte-identical WAV/MID")
  </read_first>
  <behavior>
    For each canonical fixture (partch_43, slendro, carlos_alpha, pythagorean_12, just_5limit):
    - Build a small .flow source string that loads the fixture, opens a tuning block, renders a short sequence (e.g. `| C4q D4q E4q F4q |`) to WAV at a deterministic path
    - Run the same source twice (two distinct FlowEngineRunner instances; ResetForTesting between)
    - Assert `File.ReadAllBytes(wavRun1).SequenceEqual(File.ReadAllBytes(wavRun2))` — bytes match exactly

    Also two-run for MIDI export (writeMidi):
    - Same shape, but `(writeMidi "/tmp/p32_det_partch.mid" ...)` -- MIDI export under custom tuning should ALSO produce identical bytes across runs (12-TET pitches per Phase 23 D-13 advisory, but the conductor track + tempo + note ordering should be deterministic)
  </behavior>
  <action>
    Write `flow-lang.Tests/Integration/Phase32/ScalaTuningDeterminismTests.cs` mirroring Pattern D (`flow-lang.Tests/Integration/Phase23/TuningDeterminismTests.cs:36-110`):

    Structure:
    - xUnit class with `[Collection("FlowScripts")]` attribute
    - Constructor calls `RenderingDiagnostics.ResetForTesting()`
    - `IDisposable.Dispose()` (if class implements IDisposable) also calls ResetForTesting
    - Each Fact:
      1. Constructs a single source string (with the .scl path and the output WAV path baked in)
      2. Creates a fresh FlowEngineRunner; runs source; captures `ok` + the output WAV bytes
      3. Calls `RenderingDiagnostics.ResetForTesting()` between runs
      4. Creates a SECOND fresh FlowEngineRunner; runs the SAME source; captures the output WAV bytes
      5. Asserts both runs returned `ok == true`
      6. Asserts `bytes1.SequenceEqual(bytes2)` via `Assert.True` with a helpful message including the first byte index where they diverge

    Facts (≥ 6):
    1. `Determinism_Partch43_WavBytesIdenticalAcrossRuns`
    2. `Determinism_CarlosAlpha_WavBytesIdenticalAcrossRuns` (non-octave path — the math-heavy case)
    3. `Determinism_Slendro_WavBytesIdenticalAcrossRuns`
    4. `Determinism_Pythagorean12_WavBytesIdenticalAcrossRuns`
    5. `Determinism_Just5Limit_WavBytesIdenticalAcrossRuns`
    6. `Determinism_PartchMidiExport_BytesIdenticalAcrossRuns` (writeMidi version — same fixture; asserts MID bytes match; verifies the D-13 advisory firing doesn't introduce ordering non-determinism)

    Per Pitfall 8, the most likely failure modes are Dictionary iteration order and locale-dependent parsing — Plan 32-02's `InvariantCulture` grep gate and Plan 32-03's eager-precompute-at-construction-time pattern both already mitigate these. This test class catches any regression in the determinism contract.

    File-scoped namespace `FlowLangTests.Integration.Phase32`.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "ClassName~ScalaTuningDeterminismTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "ClassName~ScalaTuningDeterminismTests" -v minimal` exits 0; reports ≥ 6 Facts passed
    - `grep -n 'ResetForTesting' flow-lang.Tests/Integration/Phase32/ScalaTuningDeterminismTests.cs` returns ≥ 2 matches (constructor + dispose OR before second run)
    - `grep -n 'SequenceEqual' flow-lang.Tests/Integration/Phase32/ScalaTuningDeterminismTests.cs` returns ≥ 6 matches (one per Fact byte comparison)
    - SPEC-6 two-run determinism gate (Pitfall 8) verified for all 5 canonical fixtures + 1 MIDI export case
  </acceptance_criteria>
  <done>Two-run byte-identical determinism contract demonstrably true across all 5 canonical fixtures + writeMidi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-source→parser | Composer-typed `.flow` source crosses the lexer/parser boundary. String-literal sugar (`tuning "x.scl" { }`) desugars at parse time into a synthetic `FunctionCallExpression(loadScala, …)`. The synthetic node carries a SourceLocation that affects runtime error messages and stack frame reporting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-AST | Tampering | `Parser.ParseTuningContextStatement` string-literal-sugar desugar path produces a synthetic `FunctionCallExpression` whose `SourceLocation` defaults to `SourceLocation.None` or a parser-internal frame instead of the user's typed `tuning` line. A runtime error during the desugared call (e.g. file-not-found from `(loadScala "missing.scl")`) would then surface an error message pointing at a synthetic location, misleading the composer about which source line caused the failure. | mitigate | (1) Capture the SourceLocation of the `Tuning` keyword token BEFORE consuming it in `ParseTuningContextStatement` step 1. (2) When the string-literal sugar path constructs the synthetic `FunctionCallExpression`, set its `SourceLocation` to that captured location (NOT `SourceLocation.None`, NOT a synthetic frame). (3) Dedicated unit Fact `Parse_TuningStringLiteralDesugar_PreservesSourceLocation` asserts `desugaredCall.Location.Line` matches the user's `tuning` line — verified at AST inspection layer, no execution needed. (4) Negative integration Fact: a runtime error from a failed string-literal-form tuning block (nonexistent .scl path) produces an error message referencing the user's `tuning` line, NOT a synthetic frame — verified by substring match against the stderr output. |
| T-32-AST-02 | Tampering | `Parser.ParseTuningContextStatement` body parsing loop accepts statements that themselves push musical contexts (e.g. nested `tempo`/`timesig`/`tuning` inside the body). If the inner block fails to pop on exception, the outer `tuning` block's `try/finally` in the interpreter MUST still pop its own frame, OR the TuningStack ends up in an inconsistent state visible to subsequent renders. | mitigate | `ExecuteTuningContext` wraps the body in `try { foreach (var stmt in tctx.Body) ExecuteStatement(stmt); } finally { _context.PopTuning(); }` per D-14. Inner blocks' own try/finally clauses handle their own frames independently. Verified by Fact `TuningBlock_BodyThrows_StackStillPops` (Task 2 Fact 4) — renders a post-exception note via FlowEngineRunner + Phase29Fft to confirm the stack reverted to pre-block state. |
| T-32-AST-03 | Information Disclosure | Parser error messages for invalid `tuning ... { ... }` syntax include source context (file:line:col + a snippet of nearby tokens). For composer-authored scripts the source is the composer's own file — no PII risk. | accept | Inherits existing parser error reporting posture. No additional sanitization needed. |
</threat_model>

<verification>
- TuningContextStatementFacts ≥ 7 Facts GREEN including T-32-AST source-location-preservation Fact
- LastWinsTuningTests ≥ 4 Facts GREEN including SPEC-6 last-wins acceptance AND the Phase29Fft-based revert-after-close Fact
- ScalaTuningDeterminismTests ≥ 6 Facts GREEN (SPEC-6 two-run determinism constraint)
- Phase 23 sub-suite 100% GREEN
- `tuning` keyword reserved, lexer recognizes, parser dispatches, interpreter executes
- All 3 D-15 expression forms (identifier / inline call / string-literal sugar) parse correctly
- String-literal desugar preserves user source location (T-32-AST mitigation)
- Pitfall 9 keyword-shadow grep documented in plan summary
</verification>

<success_criteria>
- SPEC-2 acceptance: `tuning t { section ... }` parses + executes + last-wins pragma interaction proven
- SPEC-6 acceptance: 5 canonical fixtures + writeMidi all two-run byte-identical
- AST conventions per CLAUDE.md respected: TuningContextStatement is a `record`; file-scoped namespace; switch-dispatch in interpreter
- Phase 23 sub-suite unchanged
- ExecuteTuningContext push/pop pairs via try/finally per D-14
- T-32-AST mitigated: string-literal sugar's desugared FunctionCallExpression preserves the user's `tuning` line in its SourceLocation
</success_criteria>

<output>
After completion, create `.planning/phases/32-full-scala-scl-tuning-loader/32-06-SUMMARY.md`. Document the Pitfall 9 grep audit findings + any keyword-shadow renames.
</output>
</content>
</invoke>
