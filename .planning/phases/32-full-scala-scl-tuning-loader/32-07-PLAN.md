---
phase: 32
plan: 07
type: execute
wave: 4
depends_on: [32-06]
files_modified:
  - examples/scala/intro.flow
  - examples/scala/README.md
  - CLAUDE.md
  - flow-lang.Tests/Integration/Phase32/TutorialScriptTests.cs
autonomous: true
requirements: [SPEC-1, SPEC-2]
must_haves:
  truths:
    - "`examples/scala/intro.flow` runs end-to-end via `dotnet run --project flow-interpreter examples/scala/intro.flow` with exit 0"
    - "The tutorial demonstrates all 3 D-15 composer surfaces (identifier-bound Tuning variable, inline (loadScala) call, string-literal sugar) plus the last-wins pragma+block interaction"
    - "CLAUDE.md `Music Types Quick Reference` table includes a `Tuning` row + the `tuning` keyword appears in `Language Features` keyword list (Pitfall 9 doc trail)"
    - "Tutorial output is audibly distinct between Partch + JI sections (verifiable by listener; programmatically by WAV byte-differ Fact)"
    - "TutorialScriptTests asserts the tutorial runs cleanly + produces the expected WAV artifact"
  artifacts:
    - path: "examples/scala/intro.flow"
      provides: "Composer-facing tutorial chapter demonstrating Scala loader (D-19)"
      contains: "loadScala"
    - path: "examples/scala/README.md"
      provides: "Plain-text intro to the tutorial chapter"
      contains: "Scala"
    - path: "CLAUDE.md"
      provides: "Updated Music Types Quick Reference + Language Features keyword list"
      contains: "Tuning|loadScala"
    - path: "flow-lang.Tests/Integration/Phase32/TutorialScriptTests.cs"
      provides: "CI gate: the tutorial keeps compiling + rendering"
      contains: "intro.flow"
  key_links:
    - from: "examples/scala/intro.flow"
      to: "flow-lang.Tests/fixtures/scala/partch_43.scl"
      via: "(loadScala) call references the in-repo fixture"
      pattern: "partch_43.scl"
    - from: "flow-lang.Tests/Integration/Phase32/TutorialScriptTests.cs"
      to: "examples/scala/intro.flow"
      via: "Test runs `dotnet run --project flow-interpreter examples/scala/intro.flow` (or FlowEngineRunner with the file contents) and asserts ok==true"
      pattern: "intro.flow"
---

<objective>
Ship the composer-facing tutorial that demonstrates the Scala loader end-to-end (D-19). Document the new keyword + new builtin in CLAUDE.md so future agents and human contributors can discover them. Add a small CI gate that re-runs the tutorial in every test sweep so doc rot is caught immediately.

Purpose: SPEC line 107 leaves tutorial inclusion to planner's discretion; CONTEXT D-19 locks it as in-scope for this phase. This is the artifact that makes the feature DISCOVERABLE — the rest of Phase 32 is plumbing. Without the tutorial, a future composer reading the repo doesn't know `loadScala` exists. The HUMAN-UAT-tier acceptance per VALIDATION.md ("Composer audibly hears different timbres between JI and Partch sections") happens here.

Output: 1 new .flow tutorial + a tiny README + CLAUDE.md updates + 1 CI test asserting the tutorial keeps running. No production-code changes — pure docs + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md
@CLAUDE.md
@examples/tutorial.flow
@flow-lang.Tests/Fixtures/FlowEngineRunner.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: examples/scala/intro.flow tutorial + README</name>
  <files>
    examples/scala/intro.flow,
    examples/scala/README.md
  </files>
  <read_first>
    examples/tutorial.flow (existing tutorial style — read first 100 lines for chapter heading conventions, comment style, length norms)
    flow-lang.Tests/fixtures/scala/partch_43.scl (relative path the tutorial references)
    .planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md (D-19 — tutorial demonstrates loadScala + tuning t {})
    .planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md (Requirement 6 acceptance: last-wins interaction — JI outside, Partch inside)
  </read_first>
  <action>
    Create `examples/scala/intro.flow` — a ~30-60 line tutorial chapter demonstrating the new Scala loader. Structure mirrors existing tutorial chapters (read `examples/tutorial.flow` for style — comment-as-heading, intermediate `(print)` calls between sections to guide the reader's ear, short renderable WAV at the end).

    Content outline (the executor expands this into actual Flow source):

    1. Header comment block explaining "Chapter N: Scala Microtonal Tunings" with a 2-3 sentence motivation: Flow ships three named tunings (justIntonation, pythagorean, equalTemperament). The Scala loader extends this to ~5300 community-curated tunings via `(loadScala "path.scl")`.

    2. Section 1 — Load + describe a tuning (D-04):
       Declare `Tuning partch = (loadScala "flow-lang.Tests/fixtures/scala/partch_43.scl")`. `(print (str partch))`. Comment narrates: the `(str t)` call returns `Tuning("Harry Partch's 43-tone pure scale", 43 steps, period 1200.00¢)`.

    3. Section 2 — Apply via the `tuning { ... }` block. Render a short ascending pattern under Partch:
       ```
       tempo 120 {
           tuning partch {
               section a { Sequence m = | C4q D4q E4q F4q | }
           }
       }
       ```
       (Illustrative shape — executor authors the actual .flow source. NO fenced code block inside this `<action>` — that snippet is documentation of the SHAPE.)

    4. Section 3 — String-literal sugar (D-15 ergonomic form). Show:
       `tuning "flow-lang.Tests/fixtures/scala/carlos_alpha.scl" { section b { Sequence n = | C4q E4q G4q B4q | } }`. Comment narrates: the string-literal form is equivalent to `(loadScala "...carlos_alpha.scl")` desugared at parse time per D-15.

    5. Section 4 — Last-wins pragma + block interaction (SPEC-6 acceptance). Show:
       `enable justIntonation;` at the top of this script, then a section c (outside any tuning block) followed by a section d wrapped in `tuning partch { ... }`. Comment narrates: section c renders under JI (the file-scope pragma); section d renders under Partch (the inner block wins).

    6. Section 5 — Render to WAV. Build a Song `[a b c d]` and `(renderSong song "sine") -> (writeWav "/tmp/p32_intro.wav")`. The output WAV is the audible deliverable.

    7. Footer comment: "Run with `dotnet run --project flow-interpreter examples/scala/intro.flow`. Listen to `/tmp/p32_intro.wav` — the four sections differ audibly because each uses a distinct tuning system."

    Constraints:
    - Total length 30-60 lines per D-19
    - Uses ONLY features that exist post-Phase-32 — no speculative builtins
    - References ONLY in-repo fixture paths (the `flow-lang.Tests/fixtures/scala/...` relative path is reliable when `dotnet run --project flow-interpreter` is invoked from repo root)
    - Output WAV at `/tmp/p32_intro.wav` (Linux convention; Phase 32 is Linux-primary per CLAUDE.md Constraints)
    - Comments use Flow's existing comment style (mostly `;` line comments per Phase 31 — read examples/tutorial.flow first to confirm)

    Also create `examples/scala/README.md` (≤ 30 lines, plain markdown):
    - Title: `Scala Microtonal Tunings — Tutorial`
    - 2-3 sentence orientation
    - Run command: `dotnet run --project flow-interpreter examples/scala/intro.flow`
    - "Listen to /tmp/p32_intro.wav after the run" hint
    - Link to `flow-lang.Tests/fixtures/scala/LICENSE.md` for attribution
    - 1-paragraph pointer to `(loadScala)` + `tuning { ... }` reference docs (point at CLAUDE.md sections updated in Task 3 below)
  </action>
  <verify>
    <automated>test -f examples/scala/intro.flow &amp;&amp; test -f examples/scala/README.md &amp;&amp; grep -q "loadScala" examples/scala/intro.flow &amp;&amp; grep -q "tuning" examples/scala/intro.flow &amp;&amp; grep -q "writeWav" examples/scala/intro.flow &amp;&amp; grep -q "partch_43" examples/scala/intro.flow &amp;&amp; wc -l examples/scala/intro.flow | awk '{exit ($1 &gt;= 25 &amp;&amp; $1 &lt;= 100) ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `examples/scala/intro.flow` exists; line count between 25 and 100 (D-19 budget with slack)
    - Contains `loadScala` (builtin call)
    - Contains `tuning` (block keyword)
    - Contains `writeWav` (renders an audible artifact)
    - Contains `partch_43` (references in-repo fixture)
    - Contains both the identifier form (`Tuning partch = ...`) and the string-literal sugar form (`tuning "..." { ... }`) — grep for both patterns
    - `examples/scala/README.md` exists; contains `Scala`, `loadScala`, and the run command
  </acceptance_criteria>
  <done>Tutorial + README authored; demonstrates all 3 D-15 forms + last-wins; ≤ 60 lines.</done>
</task>

<task type="auto">
  <name>Task 2: TutorialScriptTests — CI gate the tutorial keeps running</name>
  <files>
    flow-lang.Tests/Integration/Phase32/TutorialScriptTests.cs
  </files>
  <read_first>
    examples/scala/intro.flow (Task 1 product)
    flow-lang.Tests/Fixtures/FlowEngineRunner.cs (test harness)
    flow-lang.Tests/Integration/Phase28/RagtimeFixtureTests.cs (Pattern B reference — on-disk .flow script integration test)
    .planning/phases/32-full-scala-scl-tuning-loader/32-VALIDATION.md (Per-Task Verification Map — W7 tutorial chapter test gate)
  </read_first>
  <behavior>
    - Running `examples/scala/intro.flow` via FlowEngineRunner produces `ok == true`
    - The output WAV exists at `/tmp/p32_intro.wav` after the run
    - The output WAV has non-zero length (real samples rendered, not an empty buffer)
    - Two-run determinism: re-running the SAME tutorial produces byte-identical WAV (catches any tutorial-specific non-determinism not covered by Plan 32-06's per-fixture determinism tests)
  </behavior>
  <action>
    Write `flow-lang.Tests/Integration/Phase32/TutorialScriptTests.cs` mirroring Pattern B (`RagtimeFixtureTests.cs`).

    Facts (≥ 2):
    1. `IntroScript_RunsToCompletion_ProducesWav` — read `examples/scala/intro.flow` via `File.ReadAllText`; run via FlowEngineRunner.RunSource (or FlowEngineRunner.RunFile if such an overload exists — read FlowEngineRunner.cs to confirm). Assert:
       - RunSource result `ok == true`
       - `File.Exists("/tmp/p32_intro.wav") == true` after the run
       - `new FileInfo("/tmp/p32_intro.wav").Length > 1024` (at least 1 KB of samples — guards against an empty WAV header-only file)
       - Clean up `/tmp/p32_intro.wav` in Dispose

    2. `IntroScript_TwoRuns_ProducesByteIdenticalWav` — run the tutorial twice (separate FlowEngineRunner instances, RenderingDiagnostics.ResetForTesting between). Compare WAV bytes via SequenceEqual. Asserts the tutorial itself respects the two-run determinism contract (SPEC-6).

    Use `[Collection("FlowScripts")]` since FlowEngineRunner is single-threaded in existing tests. File-scoped namespace `FlowLangTests.Integration.Phase32`.

    Path resilience: the tests run from whatever cwd `dotnet test` chooses. The tutorial references `flow-lang.Tests/fixtures/scala/partch_43.scl` as a relative path from repo root; the test process's cwd may differ. Mitigation: before invoking FlowEngineRunner, `Directory.SetCurrentDirectory(<repo root>)` — the repo root can be derived by walking up from `AppContext.BaseDirectory` looking for the `.git` directory or for `flow-lang.Tests/` itself. Read the existing Phase 23 / Phase 28 integration tests to find the established cwd-handling pattern; if none exists, use a single `Directory.SetCurrentDirectory(Path.GetFullPath(Path.Combine(AppContext.BaseDirectory, "../../../..")))` (mirror the 4-level-up `bin/Debug/net10.0` -> repo-root walk). Save the original cwd; restore in Dispose.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "ClassName~TutorialScriptTests" -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "ClassName~TutorialScriptTests" -v minimal` exits 0; reports ≥ 2 Facts passed
    - Direct CLI invocation also works: `dotnet run --project flow-interpreter examples/scala/intro.flow` from repo root exits 0 and produces `/tmp/p32_intro.wav` (manual smoke test — VALIDATION.md W7)
    - Two-run determinism Fact `IntroScript_TwoRuns_ProducesByteIdenticalWav` passes
  </acceptance_criteria>
  <done>Tutorial gated by CI; two-run determinism verified at the tutorial level too.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md documentation updates — Music Types row + keyword list + builtin docs</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md (sections: `Music Types Quick Reference` table; `Language Features` -> `Music-Specific` list; `Built-in Function Categories`)
    .planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md (CLAUDE.md update points listed under `<canonical_refs>`)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pitfall 9 — keyword list documentation gap; Project intel section)
  </read_first>
  <behavior>
    - `Music Types Quick Reference` table in CLAUDE.md has a NEW row for `Tuning` documenting the literal-binding form, type, coercion, accepted-at sites
    - `Language Features` (the `Music-Specific` bullet list) mentions the `tuning { ... }` musical-context block alongside the existing tempo/timesig/key/swing entries
    - The `tuning` keyword is documented as reserved (Pitfall 9 + Claude's Discretion)
    - `Built-in Function Categories` lists `(loadScala "path")` + `(loadScala "scl" "kbm")` under a Tuning subsection
    - Phase 32 SHIPPED line at the appropriate spot (the existing CLAUDE.md `Language Features` section already mentions Phase numbers; add `(Phase 32)` to the new entries to mirror the existing pattern)
  </behavior>
  <action>
    Edit `CLAUDE.md` in-place (use the Edit tool — locate exact existing strings and replace with augmented content; do NOT rewrite the whole file):

    1. Find the `### Music Types Quick Reference` section (table with columns Literal | Type | IsCompatibleWith | Accepted at). Add a new row:
       - Literal: `(loadScala "x.scl")`
       - Type: `Tuning`
       - IsCompatibleWith: strict (reference identity; no Double/Float fall-through)
       - Accepted at: `tuning { ... }` block; `(str t)`; reference-equality usage
       Place the row in alphabetical position or at the end of the table — match the existing ordering style.

    2. Find the `### Music-Specific` bullet list under `Language Features`. Add a new bullet documenting the `tuning { ... }` block. Mention:
       - The block applies a `Tuning` value
       - Three composer surface forms per D-15 (identifier, inline call, string-literal sugar)
       - Last-wins with pragmas (the JI-outside-Partch-inside example from SPEC §6)
       - Cross-reference: `(loadScala "path")` is the builtin used to obtain a Tuning value
       - Mark the bullet `(Phase 32)` to mirror the existing pattern
       - Add the `tuning` keyword to any existing list of reserved keywords (search for `voicePool` — voicePool was added in Phase 28 and may or may not appear in the keyword list per RESEARCH Pitfall 9; if voicePool is documented, add tuning the same way; if voicePool isn't documented either, add BOTH for consistency)

    3. Find `Built-in Function Categories` section. Either:
       - Add a new subsection `### Tuning (Tuning/)` with two bullets: `loadScala` (1-arg) and `loadScala` (2-arg overload with .kbm)
       - OR extend an existing subsection if the section structure prefers grouped categories
       Match the surrounding doc style.

    4. (Optional but recommended) Update the `Project Structure` block — `Directory Layout (flow-lang)` lists the existing subdirectories. The `Audio/Tuning/` subdirectory expanded with new files (ScalaParser, ScalaKbmParser, ScalaKbm, ResolvedTuning, ScalaBuiltins, ScalaParseException, ScalaKbmParseException). If the directory listing currently spells out files at this level of detail, add the new ones; if it's just a directory-level summary, leave it — the directory itself is already mentioned.

    All edits via the Edit tool with precise old-string / new-string pairs. Make no other content changes.
  </action>
  <verify>
    <automated>grep -q "loadScala" CLAUDE.md &amp;&amp; grep -q "Tuning" CLAUDE.md &amp;&amp; grep -q "tuning { " CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'loadScala' CLAUDE.md` returns ≥ 1 match
    - `grep -n '| (loadScala\|| Tuning ' CLAUDE.md` matches the new Music Types row format
    - `grep -n 'tuning {' CLAUDE.md` returns ≥ 1 match (documents the block syntax)
    - `grep -n 'Phase 32' CLAUDE.md` returns ≥ 1 new match attributing the addition
    - No other CLAUDE.md sections accidentally modified (verify via `git diff CLAUDE.md` review by the executor)
  </acceptance_criteria>
  <done>CLAUDE.md teaches future agents about loadScala + tuning block; Pitfall 9 keyword reservation documented.</done>
</task>

</tasks>

<verification>
- `examples/scala/intro.flow` exists, ≤ 100 lines, demonstrates all 3 D-15 forms + last-wins
- `examples/scala/README.md` orients composers to the tutorial
- `TutorialScriptTests` CI gate prevents tutorial rot (2 Facts: runs-to-completion + two-run determinism)
- CLAUDE.md updated with Music Types row + `tuning` keyword + `(loadScala)` builtin docs
- Phase 23 + Phase 32 sub-suites both stay GREEN
- HUMAN-UAT-tier verification: composer can listen to `/tmp/p32_intro.wav` and audibly hear the 3-4 distinct tunings
- **Phase-exit failure ceiling preserved (Pitfall 7):** Full suite failure count remains ≤ 62 — the pre-existing Phase 28 `PerSynthArticulation` baseline ceiling Plan 32-04 also gates against. Confirm via `dotnet test 2>&1 | tail -20` after all Phase 32 work lands; the "Failed: N" line MUST satisfy `N ≤ 62`. This is the phase-end version of the per-plan gate already enforced inside Plan 32-04.
</verification>

<success_criteria>
- SPEC-1 + SPEC-2 acceptance battery rounded out by a discoverable end-to-end tutorial (D-19)
- Tutorial Test class catches any future regression that breaks the demo
- CLAUDE.md teaches future Claude / human contributors how to use the Scala loader
- Pitfall 9 (keyword shadowing) documented in CLAUDE.md as a reserved word
- Two-run determinism holds at the tutorial level too (defense in depth on top of Plan 32-06 Task 3's per-fixture determinism Facts)
- Phase-exit gate: full-suite failure count ≤ 62 (Pitfall 7 ceiling preserved across the entire Phase 32 closure)
</success_criteria>

<output>
After completion, create `.planning/phases/32-full-scala-scl-tuning-loader/32-07-SUMMARY.md`.
</output>
