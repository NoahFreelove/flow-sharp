---
phase: 32
plan: 05
type: execute
wave: 2
depends_on: [32-03]
files_modified:
  - flow-lang/Runtime/MusicalContext.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang/Core/FlowEngine.cs
  - flow-lang/StandardLibrary/Audio/SongRenderer.cs
  - flow-lang/StandardLibrary/Audio/MidiExport.cs
  - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
  - flow-lang.Tests/Unit/Phase32/TuningStackFacts.cs
# NOTE: files_modified above lists production code. Task 2 step 5 ALSO migrates
# grep-discovered Phase 23 test files that call MusicalContext.Tuning directly
# (RESEARCH says "5+ test files" under flow-lang.Tests/Unit/Phase23/ and
# flow-lang.Tests/Integration/Phase23/). Exact file set is enumerated at
# execute-time via:
#   grep -rn 'MusicalContext\.Tuning\|\.Tuning ==' \
#     flow-lang.Tests/Unit/Phase23/ flow-lang.Tests/Integration/Phase23/
# Discovered paths are recorded in 32-05-SUMMARY.md after Task 2 completes.
# Each match's access path is migrated to ActiveTuning (read) or
# SetFileScopeTuning (write) WITHOUT changing test semantics.
autonomous: true
requirements: [SPEC-2, SPEC-6]
must_haves:
  truths:
    - "MusicalContext exposes `Stack<RenderTuning> TuningStack` AND an `ActiveTuning` getter that returns Peek() or Default if empty (D-12)"
    - "Phase 23 file-scope tuning pragmas (justIntonation/pythagorean/equalTemperament) PUSH once on TuningStack at engine startup; never popped"
    - "REPL eval boundary pops the stack back to the bottom file-scope pragma frame (D-14 + D-08 coexistence per Pitfall 2)"
    - "All 8 Phase 23 readers of `MusicalContext.Tuning` now consume `ActiveTuning` (or equivalent helper) — no broken call sites"
    - "Phase 23 D-13 MIDI-export advisory fires under EITHER `RenderTuning.Custom != null` OR `System != EqualTemperament` (Pitfall 6)"
    - "Phase 23 sub-suite (`--filter Phase23`) remains 100% GREEN after refactor"
  artifacts:
    - path: "flow-lang/Runtime/MusicalContext.cs"
      provides: "Refactored with TuningStack + ActiveTuning getter"
      contains: "TuningStack"
    - path: "flow-lang/Runtime/ExecutionContext.cs"
      provides: "GetMusicalContext + SetTuning + new PushTuning/PopTuning resolve via TuningStack"
      contains: "PushTuning|PopTuning"
    - path: "flow-lang.Tests/Unit/Phase32/TuningStackFacts.cs"
      provides: "Stack semantics + REPL sticky-vs-ephemeral Facts"
      contains: "public class TuningStackFacts"
  key_links:
    - from: "flow-lang/Runtime/MusicalContext.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "ExecutionContext reads/writes TuningStack; readers call ActiveTuning"
      pattern: "TuningStack|ActiveTuning"
    - from: "flow-lang/Core/FlowEngine.cs"
      to: "flow-lang/Runtime/MusicalContext.cs"
      via: "ApplyTuningPragma pushes the bottom file-scope frame (never popped per Pitfall 2)"
      pattern: "PushTuning|ApplyTuningPragma"
    - from: "flow-lang/StandardLibrary/Audio/MidiExport.cs"
      to: "flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs"
      via: "D-13 advisory predicate updated to `Custom != null || System != EqualTemperament` (Pitfall 6)"
      pattern: "Custom != null|Custom is not null"
---

<objective>
Refactor `MusicalContext.Tuning` (Phase 23's nullable scalar field) into `Stack<RenderTuning> TuningStack` per D-12. Update the 5 code-change sites identified in RESEARCH §"Readers of MusicalContext.Tuning". Bridge Phase 23's file-scope pragma handlers to push onto the new stack (D-12 + Pitfall 2 — bottom frame is sticky across REPL evals; block frames force-pop at REPL boundary per D-14). Update the D-13 MIDI-export advisory predicate to detect custom Scala tunings too (Pitfall 6).

Purpose: this is the SINGLE-HIGHEST-BLAST-RADIUS structural change in Phase 32 (RESEARCH §Pitfall 1 explicitly calls it out). It MUST land before the `tuning { ... }` AST plan (32-06) can push/pop. Per RESEARCH recommendation: this plan is isolated from parser work; only touches the runtime stack + the readers.

Output: refactored Runtime classes + one new Facts file + bridge updates in `FlowEngine.cs` (pragma push), `SongRenderer.cs` (ResolveRenderTuning peeks stack), `MidiExport.cs` (Pitfall 6 advisory predicate), `HarmonyFunctions.cs` (enharmonic guard reads stack).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-full-scala-scl-tuning-loader/32-SPEC.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md
@.planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md
@flow-lang/Runtime/MusicalContext.cs
@flow-lang/Runtime/ExecutionContext.cs
@flow-lang/Core/FlowEngine.cs
@flow-lang/StandardLibrary/Audio/SongRenderer.cs
@flow-lang/StandardLibrary/Audio/MidiExport.cs
@flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
@flow-lang.Tests/Integration/Phase23/TuningDeterminismTests.cs
@flow-lang.Tests/Unit/Phase23/WriteMidiWarningFacts.cs

<interfaces>
Public surface refactor (Plan 32-06 — `TuningContextStatement` interpreter — consumes these):

`MusicalContext` (file: `flow-lang/Runtime/MusicalContext.cs`):
  - Remove or deprecate the scalar `TuningSystem? Tuning { get; set; }` field (per D-12 supersession of Phase 23 D-05)
  - Add `Stack<RenderTuning> TuningStack { get; }` (private setter; expose mutation via ExecutionContext methods)
  - Add `RenderTuning ActiveTuning => TuningStack.Count > 0 ? TuningStack.Peek() : RenderTuning.Default;` (the single readable resolution accessor)
  - `Clone()` deep-copies the stack (immutable RenderTuning values; new Stack with same items)
  - `ToString()` reflects top-of-stack via `ActiveTuning`

`ExecutionContext` (file: `flow-lang/Runtime/ExecutionContext.cs`):
  - `GetMusicalContext()` walks call stack top-to-bottom, returns first frame with non-empty TuningStack OR the bottom-most frame with the file-scope pragma frame
  - `SetTuning(RenderTuning rt)` REPLACES the bottom file-scope pragma frame (deprecates the Phase 23 `SetTuning(TuningSystem?)` signature; either rename to `SetFileScopeTuning` OR keep `SetTuning` overloaded)
  - NEW: `PushTuning(RenderTuning rt)` — pushes a block frame onto the active frame's TuningStack
  - NEW: `PopTuning()` — pops the top frame; throws if popping the file-scope pragma frame
  - REPL eval boundary semantics per D-14 + Pitfall 2: a separate method (e.g. `ResetBlockTuningStack()`) pops everything ABOVE the bottom pragma frame. Pragmas remain sticky.

`FlowEngine.ApplyTuningPragma` (file: `flow-lang/Core/FlowEngine.cs:148-157`):
  - Replace `_context.SetTuning(TuningSystem.JustIntonation)` with `_context.SetFileScopeTuning(BuildRenderTuningFromPragma(...))`
  - The `BuildRenderTuningFromPragma` helper takes the pragma name (`"justIntonation"` / `"pythagorean"` / `"equalTemperament"`) and produces a `RenderTuning(<system>, <mode>, <tonicLetter>, <tonicAlteration>, custom: null)`. Tonic + mode come from the same logic SongRenderer.ResolveRenderTuning currently uses at section-render time. The key insight per RESEARCH lines 226-229: file-scope pragmas have no key context yet, so use the same defaults Phase 23's `ResolveRenderTuning` uses when no key context exists. Replicate that logic here. (Confirmed it's a one-shot push; never popped per Pitfall 2 file-scope frame.)
  - REPL sticky behavior (D-08): if the new program has NO pragma push, do nothing — leave the existing pragma push on the stack (per RESEARCH lines 228-229).

`SongRenderer.ResolveRenderTuning` (file: `flow-lang/StandardLibrary/Audio/SongRenderer.cs:162-185`):
  - Replace the current "read MusicalContext.Tuning scalar" with `ctx.ActiveTuning` (or whatever the resolution helper is named)
  - When ActiveTuning's `Custom` is non-null, just return ActiveTuning verbatim (custom tunings win — RESEARCH Pitfall 3)
  - When Custom is null, fall through to existing Phase 23 logic that picks tonic + mode from key context

`MidiExport.cs:199-204` (D-13 advisory):
  - Current predicate: `musicalCtx?.Tuning is TuningSystem activeTuning && activeTuning != TuningSystem.EqualTemperament`
  - NEW predicate: `var rt = musicalCtx?.ActiveTuning ?? RenderTuning.Default; if (rt.Custom != null || rt.System != TuningSystem.EqualTemperament) { ...fire D-13 advisory... }`
  - This is Pitfall 6 — D-13 must fire under EITHER non-EQ system OR custom Scala tuning

`HarmonyFunctions.cs:49-60` (enharmonic warning + tuning-aware lookup):
  - Same shape — replace `MusicalContext.Tuning` reads with `ActiveTuning` reads. Per RESEARCH §"Readers of MusicalContext.Tuning", lines 49, 56, 60 reference Tuning.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MusicalContext refactor + ExecutionContext push/pop/active accessors</name>
  <files>
    flow-lang/Runtime/MusicalContext.cs,
    flow-lang/Runtime/ExecutionContext.cs
  </files>
  <read_first>
    flow-lang/Runtime/MusicalContext.cs (lines 1-150 — full file; especially lines 62-69 Tuning field, line 89 Clone, line 137 ToString)
    flow-lang/Runtime/ExecutionContext.cs (lines 195-300 — GetMusicalContext at 195-238; SetTuning at 280-293)
    flow-lang/StandardLibrary/Audio/Tuning/RenderTuning.cs (post-Plan-32-03 shape with Custom field + Default factory)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pitfall 1 + Pitfall 2 — REPL sticky-vs-ephemeral semantics; section "Runtime / interpreter integration")
  </read_first>
  <action>
    Refactor `flow-lang/Runtime/MusicalContext.cs`:

    1. Remove (or deprecate via `[Obsolete]` annotation kept-for-now-but-internal) the scalar field `TuningSystem? Tuning { get; set; }`. Per RESEARCH §"Recommendation" (Option B), keep `Tuning` as a private field if any test references it directly; surface readers consume the new `ActiveTuning` accessor.
    2. Add `Stack<RenderTuning> TuningStack { get; }` — initialized in the constructor to an empty `new Stack<RenderTuning>()`. Internal mutation only; readers consume `ActiveTuning`.
    3. Add `public RenderTuning ActiveTuning => TuningStack.Count > 0 ? TuningStack.Peek() : RenderTuning.Default;`
    4. Update `Clone()` (currently at line 89): when cloning a MusicalContext, also clone the TuningStack — `Stack<RenderTuning>` clones via `new Stack<RenderTuning>(new Stack<RenderTuning>(original.TuningStack))` (two-reversal trick to preserve order; RenderTuning is a struct so reference issues don't apply).
    5. Update `ToString()` (currently at line 137): include `ActiveTuning` in the textual representation; remove any reference to the deprecated scalar Tuning field.

    Refactor `flow-lang/Runtime/ExecutionContext.cs`:

    1. `GetMusicalContext()` (line 195-238) walks the call stack top-to-bottom looking for a frame whose MusicalContext has a non-empty TuningStack. Per RESEARCH §"Option B": readers consume `ActiveTuning` via the returned MusicalContext, so the resolution becomes "return the MusicalContext whose TuningStack is the deepest non-empty stack" — equivalent to "return the top-of-call-stack frame's MusicalContext that has TuningStack.Count > 0; if none, return GlobalFrame.MusicalContext". Specifically: replace the existing `frame.MusicalContext.Tuning ??= ...` logic with a stack-aware lookup that walks frames top-to-bottom.

    2. Rename `SetTuning(TuningSystem?)` → `SetFileScopeTuning(RenderTuning)`. This method REPLACES the bottom of `GlobalFrame.MusicalContext.TuningStack`. Algorithm: while `TuningStack.Count > 1` (something pushed above the file-scope frame is anomalous at SetFileScopeTuning-time, but possible if the runtime is mid-eval), pop. Then if `TuningStack.Count == 1` pop the single file-scope frame, push the new one. Else push the new one. Net: stack contains exactly one entry, the new file-scope frame. Preserve REPL sticky behavior (D-08): if called with the SAME RenderTuning as what's already there, no-op. (The simpler rule per RESEARCH lines 228-229: "if the new program has NO tuning pragma, do nothing — leave the existing pragma push on the stack". FlowEngine.ApplyTuningPragma handles that check; SetFileScopeTuning unconditionally replaces.)

    3. ADD `public void PushTuning(RenderTuning rt)` — pushes onto the topmost frame's TuningStack (NOT the global frame). This is the entry point Plan 32-06's interpreter case uses for `tuning t { ... }` block entry.

    4. ADD `public void PopTuning()` — pops the topmost frame's TuningStack. Throws `InvalidOperationException` if the stack would underflow (defensive — should never happen if push/pop pairs are balanced via try/finally per Plan 32-06).

    5. ADD `public void ResetBlockTuningStack()` for D-14 REPL eval boundary: pops the GlobalFrame.MusicalContext.TuningStack down to its bottom (file-scope frame) — i.e. leave Count ≤ 1. This method is called by FlowEngine at REPL eval boundaries (next task). Per Pitfall 2: pragmas stay; blocks force-close.

    Update existing readers in this same file (if any) — `ExecutionContext.cs` may itself read `MusicalContext.Tuning` somewhere (especially around the SetTuning logic at 280-293). Switch those reads to `ActiveTuning`.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj -v minimal 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Stack<RenderTuning> TuningStack' flow-lang/Runtime/MusicalContext.cs` returns ≥ 1 match
    - `grep -n 'public RenderTuning ActiveTuning' flow-lang/Runtime/MusicalContext.cs` returns 1 match
    - `grep -n 'public void PushTuning\|public void PopTuning\|public void SetFileScopeTuning\|public void ResetBlockTuningStack' flow-lang/Runtime/ExecutionContext.cs` returns ≥ 4 matches (all four new methods)
    - `dotnet build flow-lang.Tests/flow-lang.Tests.csproj -v minimal` exits 0
    - Phase 23 sub-suite still compiles (some tests may fail because readers haven't been migrated yet — that's expected and addressed in Task 2 below)
  </acceptance_criteria>
  <done>Stack data structure + push/pop/active/reset API surface present and compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Phase 23 readers + FlowEngine pragma bridge + Pitfall 6 D-13 predicate</name>
  <files>
    flow-lang/Core/FlowEngine.cs,
    flow-lang/StandardLibrary/Audio/SongRenderer.cs,
    flow-lang/StandardLibrary/Audio/MidiExport.cs,
    flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
  </files>
  <read_first>
    flow-lang/Core/FlowEngine.cs (lines 120-180 — ApplyTuningPragma at 148-157)
    flow-lang/StandardLibrary/Audio/SongRenderer.cs (lines 140-200 — ResolveRenderTuning at 162-185)
    flow-lang/StandardLibrary/Audio/MidiExport.cs (lines 160-220 — D-13 advisory at 199-204)
    flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (lines 40-70 — Tuning reads at 49, 56, 60)
    flow-lang/Runtime/MusicalContext.cs (post-Task-1 shape with ActiveTuning + TuningStack)
    flow-lang/Runtime/ExecutionContext.cs (post-Task-1 shape with PushTuning/PopTuning/SetFileScopeTuning/ResetBlockTuningStack)
    flow-lang/StandardLibrary/Audio/Tuning/TuningSystem.cs (enum values)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pitfall 6 — D-13 predicate update; section "Pragma bridge — Phase 23 → Phase 32 transition")
  </read_first>
  <action>
    Migrate the 5 code-change reader sites identified in RESEARCH §"Readers of MusicalContext.Tuning":

    1. `flow-lang/Core/FlowEngine.cs:148-157` (ApplyTuningPragma): replace `_context.SetTuning(TuningSystem.X)` with `_context.SetFileScopeTuning(BuildRenderTuningFromPragmaName("X"))`. Add a private helper `BuildRenderTuningFromPragmaName(string name)` that returns `new RenderTuning(<system>, Mode.Major, 'C', 0, custom: null)` per Phase 23 conventions — read SongRenderer.ResolveRenderTuning to understand the tonic+mode defaults Phase 23 uses when no key context exists yet (at file-scope pragma time). Use Major mode, C tonic, 0 alteration as the Phase 23 baseline if unsure (mirror what SongRenderer falls back to). Per Pitfall 2 + REPL D-08: if the program has no pragma, FlowEngine should NOT call SetFileScopeTuning at all — the existing stack frame survives. Read the existing `if (tuning is null) return;` guard at line 289 (per RESEARCH line 228) and mirror its behavior.

       ALSO: if FlowEngine has a REPL eval boundary point (look for where the engine handles `Eval` or similar for the REPL), insert a call to `_context.ResetBlockTuningStack()` per D-14 + Pitfall 2 ("blocks force-close at REPL eval boundary"). If no obvious REPL boundary exists in FlowEngine.cs, document via inline comment that this is the future hook point and mark with `// TODO(Phase 32 D-14): ResetBlockTuningStack on REPL boundary` so the next REPL test catches it.

    2. `flow-lang/StandardLibrary/Audio/SongRenderer.cs:162-185` (ResolveRenderTuning): replace reads of `MusicalContext.Tuning` (scalar) with `ctx.ActiveTuning`. If `ActiveTuning.Custom != null`, return ActiveTuning verbatim (custom wins). Else fall through to existing Phase 23 logic that resolves tonic + mode from key context AND switches on TuningSystem to pick the right table.

    3. `flow-lang/StandardLibrary/Audio/MidiExport.cs:199-204` (Pitfall 6 — D-13 advisory): current predicate uses `musicalCtx?.Tuning is TuningSystem activeTuning && activeTuning != TuningSystem.EqualTemperament`. Replace with:
        ```
        var rt = musicalCtx?.ActiveTuning ?? RenderTuning.Default;
        if (rt.Custom != null || rt.System != TuningSystem.EqualTemperament)
        {
            // fire D-13 advisory: emit one-shot stderr warning that MIDI export is 12-TET
        }
        ```
       (Illustrative only — no fenced code in <action>; the executor reads the file and applies the equivalent C# change.)

       Use the existing warn-once sentinel logic; if RenderingDiagnostics.WarnOnce is already keyed by some sentinel like `midi:non12tet`, keep the same sentinel format — the message text may be unchanged, only the trigger predicate changes.

    4. `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs:49,56,60`: replace `MusicalContext.Tuning` reads with `ActiveTuning` reads. The semantic is the same — the enharmonic guard and tuning-aware lookup just need access to the resolved RenderTuning. May need to switch on `ActiveTuning.System` AND check `ActiveTuning.Custom != null` depending on what the original code does.

    5. Phase 23 test files that construct or assert on `MusicalContext.Tuning` directly: per RESEARCH §"Plus 5+ test files that construct or assert on `MusicalContext.Tuning` directly", search for `MusicalContext.Tuning` references in `flow-lang.Tests/Unit/Phase23/` and `flow-lang.Tests/Integration/Phase23/`. Migrate each direct access to `ActiveTuning` (read access) OR `SetFileScopeTuning` (write access). DO NOT change test SEMANTICS — keep the assertions the same, only the access path changes.

       Grep first: `grep -rn 'MusicalContext\.Tuning\|\.Tuning ==' flow-lang.Tests/Unit/Phase23/ flow-lang.Tests/Integration/Phase23/`. Record the full list of matched file paths in `32-05-SUMMARY.md` under a `### Phase 23 test files migrated (frontmatter note resolution)` heading — this satisfies the files_modified frontmatter note that defers enumeration to execute-time. Update each match to the new API. Some may need `ActiveTuning.System` instead of just `Tuning` (since Phase 23 stored a TuningSystem enum and Phase 32 now resolves through RenderTuning).

    Final regression sweep: `dotnet test --filter "FullyQualifiedName~Phase23" --no-build` MUST pass 100% after the migration. This is the critical regression gate.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase23" --no-build -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SetFileScopeTuning\|SetTuning' flow-lang/Core/FlowEngine.cs` returns ≥ 1 match calling the new push-based API
    - `grep -n 'ActiveTuning' flow-lang/StandardLibrary/Audio/SongRenderer.cs` returns ≥ 1 match
    - `grep -n 'Custom != null\|Custom is not null' flow-lang/StandardLibrary/Audio/MidiExport.cs` returns ≥ 1 match (Pitfall 6 predicate update grep-verified)
    - `grep -n 'ActiveTuning' flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` returns ≥ 1 match
    - `dotnet test --filter "FullyQualifiedName~Phase23" --no-build -v minimal` exits 0 (CRITICAL — Phase 23 regression gate)
    - `dotnet build flow-lang.Tests/flow-lang.Tests.csproj -v minimal` exits 0 (no compile errors anywhere)
  </acceptance_criteria>
  <done>5 reader sites migrated; FlowEngine pragma push wired; D-13 advisory predicate covers custom tunings; Phase 23 sub-suite 100% GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TuningStackFacts — stack semantics + REPL sticky-vs-ephemeral</name>
  <files>
    flow-lang.Tests/Unit/Phase32/TuningStackFacts.cs
  </files>
  <read_first>
    flow-lang/Runtime/MusicalContext.cs (post-Tasks-1-2 shape)
    flow-lang/Runtime/ExecutionContext.cs (post-Tasks-1-2 shape)
    flow-lang/Core/FlowEngine.cs (post-Task-2 shape)
    flow-lang.Tests/Integration/Phase23/TuningDeterminismTests.cs (REPL ResetForTesting pattern)
    .planning/phases/32-full-scala-scl-tuning-loader/32-CONTEXT.md (D-08 REPL sticky pragma; D-12 stack; D-14 REPL block ephemeral)
    .planning/phases/32-full-scala-scl-tuning-loader/32-RESEARCH.md (Pitfall 2 — REPL stickiness vs block ephemeral coexistence)
  </read_first>
  <behavior>
    - `new MusicalContext()` has `TuningStack.Count == 0` AND `ActiveTuning == RenderTuning.Default`
    - `PushTuning(rt1)` then `ActiveTuning == rt1`
    - `PushTuning(rt1); PushTuning(rt2);` then `ActiveTuning == rt2`
    - `PushTuning(rt1); PushTuning(rt2); PopTuning();` then `ActiveTuning == rt1`
    - `SetFileScopeTuning(rt1); SetFileScopeTuning(rt2);` results in `TuningStack.Count == 1` and `ActiveTuning == rt2` (replaces bottom frame, doesn't accumulate)
    - `SetFileScopeTuning(rt1); PushTuning(rt2); ResetBlockTuningStack();` then `ActiveTuning == rt1` (pragma sticks, block popped — Pitfall 2 contract)
    - `PopTuning` on empty stack throws (defensive)
    - File-scope pragma push survives a `ResetBlockTuningStack()` call (D-08 REPL sticky)
  </behavior>
  <action>
    Write `flow-lang.Tests/Unit/Phase32/TuningStackFacts.cs` exercising the ExecutionContext stack API end-to-end. xUnit; no FlowEngineRunner needed (operates at ExecutionContext layer). Construct ExecutionContext directly — read TuningDeterminismTests.cs or PitchConversionTuningFacts.cs for the construction pattern Phase 23 tests use.

    Facts (≥ 7):
    1. `MusicalContext_Default_HasEmptyStack_ActiveTuningIsDefault`
    2. `PushTuning_Once_ActiveTuningReturnsPushedValue`
    3. `PushTuning_Twice_ActiveTuningReturnsTopValue`
    4. `PopTuning_AfterTwoPushes_RevealsLowerValue`
    5. `SetFileScopeTuning_TwiceReplaces_DoesNotAccumulate` (TuningStack.Count == 1 after second call)
    6. `ResetBlockTuningStack_PreservesPragmaFrame_PopsBlocks` (Pitfall 2 — D-08 sticky + D-14 ephemeral coexistence)
    7. `PopTuning_OnEmptyStack_Throws`

    Each Fact constructs a fresh ExecutionContext + MusicalContext, exercises the relevant method sequence, asserts the resulting ActiveTuning OR TuningStack.Count state.

    Construct `RenderTuning` test values via the existing 4-arg construction `new RenderTuning(TuningSystem.JustIntonation, Mode.Major, 'C', 0)` (the 5th `Custom` parameter defaults to null per Plan 32-03's change). For one Fact, also construct `new RenderTuning(TuningSystem.EqualTemperament, Mode.Major, 'C', 0, custom: someResolved)` to verify the stack handles custom tunings cleanly.

    File-scoped namespace `FlowLangTests.Unit.Phase32`.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests --filter "ClassName~TuningStackFacts" -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test --filter "ClassName~TuningStackFacts" -v minimal` exits 0; reports ≥ 7 Facts passed
    - Pitfall 2 explicit Fact `ResetBlockTuningStack_PreservesPragmaFrame_PopsBlocks` passes
    - Phase 23 sub-suite regression sweep still GREEN: `dotnet test --filter "FullyQualifiedName~Phase23" --no-build -v minimal` exits 0
  </acceptance_criteria>
  <done>Stack semantics verified; Pitfall 2 sticky-vs-ephemeral contract proven; Phase 23 still GREEN.</done>
</task>

</tasks>

<verification>
- `dotnet build` clean
- TuningStackFacts ≥ 7 Facts GREEN
- Phase 23 sub-suite 100% GREEN (the CRITICAL regression gate flagged in plan_structure_guidance)
- 5 reader sites migrated: FlowEngine, SongRenderer, MidiExport, HarmonyFunctions, plus the Phase 23 tests that referenced `MusicalContext.Tuning` directly
- Pitfall 6 D-13 advisory predicate now covers `Custom != null` (grep-verified)
- Pitfall 2 D-08-sticky-+-D-14-ephemeral coexistence verified by Fact 6
</verification>

<success_criteria>
- `MusicalContext.Tuning` (scalar) replaced by `Stack<RenderTuning> TuningStack` + `ActiveTuning` accessor
- File-scope pragmas push the bottom frame (never popped); blocks push above; REPL boundary pops back to the bottom (D-12 + D-14 + Pitfall 2)
- Phase 23 D-13 advisory predicate updated to detect custom Scala tunings (Pitfall 6)
- Phase 23 sub-suite remains 100% GREEN — the structural refactor is observably-invisible to existing tests
- TuningStackFacts proves the new stack semantics
</success_criteria>

<output>
After completion, create `.planning/phases/32-full-scala-scl-tuning-loader/32-05-SUMMARY.md`.
</output>
