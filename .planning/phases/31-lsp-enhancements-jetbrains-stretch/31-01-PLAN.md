---
phase: 31-lsp-enhancements-jetbrains-stretch
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-cli/Commands/LspCommand.cs
  - flow-cli/Commands/CommandRegistry.cs
  - flow-lsp/Symbols/StdlibSymbolIndex.cs
  - flow-lang.Tests/Unit/Phase17/LspFixtures.cs
  - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md
autonomous: true
requirements: [SPEC-1, SPEC-2, SPEC-3, SPEC-4, SPEC-7]
must_haves:
  truths:
    - "`flow lsp` is a registered subcommand on the unified `flow` CLI and starts the LSP via stdio"
    - "StdlibSymbolIndex exposes ProcsForModule(moduleName) returning every StdProc whose Module matches"
    - "Phase 17 LspFixtures helper exposes StdlibIndex() so Phase 31 fact classes can build the index without recreating wiring"
    - "Plan-phase locked decisions D-11 (semicolon Option A) and D-12 (Unicode ellipsis re-confirmation) are recorded in 31-DECISIONS.md"
  artifacts:
    - path: "flow-cli/Commands/LspCommand.cs"
      provides: "`flow lsp` subcommand that delegates to FlowLsp.Program.Main"
      contains: "internal static class LspCommand"
    - path: "flow-cli/Commands/CommandRegistry.cs"
      provides: "LspCommand.Build() inserted into BuildAllCommands returning 12 subcommands"
      contains: "LspCommand.Build()"
    - path: "flow-lsp/Symbols/StdlibSymbolIndex.cs"
      provides: "ProcsForModule(string moduleName) reverse-lookup helper for UnusedImportAnalyzer + FilterByImports"
      contains: "public IEnumerable<StdProc> ProcsForModule"
    - path: "flow-lang.Tests/Unit/Phase17/LspFixtures.cs"
      provides: "StdlibIndex() helper accessible from Phase31 fact classes"
      contains: "public static StdlibSymbolIndex StdlibIndex"
    - path: ".planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md"
      provides: "Plan-phase locked decisions D-11 (semicolon Option A) and D-12 (Unicode `…` ellipsis re-confirmation per CONTEXT D-01)"
      contains: "D-11"
  key_links:
    - from: "flow-cli/Commands/CommandRegistry.cs"
      to: "flow-cli/Commands/LspCommand.cs"
      via: "LspCommand.Build() invocation in BuildAllCommands array"
      pattern: "LspCommand\\.Build\\(\\)"
    - from: "flow-cli/Commands/LspCommand.cs"
      to: "flow-lsp/Program.cs"
      via: "FlowLsp.Program.Main invocation from SetAction handler"
      pattern: "FlowLsp\\.Program\\.Main"
---

<objective>
Wave-0 scaffolding for Phase 31. Three pieces unblock every downstream plan:

1. **`flow lsp` subcommand** — resolves RESEARCH Pitfall 7 (binary discoverability from the JetBrains plugin). The plugin's `GeneralCommandLine("flow", "lsp")` factory wiring (Plan 31-08) needs this present; the Phase 30 unified `flow` CLI shipped 11 subcommands and does NOT include `lsp` — confirmed by `ls flow-cli/Commands/`.
2. **StdlibSymbolIndex.ProcsForModule helper** — reverse-lookup needed by `UnusedImportAnalyzer` (Plan 31-02) and `CompletionHandler.FilterByImports` (Plan 31-04). Without it both downstream plans would re-implement the same loop.
3. **31-DECISIONS.md** — records plan-phase locked decisions: D-11 (`;` Option A position-sensitive, mirrors `Note:` arm at SimpleLexer.cs:1144) and D-12 (re-confirms Unicode `…` U+2026 ellipsis per CONTEXT D-01; planning_context proposed ASCII but CONTEXT.md is authoritative).

Purpose: Zero file-conflicts in Wave 1; downstream plans (02, 04, 08) consume these scaffolds directly without exploring the codebase.

Output: `LspCommand.cs`, modified `CommandRegistry.cs`, extended `StdlibSymbolIndex.cs`, extended `LspFixtures.cs`, new `31-DECISIONS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-CONTEXT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md

<interfaces>
<!-- Extracted from existing codebase — executor uses these directly. -->

From flow-cli/Commands/ReplCommand.cs (closest analog):
- internal static class ReplCommand
- public static Command Build() — returns System.CommandLine.Command
- The Build() method declares Command name + description, attaches SetAction(parseResult => { /* delegate */ return 0; }), and returns the command.
- Pattern: thin wrapper that delegates to an entry point in another project.

From flow-cli/Commands/CommandRegistry.cs:
- internal static class CommandRegistry
- public static IEnumerable<Command> BuildAllCommands() — currently returns 11 commands (Run/Eval/Repl/Watch/Play/Render/Flow2Midi/Midi2Flow/Check/Version/New).
- Each row calls XxxCommand.Build().

From flow-lsp/Program.cs:
- Entry point shape matches OmniSharp.Extensions.LanguageServer.LanguageServer.From(...) wiring; called as `FlowLsp.Program.Main(Array.Empty<string>()).GetAwaiter().GetResult()` to delegate.
- Returns int exit code (Main returns Task<int> per OmniSharp pattern).

From flow-lsp/Symbols/StdlibSymbolIndex.cs:
- public sealed record StdProc(string Name, string Module, string FilePath)
- private readonly Dictionary<string, StdProc> _byName
- public StdProc? Find(string name)
- public static readonly string[] ModuleNames (or property exposing module names — used by FilterByImports `@std` transitivity)
- public IEnumerable<CompletionItem> Items() — existing surface

From flow-lang.Tests/Unit/Phase17/LspFixtures.cs:
- public static class LspFixtures
- public static ParseResult Parse(string source, string? path = null) — existing; uses new ParseSession().Parse(...)

From flow-lang.Tests/Unit/Phase17/HoverHandlerTests.cs:18-23 (the existing Indices() helper pattern):
- private static (BuiltInIndex bi, UserSymbolIndex ui, StdlibSymbolIndex si) Indices() — local helper that constructs the three indices.
- Phase 31 lifts this pattern up to LspFixtures for reuse.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `flow lsp` subcommand + register it</name>
  <files>flow-cli/Commands/LspCommand.cs, flow-cli/Commands/CommandRegistry.cs</files>
  <read_first>
    - flow-cli/Commands/ReplCommand.cs (analog — copy the static-class + Build() shape verbatim, swap name/description/action)
    - flow-cli/Commands/CommandRegistry.cs (insertion site; the BuildAllCommands array)
    - flow-lsp/Program.cs (verify the entry point signature; the SetAction handler invokes FlowLsp.Program.Main and returns its exit code)
    - flow-cli/flow-cli.csproj (verify a ProjectReference to ../flow-lsp/flow-lsp.csproj already exists; if not, the executor must add it — Phase 30 shipped flow-cli with references to flow-interpreter and flow-lang, plus flow-midi; flow-lsp reference may be new)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (LspCommand section + CommandRegistry section)
  </read_first>
  <action>
    Create `flow-cli/Commands/LspCommand.cs`. Declare `internal static class LspCommand` in namespace `FlowCli.Commands`. Implement a single `public static Command Build()` that constructs a `System.CommandLine.Command` named `"lsp"` with description `"Start the Flow Language Server (stdio LSP)"`. The Build method attaches a `SetAction(parseResult => ...)` handler that invokes `FlowLsp.Program.Main(Array.Empty<string>()).GetAwaiter().GetResult()` and returns the int result (or 0 if Main returns void; check Program.cs signature — if it returns `Task<int>` use .Result, if `Task` use .Wait() then return 0). Add `using FlowLsp;` and `using System.CommandLine;`.

    Modify `flow-cli/Commands/CommandRegistry.cs`: insert `LspCommand.Build(),` as the LAST row of the array returned by `BuildAllCommands()` (after `NewCommand.Build()`). Update the leading XML-doc / comment block to say "12 subcommands; Phase 31 added LspCommand for JetBrains plugin binary-discoverability per RESEARCH Pitfall 7".

    If `flow-cli/flow-cli.csproj` lacks a `<ProjectReference Include="..\flow-lsp\flow-lsp.csproj" />` line, add it under the same `<ItemGroup>` that holds the other ProjectReferences (Phase 30 wired flow-interpreter + flow-lang + flow-midi; flow-lsp is the new reference).

    Per D-10 + Pitfall 7: the JetBrains plugin's FlowLanguageServerFactory.kt (Plan 31-08) MUST use `GeneralCommandLine("flow", "lsp")` — this task is the prerequisite that makes that command resolvable.
  </action>
  <verify>
    <automated>dotnet build flow-cli/flow-cli.csproj /clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-cli/Commands/LspCommand.cs` exists and contains class `LspCommand` with method `Build`.
    - `flow-cli/Commands/CommandRegistry.cs` contains the literal token `LspCommand.Build()` in the BuildAllCommands array.
    - `dotnet build flow-cli/flow-cli.csproj` exits 0 with no errors.
    - `dotnet run --project flow-cli -- lsp --help` (or `flow lsp --help` after install) exits 0 and prints a help line containing "Start the Flow Language Server".
    - `flow-cli/flow-cli.csproj` contains a ProjectReference whose Include attribute ends in `flow-lsp\flow-lsp.csproj` or `flow-lsp/flow-lsp.csproj`.
  </acceptance_criteria>
  <done>`flow lsp` is invokable from the unified CLI; the JetBrains plugin (Plan 31-08) can spawn the server via `GeneralCommandLine("flow", "lsp")`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend StdlibSymbolIndex with ProcsForModule + extend LspFixtures with StdlibIndex helper</name>
  <files>flow-lsp/Symbols/StdlibSymbolIndex.cs, flow-lang.Tests/Unit/Phase17/LspFixtures.cs</files>
  <read_first>
    - flow-lsp/Symbols/StdlibSymbolIndex.cs (current shape — sealed record StdProc + Dictionary _byName + Find method)
    - flow-lang.Tests/Unit/Phase17/LspFixtures.cs (existing Parse helper)
    - flow-lang.Tests/Unit/Phase17/HoverHandlerTests.cs (Indices() helper pattern at lines 18-23 — lift into LspFixtures)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (StdlibSymbolIndex section)
  </read_first>
  <action>
    In `flow-lsp/Symbols/StdlibSymbolIndex.cs`, add a new public method `public IEnumerable<StdProc> ProcsForModule(string moduleName)` that iterates `_byName.Values` and yields every `StdProc` whose `Module == moduleName`. Add a triple-slash docblock referencing Phase 31 UnusedImportAnalyzer and CompletionHandler.FilterByImports as the consumers. No other changes to the class — Find / Items / ModuleNames stay as-is.

    In `flow-lang.Tests/Unit/Phase17/LspFixtures.cs`, add `public static StdlibSymbolIndex StdlibIndex() => new(new FlowLang.Runtime.ParseSession());` so Phase 31 fact classes can construct the index without rebuilding the local Indices() pattern. Keep the existing Parse helper.

    Both additions are additive; no existing call sites change.
  </action>
  <verify>
    <automated>dotnet build flow-lsp/flow-lsp.csproj /clp:ErrorsOnly &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj /clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public IEnumerable<StdProc> ProcsForModule" flow-lsp/Symbols/StdlibSymbolIndex.cs` returns 1.
    - `grep -c "public static StdlibSymbolIndex StdlibIndex" flow-lang.Tests/Unit/Phase17/LspFixtures.cs` returns 1.
    - `dotnet build flow-lsp/flow-lsp.csproj` exits 0.
    - `dotnet build flow-lang.Tests/flow-lang.Tests.csproj` exits 0.
    - No existing Phase17 test changes behavior (run `dotnet test --filter "FullyQualifiedName~Phase17"` — exits 0).
  </acceptance_criteria>
  <done>UnusedImportAnalyzer (Plan 31-02) can call `stdlib.ProcsForModule("harmony")`; Phase 31 fact classes call `LspFixtures.StdlibIndex()` without duplicating wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Record plan-phase locked decisions in 31-DECISIONS.md</name>
  <files>.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md</files>
  <read_first>
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-CONTEXT.md (existing D-01..D-10 — references for the new decisions)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md (Critical Decision section: Option A vs B vs C for semicolon)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (Migration Audit section: zero in-repo .flow files need migration under Option A)
  </read_first>
  <action>
    Create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md` with a header noting "Plan-phase decisions (locked at plan-phase per CONTEXT.md punt list)". Add two locked decisions:

    **D-11 [semicolon-comment-position]** — `;` as line comment is **position-sensitive (Option A)** per RESEARCH §Critical Decision. `;` at column-0 (with optional leading whitespace, gated by `IsStartOfLineContent()`) consumed as line comment to end-of-line by `SkipWhitespaceAndComments`. `;` mid-line remains `TokenType.Semicolon` and continues to terminate statements. Mirrors the existing `Note:` arm at SimpleLexer.cs:1144. RESEARCH grep audit confirms zero column-0 `;` exist in any in-repo `.flow` file across all 647 files — REQ-6 migration is zero files. This decision keeps backward compat with every shipped pragma (`enable hAsB;`) and typed declaration (`Int x = 5;`).

    **D-12 [varargs-ellipsis-character]** — Re-confirm Unicode horizontal ellipsis `…` (U+2026) per CONTEXT D-01. The planning_context orchestrator note proposed ASCII `...` as an alternative; CONTEXT.md is authoritative and locked Unicode. RESEARCH Pitfall 3 mitigated by populating `SignatureInformation.Parameters` with explicit `ParameterInformation` ranges (LspMappings.BuildParameters helper) instead of relying on byte-offset math inside the merged label string. Rendered format: `name: Type…` (D-02) — ellipsis trails the type, not the parameter name.

    Add a third section "Stretch-bar criterion (clarification — not a new decision)" restating REQ-7 acceptance: `flow-jetbrains/` scaffolding ALWAYS lands at phase closure (D-10); stretch IS-MET when the .zip builds AND a manual UAT in IntelliJ Community 2024.2+ shows completions on `examples/tutorial.flow`; stretch IS-DEFERRED if either gate fails — phase closure documents which.

    Close with "Plan-phase planner: Claude Opus 4.7 (1M context). Locked 2026-05-12."
  </action>
  <verify>
    <automated>test -f .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md &amp;&amp; grep -cE '^\*\*D-11' .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md &amp;&amp; grep -cE '^\*\*D-12' .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md` exists.
    - File contains the literal token `D-11` and the phrase `position-sensitive`.
    - File contains the literal token `D-12` and the literal Unicode character `…` (U+2026).
    - File references CONTEXT D-01 and CONTEXT D-10 by name.
    - File closes with a "Locked 2026-05-12" line.
  </acceptance_criteria>
  <done>Downstream plans (02 for SimpleLexer, 05 for varargs render) cite D-11 / D-12 explicitly; the user-decision audit trail is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → `flow` CLI | Untrusted argv; System.CommandLine sanitizes |
| `flow lsp` → flow-lsp Program.Main | Process boundary, but same process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | LspCommand.cs invokes wrong assembly | accept | Build-time linker resolves FlowLsp.Program.Main; no runtime dynamic discovery |
| T-31-01-02 | DoS | StdlibSymbolIndex.ProcsForModule unbounded yield | accept | _byName.Values is bounded by stdlib proc count (~100); linear walk fine |
| T-31-01-03 | Information disclosure | 31-DECISIONS.md leaks internal architecture | accept | Repo is open-source; planning artifacts are public by design |
</threat_model>

<verification>
- `dotnet build` succeeds for flow-cli, flow-lsp, and flow-lang.Tests.
- `dotnet test --filter "FullyQualifiedName~Phase17"` exits 0 (no regression in existing 16+ Phase 17 tests).
- `dotnet run --project flow-cli -- lsp --help` prints a help line.
- 31-DECISIONS.md exists and is well-formed.
</verification>

<success_criteria>
- 3 new code surfaces ship without breaking any existing test.
- D-11 and D-12 are locked in writing; downstream plans cite them by ID.
- The JetBrains stretch plan (31-08) can wire `GeneralCommandLine("flow", "lsp")` without further prerequisites.
</success_criteria>

<output>
After completion, create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-01-SUMMARY.md`
</output>
