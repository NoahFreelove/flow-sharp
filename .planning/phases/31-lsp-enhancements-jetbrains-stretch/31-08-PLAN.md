---
phase: 31-lsp-enhancements-jetbrains-stretch
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 04, 05, 06, 07]
files_modified:
  - flow-jetbrains/build.gradle.kts
  - flow-jetbrains/settings.gradle.kts
  - flow-jetbrains/gradle.properties
  - flow-jetbrains/.gitignore
  - flow-jetbrains/README.md
  - flow-jetbrains/src/main/resources/META-INF/plugin.xml
  - flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt
  - flow-jetbrains/gradle/wrapper/gradle-wrapper.properties
  - flow-jetbrains/gradlew
  - flow-jetbrains/gradlew.bat
autonomous: false
user_setup:
  - service: intellij-community
    why: "Manual UAT for SPEC-7 acceptance — install the built plugin .zip and open a .flow file to verify completions"
    dashboard_config:
      - task: "Install IntelliJ IDEA Community 2024.2 or later"
        location: "https://www.jetbrains.com/idea/download/"
requirements: [SPEC-7]
must_haves:
  truths:
    - "`flow-jetbrains/` directory exists with Gradle build files + plugin.xml + FlowLanguageServerFactory.kt — landed UNCONDITIONALLY per CONTEXT D-10"
    - "`cd flow-jetbrains && ./gradlew buildPlugin` exits 0 (or, if Gradle/JDK absent, documents a deferred status)"
    - "If gradlew buildPlugin succeeds, `flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip` exists"
    - "FlowLanguageServerFactory.kt spawns `flow lsp` via stdio per RESEARCH Pitfall 7 resolution + Plan 31-01 LspCommand"
    - "plugin.xml declares LSP4IJ 0.19.3 dependency + IntelliJ Platform 2024.2+ (sinceBuild 242)"
    - "Manual UAT result (stretch-met vs stretch-deferred) recorded by checkpoint task"
  artifacts:
    - path: "flow-jetbrains/build.gradle.kts"
      provides: "Gradle build with LSP4IJ 0.19.3 pin + IntelliJ Platform 2024.2 + Kotlin 1.9.25"
      contains: "com.redhat.devtools.lsp4ij:0.19.3"
    - path: "flow-jetbrains/src/main/resources/META-INF/plugin.xml"
      provides: "Plugin descriptor declaring Flow file type + LSP4IJ language server + language mapping"
      contains: "dev.flowlang.jetbrains"
    - path: "flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt"
      provides: "LanguageServerFactory implementation spawning `flow lsp` via OSProcessStreamConnectionProvider"
      contains: "GeneralCommandLine"
    - path: "flow-jetbrains/README.md"
      provides: "User-facing README documenting build + install + binary discoverability fallback + IntelliJ 2024.2+ requirement"
      contains: "FLOW_LSP_PATH"
  key_links:
    - from: "flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt"
      to: "flow-cli/Commands/LspCommand.cs"
      via: "GeneralCommandLine(\"flow\", \"lsp\") invokes the Phase 31 Plan 01 subcommand"
      pattern: "GeneralCommandLine"
    - from: "flow-jetbrains/src/main/resources/META-INF/plugin.xml"
      to: "flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt"
      via: "lsp4ij extension references factoryClass=dev.flowlang.jetbrains.FlowLanguageServerFactory"
      pattern: "FlowLanguageServerFactory"
---

<objective>
Ship SPEC-7 stretch: JetBrains plugin scaffolding via LSP4IJ. Per CONTEXT D-10, the scaffolding ALWAYS lands at phase closure regardless of whether the "builds + opens .flow with completions" stretch bar is met. Closure documents whether stretch is MET (plugin .zip attached to v1.4 release tag) or DEFERRED to v1.5 (scaffolding ready, demo failed).

Per CONTEXT D-09: LSP4IJ pin to `com.redhat.devtools.lsp4ij:0.19.3` (RESEARCH §Standard Stack verified version). IntelliJ Platform `2024.2` (sinceBuild `242`).
Per RESEARCH §Open Question 4: Kotlin package `dev.flowlang.jetbrains`.
Per RESEARCH §Pitfall 7: binary discoverability resolved by Plan 31-01's `flow lsp` subcommand — FlowLanguageServerFactory.kt invokes `GeneralCommandLine("flow", "lsp")`. Fallback: `FLOW_LSP_PATH` env var documented in README.

Stretch bar (REQ-7 acceptance):
- (a) all 6 mandatory areas (Plans 31-01..07) are GREEN — verified at this plan's start.
- (b) `gradlew buildPlugin` produces `flow-jetbrains/build/distributions/flow-jetbrains-*.zip`.
- (c) manual UAT: load the .zip into IntelliJ Community 2024.2+, open `examples/tutorial.flow`, trigger completion, verify flow-lsp items appear.

This plan has a checkpoint task for the manual UAT — autonomous: false. If the UAT passes, stretch is MET; if it fails, stretch is DEFERRED to v1.5 and the closure plan (31-09) documents that. The scaffolding files still ship — D-10 is unconditional.

Output: 6 new files in `flow-jetbrains/` (build files + plugin.xml + factory.kt + README) + Gradle wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-CONTEXT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-01-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-07-SUMMARY.md

<interfaces>
<!-- All scaffolding has NO in-repo analog. Use RESEARCH §6 + §Code Examples exemplars verbatim. -->

build.gradle.kts shape (RESEARCH §Code Examples lines 786-826):
- plugins: id("java"), id("org.jetbrains.kotlin.jvm") version "1.9.25", id("org.jetbrains.intellij.platform") version "2.2.0"
- group = "dev.flowlang"; version = "0.1.0"
- repositories: mavenCentral(); intellijPlatform { defaultRepositories() }
- dependencies.intellijPlatform: intellijIdeaCommunity("2024.2"); plugin("com.redhat.devtools.lsp4ij:0.19.3")
- kotlin { jvmToolchain(21) }
- tasks.withType JavaCompile { options.release.set(17) }
- intellijPlatform.pluginConfiguration.ideaVersion.sinceBuild = "242"

plugin.xml shape (RESEARCH lines 391-414):
- id=dev.flowlang.jetbrains, name=Flow Language, vendor=Flow Language, version=0.1.0
- idea-version since-build 242
- depends com.intellij.modules.platform + com.redhat.devtools.lsp4ij
- com.intellij fileType extension: name=Flow, language=Flow, extensions=flow
- com.redhat.devtools.lsp4ij server: id=flow, factoryClass=dev.flowlang.jetbrains.FlowLanguageServerFactory
- com.redhat.devtools.lsp4ij languageMapping: language=Flow, serverId=flow

FlowLanguageServerFactory.kt shape (RESEARCH lines 369-388):
- package dev.flowlang.jetbrains
- imports com.intellij.execution.configurations.GeneralCommandLine + com.intellij.openapi.project.Project + com.redhat.devtools.lsp4ij.LanguageServerFactory + com.redhat.devtools.lsp4ij.server.OSProcessStreamConnectionProvider + com.redhat.devtools.lsp4ij.server.StreamConnectionProvider
- class FlowLanguageServerFactory : LanguageServerFactory
- override fun createConnectionProvider(project: Project): StreamConnectionProvider returns OSProcessStreamConnectionProvider with commandLine = GeneralCommandLine("flow", "lsp")
- Respect FLOW_LSP_PATH env var if set: invoke GeneralCommandLine(System.getenv("FLOW_LSP_PATH"), "lsp") falling back to ("flow", "lsp")

settings.gradle.kts: one line — rootProject.name = "flow-jetbrains"
gradle.properties: standard JVM args + kotlin code style official
gradle/wrapper/gradle-wrapper.properties: Gradle 8.6 wrapper config
gradlew + gradlew.bat: standard Gradle wrapper scripts (gradle wrapper --gradle-version 8.6 generates)
.gitignore: build/, .gradle/, .idea/, *.iml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — re-run full unit suite + LSP smoke to confirm Plans 01-07 are GREEN</name>
  <files></files>
  <read_first>
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-07-SUMMARY.md (Plan 31-07 migration audit result)
  </read_first>
  <action>
    Pre-flight gate. The SPEC-7 stretch bar clause (a) requires all 6 mandatory areas GREEN before the JetBrains scaffolding even attempts to build. Confirm with:
    1. dotnet test --logger console verbosity=minimal — record total/passed/failed.
    2. cd vscode-extension and run npm run test:grammar — record exit status.
    3. Read 31-07-SUMMARY.md — verify the migration audit recorded zero collisions.

    If any step is RED, do NOT abort — D-10 says scaffolding lands unconditionally. Mark the stretch bar's clause (a) as FAILED in the SUMMARY and proceed to Tasks 2-3 (file creation). Task 4 (gradlew buildPlugin) and Task 5 (manual UAT) record the gate outcomes.
  </action>
  <verify>
    <automated>dotnet test --logger "console;verbosity=minimal" 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` exits with a clear pass/fail count printed.
    - 31-07-SUMMARY.md exists.
    - Pre-flight result (GREEN or partial) recorded in this plan's SUMMARY for closure consumption.
  </acceptance_criteria>
  <done>Stretch bar gate evaluated; outcome captured for the closure plan.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Gradle build infrastructure (build.gradle.kts, settings.gradle.kts, gradle.properties, wrapper, .gitignore, README)</name>
  <files>flow-jetbrains/build.gradle.kts, flow-jetbrains/settings.gradle.kts, flow-jetbrains/gradle.properties, flow-jetbrains/.gitignore, flow-jetbrains/README.md, flow-jetbrains/gradle/wrapper/gradle-wrapper.properties, flow-jetbrains/gradlew, flow-jetbrains/gradlew.bat</files>
  <read_first>
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (flow-jetbrains sections at lines 709-770)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md (Code Examples build.gradle.kts at lines 786-826; Environment Availability Gradle wrapper fallback at line 893)
  </read_first>
  <action>
    Create the flow-jetbrains directory. Inside it create:

    1. build.gradle.kts — Kotlin DSL Gradle build per RESEARCH §Code Examples lines 789-826. Use kotlin.jvm version 1.9.25 and org.jetbrains.intellij.platform version 2.2.0. Pin LSP4IJ to com.redhat.devtools.lsp4ij:0.19.3 (CONTEXT D-09). Pin intellijIdeaCommunity to 2024.2. JDK toolchain 21 with Java release target 17 (LSP4IJ baseline). Set sinceBuild to 242 inside intellijPlatform.pluginConfiguration.ideaVersion. Set group to dev.flowlang and version to 0.1.0.

    2. settings.gradle.kts — single line: rootProject.name = "flow-jetbrains"

    3. gradle.properties — two lines: org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8 then kotlin.code.style=official

    4. .gitignore — exclude build/, .gradle/, .idea/, *.iml

    5. README.md — explain (a) what the plugin is, (b) build command `./gradlew buildPlugin`, (c) install via IntelliJ Settings > Plugins > Install from disk, (d) requirements IntelliJ Community 2024.2+ and JDK 21 toolchain and flow binary on PATH, (e) FLOW_LSP_PATH env var fallback per RESEARCH Pitfall 7, (f) Phase 31 status note that scaffolding lands unconditionally per CONTEXT D-10 and stretch demo result is recorded in 31-VERIFICATION.md.

    6. gradle/wrapper/gradle-wrapper.properties — standard Gradle 8.6 wrapper config (distributionBase GRADLE_USER_HOME, distributionPath wrapper/dists, distributionUrl pointing at services.gradle.org distributions gradle-8.6-bin.zip, networkTimeout 10000, validateDistributionUrl true, zipStoreBase GRADLE_USER_HOME, zipStorePath wrapper/dists)

    7. gradlew + gradlew.bat — standard Gradle wrapper scripts. If gradle is on the host, run `cd flow-jetbrains; gradle wrapper --gradle-version 8.6` to generate them. If gradle is not on the host, copy them from a known-good Gradle 8.6 wrapper reference (standard boilerplate, has been stable for years). The shell script must be executable (chmod +x). Conventionally gradle-wrapper.jar is also committed alongside the .properties file — if `gradle wrapper` produces it, include it; otherwise document the missing jar in the README and recommend the user invoke `gradle wrapper --gradle-version 8.6` after cloning. Add the .jar to the SUMMARY files-modified list at task completion if it was generated.

    Verify each file's shape after writing by reading back its first 5-10 lines.
  </action>
  <verify>
    <automated>test -f flow-jetbrains/build.gradle.kts &amp;&amp; test -f flow-jetbrains/settings.gradle.kts &amp;&amp; test -f flow-jetbrains/gradle.properties &amp;&amp; test -f flow-jetbrains/README.md &amp;&amp; test -f flow-jetbrains/.gitignore &amp;&amp; test -f flow-jetbrains/gradle/wrapper/gradle-wrapper.properties &amp;&amp; test -x flow-jetbrains/gradlew &amp;&amp; grep -c "com.redhat.devtools.lsp4ij:0.19.3" flow-jetbrains/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - flow-jetbrains directory exists.
    - All 8 files listed in `files` exist.
    - `grep -c "com.redhat.devtools.lsp4ij:0.19.3" flow-jetbrains/build.gradle.kts` returns ≥ 1.
    - `grep -c "intellijIdeaCommunity(\"2024.2\")" flow-jetbrains/build.gradle.kts` returns 1.
    - `grep -c "sinceBuild = \"242\"" flow-jetbrains/build.gradle.kts` returns 1.
    - `grep -c "FLOW_LSP_PATH" flow-jetbrains/README.md` returns ≥ 1.
    - `test -x flow-jetbrains/gradlew` succeeds (gradlew has execute permission).
    - `cat flow-jetbrains/settings.gradle.kts` returns the single rootProject.name line.
  </acceptance_criteria>
  <done>Gradle build infrastructure lands; scaffolding is unconditional per D-10 regardless of whether Task 4's gradlew buildPlugin succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Create plugin.xml + FlowLanguageServerFactory.kt</name>
  <files>flow-jetbrains/src/main/resources/META-INF/plugin.xml, flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt</files>
  <read_first>
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (plugin.xml + FlowLanguageServerFactory.kt sections at lines 772-829)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md (Code Examples §6 lines 369-414)
  </read_first>
  <action>
    Create flow-jetbrains/src/main/resources/META-INF/plugin.xml per RESEARCH §Code Examples lines 391-414. Required elements: <id>dev.flowlang.jetbrains</id>, <name>Flow Language</name>, <vendor>Flow Language</vendor>, <version>0.1.0</version>, <idea-version since-build="242"/>, two <depends> entries (com.intellij.modules.platform and com.redhat.devtools.lsp4ij), and two <extensions> blocks — one for the com.intellij file type registration (name=Flow, language=Flow, extensions=flow, fieldName=INSTANCE pointing at LanguageFileType), one for the com.redhat.devtools.lsp4ij server registration (server id=flow, factoryClass=dev.flowlang.jetbrains.FlowLanguageServerFactory, plus a languageMapping element binding language=Flow to serverId=flow). Wrap server description text in CDATA to allow embedded characters.

    Create flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt per RESEARCH §Code Examples lines 369-388 with the Pitfall 7 binary-discovery fallback applied. Package declaration: dev.flowlang.jetbrains. Imports: com.intellij.execution.configurations.GeneralCommandLine, com.intellij.openapi.project.Project, com.redhat.devtools.lsp4ij.LanguageServerFactory, com.redhat.devtools.lsp4ij.server.OSProcessStreamConnectionProvider, com.redhat.devtools.lsp4ij.server.StreamConnectionProvider. Define `class FlowLanguageServerFactory : LanguageServerFactory` with one override:

    ```
    override fun createConnectionProvider(project: Project): StreamConnectionProvider {
        val flowPath = System.getenv("FLOW_LSP_PATH") ?: "flow"
        val cmd = GeneralCommandLine(flowPath, "lsp")
        return object : OSProcessStreamConnectionProvider() {
            init { commandLine = cmd }
        }
    }
    ```

    (Use Kotlin string-template or concatenation as preferred; the literal token GeneralCommandLine must appear in the source.) Add a class-level KDoc comment citing Phase 31 SPEC-7 / Plan 01 LspCommand / RESEARCH Pitfall 7 binary-discovery resolution.
  </action>
  <verify>
    <automated>test -f flow-jetbrains/src/main/resources/META-INF/plugin.xml &amp;&amp; test -f flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt &amp;&amp; grep -c "dev.flowlang.jetbrains" flow-jetbrains/src/main/resources/META-INF/plugin.xml &amp;&amp; grep -c "GeneralCommandLine" flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt</automated>
  </verify>
  <acceptance_criteria>
    - File flow-jetbrains/src/main/resources/META-INF/plugin.xml exists and is well-formed XML (verify with `xmllint --noout flow-jetbrains/src/main/resources/META-INF/plugin.xml` if xmllint is available; else assert via grep that opening and closing idea-plugin tags exist).
    - `grep -c "dev.flowlang.jetbrains" flow-jetbrains/src/main/resources/META-INF/plugin.xml` returns ≥ 2 (id + factoryClass).
    - `grep -c "since-build=\"242\"" flow-jetbrains/src/main/resources/META-INF/plugin.xml` returns 1.
    - `grep -c "com.redhat.devtools.lsp4ij" flow-jetbrains/src/main/resources/META-INF/plugin.xml` returns ≥ 1 (depends entry).
    - File flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt exists.
    - `grep -c "package dev.flowlang.jetbrains" flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt` returns 1.
    - `grep -c "class FlowLanguageServerFactory : LanguageServerFactory" flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt` returns 1.
    - `grep -c "GeneralCommandLine" flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt` returns ≥ 1.
    - `grep -c "FLOW_LSP_PATH" flow-jetbrains/src/main/kotlin/dev/flowlang/jetbrains/FlowLanguageServerFactory.kt` returns 1 (env-var fallback shipped).
  </acceptance_criteria>
  <done>Plugin descriptor + factory wired; LSP4IJ can load the plugin and spawn the flow LSP server via stdio.</done>
</task>

<task type="auto">
  <name>Task 4: Attempt gradlew buildPlugin and record the result</name>
  <files></files>
  <read_first>
    - flow-jetbrains/build.gradle.kts (verify the file exists and contains LSP4IJ pin)
    - flow-jetbrains/gradlew (must be executable)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md (Environment Availability §Gradle CLI line 893 — JDK 21 present, Gradle wrapper bootstraps Gradle 8.6 itself; Pitfall 6 § IntelliJ Platform baseline mismatch)
  </read_first>
  <action>
    Run `cd flow-jetbrains; ./gradlew buildPlugin` with a generous timeout (Gradle wrapper downloads ~150 MB Gradle distribution + ~500 MB IntelliJ Platform on first run; budget 10 minutes). Record:
    - Exit code.
    - Whether `flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip` was produced.
    - Stdout tail (last 50 lines) — capture in /tmp/gradle-build.log.

    If exit 0 AND the .zip exists: stretch bar clause (b) is MET. Record in the plan SUMMARY: "Build succeeded; .zip produced at flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip. Proceeding to Task 5 (manual UAT)."

    If exit != 0 OR no .zip: stretch bar clause (b) is FAILED. Record the failure mode (dependency resolution / IntelliJ Platform incompatibility / Gradle wrapper download failure / etc.) and the recommended remediation in the SUMMARY. Per D-10, do NOT delete the scaffolding — it stays for v1.5 follow-up. Task 5 (manual UAT) marks the stretch as DEFERRED.

    Common failure modes to document (from RESEARCH Pitfalls 6 + 7):
    - Pitfall 6: IntelliJ Platform Gradle Plugin 2.2.0 may require a newer JDK than the host has — if so, document JDK 21 as the minimum.
    - Pitfall 7: this task does NOT exercise the runtime binary-discovery path; that surfaces only in Task 5.
    - LSP4IJ artifact resolution failure — verify the Maven coords by reading flow-jetbrains/build.gradle.kts again; if `com.redhat.devtools.lsp4ij:0.19.3` is misspelled or the artifact path is wrong, fix and retry once.
  </action>
  <verify>
    <automated>cd flow-jetbrains &amp;&amp; (ls build/distributions/flow-jetbrains-*.zip 2&gt;/dev/null &amp;&amp; echo PASS || echo FAIL-DEFERRED)</automated>
  </verify>
  <acceptance_criteria>
    - The build was attempted (gradlew was invoked).
    - The result is recorded in the plan SUMMARY: either "PASS — .zip at flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip" or "FAIL — <failure mode>; deferred to v1.5".
    - flow-jetbrains/build.gradle.kts is unchanged from Task 2 (the build does not require build-file edits; if it does, that's a Task 2 bug that bubbles back).
    - If build succeeded: `test -f flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip` succeeds.
    - `grep -cE "(PASS|FAIL-DEFERRED)" .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-08-SUMMARY.md` returns ≥ 1 (Task 4 outcome recorded as a machine-parseable marker in the plan SUMMARY).
  </acceptance_criteria>
  <done>Stretch bar clause (b) evaluated and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual UAT — load plugin .zip in IntelliJ and verify completions on examples/tutorial.flow</name>
  <what-built>
    The plugin scaffolding at flow-jetbrains/ exists (Tasks 2-3). If Task 4 succeeded, flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip exists. The plugin wraps flow-lsp via LSP4IJ; when loaded into IntelliJ Community 2024.2+ it should auto-start `flow lsp` and serve completions on .flow files.
  </what-built>
  <how-to-verify>
    Skip this checkpoint if Task 4 reported FAIL — mark stretch as DEFERRED in the resume signal.

    If Task 4 reported PASS:
    1. Open IntelliJ IDEA Community 2024.2 or later (download from https://www.jetbrains.com/idea/download/ if not installed).
    2. Settings > Plugins > gear icon > Install Plugin from Disk... > select `flow-jetbrains/build/distributions/flow-jetbrains-0.1.0.zip` > Restart IntelliJ.
    3. After restart, open the project at the flow-sharp repo root (File > Open... > select the repository directory).
    4. Navigate to `examples/tutorial.flow` and open it.
    5. Verify the IntelliJ status bar shows the language server starting (look for an "LSP" status indicator from LSP4IJ).
    6. Click inside a `proc ... ()` body, after typing a space, press Ctrl+Space to trigger completion.
    7. Verify completions appear and include flow-lsp builtin items (e.g. `print`, `reverb`, `transpose`, `arpeggio`).
    8. Hover over `concat` (or any varargs builtin) and verify the tooltip shows the Unicode `…` ellipsis (the SPEC-3 / Plan 31-05 deliverable).
    9. Confirm at least one Phase 31 diagnostic surfaces: write `use "@harmony";` at the top of a temp file without using any @harmony proc — verify the Unused Import warning appears (the SPEC-1 / Plan 31-02 deliverable).

    Expected outcomes:
    - completions appear → stretch is MET
    - completions appear but Unicode ellipsis missing → stretch is MET partially; record a v1.5 follow-up to fix encoding
    - LSP fails to start (status bar shows error) → stretch is DEFERRED; check the IntelliJ Event Log for the LSP error message; common cause is `flow` not on PATH — try setting FLOW_LSP_PATH and reloading
    - No completions / IntelliJ hangs → stretch is DEFERRED; record the symptom
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "stretch met" — completions appeared, varargs ellipsis renders correctly
    - "stretch met partial: <description>" — completions appeared but a specific feature regressed (record for v1.5 follow-up)
    - "stretch deferred: <reason>" — UAT failed; record reason and defer the visible-demo bar to v1.5
    - "skip-stretch: Task 4 failed" — Task 4 already failed; recording deferral without further UAT
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntelliJ Plugin sandbox → flow-lsp child process | OSProcessStreamConnectionProvider spawns flow-lsp via GeneralCommandLine; JVM child-process boundary |
| FLOW_LSP_PATH env var → process command line | User-controlled binary path; trust comes from user's environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08-01 | Tampering | FLOW_LSP_PATH redirects to malicious binary | accept | User controls their own env var; this is a feature, not a vulnerability — matches Phase 30 `flow install` philosophy of trusting user PATH |
| T-31-08-02 | Information disclosure | flow-lsp child process inherits IntelliJ's environment | accept | Standard JVM child-process inheritance; no secrets in flow-lsp's normal operation |
| T-31-08-03 | DoS | gradlew buildPlugin downloads hundreds of MB on first run | accept | One-shot cost; subsequent builds use cached distributions in ~/.gradle |
| T-31-08-04 | Tampering | LSP4IJ plugin loads malicious dependencies via mavenCentral | accept | Standard supply-chain trust assumption for any Gradle build; the LSP4IJ pin to 0.19.3 limits the surface to the audited release |
</threat_model>

<verification>
- flow-jetbrains/ directory and all 10 files exist after Task 3.
- Task 4 result (PASS / FAIL) is recorded in plan SUMMARY.
- Task 5 manual UAT result (MET / DEFERRED) is recorded in plan SUMMARY.
- Phase mandatory work (Plans 31-01..07) is unaffected by this plan — no regression in the unit suite.
</verification>

<success_criteria>
- 10 new files exist under flow-jetbrains/ regardless of stretch outcome (D-10 unconditional).
- Stretch outcome (MET / PARTIAL / DEFERRED) is captured for the closure plan to surface in 31-VERIFICATION.md and the v1.4 release notes.
</success_criteria>

<output>
After completion, create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-08-SUMMARY.md` documenting all 5 tasks' outcomes including the stretch verdict.
</output>
