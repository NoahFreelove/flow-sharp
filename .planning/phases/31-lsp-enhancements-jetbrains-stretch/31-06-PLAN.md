---
phase: 31-lsp-enhancements-jetbrains-stretch
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - vscode-extension/syntaxes/flow.tmLanguage.json
  - vscode-extension/tests/grammar/comment-forms.flow
  - vscode-extension/tests/grammar/function-calls.flow
  - vscode-extension/tests/grammar/sample.flow.snap
  - vscode-extension/tests/grammar/comment-forms.flow.snap
  - vscode-extension/tests/grammar/function-calls.flow.snap
autonomous: true
requirements: [SPEC-4, SPEC-5]
must_haves:
  truths:
    - "VSCode opens a `.flow` file and shows `;` lines at column-0 with `comment.line.semicolon.flow` scope (typically gray)"
    - "VSCode shows `Note:` chapter-divider lines with `comment.line.documentation.flow` scope (typically teal/orange documentation color)"
    - "VSCode shows `TODO:` lines with `comment.line.todo.flow` scope (typically orange)"
    - "VSCode shows `FIXME:` lines with `comment.line.fixme.flow` scope (typically red)"
    - "VSCode visually distinguishes the head of `(funcName ...)` forms from bare identifier references (`entity.name.function.flow` vs `variable.other.flow`)"
    - "Music-specific patterns (chords, notes, types, booleans, keywords) keep their existing precedence — function-call match does NOT swallow them"
  artifacts:
    - path: "vscode-extension/syntaxes/flow.tmLanguage.json"
      provides: "Extended #comments repository node with 4 new patterns + new #function-call and #variable-ref repository nodes + updated top-level patterns array with function-call BEFORE variable-ref"
      contains: "entity.name.function.flow"
    - path: "vscode-extension/tests/grammar/comment-forms.flow"
      provides: "Fixture exercising `//` / `;` / `Note:` / `TODO:` / `FIXME:` lead-ins + `(print \"TODO: x\")` string-context regression"
      contains: "FIXME:"
    - path: "vscode-extension/tests/grammar/function-calls.flow"
      provides: "Fixture exercising function-call vs variable-ref positions"
      contains: "(print"
  key_links:
    - from: "vscode-extension/syntaxes/flow.tmLanguage.json"
      to: "vscode-extension/tests/grammar/comment-forms.flow.snap"
      via: "Regenerated snapshot pins every scope assignment for the new comment forms"
      pattern: "comment\\.line\\.(semicolon|todo|fixme|documentation)\\.flow"
    - from: "vscode-extension/syntaxes/flow.tmLanguage.json"
      to: "vscode-extension/tests/grammar/function-calls.flow.snap"
      via: "Regenerated snapshot pins function-call head positions"
      pattern: "entity\\.name\\.function\\.flow"
---

<objective>
Ship SPEC-4 (TextMate grammar side for the four new comment forms — the lexer side ships in Plan 31-03) and SPEC-5 (function-call coloring distinct from bare-identifier references).

Per D-06: standard TextMate scopes with `.flow` language suffix.
Per D-07: four comment scopes — `comment.line.semicolon.flow`, `comment.line.documentation.flow` (for `Note:`), `comment.line.todo.flow`, `comment.line.fixme.flow`.
Per D-08: `entity.name.function.flow` for `(funcName ...)` head positions; `variable.other.flow` for bare identifier references.

The `^\\s*` anchor mirrors the lexer's `IsStartOfLineContent()` gate — column-0 OR leading-whitespace-only.

Pitfall 4 mitigation: after editing `flow.tmLanguage.json`, MUST run `npm run test:grammar:update` to regenerate the 3 `.snap` files (existing sample.flow.snap + 2 new fixtures). Commit the regenerated snapshots.

Pitfall 2: theme inheritance — new scope-name strings will show in snapshot diff but user-visible color depends on the theme. Document in the SUMMARY that themes without explicit Flow rules inherit from `comment.line.*` parent scopes (universal).

Output: 1 modified grammar JSON + 2 new fixture .flow files + 3 regenerated .snap files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-CONTEXT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md
@vscode-extension/syntaxes/flow.tmLanguage.json
@vscode-extension/tests/grammar/sample.flow
@vscode-extension/tests/grammar/sample.flow.snap
@vscode-extension/package.json

<interfaces>
<!-- Extracted from existing codebase — executor uses these directly. -->

From vscode-extension/syntaxes/flow.tmLanguage.json:
- Top-level patterns array at lines 6-17 — current order: comments, strings, chords, notes, numbers, keywords, types, booleans, identifiers, operators, pipes
- `#comments` repository node at lines 19-26 — single pattern `comment.line.double-slash.flow` matching `//.*$`
- Music-specific patterns (chords/notes/types/booleans/keywords) keep their precedence in the top-level array — they each match more specific shapes than `[A-Za-z_]` so the new function-call/variable-ref patterns don't swallow them.

From vscode-extension/package.json:
- "scripts": { "test:grammar": "vscode-tmgrammar-snap ...", "test:grammar:update": "vscode-tmgrammar-snap --updateSnapshot ..." } — existing scripts (verify exact names by reading package.json)
- devDependencies include "vscode-tmgrammar-snap": "0.1.3" — already wired

From vscode-extension/tests/grammar/sample.flow + sample.flow.snap:
- Existing fixture + snapshot — Phase 31 regenerates sample.flow.snap because the new function-call/variable-ref scopes apply to sample.flow content (it has `(print` etc.)
- Snapshot format: each line annotated with `#^^^` ranges and the scope chain `source.flow <scope>.flow`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend flow.tmLanguage.json with 4 new comment scopes + function-call vs variable-ref patterns</name>
  <files>vscode-extension/syntaxes/flow.tmLanguage.json</files>
  <read_first>
    - vscode-extension/syntaxes/flow.tmLanguage.json (read in full — 105 lines; identify the #comments repository node and the top-level patterns array)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (flow.tmLanguage.json MODIFY at lines 585-647)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md (Code Examples §TextMate Grammar at lines 744-781)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md (D-11 references `^\\s*` anchor mirroring the lexer position-sensitive gate)
  </read_first>
  <action>
    Modify `vscode-extension/syntaxes/flow.tmLanguage.json`:

    **Extend the `#comments` repository node** to include 4 NEW patterns (in addition to the existing `//`):
    - `{ "name": "comment.line.double-slash.flow", "match": "//.*$" }` (existing — keep first)
    - `{ "name": "comment.line.semicolon.flow",     "match": "^\\s*;.*$" }` (per D-07)
    - `{ "name": "comment.line.todo.flow",          "match": "^\\s*TODO:.*$" }`
    - `{ "name": "comment.line.fixme.flow",         "match": "^\\s*FIXME:.*$" }`
    - `{ "name": "comment.line.documentation.flow", "match": "^\\s*Note:.*$" }`

    The `^\\s*` anchor mirrors the lexer's `IsStartOfLineContent()` gate. Order: keep `//` first; the others are anchored so order between them doesn't matter, but for readability place semicolon/todo/fixme/documentation in that order.

    **Add two new repository nodes** (siblings of `#comments`):
    - `"function-call": { "match": "\\b([A-Za-z_][A-Za-z0-9_]*)(?=\\s*\\()", "captures": { "1": { "name": "entity.name.function.flow" } } }`
    - `"variable-ref": { "match": "\\b[A-Za-z_][A-Za-z0-9_]*\\b", "name": "variable.other.flow" }`

    **Update the top-level `patterns` array** — insert `#function-call` and `#variable-ref` AFTER the music-specific patterns (chords, notes, types, booleans, keywords) but BEFORE the existing identifier-shaped fall-through patterns (if any) / operators / pipes. The exact final ordering (top-down precedence):
    1. `{ "include": "#comments" }`
    2. `{ "include": "#strings" }`
    3. `{ "include": "#chords" }`
    4. `{ "include": "#notes" }`
    5. `{ "include": "#numbers" }`
    6. `{ "include": "#keywords" }`
    7. `{ "include": "#types" }`
    8. `{ "include": "#booleans" }`
    9. `{ "include": "#function-call" }` (NEW — MUST precede #variable-ref)
    10. `{ "include": "#variable-ref" }` (NEW)
    11. `{ "include": "#operators" }`
    12. `{ "include": "#pipes" }`

    If the current grammar has a generic identifier pattern in the top-level patterns array, REPLACE it with `#function-call` + `#variable-ref` (or remove the generic identifier and rely on the two new patterns). Read the current file in full before deciding — the existing array has 11 patterns per RESEARCH; verify the actual list and adjust precisely.

    Do NOT modify `vscode-extension/language-configuration.json`. The `lineComment` field stays `//`. Grammar scope coloring (via tmLanguage.json) is sufficient for all 4 new comment forms — VSCode applies the comment scope to `;`, `Note:`, `TODO:`, `FIXME:` lines based on the grammar match alone, independent of language-configuration.json. Per the post-discuss revision (Option A), the "Toggle Line Comment" keyboard shortcut continues to insert `//`; SPEC-4 does NOT promise keyboard-toggle parity for the new forms, only visual coloring + lexer recognition.
  </action>
  <verify>
    <automated>cd vscode-extension &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('syntaxes/flow.tmLanguage.json'));console.log('json ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "comment.line.semicolon.flow" vscode-extension/syntaxes/flow.tmLanguage.json` returns ≥ 1.
    - `grep -c "comment.line.todo.flow" vscode-extension/syntaxes/flow.tmLanguage.json` returns ≥ 1.
    - `grep -c "comment.line.fixme.flow" vscode-extension/syntaxes/flow.tmLanguage.json` returns ≥ 1.
    - `grep -c "comment.line.documentation.flow" vscode-extension/syntaxes/flow.tmLanguage.json` returns ≥ 1.
    - `grep -c "entity.name.function.flow" vscode-extension/syntaxes/flow.tmLanguage.json` returns ≥ 1.
    - `grep -c "variable.other.flow" vscode-extension/syntaxes/flow.tmLanguage.json` returns ≥ 1.
    - `node -e "JSON.parse(require('fs').readFileSync('vscode-extension/syntaxes/flow.tmLanguage.json'));console.log('ok')"` exits 0 (valid JSON).
    - In flow.tmLanguage.json's top-level patterns array, `#function-call` appears BEFORE `#variable-ref` (verify by reading the file's patterns array order).
  </acceptance_criteria>
  <done>Grammar JSON valid, new scopes declared, ordering correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create comment-forms.flow + function-calls.flow fixtures and regenerate ALL grammar snapshots</name>
  <files>vscode-extension/tests/grammar/comment-forms.flow, vscode-extension/tests/grammar/function-calls.flow, vscode-extension/tests/grammar/sample.flow.snap, vscode-extension/tests/grammar/comment-forms.flow.snap, vscode-extension/tests/grammar/function-calls.flow.snap</files>
  <read_first>
    - vscode-extension/tests/grammar/sample.flow (existing fixture — copy its style)
    - vscode-extension/tests/grammar/sample.flow.snap (existing snapshot — understand the annotation format: each scope-bearing line annotated with `#^^^` ranges + scope chain)
    - vscode-extension/package.json (verify the `test:grammar:update` script name; verify devDeps include `vscode-tmgrammar-snap`)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (comment-forms.flow + function-calls.flow fixture sections at lines 650-706)
  </read_first>
  <action>
    **Create `vscode-extension/tests/grammar/comment-forms.flow`** with content exercising every comment form per PATTERNS.md lines 669-681:

    ```
    // Existing double-slash comment.
    ; Lisp-style line comment at column 0.
      ; Indented Lisp-style comment.
    Note: Documentation comment chapter divider.
    TODO: Fix the foo handling.
    FIXME: This is broken.
    proc main () {
        (print "TODO: this is a string, not a comment");
        Int x = 5;
    }
    ```

    The mix is deliberate: double-slash + 2 semicolon variants (column-0 + indented) + Note + TODO + FIXME + a string literal containing "TODO:" (regression for Pitfall 8 + SPEC-4 acceptance criterion "String-literal context unaffected").

    **Create `vscode-extension/tests/grammar/function-calls.flow`** per PATTERNS.md lines 691-700:

    ```
    proc demo () {
        Int x = 5;
        (print x);
        (mul x 2);
        Int y = (add x 3);
        x -> doubler;
    }
    ```

    Expected scope chain after regen: `print`, `mul`, `add`, `doubler` map to `entity.name.function.flow`; `x`, `y` map to `variable.other.flow`; `Int` keeps `storage.type.flow` (existing); `proc` / `demo` follow existing keyword + identifier conventions (`demo` is the proc name — verify against current grammar whether proc names get a different scope; if not, it falls to `variable.other.flow` per the new fallthrough).

    **Run `npm run test:grammar:update` in `vscode-extension/`** to regenerate:
    - `vscode-extension/tests/grammar/sample.flow.snap` (existing fixture — re-snap because the new grammar applies new scopes to existing tokens like `print` and `proc`)
    - `vscode-extension/tests/grammar/comment-forms.flow.snap` (NEW — first generation)
    - `vscode-extension/tests/grammar/function-calls.flow.snap` (NEW — first generation)

    Commit all three regenerated snapshot files. Inspect the diff on sample.flow.snap manually to confirm the new scope assignments make semantic sense (function-call positions now show `entity.name.function.flow`; bare identifiers now show `variable.other.flow`; existing chord/note/keyword/type matches preserved).

    After regen, run `npm run test:grammar` to verify the snapshots are clean (no diffs).
  </action>
  <verify>
    <automated>cd vscode-extension &amp;&amp; npm run test:grammar 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `vscode-extension/tests/grammar/comment-forms.flow` exists and contains every literal token: `//`, `;`, `Note:`, `TODO:`, `FIXME:`.
    - File `vscode-extension/tests/grammar/function-calls.flow` exists and contains `(print`, `(mul`, `(add`, `-> doubler`.
    - File `vscode-extension/tests/grammar/comment-forms.flow.snap` exists.
    - File `vscode-extension/tests/grammar/function-calls.flow.snap` exists.
    - `grep -c "comment.line.semicolon.flow" vscode-extension/tests/grammar/comment-forms.flow.snap` returns ≥ 1.
    - `grep -c "comment.line.todo.flow" vscode-extension/tests/grammar/comment-forms.flow.snap` returns ≥ 1.
    - `grep -c "comment.line.fixme.flow" vscode-extension/tests/grammar/comment-forms.flow.snap` returns ≥ 1.
    - `grep -c "comment.line.documentation.flow" vscode-extension/tests/grammar/comment-forms.flow.snap` returns ≥ 1.
    - `grep -c "entity.name.function.flow" vscode-extension/tests/grammar/function-calls.flow.snap` returns ≥ 3 (print, mul, add — possibly doubler).
    - `grep -c "variable.other.flow" vscode-extension/tests/grammar/function-calls.flow.snap` returns ≥ 1 (x, y).
    - `cd vscode-extension && npm run test:grammar` exits 0 (snapshots are clean).
    - The string-literal regression: `comment-forms.flow.snap` shows the `"TODO: this is a string"` token scoped as `string.quoted.double.flow` (or whatever the existing string scope is) — NOT as `comment.line.todo.flow`.
  </acceptance_criteria>
  <done>SPEC-4 + SPEC-5 closed (grammar side); snapshots regenerated and clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .flow source → VSCode tokenizer | Untrusted text; TextMate regex execution sandboxed by VSCode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-01 | DoS | Pathological regex `^\\s*` causes catastrophic backtracking | accept | Anchored to line start; `\\s*` followed by literal token is non-greedy in TextMate engines; no nested alternation; standard pattern shape from Rust grammar reference. |
| T-31-06-02 | Information disclosure | New scopes leak language internals to extension users | accept | Public scope naming convention; user-facing colorization is the desired UX. |
| T-31-06-03 | Tampering | Stale snapshots committed alongside grammar change → CI fails | mitigate | Pitfall 4 — Task 2 explicitly regenerates ALL three snapshots and runs `npm run test:grammar` as the final acceptance check. |
</threat_model>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('vscode-extension/syntaxes/flow.tmLanguage.json'))"` exits 0.
- `cd vscode-extension && npm run test:grammar` exits 0.
- All three snapshot files exist + contain expected new scope strings.
- comment-forms.flow.snap shows `"TODO: this is a string"` as a string token, not a comment (Pitfall 8 regression).
</verification>

<success_criteria>
- Grammar JSON validly extends `#comments` with 4 new patterns + adds `#function-call` and `#variable-ref` repository nodes.
- Top-level patterns array places #function-call before #variable-ref AFTER music-specific patterns.
- 2 new fixture .flow files + 3 regenerated .snap files commit clean.
- `npm run test:grammar` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-06-SUMMARY.md`
</output>
