---
phase: 31-lsp-enhancements-jetbrains-stretch
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - flow-lsp/Diagnostics/UnusedImportAnalyzer.cs
  - flow-lsp/Diagnostics/UnreachableSectionAnalyzer.cs
  - flow-lsp/Diagnostics/ShadowedVariableAnalyzer.cs
  - flow-lsp/Diagnostics/ScaleLintAnalyzer.cs
  - flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs
  - flow-lang/Lexing/PragmaRegistry.cs
  - flow-lang.Tests/Unit/Phase31/UnusedImportAnalyzerFacts.cs
  - flow-lang.Tests/Unit/Phase31/UnreachableSectionAnalyzerFacts.cs
  - flow-lang.Tests/Unit/Phase31/ShadowedVariableAnalyzerFacts.cs
  - flow-lang.Tests/Unit/Phase31/ScaleLintDefaultOnFacts.cs
autonomous: true
requirements: [SPEC-1]
must_haves:
  truths:
    - "Composer's editor shows a Warning-severity squiggle on an unreferenced `use \"@harmony\"` import"
    - "Composer's editor shows an Information-severity squiggle on a defined-but-never-referenced `section` declaration"
    - "Composer's editor shows a Warning-severity squiggle on a nested-scope `VariableDeclaration` whose name matches an outer-scope declaration"
    - "Composer's editor shows Information-severity squiggles on non-diatonic notes inside `key { ... }` blocks even when `enable scaleLint;` is NOT declared (D-03 default-on)"
    - "The `enable scaleLint;` pragma still parses without error and is a silent no-op for v1.3 backward compat"
  artifacts:
    - path: "flow-lsp/Diagnostics/UnusedImportAnalyzer.cs"
      provides: "Static UnusedImportAnalyzer.Analyze that emits Warning per unreferenced ImportStatement"
      contains: "public static class UnusedImportAnalyzer"
    - path: "flow-lsp/Diagnostics/UnreachableSectionAnalyzer.cs"
      provides: "Static UnreachableSectionAnalyzer.Analyze emitting Information per orphan SectionDeclaration"
      contains: "public static class UnreachableSectionAnalyzer"
    - path: "flow-lsp/Diagnostics/ShadowedVariableAnalyzer.cs"
      provides: "Static ShadowedVariableAnalyzer.Analyze emitting Warning per nested-shadow VariableDeclaration"
      contains: "public static class ShadowedVariableAnalyzer"
    - path: "flow-lsp/Diagnostics/ScaleLintAnalyzer.cs"
      provides: "D-03 promotion — Phase 24 pragma-gate removed; analyzer always runs"
      contains: "Source = \"flow.scaleLint\""
    - path: "flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs"
      provides: "BuildAll routes ALL five diagnostic sources (parse, scaleLint, unusedImport, unreachableSection, shadowedVariable) through one publish call"
      contains: "UnusedImportAnalyzer.Analyze"
  key_links:
    - from: "flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs"
      to: "flow-lsp/Diagnostics/UnusedImportAnalyzer.cs"
      via: "BuildAll calls UnusedImportAnalyzer.Analyze(ast, tokens, source, stdlib)"
      pattern: "UnusedImportAnalyzer\\.Analyze"
    - from: "flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs"
      to: "flow-lsp/Diagnostics/UnreachableSectionAnalyzer.cs"
      via: "BuildAll calls UnreachableSectionAnalyzer.Analyze"
      pattern: "UnreachableSectionAnalyzer\\.Analyze"
    - from: "flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs"
      to: "flow-lsp/Diagnostics/ShadowedVariableAnalyzer.cs"
      via: "BuildAll calls ShadowedVariableAnalyzer.Analyze"
      pattern: "ShadowedVariableAnalyzer\\.Analyze"
---

<objective>
Ship SPEC-1: four diagnostic surfaces. Three NEW analyzers (UnusedImport Warning, UnreachableSection Information, ShadowedVariable Warning) plus D-03 promotion of ScaleLintAnalyzer to default-on (pragma-gate removed; pragma stays accepted as a no-op).

Every analyzer follows the Phase 24 D-04 "analyzer-per-diagnostic-type" pattern verbatim: `public static class XxxAnalyzer` with one public `Analyze` method returning `IReadOnlyList<Diagnostic>`, dotted source string `"flow.<analyzerKind>"`, charitable fail-open on malformed AST.

Purpose: Surface real composer mistakes (unused imports, dead sections, accidental shadows) and stop forcing composers to remember `enable scaleLint;` for the lint they almost always want.

Output: 3 new analyzer files + 1 modified analyzer (ScaleLint) + 1 modified orchestrator (CombinedDiagnosticsPublisher) + 1 modified registry (PragmaRegistry description) + 4 new fact classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-CONTEXT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-01-SUMMARY.md
@flow-lsp/Diagnostics/ScaleLintAnalyzer.cs
@flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs
@flow-lang/Ast/Statements/ImportStatement.cs
@flow-lang/Ast/Statements/SectionDeclaration.cs
@flow-lang/Ast/Statements/VariableDeclaration.cs

<interfaces>
<!-- Extracted from existing codebase — executor uses these directly. -->

From flow-lsp/Diagnostics/ScaleLintAnalyzer.cs (the exact analog pattern):
- public static class ScaleLintAnalyzer
- public static IReadOnlyList<Diagnostic> Analyze(FlowProgram ast, IReadOnlyList<Token> tokens, string source)
- Pragma-gate (lines 42-43): `if (!ast.Pragmas.Has("scaleLint")) return Array.Empty<Diagnostic>();` — DELETE these two lines for D-03.
- WalkStatements pattern (lines 50-78): recursive helper that descends MusicalContextStatement.Body, SectionDeclaration.Body, ProcDeclaration.Body.
- Diagnostic construction (lines 286-300): builds new Diagnostic { Severity, Source = "flow.scaleLint", Message, Range = new Range(new Position(line0, col0), new Position(line0, col0 + width)) }.

From flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs:
- public static class CombinedDiagnosticsPublisher
- public static IReadOnlyList<Diagnostic> BuildAll(ParseResult result, string source) — current signature (extend with StdlibSymbolIndex stdlib parameter)
- The instance Publish method calls PublishDiagnostics UNCONDITIONALLY (no `if (merged.Count > 0)` guard) — preserve this contract (empty publish is the only way to clear stale squiggles on a URI).
- Class docblock at lines 23-28 documents the unconditional-publish contract.

From flow-lang/Ast/Statements/ImportStatement.cs:
- Record with Location (SourceLocation), FilePath (string e.g. `"@harmony"`)
- ExtractModuleName helper needed in UnusedImportAnalyzer: `"@harmony"` → `"harmony"` (strip the `@` prefix; handle bare names + relative paths charitably — return raw filename for non-@ paths so they don't match any stdlib module and are conservatively kept).

From flow-lang/Ast/Statements/SectionDeclaration.cs:
- Record with Location, Name (string), Body (IReadOnlyList<Statement>)

From flow-lang/Ast/Expressions/SongExpression.cs (or similar):
- Song = [intro verse chorus] — each token is a section reference. UnreachableSectionAnalyzer needs to collect the referenced names from SongExpression nodes that appear as ExpressionStatement bodies (or as RHS of VariableDeclaration assignments).

From flow-lang/Ast/Statements/VariableDeclaration.cs:
- Record with Location, Type (FlowType), Name (string), Value (Expression)

From flow-lsp/LspMappings.cs:
- public static Range ToRange(SourceLocation loc) — existing helper used by ScaleLintAnalyzer; reuse for new analyzers.

From flow-lsp/Symbols/StdlibSymbolIndex.cs (post-Plan 31-01):
- public sealed record StdProc(string Name, string Module, string FilePath)
- public StdProc? Find(string name)
- public IEnumerable<StdProc> ProcsForModule(string moduleName) — NEW from Plan 31-01

From flow-lang.Tests/Unit/Phase17/LspFixtures.cs (post-Plan 31-01):
- public static ParseResult Parse(string source, string? path = null)
- public static StdlibSymbolIndex StdlibIndex() — NEW from Plan 31-01

From flow-lang/Lexing/PragmaRegistry.cs:
- public static class PragmaRegistry
- KnownPragmas dictionary line 16-24 — "scaleLint" entry at line 23. Keep the entry (recognized pragma) but update its description to reflect D-03's no-op semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship UnusedImportAnalyzer + UnreachableSectionAnalyzer + ShadowedVariableAnalyzer (3 new files + 3 test fact classes)</name>
  <files>flow-lsp/Diagnostics/UnusedImportAnalyzer.cs, flow-lsp/Diagnostics/UnreachableSectionAnalyzer.cs, flow-lsp/Diagnostics/ShadowedVariableAnalyzer.cs, flow-lang.Tests/Unit/Phase31/UnusedImportAnalyzerFacts.cs, flow-lang.Tests/Unit/Phase31/UnreachableSectionAnalyzerFacts.cs, flow-lang.Tests/Unit/Phase31/ShadowedVariableAnalyzerFacts.cs</files>
  <read_first>
    - flow-lsp/Diagnostics/ScaleLintAnalyzer.cs (analog — class shape, Analyze signature, WalkStatements recursion, Diagnostic construction)
    - flow-lang.Tests/Unit/Phase24/ScaleLintAnalyzerFacts.cs (test class shape — namespace, imports, [Fact] pattern, LspFixtures.Parse usage)
    - flow-lang/Ast/Statements/ImportStatement.cs, SectionDeclaration.cs, VariableDeclaration.cs (AST shapes — Location, Name, Body fields)
    - flow-lang/Ast/Expressions/SongExpression.cs (for UnreachableSectionAnalyzer — how section names appear in Song expressions)
    - flow-lsp/LspMappings.cs (ToRange helper — pass SourceLocation through to LSP Range)
    - flow-lsp/Symbols/StdlibSymbolIndex.cs (Plan 31-01 added ProcsForModule)
    - flow-lang.Tests/Unit/Phase17/LspFixtures.cs (Plan 31-01 added StdlibIndex helper)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (lines 43-167 — the three analyzer sections + shared patterns)
  </read_first>
  <behavior>
    - UnusedImportAnalyzer: given `use "@harmony";\nproc main () { (print "hi") }` returns exactly 1 Diagnostic with Severity=Warning, Source="flow.unusedImport".
    - UnusedImportAnalyzer: given `use "@harmony";\nChord c = (arpeggio Cmaj 4);` returns 0 Diagnostics (arpeggio is from @harmony — usage detected).
    - UnusedImportAnalyzer: given `use "@std";` (no body) returns 0 Diagnostics IFF @std's transitive imports are NOT in use anywhere (charitable: @std imports many modules; if even one is referenced, the import is "used"). Edge case: keep @std flagged only if NOTHING from any transitive module is referenced.
    - UnreachableSectionAnalyzer: given `section intro { ... }\nSong s = [verse]` returns 1 Diagnostic for `intro` with Severity=Information, Source="flow.unreachableSection".
    - UnreachableSectionAnalyzer: given `section intro { ... }\nSong s = [intro verse]` returns 0 Diagnostics for `intro` (referenced in Song).
    - ShadowedVariableAnalyzer: given `Int x = 1;\nproc f () { Int x = 2; }` returns 1 Diagnostic on the inner `x` with Severity=Warning, Source="flow.shadowedVariable", Message containing "shadows declaration at line 1".
    - ShadowedVariableAnalyzer: given `Int x = 1;\nInt y = 2;` returns 0 Diagnostics (different names, same scope).
    - All three analyzers: malformed/null AST returns Array.Empty<Diagnostic>() (charitable fail-open per Phase 24 D-22 precedent).
    - All three analyzers: no exceptions propagate to caller.
  </behavior>
  <action>
    Create the three analyzer files in `flow-lsp/Diagnostics/`. Each is `public static class XxxAnalyzer` in namespace `FlowLsp.Diagnostics`, exposes one `public static IReadOnlyList<Diagnostic> Analyze(FlowProgram ast, IReadOnlyList<Token> tokens, string source [, StdlibSymbolIndex stdlib])` method. Mirror ScaleLintAnalyzer.cs's class docblock — "Pure read-only AST + token traversal; never throws, never publishes" — adapted to each analyzer's purpose. Cite Phase 31 D-03/D-05 for the source-string convention.

    **UnusedImportAnalyzer** — takes extra `StdlibSymbolIndex stdlib` parameter. Algorithm:
    1. Collect every `ImportStatement` from `ast.Statements.OfType<ImportStatement>()`.
    2. Collect every identifier referenced anywhere in the AST (walk every `FunctionCallExpression.FunctionName`, `VariableExpression.Name`, `MemberAccessExpression.MemberName`) into a `HashSet<string>`.
    3. For each import, extract the module name from `import.FilePath` (helper `ExtractModuleName`: strip `@` prefix; if no `@`, return the raw filename without extension as a defensive fallback).
    4. Special case for `@std`: it transitively imports several modules — if any proc from any transitively-imported module is referenced, the @std import is "used". Use `StdlibSymbolIndex.ModuleNames` (existing static field on StdlibSymbolIndex per PATTERNS.md line 277) to expand `@std` to its child modules.
    5. Get `stdlib.ProcsForModule(moduleName)` for the import's module; if any of those procs' Names appear in the referenced-names set, the import is "used" — skip emitting a diagnostic.
    6. Else emit `new Diagnostic { Severity = Warning, Source = "flow.unusedImport", Message = $"Unused import: \"{import.FilePath}\"", Range = LspMappings.ToRange(import.Location) }`.

    **UnreachableSectionAnalyzer** — takes the standard 3-arg signature. Algorithm:
    1. Walk all statements (using a recursive helper that descends into MusicalContextStatement.Body, SectionDeclaration.Body, ProcDeclaration.Body) collecting `(name → location)` of every `SectionDeclaration`.
    2. Walk every `ExpressionStatement` and `VariableDeclaration.Value` looking for `SongExpression` nodes. For each `SongExpression`, collect referenced section names (the section-reference tokens — including `name*N` repeat-syntax — the underlying name token feeds the referenced-set).
    3. For each defined section whose name is NOT in the referenced-set, emit `new Diagnostic { Severity = Information, Source = "flow.unreachableSection", Message = $"Section '{name}' is defined but never referenced in any Song", Range = LspMappings.ToRange(location) }`.

    **ShadowedVariableAnalyzer** — takes the standard 3-arg signature. Algorithm:
    1. Maintain a `Stack<Dictionary<string, SourceLocation>> scopes` (each frame = one scope).
    2. Recursive walker over statements. On entry to `MusicalContextStatement.Body`, `SectionDeclaration.Body`, or `ProcDeclaration.Body` push a new scope; on exit pop.
    3. For each `VariableDeclaration` encountered: walk the stack from outer→current; if `Name` matches any OUTER frame's entry, emit `new Diagnostic { Severity = Warning, Source = "flow.shadowedVariable", Message = $"Variable '{name}' shadows declaration at line {outerLine}, column {outerCol}", Range = LspMappings.ToRange(location) }`. Then add `Name → location` to the current frame.
    4. Same-scope re-declaration is NOT shadowing — only nested-scope counts.

    Each analyzer wraps its body in `try { ... } catch { return Array.Empty<Diagnostic>(); }` for the charitable fail-open contract.

    **Test fact classes** in `flow-lang.Tests/Unit/Phase31/`: namespace `FlowLang.Tests.Unit.Phase31`, import `FlowLang.Tests.Unit.Phase17` for LspFixtures, import `FlowLsp.Diagnostics`. Each Facts class has at least 3 [Fact] tests per behavior listed above (positive: emits diagnostic with correct severity/source; negative: emits zero; edge: malformed AST returns empty). Test names follow `Behavior_Setup_AssertOutcome` shape from Phase 24 precedent.
  </action>
  <verify>
    <automated>dotnet build flow-lsp/flow-lsp.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31.UnusedImport|FullyQualifiedName~Phase31.UnreachableSection|FullyQualifiedName~Phase31.ShadowedVariable" --logger "console;verbosity=minimal"</automated>
  </verify>
  <acceptance_criteria>
    - Files `flow-lsp/Diagnostics/UnusedImportAnalyzer.cs`, `UnreachableSectionAnalyzer.cs`, `ShadowedVariableAnalyzer.cs` exist.
    - Each file `grep -c "public static class .*Analyzer"` returns 1.
    - Each file `grep -c "public static IReadOnlyList<Diagnostic> Analyze"` returns 1.
    - `grep -c "Source = \"flow.unusedImport\"" flow-lsp/Diagnostics/UnusedImportAnalyzer.cs` returns ≥ 1.
    - `grep -c "Source = \"flow.unreachableSection\"" flow-lsp/Diagnostics/UnreachableSectionAnalyzer.cs` returns ≥ 1.
    - `grep -c "Source = \"flow.shadowedVariable\"" flow-lsp/Diagnostics/ShadowedVariableAnalyzer.cs` returns ≥ 1.
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31.UnusedImport"` exits 0 with ≥ 3 tests run.
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31.UnreachableSection"` exits 0 with ≥ 3 tests run.
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31.ShadowedVariable"` exits 0 with ≥ 3 tests run.
    - Each analyzer file body contains a try/catch returning `Array.Empty<Diagnostic>()` on exception (charitable fail-open assertion).
  </acceptance_criteria>
  <done>SPEC-1 partial: three new analyzers ship with passing fact coverage; CombinedDiagnosticsPublisher wiring still pending in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote ScaleLintAnalyzer to default-on (D-03) + update PragmaRegistry description</name>
  <files>flow-lsp/Diagnostics/ScaleLintAnalyzer.cs, flow-lang/Lexing/PragmaRegistry.cs, flow-lang.Tests/Unit/Phase31/ScaleLintDefaultOnFacts.cs</files>
  <read_first>
    - flow-lsp/Diagnostics/ScaleLintAnalyzer.cs (current pragma-gate at lines 42-43 — DELETE these)
    - flow-lang.Tests/Unit/Phase24/ScaleLintAnalyzerFacts.cs (existing facts — most will still pass; the "pragma must be present" facts need rewiring)
    - flow-lang/Lexing/PragmaRegistry.cs (KnownPragmas entry for "scaleLint" at line 23)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (ScaleLintAnalyzer MODIFY section at lines 216-229)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md (D-03 / D-04 references)
  </read_first>
  <behavior>
    - Given `key Cmajor { | C4 D4 E4 F#4 G4 | }` WITHOUT `enable scaleLint;` — emits exactly 1 Information diagnostic on F#4 with Source="flow.scaleLint" (D-03 default-on).
    - Given `enable scaleLint;\nkey Cmajor { | C4 D4 E4 F#4 G4 | }` — emits exactly 1 Information diagnostic on F#4 (pragma is now a no-op, behavior identical).
    - Given `enable scaleLint;` followed by no key block — emits 0 diagnostics (no key context → analyzer is silent per Phase 24 D-22).
    - Existing Phase 24 facts that asserted "no pragma → zero diagnostics" must be either updated or removed: those that pin LINT-02's opt-in semantics are obsolete under D-03. Migrate Phase 24 facts that pin "with pragma, F#4 flags" — those continue to pass.
    - Existing Phase 21 `PragmaIsolationFacts` should not regress (the pragma still parses cleanly).
  </behavior>
  <action>
    In `flow-lsp/Diagnostics/ScaleLintAnalyzer.cs`:
    1. **Delete** lines 42-43 (the `if (!ast.Pragmas.Has("scaleLint")) return Array.Empty<Diagnostic>();` short-circuit).
    2. Update the class-level docblock (lines 14-28) to add a note that Phase 24 D-19 (pragma-required) is superseded by Phase 31 D-03; the pragma now parses as a recognized no-op per Phase 31 D-03. Preserve the historical Phase 24 D-22 silent-on-unrecognized-key precedent — that stays.
    3. The `Source = "flow.scaleLint"` string stays UNCHANGED per Phase 31 D-05.

    In `flow-lang/Lexing/PragmaRegistry.cs`:
    1. Update line 23's `"scaleLint"` description to: `"Phase 31 D-03: scale-lint is now default-on; this pragma is accepted as a no-op for v1.3 backward compat."`. The KEY stays `"scaleLint"` (recognized pragma). The closed-set rejection of unknown pragma names (Phase 21 D-12 error) is unchanged.

    In `flow-lang.Tests/Unit/Phase24/`: identify any fact that asserts "absence of pragma → zero scale-lint diagnostics" (LINT-02 opt-in pin). These are now obsolete; either delete the obsolete fact or migrate it to assert the inverse — "absence of pragma → diagnostic STILL emits" (the D-03 default-on contract). Choose migration over deletion when feasible — preserves the line-coverage trail. Do NOT touch Phase 21 PragmaIsolation facts (those test pragma propagation, not scaleLint activation).

    Create `flow-lang.Tests/Unit/Phase31/ScaleLintDefaultOnFacts.cs` with ≥ 4 [Fact] tests:
    1. `ScaleLint_DefaultOn_NoPragma_EmitsInformationOnNonDiatonic` — `key Cmajor { | C4 F#4 | }` → 1 Information diagnostic on F#4.
    2. `ScaleLint_DefaultOn_WithPragma_StillEmits_PragmaIsNoOp` — `enable scaleLint;\nkey Cmajor { | C4 F#4 | }` → 1 Information diagnostic on F#4 (identical to no-pragma case).
    3. `ScaleLint_DefaultOn_NoKeyBlock_EmitsZero` — `| C4 F#4 |` (no key context) → 0 diagnostics (D-22 silent-on-no-key precedent preserved).
    4. `ScaleLint_DefaultOn_PragmaStillParses_RegistryAccepts` — assert `PragmaRegistry.KnownPragmas.ContainsKey("scaleLint")` returns true; assert `enable scaleLint;` parses without error (use existing Phase 21 fixture pattern).
  </action>
  <verify>
    <automated>dotnet build flow-lsp/flow-lsp.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31.ScaleLintDefaultOn|FullyQualifiedName~Phase24.ScaleLint|FullyQualifiedName~Phase21.PragmaIsolation" --logger "console;verbosity=minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ast.Pragmas.Has(\"scaleLint\")" flow-lsp/Diagnostics/ScaleLintAnalyzer.cs` returns 0 (the short-circuit is deleted).
    - `grep -c "Phase 31 D-03" flow-lsp/Diagnostics/ScaleLintAnalyzer.cs` returns ≥ 1 (docblock cites the supersession).
    - `grep "scaleLint" flow-lang/Lexing/PragmaRegistry.cs` returns ≥ 1 line containing "no-op".
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31.ScaleLintDefaultOn"` exits 0 with ≥ 4 tests run.
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase21.PragmaIsolation"` exits 0 (regression: pragma isolation still works).
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase24"` exits 0 (Phase 24 facts pass after the LINT-02 obsolete-facts migration).
  </acceptance_criteria>
  <done>ScaleLintAnalyzer fires by default; pragma is silent no-op; Phase 24 facts migrated; Phase 31 default-on facts pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the 4 analyzers into CombinedDiagnosticsPublisher.BuildAll and Program.cs DI</name>
  <files>flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs</files>
  <read_first>
    - flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs (current BuildAll at line 48; Publish method's unconditional-publish contract at lines 23-28)
    - flow-lsp/Program.cs (DI registration — find the existing call site that invokes CombinedDiagnosticsPublisher; the BuildAll signature changes from `BuildAll(result, source)` to `BuildAll(result, source, stdlib)` so any caller must pass StdlibSymbolIndex)
    - flow-lsp/DocumentManager.cs (onParse callback — invokes Publish; needs StdlibSymbolIndex available)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (CombinedDiagnosticsPublisher MODIFY section at lines 169-213)
  </read_first>
  <action>
    Modify `flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs`:
    1. Change `BuildAll` signature to `public static IReadOnlyList<Diagnostic> BuildAll(ParseResult result, string source, StdlibSymbolIndex stdlib)`.
    2. Inside BuildAll: call all five analyzers in order — parse errors via DiagnosticsPublisher.BuildDiagnostics(result.Errors), then ScaleLintAnalyzer.Analyze (no pragma gate per Task 2), then UnusedImportAnalyzer.Analyze (passing stdlib), then UnreachableSectionAnalyzer.Analyze, then ShadowedVariableAnalyzer.Analyze.
    3. Replace the early-return short-circuit `if (parseDiags.Count == 0 && lintDiags.Count == 0) return Array.Empty<Diagnostic>();` with a five-way merge (do NOT preserve the short-circuit — the new analyzers might fire even when parseDiags + lintDiags are empty).
    4. The merged List<Diagnostic> capacity hint should be `parseDiags.Count + lintDiags.Count + unusedDiags.Count + unreachDiags.Count + shadowDiags.Count`. AddRange each in order. Return the merged list.
    5. **Preserve** the unconditional-publish contract in the instance `Publish` method (lines 23-28 docblock + the implementation). The instance Publish must continue to call `_server.TextDocument.PublishDiagnostics` UNCONDITIONALLY even when merged.Count == 0 — that empty-publish is the only way to clear stale squiggles on a URI.

    Modify `flow-lsp/Program.cs` (or DocumentManager.cs / wherever the onParse callback invokes Publish): pass the StdlibSymbolIndex instance into the call chain. The DI container already constructs StdlibSymbolIndex (per Phase 17); inject it into the publisher's owner. If Publish is invoked as a method on an instance that already has access to StdlibSymbolIndex via DI, the change is just `Publish(uri, ParseResult result, string source)` → `Publish(uri, ParseResult result, string source)` internally calling `BuildAll(result, source, _stdlib)`. Document the DI flow in a brief code comment.

    Note: do NOT introduce IUnusedImportPublisher / IUnreachableSectionPublisher / IShadowedVariablePublisher interfaces. Per RESEARCH §Open Questions answer #3, the three new analyzers are invoked via direct static calls in BuildAll. DI symmetry can wait for v1.5.
  </action>
  <verify>
    <automated>dotnet build flow-lsp/flow-lsp.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase17" --logger "console;verbosity=minimal"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BuildAll(ParseResult result, string source, StdlibSymbolIndex stdlib)" flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs` returns 1.
    - `grep -c "UnusedImportAnalyzer.Analyze" flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs` returns 1.
    - `grep -c "UnreachableSectionAnalyzer.Analyze" flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs` returns 1.
    - `grep -c "ShadowedVariableAnalyzer.Analyze" flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs` returns 1.
    - `grep -c "ScaleLintAnalyzer.Analyze" flow-lsp/Diagnostics/CombinedDiagnosticsPublisher.cs` returns 1 (still wired; D-03 just removed the internal pragma-gate).
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase17"` exits 0 (regression — Phase 17 publisher tests still pass with the new BuildAll signature, possibly via signature-update in those tests).
    - `dotnet test flow-lang.Tests` exits 0 (full suite green).
  </acceptance_criteria>
  <done>SPEC-1 closed: composer sees all four diagnostic severity levels emitted through one publish call per URI per parse cycle; LSP `textDocument/publishDiagnostics` notification carries the merged Diagnostic array.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .flow source → flow-lsp parser → analyzer chain | Untrusted text already trusted by user; existing ErrorReporter soft-fail model |
| Analyzer → CombinedDiagnosticsPublisher → LSP wire | Internal; per-URI publish replaces prior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | DoS | Malicious .flow with deeply nested scopes causes ShadowedVariableAnalyzer stack overflow | mitigate | Charitable fail-open: try/catch in Analyze returns Array.Empty<Diagnostic>() on exception. ShadowedVariableAnalyzer uses iterative scope stack (Stack<Dictionary>) — same depth as parser; if parser handles it, analyzer does. |
| T-31-02-02 | DoS | UnusedImportAnalyzer walks every node — O(n) over AST | accept | Linear; matches ScaleLintAnalyzer cost; 150ms debounce already in place. |
| T-31-02-03 | Tampering | Analyzer exception crashes LSP server | mitigate | Each Analyze() body wraps in try/catch; CombinedDiagnosticsPublisher.Publish's outer wrapper terminates the publish but does NOT crash the server (preserved from Phase 24). |
| T-31-02-04 | Information disclosure | ScaleLint default-on surprises composers with cascade of Info squiggles | accept | Editor-side opt-out documented (CONTEXT D-04); Pitfall 5 anticipates and v1.4 release notes will announce the flip. |
</threat_model>

<verification>
- `dotnet build flow-lsp/flow-lsp.csproj` exits 0.
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase31"` exits 0 with ≥ 13 tests run (4 UnusedImport + 3 UnreachableSection + 3 ShadowedVariable + 4 ScaleLintDefaultOn).
- `dotnet test flow-lang.Tests` full suite exits 0 (no regressions).
- Phase 18/25/27/28 ByteIdentical*Tests stay GREEN (analyzers are LSP-only and don't touch flow-lang determinism).
</verification>

<success_criteria>
- 3 new analyzer files exist with public-static-class shape matching ScaleLintAnalyzer.
- ScaleLintAnalyzer activates without `enable scaleLint;` pragma per D-03.
- CombinedDiagnosticsPublisher.BuildAll routes all 5 sources through one publish.
- Diagnostic source strings: "flow.unusedImport", "flow.unreachableSection", "flow.shadowedVariable", "flow.scaleLint" (preserved) — let editors filter independently per Phase 24 D-18.
- All Phase 31 fact classes pass; Phase 17/21/24 facts continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-02-SUMMARY.md`
</output>
