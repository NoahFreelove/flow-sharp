---
phase: 31-lsp-enhancements-jetbrains-stretch
plan: 09
type: execute
wave: 5
depends_on: [02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md
  - .planning/phases/17-flow-language-server/17-HUMAN-UAT.md
autonomous: false
requirements: [SPEC-1, SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6, SPEC-7]
must_haves:
  truths:
    - "REQUIREMENTS.md adds a Phase 31 traceability section mapping SPEC-1..SPEC-7 to plans 31-01..31-08 with commit SHAs"
    - "ROADMAP.md Phase 31 entry is marked complete with the SPEC-7 stretch verdict (MET or DEFERRED) recorded inline"
    - "STATE.md reflects Phase 31 closure; Current Position advances to Phase 32 (Full Scala loader) or the next milestone-defined phase"
    - "31-VERIFICATION.md exists pinning every SPEC-1..SPEC-7 acceptance criterion with empirical evidence"
    - "17-HUMAN-UAT.md rows 1-3 marked CLOSED (manual smoke completed during VSCode dev-host UAT in this plan); rows 4-5 stay DEFERRED to v1.5 per CONTEXT/SPEC out-of-scope decision"
  artifacts:
    - path: ".planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md"
      provides: "Empirical acceptance evidence for all 7 SPEC requirements + stretch verdict"
      contains: "SPEC-7"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 31 traceability section appended after Phase 30 section"
      contains: "Phase 31"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/31-lsp-enhancements-jetbrains-stretch/"
      via: "Phase 31 section references the 9 plans by SUMMARY filename + commit SHA"
      pattern: "31-(01|02|03|04|05|06|07|08|09)-SUMMARY"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "Both reference Phase 31 as Complete with stretch verdict"
      pattern: "Phase 31.*(Complete|Shipped)"
---

<objective>
Phase 31 closure. Three docs-only updates + one manual smoke for Phase 17 HUMAN-UAT rows 1-3 (orthogonal carry-overs that close inside this phase per the planning context).

1. **REQUIREMENTS.md** — append a "v1.4 Phase 31" traceability section mapping SPEC-1..SPEC-7 to plans 31-01..31-08 with commit SHAs (mirror the Phase 30 anchor pattern at REQUIREMENTS.md lines 204-222).
2. **ROADMAP.md** — mark Phase 31 entry complete; record the SPEC-7 stretch verdict (MET vs DEFERRED) inline; update the per-phase plan-list with all 9 plans + their statuses.
3. **STATE.md** — flip `status` from `phase-31-planning-paused` to `phase-31-complete` (or whatever the next-phase indicator should be); update Current Position; record post-completion metrics (tests run, tests passed, full suite green, byte-identical contracts preserved).
4. **31-VERIFICATION.md** — new file, pins every SPEC-1..SPEC-7 acceptance criterion with empirical evidence (test-name + pass/fail + git-SHA).
5. **17-HUMAN-UAT.md** — update rows 1-3 to CLOSED (manual smoke in VSCode dev-host); rows 4-5 stay DEFERRED to v1.5.

Manual checkpoint: VSCode dev-host smoke. Composer opens `examples/tutorial.flow` in the dev-host (existing `vscode-extension/.vscode/launch.json`), verifies completions reflect imports + pragmas, verifies diagnostics at correct severity, verifies new comment forms colorize. This is the SPEC §Acceptance Criteria "Manual LSP smoke" item and closes Phase 17 HUMAN-UAT rows 1-3.

Output: 5 doc files updated/created; phase officially shipped; STATE points at Phase 32 or the next-phase as appropriate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-CONTEXT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-01-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-02-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-03-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-04-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-05-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-06-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-07-SUMMARY.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-08-SUMMARY.md
@.planning/phases/17-flow-language-server/17-HUMAN-UAT.md

<interfaces>
<!-- Doc patterns to follow. -->

REQUIREMENTS.md Phase-30 anchor pattern at lines 204-222:
- Header "## v1.4 Phase 30 — <name>" followed by a brief intro paragraph
- Table with columns REQ | Phase | Status (use SPEC-N column for Phase 31 to match the SPEC naming convention)
- Reference plan SUMMARY filenames + commit SHAs in the Status column

ROADMAP.md Phase-30 update pattern (look at the Phase-30 entry — `## v1.4 Phase 30: Flow CLI + Formal Install`):
- Goal / Depends on / Requirements / Success Criteria / Plans list
- Mark plans as `[x] 30-NN-PLAN.md — <description> — Shipped <commit-sha>`
- After all plans ship, the phase header gets `— SHIPPED YYYY-MM-DD` appended

STATE.md fields to update (frontmatter at top of file):
- status: change from "phase-31-planning-paused" to "phase-31-complete"
- stopped_at / last_updated: today's date
- last_activity: brief description of Phase 31 closure
- progress.completed_phases: increment
- progress.completed_plans: add 9 (the 9 Phase 31 plans)
- progress.percent: recompute

17-HUMAN-UAT.md rows 1-3 are the in-scope rows per the planning_context. The file's structure follows a row-per-UAT-step pattern; mark rows 1-3 as CLOSED with the in-this-phase manual-smoke date; mark rows 4-5 as DEFERRED to v1.5.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: VSCode dev-host manual smoke (closes Phase 17 HUMAN-UAT rows 1-3)</name>
  <what-built>
    The full Phase 31 LSP enhancement surface: 3 new diagnostic analyzers (Plan 31-02), 3 completion filters (Plan 31-04), varargs Unicode ellipsis render (Plan 31-05), 4 new comment forms in lexer + grammar (Plans 31-03 + 31-06), function-call vs variable-ref grammar split (Plan 31-06).
  </what-built>
  <how-to-verify>
    1. Open the flow-sharp repo in VSCode.
    2. Launch the LSP dev host: F5 (or "Run Extension" from the Run and Debug panel) — uses the existing vscode-extension/.vscode/launch.json wiring.
    3. In the dev-host window, open `examples/tutorial.flow`.
    4. **Diagnostics check (SPEC-1):**
       - Look for any squiggle on a `key Cmajor { ... }` block with a non-diatonic note — should appear as Information severity (D-03 default-on, no pragma needed).
       - Add `use "@harmony";` at the top of a temp file without using any @harmony proc — should appear as a Warning underline on the import.
       - Add `Int x = 1; proc f () { Int x = 2; }` — should appear as a Warning on the inner `x`.
    5. **Completion filter check (SPEC-2):**
       - Inside a `proc main ()` body with NO imports, trigger Ctrl+Space — verify that `arpeggio` does NOT appear (it's from @harmony).
       - Add `use "@harmony";` at top; trigger completion again — verify `arpeggio` NOW appears.
       - Inside a `| C4 ... |` note stream with no pragma, trigger completion — verify no `H4` / `H5` suggestions appear.
       - Add `enable hAsB;` at top; verify `H4` / `H5` now appear in note-stream completions.
       - Inside a `key Cmajor { ... }` block, trigger completion — verify roman-numeral completions (I, IV, V7, vi) bubble to the top of the list (sortText boost).
    6. **Varargs render check (SPEC-3):**
       - Hover over `concat` somewhere in the source — verify the tooltip shows `concat(String…)` with the Unicode ellipsis (NOT three ASCII dots).
       - Type `(concat "a" |` and trigger signature help (Ctrl+Shift+Space) — verify the signature popup shows the Unicode ellipsis.
    7. **Comment form colorization check (SPEC-4 + SPEC-5):**
       - Verify chapter-divider `Note:` lines in tutorial.flow render with documentation color (typically teal/orange).
       - Add a `;` line comment at column 0 — verify it colorizes as a comment.
       - Add `TODO: foo` and `FIXME: bar` at column 0 — verify TODO renders orange and FIXME renders red (theme-dependent — verify at minimum that they colorize as comments, not as identifiers).
       - Verify `(print ...)` calls now visually distinguish `print` (entity.name.function.flow) from `x` (variable.other.flow) — typically a different color hue.
    8. Use the VSCode command "Developer: Inspect Editor Tokens and Scopes" on each new construct to confirm the scope chain matches expectation.
    9. **Phase 17 HUMAN-UAT rows 1-3 close:** these correspond to manual smoke items "completions reflect what's imported", "diagnostics surface", "comment forms colorize" — all covered by the steps above.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "smoke pass" — every SPEC check works as described; rows 1-3 of Phase 17 HUMAN-UAT close.
    - "smoke partial: <list of failed checks>" — specific checks fail; record per check whether the failure is a regression that needs a follow-up plan or an acceptable v1.5 carry-over.
    - "smoke fail: <reason>" — the dev host doesn't launch; record the symptom and propose a remediation plan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create 31-VERIFICATION.md with SPEC-1..SPEC-7 empirical evidence</name>
  <files>.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md (Acceptance Criteria section — 15+ pass/fail criteria)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-01-SUMMARY.md through 31-08-SUMMARY.md (each plan's outcome)
    - Look for prior VERIFICATION.md exemplars: `.planning/phases/30-flow-cli-formal-install/30-VERIFICATION.md` (if it exists — check first) OR `.planning/phases/28-midi-audio-polyphony-articulation-rewrite/28-VERIFICATION.md` for the structural template.
  </read_first>
  <action>
    Create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md`. Structure:

    - **Header:** "Phase 31 — LSP Enhancements + JetBrains Stretch — Verification"
    - **Closure date:** today's date + git SHA (run `git rev-parse HEAD`)
    - **Summary table:** one row per SPEC-1..SPEC-7 with columns SPEC | Plan(s) | Status (PASS/PARTIAL/DEFERRED) | Evidence (test names + commit SHAs).
    - **Per-SPEC sections:**
      - **SPEC-1 — Structured diagnostic severity expansion:** evidence = Plans 31-02 test counts (UnusedImport ≥ 3 / UnreachableSection ≥ 3 / ShadowedVariable ≥ 3 / ScaleLintDefaultOn ≥ 4); cite the file paths of the analyzers + the CombinedDiagnosticsPublisher.BuildAll signature change.
      - **SPEC-2 — Context-aware completion filtering:** evidence = Plan 31-04 CompletionFilterFacts ≥ 7 tests pass; cite the FilterByImports / FilterByPragmas / BoostByMusicalContext static helpers.
      - **SPEC-3 — Varargs visibility:** evidence = Plan 31-05 VarargsRenderingFacts ≥ 6 tests pass; cite the LspMappings.FormatSignature + BuildParameters helpers + the U+2026 character presence in source.
      - **SPEC-4 — Grammar enhancements (comment forms):** evidence = Plan 31-03 Phase31LexerCommentFormsTests ≥ 8 tests pass + Plan 31-06 grammar snapshot regen clean; cite D-11 lock.
      - **SPEC-5 — Grammar enhancement (function-call coloring):** evidence = Plan 31-06 function-calls.flow.snap shows entity.name.function.flow + variable.other.flow scopes; npm run test:grammar exits 0.
      - **SPEC-6 — Lexer migration of in-repo v1.3 fixtures:** evidence = Plan 31-07 31-MIGRATION-AUDIT.md zero collisions + full unit suite green + Phase 18/25/27/28 ByteIdentical*Tests pass.
      - **SPEC-7 — JetBrains plugin stretch:** evidence = Plan 31-08 outcome — record MET / PARTIAL / DEFERRED per the manual UAT result. If MET: cite the .zip artifact path + IntelliJ build it loaded under. If DEFERRED: cite the scaffolding files that landed unconditionally per D-10 + the v1.5 follow-up plan.
    - **VSCode dev-host smoke:** record Task 1's result. Phase 17 HUMAN-UAT rows 1-3 status (CLOSED if smoke passed).
    - **Phase 17 HUMAN-UAT carry-over:** rows 4-5 (VSCode Marketplace publish + OpenVSX publish) remain DEFERRED to v1.5 per SPEC out-of-scope and CONTEXT D-* (matching Round 1 decision).
    - **Cross-phase regression:** state that Phase 18/25/27/28 ByteIdentical tests + Phase 17/21/24 regression suite are GREEN.
    - **Locked plan-phase decisions:** restate D-11 (semicolon Option A) + D-12 (Unicode `…` re-confirm).
    - **Closure verdict:** PHASE 31 SHIPPED date + git SHA. If SPEC-7 is DEFERRED, the verdict is "Phase 31 SHIPPED 2026-MM-DD; SPEC-7 stretch deferred to v1.5 — scaffolding ready in flow-jetbrains/."
  </action>
  <verify>
    <automated>test -f .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md &amp;&amp; grep -c "SPEC-7" .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-VERIFICATION.md` exists.
    - Each of SPEC-1, SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6, SPEC-7 has its own section with empirical evidence (test names or commit SHAs or artifact paths).
    - The Closure verdict line records a PHASE 31 SHIPPED date.
    - The SPEC-7 stretch outcome is explicitly recorded as MET / PARTIAL / DEFERRED.
    - Phase 17 HUMAN-UAT rows 1-3 status is recorded (CLOSED if Task 1 smoke passed).
  </acceptance_criteria>
  <done>Empirical verification document committed; downstream phases can cite Phase 31's SHIPPED status.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md + ROADMAP.md + STATE.md + 17-HUMAN-UAT.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/17-flow-language-server/17-HUMAN-UAT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Phase 30 anchor at lines 204-222 — the template for the new Phase 31 section)
    - .planning/ROADMAP.md lines 340-352 (the Phase 31 entry — currently shows placeholder content per planning_context note "older success criteria with marketplace publish")
    - .planning/STATE.md frontmatter (status, last_updated, last_activity, progress.*)
    - .planning/phases/17-flow-language-server/17-HUMAN-UAT.md (5 rows — rows 1-3 close in this phase, rows 4-5 defer to v1.5)
  </read_first>
  <action>
    **REQUIREMENTS.md:** Append a new section after Phase 30's anchor (after line 222). Format:

    ```
    ## v1.4 Phase 31 — LSP Enhancements + JetBrains Stretch

    Phase 31 closes four flow-lsp gaps + adds JetBrains plugin scaffolding via LSP4IJ. v1.3 fixtures migrated under the new lexer (zero-collision audit per RESEARCH). SPEC-7 stretch verdict recorded inline.

    SPEC-IDs map 1:1 to .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md requirements 1-7.

    | REQ | Phase | Status |
    |-----|-------|--------|
    | SPEC-1 (Structured diagnostic severity — 3 new analyzers + ScaleLint default-on) | Phase 31 | Shipped {commit-sha-from-Plan-02} |
    | SPEC-2 (Context-aware completion filtering — 3 filters) | Phase 31 | Shipped {commit-sha-from-Plan-04} |
    | SPEC-3 (Varargs visibility — Unicode U+2026 ellipsis) | Phase 31 | Shipped {commit-sha-from-Plan-05} |
    | SPEC-4 (Grammar enhancements — 4 new comment forms) | Phase 31 | Shipped {commit-sha-from-Plan-03 + 31-06} |
    | SPEC-5 (Grammar enhancement — function-call coloring) | Phase 31 | Shipped {commit-sha-from-Plan-06} |
    | SPEC-6 (Lexer migration of in-repo v1.3 fixtures) | Phase 31 | Shipped {commit-sha-from-Plan-07} (zero collisions per audit) |
    | SPEC-7 (JetBrains plugin stretch) | Phase 31 | {MET/PARTIAL/DEFERRED-to-v1.5} per Plan 31-08 |
    ```

    Use real commit SHAs by running `git log --oneline -20` and identifying the SHAs that correspond to each plan's shipped commits. If a single plan ships across multiple commits, list them.

    **ROADMAP.md:** Find the Phase 31 section at line 340. Update:
    - The Goal sentence — current text mentions "VSCode extension to the Marketplace + OpenVSX" which is now out-of-scope per the SPEC Round 1 decision. Rewrite the Goal to match the SPEC: "Close four flow-lsp gaps (diagnostics expansion, context-aware completion, varargs visibility, grammar enhancements) and add JetBrains plugin scaffolding via LSP4IJ. Marketplace publish deferred to v1.5."
    - Update Requirements line: `**Requirements**: SPEC-1, SPEC-2, SPEC-3, SPEC-4, SPEC-5, SPEC-6, SPEC-7`
    - Update Success Criteria to match the SPEC (drop the "VSCode extension published to Marketplace" criterion; replace with "VSCode dev-host manual smoke closes Phase 17 HUMAN-UAT rows 1-3"). Drop the "(Stretch) JetBrains plugin published to JetBrains Marketplace" item — replaced by "(Stretch) JetBrains plugin scaffolding lands per CONTEXT D-10; if all 6 mandatory areas GREEN, the built .zip is attached to v1.4 release tag".
    - Update Plans list to enumerate all 9 plans with `[x] 31-NN-PLAN.md — <description> — Shipped <commit-sha>`.
    - Append `— SHIPPED <today>` to the section header.

    **STATE.md:** Update the frontmatter:
    - `status:` change to `phase-31-complete` (or whatever the next Phase 32-pre-spec indicator is per the GSD convention used in prior phase closures)
    - `stopped_at:` "Phase 31 closure complete; SPEC-1..SPEC-6 fully shipped; SPEC-7 stretch {MET/DEFERRED}"
    - `last_updated:` today's date in ISO 8601
    - `last_activity:` "Phase 31 shipped — LSP diagnostic expansion + completion filters + varargs Unicode ellipsis + new comment forms + grammar function-call coloring + JetBrains plugin scaffolding. Next: Phase 32 (Full Scala loader) spec-phase."
    - `progress.completed_phases:` increment by 1
    - `progress.completed_plans:` increment by 9 (the 9 Phase 31 plans)
    - `progress.percent:` recompute against the new total

    Update the Current Position section: advance to "Phase: 32 (full-scala-loader) — NOT STARTED" (or whatever the next phase per ROADMAP is). Add an entry to the "Phase performance metrics" table for each of Phase 31's 9 plans with approximate duration + task count + file count (best estimate from each plan's SUMMARY).

    **17-HUMAN-UAT.md:** Update rows 1-3 to "CLOSED — Phase 31 dev-host smoke completed <today>"; leave rows 4-5 marked "DEFERRED to v1.5 — Marketplace publisher account not set up per Phase 31 SPEC Round 1 decision."
  </action>
  <verify>
    <automated>grep -c "v1.4 Phase 31" .planning/REQUIREMENTS.md &amp;&amp; grep -c "SPEC-7" .planning/REQUIREMENTS.md &amp;&amp; grep -c "phase-31-complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains a new section header `## v1.4 Phase 31 — LSP Enhancements + JetBrains Stretch`.
    - REQUIREMENTS.md contains rows for SPEC-1 through SPEC-7 with status entries.
    - ROADMAP.md Phase 31 section has every plan marked `[x]` with a commit SHA.
    - ROADMAP.md Phase 31 success criteria do NOT contain "Marketplace publish" (it's out of scope per SPEC).
    - STATE.md frontmatter `status` is `phase-31-complete` (or equivalent next-phase indicator).
    - STATE.md `progress.completed_plans` increased by 9.
    - 17-HUMAN-UAT.md rows 1-3 are marked CLOSED.
    - 17-HUMAN-UAT.md rows 4-5 are still marked DEFERRED (no false closure).
  </acceptance_criteria>
  <done>All planning docs reflect Phase 31's SHIPPED status; the milestone is positioned to advance to Phase 32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc files → repo public history | All artifacts already public; pre-public lean per project memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09-01 | Tampering | Wrong commit SHA recorded in REQUIREMENTS.md | mitigate | Task 3 runs `git log --oneline` before writing; cross-check against each plan's SUMMARY commit reference |
| T-31-09-02 | Information disclosure | VERIFICATION.md leaks internal acceptance details | accept | Repo is open-source by design; planning artifacts are public |
| T-31-09-03 | DoS | Phase 17 HUMAN-UAT row mis-closure | mitigate | Task 1 manual smoke directly exercises rows 1-3; rows 4-5 stay DEFERRED with explicit reason |
</threat_model>

<verification>
- 31-VERIFICATION.md exists and documents every SPEC's status with empirical evidence.
- REQUIREMENTS.md has a Phase 31 traceability section.
- ROADMAP.md Phase 31 is marked SHIPPED with all 9 plans listed.
- STATE.md advances to phase-31-complete.
- 17-HUMAN-UAT.md rows 1-3 CLOSED; rows 4-5 DEFERRED.
- Full unit suite passes (run `dotnet test --logger console verbosity=minimal` as final closure check).
- Phase 18/25/27/28 ByteIdentical contracts GREEN.
</verification>

<success_criteria>
- Phase 31 officially SHIPPED — all 7 SPECs have documented status.
- Phase 17 HUMAN-UAT rows 1-3 close (manual smoke evidence in 31-VERIFICATION.md).
- Phase 17 HUMAN-UAT rows 4-5 stay DEFERRED to v1.5 (Marketplace + OpenVSX).
- STATE.md positions the next phase (32) for spec-phase work.
</success_criteria>

<output>
After completion, create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-09-SUMMARY.md`. This is the closure summary that downstream milestone checks consult.
</output>
