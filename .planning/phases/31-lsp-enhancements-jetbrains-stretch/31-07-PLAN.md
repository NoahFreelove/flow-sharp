---
phase: 31-lsp-enhancements-jetbrains-stretch
plan: 07
type: execute
wave: 3
depends_on: [03]
files_modified:
  - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md
autonomous: true
requirements: [SPEC-6]
must_haves:
  truths:
    - "Every committed `.flow` file in the repo parses cleanly under the Phase 31 lexer (3 new column-0 comment arms + existing `Note:`/`//` arms)"
    - "Phase 18 / 25 / 27 / 28 ByteIdentical*Tests stay GREEN after the lexer changes — two-run byte-identical determinism preserved"
    - "All 70+ `tests/test_*.flow` files run to completion (exit 0) under the new lexer"
    - "Phase 27 fixtures (tutorial.flow, showcase.flow, h_alias.flow, microtonal_ji.flow) + Phase 28 ragtime fixtures parse and render successfully"
    - "Zero in-repo `.flow` files needed source-text migration under D-11 Option A (RESEARCH grep audit pre-confirmed; this plan re-verifies after the lexer change actually lands)"
  artifacts:
    - path: ".planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md"
      provides: "Post-lexer-change audit document: grep-of-collisions + full-suite test results + byte-identical results + per-fixture parse results"
      contains: "Migration audit"
  key_links:
    - from: ".planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md"
      to: "flow-lang/Lexing/SimpleLexer.cs"
      via: "Audit pins the empirical zero-collision result against the lexer's behavior post-Plan 31-03"
      pattern: "31-03|SimpleLexer"
---

<objective>
Ship SPEC-6: empirical audit of every committed `.flow` file in the repo against the NEW lexer (post-Plan 31-03). Confirm:
- Zero column-0 `;` outside strings/comments collide with the new `;` arm.
- Zero column-0 `TODO:` / `FIXME:` collide with the new lead-in arms.
- All 70+ `tests/test_*.flow` smoke-run cleanly.
- Phase 27 fixtures + Phase 28 ragtime fixtures parse + render.
- Phase 18 / 25 / 27 / 28 ByteIdentical*Tests stay GREEN.
- Full Phase 17 + Phase 21 + Phase 24 + Phase 31 unit suite green.

Per RESEARCH §Migration Audit, the pre-plan grep audit found ZERO collisions across all 647 in-repo `.flow` files under D-11 Option A. This plan re-verifies empirically after the lexer arms actually exist in source — converts the research hypothesis into a recorded observation.

If any collision IS found, this plan migrates the source files (rare path; CONTEXT.md acknowledges intentional backward compat break per `project_pre_public_no_legacy_burden`) and re-runs the audit. The migration audit document is the deliverable — it pins the zero-collision result for verification + closure.

Purpose: prove the lexer change is non-disruptive in practice; no surprises at execute time.

Output: 1 new audit document recording the empirical result + zero source-text migrations expected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-SPEC.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md
@.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-03-SUMMARY.md

<interfaces>
<!-- Verification commands from RESEARCH §Migration Audit + PATTERNS §Migration Audit. -->

Grep commands (must return zero hits for clean audit):
- `grep -nE "^\\s*;" examples/**/*.flow tests/**/*.flow flow-lang/*.flow 2>/dev/null | grep -v "^[^:]*:[^:]*://" || true`
- `grep -nE "^\\s*(TODO|FIXME):" examples/**/*.flow tests/**/*.flow flow-lang/*.flow 2>/dev/null || true`

Smoke-run command for individual .flow files:
- `dotnet run --project flow-interpreter -- <path-to-flow>` — exit 0 = parse + run cleanly.

ByteIdentical regression suite:
- `dotnet test flow-lang.Tests --filter "FullyQualifiedName~ByteIdentical"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run grep audit + smoke-run all committed .flow files + byte-identical regression suite; record results in 31-MIGRATION-AUDIT.md</name>
  <files>.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md</files>
  <read_first>
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-RESEARCH.md (§Migration table at lines 484-494)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-PATTERNS.md (§Migration Audit at lines 1225-1248)
    - .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-DECISIONS.md (D-11 Option A — the audit is conditioned on this)
  </read_first>
  <action>
    Run the following audit sequence and record EACH STEP'S OUTPUT in `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md`. The document is the deliverable; pin every command + its observed output.

    **Step 1: Source-text grep audit.**
    Run (capture exit code + output lines):
    - `grep -rnE "^\\s*;" examples/ tests/ flow-lang/ --include="*.flow"` (filter `://` URL-style false positives if any appear)
    - `grep -rnE "^\\s*TODO:" examples/ tests/ flow-lang/ --include="*.flow"`
    - `grep -rnE "^\\s*FIXME:" examples/ tests/ flow-lang/ --include="*.flow"`

    Expected: all three return 0 matches (per RESEARCH grep audit). If any match appears, IDENTIFY the file + line and either (a) migrate the source by escaping or rewriting to remove the collision, or (b) document why the collision is acceptable (e.g. the line is inside a `"..."` string literal — the lexer's `SkipWhitespaceAndComments` doesn't run inside ScanString per Pitfall 8, so the match is a false-positive at the grep level but not at the lexer level).

    **Step 2: Smoke-run all committed .flow files.**
    Run:
    - `for f in tests/test_*.flow; do echo "=== $f ==="; dotnet run --project flow-interpreter -- "$f" > /tmp/flow-audit.out 2>&1; echo "exit=$?"; done | tee /tmp/flow-audit-tests.log`
    - `for f in examples/*.flow examples/pragmas/*.flow examples/symphony/**/*.flow 2>/dev/null; do echo "=== $f ==="; dotnet run --project flow-interpreter -- "$f" > /tmp/flow-audit.out 2>&1; echo "exit=$?"; done | tee /tmp/flow-audit-examples.log`

    Record in 31-MIGRATION-AUDIT.md: total file count, count of exit=0, count of exit!=0, and the file paths of any non-zero-exit cases. Document each failure with its error message and a remediation note. If all files exit 0, record "All N files exit 0."

    **Step 3: ByteIdentical regression suite.**
    Run:
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~ByteIdentical" --logger "console;verbosity=minimal"`

    Record: number of tests run, number passed, number failed, names of any failures. Expected: every Phase 18/25/27/28 ByteIdentical*Test passes — the new lexer arms emit zero tokens for any existing valid program (the audit grep returns zero hits → existing programs are unaffected by the new arms).

    **Step 4: Full Phase 17 / 21 / 24 / 31 regression run.**
    Run:
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase17|FullyQualifiedName~Phase21|FullyQualifiedName~Phase24|FullyQualifiedName~Phase31" --logger "console;verbosity=minimal"`

    Record: total tests run, total passed, total failed. Expected: all green.

    **Step 5: Full unit suite (the gate).**
    Run:
    - `dotnet test --logger "console;verbosity=minimal"`

    Record: total tests run, total passed, total failed. Expected: all green.

    **Step 6: Compose the audit document.**

    `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md` content shape (write all sections):

    - Header: "Phase 31 SPEC-6 Migration Audit — empirical result post-Plan 31-03 lexer change"
    - Section "Audit date": today's date + git SHA (run `git rev-parse HEAD` and record)
    - Section "Step 1 — Grep audit" with each grep command + output + interpretation
    - Section "Step 2 — Smoke-run" with summary counts + any failures
    - Section "Step 3 — ByteIdentical regression" with test counts + pass/fail status
    - Section "Step 4 — Phase 17/21/24/31 regression" with test counts
    - Section "Step 5 — Full unit suite" with totals
    - Section "Migrations performed" — if any source files needed editing, list them with before/after diff snippets; otherwise write "ZERO source-text migrations performed; D-11 Option A held empirically."
    - Section "Conclusion": SPEC-6 acceptance criteria check-list (every criterion green/red) + recommendation to proceed to Plan 31-08 (JetBrains stretch).

    If ANY step shows a failure that is NOT trivially remediable (e.g. a hidden column-0 `;` that needs source migration), perform the migration in the same task, re-run the affected steps, and append the migration to the "Migrations performed" section. Do NOT close the task until the full unit suite is green.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~ByteIdentical" --logger "console;verbosity=minimal"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-MIGRATION-AUDIT.md` exists.
    - File contains the literal phrase "Migration audit" or "ZERO source-text migrations performed" or equivalent.
    - File records the output of every numbered audit step (grep + smoke + byte-identical + Phase17/21/24/31 + full-suite).
    - File ends with a "Conclusion" section listing each SPEC-6 acceptance criterion as ✓ or ✗.
    - `dotnet test flow-lang.Tests --filter "FullyQualifiedName~ByteIdentical"` exits 0 with all tests passing.
    - `dotnet test flow-lang.Tests` full-suite exits 0.
    - Grep audit step records: `grep -rnE "^\\s*;" examples/ tests/ flow-lang/ --include="*.flow"` returns 0 matches OR every match is justified (e.g. inside a string literal — verified by re-reading the file).
  </acceptance_criteria>
  <done>SPEC-6 closed empirically; the audit document is the post-execute receipt that downstream phases reference for "v1.3 fixtures all parse + render under the Phase 31 lexer."</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit grep regex → file system | Read-only grep; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07-01 | Tampering | Audit misses a hidden collision because grep regex is wrong | mitigate | Two grep patterns (`^\\s*;` and `^\\s*(TODO|FIXME):`) cover both cases; smoke-run sweep ALSO exercises every file end-to-end — any unhandled token surfaces as a parse error in Step 2 |
| T-31-07-02 | DoS | Smoke-run of 70+ .flow files is slow | accept | dotnet run cold-start ~2s each → ~150s total; one-shot audit, not in CI hot path |
</threat_model>

<verification>
- 31-MIGRATION-AUDIT.md exists and is structured per the action description.
- Full unit suite exits 0.
- ByteIdentical regression suite exits 0.
</verification>

<success_criteria>
- Empirical zero-collision result recorded in 31-MIGRATION-AUDIT.md.
- All 70+ `tests/test_*.flow` exit 0.
- Phase 27 fixtures + Phase 28 ragtime fixtures parse + render.
- Phase 18/25/27/28 ByteIdentical tests stay GREEN.
- Full Phase 17/21/24/31 regression suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/31-lsp-enhancements-jetbrains-stretch/31-07-SUMMARY.md`
</output>
