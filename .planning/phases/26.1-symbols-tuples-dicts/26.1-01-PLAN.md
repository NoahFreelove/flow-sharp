---
phase: 26.1-symbols-tuples-dicts
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-lang.Tests/Unit/Phase26_1/SymbolFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/TupleFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/TupleDestructureFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/UnpackFlowFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/UnpackRuntimeFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/DictKeyTypeFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/DictTypeRejectionFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/DictConstructFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/DictOpsFacts.cs
  - flow-lang.Tests/Unit/Phase26_1/DictNanKeyFacts.cs
  - flow-lang.Tests/FlowScriptData.cs
  - tests/test_symbol_literal.flow
  - tests/test_tuple_literal.flow
  - tests/test_tuple_destructure.flow
  - tests/test_unpack_flow.flow
  - tests/test_unpack_runtime.flow
  - tests/test_dict_construct.flow
  - tests/test_dict_ops.flow
  - tests/test_dict_keys.flow
  - tests/test_dict_type_errors.flow
autonomous: true
requirements: [SYM-01, TUP-09, TUP-10, TUP-11, DICT-01, DICT-02, DICT-03]
must_haves:
  truths:
    - "All 10 xUnit Fact files exist under flow-lang.Tests/Unit/Phase26_1/ as compilable stubs (Skip=\"Wave N stub\" annotation present on every [Fact])"
    - "All 9 acceptance .flow scripts exist under tests/ as minimal-stub bodies (each ending with `(print \"test_NAME: PASSED\")`) that parse and run cleanly under the CURRENT (Wave 0) interpreter — no new syntax exercised yet"
    - "FlowScriptData.ExpectedErrorScripts has an entry for test_dict_type_errors.flow keyed by relative path with substring \"Dict key type 'Buffer' is not hashable\""
    - "dotnet build over the full solution passes (no compile errors)"
    - "dotnet test --filter \"FullyQualifiedName~Phase26_1\" runs and shows all Phase26_1 facts as Skipped (intentional Wave 0 RED-via-Skip state)"
    - "Phase 18 ByteIdenticalShowcaseTests + Phase 25 ByteIdenticalShowcaseGaussianTests remain GREEN (regression gate)"
  artifacts:
    - path: "flow-lang.Tests/Unit/Phase26_1/SymbolFacts.cs"
      provides: "Skipped fact stubs covering Interning + StrictSeparation (with Strings) for SYM-01"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/TupleFacts.cs"
      provides: "Skipped fact stubs for Literal, EmptyAndSingleton, AnnotationParse, IndexAccess, StructuralEquality, AnyArityMatches"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/TupleDestructureFacts.cs"
      provides: "Skipped fact stubs for `<<a, b>> = expr` destructuring"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/UnpackFlowFacts.cs"
      provides: "Skipped fact stubs for ~> multi-arg + non-tuple fallthrough"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/UnpackRuntimeFacts.cs"
      provides: "Skipped fact stubs covering ZeroArg / SingleArg / MultiArg / DynamicFunction (4 cases per CONTEXT)"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/DictKeyTypeFacts.cs"
      provides: "Skipped fact stubs covering AllowedKeys (8 rows for Int/Long/Float/String/Symbol/Note/Chord/Tuple-of-hashables)"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/DictTypeRejectionFacts.cs"
      provides: "Skipped fact stubs covering Buffer/Voice/Lazy/Function/Sequence rejection at type-check site"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/DictConstructFacts.cs"
      provides: "Skipped fact stubs covering Flat (dict) and TuplePair (dictTuple) constructor forms"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/DictOpsFacts.cs"
      provides: "Skipped fact stubs covering Get/Set/Remove/Has/Keys/Values/Size/Merge/Map/Filter/Each + KeysInsertionOrder + ValuesInsertionOrder + EachUnpacks"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "flow-lang.Tests/Unit/Phase26_1/DictNanKeyFacts.cs"
      provides: "Skipped fact stubs covering NanAsSelfInDict + GeneralEqualityUnchanged regression"
      contains: "namespace FlowLang.Tests.Unit.Phase26_1"
    - path: "tests/test_symbol_literal.flow"
      provides: "Stub acceptance script for SYM-01 (header + use \"@std\" + sentinel only — Wave 1 fills in real body)"
      contains: "test_symbol_literal: PASSED"
    - path: "tests/test_dict_type_errors.flow"
      provides: "Intentional-error stub script — Wave 4 fills in `Dict<Buffer, Int> bad = ...` line that triggers ParseException"
      contains: "test_dict_type_errors"
    - path: "flow-lang.Tests/FlowScriptData.cs"
      provides: "ExpectedErrorScripts entry mapping test_dict_type_errors.flow → \"Dict key type 'Buffer' is not hashable\""
      contains: "test_dict_type_errors.flow"
  key_links:
    - from: "flow-lang.Tests/Unit/Phase26_1/*.cs"
      to: "flow-lang.Tests/Unit/Phase26/NegOverloadFacts.cs"
      via: "Same registry-direct BuildRegistry/Call helper convention (S4 from PATTERNS.md)"
      pattern: "BuildRegistry|Call\\(registry"
    - from: "flow-lang.Tests/FlowScriptData.cs"
      to: "tests/test_dict_type_errors.flow"
      via: "ExpectedErrorScripts dictionary key matches relative test path"
      pattern: "test_dict_type_errors\\.flow"
---

<objective>
Wave 0 scaffolding for Phase 26.1. Create all 10 xUnit Fact files under `flow-lang.Tests/Unit/Phase26_1/` (every `[Fact]` annotated with `Skip="Wave N stub"` so the suite stays GREEN), and create all 9 acceptance `.flow` scripts under `tests/` as minimal stubs (header + `use "@std"` + final `PASSED` sentinel — no new syntax yet). Wire the intentional-error script `test_dict_type_errors.flow` into `FlowScriptData.ExpectedErrorScripts`.

Purpose: Per `26.1-VALIDATION.md` § Wave 0 Requirements and `26.1-RESEARCH.md` § Validation Architecture, every subsequent wave's task verification map references these files. By creating the scaffolding first, Waves 1-4 each (a) un-skip the relevant Facts in their commit, (b) flip them GREEN, and (c) extend the matching `.flow` smoke script body — no per-wave file-creation churn.

Output: 10 Fact files (skipped stubs) + 9 .flow stubs + FlowScriptData.cs entry. After this plan, `dotnet build` passes, `dotnet test --filter "FullyQualifiedName~Phase26_1"` runs and reports all skipped (no failures), Phase 18 + 25 byte-identical regression suites stay GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-CONTEXT.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-RESEARCH.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-PATTERNS.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md

<interfaces>
<!-- Test scaffolding mirrors Phase 26's pattern. Executor uses these directly. -->

From flow-lang.Tests/Unit/Phase26/NegOverloadFacts.cs:1-40 (TEMPLATE for every Fact file):
```csharp
using System;
using System.Collections.Generic;
using FlowLang.Runtime;
using FlowLang.StandardLibrary;
using FlowLang.TypeSystem;
using FlowLang.TypeSystem.PrimitiveTypes;
using Xunit;

namespace FlowLang.Tests.Unit.Phase26_1;

public class XxxFacts
{
    private static InternalFunctionRegistry BuildRegistry()
    {
        var registry = new InternalFunctionRegistry();
        BuiltInFunctions.RegisterAllImplementations(registry);
        return registry;
    }

    private static Value Call(InternalFunctionRegistry registry, string name,
                              IReadOnlyList<FlowType> argTypes, IReadOnlyList<Value> args)
    {
        var sig = new FunctionSignature(name, argTypes);
        if (!registry.TryGetImplementation(name, sig, out var fn, out _) || fn is null)
            throw new InvalidOperationException($"{name} overload {sig} not registered");
        return fn(args);
    }

    [Fact(Skip = "Wave N stub — implementation lands in plan 26.1-0N")]
    public void NameOfBehaviorPinned() { /* TODO Wave N */ }
}
```

From flow-lang.Tests/FlowScriptData.cs:26-59 (ExpectedErrorScripts shape — append entry):
```csharp
public static readonly Dictionary<string, string> ExpectedErrorScripts = new()
{
    // ... existing entries ...
    ["test_dict_type_errors.flow"] = "Dict key type 'Buffer' is not hashable",
};
```

From tests/test_humanize_gaussian.flow (acceptance-script header convention):
```
Note: Test description goes here
Note: Tests: <enumeration of behaviors>

use "@std"

Note: ===== STUB BODY (Wave N fills this in) =====

(print "test_NAME: PASSED")
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 10 xUnit Fact stub files under flow-lang.Tests/Unit/Phase26_1/</name>
  <files>
    flow-lang.Tests/Unit/Phase26_1/SymbolFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/TupleFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/TupleDestructureFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/UnpackFlowFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/UnpackRuntimeFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/DictKeyTypeFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/DictTypeRejectionFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/DictConstructFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/DictOpsFacts.cs,
    flow-lang.Tests/Unit/Phase26_1/DictNanKeyFacts.cs
  </files>
  <read_first>
    - flow-lang.Tests/Unit/Phase26/NegOverloadFacts.cs (full file — copy-paste-modify template; the BuildRegistry+Call helpers are the contract per S4 in PATTERNS.md)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-PATTERNS.md § F1 (xUnit Facts excerpt; lines 1366-1421)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-RESEARCH.md § Validation Architecture (lines 833-897 — the canonical req-id → fact-name mapping)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md § Wave 0 Requirements (lines 51-68 — the exact 10 file names)
  </read_first>
  <action>
Create 10 xUnit Fact files under `flow-lang.Tests/Unit/Phase26_1/` (the directory does not exist — create it). All files use namespace `FlowLang.Tests.Unit.Phase26_1`. Every file follows the Phase 26 `NegOverloadFacts.cs` template: imports + `BuildRegistry` helper + `Call` helper, then a series of `[Fact(Skip = "Wave N stub — implementation lands in plan 26.1-0N")]` annotated stub methods with empty bodies (or `Assert.True(false, "stub")` — either is acceptable since Skip suppresses execution).

Each Fact file must contain at minimum the [Fact] method names listed below (these names are the canonical pin points referenced in `26.1-RESEARCH.md` § Validation Architecture lines 850-881):

`SymbolFacts.cs` (Wave 1 stubs):
- `Interning_SamePointer` — pins `OfSymbol(ctx, "foo")` returning the same Value reference twice
- `StrictSeparation_SymbolNeqString` — pins `(equals #foo "foo") → false`
- `EqualsBuiltinReturnsTrueForSameSymbol` — pins `(equals #foo #foo) → true`

`TupleFacts.cs` (Wave 2 stubs):
- `Literal_ParsesAndEvaluates`
- `EmptyAndSingleton_BothValid`
- `AnnotationParse_TupleOfNoteAndBeat`
- `IndexAccess_AtZeroAndOne`
- `StructuralEquality_PerPositionMatch`
- `AnyArityMatches_AnyTuple` — locks Assumption A5 from RESEARCH (TupleType.AnyArity sentinel matches any tuple)

`TupleDestructureFacts.cs` (Wave 2 stubs):
- `Destructure_BindsBothComponents`
- `Destructure_TypedSlots_NoteBeat` — `<<Note p, Beat d>> = ...`
- `Destructure_BareNamesNoTypes` — names without type slots

`UnpackFlowFacts.cs` (Wave 3 stubs):
- `MultiArg_UnpacksAndCalls`
- `NonTupleLhs_FallsThroughToArrowSemantics`

`UnpackRuntimeFacts.cs` (Wave 3 stubs — exactly 4 theory cases per CONTEXT § (unpack tuple func)):
- `ZeroArg_EmptyTuple`
- `SingleArg_OneTuple`
- `MultiArg_TwoTuple`
- `DynamicFunctionValue` — `Function f = (get d k); (unpack ev f)`

`DictKeyTypeFacts.cs` (Wave 4 stubs — 8 rows for the allowlist):
- `AllowedKeys_Int`, `AllowedKeys_Long`, `AllowedKeys_Float`, `AllowedKeys_String`, `AllowedKeys_Symbol`, `AllowedKeys_Note`, `AllowedKeys_Chord`, `AllowedKeys_TupleOfHashables`

`DictTypeRejectionFacts.cs` (Wave 4 stubs):
- `RejectsBuffer`, `RejectsVoice`, `RejectsLazy`, `RejectsFunction`, `RejectsSequence`
- `RejectsTupleWithNonHashableElement` — `Dict<Tuple<<Buffer, Int>>, Int>` rejected at the inner Buffer

`DictConstructFacts.cs` (Wave 4 stubs):
- `Flat_DictKVKV` — `(dict #kick 90 #snare 70)` returns Dict<Symbol, Int> size 2
- `TuplePair_DictTuple` — `(dictTuple <<#kick, 90>> <<#snare, 70>>)` matches the flat form

`DictOpsFacts.cs` (Wave 4 stubs — one [Fact] per op verb plus 3 cross-cutting properties):
- `GetMissing_ReturnsNothing` (Value.Void())
- `GetOr_ReturnsDefault`
- `Set_ReturnsNewDict_OriginalUntouched`
- `Remove_ReturnsNewDict`
- `Has_TrueWhenPresent`, `Has_FalseWhenAbsent`
- `Size_CountsEntries`
- `Merge_LastWriteWins`
- `KeysInsertionOrder` — `(keys (dict "a" 1 "b" 2 "c" 3))` returns `["a", "b", "c"]` in insertion order
- `ValuesInsertionOrder`
- `EachUnpacks_TwoArgCallback` — pins Pitfall 6 dict-vs-array overload disambiguation
- `MapTransform_ValuesOnly`
- `FilterMatches_KeepsPredicateTrue`

`DictNanKeyFacts.cs` (Wave 4 stubs):
- `NanAsSelfInDict` — `(set d nan v)` then `(get d nan) → v`
- `GeneralEqualityUnchanged` — REGRESSION: `(equals nan nan) → false` (Pitfall 3 — verifies `Utils.StrictEquals` was NOT touched)

Use `[Fact(Skip = "Wave 1 stub — implementation lands in plan 26.1-02")]` for SymbolFacts, `Wave 2 stub — plan 26.1-03` for TupleFacts/TupleDestructureFacts, `Wave 3 stub — plan 26.1-04` for UnpackFlowFacts/UnpackRuntimeFacts, `Wave 4 stub — plan 26.1-05` for the 5 Dict fact files.

DO NOT use `Skip = ""` (skipping with empty reason) — xUnit will still run the test if Skip is empty. Always include the reason.

Body for every stub method: a single comment `// TODO Wave N — implementation lands in plan 26.1-0N` and an empty body. This compiles cleanly and stays Skipped under `dotnet test`.

Per S4 (PATTERNS.md): every file includes the BuildRegistry + Call helpers verbatim from `NegOverloadFacts.cs:26-40`. SymbolFacts.cs additionally constructs an `ExecutionContext` (since Symbol interning needs one) — see PATTERNS.md F1 § "Differ — Symbol-specific helper". The exact one-liner: `var ctx = new ExecutionContext(new ErrorReporter(), BuildRegistry());` — but this is INSIDE the test body at Wave 1; the Wave 0 stub just needs the BuildRegistry helper present.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls flow-lang.Tests/Unit/Phase26_1/*.cs | wc -l` returns `10`
    - `grep -c "namespace FlowLang.Tests.Unit.Phase26_1" flow-lang.Tests/Unit/Phase26_1/*.cs` returns `10` (one per file)
    - `grep -rE "Skip\\s*=\\s*\"" flow-lang.Tests/Unit/Phase26_1/ | wc -l` returns at least `40` (every Fact has a non-empty Skip reason — minimum-fact counts: 3+6+3+2+4+8+6+2+13+2 = 49)
    - `grep -E "Skip\\s*=\\s*\"\"" flow-lang.Tests/Unit/Phase26_1/*.cs` returns 0 lines (no empty Skip reason — empty Skip would actually run the stubs)
    - `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase26_1" --logger "console;verbosity=minimal" 2>&amp;1 | grep -E "(Failed|Passed|Skipped):"` shows `Failed: 0` (Phase 26.1 facts compile and stay skipped)
    - `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase18" --logger "console;verbosity=minimal" 2>&amp;1 | grep -E "Failed:"` shows `Failed: 0` (Phase 18 byte-identical regression GREEN)
    - `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase25" --logger "console;verbosity=minimal" 2>&amp;1 | grep -E "Failed:"` shows `Failed: 0` (Phase 25 byte-identical regression GREEN)
    - Each fact file contains the BuildRegistry + Call helpers via `grep -l "BuildRegistry" flow-lang.Tests/Unit/Phase26_1/*.cs | wc -l` returning `10`
  </acceptance_criteria>
  <done>10 Fact stub files compile, all stubs skipped, no test failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create 9 acceptance .flow stub scripts under tests/ + register intentional-error script in FlowScriptData</name>
  <files>
    tests/test_symbol_literal.flow,
    tests/test_tuple_literal.flow,
    tests/test_tuple_destructure.flow,
    tests/test_unpack_flow.flow,
    tests/test_unpack_runtime.flow,
    tests/test_dict_construct.flow,
    tests/test_dict_ops.flow,
    tests/test_dict_keys.flow,
    tests/test_dict_type_errors.flow,
    flow-lang.Tests/FlowScriptData.cs
  </files>
  <read_first>
    - tests/test_humanize_gaussian.flow (acceptance-script header + sentinel convention — Phase 25's most recent example)
    - tests/test_lambdas.flow (Function-typed values / HOF pattern — Wave 3 will reference this for Function-value test)
    - flow-lang.Tests/FlowScriptData.cs (entire file — to see exact dictionary insertion location around line 58)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-PATTERNS.md § F2 + § S5 (header + sentinel convention)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-CONTEXT.md § Specifics (the 9 acceptance blocks — lines 148-211 — each block maps to one .flow file)
  </read_first>
  <action>
Create 9 stub `.flow` scripts under `tests/`. Each follows the canonical header + sentinel convention from `tests/test_humanize_gaussian.flow`:

```
Note: <description>
Note: Tests: <comma-separated behavior list>
Note: Wave 0 STUB — Wave N fills in real body (see plan 26.1-0N)

use "@std"

Note: ===== STUB BODY (Wave N fills this in) =====

(print "test_NAME: PASSED")
```

Specifics — exact stub bodies (each must currently parse and run cleanly under the existing interpreter without using any Phase 26.1 syntax):

1. `test_symbol_literal.flow` — Wave 1 fills in. Header description "Symbol literal interning + strict separation from String"; sentinel `test_symbol_literal: PASSED`.

2. `test_tuple_literal.flow` — Wave 2 fills in. Header "Tuple literal `<<a, b, c>>`, empty + singleton, indexing, structural equality"; sentinel `test_tuple_literal: PASSED`.

3. `test_tuple_destructure.flow` — Wave 2 fills in. Header "Tuple destructuring assignment `<<Type? name, Type? name>> = expr`"; sentinel `test_tuple_destructure: PASSED`.

4. `test_unpack_flow.flow` — Wave 3 fills in. Header "`~>` flow operator unpack + non-tuple fallthrough"; sentinel `test_unpack_flow: PASSED`.

5. `test_unpack_runtime.flow` — Wave 3 fills in. Header "(unpack tuple func) — zero-arg, single-arg, multi-arg, dynamic-Function-value"; sentinel `test_unpack_runtime: PASSED`.

6. `test_dict_construct.flow` — Wave 4 fills in. Header "Dict construction via `(dict K V K V)` flat + `(dictTuple <<K,V>> ...)` tuple-pair forms"; sentinel `test_dict_construct: PASSED`.

7. `test_dict_ops.flow` — Wave 4 fills in. Header "Dict 14-op surface — get/getOr/set/remove/has/keys/values/size/merge/each/map/filter + insertion-order"; sentinel `test_dict_ops: PASSED`.

8. `test_dict_keys.flow` — Wave 4 fills in. Header "Dict key types: Symbol/Note/Chord/Tuple-of-hashables + NaN-as-key special case"; sentinel `test_dict_keys: PASSED`.

9. `test_dict_type_errors.flow` — Wave 4 fills in. INTENTIONAL-ERROR SCRIPT. Header "Dict<Buffer, Int> type-rejection at parse time (CONTEXT § Hashable enforcement)". For Wave 0 the body is the standard PASSED-sentinel stub form (so `dotnet test` doesn't fail before Wave 4); the Wave 4 commit will replace the body with `Dict<Buffer, Int> bad = (dict)` to trigger the `ParseException`.

For all 9 stubs, the body between the `use "@std"` line and the sentinel must be EMPTY (just `Note:` lines or blank). Do NOT add any Symbol/Tuple/Dict syntax. Verify by running each stub: `dotnet run --project flow-interpreter tests/test_symbol_literal.flow` should print `test_symbol_literal: PASSED` and exit 0.

Then edit `flow-lang.Tests/FlowScriptData.cs`. Locate the `ExpectedErrorScripts` dictionary (around line 26-59). Append a new entry inside the existing `new() { ... }` initializer, immediately after the last existing entry but before the closing `};`:

```csharp
// Phase 26.1 plan 26.1-05 (Wave 4) will replace the stub body with `Dict<Buffer, Int> bad = ...`
// which raises `Dict key type 'Buffer' is not hashable` at parse time. Until Wave 4 lands the
// stub body just prints PASSED and this entry is dormant — the Theory row's substring check is
// only consulted when stderr is non-empty, so the entry sits inert through Waves 0-3.
["test_dict_type_errors.flow"] = "Dict key type 'Buffer' is not hashable",
```

NOTE on the dormant-stub interaction with the Theory runner: when Wave 0 lands, `test_dict_type_errors.flow` body prints PASSED and produces no stderr. The Theory row WILL fail because the runner expects the substring in stderr. To prevent a Wave 0 RED state on this row, the Wave 0 stub body for `test_dict_type_errors.flow` must intentionally PRODUCE the expected error substring as a comment-stub — use the literal `Note: stub-error-placeholder Dict key type 'Buffer' is not hashable` line so the substring appears via `Note:` body printing... Actually that doesn't work because `Note:` is a comment. INSTEAD: gate the Wave 0 row OFF by adding a SECOND temporary entry to a per-wave skip list — but FlowScriptData has no such list. SIMPLEST CORRECT APPROACH: leave the FlowScriptData.cs entry commented out at Wave 0 (file edit shows `// ["test_dict_type_errors.flow"] = "Dict key type 'Buffer' is not hashable",`) and uncomment in Wave 4 (plan 26.1-05). The Wave 0 commit's `dotnet test` then leaves the stub script in the GREEN normal-script bucket because there's no entry telling the runner to expect an error. Wave 4 simultaneously (a) replaces the stub body with the real `Dict<Buffer, Int> bad` line and (b) uncomments the FlowScriptData entry. Both edits land atomically.

So: Wave 0 ADDS the COMMENTED-OUT line as documented above, with the explanation comment. Wave 4 (plan 26.1-05) flips the comment on.

Final acceptance: every stub `.flow` file runs and prints PASSED under the current Wave 0 interpreter, no Theory rows turn RED.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; for f in tests/test_symbol_literal.flow tests/test_tuple_literal.flow tests/test_tuple_destructure.flow tests/test_unpack_flow.flow tests/test_unpack_runtime.flow tests/test_dict_construct.flow tests/test_dict_ops.flow tests/test_dict_keys.flow tests/test_dict_type_errors.flow; do dotnet run --project flow-interpreter "$f" 2>&amp;1 | tail -2; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/test_symbol_literal.flow tests/test_tuple_literal.flow tests/test_tuple_destructure.flow tests/test_unpack_flow.flow tests/test_unpack_runtime.flow tests/test_dict_construct.flow tests/test_dict_ops.flow tests/test_dict_keys.flow tests/test_dict_type_errors.flow 2>/dev/null | wc -l` returns `9`
    - For each .flow file `f`, `dotnet run --project flow-interpreter "$f"` produces stdout containing the matching `test_NAME: PASSED` sentinel and exit code 0
    - `grep -c "test_dict_type_errors.flow" flow-lang.Tests/FlowScriptData.cs` returns at least `1` (the line is present, possibly commented)
    - `grep "test_dict_type_errors.flow" flow-lang.Tests/FlowScriptData.cs` shows the line is COMMENTED OUT for Wave 0 (begins with `//` after any leading whitespace)
    - `grep -c "use \"@std\"" tests/test_symbol_literal.flow tests/test_tuple_literal.flow tests/test_tuple_destructure.flow tests/test_unpack_flow.flow tests/test_unpack_runtime.flow tests/test_dict_construct.flow tests/test_dict_ops.flow tests/test_dict_keys.flow tests/test_dict_type_errors.flow | grep -v ':0$' | wc -l` returns `9` (every script imports std)
    - None of the 9 stub bodies contain `<<`, `>>`, `#`, `~>`, `(dict)`, `(unpack)`, `Dict<`, or `Tuple<<` (verify via `grep -E "<<|>>|~>|\\(dict|\\(unpack|Dict<|Tuple<<" tests/test_symbol_literal.flow tests/test_tuple_literal.flow tests/test_tuple_destructure.flow tests/test_unpack_flow.flow tests/test_unpack_runtime.flow tests/test_dict_construct.flow tests/test_dict_ops.flow tests/test_dict_keys.flow tests/test_dict_type_errors.flow | grep -v "^[^:]*:Note:" | wc -l` returning `0` — `#` is allowed inside `Note:` comments only)
    - `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~FlowScriptTests" --logger "console;verbosity=minimal" 2>&amp;1 | grep -E "Failed:"` shows `Failed: 0` (no Theory rows turn RED)
  </acceptance_criteria>
  <done>All 9 stub .flow scripts run GREEN; FlowScriptData entry is COMMENTED OUT (Wave 4 will flip on).</done>
</task>

</tasks>

<threat_model>
No threat surface — interpreter internals; new tokens/builtins do not introduce parsable user data ingress beyond existing source-file pipeline.
</threat_model>

<verification>
- `dotnet build` over the full solution passes
- `dotnet test --filter "FullyQualifiedName~Phase26_1"` shows 0 failures (all skipped)
- `dotnet test --filter "FullyQualifiedName~Phase18"` and `~Phase25` remain GREEN (byte-identical regression)
- All 9 acceptance .flow stubs run GREEN under the current interpreter
</verification>

<success_criteria>
- 10 xUnit Fact files exist with skipped stubs spanning all SYM-01/TUP-09/TUP-10/TUP-11/DICT-01/DICT-02/DICT-03 fact-name pins from VALIDATION.md
- 9 acceptance .flow scripts exist as stubs and run GREEN under the current interpreter
- FlowScriptData.cs has the `test_dict_type_errors.flow` entry as a COMMENTED-OUT line (uncommented in Wave 4)
- Phase 18 + 25 byte-identical regression GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-symbols-tuples-dicts/26.1-01-SUMMARY.md` with file inventory + Wave 0 GREEN proof.
</output>
</content>
</invoke>