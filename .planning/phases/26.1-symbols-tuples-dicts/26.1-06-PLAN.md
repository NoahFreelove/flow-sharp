---
phase: 26.1-symbols-tuples-dicts
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md
  - .planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md
  - CLAUDE.md
autonomous: true
requirements: [SYM-01, TUP-09, TUP-10, TUP-11, DICT-01, DICT-02, DICT-03]
must_haves:
  truths:
    - "REQUIREMENTS.md DICT-01/02/03 entries rewritten to match ROADMAP scope (CONTEXT § REQUIREMENTS-vs-ROADMAP reconciliation): Dict<K,V> 8-key-type allowlist; 14-op surface; immutable updates; insertion-order preservation; NaN-key special case"
    - "REQUIREMENTS.md Traceability table updated: SYM-01, TUP-09, TUP-10, TUP-11, DICT-01, DICT-02, DICT-03 rows show 'Shipped <hash>' status referencing the Wave 1-4 commits"
    - "ROADMAP.md Phase 26.1 entry: Plans count updated to 6, plan list complete with 26.1-01..26.1-06 entries marked [x] with brief shipped-hash annotations, Phase 26.1 row in the Progress table marked Complete with the closure date 2026-05-09"
    - "STATE.md current-position updated to 'Phase 26.1 fully shipped' with the closure context; Resume Instructions point to Phase 26.2 next"
    - "26.1-VERIFICATION.md exists with the closure sign-off — every must-have from the 6 plans cross-referenced GREEN; Phase 18+25 byte-identical regression confirmed; full test suite green"
    - "26.1-VALIDATION.md frontmatter updated: status: shipped; nyquist_compliant: true; wave_0_complete: true; sign-off section approved"
    - "CLAUDE.md Core Language Features section gains 4 new bullets: Symbol primitive (#foo), Tuple type (<<a, b>>, @N, destructure), Generic Dict<K,V> via builtins, ~> flow operator"
    - "CLAUDE.md AST table (lines 168-186 — find via grep) gains 4 new rows: SymbolLiteralExpression, TupleLiteralExpression, TupleUnpackFlowExpression, TupleDestructureStatement (the last is in the Statements sub-table)"
    - "examples/tutorial.flow + examples/showcase.flow are NOT modified — Phase 27 owns those (RESEARCH § Risks: byte-identical regression gate must hold; Phase 27 will do the tutorial chapter additions)"
    - "Phase 18 ByteIdenticalShowcaseTests + Phase 25 ByteIdenticalShowcaseGaussianTests STILL GREEN — confirmed in 26.1-VERIFICATION.md"
    - "Full dotnet test suite green; the 2 pre-existing DecibelBeatNumericCompatFacts deferred items remain orthogonal (logged in STATE.md)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DICT-01/02/03 entries reflecting ROADMAP scope; Traceability rows shipped"
      contains: "Phase 26.1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26.1 marked complete with shipped commits referenced"
      contains: "Phase 26.1"
    - path: ".planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md"
      provides: "Closure sign-off with full plan-by-plan must-have evidence"
      contains: "Phase 26.1 — Verification"
      min_lines: 50
    - path: "CLAUDE.md"
      provides: "AST table entries + Core Language Features bullets for Symbol/Tuple/Dict/~>"
      contains: "Symbol primitive"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "DICT-01/02/03 text now matches ROADMAP Phase 26.1 success criteria 5-7"
      pattern: "DICT-0[123]"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Current-position field references Phase 26.1 complete; Phase 26.2 next"
      pattern: "Phase 26\\.[12]"
    - from: "CLAUDE.md"
      to: "flow-lang/Ast/Expressions/SymbolLiteralExpression.cs"
      via: "AST table row references the actual file by node-name"
      pattern: "SymbolLiteralExpression"
---

<objective>
Wave 5 — closure. Pure documentation updates: REQUIREMENTS.md DICT-01/02/03 rewritten to match ROADMAP scope; ROADMAP.md Phase 26.1 marked Complete with plan list and shipped-hash annotations; STATE.md current-position advanced; 26.1-VERIFICATION.md authored with full plan-by-plan evidence; 26.1-VALIDATION.md frontmatter flipped to shipped; CLAUDE.md gains Core Language Features bullets + AST table rows for Symbol/Tuple/Dict/`~>`.

DO NOT modify `examples/tutorial.flow` or `examples/showcase.flow` — Phase 27 owns those. Touching them risks byte-identical regression on the Phase 18 + 25 guards. The tutorial-side Symbol/Tuple/Dict chapter belongs to Phase 27 per CONTEXT § Claude's Discretion + RESEARCH.md § Risks.

Purpose: Standard closure pattern (Phase 20-04 / 21-03 / 22-07 / 23-05 / 25-04 / 26-05 precedent). The closure plan is docs-only and atomic — separates feature commits from documentation commits per D-13 commit-granularity precedent.

Output: All v1.3 milestone tracking artifacts agree on Phase 26.1's completion. Phase 26.2 is the next ROADMAP target. The full test suite is green; byte-identical regression remains intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-CONTEXT.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-RESEARCH.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-01-SUMMARY.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-02-SUMMARY.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-03-SUMMARY.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-04-SUMMARY.md
@.planning/phases/26.1-symbols-tuples-dicts/26.1-05-SUMMARY.md
@.planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md

<interfaces>
<!-- Pure docs plan — no code interfaces. The "interfaces" here are file shapes/conventions to mirror. -->

REQUIREMENTS.md DICT-01/02/03 — current STALE text at lines 107-111. Per RESEARCH § Q14 the closure plan rewrites these in lockstep. The new text replaces the single-`Dict[T]`-with-String-keys spec with the ROADMAP-correct 8-key-type generic form.

ROADMAP.md Phase 26.1 entry at lines 224-238 — Plans count + plan list + Progress table row. Pattern from Phase 26 closure (commits 86fa69a + 2d3efe1 + closure):
```
**Plans**: 6 plans
- [x] 26.1-01-PLAN.md — Wave 0 RED scaffolding (10 Fact stubs + 9 .flow stubs + FlowScriptData entry) — Shipped <hash>
- [x] 26.1-02-PLAN.md — Wave 1: Symbol primitive (SYM-01) + IsHashable() virtual — Shipped <hash>
- [x] 26.1-03-PLAN.md — Wave 2: Tuple type (TUP-09) — literal/index/destructure/AnyArity — Shipped <hash>
- [x] 26.1-04-PLAN.md — Wave 3: ~> + (unpack) (TUP-10/TUP-11) — Shipped <hash>
- [x] 26.1-05-PLAN.md — Wave 4: Dict<K,V> + 14 ops (DICT-01/02/03) — Shipped <hash>
- [x] 26.1-06-PLAN.md — Wave 5: Closure (REQUIREMENTS/ROADMAP/STATE/CLAUDE.md) — Shipped <hash>
```

CLAUDE.md AST table at lines 168-186 — Expressions sub-table (12 types currently) and Statements sub-table (8 types currently). New rows:
```
| Expression | Description |
| ... existing 12 ... |
| `SymbolLiteralExpression` | Symbol literal (#foo) — interned at evaluation time |
| `TupleLiteralExpression` | Tuple literal (<<a, b, c>>) with empty + singleton arities |
| `TupleUnpackFlowExpression` | Tuple-unpack flow operator (~>) |
```

```
| Statement | Description |
| ... existing 8 ... |
| `TupleDestructureStatement` | Destructuring assignment (<<Type? name, ...>> = expr) |
```

CLAUDE.md Core Language Features (lines 142-153) — add 4 new bullets at the end of the Core block:
```
- **Symbol primitive type:** `#foo` interned literal — pointer-equality, strict separation from String
- **Tuple type:** `<<a, b, c>>` literal with per-position types and arity; `tup@N` indexing; `<<a, b>> = expr` destructuring; structural equality
- **`~>` flow operator:** unpacks tuple into multi-arg call; falls through to `->` semantics on non-tuple LHS
- **Generic `Dict<K, V>`:** insertion-order preservation; allowed keys: Int, Long, Float, String, Symbol, Note, Chord, Tuple-of-hashables; constructed via `(dict K V K V ...)` or `(dictTuple <<K,V>> ...)` builtins; 14-op surface (get/getOr/set/remove/has/keys/values/size/merge/each/map/filter)
- **`(unpack tuple func)`:** runtime first-class apply (S-expression equivalent of `~>`)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md DICT-01/02/03 + update Traceability rows; update ROADMAP.md Phase 26.1 entry; update STATE.md current position</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 96-115 (current SYM-01, TUP-09/10/11, DICT-01/02/03 entries — DICT entries are stale per CONTEXT § REQUIREMENTS vs ROADMAP reconciliation; SYM/TUP entries already match ROADMAP and need no rewrite)
    - .planning/REQUIREMENTS.md lines 175-185 (Traceability table — find SYM-01..DICT-03 rows showing Pending; update each to Shipped with the actual hash from `git log --oneline -10`)
    - .planning/ROADMAP.md lines 224-238 (Phase 26.1 entry) and lines 290-300 (Progress table — find the Phase 26.1 row showing 0/N Not started; flip to Complete N/N with closure date)
    - .planning/STATE.md lines 1-50 (current-position frontmatter + Resume Instructions — update to Phase 26.1 complete; Phase 26.2 next)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-CONTEXT.md § REQUIREMENTS vs ROADMAP reconciliation (lines 30-44 — the canonical text the rewrite must match)
    - All 5 SUMMARY.md files from plans 26.1-01..26.1-05 (extract actual commit hashes for the Traceability rows)
    - .planning/phases/26-op-standardization-prefix-only/26-05-PLAN.md (Phase 26 closure pattern — Wave 4: CLAUDE.md prefix-only rule + REQUIREMENTS/ROADMAP/STATE closure + 26-VERIFICATION.md)
  </read_first>
  <action>
**1. `.planning/REQUIREMENTS.md`** — Rewrite DICT-01/02/03 entries (lines 107-111). The CURRENT stale text says `Dict[T]` with String-only keys. Per CONTEXT § REQUIREMENTS vs ROADMAP reconciliation, replace with:

```markdown
- [x] **DICT-01**: A new generic `Dict<K, V>` type lives in `flow-lang/TypeSystem/SpecialTypes/`. Allowed key types are an 8-element allowlist: Int, Long, Float, String, Symbol, Note, Chord, Tuple-of-hashables (recursive — every component must be hashable). Disallowed key types are rejected at parse-time at the annotation site with a `ParseException` citing the allowlist. S-expression constructors: `(dict K V K V ...)` flat interleaved + `(dictTuple <<K,V>> <<K,V>> ...)` tuple-pair (memory: "Keep functional S-expression style, no infix operators"). Empty dict via `(dict)`. Type inference: `Dict<K, V>` annotation specifies K and V; runtime constructor narrows to the actual element types. Acceptance: `(dict #kick 90 #snare 70)` returns a `Dict<Symbol, Int>` with size 2; `Dict<Buffer, Int> bad = ...` raises a parse error. — Shipped <wave-4-hash>

- [x] **DICT-02**: 14-op dict surface, all immutable (mutations return new dicts). `(get d k)` returns the value at `k` or `Value.Void()` (Flow's "Nothing" sentinel) when absent. `(getOr d k default)` returns `default` when absent. `(set d k v)` returns a NEW dict with `k → v`. `(remove d k)` returns a new dict without `k`. `(has d k)` → `Bool`. `(keys d)` → `Array[K]` in insertion order. `(values d)` → `Array[V]` in insertion order. `(size d)` → `Int`. `(merge d1 d2)` last-write-wins (d2 keys override d1). NaN-key special-case scoped to Dict-internal equality only (Float NaN as self) — Flow's general `(equals nan nan)` continues to follow IEEE 754 (returns false). Missing-key behavior is not an error per the charitable-interpretation memory. Acceptance: `(get (dict "a" 1) "missing")` → `Nothing`; `(set d "a" 2)` returns NEW dict, original `d` unchanged; `(merge (dict #a 1) (dict #a 2 #b 3))` → size 2, get #a returns 2. — Shipped <wave-4-hash>

- [x] **DICT-03**: Functional iteration + introspection: `(each d cb)` yields `<<key, value>>` per entry and invokes the callback via `~>` semantics — the dict-side internally unpacks the tuple into 2 positional args so the user writes a normal `(fn Symbol k, Int v => ...)` 2-arg lambda (no lambda-side destructuring). `(map d cb)` returns `Dict<K, V'>` with values transformed (keys preserved). `(filter d pred)` returns `Dict<K, V>` with entries where `pred(K, V) → true`. INSERTION ORDER preserved across all ops (not hash order — preserves byte-identical determinism contract). Acceptance: `(keys (dict "a" 1 "b" 2 "c" 3))` → `["a", "b", "c"]` in insertion order; `(each)` over Dict invokes 2-arg lambda; Pitfall 6 — separate `(each Dict Function)` overload coexists with existing `(each Array Function)`. — Shipped <wave-4-hash>
```

Replace the actual `<wave-4-hash>` placeholders with the commit hash from `git log --oneline -10 | grep "feat(26.1):.*Dict" | head -1 | cut -d' ' -f1`. If multiple Dict-related commits exist, pick the Wave 4 mega-commit (look for `Dict<K, V>` or `DICT-01/02/03` in the message).

ALSO update the Traceability table at lines 175-185:
- SYM-01 Pending → Shipped <wave-1-hash>
- TUP-09 Pending → Shipped <wave-2-hash>
- TUP-10 Pending → Shipped <wave-3-hash>
- TUP-11 Pending → Shipped <wave-3-hash>
- DICT-01/02/03 Pending → Shipped <wave-4-hash>

Hashes from `git log --oneline -10` filtered by `26.1` commits.

**2. `.planning/ROADMAP.md`** — Three coordinated edits:

  **2a. Phase 26.1 entry (lines 224-238).** Update the `**Plans**: TBD` line to `**Plans**: 6 plans`. Append the plan list immediately below:
  ```
  - [x] 26.1-01-PLAN.md — Wave 0 RED scaffolding (10 Fact stubs + 9 .flow stubs + FlowScriptData entry) — Shipped <hash-01>
  - [x] 26.1-02-PLAN.md — Wave 1: Symbol primitive (SYM-01) + IsHashable() virtual on FlowType — Shipped <hash-02>
  - [x] 26.1-03-PLAN.md — Wave 2: Tuple type (TUP-09) literal/index/destructure/AnyArity sentinel — Shipped <hash-03>
  - [x] 26.1-04-PLAN.md — Wave 3: ~> flow op (TUP-10) + (unpack) runtime (TUP-11) — Shipped <hash-04>
  - [x] 26.1-05-PLAN.md — Wave 4: Dict<K, V> + 14-op surface (DICT-01/02/03) — Shipped <hash-05>
  - [x] 26.1-06-PLAN.md — Wave 5: Closure (REQUIREMENTS/ROADMAP/STATE/CLAUDE.md/26.1-VERIFICATION.md) — Shipped <hash-06>
  ```

  **2b. Phase row checkbox.** In the Phase 26.1 line at line 73 (or wherever it appears in the v1.3 phase summary list), change `- [ ] **Phase 26.1: Symbols + Tuples + Dicts (INSERTED)** ...` to `- [x] **Phase 26.1: Symbols + Tuples + Dicts (INSERTED)** ...`. Append the closure note: `— Shipped 2026-05-09 (Waves 0-5: <hash-01> + <hash-02> + <hash-03> + <hash-04> + <hash-05> + <hash-06>)`.

  **2c. Progress table** (around lines 290-300). Update the Phase 26.1 row from `0/N | Not started | -` to `6/6 | Complete | 2026-05-09`.

**3. `.planning/STATE.md`** — Two coordinated edits:

  **3a. Frontmatter `current-position` / `last-activity` / `stopped_at` fields** at the top (around lines 1-15). Update:
  - `stopped_at: Phase 26.1 fully shipped`
  - `last_activity: 2026-05-09 - Phase 26.1 (Symbols + Tuples + Dicts) fully shipped; SYM-01/TUP-09/TUP-10/TUP-11/DICT-01/02/03 GREEN; Phase 18+25 byte-identical regression GREEN; v1.3 has 2 phases remaining (26.2 + 27)`
  - `progress.completed_phases: 10` (was 9; Phase 26.1 added)
  - `progress.completed_plans: 48` (42 + 6 new plans from this phase)
  - `progress.percent: 100` of the inner-progress metric — verify by reading the existing percent semantics; it may track a different metric

  **3b. Current Position section** (around lines 17-35). Update from "Phase 26 (op-standardization-prefix-only) — COMPLETE 2026-05-09" to:
  ```
  Milestone: v1.3 Composer DX Tier B/C
  Phase: 26.1 (symbols-tuples-dicts) — COMPLETE 2026-05-09
  Plan: 6 of 6 (26.1-01..26.1-06 all shipped — SYM-01, TUP-09, TUP-10, TUP-11, DICT-01/02/03)
  Status: Phase 26.1 fully shipped 2026-05-09. Symbol primitive (#foo, interned, pointer-equality), Tuple type (<<a, b, c>>, @N indexing, destructure assignment, structural equality, AnyArity sentinel), ~> flow op + (unpack) runtime builtin (TUP-10/TUP-11; charitable fallthrough on non-tuple LHS), generic Dict<K, V> (8-key-type allowlist, 14-op surface, OrderedDictionary backing, NaN-key Dict-internal special case, insertion-order preservation, immutable updates) all GREEN. Phase 18 ByteIdenticalShowcaseTests + Phase 25 ByteIdenticalShowcaseGaussianTests STILL GREEN. v1.3 milestone has 2 phases remaining: Phase 26.2 (Music Type Ergonomics + FX Overloads) and Phase 27 (Tutorial + Showcase Refresh).
  Last activity: 2026-05-09 - Phase 26.1 closure (plan 26.1-06)
  ```

  **3c. Resume Instructions** (around lines 36-45). Replace with: "Phase 26.1 fully shipped 2026-05-09. v1.3 milestone now 10/12 phases complete. Next ROADMAP target is Phase 26.2 (Music Type Ergonomics + FX Overloads), then Phase 27 (Tutorial + Showcase Refresh) to close the milestone."

The Phase 17 HUMAN-UAT note (rows 1-3 of manual-smoke.md) carries forward unchanged — orthogonal to v1.3 work.

ALSO append a new Performance Metrics row to the table at lines 110+ for Phase 26.1's 6 plans (use the actual values once execution-time data is available; for the closure plan rows use placeholders like `~Xmin | N tasks | M files`).

**4. Verification within this task: do not commit yet.** Re-read the 3 modified files after editing to confirm:
- DICT-01/02/03 text matches CONTEXT § REQUIREMENTS vs ROADMAP reconciliation
- ROADMAP plan list has 6 entries with correct commit hashes
- STATE.md current-position references Phase 26.1 complete

Commit at the end of Task 2 alongside the CLAUDE.md and verification doc updates.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; git log --oneline -10 &amp;&amp; grep -E "Phase 26\\.1|SYM-01|TUP-09|TUP-1[01]|DICT-0[123]" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Shipped" .planning/REQUIREMENTS.md` shows >= 7 new entries (SYM-01, TUP-09, TUP-10, TUP-11, DICT-01, DICT-02, DICT-03)
    - `grep -A2 "DICT-01" .planning/REQUIREMENTS.md` shows the new generic `Dict<K, V>` text (NOT the stale `Dict[T]` with String keys)
    - `grep -E "8-element allowlist|Tuple-of-hashables" .planning/REQUIREMENTS.md` returns at least 1 line (DICT-01 mentions the allowlist)
    - `grep -c "26.1-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns at least `6`
    - `grep -E "Phase 26\\.1.*Complete|Phase 26\\.1.*Shipped" .planning/ROADMAP.md` returns at least `1` (Phase 26.1 marked complete in Progress table or phase-summary list)
    - `grep -E "Phase 26\\.1.*COMPLETE|Phase 26\\.1 fully shipped" .planning/STATE.md` returns at least `1`
    - `grep -E "Phase 26\\.2.*next|26\\.2.*remaining" .planning/STATE.md` returns at least `1` (Resume points forward correctly)
    - `git log --oneline -10` shows 5 Phase 26.1 feature commits (Waves 0-4) prior to this closure task — i.e. the hashes inserted in REQUIREMENTS.md / ROADMAP.md correspond to actual commits
  </acceptance_criteria>
  <done>REQUIREMENTS.md DICT entries rewritten + Traceability shipped; ROADMAP.md Phase 26.1 marked complete with plan list + commit hashes; STATE.md advances to Phase 26.2-as-next.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md (Core Language Features bullets + AST table rows); author 26.1-VERIFICATION.md; flip 26.1-VALIDATION.md frontmatter; commit Wave 5 closure</name>
  <files>
    CLAUDE.md,
    .planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md,
    .planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md
  </files>
  <read_first>
    - CLAUDE.md lines 140-200 (Core Language Features bullets + AST table — locate exact insertion points)
    - .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md (TEMPLATE for closure verification — Phase 26's most recent precedent)
    - .planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md (frontmatter to flip to shipped)
    - All 5 plan SUMMARY.md files from plans 26.1-01..26.1-05 (the cross-references for VERIFICATION.md)
  </read_first>
  <action>
**1. `CLAUDE.md`** — Two coordinated edits:

  **1a. Core Language Features section (lines 142-153 area).** Locate the bulleted list under `### Core` (near "Static typing", "Flow operator `->`", etc.). Append 5 new bullets at the end of the Core block (just before the `### Music-Specific` header):
  ```markdown
  - **Symbol primitive type:** `#foo` interned literal — pointer-equality, strict separation from String (Phase 26.1)
  - **Tuple type:** `<<a, b, c>>` literal with per-position types and arity (`<<>>` empty + `<<x>>` singleton valid); `tup@N` indexing; `<<a, b>> = expr` destructuring assignment; structural equality (Phase 26.1)
  - **`~>` flow operator:** unpacks a tuple into a multi-arg call as a parse-time chain operator; falls through to single-arg `->` semantics on non-tuple LHS (Phase 26.1)
  - **Generic `Dict<K, V>`:** insertion-order preservation; allowed keys are Int / Long / Float / String / Symbol / Note / Chord / Tuple-of-hashables; constructed via `(dict K V K V ...)` or `(dictTuple <<K,V>> ...)` builtins; 14-op surface — `(get)` / `(getOr)` / `(set)` / `(remove)` / `(has)` / `(keys)` / `(values)` / `(size)` / `(merge)` / `(each)` / `(map)` / `(filter)` (Phase 26.1)
  - **`(unpack tuple func)`:** runtime first-class apply — S-expression equivalent of `~>`; mirrors Lisp's `(apply f args)` (Phase 26.1)
  ```

  **1b. AST table (lines 168-186).** Locate the `### Expressions (12 types in Ast/Expressions/)` table. Update the count in the heading to `15 types` (12 + 3 new). Append 3 new rows BEFORE the `**Note:**` block following the table:
  ```markdown
  | `SymbolLiteralExpression` | Symbol literal (`#foo`) — interned at evaluation time via `ExecutionContext.SymbolInternTable` |
  | `TupleLiteralExpression` | Tuple literal (`<<a, b, c>>`) with empty + singleton arities |
  | `TupleUnpackFlowExpression` | Tuple-unpack flow operator (`~>`) — runtime dispatch with non-tuple fallthrough |
  ```

  Locate the `### Statements (8 types in Ast/Statements/)` table. Update the count in the heading to `9 types`. Append:
  ```markdown
  | `TupleDestructureStatement` | Destructuring assignment (`<<Type? name, Type? name>> = expr`) |
  ```

  DO NOT touch the rest of CLAUDE.md (Audio Pipeline / Music-Specific / Architecture sections etc.).

**2. `.planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md`** (NEW) — Author the closure verification document. Mirror the structure of `.planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md`:

```markdown
---
phase: 26.1-symbols-tuples-dicts
status: shipped
shipped: 2026-05-09
nyquist_compliant: true
---

# Phase 26.1 — Verification (Closure)

**Goal:** Three tightly-coupled language additions land together — Symbol primitive, Tuple type, generic `Dict<K, V>` with hashable keys + 14-op surface.

## Plans (6/6)

| Plan | Wave | Requirement | Commit | Status |
|------|------|-------------|--------|--------|
| 26.1-01 | 0 | (scaffolding) | <hash-01> | ✅ Shipped |
| 26.1-02 | 1 | SYM-01 | <hash-02> | ✅ Shipped |
| 26.1-03 | 2 | TUP-09 | <hash-03> | ✅ Shipped |
| 26.1-04 | 3 | TUP-10, TUP-11 | <hash-04> | ✅ Shipped |
| 26.1-05 | 4 | DICT-01, DICT-02, DICT-03 | <hash-05> | ✅ Shipped |
| 26.1-06 | 5 | (closure) | <hash-06> | ✅ Shipped |

## Must-Haves Audit

(Cross-reference each plan's `must_haves.truths` and confirm GREEN. Bullet each truth with ✅ + commit ref.)

### Plan 26.1-01 (Wave 0)
- ✅ 10 xUnit Fact files exist under flow-lang.Tests/Unit/Phase26_1/ — verified via ls
- ✅ 9 acceptance .flow scripts in tests/ run cleanly under stub-body interpreter (Wave 0 gate)
- ✅ FlowScriptData.ExpectedErrorScripts entry for test_dict_type_errors.flow added (commented through Waves 0-3, uncommented in Wave 4)

### Plan 26.1-02 (Wave 1 — SYM-01)
- ✅ #foo lexes as SymbolLiteral; # is a token boundary
- ✅ FlowType.IsHashable() virtual + 7 hashable overrides (Int/Long/Float/String/Symbol/Note/Chord)
- ✅ ExecutionContext.SymbolInternTable per-context
- ✅ Value.Symbol(name, ctx) factory with reference-equal interning (Assert.Same in SymbolFacts.Interning_SamePointer)
- ✅ (equals #foo "foo") returns false (StrictSeparation_SymbolNeqString)
- ✅ TypeParser recognizes Symbol identifier; Parser dispatches SymbolLiteral to SymbolLiteralExpression
- ✅ SymbolFacts (3) GREEN; tests/test_symbol_literal.flow PASSED

### Plan 26.1-03 (Wave 2 — TUP-09)
- ✅ Lexer emits LessLess / GreaterGreater two-char tokens at expression-start ONLY (gate verified by note-stream `>` accent regression)
- ✅ TupleLiteralExpression + TupleDestructureStatement AST nodes ship
- ✅ TupleType with per-position element types + AnyArity sentinel + IsHashable recursion
- ✅ TypeParser parses Tuple<<...>> annotation supporting both LessLess and two-LessThan token forms
- ✅ ArrayIndexExpression evaluator extends to dispatch on tuple operand type (Q4 reuse)
- ✅ Value.Tuple factory + ToString <<...>> branch
- ✅ Interpreter handles TupleDestructureStatement with arity + per-slot type checking
- ✅ TupleFacts (6) + TupleDestructureFacts (3) GREEN
- ✅ Note-stream `>` accent regression GREEN (Pitfall 1 verified)

### Plan 26.1-04 (Wave 3 — TUP-10/TUP-11)
- ✅ Lexer emits TildeArrow BEFORE single-char Tilde at SimpleLexer.cs:131
- ✅ Parser ParseFlowExpression branches on TildeArrow → always emits TupleUnpackFlowExpression (no parse-time arity peek; RESEARCH Q5)
- ✅ Evaluator: tuple LHS unpacks into positional args; non-tuple LHS falls through to single-arg semantics (charitable; ROADMAP success criterion 3)
- ✅ DictFunctions.Unpack with [TupleType.AnyArity, FunctionType.Instance] signature
- ✅ std.flow `internal proc unpack` declaration
- ✅ UnpackFlowFacts (2) + UnpackRuntimeFacts (4) GREEN; both .flow scripts PASSED
- ✅ Note-stream tied-note `~` regression GREEN (Pitfall 3 — `~>` two-char before `~` single-char)

### Plan 26.1-05 (Wave 4 — DICT-01/02/03)
- ✅ DictType + DictData (OrderedDictionary<Value, Value>) + DictKeyComparer (NaN/Symbol/Tuple branches; default Utils.StrictEquals)
- ✅ TypeParser Dict<K, V> branch with IsHashable() enforcement at K position
- ✅ Value.Dict factory + ToString {k: v, ...} branch
- ✅ 14 dict ops in DictFunctions.cs (Dict, DictTuple, Get, GetOr, Set, Remove, Has, Keys, Values, Size, Merge, Each, Map, Filter)
- ✅ BuiltInFunctions.RegisterDict registers all 14 with separate `(each Dict ...)` overload coexisting with existing `(each Array ...)` (Pitfall 6 verified by DictOpsFacts.EachUnpacks)
- ✅ NaN-as-key Dict-internal scope verified by DictNanKeyFacts.GeneralEqualityUnchanged regression (Pitfall 3 — Utils.StrictEquals UNTOUCHED)
- ✅ DictKeyTypeFacts (8 allowed-key rows) + DictTypeRejectionFacts (6 disallowed-key rows) + DictConstructFacts (2) + DictOpsFacts (13+) + DictNanKeyFacts (2) all GREEN
- ✅ test_dict_construct.flow + test_dict_ops.flow + test_dict_keys.flow PASSED
- ✅ test_dict_type_errors.flow ParseException matches FlowScriptData ExpectedErrorScripts substring

### Plan 26.1-06 (Wave 5 — closure)
- ✅ REQUIREMENTS.md DICT-01/02/03 rewritten per CONTEXT § REQUIREMENTS vs ROADMAP reconciliation
- ✅ REQUIREMENTS.md Traceability rows shipped for SYM-01..DICT-03
- ✅ ROADMAP.md Phase 26.1 entry: Plans 6, plan list + commit hashes, Progress table Complete 2026-05-09
- ✅ STATE.md current-position points to Phase 26.2 next
- ✅ CLAUDE.md Core Language Features + AST table updated
- ✅ 26.1-VERIFICATION.md authored (this document)
- ✅ 26.1-VALIDATION.md frontmatter status: shipped, nyquist_compliant: true, wave_0_complete: true
- ✅ examples/tutorial.flow + examples/showcase.flow NOT touched (Phase 27's job)

## Regression Gates

| Gate | Status |
|------|--------|
| Phase 18 ByteIdenticalShowcaseTests | ✅ GREEN |
| Phase 25 ByteIdenticalShowcaseGaussianTests | ✅ GREEN |
| `dotnet test` full suite | ✅ GREEN (allow only the 2 pre-existing DecibelBeatNumericCompatFacts deferred per STATE.md) |
| Note-stream `>` accent (Pitfall 1) | ✅ GREEN |
| Note-stream `~` tied-note (Pitfall 3-lex) | ✅ GREEN |
| Utils.StrictEquals untouched (Pitfall 3-eq) | ✅ Verified by `git diff flow-lang/StandardLibrary/Utils.cs` empty |
| Collections.cs Each/Map/Filter untouched (Pitfall 6) | ✅ Verified by `git diff flow-lang/StandardLibrary/Collections.cs` empty for these methods |

## Sign-Off

Phase 26.1 closure verified. v1.3 milestone progresses to Phase 26.2 (Music Type Ergonomics + FX Overloads).

**Approver:** automated (planner + executor + checker per GSD).
**Date:** 2026-05-09.
```

Replace each `<hash-NN>` with the actual commit hash. The commit list comes from `git log --oneline -10`.

**3. `.planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md`** — Update the frontmatter:
- `status: shipped` (was draft)
- `nyquist_compliant: true` (was false)
- `wave_0_complete: true` (was false)
- `Approval: approved 2026-05-09 (planner + executor + checker per GSD)` in the Sign-Off section at the bottom (was "pending")

The body of the document otherwise stays as-is — it's the strategy document, not the closure record.

**4. Wave 5 closure commit** atomic per the Phase 26-05 / 25-04 / 23-05 docs-only-closure precedent. Commit message: `docs(26.1): closure — REQUIREMENTS/ROADMAP/STATE/VERIFICATION/CLAUDE.md`. Pre-commit verification:
- `dotnet test` — full suite GREEN (no regressions; the 2 pre-existing DecibelBeat deferred items remain logged but orthogonal)
- `dotnet test --filter "FullyQualifiedName~Phase26_1|FullyQualifiedName~Phase18|FullyQualifiedName~Phase25"` — GREEN
- All 5 plan SUMMARY.md files exist under `.planning/phases/26.1-symbols-tuples-dicts/`
- `examples/tutorial.flow` and `examples/showcase.flow` NOT modified — verified by `git diff examples/tutorial.flow examples/showcase.flow` empty

After the commit, run a final regression sample loop to be safe (matches Phase 26 closure pattern):
```
dotnet run --project flow-interpreter examples/tutorial.flow > /dev/null
dotnet run --project flow-interpreter examples/showcase.flow > /dev/null
```
Both should exit 0 with non-empty `examples/output/*.wav` and `*.mid` produced.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --logger "console;verbosity=minimal" 2>&amp;1 | grep -E "(Failed|Passed):" &amp;&amp; grep -c "Phase 26\\.1\\|Symbol primitive\\|TupleLiteralExpression\\|Generic .Dict.K, V.\\|~> flow operator" CLAUDE.md &amp;&amp; ls -la .planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md &amp;&amp; head -10 .planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md &amp;&amp; git diff --name-only examples/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Symbol primitive type:" CLAUDE.md` returns at least `1`
    - `grep -c "Tuple type:" CLAUDE.md` returns at least `1`
    - `grep -c "Generic .Dict.K, V" CLAUDE.md` returns at least `1` (handles backtick-escaped form)
    - `grep -c "SymbolLiteralExpression" CLAUDE.md` returns at least `1` (AST table row)
    - `grep -c "TupleLiteralExpression" CLAUDE.md` returns at least `1`
    - `grep -c "TupleUnpackFlowExpression" CLAUDE.md` returns at least `1`
    - `grep -c "TupleDestructureStatement" CLAUDE.md` returns at least `1`
    - File `.planning/phases/26.1-symbols-tuples-dicts/26.1-VERIFICATION.md` exists with at least 50 lines and contains "✅ Shipped" and "Sign-Off"
    - `head -10 .planning/phases/26.1-symbols-tuples-dicts/26.1-VALIDATION.md | grep -E "status: shipped|nyquist_compliant: true|wave_0_complete: true"` returns at least 3 lines
    - `git diff --name-only examples/` shows NO modifications to examples/tutorial.flow or examples/showcase.flow
    - `dotnet test` (full suite) reports `Failed:` count <= 2 (only the pre-existing DecibelBeat deferred items)
    - `dotnet test --filter "FullyQualifiedName~Phase26_1"` reports `Failed: 0` and >= 49 passing facts (3 SymbolFacts + 9 TupleFacts/Destructure + 6 UnpackFlow/Runtime + 8+6+2+13+2 = 31 Dict... approx 49+ total)
    - `dotnet test --filter "FullyQualifiedName~Phase18|FullyQualifiedName~Phase25"` reports `Failed: 0`
    - `git log --oneline -1 | grep -E "docs\\(26\\.1\\):.*closure"` confirms the Wave 5 commit message
  </acceptance_criteria>
  <done>Wave 5 docs-only closure commit lands; CLAUDE.md updated; 26.1-VERIFICATION.md authored; 26.1-VALIDATION.md frontmatter shipped; examples/tutorial.flow + examples/showcase.flow UNTOUCHED; full test suite green; byte-identical regression intact.</done>
</task>

</tasks>

<threat_model>
No threat surface — interpreter internals; new tokens/builtins do not introduce parsable user data ingress beyond existing source-file pipeline.
</threat_model>

<verification>
- REQUIREMENTS.md DICT-01/02/03 rewritten + Traceability shipped
- ROADMAP.md Phase 26.1 marked Complete 2026-05-09 with full plan list + commit hashes; Progress table updated
- STATE.md current-position advances to Phase 26.2 next
- 26.1-VERIFICATION.md authored with full plan-by-plan must-have evidence
- 26.1-VALIDATION.md frontmatter flipped to shipped + nyquist_compliant
- CLAUDE.md Core Language Features + AST table updated (5 bullets + 4 rows)
- examples/tutorial.flow + examples/showcase.flow NOT modified
- Full dotnet test suite green (Phase 18 + 25 byte-identical regression intact)
</verification>

<success_criteria>
- All 6 plans of Phase 26.1 documented as Shipped
- v1.3 progress: 10/12 phases complete
- Phase 26.2 is the next ROADMAP target
- Tutorial-side Symbol/Tuple/Dict additions deferred to Phase 27 per CONTEXT § Claude's Discretion
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-symbols-tuples-dicts/26.1-06-SUMMARY.md` documenting the closure.
</output>
</content>
</invoke>
