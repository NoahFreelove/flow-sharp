---
phase: 26.1
slug: symbols-tuples-dicts
status: shipped
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-09
shipped: 2026-05-09
---

# Phase 26.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `26.1-RESEARCH.md` § Validation Architecture (canonical map).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit (existing — `flow-lang.Tests/flow-lang.Tests.csproj`) |
| **Config file** | `flow-lang.Tests/flow-lang.Tests.csproj` |
| **Quick run command** | `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase26_1"` |
| **Full suite command** | `dotnet test` |
| **Flow-script smoke** | `dotnet run --project flow-interpreter tests/test_NAME.flow` |
| **Estimated runtime** | ~5 min full; ~30 s per-wave filtered |

---

## Sampling Rate

- **After every task commit:** Run the wave's relevant `*Facts.cs` filter only (e.g., Wave 1 → `--filter "FullyQualifiedName~SymbolFacts"`) PLUS the byte-identical regression filters (`Phase18`, `Phase25`). Target latency: <30 s.
- **After every plan wave:** Run all Phase 26.1 Facts + xUnit Integration tests (~5 min).
- **Before `/gsd-verify-work`:** Full `dotnet test` suite green AND all 9 acceptance `.flow` smoke scripts run to completion.
- **Max feedback latency:** 30 s for per-task; 5 min for per-wave.

---

## Per-Task Verification Map

> See `26.1-RESEARCH.md` § Validation Architecture lines 847-883 for the canonical Req-ID → Test map. The planner expands this into per-Plan / per-Task rows below as plans are written.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| TBD-by-planner | TBD | 1-5 | SYM-01, TUP-09, TUP-10, TUP-11, DICT-01, DICT-02, DICT-03 | unit Fact + smoke | per-row in RESEARCH.md table | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `flow-lang.Tests/Unit/Phase26_1/` directory created
- [x] xUnit fact-file scaffolding (stubs only, all skipped):
  - [x] `SymbolFacts.cs`
  - [x] `TupleFacts.cs`
  - [x] `TupleDestructureFacts.cs`
  - [x] `UnpackFlowFacts.cs`
  - [x] `UnpackRuntimeFacts.cs`
  - [x] `DictKeyTypeFacts.cs`
  - [x] `DictConstructFacts.cs`
  - [x] `DictOpsFacts.cs`
  - [x] `DictNanKeyFacts.cs`
  - [x] `DictTypeRejectionFacts.cs`
- [x] 9 acceptance `tests/test_*.flow` files stubbed (one per CONTEXT § Specifics block)
- [x] `FlowScriptData.ExpectedErrorScripts` entry for `test_dict_type_errors.flow` (intentional-error path)

*No framework install needed — xUnit already present.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| (none expected — all 9 acceptance specifics are scriptable) | — | — | — |

*All phase behaviors have automated verification.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30 s (per-task), < 5 min (per-wave)
- [x] Phase 18 + Phase 25 byte-identical regression remains GREEN every commit
- [x] `nyquist_compliant: true` set in frontmatter once all rows green

**Approval:** approved 2026-05-09 (planner + executor + checker per GSD)
