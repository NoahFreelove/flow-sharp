---
phase: 37-sound-design-sampler-polish
plan: 06
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - flow-lang/sfz.flow
  - flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
  - .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md
  - flow-lang.Tests/Integration/Phase37/SfzDrumsLoadTest.cs
  - flow-lang.Tests/Integration/Phase37/DrumPitchShiftAutoTests.cs
autonomous: false
requirements: [DRUM-01]
user_setup:
  - service: vsco-ce-drum-sfz
    why: "Per D-37-13 (CONTEXT): sampled drums route via VSCO Community Edition's GM-StylePerc.sfz patch through the existing Phase 33 SFZ surface, NOT a new bundled .wav directory. DRUM-01 ships zero new asset files in flow-lang/Samples/drums/; the SFZ patch lives wherever the composer's `sfz_root` config key (Phase 30) points. Per D-37-14: pitch shift routes through DSP-02/03 #auto pipeline (Plan 37-02 dependency)."
    env_vars: []
    dashboard_config:
      - task: "Composer installs VSCO Community Edition SFZ branch + configures sfz_root in ~/.config/flow/config.toml"
        location: "https://github.com/sgossner/VSCO-2-CE — SFZ branch contains GM-StylePerc.sfz at top level"
        steps:
          - "git clone --branch SFZ https://github.com/sgossner/VSCO-2-CE.git ~/.local/share/vsco-ce (or any path the composer prefers)"
          - "Verify file presence: ls ~/.local/share/vsco-ce/GM-StylePerc.sfz exists (~ a few KB; VSCO-CE is ~ 1.5 GB total — full clone is large but only the percussion SFZ + referenced samples are loaded on demand)"
          - "Edit ~/.config/flow/config.toml: set sfz_root = \"~/.local/share/vsco-ce\" (or absolute path — Phase 30 config key, XDG-compliant)"
          - "Test: dotnet run --project flow-interpreter -e 'use \"@sfz\" Sfz drums = (loadSfz #drums); (print (str drums))' — expect 'Sfz(GM-StylePerc.sfz, N regions)' or similar (Phase 33 (str Sfz) format)"
must_haves:
  truths:
    - "(loadSfz #drums) resolves to GM-StylePerc.sfz via the sfz.flow GM dict; Phase 33 SFZ surface validates parsing without error"
    - "VSCO-PATH-AUDIT.md (Phase 33 doc) flips the 20th entry (drums) verified per RESEARCH §Pattern 11"
    - "Composer can write Sfz drums = (loadSfz #drums); renderSong song \"sampler:drums\" — kick (MIDI 36) + snare (MIDI 38) + hi-hat (MIDI 42) trigger their VSCO regions"
    - "**W7 LOCK (revision pass 2/3)**: Drum-vs-non-drum detection lives at SfzBuiltins LOAD TIME via the dict-symbol arg, NOT via filename-suffix inspection. When `loadSfz` is called with `#drums`, the resolved SfzData gets `IsPercussion = true`. The dict-symbol is the source of truth — composers who later move GM-StylePerc.sfz, rename it, or extend the dict with custom percussion patches don't lose the percussion flag."
    - "Drum note pitch-shift routes through #auto pipeline (D-37-14 + Plan 37-02 dependency) — PSOLA for transient kits, vocoder for sustained cymbal/gong — gated on SfzData.IsPercussion (W7 LOCK), not filename"
    - "Pitch shifts > 12 semitones from sample center emit one-shot stderr advisory '[pitchShift] >12st shift on drum sample at MIDI N — varispeed artifacts likely dominate' per RESEARCH §Pattern 11 sub-recommendation + OQ3"
    - "Round-robin (SAMP-01) + velocity crossfade (SAMP-02) opcodes from Plan 37-03 already exercise heavily in VSCO drums (kick has 7 vel layers × 2 rr alternates per RESEARCH §Pattern 11)"
  artifacts:
    - path: "flow-lang/sfz.flow"
      provides: "GM dict gains #drums entry — 20th entry (was 19, RESEARCH §Pattern 11 + integration step 1)"
      contains: "#drums.*GM-StylePerc"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      provides: "**W7 LOCK** — SfzData record gains `bool IsPercussion` field (default false; set true by SfzBuiltins.LoadSfzSymbol when the dict-symbol arg matches `#drums`)"
      contains: "IsPercussion"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs"
      provides: "**W7 LOCK** — LoadSfzSymbol marks SfzData.IsPercussion = true when the resolved symbol is `#drums`. Constructs the SfzData via the new positional-with-default field or via a wither, depending on the existing record shape."
      contains: "IsPercussion = true"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      provides: "Drum-render path threads through #auto pitch-shift dispatcher (Plan 37-02 PitchShiftEngine) for non-recorded pitches — GATED on sfz.IsPercussion (W7 LOCK)"
      contains: "PitchShiftEngine\\|IsPercussion"
    - path: ".planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md"
      provides: "20th GM-dict entry marked verified post-Plan-37-06"
  key_links:
    - from: "flow-lang/sfz.flow"
      to: "GM-StylePerc.sfz (external VSCO-CE)"
      via: "Composer's sfz_root + dict lookup"
      pattern: "#drums.*GM-StylePerc.sfz"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs"
      to: "flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs"
      via: "W7 LOCK — when symbol == #drums, construct SfzData with IsPercussion = true"
      pattern: "IsPercussion = true"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      to: "flow-lang/StandardLibrary/Audio/DSP/PitchShiftEngine.cs"
      via: "When drum note pitch ≠ sample center AND sfz.IsPercussion == true, route through PitchShiftEngine.Process(..., StretchMode.Auto)"
      pattern: "PitchShiftEngine\\.Process"
---

<objective>
Plan 37-06 ships DRUM-01: sampled drums via the VSCO Community Edition SFZ surface (D-37-13 lock) with transient-preserving pitch shift via Plan 37-02's `#auto` pipeline (D-37-14 lock).

Strict dependency on Plan 37-02: `PitchShiftEngine` and `StretchEngine` MUST be available before Plan 37-06 can ship. Per D-37-05 wave order: Wave 3 (after 37-02 lands in Wave 2).

**Open Question Resolution (OQ3 — DRUM-01 dependency on DSP-02/03):** locked to **strict** per D-37-14. Most composer drum notes land at GM percussion MIDI numbers (36/38/40/42/49/etc.) which ARE recorded in GM-StylePerc.sfz — no pitch shift needed in the common case. The `#auto` pitch-shift path activates ONLY for non-GM-mapped percussion lookups (e.g. composer authors a kick at MIDI 35 instead of 36, or experiments with detuned drums). Emit stderr advisory when shifting > 12 semitones (per RESEARCH §Pattern 11 sub-recommendation): varispeed artifacts dominate at large shifts so composer should know they're outside the intended sample range.

**W7 LOCK (revision pass 2/3, plan-checker WARNING):** The prior revision detected "is this a drum patch?" via filename inspection (`sfz.PatchPath.EndsWith("Perc.sfz")`). That's fragile: composers can rename the file, fork VSCO-CE under a different filename, or extend the GM dict with custom percussion patches. The dict-symbol passed to `loadSfz` is the natural source of truth — if the composer says `#drums`, they're loading a percussion patch by construction.

Fix locked in this plan: extend `SfzData` with a `bool IsPercussion` field (default false). In `SfzBuiltins.LoadSfzSymbol`, when the resolved symbol matches `#drums`, construct the returned SfzData with `IsPercussion = true`. SfzRenderer then gates the `#auto` pitch-shift route on `sfz.IsPercussion` (NOT filename). Per the W7 spec line: *"The dict-symbol is the source of truth, NOT the filename."*

**autonomous: false** because the composer must install VSCO-CE locally + configure `sfz_root` before any DRUM-01 test can run.

Output: 1 dict entry in sfz.flow + 1 SfzData field addition + 1 SfzBuiltins flag set + 1 SfzRenderer code path for drum pitch-shift + 1 VSCO-PATH-AUDIT.md flip + 2 test classes filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-sound-design-sampler-polish/37-CONTEXT.md
@.planning/phases/37-sound-design-sampler-polish/37-RESEARCH.md
@.planning/phases/37-sound-design-sampler-polish/37-PATTERNS.md
@.planning/phases/37-sound-design-sampler-polish/37-02-SUMMARY.md
@.planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md

@CLAUDE.md

@flow-lang/sfz.flow
@flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
@flow-lang/StandardLibrary/Audio/DSP/PitchShiftEngine.cs
@flow-lang/StandardLibrary/Audio/DSP/StretchEngine.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs

<interfaces>
From Plan 37-02 outputs (already shipping in Wave 2):

namespace FlowLang.StandardLibrary.Audio.DSP;
public enum StretchMode { Vocoder, Psola, Auto }
public static class PitchShiftEngine {
    public static AudioBuffer Process(AudioBuffer input, double cents,
                                       StretchMode mode = StretchMode.Auto,
                                       int frameSize = 2048, int hopSize = 512, int overlap = 4,
                                       double transientThreshold = 0.3,
                                       int? pitchPeriod = null, int? windowSize = null,
                                       SourceLocation site = default);
}

From flow-lang/sfz.flow (Phase 33 — current 19-entry dict at lines 38-62):

Dict<Symbol, String> __sfzInstruments = (dict
    ...
    Note: ----- Keys + Plucked + Percussion (3 verified) -----
    #piano        "UprightPiano.sfz"
    #harp         "Harp.sfz"
    #timpani      "Timpani.sfz"
    Note: ----- 4 TBD rows (not bundled with VSCO-CE 1.1.0) -----
    ...
)

From flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs (current shape — audit-confirmed):

public sealed record SfzData(
    string Description,
    string BasePath,
    IReadOnlyList<SfzRegion> Regions,
    SfzRegion?[,] Grid,
    int[] SortedByPitch);

W7 LOCK extension (this plan):

public sealed record SfzData(
    string Description,
    string BasePath,
    IReadOnlyList<SfzRegion> Regions,
    SfzRegion?[,] Grid,
    int[] SortedByPitch,
    bool IsPercussion = false);
// IsPercussion: true ONLY when SfzBuiltins.LoadSfzSymbol was invoked with #drums
// (or any future percussion-class symbol). Filename is NOT consulted.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Composer installs VSCO-CE SFZ bundle + configures sfz_root</name>
  <what-built>
    DRUM-01 routes through the Phase 33 SFZ surface and the composer's locally-installed VSCO Community Edition. Plan 37-06 ships NO new sample asset files. The composer MUST install VSCO-CE before any DRUM-01 test can run.
  </what-built>
  <how-to-verify>
    Composer setup steps:

    1. Verify Phase 33 `sfz_root` config is already configured (Phase 33 UAT step):
       ```bash
       cat ~/.config/flow/config.toml | grep sfz_root
       # Expected: sfz_root = "/path/to/vsco-ce" (or similar)
       ```
       If `sfz_root` is already configured AND the directory contains `GM-StylePerc.sfz`, skip to step 3.

    2. If not configured OR GM-StylePerc.sfz missing:
       ```bash
       git clone --branch SFZ https://github.com/sgossner/VSCO-2-CE.git ~/.local/share/vsco-ce
       # This is a large clone (~ 1.5 GB); composer can use --depth 1 to reduce time:
       # git clone --depth 1 --branch SFZ https://github.com/sgossner/VSCO-2-CE.git ~/.local/share/vsco-ce
       ```

       Edit `~/.config/flow/config.toml`:
       ```toml
       sfz_root = "~/.local/share/vsco-ce"
       # Tilde expansion is supported per Phase 30 config; alternatively absolute path.
       ```

    3. Verify the GM-StylePerc.sfz file is present + readable:
       ```bash
       ls $(eval echo ~/.local/share/vsco-ce)/GM-StylePerc.sfz
       # Expected: file exists, ~few KB
       ```

    4. Smoke-test the load via the existing Phase 33 surface:
       ```bash
       dotnet run --project flow-interpreter -e 'use "@sfz" Sfz drums = (loadSfz #drums); (print (str drums))'
       # Expected: an Sfz(...) description line; no errors. Currently FAILS because #drums is not in the GM dict — that's exactly what Task 2 adds.
       ```
       So the smoke test is expected to fail at the dict-lookup level until Task 2 runs. Composer's goal here is to confirm VSCO-CE is on disk + sfz_root resolves correctly.

    5. If composer cannot install VSCO-CE (network/storage/license): respond `defer to v1.6` and DRUM-01 ships as known-limitation in Phase 37 (sfz.flow gets a stub entry but no integration test exercises it; closer plan 37-07 documents).

    Resume only after `ls ~/.local/share/vsco-ce/GM-StylePerc.sfz` (or composer's chosen sfz_root) returns a valid file path AND the sfz_root config key is set in ~/.config/flow/config.toml.
  </how-to-verify>
  <resume-signal>Type `approved` (VSCO-CE installed + sfz_root configured + GM-StylePerc.sfz exists), `defer to v1.6` (DRUM-01 ships as stub-only, no integration test), or `escalate {reason}`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SfzData.IsPercussion field + SfzBuiltins symbol-driven flag + sfz.flow #drums entry + SfzRenderer drum-pitch-shift path + VSCO-PATH-AUDIT.md flip</name>
  <read_first>
    - flow-lang/sfz.flow (lines ~38-62 — existing 19-entry GM dict)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs (current shape — positional record; add IsPercussion = false default)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs (LoadSfzSymbol lines ~163 onward — SfzData construction site)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs (the pitch-shift / varispeed code path — find where Phase 29 handles non-center-pitch playback for sampled instruments)
    - flow-lang/StandardLibrary/Audio/DSP/PitchShiftEngine.cs (Plan 37-02 output)
    - .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md (Phase 33 audit doc — VSCO entry verification table)
    - .planning/phases/37-sound-design-sampler-polish/37-RESEARCH.md §Pattern 11 (VSCO-CE GM-StylePerc.sfz integration steps + > 12 semitone advisory recommendation)
    - .planning/phases/37-sound-design-sampler-polish/37-PATTERNS.md §sfz.flow (#drums entry insertion)
    - flow-lang.Tests/Integration/Phase37/SfzDrumsLoadTest.cs + DrumPitchShiftAutoTests.cs (Wave 0 scaffolds)
  </read_first>
  <files>
    flow-lang/sfz.flow,
    flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs,
    flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs,
    flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs,
    .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md,
    flow-lang.Tests/Integration/Phase37/SfzDrumsLoadTest.cs,
    flow-lang.Tests/Integration/Phase37/DrumPitchShiftAutoTests.cs
  </files>
  <behavior>
    - sfz.flow GM dict gains `#drums "GM-StylePerc.sfz"` entry; total entries 19 → 20 (per RESEARCH §Pattern 11 integration step 1). Update header xmldoc count.
    - **W7 LOCK** — SfzData record gains a 6th positional field `bool IsPercussion = false` with sentinel default. Existing constructor call sites continue compiling (positional record append preserves back-compat).
    - **W7 LOCK** — SfzBuiltins.LoadSfzSymbol: after resolving `args[0]` to a string symbol name, when the symbol matches `"drums"` (the unwrapped Phase 26.1 Symbol name from `#drums`), construct the returned SfzData with `IsPercussion: true`. For non-drum symbols (the other 19 GM entries), `IsPercussion` defaults to `false`.
    - **W7 LOCK** — SfzRenderer's pitch-shift path: when the composer's note MIDI pitch ≠ region's pitch_keycenter AND `sfz.IsPercussion == true` AND the absolute shift > 0, route through `PitchShiftEngine.Process(buffer, semitones * 100.0, StretchMode.Auto, site=...)` instead of varispeed (per D-37-14 transient-preserving guarantee). Filename is NOT consulted.
    - When the shift exceeds 12 semitones in absolute value (drum patches only), emit one-shot stderr `[pitchShift] >12st shift on drum sample at MIDI {N} — varispeed artifacts likely dominate (D-37-14 + RESEARCH Pattern 11 advisory)` per OQ3 resolution.
    - VSCO-PATH-AUDIT.md: flip the row for `#drums` from `TBD` to `verified — Plan 37-06`.
    - SfzDrumsLoadTest verifies the dict resolution works AND `IsPercussion == true`; DrumPitchShiftAutoTests verifies the #auto routing fires for non-recorded pitches AND gates on IsPercussion.
  </behavior>
  <action>
**flow-lang/sfz.flow** — insert the `#drums` entry under the "Keys + Plucked + Percussion" section (currently 3 verified entries — flipping to 4 with this addition):

Locate the existing block (~lines 38-62) and insert `#drums "GM-StylePerc.sfz"` adjacent to `#timpani`:
```
Note: ----- Keys + Plucked + Percussion (4 verified) -----
#piano        "UprightPiano.sfz"
#harp         "Harp.sfz"
#timpani      "Timpani.sfz"
#drums        "GM-StylePerc.sfz"
```

Update the xmldoc count claim in sfz.flow's header — every `19-entry` reference flips to `20-entry`. Use `grep -n "19-entry\|19 entries" flow-lang/sfz.flow` to find all hits and update each.

**SfzData.cs — W7 LOCK field addition** — open the file; the current record has 5 positional fields (Description, BasePath, Regions, Grid, SortedByPitch). Append `bool IsPercussion = false` as a 6th positional field with default. Use C# positional-record-with-default syntax:

```csharp
public sealed record SfzData(
    string Description,
    string BasePath,
    IReadOnlyList<SfzRegion> Regions,
    SfzRegion?[,] Grid,
    int[] SortedByPitch,
    bool IsPercussion = false);
```

Extend the xmldoc summary block with a `<item>` entry documenting `IsPercussion`: *"True when this Sfz value was produced by `loadSfz(#drums)` or any future percussion-class dict-symbol. False for all 19 non-drum GM-dict resolutions and for the `loadSfz(String)` path (filename arg is opaque to the load-time flag — composer using the string path opts out of percussion routing). Drives SfzRenderer's #auto pitch-shift gate per D-37-14 / W7 LOCK."*

The default = `false` ensures existing construction sites (Phase 33 parser tests, the `loadSfz(String)` bypass path, fixture loaders) continue compiling unchanged.

**SfzBuiltins.cs — W7 LOCK flag set** — open the file; locate `LoadSfzSymbol` around line 163. After the existing symbol-to-filename resolution (the dict lookup) AND before the SfzParser.Parse call OR after it (whichever provides the SfzData), inject the IsPercussion flag:

Pseudo-code (adapt to actual code shape):
```csharp
private static Value LoadSfzSymbol(IReadOnlyList<Value> args, FlowLang.Runtime.ExecutionContext ctx)
{
    // ... existing dict resolution producing `string sfzFilename` and parsing into `SfzData parsed` ...

    // W7 LOCK (revision pass 2/3) — dict-symbol drives percussion routing, NOT filename.
    bool isPercussion = symbolName == "drums";   // SymbolName is the unwrapped Phase 26.1 Symbol arg
    // Future expansion: && (otherPercussionSymbols.Contains(symbolName))
    SfzData result = parsed with { IsPercussion = isPercussion };
    // OR construct directly: SfzData result = new SfzData(parsed.Description, parsed.BasePath, parsed.Regions, parsed.Grid, parsed.SortedByPitch, IsPercussion: isPercussion);
    return Value.Sfz(result);
}
```

If the existing `LoadSfzSymbol` constructs SfzData directly (not via SfzParser.Parse), inject `IsPercussion: isPercussion` into that constructor call.

For the `loadSfz(String)` overload (`LoadSfzString` or equivalent): the filename arg is opaque — the composer chose the string path explicitly, so we treat `IsPercussion = false` by default. Composers who want percussion routing via the string path can manually wrap the result (future v1.6 builtin if needed); for v1.5 this is acceptable per the language philosophy (composer trust + don't over-engineer).

**SfzRenderer.cs — drum pitch-shift path GATED on IsPercussion (W7 LOCK)** — find the existing pitch-shift code (likely varispeed in `GetVarispeed` call, or similar — Phase 29 SampleCache.GetVarispeed pattern from PATTERNS analog). For SFZ-rendered notes where `note.MidiPitch != region.PitchKeycenter`, current Phase 33 behavior is likely varispeed (couples pitch + time).

Add the new code path GATED on `sfz.IsPercussion` (W7 LOCK):

Pseudo-code (adapt to the actual SfzRenderer signatures — the executor reads the code first):
```csharp
int semitones = note.MidiPitch - region.PitchKeycenter;

// DRUM-01 (D-37-14) — drums use #auto pitch-shift instead of varispeed.
// W7 LOCK: detect "drums" via SfzData.IsPercussion (set at SfzBuiltins load time by dict-symbol)
// — NOT via filename suffix. Composer-controlled at load time, robust to filename changes,
// extensible for future percussion-class dict-symbols.
bool isPercussionPatch = sfz.IsPercussion;

if (isPercussionPatch && semitones != 0)
{
    // > 12 semitone advisory (OQ3 resolution + RESEARCH Pattern 11)
    if (Math.Abs(semitones) > 12)
    {
        RenderingDiagnostics.WarnOnce(
            $"pitchShift:drum:large:{note.MidiPitch}:{region.PitchKeycenter}",
            $"[pitchShift] >12st shift on drum sample at MIDI {note.MidiPitch} (sample center MIDI {region.PitchKeycenter}) — varispeed artifacts likely dominate");
    }
    // Load raw sample at center pitch + pitch-shift via #auto:
    var rawAtCenter = _cache.LoadSampleData(sfz.BasePath, region.SamplePath);   // existing helper — adapt
    double cents = semitones * 100.0;
    fitted = PitchShiftEngine.Process(rawAtCenter, cents, StretchMode.Auto, site: ctx.SourceLocation);
}
else
{
    // Existing Phase 29/33 path (varispeed) — preserved for non-drum SFZ patches AND
    // for the loadSfz(String) bypass path where IsPercussion defaults to false.
    fitted = ...; // existing varispeed call
}
```

If the existing SfzRenderer pre-routes via filename in any other place (e.g. SampleCache lookup), keep those filename-based routes unchanged — W7 only governs the pitch-shift fork. The pitch-shift gate is the load-bearing decision; everything else (sample loading, region matching, envelope application) stays filename-agnostic per Phase 33's existing design.

**.planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md** — flip the `#drums` row in the verification table.

Read the existing file; find the row marked `TBD` (or equivalent — RESEARCH §Pattern 11 step 2 says "Mark the 20th entry verified"). Update to:
```
| #drums | GM-StylePerc.sfz | verified | Plan 37-06 (2026-XX-XX) | DRUM-01 — VSCO-CE percussion via Phase 33 SFZ surface; W7 LOCK — IsPercussion flag set at SfzBuiltins load time |
```

**Fill in SfzDrumsLoadTest.cs** (Wave 0 scaffold) — 3 `[Fact]` methods (skip all if composer chose `defer to v1.6` at the prior checkpoint):
1. `LoadSfzDrums_ResolvesFromGmDict` — execute `Sfz drums = (loadSfz #drums)` via FlowEngine; assert the returned Sfz value's `Description` ends with `GM-StylePerc.sfz`; assert at least 1 region exists in `drums.Regions` (parser should find ≥ 1 valid `<region>` block).
2. **W7 ACCEPTANCE** — `LoadSfzDrums_SetsIsPercussionTrue` — execute `Sfz drums = (loadSfz #drums)` AND `Sfz piano = (loadSfz #piano)`; assert `drums.IsPercussion == true` AND `piano.IsPercussion == false` (W7 LOCK — only the `#drums` symbol activates the flag, NOT the filename or any other property).
3. `LoadSfzDrums_SfzRootConfigured_FailsCleanlyIfMissing` — temporarily set `FlowConfig.SfzRoot = "/nonexistent/path"`; call `(loadSfz #drums)`; assert a clear ArgumentException OR charitable WarnOnce + empty Sfz (matches Phase 33 behavior for absent paths — verify which behavior is correct).

**Fill in DrumPitchShiftAutoTests.cs** (Wave 0 scaffold) — 3 `[Fact]` methods:
1. `DrumPitchShift_AtGmCenter_NoShiftNeeded` — render a kick (MIDI 36) through GM-StylePerc.sfz; verify the renderer picks the region with pitch_keycenter=36 + does NOT invoke PitchShiftEngine.Process (use a spy / RecordingPitchShiftEngine wrapper, OR inspect via debug counter).
2. `DrumPitchShift_OffCenter5Semitones_RoutesThroughAuto_NotVarispeed` — render a snare at MIDI 43 (which doesn't match a recorded VSCO snare center — common centers are 38 and 40); assert PitchShiftEngine.Process was invoked once with `cents ≈ 500.0` and `mode = StretchMode.Auto`; assert the result Buffer is non-empty + non-silent.
3. **W7 ACCEPTANCE** — `DrumPitchShift_NonPercussionPatchOffCenter_UsesVarispeed_NotAuto` — render a piano off-center via `Sfz piano = (loadSfz #piano)` (which has `IsPercussion == false`); assert PitchShiftEngine.Process was NOT invoked (W7 LOCK — gate on IsPercussion, not filename; piano's varispeed path stays untouched).

If composer chose `defer to v1.6`, all tests gain `[Fact(Skip = "DRUM-01 deferred — composer skipped VSCO-CE install per checkpoint")]`.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build -c Debug -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase37.SfzDrumsLoadTest|FullyQualifiedName~Phase37.DrumPitchShiftAutoTests" --no-build --logger "console;verbosity=minimal" 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>sfz.flow #drums entry added; **W7 LOCK honored — SfzData.IsPercussion field added with dict-symbol-at-load-time flag**; SfzRenderer routes drum pitch-shift through Plan 37-02's PitchShiftEngine GATED on IsPercussion; VSCO-PATH-AUDIT.md flipped; 6 facts pass (or 6 skipped if composer deferred).</done>
  <acceptance_criteria>
    - grep -c "#drums.*GM-StylePerc.sfz" flow-lang/sfz.flow returns &gt;= 1
    - grep -c "20-entry\|20 entries" flow-lang/sfz.flow returns &gt;= 1 (count updated)
    - **W7 ACCEPTANCE — SfzData field**: grep -c "IsPercussion" flow-lang/StandardLibrary/Audio/Sfz/SfzData.cs returns &gt;= 2 (field declaration + xmldoc reference)
    - **W7 ACCEPTANCE — SfzBuiltins flag-set**: grep -c "IsPercussion = true\|IsPercussion: true" flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs returns &gt;= 1 (the `symbolName == "drums"` branch sets the flag)
    - **W7 ACCEPTANCE — SfzRenderer gate**: grep -c "IsPercussion" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1 (gate on the flag, not on filename)
    - **W7 ACCEPTANCE — no filename-based gate**: grep -cE "EndsWith\s*\(\s*\"Perc\.sfz\"" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns 0 (the legacy filename-detection branch is REMOVED)
    - grep -c "PitchShiftEngine" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1
    - grep -c "&gt;12st\|>12st\|12 semitone" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1 (advisory threshold)
    - grep -c "#drums.*verified\|verified.*Plan 37-06" .planning/phases/33-sfz-orchestral-sampler/33-VSCO-PATH-AUDIT.md returns &gt;= 1
    - dotnet test --filter "FullyQualifiedName~Phase37.SfzDrumsLoadTest|FullyQualifiedName~Phase37.DrumPitchShiftAutoTests" --no-build reports zero failures
    - dotnet test --filter "FullyQualifiedName~Phase33" --no-build reports zero failures (existing SFZ surface preserved — non-drum patches default to IsPercussion=false, no behavioral change)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer sfz_root path -&gt; SfzParser file loading | Phase 33 contract: sfz_root anchored path resolution; no .. escape (Phase 33 SPEC-3) |
| composer-authored drum MIDI pitch -&gt; PitchShiftEngine | composer-supplied cents value via semitone math |
| composer dict-symbol arg to loadSfz -&gt; SfzData.IsPercussion | W7 LOCK — single source of truth for percussion routing decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06-01 | Tampering | composer points sfz_root at malicious SFZ patch | mitigate | Phase 33 SPEC-3 path-traversal protection (existing); SFZ opcode whitelist (Plan 37-03 extension) |
| T-37-06-02 | Integrity | DRUM-01 path activated for non-drum SFZ patches via filename inspection | mitigate | **W7 LOCK (revision pass 2/3)**: gate on `SfzData.IsPercussion` (dict-symbol-driven at load time), NOT on filename suffix. Renaming GM-StylePerc.sfz no longer disables the route; loading a non-drum patch via `#piano` keeps IsPercussion=false. |
| T-37-06-03 | DoS | composer authors drum at MIDI 0 with sample center MIDI 60 (60 semitone shift) | mitigate | WarnOnce on >12 semitone shift; PitchShiftEngine doesn't reject (composer trust per philosophy) |
| T-37-06-04 | Integrity | PitchShiftEngine called for shift=0 (Phase 33 byte-identical break) | mitigate | Plan 37-02 identity fast-path on cents=0.0 (Pitfall 11) — PitchShiftEngine returns input verbatim |
| T-37-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 37 zero external packages |
</threat_model>

<verification>
After tasks:
- `ls ~/.config/flow/config.toml` exists with `sfz_root = ...` configured (composer step at checkpoint)
- `dotnet run --project flow-interpreter -e 'use "@sfz" Sfz drums = (loadSfz #drums); (print (str drums))'` returns a valid Sfz description (or skipped if deferred)
- sfz.flow `#drums` entry present; count 19→20 updated
- **W7 LOCK verification** — SfzData.IsPercussion field present; SfzBuiltins sets it to true ONLY for `#drums`; SfzRenderer gates pitch-shift route on it; no filename-EndsWith call in SfzRenderer
- VSCO-PATH-AUDIT.md flipped for #drums row
- SfzDrumsLoadTest + DrumPitchShiftAutoTests pass (or all 6 skipped if composer deferred) — includes the W7-specific tests for `LoadSfzDrums_SetsIsPercussionTrue` AND `DrumPitchShift_NonPercussionPatchOffCenter_UsesVarispeed_NotAuto`
- Phase 33 regression tests unchanged (existing 19 GM-dict entries still resolve; default IsPercussion=false preserves byte-identical varispeed path)
- Composer smoke: render a simple drum line through `renderSong song "sampler:drums"` — kick + snare + hat trigger correct GM regions, no errors
</verification>

<success_criteria>
- DRUM-01 ships: sampled drums via the Phase 33 SFZ surface using composer-installed VSCO-CE.
- `#drums` entry added to sfz.flow GM dict (20-entry total).
- **W7 LOCK honored (revision pass 2/3)**: SfzData.IsPercussion field added; SfzBuiltins.LoadSfzSymbol sets it true at load time when the dict-symbol is `#drums`; SfzRenderer gates pitch-shift route on the flag, not filename. Filename-based detection removed.
- Non-center pitches route through Plan 37-02's PitchShiftEngine #auto path (D-37-14 strict dependency satisfied) — but ONLY for percussion patches per the IsPercussion gate.
- `>12 semitone` shift advisory emits one-shot per Pattern 11 + OQ3 resolution.
- VSCO-PATH-AUDIT.md flipped to mark the 20th entry verified.
- Phase 33 SFZ surface preserved (default IsPercussion=false preserves byte-identical pre-W7 behavior for non-drum patches).
</success_criteria>

<output>
Create `.planning/phases/37-sound-design-sampler-polish/37-06-SUMMARY.md`. Record:
- Composer's verdict at checkpoint (`approved` / `defer to v1.6`)
- Composer's chosen sfz_root path
- VSCO-CE clone size (full vs shallow `--depth 1` for posterity)
- **W7 LOCK evidence (revision pass 2/3)**: grep counts for `IsPercussion` appearance in SfzData.cs (field decl), SfzBuiltins.cs (flag-set on `#drums`), SfzRenderer.cs (gate on the field); absence of `EndsWith("Perc.sfz")` in SfzRenderer.cs
- OQ3 resolution locked: drums DO go through PitchShiftEngine in practice (or NOT, if the smoke test reveals most composer-authored drum notes already land at GM centers and the path is rarely exercised — note that for v1.6 follow-up)
- Composer smoke command + observed stderr advisory (or lack thereof) for a drum line that mixes GM-center notes and one off-center note
- Any Phase 33 / Phase 28 regressions found (should be zero; investigate if any)
</output>
