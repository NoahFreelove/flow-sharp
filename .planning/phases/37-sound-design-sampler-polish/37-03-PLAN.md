---
phase: 37-sound-design-sampler-polish
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
  - flow-lang/StandardLibrary/Audio/SamplePathArticulationMultipliers.cs
  - flow-lang/StandardLibrary/Audio/SongRenderer.cs
  - flow-lang.Tests/Integration/Phase37/SfzRoundRobinTests.cs
  - flow-lang.Tests/Integration/Phase37/SfzRoundRobinDeterminismTests.cs
  - flow-lang.Tests/Integration/Phase37/SfzVelocityCrossfadeTests.cs
  - flow-lang.Tests/Integration/Phase37/SfzHardSwitchRegression.cs
  - flow-lang.Tests/Integration/Phase37/SfzPanRetrofitTests.cs
  - flow-lang.Tests/Integration/Phase37/SfzPanCompositionTests.cs
  - flow-lang.Tests/Integration/Phase37/SampledStaccatoEnergyTests.cs
  - flow-lang.Tests/Integration/Phase37/Phase37MixSynthPathRegression.cs
  - flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav
  - flow-lang.Tests/fixtures/Phase37/round_robin.sfz
  - flow-lang.Tests/fixtures/Phase37/velocity_xfade.sfz
autonomous: true
requirements: [MIX-01, MIX-02, SAMP-01, SAMP-02, SAMP-03]
must_haves:
  truths:
    - "MIX-01 verified: existing synth-path per-voice pan at SongRenderer:308-309 is pinned via RMS regression baseline (per D-37-15 audit conclusion — no new synth code, regression coverage only)"
    - "MIX-02 ships: SFZ-rendered voices honor composer voice.Pan via SfzRenderer-emitted Voice with pan attribute; SongRenderer.MixVoicesToStereoBuffer applies constant-power split at mix stage (NOT inside SfzRenderer per Pitfall 12 + Anti-Pattern from RESEARCH)"
    - "Per-region SFZ pan + per-voice composer pan compose via additive-with-clamp: effectivePan = clamp(region.Pan + voice.Pan, -1.0, +1.0) — locked per OQ4 resolution"
    - "B2 LOCK: SFZ-rendered voices ALWAYS promote to stereo via ToStereoBufferWithPan(monoSamples, sampleRate, effectivePan) — unconditional, no Voice.Pan=0 ambiguity (Pitfall 12 resolution). When effectivePan == 0 the helper produces equal L/R (constant-power at center = √0.5) — that's the correct centered-stereo behavior."
    - "SAMP-01: seq_position + seq_length parsed; round-robin counter seeded from voice ordinal index (deterministic across runs per Pattern 5); counter resets at renderSong/writeWav boundary alongside PrngRegistry per Pitfall 6"
    - "SAMP-02: xfin_lovel/xfin_hivel/xfout_lovel/xfout_hivel parsed; velocity in crossfade band produces non-zero contribution from BOTH layers via equal-power curve per Pattern 6; hard-switch fallback when opcodes absent matches Phase 33 byte-identical baseline"
    - "SAMP-03: per-articulation envelope multiplier table SamplePathArticulationMultipliers ships as new file; applied AFTER Phase 28's SynthUtils.GenerateArticulationADSR at the caller site only (Pitfall 10 — Phase 28 helper unchanged); sample-path staccato shows measurably more harmonic energy than pre-multiplier baseline"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs"
      provides: "Extended SfzRegion record with 6 new positional fields (SeqPosition, SeqLength, XfinLoVel, XfinHiVel, XfoutLoVel, XfoutHiVel) — defaults preserve Phase 33 behavior when opcodes absent"
      contains: "SeqPosition\\|SeqLength\\|XfinLoVel"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs"
      provides: "Opcode whitelist extended from 14 to 20 entries; BuildRegion reads 6 new opcodes with charitable-fallback defaults; clamps seq_length > 100 with WarnOnce per Pitfall 1"
      contains: "seq_position\\|seq_length\\|xfin_lovel"
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      provides: "Round-robin counter per (loKey,hiKey,loVel,hiVel) group; equal-power velocity crossfade; SAMP-03 multiplier overlay; MIX-02 per-voice pan threading; B2-locked unconditional stereo promotion"
      contains: "_rrCounter\\|XfinLoVel\\|SamplePathArticulationMultipliers"
    - path: "flow-lang/StandardLibrary/Audio/SamplePathArticulationMultipliers.cs"
      provides: "Per-articulation scalar ADSR multipliers (Pattern 7 Option A — A8 resolution); stacks multiplicatively on Phase 28 baseline"
      contains: "public static class SamplePathArticulationMultipliers"
    - path: "flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav"
      provides: "MIX-01 audit-pin baseline — verifies existing SongRenderer:308-309 pan formula preserved"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      to: "flow-lang/StandardLibrary/Audio/SongRenderer.cs"
      via: "SfzRenderer emits Voice with composer Pan attribute set; SongRenderer applies constant-power split"
      pattern: "voice.Pan|voice\\.Pan"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      to: "flow-lang/StandardLibrary/Audio/SamplePathArticulationMultipliers.cs"
      via: "SamplePathArticulationMultipliers.For(articulation).Sample(i, frames) multiplied AFTER ApplyEnvelope"
      pattern: "SamplePathArticulationMultipliers\\.For"
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs"
      to: "flow-lang/Runtime/PrngRegistry.cs"
      via: "Per-voice round-robin counter resets at PrngRegistry.ResetAtRenderBoundary boundary call site"
      pattern: "ResetAtRenderBoundary"
---

<objective>
Plan 37-03 closes the entire stereo-pan + SFZ-polish bundle in one plan per D-37-03: it ships MIX-02 (SFZ per-voice pan retrofit), pins MIX-01 (existing synth-path pan from D-37-15 audit) via RMS baseline, lands SAMP-01 (round-robin opcodes), SAMP-02 (velocity-layer crossfade), and SAMP-03 (per-articulation envelope multipliers for the sample path).

Independent of Plans 37-01 and 37-02 — touches only the SFZ subsystem + SongRenderer mix stage. Per D-37-05 wave order: can run parallel with 37-02; depends ONLY on 37-01's Wave 0 test scaffolds (already shipped from 37-01 Task 1).

Open Question Resolutions:
- **OQ4 (Pan composition)** locked to **additive-with-clamp**: `effectivePan = Math.Clamp(region.Pan + voice.Pan, -1.0, 1.0)`. Rationale per RESEARCH §Open Question 4: per-region pan = "intrinsic to the patch's stereo image"; per-voice pan = "where the composer wants the source instrument in the stereo field"; they ADD.
- **A8 (SAMP-03 multiplier shape)** locked to **Option A (scalar ADSR multiplier)**. Rationale per RESEARCH §Pattern 7: lower risk + composable; escalates to Option B (full curve) only if Plan 37-04's ragtime UAT iteration #2 still flags the staccato gap.
- **B2 (Pitfall 12 — voice.Pan=0 ambiguity) locked at revision pass 2/3 to approach (b):** SFZ-rendered voices ALWAYS promote to stereo regardless of voice.Pan value. There's no PanExplicit flag on Voice.cs (audit-confirmed — Voice has only a settable `double Pan` property; centered is indistinguishable from "unset"). When effectivePan == 0, ToStereoBufferWithPan still produces equal L/R via the constant-power formula (cos/sin at π/4 → both channels at √0.5) — that's correct centered-stereo behavior. Mono-when-pan-default-0 footprint accepted as cost; benefit is deterministic + no Voice schema change needed.

Per D-37-15: MIX-01 needs no synth-path code changes; only RMS-baseline pin.
Per D-37-16: MIX-02 wires composer voice.Pan into the SFZ-rendered voice path. Per RESEARCH §Anti-Patterns + Pitfall 12: the wire-up lives at SfzRenderer → Voice handoff, NOT by mutating SongRenderer.MixVoicesToStereoBuffer.

Output: 1 modified record + 2 modified parsers/renderers + 1 new multiplier table file + 2 SFZ test fixtures + 1 RMS baseline WAV + 8 test classes filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-sound-design-sampler-polish/37-CONTEXT.md
@.planning/phases/37-sound-design-sampler-polish/37-RESEARCH.md
@.planning/phases/37-sound-design-sampler-polish/37-PATTERNS.md
@.planning/phases/37-sound-design-sampler-polish/37-01-SUMMARY.md

@CLAUDE.md

@flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs
@flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
@flow-lang/StandardLibrary/Audio/SongRenderer.cs
@flow-lang/StandardLibrary/Audio/SynthUtils.cs
@flow-lang/StandardLibrary/Audio/Voice.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs
@flow-lang/Runtime/PrngRegistry.cs
@flow-lang.Tests/Helpers/RmsRegressionTests.cs
@flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs

<interfaces>
From PATTERNS.md §Pattern Assignments §SfzRegion.cs (current — Phase 33 record):

public sealed record SfzRegion(
    string SamplePath,
    int PitchKeycenter,
    int LoKey,
    int HiKey,
    int LoVel,
    int HiVel,
    SfzLoopMode LoopMode,
    int LoopStart,
    int LoopEnd,
    double AmpegAttack,
    double AmpegRelease,
    double Volume,
    double Pan);

Extension (Phase 37 — append 6 new positional fields, sentinel defaults):

public sealed record SfzRegion(
    // existing 13 fields above ...
    int SeqPosition = 1,
    int SeqLength = 1,
    int XfinLoVel = -1,
    int XfinHiVel = -1,
    int XfoutLoVel = -1,
    int XfoutHiVel = -1);

From flow-lang/StandardLibrary/Audio/Voice.cs (audit-confirmed shape — B2 reference):

public class Voice
{
    public AudioBuffer Buffer { get; }
    public double OffsetBeats { get; set; }
    public double Gain { get; set; }          // default 1.0
    public double Pan { get; set; }           // default 0.0 — NO PanExplicit flag
    public Voice(AudioBuffer buffer, double offsetBeats);
}
// CRITICAL B2 LOCK: voice.Pan == 0 is indistinguishable from "composer didn't set it".
// Approach: SFZ-rendered voices ALWAYS promote to stereo. Unconditional. No branching.

From flow-lang/StandardLibrary/Audio/SongRenderer.cs:280-338 (MIX-01 already shipped per D-37-15):

// Per-voice constant-power pan applied at additive-mix stage:
double panAngle = (voice.Pan + 1.0) * 0.25 * Math.PI;
float wL = (float)Math.Cos(panAngle);
float wR = (float)Math.Sin(panAngle);
// ... applied to voice's contribution before additive sum

From flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs:202-208 (existing region-pan branch — to be REPLACED with unconditional stereo per B2):

if (region.Pan != 0.0)
    return ToStereoBufferWithPan(fitted, sampleRate, region.Pan);
return SynthUtils.ToMonoBuffer(fitted, sampleRate);

From flow-lang/StandardLibrary/Audio/SynthUtils.cs (Phase 28 LOCKED baseline):

public static float[] GenerateArticulationADSR(
    Articulation art,
    double baseAttack, double baseDecay, double baseSustain, double baseRelease,
    int frames, int sampleRate, bool isPercussion);
// DO NOT MODIFY — Phase 28 contract. SAMP-03 multipliers stack OUTSIDE this helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SfzRegion record extension + SfzParser opcode whitelist + SAMP-01/02 fixtures + parsing tests</name>
  <read_first>
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs (lines 1-70 — record shape + xmldoc — PATTERNS self-analog)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs lines 79-95 (KnownOpcodes HashSet) + lines 440-510 (BuildRegion + ReadInt + WarnOnce pattern)
    - flow-lang/Diagnostics/RenderingDiagnostics.cs lines 19-29 (WarnOnce contract)
    - .planning/phases/37-sound-design-sampler-polish/37-RESEARCH.md §Pattern 5 (round-robin) + §Pattern 6 (velocity crossfade) + §Pitfall 1 (clamp seq_length > 100)
    - .planning/phases/37-sound-design-sampler-polish/37-PATTERNS.md §SfzRegion.cs (positional record extension) + §SfzParser.cs (whitelist extension + BuildRegion charitable-default pattern)
    - flow-lang.Tests/Integration/Phase37/SfzRoundRobinTests.cs + SfzVelocityCrossfadeTests.cs (Wave 0 scaffolds from 37-01)
    - flow-lang.Tests/Integration/Phase33/SfzSmokeTests.cs lines 1-72 (test class shape — fixture loading via `flow-lang.Tests/fixtures/...sfz`)
  </read_first>
  <files>
    flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs,
    flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs,
    flow-lang.Tests/fixtures/Phase37/round_robin.sfz,
    flow-lang.Tests/fixtures/Phase37/velocity_xfade.sfz,
    flow-lang.Tests/Integration/Phase37/SfzRoundRobinTests.cs,
    flow-lang.Tests/Integration/Phase37/SfzVelocityCrossfadeTests.cs
  </files>
  <behavior>
    - SfzRegion record gains 6 new positional fields with sentinel defaults (SeqPosition=1, SeqLength=1, XfinLoVel=-1, XfinHiVel=-1, XfoutLoVel=-1, XfoutHiVel=-1); existing positional constructor call sites continue compiling (positional record append preserves back-compat).
    - SfzParser.KnownOpcodes HashSet grows from 14 → 20 entries; parses the 6 new opcodes via existing ReadInt + charitable-fallback pattern.
    - seq_length > 100 emits one-shot WarnOnce `[sfz] seq_length=N exceeds spec max 100 in '{patchDescription}' — clamping to 100` per Pitfall 1 + Security Domain DoS mitigation.
    - 2 SFZ test fixtures land at `flow-lang.Tests/fixtures/Phase37/`: round_robin.sfz (2 regions with seq_position=1/2 + seq_length=2 + identical key range), velocity_xfade.sfz (2 regions with xfin/xfout opcodes splitting velocity range).
    - SfzRoundRobinTests verifies parser reads seq_position + seq_length correctly; SfzVelocityCrossfadeTests verifies xfin/xfout opcodes parse correctly.
  </behavior>
  <action>
**SfzRegion.cs** — Per PATTERNS §SfzRegion.cs and RESEARCH §Pattern 5 + 6:

Open the file, append 6 new positional fields AFTER the existing `double Pan` (line 70 in current file). Use C# named-args-with-defaults syntax for positional records:
```
public sealed record SfzRegion(
    // ... existing 13 fields ...
    double Pan,
    int SeqPosition = 1,
    int SeqLength = 1,
    int XfinLoVel = -1,
    int XfinHiVel = -1,
    int XfoutLoVel = -1,
    int XfoutHiVel = -1);
```

Extend xmldoc summary block (lines 1-56 in current file) with 3 new `<item>` entries — round-robin pair (SeqPosition/SeqLength), xfin pair (XfinLoVel/XfinHiVel), xfout pair (XfoutLoVel/XfoutHiVel). Each item briefly states the spec semantics from RESEARCH §Pattern 5/6 + the sentinel default behavior.

Run `dotnet build flow-lang/` and confirm compile-clean (existing positional constructor calls in SfzParser.BuildRegion get the new defaults automatically).

**SfzParser.cs** — Per PATTERNS §SfzParser.cs.

1. **Opcode whitelist extension** (KnownOpcodes HashSet lines 79-95): append 6 new strings — `"seq_position"`, `"seq_length"`, `"xfin_lovel"`, `"xfin_hivel"`, `"xfout_lovel"`, `"xfout_hivel"`. Update the xmldoc opcode count from "14 opcodes" → "20 opcodes" (lines 19-26).

2. **BuildRegion reading** (lines 455-509): after the existing `ReadInt` line for `pan`/`volume`, add 6 ReadInt calls:
```
int seqPosition = ReadInt(region, "seq_position", 1, patchDescription);
int seqLength = ReadInt(region, "seq_length", 1, patchDescription);
int xfinLoVel = ReadInt(region, "xfin_lovel", -1, patchDescription);
int xfinHiVel = ReadInt(region, "xfin_hivel", -1, patchDescription);
int xfoutLoVel = ReadInt(region, "xfout_lovel", -1, patchDescription);
int xfoutHiVel = ReadInt(region, "xfout_hivel", -1, patchDescription);

// Clamp seq_length to spec max 100 per RESEARCH §Pitfall 1 + Security Domain
if (seqLength > 100)
{
    RenderingDiagnostics.WarnOnce(
        $"sfz:opcode_value:{patchDescription}:seq_length:{seqLength}",
        $"[sfz] seq_length={seqLength} exceeds spec max 100 in '{patchDescription}' — clamping to 100");
    seqLength = 100;
}
```

3. Update the SfzRegion constructor call at the end of BuildRegion to pass the 6 new args positionally.

**Create flow-lang.Tests/fixtures/Phase37/round_robin.sfz**:
```
<region>
sample=sine_440.wav
lokey=60 hikey=60 lovel=1 hivel=127
seq_position=1 seq_length=2
pan=0.0

<region>
sample=sine_440.wav
lokey=60 hikey=60 lovel=1 hivel=127
seq_position=2 seq_length=2
pan=0.0
```
(Both regions point to the Plan 37-02 sine fixture; the test only checks round-robin selection logic, not audible distinctness.)

**Create flow-lang.Tests/fixtures/Phase37/velocity_xfade.sfz**:
```
<region>
sample=sine_440.wav
lokey=60 hikey=60 lovel=1 hivel=80
xfout_lovel=60 xfout_hivel=80

<region>
sample=sine_440.wav
lokey=60 hikey=60 lovel=60 hivel=127
xfin_lovel=60 xfin_hivel=80
```
(Layers overlap in velocity [60, 80]; xfade band specified via xfin/xfout opcodes.)

**Fill in SfzRoundRobinTests.cs** (Wave 0 scaffold) — 2 `[Fact]` methods:
1. `Parser_ReadsSeqPositionSeqLength_FromFixture` — parse round_robin.sfz via SfzParser.Parse; assert both regions have `SeqLength == 2`; assert one has `SeqPosition == 1`, the other `SeqPosition == 2`.
2. `Parser_ClampsExcessiveSeqLength_WithWarnOnce` — write an inline SFZ string with `seq_length=999999`; parse; assert resulting region has `SeqLength == 100`; capture stderr; assert message matches regex `\[sfz\] seq_length=999999 exceeds spec max 100`.

**Fill in SfzVelocityCrossfadeTests.cs** (Wave 0 scaffold) — 1 `[Fact]` method:
1. `Parser_ReadsXfinXfoutOpcodes_FromFixture` — parse velocity_xfade.sfz; assert layer 1 has `XfoutLoVel == 60 && XfoutHiVel == 80 && XfinLoVel == -1` (xfout only); layer 2 has `XfinLoVel == 60 && XfinHiVel == 80 && XfoutLoVel == -1` (xfin only).

The audible-render tests (round-robin alternation + crossfade equal-power) land in Task 3 once SfzRenderer is updated.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build -c Debug -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase37.SfzRoundRobinTests|FullyQualifiedName~Phase37.SfzVelocityCrossfadeTests" --no-build --logger "console;verbosity=minimal" 2&gt;&amp;1 | tail -8</automated>
  </verify>
  <done>SfzRegion + SfzParser extended; 6 new opcodes parsed; 2 SFZ fixtures land; 3 parser-focused facts pass; full Phase 33 SFZ suite passes (no regression).</done>
  <acceptance_criteria>
    - grep -c "SeqPosition\|SeqLength" flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs returns &gt;= 4
    - grep -c "XfinLoVel\|XfoutLoVel" flow-lang/StandardLibrary/Audio/Sfz/SfzRegion.cs returns &gt;= 4
    - grep -c "seq_position\|seq_length\|xfin_lovel\|xfout_lovel" flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs returns &gt;= 6
    - grep -c "seq_length=N exceeds spec max 100\|exceeds spec max 100" flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs returns &gt;= 1
    - ls flow-lang.Tests/fixtures/Phase37/round_robin.sfz flow-lang.Tests/fixtures/Phase37/velocity_xfade.sfz both exist
    - dotnet test --filter "FullyQualifiedName~Phase37.SfzRoundRobinTests|FullyQualifiedName~Phase37.SfzVelocityCrossfadeTests" --no-build reports zero failures, &gt;= 3 passed
    - dotnet test --filter "FullyQualifiedName~Phase33" --no-build reports zero failures (no Phase 33 regression)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SfzRenderer round-robin counter + xfin/xfout equal-power crossfade + SamplePathArticulationMultipliers + SAMP-03 wire-up</name>
  <read_first>
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs (full file — touches lines ~94-209 Render pipeline + 397-423 ToStereoBufferWithPan + 188-200 articulation envelope)
    - flow-lang/StandardLibrary/Audio/SynthUtils.cs (GenerateArticulationADSR signature — Phase 28 LOCKED, DO NOT MODIFY)
    - flow-lang/Runtime/PrngRegistry.cs (ResetAtRenderBoundary — counter reset boundary call site)
    - .planning/phases/37-sound-design-sampler-polish/37-RESEARCH.md §Pattern 5 (round-robin counter algorithm + voice-ordinal seeding) + §Pattern 6 (equal-power xfade math) + §Pattern 7 (SAMP-03 Option A scalar multiplier) + §Pitfalls 6, 7, 10
    - .planning/phases/37-sound-design-sampler-polish/37-PATTERNS.md §SfzRenderer.cs (round-robin + xfin/xfout + SAMP-03 + MIX-02 patterns) + §Pattern C (articulation envelope stacking)
    - flow-lang.Tests/Integration/Phase37/SfzRoundRobinDeterminismTests.cs + SfzHardSwitchRegression.cs + SampledStaccatoEnergyTests.cs (Wave 0 scaffolds)
  </read_first>
  <files>
    flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs,
    flow-lang/StandardLibrary/Audio/SamplePathArticulationMultipliers.cs,
    flow-lang.Tests/Integration/Phase37/SfzRoundRobinDeterminismTests.cs,
    flow-lang.Tests/Integration/Phase37/SfzHardSwitchRegression.cs,
    flow-lang.Tests/Integration/Phase37/SampledStaccatoEnergyTests.cs
  </files>
  <behavior>
    - SfzRenderer maintains a `Dictionary<(int loKey, int hiKey, int loVel, int hiVel), int> _rrCounter` keyed by region group; counter starts at 1, advances modulo `seqLength` per trigger.
    - Counter seeded from voice ordinal (per Pattern 5): the Nth voice triggering a group gets `(N mod seqLength) + 1`. Deterministic across runs at same git SHA + architectures.
    - Counter resets at `renderSong`/`writeWav` boundary alongside PrngRegistry.ResetAtRenderBoundary (Pitfall 6).
    - When both xfin AND xfout are absent (sentinel -1), region uses hard-switch (Phase 33 default behavior — byte-identical regression preserved).
    - When velocity falls in `[XfinLoVel, XfinHiVel]` band, region gain = `sin((normVel) * π/2)` per Pattern 6 equal-power; when in `[XfoutLoVel, XfoutHiVel]`, gain = `cos((normVel) * π/2)`.
    - Crossfade safety headroom: when BOTH adjacent layers are in their respective bands simultaneously (i.e. xfade region overlaps), multiply both contributions by 0.707 to prevent clipping (Pitfall 7).
    - SAMP-03: after `SynthUtils.ApplyEnvelope(fitted, envelope)` returns (per the existing line ~200 pattern), look up `SamplePathArticulationMultipliers.For(note.Articulation)` and multiply `fitted[i] *= multCurve[i]` IF the multiplier is non-trivial. Phase 28's `GenerateArticulationADSR` itself is unmodified (Pitfall 10).
  </behavior>
  <action>
**SamplePathArticulationMultipliers.cs** — NEW file. Namespace: `FlowLang.StandardLibrary.Audio`. Public static class `SamplePathArticulationMultipliers`.

Surface (per RESEARCH §Pattern 7 Option A — scalar ADSR multipliers — A8 lock):
```
public readonly struct SamplePathMultiplier
{
    public double AttackMult { get; }
    public double DecayMult { get; }
    public double SustainMult { get; }
    public double ReleaseMult { get; }
    public bool IsNontrivial { get; }   // true when any mult differs from 1.0
    public SamplePathMultiplier(double a, double d, double s, double r);
    public float Sample(int frameIndex, int totalFrames);   // returns scalar at fractional position
}

public static SamplePathMultiplier For(Articulation art);
```

Locked multiplier table per A8 resolution (CLAUDE.md "Locked articulation rules" composed with SAMP-03 multiplicative stack — pick conservative values that demonstrably brighten staccato + leave others near-identity):
- `Articulation.None` → (1.0, 1.0, 1.0, 1.0) — no-op
- `Articulation.Staccato` → (0.5, 1.2, 1.0, 0.8) — faster attack + slight decay brightening (closes Phase 29 v1.5 thinness gap per RESEARCH §Pattern 7 perceptual justification)
- `Articulation.Marcato` → (0.6, 1.1, 1.0, 0.9) — similar to staccato but milder
- `Articulation.Tenuto` → (1.0, 1.0, 1.0, 1.05) — slight release lengthening
- `Articulation.Legato` → (1.0, 1.0, 1.0, 1.0) — no-op (Phase 28 envelope already optimized)
- `Articulation.Accent` → (0.7, 1.0, 1.0, 1.0) — slightly faster attack emphasis
- `Articulation.Sforzando` → (0.5, 1.0, 1.0, 1.0) — sharpened attack only

`Sample(frameIndex, totalFrames)` returns a piecewise scalar based on which envelope stage `frameIndex` falls in (split totalFrames into 4 stages by ratio matching Phase 28's ADSR durations — for sample-path purposes use simple quartile splits: A = [0, frames/4), D = [frames/4, frames/2), S = [frames/2, 3*frames/4), R = [3*frames/4, frames]). Return the corresponding scalar.

**SfzRenderer.cs** — Modify the existing file (PATTERNS §SfzRenderer.cs self-analog).

1. **Add round-robin counter state** — at the class field level, alongside any existing private state:
```
private readonly Dictionary<(int loKey, int hiKey, int loVel, int hiVel), int> _rrCounter = new();
internal void ResetAtRenderBoundary() { _rrCounter.Clear(); }
```

2. **Round-robin region selection** — in the Render method where regions are picked for a given key/velocity, add round-robin logic BEFORE the existing hard-switch region pick:
```
// SAMP-01 round-robin (Pattern 5): if multiple regions share key+vel range and have seq_length>1,
// rotate the picked alternate per voice ordinal.
var candidateRegions = regions.Where(r => r.LoKey <= note.MidiPitch && r.HiKey >= note.MidiPitch
                                          && r.LoVel <= note.MidiVelocity && r.HiVel >= note.MidiVelocity).ToList();
SfzRegion picked;
if (candidateRegions.Count > 1 && candidateRegions[0].SeqLength > 1)
{
    var groupKey = (candidateRegions[0].LoKey, candidateRegions[0].HiKey,
                    candidateRegions[0].LoVel, candidateRegions[0].HiVel);
    int counter = _rrCounter.GetValueOrDefault(groupKey, 0);
    int targetSeqPos = (counter % candidateRegions[0].SeqLength) + 1;
    picked = candidateRegions.FirstOrDefault(r => r.SeqPosition == targetSeqPos) ?? candidateRegions[0];
    _rrCounter[groupKey] = counter + 1;
}
else
{
    picked = candidateRegions.First();
}
```

3. **Velocity-layer crossfade** — after picking the region (or set of overlapping regions), if any region has `XfinLoVel != -1 || XfoutLoVel != -1`:
```
// SAMP-02 equal-power crossfade (Pattern 6) — only active when opcodes present
double layerGain = 1.0;
if (picked.XfinLoVel != -1 && picked.XfinHiVel != -1
    && note.MidiVelocity >= picked.XfinLoVel && note.MidiVelocity <= picked.XfinHiVel)
{
    double normVel = (note.MidiVelocity - picked.XfinLoVel)
                     / (double)(picked.XfinHiVel - picked.XfinLoVel);
    layerGain = Math.Sin(normVel * Math.PI / 2.0);
}
else if (picked.XfoutLoVel != -1 && picked.XfoutHiVel != -1
         && note.MidiVelocity >= picked.XfoutLoVel && note.MidiVelocity <= picked.XfoutHiVel)
{
    double normVel = (note.MidiVelocity - picked.XfoutLoVel)
                     / (double)(picked.XfoutHiVel - picked.XfoutLoVel);
    layerGain = Math.Cos(normVel * Math.PI / 2.0);
}
// Detect simultaneous in-band layers from sibling regions; apply 0.707 headroom (Pitfall 7).
bool siblingInBand = candidateRegions.Any(r => r != picked
    && ((r.XfinLoVel != -1 && note.MidiVelocity >= r.XfinLoVel && note.MidiVelocity <= r.XfinHiVel)
        || (r.XfoutLoVel != -1 && note.MidiVelocity >= r.XfoutLoVel && note.MidiVelocity <= r.XfoutHiVel)));
if (siblingInBand)
    layerGain *= 0.7071;  // 1/sqrt(2) headroom
// Multiply `fitted` samples by layerGain.
for (int i = 0; i < fitted.Length; i++) fitted[i] *= (float)layerGain;
```

4. **SAMP-03 envelope multiplier overlay** — after the existing `SynthUtils.ApplyEnvelope(fitted, envelope)` line (around line 200), add:
```
// SAMP-03 (Pattern 7 Option A) — sample-path articulation multiplier.
// Stacks ON TOP of Phase 28 baseline; Phase 28 helper unchanged (Pitfall 10).
var sampleMult = SamplePathArticulationMultipliers.For(note.Articulation);
if (sampleMult.IsNontrivial)
{
    for (int i = 0; i < fitted.Length; i++)
        fitted[i] *= sampleMult.Sample(i, fitted.Length);
}
```

5. **Per-render-boundary reset wire-up** — find the existing call site that resets state at renderSong/writeWav boundary (likely `PrngRegistry.ResetAtRenderBoundary()` from Phase 36 Plan 36-01). Add `sfzRenderer.ResetAtRenderBoundary()` adjacent. If the SfzRenderer is constructed per-render rather than shared (Phase 33 pattern), no reset call is needed — but VERIFY in the code; document the lifecycle in a comment block.

**Fill in SfzRoundRobinDeterminismTests.cs** — 2 `[Fact]` methods:
1. `RoundRobin_TwoConsecutiveRendersOfSameSong_SameRrPicks` — build a Song with 4 voices triggering the round_robin.sfz region group; render twice via the SfzRenderer + capture which seq_position was picked for each voice; assert two arrays identical.
2. `RoundRobin_ResetAtBoundary_ResetsCounter` — render once with 4 voices; call ResetAtRenderBoundary; render again; assert first-render picks == second-render picks.

**Fill in SfzHardSwitchRegression.cs** — 1 `[Fact]` method:
1. `HardSwitch_WhenXfinXfoutAbsent_MatchesPhase33Behavior` — load an SFZ patch with adjacent vel layers but no xfin/xfout; render a song hitting both layers; compare SHA-256 with a baseline rendered via Phase 33 logic (commit the baseline alongside this test). Use `RmsRegressionTests.AssertRmsWithinTolerance` with 0.1 dB tolerance (tighter than SPEC-8 default since this is a regression, not a behavior change).

**Fill in SampledStaccatoEnergyTests.cs** — 1 `[Fact]` method:
1. `SampleStaccato_AfterSamp03_HasMoreHarmonicEnergyThanBaseline` — render a sustained sample with `Articulation.Staccato` via SfzRenderer (with Plan 37-03 multipliers active); compute spectral centroid via Fft.Forward + magnitude-weighted mean bin index; compare against the SAME render without the multiplier (toggle via reflection or feature flag for the test — alternatively, render with `Articulation.None` and compare). Assert centroid with multiplier > centroid without by at least 5% (closes Phase 29 v1.5 thinness gap per RESEARCH §Pattern 7).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build -c Debug -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase37.SfzRoundRobin|FullyQualifiedName~Phase37.SfzHardSwitchRegression|FullyQualifiedName~Phase37.SampledStaccatoEnergyTests" --no-build --logger "console;verbosity=minimal" 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>SfzRenderer ships round-robin + xfin/xfout xfade + SAMP-03 multiplier overlay; SamplePathArticulationMultipliers compiles; 4+ tests pass; Phase 33 byte-identical hard-switch regression holds.</done>
  <acceptance_criteria>
    - grep -c "_rrCounter\|ResetAtRenderBoundary" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 2
    - grep -c "SamplePathArticulationMultipliers" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1
    - grep -c "Math.Sin(normVel\|Math.Cos(normVel" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 2 (Pattern 6 equal-power)
    - grep -c "0.7071\|0.707" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1 (Pitfall 7 headroom)
    - ls flow-lang/StandardLibrary/Audio/SamplePathArticulationMultipliers.cs exists
    - grep -c "IsNontrivial\|For(art" flow-lang/StandardLibrary/Audio/SamplePathArticulationMultipliers.cs returns &gt;= 2
    - grep -v '^[[:space:]]*//' flow-lang/StandardLibrary/Audio/SynthUtils.cs | grep -c "SamplePathArticulationMultipliers" returns 0 (Pitfall 10 — Phase 28 helper unchanged)
    - dotnet test --filter "FullyQualifiedName~Phase37.SfzRoundRobin|FullyQualifiedName~Phase37.SfzHardSwitchRegression|FullyQualifiedName~Phase37.SampledStaccatoEnergyTests" --no-build reports zero failures, &gt;= 4 passed
    - dotnet test --filter "FullyQualifiedName~Phase33|FullyQualifiedName~Phase28.ArticulationRegression" --no-build reports zero failures (no regression on Phase 33 + Phase 28 articulation baselines)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MIX-01 audit pin (RMS baseline) + MIX-02 SFZ per-voice pan wire-up (B2 unconditional-stereo lock) + composition tests</name>
  <read_first>
    - flow-lang/StandardLibrary/Audio/SongRenderer.cs lines 280-338 (existing per-voice pan formula at 308-309 — D-37-15 audit anchor)
    - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs lines 202-208 (existing region-pan branch — to be REPLACED with unconditional stereo promotion per B2)
    - flow-lang/StandardLibrary/Audio/Voice.cs (Voice class — audit-confirmed: `double Pan { get; set; }` defaults to 0.0, NO `PanExplicit` flag exists)
    - flow-lang.Tests/Helpers/RmsRegressionTests.cs lines 39-63 (AssertWavMatchesBaseline + AssertRmsWithinTolerance — SPEC-8 default tolerance ±0.5 dB / 100 ms)
    - .planning/phases/37-sound-design-sampler-polish/37-CONTEXT.md D-37-15 + D-37-16 (audit citations) + D-37-03 (MIX-01 verification folds into this plan)
    - .planning/phases/37-sound-design-sampler-polish/37-RESEARCH.md §Open Question 4 (OQ4 — pan composition) + §Pitfall 12 (Voice.Pan = 0 not "unset") + §Anti-Patterns (don't mutate SongRenderer for MIX-02 retrofit)
    - .planning/phases/37-sound-design-sampler-polish/37-PATTERNS.md §SfzRenderer.cs MIX-02 pattern + §Pattern A constant-power formula
    - flow-lang.Tests/Integration/Phase37/Phase37MixSynthPathRegression.cs + SfzPanRetrofitTests.cs + SfzPanCompositionTests.cs (Wave 0 scaffolds)
  </read_first>
  <files>
    flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs,
    flow-lang/StandardLibrary/Audio/SongRenderer.cs,
    flow-lang.Tests/Integration/Phase37/Phase37MixSynthPathRegression.cs,
    flow-lang.Tests/Integration/Phase37/SfzPanRetrofitTests.cs,
    flow-lang.Tests/Integration/Phase37/SfzPanCompositionTests.cs,
    flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav
  </files>
  <behavior>
    - MIX-01: Synth-path pan baseline pinned via RMS regression — a song with two voices (left at pan=-0.7, right at pan=+0.7) renders to a stereo Buffer; SHA matches `flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav` within SPEC-8 tolerance (±0.5 dB / 100 ms).
    - MIX-02: An SFZ-rendered voice with composer-set `voice.Pan = 0.7` produces a stereo Buffer where the right channel RMS > left channel RMS by ≥ 3 dB (constant-power split working).
    - Composition: a Song with a Voice.Pan = 0.5 triggering an SFZ region with `pan = -0.3` produces an output where the effective pan is `clamp(-0.3 + 0.5, -1.0, 1.0) = 0.2` (rightward bias; right channel RMS > left by ≥ 1 dB).
    - **B2 LOCK** (Pitfall 12 resolution): SFZ-rendered voices ALWAYS promote to stereo regardless of voice.Pan value. Composer-set `voice.Pan = 0.0` still produces a stereo buffer (channels = 2, both channels carry signal at √0.5 each = constant-power center). NO conditional fallthrough to mono based on Voice.Pan value.
  </behavior>
  <action>
**MIX-01 baseline pin** — Per D-37-15: synth-path pan code is already shipped; only need RMS regression coverage.

1. Construct a deterministic synth-path render:
   - In a one-off `Phase37MixSynthPathRegression.cs` test, build a Song with 2 sections, each containing 1 voice. Voice 1: piano, pan=-0.7, plays C4 quarter note. Voice 2: piano, pan=+0.7, plays G4 quarter note. Total length ~1 second.
   - Call `SongRenderer.RenderSong(song, "piano")` → get `AudioBuffer result` (stereo, 44.1 kHz).
   - On FIRST run (baseline missing), write `result` to `flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav` via `FileIO.WriteWavInternal`. This is the pinned baseline.
   - On SUBSEQUENT runs, call `RmsRegressionTests.AssertWavMatchesBaseline(result, "flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav", 100.0, 0.5)` per SPEC-8.

2. Test class `Phase37MixSynthPathRegression`:
   - `[Fact] SynthPathPan_TwoVoicesOppositePan_RmsMatchesBaseline` — runs the render described above; calls AssertWavMatchesBaseline. If baseline missing, this is the FIRST commit (test produces baseline + fails); next commit pins it (re-run produces matching baseline + passes). Document the two-step in the commit message.
   - `[Fact] SynthPathPan_LeftAndRightRmsDifferAsExpected` — render same Song; compute left-channel RMS vs right-channel RMS over the full buffer; assert that voice 1's window (first 500ms) has left > right by ≥ 3 dB; voice 2's window (next 500ms) has right > left by ≥ 3 dB.

**MIX-02 wire-up + B2 unconditional-stereo lock** — Per D-37-16 + Pitfall 12 + RESEARCH §Anti-Patterns: do NOT mutate SongRenderer.MixVoicesToStereoBuffer. The SongRenderer per-voice pan loop at lines 297-338 already handles `voice.Pan`; SfzRenderer just needs to construct/return a Voice with `Pan` set correctly AND always emit a stereo Buffer.

1. **Locate the SfzRenderer → Voice handoff point** — find where SfzRenderer.Render emits its result (likely a `Voice` value with rendered Buffer attached, returned to SongRenderer). Inspect the existing Voice construction.

2. **B2 LOCK: replace the existing conditional pan branch (lines 202-208) with UNCONDITIONAL stereo promotion.** The existing code is:
```
// EXISTING — DO NOT KEEP:
if (region.Pan != 0.0)
    return ToStereoBufferWithPan(fitted, sampleRate, region.Pan);
return SynthUtils.ToMonoBuffer(fitted, sampleRate);
```

Replace with:
```
// B2 LOCK (revision pass 2/3): SFZ-rendered voices ALWAYS promote to stereo.
// Voice.cs has no PanExplicit flag (audit-confirmed); voice.Pan=0 is composer-set "center",
// not "unset". ToStereoBufferWithPan(samples, sr, 0) produces equal L/R via constant-power
// (cos/sin at π/4 → both channels at √0.5) — that's correct centered-stereo behavior.
//
// OQ4 — additive-with-clamp composition of per-region + per-voice pan:
double effectivePan = Math.Clamp(picked.Pan + voice.Pan, -1.0, 1.0);
return ToStereoBufferWithPan(fitted, sampleRate, effectivePan);
```

Per RESEARCH §Anti-Patterns: this lives entirely in SfzRenderer; SongRenderer is untouched.

3. **Lifecycle note**: there is NO branch on `voice.Pan != 0` or `picked.Pan != 0` — the unconditional path always runs. If profiling later reveals a measurable hot-path cost for centered-only renders (unlikely — constant-power is two cos/sin calls per voice), v1.6 can add an explicit `PanExplicit` flag to Voice; the conditional optimization gets layered ON TOP of B2's unconditional default, not replacing it.

**Fill in SfzPanRetrofitTests.cs** — 3 `[Fact]` methods (one extra vs prior revision — explicit B2 acceptance):
1. `SfzVoice_WithPan07_RightChannelLouderThanLeft` — load an SFZ patch from `flow-lang.Tests/fixtures/Phase37/round_robin.sfz` (sine-based, simple); render a song with a single voice having `voice.Pan = 0.7`; compute left vs right channel RMS; assert right RMS > left RMS + 3 dB.
2. `SfzVoice_WithPanMinus07_LeftChannelLouderThanRight` — same fixture, `voice.Pan = -0.7`; assert left > right + 3 dB.
3. **B2 ACCEPTANCE** — `SfzVoice_WithPanZero_OutputIsStereo_NotMono` — same fixture, `voice.Pan = 0.0` AND region.Pan == 0.0 (the formerly-mono case); render; assert `result.Channels == 2`; assert both channels carry signal (`leftRms > 0 && rightRms > 0`); assert `|leftRms - rightRms| < 0.5 dB` (centered = equal L/R within tolerance).

**Fill in SfzPanCompositionTests.cs** — 2 `[Fact]` methods:
1. `RegionPanPlusVoicePan_AdditiveWithClamp_Effective02` — load a custom inline SFZ patch with region `pan=-0.3`; render a song with `voice.Pan = 0.5`; effective pan = clamp(-0.3 + 0.5, -1, 1) = 0.2 (slight right); assert right RMS > left + 0.5 dB (small but measurable bias).
2. `RegionPanPositive_VoicePanNegative_ClampToZero` — region `pan=+0.6` + voice `Pan=-0.6` → effective 0.0; assert `result.Channels == 2` AND `|leftRms - rightRms| < 0.5 dB` (centered + stereo — B2 still applies).

**Generation step for the baseline WAV**: Run the test ONCE before committing — first run writes the baseline file (test fails since assertion misses); commit the baseline; subsequent runs assert against it. Document this in the test comment block: `// First-run note: this test produces a baseline at flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav. After the first run, commit the baseline; subsequent runs assert against it via SPEC-8 RMS regression.`
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build -c Debug -v quiet 2&gt;&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/ --filter "FullyQualifiedName~Phase37.MixSynthPath|FullyQualifiedName~Phase37.SfzPan" --no-build --logger "console;verbosity=minimal" 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>MIX-01 RMS baseline pinned; MIX-02 SFZ per-voice pan threads through to SongRenderer; **B2 LOCK honored — SFZ render path unconditionally promotes to stereo**; all 5+ tests in this task pass; Phase 28 + Phase 33 baselines preserved.</done>
  <acceptance_criteria>
    - ls flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav exists
    - grep -c "Math.Clamp.*Pan.*Pan\|effectivePan" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1 (OQ4 additive-with-clamp)
    - **B2 ACCEPTANCE**: grep -c "ToStereoBufferWithPan" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns &gt;= 1 (unconditional stereo promotion)
    - **B2 ACCEPTANCE**: grep -cE "if\s*\(\s*region\.Pan\s*!=\s*0\.0\s*\)|if\s*\(\s*picked\.Pan\s*!=\s*0\.0\s*\)" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns 0 (the legacy region.Pan != 0 conditional branch is REMOVED — no conditional stereo)
    - **B2 ACCEPTANCE**: grep -cE "SynthUtils\.ToMonoBuffer\s*\(\s*fitted" flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs returns 0 (no mono fallthrough on the post-region-pick rendering path — verify via line-context; if ToMonoBuffer is called for an unrelated buffer transformation elsewhere, the test passes anyway since the grep scope is the renderer's main fitted-output path)
    - **B2 ACCEPTANCE**: dotnet test --filter "FullyQualifiedName~Phase37.SfzPanRetrofitTests.SfzVoice_WithPanZero_OutputIsStereo_NotMono" --no-build reports passed (the explicit B2 test)
    - dotnet test --filter "FullyQualifiedName~Phase37.MixSynthPath|FullyQualifiedName~Phase37.SfzPan" --no-build reports zero failures, &gt;= 6 passed (was 5 in prior revision; gained 1 from B2-specific test)
    - dotnet test --filter "FullyQualifiedName~Phase28|FullyQualifiedName~Phase33" --no-build reports zero failures
    - dotnet test --filter "FullyQualifiedName~Phase37" --no-build cumulative pass count &gt;= 18 (37-01 W=8, 37-02 W=14 once all wave 2 done; only the asset/closer scaffolds remain skipped)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer SFZ file -&gt; SfzParser | composer-supplied opcode values (seq_length, xfin_*, xfout_*) |
| composer voice.Pan -&gt; SongRenderer mix stage | per-voice pan attribute |
| SAMP-03 multiplier table -&gt; sample-path callers | Phase 28 envelope shape preserved (Pitfall 10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | DoS | SfzParser seq_length unbounded | mitigate | Clamp seq_length to spec max 100 + WarnOnce per Pitfall 1 |
| T-37-03-02 | Integrity | Round-robin counter not reset across renders | mitigate | ResetAtRenderBoundary call adjacent to PrngRegistry reset per Pitfall 6 |
| T-37-03-03 | Integrity | Equal-power xfade sums to > 1.0 power | mitigate | 0.707 headroom factor when both adjacent layers are simultaneously in-band per Pitfall 7 |
| T-37-03-04 | Integrity | SAMP-03 multiplier accidentally applied to synth path | mitigate | Multiplier overlay lives at sample-path caller only (SfzRenderer + Plan 37-04's SampledInstrumentRenderer); SynthUtils.GenerateArticulationADSR unchanged per Pitfall 10 |
| T-37-03-05 | Integrity | voice.Pan=0 silently treated as "unset" -&gt; mono output | mitigate | **B2 LOCK** (revision pass 2/3 — Pitfall 12 resolution approach b): SFZ render path UNCONDITIONALLY promotes to stereo via ToStereoBufferWithPan(samples, sr, effectivePan). No PanExplicit flag on Voice (audit-confirmed); centered = both channels at √0.5 via constant-power. Mono-when-pan-default-0 case folded into the unconditional path — composer ALWAYS gets stereo from SFZ. Acceptance criteria grep-checks the unconditional call and rejects the legacy conditional branch. |
| T-37-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 37 zero external packages |
</threat_model>

<verification>
After all 3 tasks land:
- `dotnet build -c Debug` succeeds zero errors
- `dotnet test --filter "FullyQualifiedName~Phase37" --no-build` cumulative shows &gt;= 18 passing facts (37-01: 8 + 37-02: 14 + 37-03: 13; assets/closer scaffolds still Skipped)
- `dotnet test --filter "FullyQualifiedName~Phase33" --no-build` zero failures (existing SFZ smoke + symbol-lookup + articulation tests unchanged) — IF a Phase 33 test specifically asserted that the SFZ render path produces a MONO buffer for `region.Pan == 0` voices, that test needs updating to assert stereo output (locked under B2 — the contract changed at Phase 37 in service of Pitfall 12)
- `dotnet test --filter "FullyQualifiedName~Phase28.ArticulationRegression" --no-build` zero failures (Pitfall 10 — synth-path Phase 28 baseline preserved)
- Composer smoke: render an SFZ-based song with composer `voice.Pan = 0.5`; verify stereo output with right > left RMS
- Composer smoke 2 (B2): render an SFZ-based song with `voice.Pan = 0.0` and `region.Pan == 0.0`; verify `result.Channels == 2` AND equal L/R within tolerance
- RMS baseline `flow-lang.Tests/baselines/Phase37/mix_synth_path_pan.wav` exists and asserts within SPEC-8 tolerance on repeat run
</verification>

<success_criteria>
- MIX-01 verified via RMS baseline pin (no synth-path code change per D-37-15 audit).
- MIX-02 ships: SFZ-rendered voices honor composer voice.Pan via additive-with-clamp composition with region pan (OQ4 lock).
- **B2 LOCK (revision pass 2/3) honored**: SFZ-rendered voices ALWAYS promote to stereo via unconditional ToStereoBufferWithPan call. No PanExplicit flag needed; no conditional mono fallthrough; centered (`effectivePan == 0`) produces correct equal L/R via constant-power formula. Acceptance criteria grep-checks both the call AND the absence of the legacy conditional.
- SAMP-01 ships: seq_position + seq_length opcodes parsed; voice-ordinal-seeded round-robin counter deterministic across runs; counter resets at render boundary (Pitfall 6).
- SAMP-02 ships: xfin/xfout opcodes parsed; equal-power velocity-layer crossfade per Pattern 6; hard-switch fallback preserves Phase 33 byte-identical baseline.
- SAMP-03 ships: SamplePathArticulationMultipliers table (Option A scalar — A8 lock); applied at SfzRenderer caller site AFTER Phase 28's ApplyEnvelope (Pitfall 10); sample-path staccato measurably brighter than baseline.
- Phase 28 + Phase 33 regression baselines preserved (modulo any Phase 33 test that asserted SFZ → mono for centered voices; under B2 that contract changes to → stereo with equal L/R, and the test updates accordingly).
</success_criteria>

<output>
Create `.planning/phases/37-sound-design-sampler-polish/37-03-SUMMARY.md`. Record:
- Filenames created/modified; commit refs per task
- Resolution of OQ4 locked to additive-with-clamp
- Resolution of A8 locked to Option A scalar multipliers with exact per-articulation values shipped
- **B2 lock honored (revision pass 2/3)**: SFZ render path unconditional stereo promotion. Note ANY Phase 33 test that needed updating (e.g. asserted mono output for centered SFZ voices). Note the SHA delta on the Phase 33 SFZ smoke baseline (likely byte-different since previously-mono-rendered centered SFZ regions now produce stereo) — if Phase 33 baseline drifts, regenerate the baseline + commit alongside the B2 change.
- mp baseline WAV SHA-256 generated
- Any Phase 33 SFZ regression deltas (should be ZERO byte-for-byte for non-centered renders; documented stereo-promotion delta for centered renders).
</output>
