---
phase: 26-op-standardization-prefix-only
plan: 05
type: execute
wave: 4
depends_on: [26-04]
files_modified:
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md
autonomous: true
requirements: [STD-03]
must_haves:
  truths:
    - "CLAUDE.md line 148 lambda example uses prefix form `(mul x 2)` and `(add a b)` instead of infix `x * 2` / `a + b`"
    - "CLAUDE.md AST table no longer contains the BinaryExpression row (row deleted; arithmetic note added under Core language features)"
    - "CLAUDE.md Core Language Features bullet list includes `Prefix-only arithmetic via (add)/(sub)/(mul)/(div)/(neg)/(idiv) and (concat) builtins`"
    - "D-16: No charitable migration-hint diagnostic shipped — Flow is pre-public per memory project_pre_public_no_legacy_burden.md; the migration script handles every existing .flow file in one sweep, nobody outside this repo has written Flow code, generic 'unexpected token' parse error is sufficient (verified by InfixRejectedFacts asserting errors > 0 only); CLAUDE.md and the migrated tutorial.flow are the canonical references for fresh post-rewrite code"
    - "D-17: Deviation from feedback_charitable_interpretation.md is intentional and bounded by D-16 — the memory's edge-case clause ('does NOT apply to syntax that was just removed where the cost is borne entirely by us') is the active path; revisit if/when Flow ships publicly"
    - "REQUIREMENTS.md adds STD-01/STD-02/STD-03 entries under a new ### Operator Standardization section (CONTEXT.md note line 109)"
    - "REQUIREMENTS.md Traceability table marks STD-01/02/03 as Shipped + commit SHA"
    - "ROADMAP.md Phase 26 entry marked complete with commit SHA list"
    - "STATE.md updated: Phase 26 closed, advance to Phase 26.1 ready (or Phase 27 if 26.1 deferred)"
    - "26-VERIFICATION.md final report exists capturing all 5 ROADMAP success criteria as PASS"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated lambda example + AST table + Core language feature bullet"
      contains: "(mul x 2)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "STD-01/02/03 entries + Traceability rows"
      contains: "STD-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 marked complete with commit list"
      contains: "Phase 26"
    - path: ".planning/STATE.md"
      provides: "Phase 26 closed; advance to next phase"
      contains: "Phase 26"
    - path: ".planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md"
      provides: "Final phase verification report (ROADMAP success criteria + Wave 0 fact PASS list + grep gate + smoke loop + SHA256 gate)"
      contains: "STD-01"
  key_links:
    - from: "CLAUDE.md"
      to: "post-Phase-26 codebase"
      via: "Lambda example must parse and run after Phase 26 — no stale infix"
      pattern: "(mul x 2)\\|(add a b)"
    - from: ".planning/REQUIREMENTS.md Traceability"
      to: "Wave 1/3 commit SHAs"
      via: "STD-01/02/03 mapped to commit SHAs for traceability"
      pattern: "STD-0[123]"
---

<objective>
Wave 4: documentation + closure. After this plan ships, Phase 26 is COMPLETE per ROADMAP success criteria 1-5.

Three documentation/closure deliverables:

1. **CLAUDE.md updates** (D-13 commit 3 + Pitfall 9):
   - Line 148 lambda example: replace infix with prefix
   - Line 175 AST table: delete BinaryExpression row + replace with prefix-only note
   - Add a sibling bullet under "### Core Language Features" documenting prefix-only arithmetic

2. **REQUIREMENTS.md / ROADMAP.md / STATE.md updates** (closure):
   - REQUIREMENTS.md: add the missing STD-01/02/03 requirements (CONTEXT.md note line 109 says these aren't yet listed)
   - REQUIREMENTS.md Traceability table: append STD-01/02/03 rows with Shipped status + commit SHA
   - ROADMAP.md Phase 26 entry: update Plans line to "5 plans" and mark all 5 as shipped
   - STATE.md: advance position to Phase 26.1 ready (or Phase 27 if 26.1 still in scope per ROADMAP)

3. **26-VERIFICATION.md** — final phase verification report capturing all 5 ROADMAP success criteria as PASS, listing the Wave 0 fact files green-counts, grep-gate confirmation, smoke loop result, SHA256 gate confirmation. This is the equivalent of the Phase 25 closure VERIFICATION.

The plan ends with a final commit and final smoke runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-op-standardization-prefix-only/26-CONTEXT.md
@.planning/phases/26-op-standardization-prefix-only/26-RESEARCH.md
@.planning/phases/26-op-standardization-prefix-only/26-04-SUMMARY.md

<verbatim_clauses>
ROADMAP success criteria (lines 211-217 of ROADMAP.md, this is what must verify PASS):

  1. Parser no longer accepts infix `+ - * /` outside negative number literals; `ParseAdditive` and `ParseMultiplicative` removed; `BinaryExpression` + `BinaryOperator` deleted (STD-01)
  2. Builtins `(add)`, `(sub)`, `(mul)`, `(div)` ship with overloads for Int, Long, Float, Double, Number; `(neg x)` for runtime negation; `(concat String String)` for string concatenation (STD-02)
  3. Negative number literals (`-5`, `-3.14`) lex as single tokens at expression-start, after `(`, after `,` — mirroring existing `-3dB`/`+50c`/`-5st` precedent (STD-02)
  4. All ~70 existing `test_*.flow` scripts migrated to prefix form and pass with byte-identical output; `tutorial.flow` + `showcase.flow` cmp-clean across two consecutive runs (STD-03)
  5. CLAUDE.md updated to remove the stale "==, !=, <, >" claim and document prefix-only rule (STD-03)
</verbatim_clauses>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md updates — lambda example + AST table + Core feature bullet</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/26-op-standardization-prefix-only/26-CONTEXT.md
    .planning/phases/26-op-standardization-prefix-only/26-PATTERNS.md
  </read_first>
  <action>
    Apply pattern P-08 from PATTERNS.md verbatim. Three edits to CLAUDE.md.

    **Edit 1 — Line 148 (lambda example).** Locate the line:
    ```
    - Lambda functions: `fn Int x => x * 2`, `fn Int a, Int b => a + b`
    ```
    Replace with:
    ```
    - Lambda functions: `fn Int x => (mul x 2)`, `fn Int a, Int b => (add a b)`
    ```
    This addresses RESEARCH Pitfall 9 — composers reading CLAUDE.md and copying the lambda example would otherwise hit a parse error post-Phase-26.

    **Edit 2 — Line 175 (AST table BinaryExpression row).** Locate the line:
    ```
    | `BinaryExpression` | Binary operations (+, -, *, /, ==, !=, <, >, etc.) |
    ```
    DELETE the entire row. Then ADD a clarifying note immediately AFTER the AST table — after the closing of the table (after `| SongExpression | Song arrangement ([intro verse chorus]) |` on line 187 or thereabouts). Add:
    ```

    **Note:** Arithmetic uses prefix builtins, not AST nodes. See `(add)`/`(sub)`/`(mul)`/`(div)`/`(neg)`/`(idiv)` and `(concat)` in the Standard Library — there is no `BinaryExpression` AST node; the parser produces `FunctionCallExpression` for arithmetic.
    ```
    Note: the original line 175 row also contains the stale `==, !=, <, >` claim that ROADMAP success criterion 5 explicitly says to remove. Comparison operators are already prefix in Flow today (per CONTEXT.md domain section: `(eq)`, `(lt)`, `(gt)` style). Removing the BinaryExpression row removes the stale comparison claim by the same edit.

    **Edit 3 — Add a sibling bullet under "### Core" Language Features.** Locate the existing bullet block (around lines 145-152). After the `Lambda functions: ...` bullet (which is line 148, just edited above), add a new bullet:
    ```
    - Prefix-only arithmetic via `(add)`/`(sub)`/`(mul)`/`(div)`/`(neg)`/`(idiv)` and `(concat)` builtins (no infix `+ - * /`)
    ```

    Verify by reading CLAUDE.md after the edits to confirm:
    - Line 148 (or thereabouts; line numbers may shift after the row deletion) shows the prefix-form lambda
    - The AST table no longer contains a `BinaryExpression` row
    - The Core bullet list contains the new prefix-only line
    - No mention of `==, !=, <, >` in the AST table

    Do NOT yet commit — Task 4 commits Wave 4 atomically.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -c "(mul x 2)\|(add a b)" CLAUDE.md && echo "---NO-BINEXPR---" && (! grep -q "BinaryExpression" CLAUDE.md && echo "OK_no_binexpr_row") && echo "---PREFIX-BULLET---" && grep -c "Prefix-only arithmetic via" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(mul x 2)" CLAUDE.md` returns 1.
    - `grep -c "(add a b)" CLAUDE.md` returns 1.
    - `! grep -n "x \* 2\|a + b" CLAUDE.md` (no infix examples remain — exception: if a literal `*2` appears in a regex or `^A + B$` text outside the lambda example, surface it manually but the lambda example specifically must be prefix).
    - `! grep "BinaryExpression" CLAUDE.md` (the AST table row was deleted).
    - `! grep "==, !=, <, >" CLAUDE.md` (stale comparison claim removed via the row deletion).
    - `grep -c "Prefix-only arithmetic via" CLAUDE.md` returns 1.
    - `grep -c "(neg)\|(idiv)" CLAUDE.md` returns at least 1 (mentioned in either the prefix-only bullet or the AST-table replacement note).
  </acceptance_criteria>
  <done>CLAUDE.md is byte-clean of stale infix and BinaryExpression references. Lambda example uses prefix form. Core feature bullet documents the new rule.</done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md — add STD-01/02/03 entries + Traceability table rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/26-op-standardization-prefix-only/26-CONTEXT.md
    .planning/phases/26-op-standardization-prefix-only/26-04-SUMMARY.md
    .planning/ROADMAP.md
  </read_first>
  <action>
    REQUIREMENTS.md does NOT currently list STD-01/02/03 in its body — CONTEXT.md note line 109 explicitly says: "STD-01/02/03 are referenced in ROADMAP.md but NOT yet listed in REQUIREMENTS.md's body — planner should add them under a new `### Operator Standardization` section to keep traceability consistent."

    The current REQUIREMENTS.md still lists DICT-01/02/03 as Phase 26 (lines 87-93 of the file) and QOL-04 as Phase 27 (line 97). With Phase 26 redefined to prefix-only standardization (per ROADMAP confirmation), DICT-01/02/03 either belong to Phase 26.1 (Symbols + Tuples + Dicts, per ROADMAP line 219-231) or stay in their original location depending on REQUIREMENTS.md milestone-id taxonomy. We do NOT touch DICT-01/02/03 entries here — leave them as-is (they may be re-homed in a future phase planning pass).

    **Edit 1 — Add new section "### Operator Standardization"** to the REQUIREMENTS.md body, somewhere logical (between existing requirement sections). The Phase 26 IDs are STD-01, STD-02, STD-03. Insert after the "### Gaussian Humanize (LAST PRNG phase)" section (around line 85 in current file, before "### Dictionary Support" at line 87) — that placement matches the v1.3 phase ordering (Phase 25 → Phase 26 → Phase 26.1 → Phase 27).

    Add the section verbatim:
    ```markdown
    ### Operator Standardization

    - [x] **STD-01**: Parser/AST cleanup. `BinaryExpression` record + `BinaryOperator` enum deleted from `flow-lang/Ast/Expressions/`. `ParseAdditive`/`ParseMultiplicative` methods removed from `Parser.cs`. `ParseUnary`'s arithmetic branch deleted; replaced by `ParseUnaryShorthand` that handles D-01 `-IDENT → (neg IDENT)` shorthand and D-03 silent `+IDENT` strip. `EvaluateBinary` + its switch case deleted from `ExpressionEvaluator.cs`. Two non-arithmetic `ParseUnary` callers (array index `arr@-1` at Parser.cs:774, optional-paren args at Parser.cs:940) rewired to `ParseUnaryShorthand`. Music-context Plus/Minus consumers (tempo/swing/pan/gain/reverbTime at Parser.cs:121-556) PRESERVED. Acceptance: `! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/` returns empty; bare infix produces parse error per `InfixRejectedFacts`.

    - [x] **STD-02**: Builtin completion + lexer single-token negative literals. `(add)`/`(sub)`/`(mul)`/`(div)` ship 5 same-type overloads each (Int, Long, Float, Double, Number — D-05 fast paths via direct CLR primitives). `(neg)` ships 5 per-type overloads (D-07). `(idiv Int Int) → Int` ships per D-08. `(div Int Int)` auto-promotes to Double per D-08. Negative number literals `-5`/`-3.14` lex as single tokens at 6 expression-start positions per D-02/D-04 (statement-start, after `(`, after `,`, after `[`, after `->`, after `|` note-stream open). Music-context keywords (tempo/swing/pan/gain/reverbTime) EXCLUDED from gate so `pan -0.5` continues to lex Minus standalone (Pitfall 1 mitigation). `EvaluateFunctionCall` coerces argValues at the invocation boundary so D-05 mixed-type convertible-scoring path works (Pitfall 2 fix). std.flow declares all 5×4 same-type arithmetic overloads + neg 5-pack + idiv (incidentally fixes pre-existing sub/mul/div Float decl asymmetry).

    - [x] **STD-03**: Migrate all in-repo `.flow` files to prefix form; preserve byte-identical `tutorial.flow`/`showcase.flow` output; CLAUDE.md updated. Throwaway tokenizer-based migration script at `scripts/Migrate26/` (standalone csproj per RESEARCH form-factor decision; `dotnet-script` not installed). Pre/post SHA256 hash gate during commit per D-14 — Phase 18 + Phase 23 + Phase 25 byte-identical xUnit Facts are the persistent guards (no new sibling test added). All ~38 affected `.flow` files migrated atomically. `cmp`-clean tutorial.flow + showcase.flow across two consecutive runs. CLAUDE.md line 148 lambda example rewritten to prefix; line 175 BinaryExpression AST row deleted; new "Prefix-only arithmetic" bullet under Core Language Features.
    ```

    **Edit 2 — Update the Traceability table** (lines 122-158 of current file). Find the table at the end of the file and append three new rows:
    ```markdown
    | STD-01 | Phase 26 | Shipped <commit-sha-of-Wave-1> |
    | STD-02 | Phase 26 | Shipped <commit-sha-of-Wave-1> |
    | STD-03 | Phase 26 | Shipped <commit-sha-of-Wave-3> |
    ```
    Where `<commit-sha-of-Wave-1>` is the actual SHA from `git log` (find via `git log --grep="feat(26): prefix-only AST" --pretty=%h | head -1`) and `<commit-sha-of-Wave-3>` is from `git log --grep="chore(26): migrate all" --pretty=%h | head -1`. Use the short SHA form (7 chars).

    NOTE: the existing Traceability table includes `DICT-01 | Phase 26 | Pending` rows (lines 155-157). Those entries reflect the OLD interpretation of Phase 26 (before the v1.3 milestone reordering inserted Phase 26 prefix-only standardization and deferred DICT-* to Phase 26.1). Update those rows:
    - DICT-01: change "Phase 26" → "Phase 26.1"
    - DICT-02: change "Phase 26" → "Phase 26.1"
    - DICT-03: change "Phase 26" → "Phase 26.1"
    - Status stays "Pending" — Phase 26.1 hasn't shipped yet.

    Do NOT yet commit — Task 4 commits Wave 4 atomically.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -n "STD-01\|STD-02\|STD-03" .planning/REQUIREMENTS.md | head -10 && echo "---DICT-REHOME---" && grep -n "DICT-0" .planning/REQUIREMENTS.md | head -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Operator Standardization" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "\*\*STD-01\*\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "\*\*STD-02\*\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "\*\*STD-03\*\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "STD-01.*Phase 26.*Shipped" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "STD-02.*Phase 26.*Shipped" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "STD-03.*Phase 26.*Shipped" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "DICT-01.*Phase 26.1\|DICT-02.*Phase 26.1\|DICT-03.*Phase 26.1" .planning/REQUIREMENTS.md` returns 3 (DICT-* re-homed to Phase 26.1).
    - The new STD-01/02/03 entries are checkbox-marked `[x]` (shipped, not pending).
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains a new ### Operator Standardization section with STD-01/02/03 marked shipped. Traceability table updated with three new rows + DICT-* re-homed to Phase 26.1.</done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP.md + STATE.md + 26-VERIFICATION.md final report</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/25-gaussian-humanize/25-VERIFICATION.md
    .planning/phases/26-op-standardization-prefix-only/26-04-SUMMARY.md
  </read_first>
  <action>
    Three closure files. Use Phase 25's VERIFICATION.md as a structural template (`.planning/phases/25-gaussian-humanize/25-VERIFICATION.md`).

    **Step A — Update `.planning/ROADMAP.md` Phase 26 entry.**

    Locate the Phase 26 section (around lines 207-217). The current entry has `**Plans**: TBD` (line 217). Update to:
    ```
    **Plans**: 5 plans
    - [x] 26-01-PLAN.md — Wave 0 RED test scaffolding (7 fact files + Migrate26 stub) — Shipped <wave0-sha>
    - [x] 26-02-PLAN.md — Wave 1 GREEN mega-commit (BinaryExpression delete + parser/lexer/evaluator + builtins) — Shipped <wave1-sha>
    - [x] 26-03-PLAN.md — Wave 2 migration tool implementation + idempotence smoke — Shipped <wave2-sha>
    - [x] 26-04-PLAN.md — Wave 3 mass migration + SHA256 byte-identical gate — Shipped <wave3-sha>
    - [x] 26-05-PLAN.md — Wave 4 docs + closure — Shipped <wave4-sha>
    ```
    Replace `<waveN-sha>` with actual short SHAs from `git log --oneline | head -10`.

    Also update the Progress table at the bottom of ROADMAP.md (around lines 273-275). Find the `Phase 26` row:
    ```
    | 26. Op Standardization (Prefix-Only) | v1.3 | 0/N | Not started | - |
    ```
    Change to:
    ```
    | 26. Op Standardization (Prefix-Only) | v1.3 | 5/5 | Complete | <YYYY-MM-DD> |
    ```
    Use the actual current date.

    Update the milestone progress line (around line 8): `🚧 v1.3 Composer DX Tier B/C — Phases 18-27 (in progress)` — keep as-is (still in progress until Phase 27 ships).

    **Step B — Update `.planning/STATE.md` to advance position.**

    Locate the Current Position block (lines 27-32 of current STATE.md). Update:
    - `Phase: 26 (Op Standardization, Prefix-Only) — READY TO PLAN` → `Phase: 26.1 (Symbols + Tuples + Dicts) — READY TO PLAN` (or `Phase 27` if 26.1 has been merged/cancelled — confirm via ROADMAP)
    - `Plan: 0 of TBD` → `Plan: 0 of TBD`
    - `Status:` line — update with current status

    Locate the Resume Instructions block (lines 36-40). Replace with:
    ```
    Phase 26 closed <YYYY-MM-DD>. v1.3 milestone now 9/11 phases complete (Phases 18-26 + future Phase 26.1 + Phase 27 remaining). Phase 26.1 (Symbols + Tuples + Dicts — Symbol primitive, Tuple type with `<<a, b>>` literal and `~>` unpack op, generic Dict<K, V>) is the next ROADMAP target. Phase 27 (Tutorial + Showcase Refresh) closes the v1.3 milestone after Phase 26.1 ships.

    Phase 17 has 3 pending HUMAN-UAT items in 17-HUMAN-UAT.md (rows 1-3 of manual-smoke.md) — orthogonal to v1.3 work, resolve at first release tag.
    ```

    Also update the YAML frontmatter:
    - `status:` line: `Phase 26 shipped (STD-01/02/03); Phase 26.1 unblocked`
    - `stopped_at:` line: `Phase 26 closed`
    - `last_updated:` ISO timestamp (use a current ISO timestamp matching the closure commit)
    - `last_activity:` `<YYYY-MM-DD> -- Phase 26 shipped (STD-01/02/03)`
    - `progress.total_plans:` increment by 5 (from 37 to 42)
    - `progress.completed_plans:` increment by 5 (from 37 to 42)
    - `progress.completed_phases:` increment by 1 (from 8 to 9)

    **Step C — Create `.planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md`.**

    Read `.planning/phases/25-gaussian-humanize/25-VERIFICATION.md` first to mirror its structure. The Phase 26 verification report has these sections:

    1. **Frontmatter** with `phase: 26`, `slug: op-standardization-prefix-only`, `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, dates.
    2. **Phase Summary** — one-paragraph recap.
    3. **ROADMAP Success Criteria** — table mapping each of the 5 success criteria to PASS evidence.
    4. **Wave 0 Fact Files** — table of the 7 fact files with PASS/FAIL counts (all PASS post-Wave-1).
    5. **Static Gate** — `! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/` returns empty (PASS).
    6. **Smoke Loop** — count of `.flow` files run × 0 failures.
    7. **SHA256 Byte-Identical Gate** — pre/post hash file content + diff exit 0.
    8. **Persistent xUnit Guards** — Phase 18 + 23 + 25 byte-identical Facts pass count.
    9. **Commits** — table of Wave 0/1/2/3/4 commit SHAs.
    10. **Closure Sign-Off** — checklist confirming each ROADMAP success criterion + each must_have from each plan.

    Concrete content (fill in actual values from earlier wave summaries):
    ```markdown
    ---
    phase: 26
    slug: op-standardization-prefix-only
    status: complete
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-04
    completed: <YYYY-MM-DD>
    ---

    # Phase 26 — Verification Report

    ## Summary

    Phase 26 (Op Standardization, Prefix-Only) eliminated infix arithmetic operators in favor of S-expression prefix builtins. Three coordinated changes shipped atomically per D-13 single-mega-commit mandate:
    - AST/parser surgery: `BinaryExpression` record + enum deleted; `ParseAdditive`/`ParseMultiplicative`/`ParseUnary` arithmetic branch removed; `ParseUnaryShorthand` replaces them with the D-01 `-IDENT → (neg IDENT)` shorthand and D-03 silent `+IDENT` strip.
    - Builtin completion: 14 new registrations covering the full numeric widening chain (Long/Number same-type fast paths) + `(neg)` 5-pack + `(idiv Int Int)` + `(div Int Int)` auto-promotes to Double per D-08. `EvaluateFunctionCall` coerces at the invocation boundary so D-05 mixed-type convertible-scoring works (Pitfall 2 fix).
    - Lexer extension: `_lastEmittedType` tracks emission positions; `TryLexSignedNumber` emits single-token signed literals at 6 expression-start positions (D-04). Music-context keywords excluded from the gate so `pan -0.5` continues to work (Pitfall 1).

    Mass migration via `scripts/Migrate26` swept ~38 in-repo `.flow` files. SHA256 byte-identical gate (D-14) PASSED on `examples/output/flow_{tutorial,showcase}.{wav,mid}` pre vs post. Phase 18/23/25 persistent xUnit byte-identical guards stay GREEN.

    ## ROADMAP Success Criteria

    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | Parser no longer accepts infix +/-/*//; ParseAdditive+ParseMultiplicative removed; BinaryExpression+BinaryOperator deleted (STD-01) | PASS | `! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/` returns empty. `flow-lang.Tests/Unit/Phase26/InfixRejectedFacts.cs` 5/5 PASS. |
    | 2 | (add)/(sub)/(mul)/(div) ship 5 same-type overloads each + (neg) + (concat String String) (STD-02) | PASS | `flow-lang.Tests/Unit/Phase26/NewOverloadFacts.cs` 8/8 PASS, `NegOverloadFacts.cs` 5/5 PASS. |
    | 3 | Negative number literals lex as single tokens at expression-start, after `(`, after `,` (STD-02) | PASS | `flow-lang.Tests/Unit/Phase26/NegativeLiteralLexFacts.cs` Theory + TempoMinus_PreservesStandaloneMinus PASS. |
    | 4 | All ~70 existing test_*.flow scripts migrated and pass with byte-identical output; tutorial+showcase cmp-clean (STD-03) | PASS | Smoke loop over 97+ .flow files: 0 failures. SHA256 hash gate empty diff. Phase 18/23/25 byte-identical xUnit guards GREEN. |
    | 5 | CLAUDE.md updated to remove the stale "==, !=, <, >" claim and document prefix-only rule (STD-03) | PASS | Line 148 lambda example uses prefix; line 175 BinaryExpression row deleted; new "Prefix-only arithmetic" bullet under Core. |

    ## Wave 0 Fact Files

    | File | Facts | Status |
    |------|-------|--------|
    | flow-lang.Tests/Unit/Phase26/NewOverloadFacts.cs | 8 | <green-count>/8 PASS |
    | flow-lang.Tests/Unit/Phase26/NegOverloadFacts.cs | 5 | <green-count>/5 PASS |
    | flow-lang.Tests/Unit/Phase26/IntegerDivisionFacts.cs | 2 | <green-count>/2 PASS |
    | flow-lang.Tests/Unit/Phase26/MixedTypeArithmeticFacts.cs | 6 | <green-count>/6 PASS |
    | flow-lang.Tests/Unit/Phase26/NegativeLiteralLexFacts.cs | 8 | <green-count>/8 PASS |
    | flow-lang.Tests/Unit/Phase26/UnaryMinusShorthandFacts.cs | 2 | <green-count>/2 PASS |
    | flow-lang.Tests/Unit/Phase26/InfixRejectedFacts.cs | 5 | <green-count>/5 PASS |

    Replace <green-count> with actual numbers from `dotnet test --filter "FullyQualifiedName~Phase26" --logger "console;verbosity=detailed"`.

    ## Static Gate

    ```bash
    $ ! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/ 2>/dev/null
    OK (zero matches)
    ```

    ## Smoke Loop

    Run over `tests/*.flow examples/*.flow flow-lang/*.flow`:
    - Total: <count> files
    - Failures: 0

    ## SHA256 Byte-Identical Gate (D-14)

    Pre-migration:
    ```
    <paste content of /tmp/26-hashes-pre.txt>
    ```

    Post-migration:
    ```
    <paste content of /tmp/26-hashes-post.txt>
    ```

    `diff /tmp/26-hashes-pre.txt /tmp/26-hashes-post.txt`: exit 0 (byte-identical).

    ## Persistent xUnit Guards

    - Phase18.ByteIdenticalShowcaseTests / ByteIdenticalTutorialTests: PASS
    - Phase23.ByteIdenticalDefaultTuningTests: PASS
    - Phase25.ByteIdenticalShowcaseGaussianTests: PASS

    ## Commits

    | Wave | SHA | Message |
    |------|-----|---------|
    | 0 | <wave0-sha> | test(26): add Phase26 fact scaffolding (RED) + Migrate26 stub |
    | 1 | <wave1-sha> | feat(26): prefix-only AST + builtin completion (Wave 1 GREEN mega-commit) |
    | 2 | <wave2-sha> | feat(26): implement migration walker + idempotence smoke test |
    | 3 | <wave3-sha> | chore(26): migrate all .flow files to prefix arithmetic form |
    | 4 | <wave4-sha> | docs(26): CLAUDE.md prefix-only rule + REQUIREMENTS/ROADMAP/STATE closure |

    ## Closure Sign-Off

    - [x] All 5 ROADMAP success criteria PASS
    - [x] All 7 Wave 0 fact files GREEN
    - [x] Static gate empty (BinaryExpression/BinaryOperator absent)
    - [x] Smoke loop zero failures
    - [x] SHA256 byte-identical gate PASS
    - [x] Phase 18/23/25 persistent xUnit guards GREEN
    - [x] CLAUDE.md updated (lambda example + AST row deletion + Core bullet)
    - [x] REQUIREMENTS.md ### Operator Standardization section added; STD-01/02/03 traceability rows shipped
    - [x] ROADMAP Phase 26 entry marked complete; Progress table row updated
    - [x] STATE.md advanced to Phase 26.1 ready
    - [x] All 13 D-NN locked decisions implemented (verify each in 26-CONTEXT.md)
    - [x] All 10 RESEARCH pitfalls mitigated (verify each in 26-RESEARCH.md)

    Phase 26 closed <YYYY-MM-DD>.
    ```

    Fill in actual values for `<wave0-sha>`, etc., from `git log --oneline | grep "(26):"` (use short SHAs).

    Do NOT yet commit — Task 4 commits Wave 4 atomically.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && grep -c "Phase 26.*5/5.*Complete" .planning/ROADMAP.md && echo "---STATE---" && grep -c "Phase 26 shipped\|Phase 26.1 unblocked\|Phase 26.1.*READY TO PLAN" .planning/STATE.md && echo "---VERIF---" && test -f .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md && head -5 .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "26-01-PLAN.md.*Shipped\|26-02-PLAN.md.*Shipped\|26-03-PLAN.md.*Shipped\|26-04-PLAN.md.*Shipped\|26-05-PLAN.md.*Shipped" .planning/ROADMAP.md` returns 5.
    - `grep -c "Phase 26.*5/5.*Complete" .planning/ROADMAP.md` returns 1 (Progress table row updated).
    - `grep -c "Phase 26 shipped\|Phase 26.1 unblocked" .planning/STATE.md` returns at least 1.
    - `grep -E "completed_phases: 9" .planning/STATE.md` returns 1 (incremented from 8).
    - `grep -E "completed_plans: 42" .planning/STATE.md` returns 1 (incremented from 37 by 5).
    - File `.planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md` exists.
    - `grep -c "ROADMAP Success Criteria" .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md` returns 1.
    - `grep -c "PASS" .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md` returns at least 10 (multiple PASS markers across criteria/sections).
    - `grep -c "status: complete" .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md` returns 1.
    - `grep -c "nyquist_compliant: true" .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md` returns 1.
  </acceptance_criteria>
  <done>ROADMAP/STATE updated; 26-VERIFICATION.md created with all PASS markers and SHA256 gate evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Final smoke run + Wave 4 atomic closure commit</name>
  <files></files>
  <read_first>
    .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md
  </read_first>
  <action>
    Final pre-commit verification — re-run every gate to confirm no regressions slipped in during Wave 4 doc-edit work.

    **Step A — Build green:**
    ```bash
    dotnet build
    ```

    **Step B — Full test suite green:**
    ```bash
    dotnet test
    ```
    Expect 0 failures. If any test fails, STOP and investigate — Wave 4 should be doc-only and not regress anything.

    **Step C — Phase 26 fact files all green:**
    ```bash
    dotnet test --filter "FullyQualifiedName~Phase26" flow-lang.Tests/flow-lang.Tests.csproj
    ```
    Expect all 36 facts (sum of fact counts across 7 files) PASS.

    **Step D — Static gate empty:**
    ```bash
    ! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/ 2>/dev/null && echo "GATE_PASS"
    ```

    **Step E — Smoke loop zero failures:**
    ```bash
    failed=0
    for f in tests/*.flow examples/*.flow flow-lang/*.flow; do
        if ! dotnet run --project flow-interpreter "$f" >/dev/null 2>&1; then
            echo "FAIL: $f"
            failed=$((failed + 1))
        fi
    done
    echo "smoke_failures=$failed"
    ```
    Expect `smoke_failures=0`.

    **Step F — Stage Wave 4 files for commit.** Stage only the doc/closure files:
    ```bash
    git add CLAUDE.md \
            .planning/REQUIREMENTS.md \
            .planning/ROADMAP.md \
            .planning/STATE.md \
            .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md
    git status --porcelain
    ```
    Verify ONLY those 5 files are staged. If any other file is staged (e.g., a stray test output), unstage it.

    **Step G — Wave 4 atomic commit.** Commit with HEREDOC:
    ```
    docs(26): CLAUDE.md prefix-only rule + REQUIREMENTS/ROADMAP/STATE closure

    Phase 26 closes. CLAUDE.md updated:
    - Line 148 lambda example rewritten to prefix form `(mul x 2)` and `(add a b)`
      (Pitfall 9 — composers copying the example would otherwise hit a parse error
      post-Phase-26)
    - Line 175 BinaryExpression AST table row deleted (the stale `==, !=, <, >`
      claim was incorrect — comparison operators are already prefix in Flow)
    - New "Prefix-only arithmetic" bullet under Core Language Features

    REQUIREMENTS.md gains a new ### Operator Standardization section listing
    STD-01/02/03 with [x] shipped status. Traceability table appended with
    three new rows. DICT-01/02/03 entries re-homed to Phase 26.1 (the v1.3
    milestone now correctly shows DICT-* under their actual phase per the
    ROADMAP reordering that inserted Phase 26 prefix-only standardization).

    ROADMAP.md Phase 26 entry marked Complete with all 5 plan SHAs listed;
    Progress table row updated.

    STATE.md advances position to Phase 26.1 ready (next ROADMAP target);
    YAML frontmatter incremented (completed_phases 8→9, total_plans 37→42).

    .planning/phases/26-op-standardization-prefix-only/26-VERIFICATION.md
    captures all 5 ROADMAP success criteria as PASS, the 7 Wave 0 fact files
    all GREEN, static gate empty, smoke loop zero failures, SHA256 byte-
    identical gate confirmed, Phase 18/23/25 persistent xUnit guards GREEN.

    All 13 D-NN locked decisions implemented; all 10 RESEARCH pitfalls
    mitigated.

    Refs: STD-01, STD-02, STD-03

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```

    **Step H — Final post-commit confirmation:**
    ```bash
    git log -1 --pretty=%H
    git log --oneline | grep "(26):" | head -10
    git status --porcelain
    ```
    Verify the working tree is clean and the last 5 commits are the Phase 26 wave commits in order.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet test 2>&1 | tail -3 && echo "---STATIC-GATE---" && (! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/ 2>/dev/null && echo "GATE_PASS") && echo "---LOG---" && git log --oneline | grep "(26):" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test` (full suite) reports 0 failures.
    - `! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/` returns empty.
    - `dotnet test --filter "FullyQualifiedName~Phase26" flow-lang.Tests/flow-lang.Tests.csproj` reports 0 failures.
    - Smoke loop over `tests/*.flow examples/*.flow flow-lang/*.flow` reports 0 failures.
    - `git log --oneline | grep "(26):" | wc -l` returns at least 5 (one commit per wave: Wave 0 test, Wave 1 feat, Wave 2 feat, Wave 3 chore, Wave 4 docs).
    - `git log -1 --pretty=%s` shows the "docs(26):" closure commit.
    - `git status --porcelain | wc -l` returns 0.
  </acceptance_criteria>
  <done>Phase 26 closed atomically. Working tree clean. All gates green.</done>
</task>

</tasks>

<verification>
- `dotnet build` exits 0.
- `dotnet test` reports 0 failures.
- `! grep -rn "BinaryExpression\|BinaryOperator" flow-lang/ flow-lsp/ flow-midi/` is empty.
- All 7 Phase26 fact files GREEN.
- Smoke loop over all in-repo .flow files: 0 failures.
- 5 Phase 26 wave commits in `git log --oneline | grep "(26):"`.
- Wave 4 closure commit landed.
</verification>

<success_criteria>
- CLAUDE.md updated per ROADMAP success criterion 5.
- REQUIREMENTS.md augmented with STD-01/02/03 + Traceability rows.
- ROADMAP.md Phase 26 entry marked complete with commit SHAs; Progress table updated.
- STATE.md advanced to Phase 26.1 ready.
- 26-VERIFICATION.md final report exists capturing all 5 ROADMAP success criteria as PASS.
- Wave 4 closure commit landed atomically.
- All gates green (build, test, static gate, smoke loop, SHA256 byte-identical, persistent xUnit guards).
</success_criteria>

<output>
After completion, create `.planning/phases/26-op-standardization-prefix-only/26-05-SUMMARY.md` capturing:
- CLAUDE.md edits (3 sites: line 148, line 175 row deletion, new Core bullet)
- REQUIREMENTS.md edits (new section + 3 Traceability rows + 3 DICT-* re-homed)
- ROADMAP/STATE closure status
- 26-VERIFICATION.md location + summary
- Wave 4 commit SHA
- Phase 26 closure status: COMPLETE
</output>
