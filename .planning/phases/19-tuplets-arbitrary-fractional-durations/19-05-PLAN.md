---
phase: 19-tuplets-arbitrary-fractional-durations
plan: 05
type: execute
wave: 4
depends_on: [19-01, 19-02, 19-03, 19-04]
files_modified:
  - flow-lang/StandardLibrary/Transforms/TransformFunctions.cs
  - flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs
  - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [TUP-07]
tags: [audit-verified, augment-diminish, tuplet-aware, closure, verification-rollup, two-pass-strict]
must_haves:
  truths:
    - "augment(tupletSeq) on a sequence with DurationFraction = [1/12, 1/12, 1/12] (whole-units) produces a sequence with DurationFraction = [1/6, 1/6, 1/6] — exact rational doubling via Fraction(2,1) × f"
    - "diminish(tupletSeq) on the same input produces [1/24, 1/24, 1/24] — exact rational halving via Fraction(1,2) × f"
    - "AUDIT-VERIFIED comment markers at TransformFunctions.cs:239 and TransformFunctions.cs:261 are updated to include `2026-04-NN: re-validated against tuplet sequences (Phase 19 TUP-07)` AND the Plan 19-05 hash"
    - "Existing power-of-2 enum path of augment/diminish stays UNCHANGED when DurationFraction is null — Phase 11/12 C5 audit remains valid for non-tuplet sequences"
    - "19-VERIFICATION.md rollup pins criteria-to-artifact mapping for all 8 TUP-XX requirements + commit hash manifest for Plans 19-01..05 + collision grep transcript reference"
    - "REQUIREMENTS.md TUP-01..08 traceability rows flip to `Shipped <hash>` with the actual 5 commit hashes"
    - "ROADMAP.md Phase 19 entry shows 5/5 plans complete with Status flipped to Complete"
    - "STATE.md advances to Phase 19 closure (current focus updated, milestone progress recomputed)"
    - "Two-pass strict authorship per CONTEXT D-15 — Pass 1 drafts the augment/diminish Facts from REQUIREMENTS.md TUP-07 wording alone; Pass 2 lands against real TransformFunctions code; any divergence logged in 19-05-SUMMARY.md"
    - "Phase 18 ByteIdentical{Tutorial,Showcase}Tests remain GREEN end-to-end; full suite at pre-Phase-19 baseline + 31 (cumulative Phase19 across all 5 plans) passed"
  artifacts:
    - path: "flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs"
      provides: "TUP-07 acceptance Facts: rational doubling + rational halving + power-of-2 enum path preserved + AUDIT-VERIFIED comment grep regression"
      contains: "namespace FlowLang.Tests.Unit.Phase19"
    - path: "flow-lang/StandardLibrary/Transforms/TransformFunctions.cs"
      provides: "Augment/Diminish gain `if (note.DurationFraction.HasValue)` branch performing Fraction(2,1)×f or Fraction(1,2)×f rational doubling/halving; existing power-of-2 enum path preserved verbatim when DurationFraction is null"
      contains: "DurationFraction.HasValue"
    - path: ".planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md"
      provides: "Phase 19 verification rollup with TUP-XX criteria-to-artifact mapping + commit hash manifest + collision grep transcript reference + final cumulative count"
      contains: "Phase 19 Verification"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TUP-01..08 rows updated from Pending to Shipped <hash>"
      contains: "TUP-08 | Phase 19 | Shipped"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 19 row updated to Complete with 5/5 plans + completion date"
      contains: "19. Tuplets & Arbitrary Fractional Durations | v1.3 | 5/5 | Complete"
    - path: ".planning/STATE.md"
      provides: "Current focus advanced to Phase 19 closure; milestone progress recomputed"
      contains: "Phase 19 (Tuplets & Arbitrary Fractional Durations) — closed"
  key_links:
    - from: "flow-lang/StandardLibrary/Transforms/TransformFunctions.cs"
      to: "flow-lang/TypeSystem/Fraction.cs"
      via: "Multiplies note.DurationFraction.Value by Fraction(2,1) (augment) or Fraction(1,2) (diminish) using Phase 18 operator*"
      pattern: "new Fraction(2, 1)\\|new Fraction(1, 2)"
    - from: "flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs"
      to: "flow-lang/StandardLibrary/Transforms/TransformFunctions.cs"
      via: "Constructs SequenceData with DurationFraction-bearing notes; calls augment/diminish via the FlowEngineRunner OR direct C# Augment(...)/Diminish(...) invocation; pins exact Fraction values"
      pattern: "augment\\|diminish"
---

<objective>
Close Phase 19 with the TUP-07 audit re-validation and the closure artifacts.

**TUP-07 mechanics (per RESEARCH §"Pitfall 9: AUDIT-VERIFIED C5 silently invalidated by tuplet sequences"):** `Augment` / `Diminish` at `flow-lang/StandardLibrary/Transforms/TransformFunctions.cs:239` and `:261` currently operate on `DurationValue` (enum int) only. After Phase 19, tuplet sequences carry `DurationFraction` and `DurationValue == null` — meaning `Augment(tupletSeq)` would silently no-op. Plan 19-05 branches both functions on `note.DurationFraction.HasValue`: when set, multiply by `Fraction(2, 1)` (augment) or `Fraction(1, 2)` (diminish); when null, run the existing enum path verbatim. New regression Facts pin the rational doubling/halving. AUDIT-VERIFIED comments at lines 239 and 261 are updated to `// AUDIT-VERIFIED 2026-04-NN: re-validated against tuplet sequences (Phase 19 TUP-07)`.

**Two-pass strict authorship per CONTEXT D-15:** Pass 1 drafts the Facts from REQUIREMENTS.md TUP-07 wording alone — input `[1/12, 1/12, 1/12]` (whole-units) → output `[1/6, 1/6, 1/6]` and `[1/24, 1/24, 1/24]`. Pass 2 lands against real TransformFunctions code. Any divergence logged in `19-05-SUMMARY.md §Divergences`. Note: Phase 18 18-02 SUMMARY pins DurationFraction in **quarter-units**, not whole-units, so the SPEC's `1/12 whole = 1/3 quarter` translation must be applied. The Pass 1 draft uses quarter-units (Fraction(1, 3) input → Fraction(2, 3) augmented → Fraction(1, 6) diminished). This is a notational adjustment, not a divergence — both representations encode the same musical durations.

**Closure artifacts:**
- `19-VERIFICATION.md` — phase-scoped rollup mapping each TUP-XX requirement to the SPECIFIC test Fact + the SPECIFIC commit hash that landed it. Includes the pre-landing collision grep transcript reference (recorded in 19-01-PLAN.md). Per Phase 14 D-21 / Phase 15 closure pattern.
- `REQUIREMENTS.md` — flips TUP-01..08 traceability rows from "Pending" to "Shipped <hash>" using the actual 5 commit hashes from the Plans 19-01..05 atomic commits.
- `ROADMAP.md` — Phase 19 row updates: 5/5 plans complete, status Complete, completion date 2026-04-NN. The Phase 19 plan-list bullets get `[x]` checkmarks with their commit hashes.
- `STATE.md` — current focus advances to Phase 19 closure; progress block recomputes (Phase 19 plans+1, total plans+5, completed_plans+5).

**Strict ordering per CONTEXT D-12:** This plan is **strictly last** in Phase 19 — depends on all prior 19-XX commits existing in HEAD's ancestor history. Verify before starting via:

```bash
git log --oneline | grep -E "feat\\(19-0[1234]\\):"
# Expected: 4 commits (one per Plan 19-01..04). If fewer, STOP and wait.
```

This plan ships in two atomic commits per the Phase 14/15/16 closure precedent:
1. `feat(19-05): TUP-07 augment/diminish tuplet-aware ...` — TransformFunctions.cs edit + TupletAugmentDiminishTests.cs Facts
2. `docs(19-05): close Phase 19 — verification rollup ...` — 19-VERIFICATION.md + REQUIREMENTS.md + ROADMAP.md + STATE.md

Output: Phase 19 closes with 8/8 TUP-XX requirements Shipped, AUDIT-VERIFIED C5 re-validated against tuplet sequences, full suite GREEN at pre-Phase-19 baseline + 31 (or final cumulative count once verified at execute time).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-SPEC.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-CONTEXT.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-RESEARCH.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-01-SUMMARY.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-02-SUMMARY.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-03-SUMMARY.md
@.planning/phases/19-tuplets-arbitrary-fractional-durations/19-04-SUMMARY.md
@.planning/phases/14-composer-dx-part-1/14-VERIFICATION.md
@.planning/phases/15-composer-dx-part-2/15-VERIFICATION.md
@.planning/phases/18-foundation-rational-duration-arithmetic/18-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Existing Augment / Diminish at flow-lang/StandardLibrary/Transforms/TransformFunctions.cs -->
Lines 239-280 (read at task time for line-exactness; edits are at lines 239 and 261 per CONTEXT + RESEARCH):

```csharp
// AUDIT-VERIFIED 2026-04-18: C5 — augment correct (lengthens); observed A=#### vs Q=## columns in visualize (tests/spike/c5-augment-diminish.flow)
private static Value Augment(IReadOnlyList<Value> args)
{
    var seq = args[0].As<SequenceData>();

    var result = TransformNotes(seq, note =>
    {
        if (!note.DurationValue.HasValue) return note;

        int newDur = note.DurationValue.Value - 1; // toward WHOLE=0
        if (newDur < (int)NoteValueType.Value.WHOLE)
        {
            Console.Error.WriteLine("Warning: augment clamped duration at whole note");
            newDur = (int)NoteValueType.Value.WHOLE;
        }

        return new MusicalNoteData(note.NoteName, note.Octave, note.Alteration, newDur, note.IsRest, note.CentOffset, note.IsTied, note.Velocity, note.Articulation, note.IsDotted, note.SourceLocation, note.SourceLength);
    });

    return Value.Sequence(result);
}

// AUDIT-VERIFIED 2026-04-18: C5 — diminish correct (shortens); observed D=# vs Q=## columns in visualize (tests/spike/c5-augment-diminish.flow)
private static Value Diminish(IReadOnlyList<Value> args)
{
    var seq = args[0].As<SequenceData>();

    var result = TransformNotes(seq, note =>
    {
        if (!note.DurationValue.HasValue) return note;

        int newDur = note.DurationValue.Value + 1; // toward THIRTYSECOND=5
        if (newDur > (int)NoteValueType.Value.THIRTYSECOND)
        {
            Console.Error.WriteLine("Warning: diminish clamped duration at thirty-second note");
            newDur = (int)NoteValueType.Value.THIRTYSECOND;
        }

        return new MusicalNoteData(note.NoteName, note.Octave, note.Alteration, newDur, note.IsRest, note.CentOffset, note.IsTied, note.Velocity, note.Articulation, note.IsDotted, note.SourceLocation, note.SourceLength);
    });

    return Value.Sequence(result);
}
```

After this plan, both functions have a `if (note.DurationFraction.HasValue) { ... return ... durationFraction: doubled-or-halved; }` branch BEFORE the existing `if (!note.DurationValue.HasValue) return note;` line. AUDIT-VERIFIED comment lines refresh.

<!-- Phase 18 Fraction primitive (still in use) -->
```csharp
public readonly record struct Fraction
{
    public int Num { get; }
    public int Denom { get; }
    public Fraction(int num, int denom);
    public static Fraction operator *(Fraction l, Fraction r);
    // ... +, <, > also available
}
```

<!-- 14-VERIFICATION.md as the structural template -->
Read .planning/phases/14-composer-dx-part-1/14-VERIFICATION.md for the verification-rollup shape: header, criteria-to-artifact map, commit hash manifest, collision grep transcript surfacing, deferred-items section. 19-VERIFICATION.md follows the same shape with TUP-XX rows.

<!-- REQUIREMENTS.md traceability table -->
The current state has TUP-01..08 rows like:
```
| TUP-01 | Phase 19 | Pending |
```

After this plan, each row becomes:
```
| TUP-01 | Phase 19 | Shipped <commit-hash-from-19-01> |
```

5 rows: TUP-01, TUP-02, TUP-03 share Plan 19-01's hash. TUP-04, TUP-08 share Plan 19-02's hash. TUP-05 = Plan 19-03's hash. TUP-06 = Plan 19-04's hash. TUP-07 = THIS plan's hash.

<!-- ROADMAP.md Phase 19 entry -->
Currently shows `0/N | Not started`. After this plan: `5/5 | Complete | 2026-04-NN`. Plus the plan-list `[ ]` checkboxes flip to `[x]` with hashes — same pattern as Phase 18 entries already shipped.

<!-- STATE.md "Current Position" + progress block -->
Currently shows Phase 18 closed. After this plan, current focus = Phase 20 (next phase per ROADMAP.md), or "v1.3 progress" status update; progress block recomputes (completed_phases+1, completed_plans+5).

<!-- Cumulative Fact count target -->
Pre-Phase-19 baseline: 306. Plan 19-01: +8 = 314. Plan 19-02: +9 = 323. Plan 19-03: +6 = 329. Plan 19-04: +6 = 335. This Plan 19-05: +N (target ~5-6). Final full-suite count: ~340-341. Verify at execute time and document in 19-VERIFICATION.md.

<!-- Two-pass strict authorship protocol (Phase 13 D-13 / Phase 14 D-13 / CONTEXT D-15) -->
Pass 1 (BEFORE reading TransformFunctions.cs body):
  Draft the augment/diminish Facts from REQUIREMENTS.md TUP-07 wording verbatim. Acceptance:
    augment([Fraction(1,12) whole, ×3]) → [Fraction(1,6) whole, ×3]
    diminish([Fraction(1,12) whole, ×3]) → [Fraction(1,24) whole, ×3]
  Translate to quarter-units per Phase 18 18-02 pin:
    augment([Fraction(1,3) quarter, ×3]) → [Fraction(2,3) quarter, ×3]
    diminish([Fraction(1,3) quarter, ×3]) → [Fraction(1,6) quarter, ×3]

Pass 2 (AFTER editing TransformFunctions.cs):
  Run the Facts. If they pass, log `Outcome A` (no divergence — REQUIREMENTS-as-drafted matches reality).
  If they fail, root-cause: did the augment/diminish edit produce the wrong Fraction? Did GetBeats branch return unexpectedly? Log under §Divergences with the actual produced Fraction values.

Plan 13-01 / 13-04 / 14-03 precedent: Outcome A is the typical outcome when prior research has reduced ambiguity; Outcome B (divergence) is rare and gets a §Divergences entry in the SUMMARY.

<!-- Atomic commit hash naming -->
The 5 commit hashes for the 19-VERIFICATION.md manifest are produced by Plans 19-01..05 (one each). At execute time:
```bash
git log --oneline -10 | grep -E "feat\\(19-0[1-5]\\)|docs\\(19-05\\)"
```

Each commit's short SHA (first 7 chars) goes into the manifest table. The docs(19-05) commit hash is added retrospectively after it lands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pass 1 — draft Facts from REQUIREMENTS alone (TWO-PASS-STRICT discipline; do NOT read TransformFunctions yet)</name>
  <files>flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs</files>
  <read_first>
    - .planning/REQUIREMENTS.md (TUP-07 row + acceptance prose)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-SPEC.md TUP-07 acceptance criteria
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-CONTEXT.md §"Decisions" D-15 (two-pass strict)
    - .planning/phases/18-foundation-rational-duration-arithmetic/18-02-SUMMARY.md §Decisions Made (DurationFraction in quarter-note units)
    - flow-lang.Tests/Unit/Phase19/TupletBracketTests.cs (Plan 19-01 — convention reference for namespace + ParseToAst / CompileNoteStream helpers)
    - flow-lang.Tests/Unit/Phase18/MusicalNoteDataTests.cs (Phase 18 — convention reference for MusicalNoteData ctor named-arg shape with durationFraction:)

    **DO NOT read flow-lang/StandardLibrary/Transforms/TransformFunctions.cs in Task 1** — Pass 1 strict authorship requires drafting Facts from REQUIREMENTS / SPEC alone. Task 2 reads TransformFunctions.cs and lands the GREEN code.
  </read_first>
  <behavior>
    Test 1: Augment_RationalDouble — input sequence with 3 notes, each DurationFraction = Fraction(1, 3) quarter (= 1/12 whole). After augment, each = Fraction(2, 3) quarter (= 1/6 whole). REQUIREMENTS-derived from TUP-07 acceptance.
    Test 2: Diminish_RationalHalve — same input. After diminish, each = Fraction(1, 6) quarter (= 1/24 whole).
    Test 3: Augment_NonTupletPath_StaysOnEnumPath — input with DurationFraction=null and DurationValue=QUARTER → augment returns DurationValue=HALF, DurationFraction stays null. Phase 18 byte-identical contract for non-tuplet sequences preserved.
    Test 4: Diminish_NonTupletPath_StaysOnEnumPath — analog: input QUARTER → diminish returns EIGHTH, DurationFraction stays null.
    Test 5: Augment_AuditVerifiedCommentRefreshed — grep `flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` for "Phase 19 TUP-07" — must return ≥ 2 (one each at lines 239 and 261).

    Pass 1 outcome contract: write the file with all 5 Facts. Run them. RED state expected for Tests 1+2 (TransformFunctions has no DurationFraction branch yet) and Tests 3+4 (those should already be GREEN — existing path is unchanged). Test 5 RED until Task 2 refreshes the comment.

    Pass 1 EXIT condition: file authored, all 5 Fact bodies pinned to expected values, run produces some RED Facts (proves TDD discipline).
  </behavior>
  <action>
**Step 1 — Verify Phase 19-01..04 atomic commits exist in HEAD's ancestor history.**

```bash
git log --oneline | grep -E "feat\\(19-0[1234]\\):" | wc -l
# MUST return 4. If less, Plans 19-01..04 are not complete; STOP and wait for them to merge.
```

**Step 2 — Author `flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` based on REQUIREMENTS.md / SPEC.md alone.**

Pass 1 draft (do not read TransformFunctions.cs body — Task 2 lands the GREEN edits):

```csharp
using System.Collections.Generic;
using System.Linq;
using FlowLang.Runtime;
using FlowLang.StandardLibrary;
using FlowLang.TypeSystem;
using FlowLang.TypeSystem.SpecialTypes;
using Xunit;

namespace FlowLang.Tests.Unit.Phase19;

/// <summary>
/// TUP-07 — AUDIT-VERIFIED C5 re-validated against tuplet sequences.
/// Per CONTEXT D-15 two-pass strict authorship: this file is Pass 1 — drafted
/// from REQUIREMENTS.md TUP-07 wording alone. SPEC's "[1/12, 1/12, 1/12] whole"
/// translates to Fraction(1, 3) quarter per Phase 18 18-02 SUMMARY's quarter-unit pin.
///
/// augment(tupletSeq) doubles → input [1/3 q] × 3 → output [2/3 q] × 3 (= [1/6 whole])
/// diminish(tupletSeq) halves → input [1/3 q] × 3 → output [1/6 q] × 3 (= [1/24 whole])
///
/// Existing power-of-2 enum path stays unchanged when DurationFraction is null —
/// Phase 11/12 C5 audit remains valid for non-tuplet sequences (Tests 3 + 4).
/// </summary>
public class TupletAugmentDiminishTests
{
    /// <summary>
    /// Build a sequence containing the given MusicalNoteData wrapped in a single bar.
    /// </summary>
    private static SequenceData BuildSequence(IEnumerable<MusicalNoteData> notes)
    {
        var seq = new SequenceData();
        var bar = new BarData(notes.ToList(), new TimeSignatureData(4, 4));
        seq.AddBar(bar);
        return seq;
    }

    private static MusicalNoteData TupletNote(char name, int octave, Fraction df)
    {
        return new MusicalNoteData(
            name, octave, alteration: 0,
            durationValue: (int)NoteValueType.Value.QUARTER,  // best-effort enum mirror; rational override applies
            isRest: false,
            durationFraction: df);
    }

    [Fact]
    public void Augment_RationalDouble()
    {
        // SPEC TUP-07: augment([1/12 whole, ×3]) → [1/6 whole, ×3]
        // Quarter-units: augment([1/3 q, ×3]) → [2/3 q, ×3]
        var input = BuildSequence(new[]
        {
            TupletNote('C', 4, new Fraction(1, 3)),
            TupletNote('D', 4, new Fraction(1, 3)),
            TupletNote('E', 4, new Fraction(1, 3)),
        });

        var result = StandardLibrary.Transforms.TransformFunctions.AugmentForTesting(input);
        var notes = result.Bars[0].MusicalNotes;
        Assert.Equal(3, notes.Count);
        foreach (var n in notes)
        {
            Assert.NotNull(n.DurationFraction);
            Assert.Equal(new Fraction(2, 3), n.DurationFraction);
        }
    }

    [Fact]
    public void Diminish_RationalHalve()
    {
        // SPEC TUP-07: diminish([1/12 whole, ×3]) → [1/24 whole, ×3]
        // Quarter-units: diminish([1/3 q, ×3]) → [1/6 q, ×3]
        var input = BuildSequence(new[]
        {
            TupletNote('C', 4, new Fraction(1, 3)),
            TupletNote('D', 4, new Fraction(1, 3)),
            TupletNote('E', 4, new Fraction(1, 3)),
        });

        var result = StandardLibrary.Transforms.TransformFunctions.DiminishForTesting(input);
        var notes = result.Bars[0].MusicalNotes;
        Assert.Equal(3, notes.Count);
        foreach (var n in notes)
        {
            Assert.NotNull(n.DurationFraction);
            Assert.Equal(new Fraction(1, 6), n.DurationFraction);
        }
    }

    [Fact]
    public void Augment_NonTupletPath_StaysOnEnumPath()
    {
        // Phase 18 byte-identical contract: DurationFraction=null + DurationValue=QUARTER → augment makes it HALF, DurationFraction stays null.
        var note = new MusicalNoteData('C', 4, 0,
            durationValue: (int)NoteValueType.Value.QUARTER, isRest: false);
        var input = BuildSequence(new[] { note });
        var result = StandardLibrary.Transforms.TransformFunctions.AugmentForTesting(input);
        var resultNote = result.Bars[0].MusicalNotes.Single();
        Assert.Equal((int)NoteValueType.Value.HALF, resultNote.DurationValue);
        Assert.Null(resultNote.DurationFraction);
    }

    [Fact]
    public void Diminish_NonTupletPath_StaysOnEnumPath()
    {
        var note = new MusicalNoteData('C', 4, 0,
            durationValue: (int)NoteValueType.Value.QUARTER, isRest: false);
        var input = BuildSequence(new[] { note });
        var result = StandardLibrary.Transforms.TransformFunctions.DiminishForTesting(input);
        var resultNote = result.Bars[0].MusicalNotes.Single();
        Assert.Equal((int)NoteValueType.Value.EIGHTH, resultNote.DurationValue);
        Assert.Null(resultNote.DurationFraction);
    }

    [Fact]
    public void AuditVerifiedComment_Phase19TUP07_PresentAtBothSites()
    {
        // SPEC TUP-07 acceptance: AUDIT-VERIFIED comment at TransformFunctions.cs:239 + 261 includes
        // "2026-04-NN: re-validated against tuplet sequences (Phase 19 TUP-07)"
        string repoRoot = System.IO.Path.GetFullPath(System.IO.Path.Combine(FlowScriptData.FindTestsRoot(), ".."));
        string sourcePath = System.IO.Path.Combine(repoRoot, "flow-lang", "StandardLibrary", "Transforms", "TransformFunctions.cs");
        string content = System.IO.File.ReadAllText(sourcePath);
        int matchCount = System.Text.RegularExpressions.Regex.Matches(
            content, "Phase 19 TUP-07").Count;
        Assert.True(matchCount >= 2,
            $"Expected ≥ 2 'Phase 19 TUP-07' AUDIT-VERIFIED markers (one each at Augment + Diminish); found {matchCount}");
    }
}
```

**Step 3 — Run Pass 1 RED check.**

```bash
dotnet build flow-sharp.sln 2>&1 | tail -10
```

Build will FAIL initially because the test references `AugmentForTesting` / `DiminishForTesting` static methods that do NOT yet exist on TransformFunctions. This is expected per Pass 1 strict — the public API of Augment/Diminish takes `IReadOnlyList<Value>` and returns `Value`, which is awkward to test directly. Task 2 must add internal-visible test wrappers (e.g. `internal static SequenceData AugmentForTesting(SequenceData seq) => Augment([Value.Sequence(seq)]).As<SequenceData>();`) AND add `[assembly: InternalsVisibleTo("flow-lang.Tests")]` if not already present. **Verify at task-2 time** whether such wrappers / IVT already exist; if so, the test compiles directly. If not, add them in Task 2.

If the build fails ONLY because of `AugmentForTesting` / `DiminishForTesting` references, log "Pass 1 RED expected — Task 2 adds testing wrappers" and proceed to Task 2.

DO NOT commit yet. Task 2 lands the production code edit + atomic commit.
  </action>
  <acceptance_criteria>
    - File exists: `flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs`
    - `grep -c "namespace FlowLang.Tests.Unit.Phase19" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` returns 1
    - `grep -c "\\[Fact\\]" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` returns 5
    - `grep -c "Augment_RationalDouble\\|Diminish_RationalHalve\\|Augment_NonTupletPath_StaysOnEnumPath\\|Diminish_NonTupletPath_StaysOnEnumPath\\|AuditVerifiedComment_Phase19TUP07_PresentAtBothSites" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` returns 5
    - `grep -c "new Fraction(2, 3)" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` returns at least 1 (augment quarter-unit expected output)
    - `grep -c "new Fraction(1, 6)" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` returns at least 1 (diminish quarter-unit expected output)
    - `grep -c "Phase 19 TUP-07" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` returns at least 1 (Test 5 grep target)
    - Build is in known state (intentional Pass 1 RED for AugmentForTesting/DiminishForTesting refs OR direct-call adjustment) — this is fine; Task 2 lands GREEN
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\\[Fact\\]" flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs</automated>
  </verify>
  <done>5 Pass 1-drafted Facts in TupletAugmentDiminishTests.cs pinning REQUIREMENTS-as-drafted expected Fractions. Build state expected to be RED (intentional — Pass 2 lands the GREEN production code in Task 2). Pass 1 strict discipline followed: TransformFunctions.cs body NOT read in Task 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass 2 — edit TransformFunctions.cs Augment/Diminish + add testing wrappers (or alternate test scaffolding) + atomic commit</name>
  <files>flow-lang/StandardLibrary/Transforms/TransformFunctions.cs, flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs</files>
  <read_first>
    - flow-lang/StandardLibrary/Transforms/TransformFunctions.cs (FULL — confirm exact line numbers of AUDIT-VERIFIED markers + Augment/Diminish bodies; lines 239 + 261 per CONTEXT)
    - flow-lang/Runtime/Value.cs (search for `As<T>` and `Value.Sequence(...)` factory)
    - flow-lang.Tests/AssemblyInfo.cs OR flow-lang/AssemblyInfo.cs (search for `InternalsVisibleTo("flow-lang.Tests")` — if present, internal test wrappers are accessible)
    - flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs (POST Task 1)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-RESEARCH.md §"Code Examples" Example 5 (Augment branch shape)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-CONTEXT.md §"Decisions" D-15 (two-pass strict)
  </read_first>
  <behavior>
    Pass 2 outcome: After landing TransformFunctions.cs edit + (optionally) testing wrappers, run all 5 Facts. Tests 1, 2 (rational doubling/halving) GREEN. Tests 3, 4 (non-tuplet path) GREEN. Test 5 (AUDIT-VERIFIED grep) GREEN.

    If any Fact fails: log under §Divergences in 19-05-SUMMARY.md with the actual produced Fraction values + root cause. Per Phase 13/14 precedent, Outcome A (no divergence) is the typical case for foundation-tier Fact-pinned acceptance.
  </behavior>
  <action>
**Step 1 — Read TransformFunctions.cs lines 230-280 to confirm exact line numbers of AUDIT-VERIFIED markers + Augment/Diminish bodies.**

The targets are:
- Line 239: AUDIT-VERIFIED comment for Augment
- Lines 240-259: Augment body (exact line range to confirm)
- Line 261: AUDIT-VERIFIED comment for Diminish
- Lines 262-281: Diminish body

Adjust line numbers in the edits below if the file has shifted.

**Step 2 — Edit Augment body to add DurationFraction branch + refresh AUDIT-VERIFIED comment.**

Replace the existing block at lines 239-260 with:

```csharp
    // AUDIT-VERIFIED 2026-04-18: C5 — augment correct (lengthens); observed A=#### vs Q=## columns in visualize (tests/spike/c5-augment-diminish.flow)
    // AUDIT-VERIFIED 2026-04-NN: re-validated against tuplet sequences (Phase 19 TUP-07)
    //   — see flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs (rational doubling pinned via Fraction(2, 1) × f)
    private static Value Augment(IReadOnlyList<Value> args)
    {
        var seq = args[0].As<SequenceData>();

        var result = TransformNotes(seq, note =>
        {
            // TUP-07: rational branch — when DurationFraction is set, double it via Phase 18 Fraction(2,1) × f.
            // Existing enum path runs verbatim when DurationFraction is null (Phase 18 byte-identical contract).
            if (note.DurationFraction.HasValue)
            {
                var doubled = note.DurationFraction.Value * new Fraction(2, 1);
                return new MusicalNoteData(
                    note.NoteName, note.Octave, note.Alteration,
                    note.DurationValue, note.IsRest, note.CentOffset, note.IsTied,
                    note.Velocity, note.Articulation, note.IsDotted,
                    note.SourceLocation, note.SourceLength,
                    durationFraction: doubled);
            }

            if (!note.DurationValue.HasValue) return note;

            int newDur = note.DurationValue.Value - 1; // toward WHOLE=0
            if (newDur < (int)NoteValueType.Value.WHOLE)
            {
                Console.Error.WriteLine("Warning: augment clamped duration at whole note");
                newDur = (int)NoteValueType.Value.WHOLE;
            }

            return new MusicalNoteData(note.NoteName, note.Octave, note.Alteration, newDur, note.IsRest, note.CentOffset, note.IsTied, note.Velocity, note.Articulation, note.IsDotted, note.SourceLocation, note.SourceLength);
        });

        return Value.Sequence(result);
    }
```

**Step 3 — Edit Diminish body to add DurationFraction branch + refresh AUDIT-VERIFIED comment.**

Replace the existing block at lines 261-282 with:

```csharp
    // AUDIT-VERIFIED 2026-04-18: C5 — diminish correct (shortens); observed D=# vs Q=## columns in visualize (tests/spike/c5-augment-diminish.flow)
    // AUDIT-VERIFIED 2026-04-NN: re-validated against tuplet sequences (Phase 19 TUP-07)
    //   — see flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs (rational halving pinned via Fraction(1, 2) × f)
    private static Value Diminish(IReadOnlyList<Value> args)
    {
        var seq = args[0].As<SequenceData>();

        var result = TransformNotes(seq, note =>
        {
            // TUP-07: rational branch — when DurationFraction is set, halve it via Phase 18 Fraction(1,2) × f.
            // Existing enum path runs verbatim when DurationFraction is null (Phase 18 byte-identical contract).
            if (note.DurationFraction.HasValue)
            {
                var halved = note.DurationFraction.Value * new Fraction(1, 2);
                return new MusicalNoteData(
                    note.NoteName, note.Octave, note.Alteration,
                    note.DurationValue, note.IsRest, note.CentOffset, note.IsTied,
                    note.Velocity, note.Articulation, note.IsDotted,
                    note.SourceLocation, note.SourceLength,
                    durationFraction: halved);
            }

            if (!note.DurationValue.HasValue) return note;

            int newDur = note.DurationValue.Value + 1; // toward THIRTYSECOND=5
            if (newDur > (int)NoteValueType.Value.THIRTYSECOND)
            {
                Console.Error.WriteLine("Warning: diminish clamped duration at thirty-second note");
                newDur = (int)NoteValueType.Value.THIRTYSECOND;
            }

            return new MusicalNoteData(note.NoteName, note.Octave, note.Alteration, newDur, note.IsRest, note.CentOffset, note.IsTied, note.Velocity, note.Articulation, note.IsDotted, note.SourceLocation, note.SourceLength);
        });

        return Value.Sequence(result);
    }
```

**Add `using FlowLang.TypeSystem;` to TransformFunctions.cs imports** if not already present (required for `new Fraction(...)`).

**Step 4 — Add internal testing wrappers.**

Augment / Diminish are `private static` and take `IReadOnlyList<Value>`. Tests need direct SequenceData access. Two options:

**Option A: Add internal-visible static wrappers on TransformFunctions.**

At end of TransformFunctions class body (or alongside existing internal helpers), add:

```csharp
#if DEBUG_OR_TEST
    // OR: gate via [assembly: InternalsVisibleTo("flow-lang.Tests")]
    internal static SequenceData AugmentForTesting(SequenceData seq) =>
        Augment(new List<Value> { Value.Sequence(seq) }).As<SequenceData>();

    internal static SequenceData DiminishForTesting(SequenceData seq) =>
        Diminish(new List<Value> { Value.Sequence(seq) }).As<SequenceData>();
#endif
```

Drop the `#if DEBUG_OR_TEST` (no need to gate — internal access alone is sufficient). Use the `internal` keyword.

**Option B: Use the existing FunctionRegistry call surface from tests.**

If `InternalsVisibleTo("flow-lang.Tests")` is NOT present in the assembly, add it OR use Reflection in the test. The cleanest option: add `[assembly: InternalsVisibleTo("flow-lang.Tests")]` to flow-lang/AssemblyInfo.cs (or in a Properties/AssemblyInfo.cs file; verify path at task time).

**Verify at task time:** `grep -rn "InternalsVisibleTo" flow-lang/`. If present and includes "flow-lang.Tests", just add the wrappers as `internal`. If not, add the assembly attribute first.

**Step 5 — Run Pass 2.**

```bash
dotnet build flow-sharp.sln 2>&1 | tail -10
# MUST report 0 errors.

dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase19.TupletAugmentDiminishTests" --no-build 2>&1 | tail -10
# Pass 2 outcome: ALL 5 Facts GREEN.
# If any RED, log under §Divergences in 19-05-SUMMARY.md (Phase 13 D-13 / 14 D-13 protocol).

dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase19" --no-build 2>&1 | tail -10
# Cumulative Phase 19: 8 + 9 + 6 + 6 + 5 = 34 passed.

dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase18" --no-build 2>&1 | tail -10
# 19 passed (Phase 18 byte-identical Facts unchanged).

dotnet test flow-sharp.sln --no-build 2>&1 | tail -5
# Full suite: pre-Phase-19 baseline (306) + 34 = 340/340.
```

**Step 6 — First atomic commit (TUP-07 production code + Facts + AUDIT-VERIFIED refresh).**

```bash
git add flow-lang/StandardLibrary/Transforms/TransformFunctions.cs \
        flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs

# Stage AssemblyInfo.cs ONLY if it was edited to add InternalsVisibleTo:
git status --short  # confirm what's staged

git commit -m "$(cat <<'EOF'
feat(19-05): TUP-07 augment/diminish tuplet-aware + AUDIT-VERIFIED refresh

Augment + Diminish gain DurationFraction.HasValue branch performing
rational doubling (Fraction(2, 1) × f) and halving (Fraction(1, 2) × f)
via Phase 18 primitive. Existing power-of-2 enum path runs VERBATIM
when DurationFraction is null — Phase 11/12 C5 audit remains valid for
non-tuplet sequences.

AUDIT-VERIFIED comment markers at lines 239 + 261 refreshed to include
"2026-04-NN: re-validated against tuplet sequences (Phase 19 TUP-07)"
preserving the 2026-04-18 Phase-11 audit trail above.

5 Facts under flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs
pin TUP-07 acceptance via two-pass strict authorship (CONTEXT D-15):
  Augment_RationalDouble — [1/3 q × 3] → [2/3 q × 3]
  Diminish_RationalHalve — [1/3 q × 3] → [1/6 q × 3]
  Augment_NonTupletPath_StaysOnEnumPath — QUARTER → HALF, DurationFraction null
  Diminish_NonTupletPath_StaysOnEnumPath — QUARTER → EIGHTH, DurationFraction null
  AuditVerifiedComment_Phase19TUP07_PresentAtBothSites — grep regression

Pass 2 outcome: <Outcome A — REQUIREMENTS-as-drafted matches reality>
OR <Outcome B — divergence logged under §Divergences in 19-05-SUMMARY.md>
(executor pins the actual outcome at commit time)

Phase 18 ByteIdentical{Tutorial,Showcase}Tests still GREEN.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    - `grep -c "if (note.DurationFraction.HasValue)" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns at least 2 (one in Augment, one in Diminish)
    - `grep -c "new Fraction(2, 1)" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns at least 1 (Augment doubling)
    - `grep -c "new Fraction(1, 2)" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns at least 1 (Diminish halving)
    - `grep -c "Phase 19 TUP-07" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns at least 2 (one each at Augment + Diminish AUDIT-VERIFIED comments)
    - `grep -c "AUDIT-VERIFIED 2026-04-18" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns at least 2 (the original Phase 11 audit trail preserved alongside the Phase 19 refresh)
    - `grep -c "using FlowLang.TypeSystem" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns at least 1 (Fraction import)
    - `grep -c "internal static SequenceData AugmentForTesting" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns 1 (test wrapper)
    - `grep -c "internal static SequenceData DiminishForTesting" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs` returns 1
    - `grep -rn "InternalsVisibleTo.*flow-lang.Tests" flow-lang/` returns at least 1 hit
    - Build: `dotnet build flow-sharp.sln 2>&1 | grep -c "error CS"` returns 0
    - All 5 TupletAugmentDiminishTests Facts pass: `dotnet test --filter "FullyQualifiedName~Phase19.TupletAugmentDiminishTests" --no-build 2>&1 | grep -E "Failed:\\s*0" | wc -l` returns at least 1
    - Phase 18 byte-identical Facts still GREEN: `dotnet test --filter "FullyQualifiedName~Phase18" --no-build 2>&1 | grep -E "Failed:\\s*0" | wc -l` returns at least 1
    - Cumulative Phase 19 = 34 Facts: `dotnet test --filter "FullyQualifiedName~Phase19" --no-build 2>&1 | grep -E "Passed:\\s*34|Failed:\\s*0" | wc -l` returns at least 1
    - Full suite still green: `dotnet test flow-sharp.sln --no-build 2>&1 | grep -E "Failed:\\s*0" | wc -l` returns at least 1
    - First atomic commit `feat(19-05): TUP-07 augment/diminish ...` lands
  </acceptance_criteria>
  <verify>
    <automated>dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase19.TupletAugmentDiminishTests"</automated>
  </verify>
  <done>TransformFunctions.cs Augment + Diminish branch on DurationFraction.HasValue performing rational doubling/halving via Phase 18 Fraction primitive. AUDIT-VERIFIED comments refreshed at both line 239 and line 261. Existing power-of-2 enum path preserved. 5 Pass 2 Facts GREEN. First atomic commit `feat(19-05)` lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase 19 closure docs (19-VERIFICATION.md + REQUIREMENTS.md + ROADMAP.md + STATE.md) + second atomic commit</name>
  <files>.planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/14-composer-dx-part-1/14-VERIFICATION.md (FULL — structural template)
    - .planning/phases/15-composer-dx-part-2/15-VERIFICATION.md (FULL — closure pattern + commit hash manifest format)
    - .planning/phases/18-foundation-rational-duration-arithmetic/18-VERIFICATION.md (FULL — most recent precedent; same milestone v1.3)
    - .planning/REQUIREMENTS.md (full traceability table — TUP-01..08 rows to flip)
    - .planning/ROADMAP.md §"Phase 19: Tuplets & Arbitrary Fractional Durations" (full entry to update)
    - .planning/STATE.md (full — current focus + progress block + Recent Decisions section)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-01-SUMMARY.md (commit hash for 19-01)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-02-SUMMARY.md (commit hash for 19-02)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-03-SUMMARY.md (commit hash for 19-03)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-04-SUMMARY.md (commit hash for 19-04)
    - .planning/phases/19-tuplets-arbitrary-fractional-durations/19-01-PLAN.md §"Pre-landing Collision Grep Transcript" (recorded transcript to surface in 19-VERIFICATION.md)
  </read_first>
  <behavior>
    No code Facts in Task 3 — this is the docs-only closure commit. Acceptance is structural via greps over the four edited files.
  </behavior>
  <action>
**Step 1 — Capture commit hashes.**

```bash
git log --oneline -10 | grep -E "feat\\(19-0[1-5]\\)"
```

Record the 5 short SHAs (one each for Plans 19-01..05). The Task 2 commit just landed is the 19-05 hash.

**Step 2 — Author `.planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md`.**

Use the 14-VERIFICATION.md / 15-VERIFICATION.md / 18-VERIFICATION.md structure. Adapt for Phase 19's 8 TUP-XX requirements + 5 plans.

```markdown
# Phase 19 Verification

**Phase:** 19 — Tuplets & Arbitrary Fractional Durations
**Milestone:** v1.3 Composer DX Tier B/C
**Closed:** 2026-04-NN
**Plans:** 5/5 complete (Wave 1: 19-01 → Wave 2: 19-02 → Wave 3: 19-03 ‖ 19-04 → Wave 4: 19-05)
**Cumulative Facts:** 34 Phase19 (8 + 9 + 6 + 6 + 5)
**Full suite at close:** {pre-Phase-19 baseline (306) + 34 = 340/340}

## Criteria → Artifact Mapping

| REQ | SPEC acceptance | Artifact | Plan | Commit |
|-----|----------------|----------|------|--------|
| TUP-01 | `\| {3:2 C4 D4 E4}q \|` → 3 notes DurationFraction=1/3 quarter | `flow-lang.Tests/Unit/Phase19/TupletBracketTests.cs::TripletQuarterGroup_ProducesThreeOneTwelfthNotes` | 19-01 | <hash-19-01> |
| TUP-02 | `{3 ...}q ≡ {3:2 ...}q` + `{12 ...}` parse error | `TupletBracketTests::ShorthandThree_EquivalentToThreeTwo` + `ShorthandTwelve_RaisesParseError` + `ShorthandFive_LookupTableLocked` + `ShorthandSeven_LookupTableLocked` | 19-01 | <hash-19-01> |
| TUP-03 | Nested `{3:2 C4 {3:2 D4 E4 F4}q G4}h` → 5 notes [2/3, 2/9, 2/9, 2/9, 2/3] q | `TupletBracketTests::NestedTriplet_OuterAndInnerComposeViaScaleAccumulation` | 19-01 | <hash-19-01> |
| TUP-04 | `\| C4/12 D4/12 E4/12 \|` → 3 notes 1/3 q each + `/0` parse error + `/1` whole | `flow-lang.Tests/Unit/Phase19/FractionalDurationTests.cs::SlashTwelve_ProducesThreeOneTwelfthNotes` + `SlashOne_ProducesWholeNote` + `SlashZero_RaisesParseError` | 19-02 | <hash-19-02> |
| TUP-05 | 4/4 exact-fit clean + 5/4 overflow truncates + Info diagnostic + no-suffix parse error + underflow accepted | `flow-lang.Tests/Unit/Phase19/BarFitOverflowTests.cs::ExactFitFourFourBar_FourTupletsPlusTwoQuarters_NoOverflow` + `OverflowFiveFourths_TruncatesAtBoundary_EmitsInfo` + `OverflowMidElement_NonZeroRemaining_TruncatesBoundary` + `NonTupletBar_DoesNotInvokeValidator` + `TupletBracketWithoutSuffix_RaisesParseError_ValidatorNeverReached` + `SixEightBarWithOneTriplet_UnderfillAccepted` | 19-03 | <hash-19-03> |
| TUP-06 | `{3:2}` → 480 + `{5:4}` → 480 + `{7:8}` → 3360 + `{11:13}` cap error + zero-tuplet → 480 + per-note parity | `flow-lang.Tests/Unit/Phase19/MidiTpqnElevationTests.cs::Triplet_StaysAt480` + `Quintuplet_StaysAt480` + `Septuplet_ElevatesTo3360` + `LargeRatioCombination_RaisesCapError` + `ZeroTuplets_StaysAt480` + `PerNoteSeptuplet_ElevatesTo3360` | 19-04 | <hash-19-04> |
| TUP-07 | `augment([1/3 q ×3])` → `[2/3 q ×3]` + `diminish([1/3 q ×3])` → `[1/6 q ×3]` + AUDIT-VERIFIED comment refresh | `flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs::Augment_RationalDouble` + `Diminish_RationalHalve` + `Augment_NonTupletPath_StaysOnEnumPath` + `Diminish_NonTupletPath_StaysOnEnumPath` + `AuditVerifiedComment_Phase19TUP07_PresentAtBothSites` | 19-05 | <hash-19-05> |
| TUP-08 | `\| C4/3:2 D4/3:2 E4/3:2 \|` ≡ `\| {3:2 C4 D4 E4}q \|` + `C4/5:4h` 1/10 whole + mixed legal + `/0:2` parse error + per-note `/7:8` triggers TPQN-elevation | `FractionalDurationTests::PerNoteThreeAgainstTwo_EquivalentToBracket` + `PerNoteWithHalfSuffix_OneTenthWhole` + `MixedRatios_AdjacentNotesLegal` + `PerNoteZeroNumerator_RaisesParseError` + `RandomChoiceWeights_AndPerNoteTuplet_DoNotCollide` + `MidiTpqnElevationTests::PerNoteSeptuplet_ElevatesTo3360` | 19-02 + 19-04 | <hash-19-02> + <hash-19-04> |

## Commit Hash Manifest

| Plan | Commit | What landed |
|------|--------|-------------|
| 19-01 | <hash-19-01> | TupletElement AST + parser + compiler (TUP-01/02/03) + NoteElement.TupletRatio field |
| 19-02 | <hash-19-02> | Per-note `/N` (TUP-04) + `/X:Y[suffix]` (TUP-08) parser + compiler branch |
| 19-03 | <hash-19-03> | Bar-fit validator + charitable overflow + Info diagnostic (TUP-05) |
| 19-04 | <hash-19-04> | MIDI TPQN auto-elevation + 9600 cap error (TUP-06) |
| 19-05 | <hash-19-05> | Augment/Diminish tuplet-aware + AUDIT-VERIFIED refresh (TUP-07) |
| 19-05 docs | <hash-19-05-docs> | Phase 19 closure: VERIFICATION.md + REQUIREMENTS.md + ROADMAP.md + STATE.md |

## Pre-landing Collision Grep

Recorded inline in `.planning/phases/19-tuplets-arbitrary-fractional-durations/19-01-PLAN.md` §"Pre-landing Collision Grep Transcript". Grep #1 (`\| .*\{|\{[0-9]` over tests/, examples/, flow-lang/*.flow) returned empty as expected — `{` `}` were unused inside note streams pre-Phase-19. Per Phase 14 D-21 / Phase 15 closure precedent.

## Phase 18 Regression Gate Held

Phase 18 ByteIdentical{Tutorial,Showcase}Tests (4 Facts at flow-lang.Tests/Integration/Phase18/) remain GREEN end-to-end. examples/tutorial.flow + examples/showcase.flow contain zero tuplet syntax (verified at execute time via grep), so they route through:
- Plan 19-01: NoteStreamCompiler.CompileBar's TupletElement switch arm — unreached
- Plan 19-02: ParseNoteStream's NoteLiteral arm with no Slash — TupletRatio stays null
- Plan 19-03: ValidateBarFit's `Any(n => n.DurationFraction.HasValue)` guard — false → validator skipped
- Plan 19-04: MidiExport.ComputeRequiredTpqn's `denominators.Count == 0 → return 480` — TPQN unchanged
- Plan 19-05: Augment/Diminish's `if (note.DurationFraction.HasValue)` branch — false → enum path runs verbatim

End-to-end byte-identical contract structurally guaranteed across all 5 plans.

## Charitable Interpretation Memory Honoured

Per CLAUDE.md memory ("music > rigid correctness") + CONTEXT D-03 lock: bar overflow is silent-truncate + Info diagnostic, NOT hard error. Composer feedback flows via ErrorReporter.ReportInfo. Same input always produces same truncation (deterministic). Plan 19-03's BarFitOverflowTests::OverflowFiveFourths Fact pins the deterministic behaviour.

## Deferred / Out of Scope

Per CONTEXT.md §Deferred Ideas (already routed to other v1.3 phases or v1.4):
- LSP semantic-tokens for `{N:M ...}` syntax — flow-lsp graceful degradation (Phase 17 pattern); possible follow-up phase
- Tuplet-aware `humanize` / `humanizeGaussian` interaction — Phase 25 (DEFER-06)
- Tutorial demonstration of tuplets — Phase 26 (QOL-04)

## Status

**Phase 19 closed.** All 8 TUP-XX requirements Shipped. AUDIT-VERIFIED C5 re-validated against tuplet sequences. Phase 18 byte-identical contract preserved. v1.3 milestone advances 2/9 phases complete.

---
*Phase: 19-tuplets-arbitrary-fractional-durations*
*Closed: 2026-04-NN*
```

Replace `<hash-19-01>`, `<hash-19-02>`, `<hash-19-03>`, `<hash-19-04>`, `<hash-19-05>` with the actual short SHAs from Step 1. Use the actual completion date for `2026-04-NN`. Use the actual full-suite count from the Task 2 verification step.

**Step 3 — Update `.planning/REQUIREMENTS.md` traceability table.**

Locate the table under `## Traceability`. Update the 8 TUP rows from "Pending" to "Shipped <hash>":

```text
| TUP-01 | Phase 19 | Shipped <hash-19-01> |
| TUP-02 | Phase 19 | Shipped <hash-19-01> |
| TUP-03 | Phase 19 | Shipped <hash-19-01> |
| TUP-04 | Phase 19 | Shipped <hash-19-02> |
| TUP-05 | Phase 19 | Shipped <hash-19-03> |
| TUP-06 | Phase 19 | Shipped <hash-19-04> |
| TUP-07 | Phase 19 | Shipped <hash-19-05> |
| TUP-08 | Phase 19 | Shipped <hash-19-02> |
```

The active-requirements bullets above (`- [ ] **TUP-01**: ...` etc.) flip to `- [x] **TUP-01**: ...`.

**Step 4 — Update `.planning/ROADMAP.md` Phase 19 entry.**

Locate `### Phase 19: Tuplets & Arbitrary Fractional Durations` and update:

1. The phase row in the v1.3 in-progress section: `- [ ] **Phase 19: ...**` → `- [x] **Phase 19: ...** — Shipped 2026-04-NN (commits <hash-19-01> + <hash-19-02> + <hash-19-03> + <hash-19-04> + <hash-19-05>)`
2. `**Plans:** TBD` → `**Plans:** 5 plans (one per TUP-XX cluster)`
3. The 5-plan list with `[x]` checkboxes:

```text
**Plans:** 5 plans
- [x] 19-01-PLAN.md — TupletElement AST + parser + compiler (TUP-01/02/03) — Shipped <hash-19-01>
- [x] 19-02-PLAN.md — Per-note `/N` + `/X:Y[suffix]` (TUP-04 + TUP-08) — Shipped <hash-19-02>
- [x] 19-03-PLAN.md — Bar-fit validator + charitable overflow (TUP-05) — Shipped <hash-19-03>
- [x] 19-04-PLAN.md — MIDI TPQN auto-elevation (TUP-06) — Shipped <hash-19-04>
- [x] 19-05-PLAN.md — TUP-07 augment/diminish + Phase 19 closure — Shipped <hash-19-05>
```

4. Update the `## Progress` table: `| 19. Tuplets & Arbitrary Fractional Durations | v1.3 | 5/5 | Complete | 2026-04-NN |`

**Step 5 — Update `.planning/STATE.md`.**

1. Update `stopped_at:` field in frontmatter to reflect Phase 19 closure (e.g. `Completed 19-05-PLAN.md (Phase 19 closes — TUP-07 audit re-validated; 34/34 Phase19 GREEN; full suite 340/340)`).
2. Update `last_updated:` to current ISO date.
3. Update `progress` block: completed_phases+1 (now 2/9 of v1.3), completed_plans+5 (now 7), recompute `percent`.
4. Under `## Current Position`, update phase + plan references.
5. Append a brief entry to `## Accumulated Context > Decisions` summarising Plan 19-05's outcome (Pass 2 Outcome A or Outcome B).
6. Under `## Recent Trend`, append a row for Phase 19 in the per-plan table if the convention is followed.

The exact edits are mechanical — match the format of the existing Phase 18 close entry.

**Step 6 — Build + run final regression check.**

```bash
dotnet test flow-sharp.sln --no-build 2>&1 | tail -5
# MUST report 340/340 (or whatever the cumulative count is at execute time).
```

**Step 7 — Second atomic commit (closure docs).**

```bash
git add .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md \
        .planning/REQUIREMENTS.md \
        .planning/ROADMAP.md \
        .planning/STATE.md

git commit -m "$(cat <<'EOF'
docs(19-05): close Phase 19 — verification rollup + traceability

19-VERIFICATION.md rollup with TUP-XX criteria-to-artifact mapping
covering all 8 TUP requirements + commit hash manifest for the 5
production-code commits + Phase 18 byte-identical regression-gate
confirmation + charitable-interpretation memory + deferred-ideas
restatement + Phase 19 closure status.

REQUIREMENTS.md TUP-01..08 traceability rows flipped from Pending to
Shipped <hash> using the 5 atomic commits feat(19-0X).

ROADMAP.md Phase 19 row + plan list updated to 5/5 Complete with
commit-hash references.

STATE.md current focus advanced past Phase 19; progress block
recomputed (completed_phases+1, completed_plans+5).

Phase 19 closes. v1.3 milestone advances 2/9 phases complete.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    - File exists: `.planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md`
    - `grep -c "Phase 19 Verification" .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md` returns at least 1
    - `grep -cE "TUP-0[1-8]" .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md` returns at least 8 (one row per TUP-XX in the criteria-to-artifact map)
    - `grep -c "Pre-landing Collision Grep" .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md` returns at least 1 (transcript surfacing reference)
    - `grep -c "Phase 18 Regression Gate Held" .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md` returns 1
    - REQUIREMENTS.md updated: `grep -cE "TUP-0[1-8] \\| Phase 19 \\| Shipped" .planning/REQUIREMENTS.md` returns 8
    - ROADMAP.md Phase 19 plan list checkboxes flipped: `grep -cE "\\[x\\] 19-0[1-5]-PLAN.md" .planning/ROADMAP.md` returns 5
    - ROADMAP.md Progress table row: `grep -c "Tuplets & Arbitrary Fractional Durations | v1.3 | 5/5 | Complete" .planning/ROADMAP.md` returns 1
    - STATE.md `last_updated:` field within last 24 hours of execute time (manual check)
    - STATE.md `stopped_at:` references Phase 19 closure
    - Build remains 0 errors: `dotnet build flow-sharp.sln 2>&1 | grep -c "error CS"` returns 0
    - Full suite still green: `dotnet test flow-sharp.sln --no-build 2>&1 | grep -E "Failed:\\s*0" | wc -l` returns at least 1
    - Second atomic commit `docs(19-05): close Phase 19 ...` lands the 4 docs files
  </acceptance_criteria>
  <verify>
    <automated>grep -c "TUP-0" .planning/phases/19-tuplets-arbitrary-fractional-durations/19-VERIFICATION.md</automated>
  </verify>
  <done>19-VERIFICATION.md ships with criteria-to-artifact map for all 8 TUP-XX + commit hash manifest + collision grep transcript reference + Phase 18 regression gate confirmation. REQUIREMENTS.md / ROADMAP.md / STATE.md flipped to Phase 19 closure with the 5 atomic commit hashes wired through. Second atomic commit `docs(19-05)` lands. Phase 19 closes; v1.3 milestone advances 2/9 phases complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sequence (in-memory MusicalNoteData list) → augment/diminish | User-derived musical content; transformation is pure C# arithmetic on Phase 18 Fraction primitive. No serialization, no network, no persistence outside the test session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-17 | Tampering (silent invalidation of AUDIT-VERIFIED C5 marker) | TransformFunctions.Augment / Diminish | mitigate | Test 5 AuditVerifiedComment_Phase19TUP07_PresentAtBothSites pins the comment refresh via grep regression. If a future refactor removes "Phase 19 TUP-07" markers without re-validating, the Fact fails RED. |
| T-19-18 | Tampering (Phase 18 byte-identical contract regression via augment/diminish enum-path drift) | TransformFunctions.Augment / Diminish | mitigate | Tests 3 + 4 (Augment/Diminish_NonTupletPath_StaysOnEnumPath) pin the existing enum-path behaviour for non-tuplet sequences. Plus Phase 18 ByteIdentical{Tutorial,Showcase}Tests run as final gate. |
| T-19-19 | Tampering (Fraction overflow during augment of large-denominator sequences) | TransformFunctions.Augment | accept | Augment doubles the numerator: a denominator-N input becomes denominator-N output (Fraction(2,1)×Fraction(1,N) = Fraction(2,N) which normalises). No multiplicative explosion. Bounded by int.MaxValue with 9-orders-of-magnitude headroom for reasonable musical durations. |
| T-19-20 | Information disclosure (test wrappers expose internal API) | TransformFunctions.AugmentForTesting / DiminishForTesting | accept | `internal` visibility limited to flow-lang.Tests via assembly-level InternalsVisibleTo attribute. Not exposed to consumers. |
</threat_model>

<verification>
1. **Pre-condition gate** — `git log --oneline | grep -E "feat\(19-0[1234]\):"` returns 4 commits before Plan 19-05 Task 1 begins.
2. **Two-pass strict authorship** — Pass 1 file authored from REQUIREMENTS / SPEC alone (Task 1); Pass 2 lands GREEN code (Task 2). Outcome (A or B) logged in 19-05-SUMMARY.md.
3. **TUP-07 acceptance Facts** — 5 Facts: rational double + rational halve + non-tuplet enum path × 2 + AUDIT-VERIFIED grep regression.
4. **Phase 18 byte-identical regression gate** — 19 Phase18 Facts pass + ByteIdentical{Tutorial,Showcase}Tests pass.
5. **Cumulative Phase 19 gate** — 8 + 9 + 6 + 6 + 5 = 34 Phase19 Facts.
6. **Full-suite gate** — pre-Phase-19 baseline (306) + 34 = 340/340 passed.
7. **Closure artifacts gate** — 19-VERIFICATION.md + REQUIREMENTS.md + ROADMAP.md + STATE.md updated with the 5 commit hashes wired through.
8. **Atomic commit gate (×2)** — `feat(19-05): TUP-07 ...` + `docs(19-05): close Phase 19 ...`.
</verification>

<success_criteria>
- [ ] Plans 19-01..04 atomic commits exist in HEAD's ancestor history (verified pre-Task-1)
- [ ] Pass 1 file `flow-lang.Tests/Unit/Phase19/TupletAugmentDiminishTests.cs` authored from REQUIREMENTS/SPEC alone in Task 1
- [ ] Pass 2 production-code edit (Augment + Diminish branch on DurationFraction.HasValue) lands in Task 2 with AUDIT-VERIFIED comment refresh at lines 239 + 261
- [ ] Test wrappers `AugmentForTesting` / `DiminishForTesting` (internal static) added; InternalsVisibleTo present
- [ ] All 5 TupletAugmentDiminishTests Facts pass (Pass 2 Outcome A logged in 19-05-SUMMARY.md, OR Outcome B with §Divergences entry)
- [ ] Cumulative Phase 19 = 34 Facts; full suite at pre-Phase-19 baseline + 34 = 340/340 passed
- [ ] Phase 18 byte-identical Facts remain GREEN end-to-end
- [ ] First atomic commit `feat(19-05): TUP-07 augment/diminish ...` lands the production code + Facts
- [ ] 19-VERIFICATION.md ships with TUP-XX criteria-to-artifact map (all 8) + commit hash manifest + collision grep transcript reference + Phase 18 regression-gate confirmation
- [ ] REQUIREMENTS.md TUP-01..08 rows flipped to Shipped <hash>
- [ ] ROADMAP.md Phase 19 row + plan list updated to 5/5 Complete with commit hashes
- [ ] STATE.md current focus + progress block + Recent Decisions section updated
- [ ] Second atomic commit `docs(19-05): close Phase 19 ...` lands the 4 docs files
- [ ] Phase 19 closes; v1.3 milestone advances 2/9 phases complete
</success_criteria>

<output>
After completion, create `.planning/phases/19-tuplets-arbitrary-fractional-durations/19-05-SUMMARY.md` documenting:
- Pass 2 Outcome (A or B). For Outcome A: confirm REQUIREMENTS-as-drafted matched reality verbatim. For Outcome B: log under §Divergences with the actual produced Fraction values + root cause (consistent with Phase 13 D-13 / Phase 14 D-13 protocol).
- TransformFunctions.cs Augment + Diminish edit shape (3 branches: rational double/halve, enum path preserved, AUDIT-VERIFIED comment refresh)
- 5 Facts shipped + acceptance values pinned (Fraction value-equality + grep regression)
- Test wrapper additions (AugmentForTesting / DiminishForTesting + IVT confirmation)
- 19-VERIFICATION.md rollup highlights (criteria-to-artifact map covers all 8 TUP-XX; commit hash manifest pinned 5 production commits + 1 docs commit)
- REQUIREMENTS.md / ROADMAP.md / STATE.md update notes
- Phase 19 closure metadata (full suite count, cumulative Phase 19 Fact count, milestone progress)
- Two atomic commits (`feat(19-05)` + `docs(19-05)`) hashes for the project's commit-trail audit
- v1.3 milestone outlook — Phase 20 (range / multi-letter enharmonics / negative slice) is now the next ROADMAP target; Phase 19's TupletElement/Fraction infrastructure remains available for Phase 22's delay-sync + Phase 23's microtonal phases
</output>
