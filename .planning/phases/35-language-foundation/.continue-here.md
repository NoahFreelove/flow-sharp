---
context: phase
phase: 35-language-foundation
milestone: v1.5 Stage, Studio, Web
plans_complete: 5
plans_remaining: 2
status: paused_mid_phase
last_updated: 2026-05-19T03:02:46.896Z
last_commit: 5c393dd
---

# Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Executor `git stash` usage in worktrees | Plans 35-01 and 35-05 both reported using `git stash` for exploratory checks during execution. The worktree contract explicitly prohibits this (`<destructive_git_prohibition>` in agents/gsd-executor.md) — `refs/stash` is shared across worktrees so stashes from parallel executors collide and leak. Both executors recovered without losing work, but the leak persists in `refs/stash` until `git gc`. | advisory | Future executors MUST use throwaway branches (`git checkout -b tmp/inspect && git add -A && git commit`) for any dirty-state inspection instead of `git stash`. Pre-execution prompt for 35-06 and 35-07 already includes this prohibition explicitly. |
| Orchestrator CWD drift into executor worktree | After Wave 2's executor agents returned, the persistent Bash shell's CWD had drifted from the primary checkout into `/home/noah/Desktop/projects/flow-sharp/.claude/worktrees/agent-a5e8383ed29ef022d`. Running `git diff/merge` from that CWD operated against the wrong branch. Caught by `pwd` + `git rev-parse --abbrev-ref HEAD` sanity-check before merge. | advisory | Run `cd /home/noah/Desktop/projects/flow-sharp && pwd && git rev-parse --abbrev-ref HEAD` as the first Bash call after every executor-agent return. Workflow's #3174 guard in execute-phase.md step 5.5 documents the same pattern. |

<current_state>
Phase 35 (v1.5 dependency root, "Language Foundation") is 5/7 plans complete and merged to `dev` at commit `5c393dd`. Build is GREEN, 59/59 Phase 35 xUnit tests passing, working tree clean. The orchestrator was about to dispatch Wave 4 (plan 35-06, music-aware extractors + matchExhaustive pragma policy) when user requested a pause.
</current_state>

<completed_work>

| Wave | Plan | Title | Status | Summary File |
|------|------|-------|--------|--------------|
| 1 | 35-01 | Span migration foundation (LANG-04 prereq) | ✅ Merged | `35-01-SUMMARY.md` |
| 1 | 35-02 | v1.4 housekeeping closeout (HK-01..04) | ✅ Merged | `35-02-SUMMARY.md` |
| 2 | 35-03 | Rust-style multi-line diagnostics renderer (LANG-04) | ✅ Merged | `35-03-SUMMARY.md` |
| 2 | 35-04 | Pure-Flow test framework + `flow test` CLI (TEST-01, TEST-02) | ✅ Merged | `35-04-SUMMARY.md` |
| 3 | 35-05 | Pattern matching foundation (LANG-01, naive linear scan per D-v1.5-11) | ✅ Merged | `35-05-SUMMARY.md` |

Wave gates passed after every merge: `dotnet build` GREEN, all Phase 35 xUnit tests GREEN, smoke-tested via the `flow test` CLI subcommand once it landed in Wave 2. Two-run determinism preserved on `examples/tutorial.flow` (SHA-256 `f2c3b2b3...` byte-identical) — Phase 18/25/27/28 determinism contract intact.
</completed_work>

<remaining_work>

| Wave | Plan | Title | REQs | Tasks | Depends On |
|------|------|-------|------|-------|------------|
| 4 | 35-06 | Music-aware extractors + matchExhaustive pragma policy | LANG-02 | 4 | 35-01, 35-03, 35-04, 35-05 |
| 5 | 35-07 | `-> as name` chain naming | LANG-03 | 4 | 35-01, 35-04, 35-05, 35-06 |

After 35-07 merges:
- Spawn `gsd-verifier` to produce `35-VERIFICATION.md`
- `gsd-sdk query phase.complete 35` — flips ROADMAP checkbox, advances STATE.md, runs verification debt scan
- Return `## PHASE COMPLETE` per the `--no-transition` contract (parent plan-phase chain ends; do NOT auto-advance to Phase 36)
</remaining_work>

<decisions_made>

- **D-v1.5-11** (added 2026-05-18 during plan-checker iteration 1): Pattern-matching backend ships as naive linear scan in Phase 35; Jacobs/Peterse decision-tree compile deferred to v1.6 per D-v1.5-01 pre-traction no-deprecation latitude. Recorded in `.planning/REQUIREMENTS.md` line 20 and `.planning/ROADMAP.md` D-v1.5-11 block. Plan 35-05 ships naive scan; future v1.6 backend swap is internal-only.
- **Research Open Questions 1-5 all RESOLVED inline in `35-RESEARCH.md` `## Open Questions (RESOLVED)` section.** Q1 → D-v1.5-11. Q2 → reset full 11-site set, document AudioPlaybackManager carve-out as Pitfall in CLAUDE.md. Q3 → HK-02 doc-only closure. Q4 → silent discard of `(test ...)` body implicit-return. Q5 → `as` right-associative with `->`, no parenthesized form.
- **VALIDATION.md frontmatter flipped** to `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved` after sign-off table populated.
- **Full 5-wave execution scope confirmed by user** at start of execute-phase orchestration. They picked "Execute all 5 waves now (Recommended for --auto)" over Wave-1-only or stop options.
- **35-07 `depends_on` corrected** during plan-checker iteration 1 from `["35-01", "35-04"]` → `["35-01", "35-04", "35-05", "35-06"]` to reflect that 35-07 also modifies `Parser.cs` + `ExpressionEvaluator.cs` (which 35-05 and 35-06 touch).
</decisions_made>

<blockers>
None. Build clean, tests green, dev clean.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable state (use this for any tooling)
2. `.planning/phases/35-language-foundation/35-05-SUMMARY.md` — most recent completed plan; confirms Pattern AST family + MatchExpression + ParseMatch + PatternMatcher naive linear scan + ConstructorPattern discriminator flags (`IsChordLiteral` / `IsRomanNumeral` / `IsArticulationSymbol`) pre-staged for Plan 35-06
3. `.planning/phases/35-language-foundation/35-06-PLAN.md` — the next plan to execute. Pay attention to Task 4's "Construction-site audit (MANDATORY)" paragraph added during plan-checker iteration 1: grep `new MatchExpression(` in Parser.cs after adding `CapturedPragmas` defaulted param
4. `.planning/phases/35-language-foundation/35-07-PLAN.md` — final plan. `as` is right-associative with `->`; no parenthesized form per resolved Open Question 5
5. `.planning/phases/35-language-foundation/35-RESEARCH.md` — especially `## Open Questions (RESOLVED)` section + §D pattern matching architecture
6. `.planning/REQUIREMENTS.md` lines 9-20 — locked v1.5 decisions D-v1.5-01 through D-v1.5-11

## Infrastructure State

- **Git**: branch `dev`, HEAD `5c393dd`, working tree clean, no uncommitted changes, not pushed to remote
- **Worktrees**: 3 leftover from PRIOR sessions still present and locked (`agent-a0c73a51bc3aab5bd`, `agent-a5032031026ab009e`, `agent-a8c01801e4f2f8e17`). All Phase 35 Wave 1-3 worktrees cleaned up. Resuming should ignore the 3 leftover worktrees.
- **Build**: `dotnet build` clean (0 errors, 15 pre-existing warnings)
- **Tests**: 59/59 Phase 35 xUnit GREEN; 26 pre-existing failures inherited from pre-Phase-35 dev tip (Phase 28 PerSynthArticulation FFT + Ragtime RMS — NOT caused by Phase 35; tracked as v1.5 backlog deferred items)
- **`flow test` CLI**: working end-to-end. Smoke commands:
  - `dotnet exec flow-cli/bin/Debug/net10.0/flow.dll test tests/test_test_framework.flow` → 6/6 PASS
  - `dotnet exec flow-cli/bin/Debug/net10.0/flow.dll test tests/test_pattern_matching.flow` → 6/6 PASS
- **No background processes** running
- **No human actions pending**

## Pre-Execution Critique Required

Not applicable — plans 35-06 and 35-07 are already plan-checker-approved (12/12 dimensions PASSED in iteration 2). No additional design critique needed before execution.

<context>
Phase 35 is the v1.5 dependency root — pattern matching, diagnostics, test framework, `-> as name` chain naming, plus v1.4 housekeeping. The internal sequencing (D-v1.5-10) is locked: spans first, test framework second, pattern matching third, `-> as name` fourth. Wave 1-3 honored this. Wave 4 (35-06) adds music-aware extractors (chord quality / roman numeral / articulation symbol patterns) plus the `enable matchExhaustive;` pragma that promotes the charitable WARN to an error. Wave 5 (35-07) is the `-> as name` parser extension — mostly a parse-time transform addition, smallest plan of the phase.

After Phase 35 closes, the rest of v1.5 unblocks: Phase 36 destructuring (uses pattern matching), Phase 39 articulation emit (uses #symbol patterns), Phase 40 MIDI dispatch (uses pattern matching). All gated on this phase shipping.

User-confirmed scope at session start: full 5-wave execution under `--auto`. Resume should re-honor that.
</context>

<next_action>
Resume with the orchestrator chain by running:

```
/gsd:execute-phase 35 --auto --no-transition
```

The orchestrator will:
1. Re-init via `gsd-sdk query init.execute-phase 35` and discover only 2 incomplete plans (35-06 + 35-07) — completed plans are skipped by the `has_summary: true` filter
2. Dispatch Wave 4 (35-06 alone) with worktree isolation
3. After 35-06 returns: merge → unlock+remove worktree → `dotnet build` + Phase 35 xUnit gate → ROADMAP plan-progress update + commit
4. Dispatch Wave 5 (35-07 alone) with worktree isolation
5. After 35-07 returns: same merge + gate cycle
6. Spawn `gsd-verifier` for `35-VERIFICATION.md`
7. On `passed`: `gsd-sdk query phase.complete 35` to close ROADMAP/STATE
8. Return `## PHASE COMPLETE` (the `--no-transition` flag stops the auto-chain there; do NOT proceed to Phase 36 discuss/plan)

If the user wants to skip a wave or stop after Wave 4 only, they can interrupt or invoke `/gsd:execute-phase 35 --wave 4` first.
</next_action>
