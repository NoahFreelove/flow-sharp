---
phase: 35-language-foundation
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - flow-lang/StandardLibrary/TestFramework/TestFunctions.cs
  - flow-lang/StandardLibrary/TestFramework/TestRunner.cs
  - flow-lang/StandardLibrary/TestFramework/AssertionHelpers.cs
  - flow-lang/StandardLibrary/TestFramework/AssertionException.cs
  - flow-lang/StandardLibrary/TestFramework/TestRecord.cs
  - flow-lang/StandardLibrary/TestFramework/RmsComparator.cs
  - flow-lang/StandardLibrary/BuiltInFunctions.cs
  - flow-lang/Core/FlowEngine.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang/test.flow
  - flow-cli/Commands/TestCommand.cs
  - flow-cli/Commands/CommandRegistry.cs
  - tests/test_test_framework.flow
  - flow-lang.Tests/Phase35/TestFrameworkBuiltinsTests.cs
  - flow-lang.Tests/Phase35/TestBodyDeferralTests.cs
  - flow-lang.Tests/Phase35/HermeticIsolationTests.cs
  - flow-lang.Tests/Phase35/AssertWithinDbTests.cs
  - flow-lang.Tests/Phase35/FlowTestCliTests.cs
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "TEST-01: composer writes `(test \"name\" body)` and the body defers to test-run time via LazyType wrap (Pitfall 10 — load-bearing per the `if` builtin precedent at BuiltInFunctions.cs:339)."
    - "TEST-01: 5 assertion primitives ship — (assert cond), (assertEq a b), (assertNotesMatch seqA seqB), (assertBytesEqual buf1 buf2), (assertWithinDb buf1 buf2 0.5dB)."
    - "TEST-01: `flow test [path]` CLI subcommand discovers test_*.flow by convention; runs every (test ...) block in each file; prints per-test PASS/FAIL + summary; exit code reflects pass/fail."
    - "TEST-02: hermetic isolation — FlowEngine.SnapshotState()/RestoreState() reset musical context stack, voice pool, PRNG state, ExecutionContext bindings, SymbolInternTable, Sfz statics, RenderingDiagnostics._emitted, SynthUtils.Rng, FixedRandSeed/FixedGen/Gen, FlowConfig.Active between (test ...) blocks per Pitfall 3 list."
    - "TEST-02: tests run sequentially in a single FlowEngine process (per-test subprocesses rejected as anti-pattern per the locked REQ-text decision)."
    - "AudioPlaybackManager NOT reset per Assumption A8 — tests must not call (play ...) live playback; documented as a pitfall in CLAUDE.md test-framework section as a follow-up doc edit."
    - "Meta-test discipline: flow-lang.Tests unit facts use xUnit directly; do NOT recursively `(test ...)` the tester in Flow (VALIDATION.md Wave 2b row 6)."
    - "RmsComparator extracted from RmsRegressionTests.AssertRmsWithinTolerance as a pure helper (no xUnit dep); (assertWithinDb ...) Flow builtin wraps it and throws AssertionException; existing RmsRegressionTests xUnit helper delegates to the new pure comparator."
  artifacts:
    - path: flow-lang/StandardLibrary/TestFramework/TestFunctions.cs
      provides: "RegisterTestFramework(registry, context) — registers (test), (assert), (assertEq), (assertNotesMatch), (assertBytesEqual), (assertWithinDb) builtins via InternalFunctionRegistry per BuiltInFunctions.cs:333-347 LazyType precedent"
      contains: "RegisterTestFramework"
    - path: flow-lang/StandardLibrary/TestFramework/TestRunner.cs
      provides: "Public Run(FlowEngine engine, string filePath) walks engine.Context.TestRegistry, snapshot/restore around each test.BodyThunk.Force()"
      contains: "TestRunner"
    - path: flow-lang/StandardLibrary/TestFramework/AssertionHelpers.cs
      provides: "C# helpers shared by all 5 assertion builtins (assertEq via Value.Equals, assertNotesMatch via SequenceData iteration, assertBytesEqual via byte[] SequenceEqual, assertWithinDb wrapping RmsComparator)"
      contains: "AssertionHelpers"
    - path: flow-lang/StandardLibrary/TestFramework/AssertionException.cs
      provides: "Exception type thrown by assertion failures; caught by TestRunner to convert to FAIL outcomes"
      contains: "AssertionException"
    - path: flow-lang/StandardLibrary/TestFramework/TestRecord.cs
      provides: "record TestRecord(string Name, Thunk BodyThunk, Span Span) — registered tests accumulate here on ExecutionContext.TestRegistry"
      contains: "TestRecord"
    - path: flow-lang/StandardLibrary/TestFramework/RmsComparator.cs
      provides: "Pure RMS comparison extracted from RmsRegressionTests — MaxWindowDeviationDb(AudioBuffer a, AudioBuffer b, double windowMs) : double"
      contains: "RmsComparator"
    - path: flow-cli/Commands/TestCommand.cs
      provides: "flow test [path] subcommand mirroring CheckCommand.cs shape; defaults to tests/ when no path arg given"
      contains: "TestCommand"
    - path: flow-lang/test.flow
      provides: "Minimal stdlib re-export module per Assumption A10 — exists so `use \"@test\"` works"
      min_lines: 3
    - path: tests/test_test_framework.flow
      provides: "Composer-facing meta-dogfooding test using the new (test ...) framework — exercises all 5 assertion primitives + body deferral"
      min_lines: 20
  key_links:
    - from: flow-lang/StandardLibrary/TestFramework/TestFunctions.cs
      to: flow-lang/Runtime/ExecutionContext.cs
      via: "(test ...) builtin appends TestRecord to context.TestRegistry"
      pattern: "TestRegistry|TestRecord"
    - from: flow-cli/Commands/TestCommand.cs
      to: flow-lang/StandardLibrary/TestFramework/TestRunner.cs
      via: "TestCommand instantiates FlowEngine, executes source, then invokes TestRunner.Run for each registered test"
      pattern: "TestRunner|Run"
    - from: flow-lang/Core/FlowEngine.cs
      to: flow-lang/Runtime/ExecutionContext.cs
      via: "FlowEngine.SnapshotState/RestoreState delegate to Context.SnapshotState/RestoreState"
      pattern: "SnapshotState|RestoreState"
    - from: flow-lang/StandardLibrary/TestFramework/RmsComparator.cs
      to: flow-lang.Tests/Helpers/RmsRegressionTests.cs
      via: "The existing xUnit helper at RmsRegressionTests.AssertRmsWithinTolerance continues to work but now delegates to RmsComparator.MaxWindowDeviationDb for the pure comparison"
      pattern: "RmsComparator|MaxWindowDeviationDb"
---

<objective>
Land the pure-Flow test framework (TEST-01 + TEST-02) — Wave 2b, parallel-safe with Plan 35-03 (Diagnostics). Composer writes `tests/test_foo.flow` containing `(test "name" body)` blocks plus 5 assertion primitives, runs `flow test [path]`, gets per-test PASS/FAIL output with hermetic isolation between tests in a single FlowEngine process. The (test ...) and assertion builtins are C# functions registered via the standard InternalFunctionRegistry; test bodies defer via LazyType wrap (Pitfall 10 — load-bearing); TestRunner orchestrates the snapshot/restore loop around each test body invocation; TestCommand mirrors CheckCommand's shape under flow-cli. The assertWithinDb primitive wraps a newly-extracted pure-C# RmsComparator that the existing xUnit RmsRegressionTests helper also delegates to (single source of truth — both call sites converge).

Purpose: TEST-01 (pure-Flow test framework + flow test CLI) + TEST-02 (hermetic isolation). Tests become first-class — existing 70+ scripts remain smoke tests verified by console output (CLAUDE.md), new tests opt into rich assertions + structured output + ExecutionContext state reset between cases.

Output: 6 TestFramework C# files + test.flow stdlib stub + TestCommand CLI subcommand + CommandRegistry registration + ExecutionContext snapshot/restore + meta-dogfooding tests/test_test_framework.flow + 5 xUnit fact classes covering builtins/deferral/isolation/dB-assertion/CLI integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@.planning/phases/35-language-foundation/35-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Builtin registration precedent — copy this shape for (test) + assertion builtins. -->

From flow-lang/StandardLibrary/BuiltInFunctions.cs:333-347:
- `eval` builtin: `var evalSignature = new FunctionSignature("eval", [new LazyType(VoidType.Instance)]); registry.Register("eval", evalSignature, StdLib.Eval);`
- `if` builtin: `var ifSignature = new FunctionSignature("if", [BoolType.Instance, new LazyType(VoidType.Instance), new LazyType(VoidType.Instance)]); registry.Register("if", ifSignature, StdLib.If);`
- The LazyType wrap is LOAD-BEARING — without it, the body argument evaluates eagerly at call-site and hermetic isolation is meaningless.

From flow-lang/Runtime/Thunk.cs:
- public Value Force() — evaluates the deferred body, memoizes the result.

From flow-lang/Runtime/ExecutionContext.cs:
- PushFrame() / PopFrame() at lines 171-191 (scope stack)
- SymbolInternTable at line 85 (Phase 26.1 — must reset)
- SectionRegistry — pattern reference for new TestRegistry property

From flow-lang.Tests/Helpers/RmsRegressionTests.cs:38-62:
- AssertRmsWithinTolerance(AudioBuffer rendered, string baselineWavPath, double windowMs = DefaultWindowMs, double toleranceDb = DefaultToleranceDb, ...)
- Currently couples to Xunit.Assert; the pure comparator extraction decouples the comparison from the assertion.

From flow-cli/Commands/CheckCommand.cs:
- public static Command Build() pattern with Argument<FileInfo> + Command + SetAction lambda — TestCommand mirrors this shape but takes Argument<string?> with ArgumentArity.ZeroOrOne (defaulting to "tests/").

From flow-cli/Commands/CommandRegistry.cs:
- public static Command[] BuildAllCommands() returns array of all subcommands — append TestCommand.Build() to this array.

From flow-lang/StandardLibrary/Audio/SynthUtils.cs:
- ResetNoiseRng() — static reset method for SynthUtils.Rng (called by hermetic isolation).

From flow-lang/Diagnostics/RenderingDiagnostics.cs:47-50:
- ResetForTesting() — static reset method for _emitted dedup set (called by hermetic isolation).

From CLAUDE.md "How to Run Tests" + flow-cli existing subcommand pattern:
- `flow test [path]` is a NEW subcommand alongside the existing 11 (run, eval, repl, watch, play, render, flow2midi, midi2flow, check, version, new — plus lsp = 12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing test stubs for TestFramework builtins + body deferral + hermetic isolation + assertWithinDb + flow-test CLI</name>
  <files>flow-lang.Tests/Phase35/TestFrameworkBuiltinsTests.cs, flow-lang.Tests/Phase35/TestBodyDeferralTests.cs, flow-lang.Tests/Phase35/HermeticIsolationTests.cs, flow-lang.Tests/Phase35/AssertWithinDbTests.cs, flow-lang.Tests/Phase35/FlowTestCliTests.cs</files>
  <read_first>
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 62-71 Wave 2b table; lines 118-131 Wave 0 list)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 441-460 Pitfall 3 state-mutable surface enumeration; lines 532-541 Pitfall 10 deferral; lines 632-752 Example 3 registration + run loop)
    - flow-lang.Tests/Phase35/ (existing folder from prior plans — confirm xUnit shape)
    - flow-lang/StandardLibrary/BuiltInFunctions.cs:333-347 (Lazy-wrapped builtin precedent)
    - flow-lang/Runtime/ExecutionContext.cs (full file — enumerate the state-mutable surface to test isolation against)
  </read_first>
  <behavior>
    - Test 1 (TestFrameworkBuiltinsTests.AllAssertBuiltinsRegistered): After FlowEngine init, `engine.Context.FunctionRegistry.HasFunction("test")`, `"assert"`, `"assertEq"`, `"assertNotesMatch"`, `"assertBytesEqual"`, `"assertWithinDb"` all return true.
    - Test 2 (TestFrameworkBuiltinsTests.AssertThrowsOnFalse): `engine.Execute("(assert false)")` raises AssertionException with message containing "assert failed".
    - Test 3 (TestBodyDeferralTests.TestBodyNotEvaluatedAtRegistration): Define a test whose body `(print "WOULD_RUN")` — execute the source, then BEFORE invoking TestRunner.Run, assert console captured no "WOULD_RUN" output (body deferred). Only after TestRunner.Run does "WOULD_RUN" appear.
    - Test 4 (HermeticIsolationTests.MusicalContextStackResetBetweenTests): Test A sets `tempo 200` inside its body (mutates MusicalContext stack). Test B reads the current tempo and asserts it's the default 120 (not the leaked 200). Run via TestRunner — assert both tests pass.
    - Test 5 (HermeticIsolationTests.PrngStateResetBetweenTests): Test A consumes a PRNG value via `(randomInt 0 100)`; test B consumes the FIRST PRNG value and asserts it equals what test A got first (proves PRNG was re-seeded between tests).
    - Test 6 (HermeticIsolationTests.SymbolInternTableResetBetweenTests): Test A interns `#foo`; test B interns `#bar` — assert each test sees a fresh SymbolInternTable.
    - Test 7 (HermeticIsolationTests.TestOrderIndependent): Randomize the order of 5 tests across 20 runs; assert pass/fail outcomes are identical regardless of order (Pitfall 3 warning sign).
    - Test 8 (AssertWithinDbTests.IdenticalBuffersPass): Two AudioBuffers with identical PCM data, `(assertWithinDb a b 0.5dB)` does not throw.
    - Test 9 (AssertWithinDbTests.DeviationExceedingTolerance Throws): Two buffers with > 0.5dB RMS deviation in any 100ms window — assertWithinDb throws AssertionException.
    - Test 10 (FlowTestCliTests.FlowTestRunsAllRegisteredTests): Spawn dotnet run --project flow-cli with `test` subcommand pointing at a fixture .flow file containing 2 (test ...) blocks; assert stdout contains both test names + a "Total: 2; Passed: 2; Failed: 0" summary line; exit code 0.
    - Test 11 (FlowTestCliTests.FailingTestExitsNonZero): Fixture file contains a failing assertion; assert exit code is 1 (or non-zero).
  </behavior>
  <action>
    Create the 5 xUnit fact files in `flow-lang.Tests/Phase35/`. Mirror the existing class shape used elsewhere in flow-lang.Tests/.

    For TestFrameworkBuiltinsTests: spin up FlowEngine and use its existing function-registry query API to assert each builtin name is registered. For AssertThrowsOnFalse, use `Assert.Throws<AssertionException>(() => engine.Execute(...))`.

    For TestBodyDeferralTests: redirect `Console.Out` to a `StringWriter` before executing the source and BEFORE invoking TestRunner; capture and assert content; then invoke TestRunner and re-assert.

    For HermeticIsolationTests: write the fixture sources as inline strings; use TestRunner.Run on the produced TestRegistry; assert state-mutable surfaces don't leak. For the TestOrderIndependent fact, shuffle the TestRegistry list 20 times and re-run, asserting identical pass/fail outcomes per run.

    For AssertWithinDbTests: construct two AudioBuffers programmatically (matching + non-matching cases). Use the new RmsComparator (will exist post-Task 2) to verify the C# layer; use FlowEngine.Execute("(assertWithinDb a b 0.5dB)") to verify the Flow surface.

    For FlowTestCliTests: shell out to `dotnet run --project flow-cli -- test <fixture>` via `System.Diagnostics.Process`; capture stdout + exit code; assert both. Fixture .flow files can be written to `Path.GetTempFileName()` per test.

    All tests MUST initially fail-to-compile (none of TestFramework / TestRunner / AssertionException / RmsComparator exist) OR fail-at-runtime. Task 2 + Task 3 make them GREEN.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; ls flow-lang.Tests/Phase35/TestFramework*.cs flow-lang.Tests/Phase35/TestBody*.cs flow-lang.Tests/Phase35/Hermetic*.cs flow-lang.Tests/Phase35/AssertWithinDb*.cs flow-lang.Tests/Phase35/FlowTestCli*.cs 2>&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source: 5 new test files exist in flow-lang.Tests/Phase35/ matching the listed class names
    - Source: `ls flow-lang.Tests/Phase35/TestFramework*.cs ...` returns at least 5 files
    - Behavior: dotnet build either fails on missing types (expected RED) OR builds with placeholder facts
    - Done: All Wave 0 stubs in place; expected-RED state confirmed
  </acceptance_criteria>
  <done>5 xUnit fact classes created covering all TEST-01 + TEST-02 behaviors; tests are RED pre-implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TestFramework C# core — TestRecord + AssertionException + RmsComparator + AssertionHelpers + TestFunctions registration + TestRunner orchestrator; extend ExecutionContext with TestRegistry + SnapshotState/RestoreState</name>
  <files>flow-lang/StandardLibrary/TestFramework/TestRecord.cs, flow-lang/StandardLibrary/TestFramework/AssertionException.cs, flow-lang/StandardLibrary/TestFramework/RmsComparator.cs, flow-lang/StandardLibrary/TestFramework/AssertionHelpers.cs, flow-lang/StandardLibrary/TestFramework/TestFunctions.cs, flow-lang/StandardLibrary/TestFramework/TestRunner.cs, flow-lang/StandardLibrary/BuiltInFunctions.cs, flow-lang/Core/FlowEngine.cs, flow-lang/Runtime/ExecutionContext.cs, flow-lang/test.flow</files>
  <read_first>
    - flow-lang/StandardLibrary/BuiltInFunctions.cs:333-347 (Lazy-wrapped builtin precedent — copy verbatim for `test` + assertions)
    - flow-lang/StandardLibrary/BuiltInFunctions.cs:371-379 (equals builtin Void-wildcard pattern — assertEq uses this)
    - flow-lang/Runtime/Thunk.cs (Thunk.Force() API for test body invocation)
    - flow-lang/Runtime/ExecutionContext.cs (existing properties — find SymbolInternTable, FixedRandSeed, FixedGen, Gen, SectionRegistry; identify Phase 33 Sfz statics)
    - flow-lang/Runtime/MusicalContext.cs (musical-context stack API for snapshot/restore)
    - flow-lang/Diagnostics/RenderingDiagnostics.cs:47-50 (ResetForTesting hook for hermetic isolation)
    - flow-lang/StandardLibrary/Audio/SynthUtils.cs (ResetNoiseRng for PRNG reset)
    - flow-lang/Core/FlowConfig.cs (Active singleton — Phase 30 + Phase 33; identify Pitfall 3 reset point)
    - flow-lang.Tests/Helpers/RmsRegressionTests.cs:25-62 (extract the comparison math into RmsComparator — pure helper, no xUnit dep)
    - flow-lang/std.flow (re-export pattern for test.flow stub)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 441-460 Pitfall 3 — full state-mutable surface enumeration; lines 532-541 Pitfall 10 deferral)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 405-583 Bucket 2b — file-by-file analog references)
  </read_first>
  <behavior>
    - Wave 0 facts pass: TestFrameworkBuiltinsTests.AllAssertBuiltinsRegistered, AssertThrowsOnFalse; TestBodyDeferralTests.TestBodyNotEvaluatedAtRegistration; HermeticIsolationTests (4 facts); AssertWithinDbTests (2 facts).
    - ExecutionContext.SnapshotState() returns a TestSnapshot record capturing musical-context stack, voice pool, PRNG state, ExecutionContext bindings, SymbolInternTable, Sfz statics (SfzInstruments, SfzPatchRegistry, SfzDiagnostics, ResolvedSfzRoot), RenderingDiagnostics._emitted, FlowEngine.CurrentSampleCache + CurrentSfzSampleCache, FixedRandSeed/FixedGen/Gen, FlowConfig.Active, SynthUtils.Rng — the 11+ items enumerated in Pitfall 3.
    - ExecutionContext.RestoreState(snap) reinstates each captured state.
    - TestRunner.Run wraps each test invocation in Snapshot/Force/Restore.
    - test.flow exists as a minimal `use "@test"` discoverability anchor.
  </behavior>
  <action>
    Create the TestFramework folder + 5 C# files per PATTERNS.md Bucket 2b §TestFramework section.

    `flow-lang/StandardLibrary/TestFramework/AssertionException.cs`: standard Exception subclass with single string-message constructor; namespace `FlowLang.StandardLibrary.TestFramework`.

    `flow-lang/StandardLibrary/TestFramework/TestRecord.cs`: `public record TestRecord(string Name, Thunk BodyThunk, Span Span);` per PATTERNS.md Bucket 2b §TestFunctions.cs implicit reference.

    `flow-lang/StandardLibrary/TestFramework/RmsComparator.cs`: lift the pure RMS-comparison math from `flow-lang.Tests/Helpers/RmsRegressionTests.cs:71+` (per PATTERNS.md Bucket 2b §RmsComparator.cs Notable Departures). Public static `MaxWindowDeviationDb(AudioBuffer a, AudioBuffer b, double windowMs) : double` returns the max-RMS-window deviation as dB. NO Xunit dependency. Then UPDATE `flow-lang.Tests/Helpers/RmsRegressionTests.cs` so that AssertRmsWithinTolerance delegates to RmsComparator.MaxWindowDeviationDb for the comparison and only the throw remains in the xUnit helper (single source of truth for both call sites). NOTE: RmsComparator lives in flow-lang (not flow-lang.Tests) so the runtime builtin can reference it — RESEARCH §Wave 0 Gaps initially suggested flow-lang.Tests/Helpers but PATTERNS.md Notable Departures flags that the runtime needs visibility, so the canonical location is `flow-lang/StandardLibrary/TestFramework/RmsComparator.cs`.

    `flow-lang/StandardLibrary/TestFramework/AssertionHelpers.cs`: C# static helpers shared by all 5 assertions. Provide internal methods like `AssertNotesEqualOrThrow(SequenceData a, SequenceData b)`, `AssertBytesEqualOrThrow(byte[] a, byte[] b)`, etc. Each throws AssertionException with a useful diagnostic message.

    `flow-lang/StandardLibrary/TestFramework/TestFunctions.cs`: per PATTERNS.md Bucket 2b §TestFunctions.cs + RESEARCH §Example 3. Public `RegisterTestFramework(InternalFunctionRegistry registry, Runtime.ExecutionContext context)` registers:
    - `(test "name" body)`: signature `[StringType.Instance, new LazyType(VoidType.Instance)]`; implementation appends a TestRecord(name, args[1].As<Thunk>(), span) to `context.TestRegistry`; returns Value.Void().
    - `(assert cond)`: signature `[BoolType.Instance]`; throws AssertionException if !args[0].As<bool>().
    - `(assertEq a b)`: signature `[VoidType.Instance, VoidType.Instance]` (Void wildcards per BuiltInFunctions.cs:371-379 equals precedent); throws on `!Value.Equals(args[0], args[1])`.
    - `(assertNotesMatch seqA seqB)`: signature `[SequenceType.Instance, SequenceType.Instance]`; iterates bars + notes; throws on any structural mismatch.
    - `(assertBytesEqual buf1 buf2)`: signature `[BufferType.Instance, BufferType.Instance]`; compares PCM byte arrays; throws on mismatch.
    - `(assertWithinDb buf1 buf2 toleranceDb)`: signature `[BufferType.Instance, BufferType.Instance, DecibelType.Instance]`; calls `RmsComparator.MaxWindowDeviationDb(a, b, windowMs: 100.0)` per SPEC-8 default; throws if deviation exceeds tolerance.

    `flow-lang/StandardLibrary/TestFramework/TestRunner.cs`: per PATTERNS.md Bucket 2b §TestRunner.cs + RESEARCH §Example 3. Public `Run(FlowEngine engine, string filePath)` returns `(int passed, int failed)`. For each `TestRecord` in `engine.Context.TestRegistry`: `engine.Context.SnapshotState()` → try `test.BodyThunk.Force()` → catch AssertionException (FAIL) → finally `engine.Context.RestoreState()`. PASS/FAIL logged to stdout via `Console.WriteLine` with TTY color per CLAUDE.md TTY precedent (Red for FAIL).

    Extend `flow-lang/Runtime/ExecutionContext.cs` per PATTERNS.md Bucket 2b §ExecutionContext.cs Apply section:
    - Add `public List<TestRecord> TestRegistry { get; } = new();` alongside the existing SectionRegistry.
    - Add `public TestSnapshot SnapshotState()` returning an immutable record capturing the 11+ items enumerated in RESEARCH §Pitfall 3 (call stack copy / MusicalContext stack snapshot / voice pool size / SymbolInternTable copy / SfzInstruments + SfzPatchRegistry + SfzDiagnostics + ResolvedSfzRoot snapshot / RenderingDiagnostics._emitted copy / FlowEngine.CurrentSampleCache + CurrentSfzSampleCache references / FixedRandSeed + FixedGen + Gen state / FlowConfig.Active snapshot / SynthUtils.Rng state).
    - Add `public void RestoreState(TestSnapshot snap)` that reinstates each captured value.
    - For static-mutable resets, call existing reset hooks: `RenderingDiagnostics.ResetForTesting()`, `SynthUtils.ResetNoiseRng()`. For sites without an existing reset hook (e.g., SfzInstruments dict), clear + repopulate from the snapshot. Use EXPLICIT calls per site (no reflection per RESEARCH Pitfall 3 Notable Departures).

    Per RESEARCH Assumption A8, DO NOT reset `AudioPlaybackManager` — tests must not call (play ...) live playback; we document this as a pitfall in CLAUDE.md as a follow-up (don't edit CLAUDE.md in this plan — record the follow-up in the SUMMARY).

    Extend `flow-lang/Core/FlowEngine.cs`: add pass-through `public TestSnapshot SnapshotState() => Context.SnapshotState();` + `public void RestoreState(TestSnapshot snap) => Context.RestoreState(snap);`. Also expose `public IReadOnlyList<TestRecord> TestRegistry => Context.TestRegistry;` for TestCommand consumption.

    Update `flow-lang/StandardLibrary/BuiltInFunctions.cs` RegisterAll orchestrator to call the new `TestFramework.TestFunctions.RegisterTestFramework(registry, context)`.

    Create `flow-lang/test.flow` per PATTERNS.md Bucket 2b §test.flow — minimal stub:
    ```
    // @test stdlib module
    // The (test), (assert), (assertEq), (assertNotesMatch), (assertBytesEqual),
    // (assertWithinDb) builtins are registered automatically by the engine —
    // this module exists so `use "@test"` works for discoverability.
    ```

    Do NOT touch flow-cli in this task — Task 3 wires the CLI surface.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.TestFrameworkBuiltinsTests|FullyQualifiedName~Phase35.TestBodyDeferralTests|FullyQualifiedName~Phase35.HermeticIsolationTests|FullyQualifiedName~Phase35.AssertWithinDbTests" 2>&amp;1 | tail -20 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: All 6 TestFramework C# files exist in flow-lang/StandardLibrary/TestFramework/
    - Source: flow-lang/test.flow exists with minimum 3 lines + comment block
    - Source: grep -c "TestRegistry" flow-lang/Runtime/ExecutionContext.cs returns at least 1
    - Source: grep -c "SnapshotState\|RestoreState" flow-lang/Runtime/ExecutionContext.cs returns at least 4 (method definitions + internal calls)
    - Source: grep -c "RegisterTestFramework" flow-lang/StandardLibrary/BuiltInFunctions.cs returns at least 1
    - Source: grep -c "RmsComparator" flow-lang.Tests/Helpers/RmsRegressionTests.cs returns at least 1 (helper delegates to new comparator)
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.TestFrameworkBuiltinsTests|FullyQualifiedName~Phase35.TestBodyDeferralTests|FullyQualifiedName~Phase35.HermeticIsolationTests|FullyQualifiedName~Phase35.AssertWithinDbTests" exits 0
    - Behavior: Full xUnit suite (dotnet test) exits 0 — Phase 18/25/27/28 byte-identical sentinels preserved (no regression from new ExecutionContext.SnapshotState code path)
    - Done: Pure-Flow test framework C# core landed; ExecutionContext snapshots/restores 11+ state surfaces; assertions throw AssertionException; tests deferred via Thunk; RmsComparator extracted as pure helper
  </acceptance_criteria>
  <done>TestFramework C# files + ExecutionContext extension + BuiltInFunctions registration + test.flow stub + RmsComparator extraction complete; 9 Wave 0 facts pass GREEN (excluding the 2 CLI facts which need Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: flow test [path] CLI subcommand + CommandRegistry registration + composer-facing meta-dogfooding test_test_framework.flow</name>
  <files>flow-cli/Commands/TestCommand.cs, flow-cli/Commands/CommandRegistry.cs, tests/test_test_framework.flow</files>
  <read_first>
    - flow-cli/Commands/CheckCommand.cs (full file — exact shape for TestCommand)
    - flow-cli/Commands/CommandRegistry.cs (full file — append TestCommand.Build() to BuildAllCommands array)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 694-752 Example 3 TestCommand body)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 468-523 Bucket 2b §TestCommand.cs + §CommandRegistry.cs)
    - flow-lang/Core/FlowEngine.cs (after Task 2 — TestRegistry property + SnapshotState/RestoreState pass-through)
    - tests/test_humanize_voice_block.flow (from Plan 35-02 — recent .flow test convention reference)
    - flow-lang.Tests/Phase35/FlowTestCliTests.cs (the Wave 0 stub from Task 1)
  </read_first>
  <behavior>
    - FlowTestCliTests facts GREEN: `flow test tests/test_test_framework.flow` runs all (test ...) blocks; PASS results print + summary line; exit 0 on all-pass, 1 on any-fail.
    - `flow test` (no arg) defaults to `tests/` directory.
    - Each PASS line is prefixed with file path + test name + PASS label; each FAIL line is colored red per TTY precedent + includes the AssertionException message.
    - Per-test snapshot/restore observable via inter-test state isolation (Hermetic facts from Task 2 already covered this).
    - test_test_framework.flow contains at least 5 (test ...) blocks exercising each assertion primitive (assert, assertEq, assertNotesMatch, assertBytesEqual, assertWithinDb); runs cleanly via `flow test tests/test_test_framework.flow`; print PASS to console on success per CLAUDE.md convention.
  </behavior>
  <action>
    Create `flow-cli/Commands/TestCommand.cs` per PATTERNS.md Bucket 2b §TestCommand.cs + RESEARCH §Example 3 (lines 694-752). Use `Argument<string?>` with `ArgumentArity.ZeroOrOne` so `flow test` defaults to `tests/`. Resolve `path` to file list via `Directory.Exists(path) ? Directory.GetFiles(path, "test_*.flow", SearchOption.TopDirectoryOnly) : new[] { path }`. For each file:
    1. `using var engine = new FlowEngine(verbose: false);`
    2. `engine.Execute(File.ReadAllText(file), file);` — registers (test) blocks via builtin
    3. For each registered TestRecord, snapshot state → invoke `TestRunner.Run` for one test → restore. Alternatively, hand the whole TestRegistry list to `TestRunner.Run(engine, file)` and let TestRunner do the snapshot/restore loop.
    4. Collect per-test PASS/FAIL outcomes; print with TTY-colored FAIL line.
    5. Print summary `Total: N; Passed: P; Failed: F`; return `failed == 0 ? 0 : 1`.

    Update `flow-cli/Commands/CommandRegistry.cs` per PATTERNS.md Bucket 2b §CommandRegistry.cs — append `TestCommand.Build(),` to the BuildAllCommands array (one-line addition). Update the inline subcommand-count comment if present (12 → 13).

    Create `tests/test_test_framework.flow` per PATTERNS.md Bucket 2b §tests/test_test_framework.flow — the composer-facing meta-dogfooding test. Use the new test framework idiom to author at least 5 tests:
    - `(test "assert with true does not throw" (assert true))`
    - `(test "assertEq integers" (assertEq 5 (add 2 3)))`
    - `(test "assertEq strings" (assertEq "hello" (concat "hel" "lo")))`
    - `(test "assertNotesMatch identical sequences" (assertNotesMatch | C4q D4q | | C4q D4q |))`
    - `(test "assertWithinDb identical buffers pass" (let buf (sine 440 1.0 0.5) (assertWithinDb buf buf 0.5dB)))` (adjust to actual buffer-creation API)

    Print "ALL TESTS REGISTERED" at the bottom so CLAUDE.md success-by-console-output convention covers smoke-run via `dotnet run --project flow-interpreter tests/test_test_framework.flow` (which would only register but not run the tests). The CLI integration test (FlowTestCliTests) confirms `flow test` actually runs them.

    Update the FlowTestCliTests Wave 0 stub (from Task 1) to use this real fixture instead of a temp file.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.FlowTestCliTests" 2>&amp;1 | tail -15 &amp;&amp; dotnet build flow-cli/flow-cli.csproj 2>&amp;1 | tail -5 &amp;&amp; dotnet run --project flow-cli -- test tests/test_test_framework.flow 2>&amp;1 | tail -20 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: flow-cli/Commands/TestCommand.cs exists with `public static Command Build()` returning a Command named "test"
    - Source: grep -c "TestCommand.Build" flow-cli/Commands/CommandRegistry.cs returns at least 1
    - Source: tests/test_test_framework.flow exists and contains at least 5 `(test "` blocks
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.FlowTestCliTests" exits 0 (FlowTestRunsAllRegisteredTests + FailingTestExitsNonZero GREEN)
    - Behavior: dotnet run --project flow-cli -- test tests/test_test_framework.flow exits 0; stdout contains "PASS" markers AND "Total:" summary line
    - Behavior: Full dotnet test exits 0 (no regression from CLI work)
    - Done: flow test [path] CLI subcommand operational; CommandRegistry exposes it; composer can author `(test ...)` blocks in .flow files and run them via the CLI
  </acceptance_criteria>
  <done>flow test [path] CLI subcommand lands; tests/test_test_framework.flow validates the full surface end-to-end via the CLI; FlowTestCliTests GREEN; full suite + .flow regression preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flow test [path] arg → file system | Composer-supplied path may attempt traversal (`flow test ../../etc/passwd`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10 | Information Disclosure | flow test path arg | mitigate | Mirror CheckCommand's FileInfo pattern; restrict directory-mode glob to `test_*.flow` only via `Directory.GetFiles(path, "test_*.flow", SearchOption.TopDirectoryOnly)` — no recursion + name-pattern filter limits exposure. File reads only; no writes. Per RESEARCH §Security Domain — single-user CLI threat model. |
| T-35-11 | Integrity | Hermetic isolation correctness | mitigate | Pitfall 3 11+ state surface enumerated as a checklist in Task 2; HermeticIsolationTests.TestOrderIndependent randomizes 20 runs and asserts identical outcomes; xUnit facts for individual state-leak categories (MusicalContext, PRNG, SymbolInternTable). |
| T-35-12 | Denial of Service | Long-running test bodies | accept | Single-user CLI; composer's own test code. No artificial timeout in v1.5; composer responsible for not authoring infinite loops. |
| T-35-13 | Tampering | LazyType wrap omission would defeat deferral | mitigate | Pitfall 10 — load-bearing; TestBodyDeferralTests.TestBodyNotEvaluatedAtRegistration directly asserts the wrap holds; if regressed, the fact RED's. |
</threat_model>

<verification>
- All 11 Wave 0 facts pass: `dotnet test --filter "FullyQualifiedName~Phase35.TestFrameworkBuiltinsTests|FullyQualifiedName~Phase35.TestBodyDeferralTests|FullyQualifiedName~Phase35.HermeticIsolationTests|FullyQualifiedName~Phase35.AssertWithinDbTests|FullyQualifiedName~Phase35.FlowTestCliTests"` exits 0
- Full xUnit suite: `dotnet test` exits 0 (Phase 18/25/27/28 byte-identical sentinels preserved despite new SnapshotState code path)
- .flow regression: every existing tests/test_*.flow continues to exit 0
- End-to-end: `flow test tests/test_test_framework.flow` exits 0 with PASS summary
- Meta-test discipline: `! grep -rn "(test " flow-lang.Tests/` returns no matches (xUnit facts do NOT recursively use the Flow test framework)
</verification>

<success_criteria>
Plan complete when:
- [ ] TestFramework folder + 6 C# files exist
- [ ] flow-lang/test.flow stub exists
- [ ] flow-lang/Runtime/ExecutionContext.cs has TestRegistry + SnapshotState/RestoreState
- [ ] flow-lang/Core/FlowEngine.cs exposes pass-through Snapshot/Restore + TestRegistry
- [ ] flow-lang/StandardLibrary/BuiltInFunctions.cs calls RegisterTestFramework
- [ ] flow-cli/Commands/TestCommand.cs + CommandRegistry registration land
- [ ] tests/test_test_framework.flow contains ≥5 (test ...) blocks across all 5 assertion primitives
- [ ] RmsComparator extracted as pure helper; xUnit RmsRegressionTests delegates to it
- [ ] All 11 Wave 0 facts pass GREEN
- [ ] Full xUnit suite + all .flow scripts remain GREEN
- [ ] `flow test tests/test_test_framework.flow` exits 0 with PASS summary
- [ ] AudioPlaybackManager reset-or-document-as-forbidden recorded as follow-up in SUMMARY (RESEARCH Assumption A8 — to be addressed in CLAUDE.md as a doc edit)
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-04-SUMMARY.md` when done with: list of 11+ state surfaces actually reset in SnapshotState/RestoreState, the test-runner output format sample, the RmsComparator API + xUnit-helper delegation result, and the AudioPlaybackManager-not-reset follow-up note for CLAUDE.md (Assumption A8 deferred doc edit).
</output>
