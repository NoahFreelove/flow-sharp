---
phase: 35-language-foundation
plan: 05
type: execute
wave: 3
depends_on: ["35-01", "35-04"]
files_modified:
  - flow-lang/Ast/Patterns/Pattern.cs
  - flow-lang/Ast/Patterns/LiteralPattern.cs
  - flow-lang/Ast/Patterns/WildcardPattern.cs
  - flow-lang/Ast/Patterns/BindingPattern.cs
  - flow-lang/Ast/Patterns/ConstructorPattern.cs
  - flow-lang/Ast/Patterns/GuardPattern.cs
  - flow-lang/Ast/Patterns/MatchArm.cs
  - flow-lang/Ast/Expressions/MatchExpression.cs
  - flow-lang/Interpreter/PatternMatcher.cs
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang/Lexing/SimpleLexer.cs
  - flow-lang/Lexing/TokenType.cs
  - flow-lang/Parsing/Parser.cs
  - flow-lang.Tests/Phase35/MatchLexerTests.cs
  - flow-lang.Tests/Phase35/MatchParserTests.cs
  - flow-lang.Tests/Phase35/PatternAstTests.cs
  - flow-lang.Tests/Phase35/MatchRuntimeTests.cs
  - tests/test_pattern_matching.flow
autonomous: true
requirements: [LANG-01]
must_haves:
  truths:
    - "LANG-01: Composer can write `(match scrutinee | pat1 => body1 | pat2 => body2 | _ => body3)`. Patterns supported: LiteralPattern (Int/Float/String/Bool/Note literal), WildcardPattern (`_`), BindingPattern (identifier captures the value), ConstructorPattern (generic name+sub-patterns; music-aware extractors added in Plan 35-06), GuardPattern (`| n when (= ...) => ...`)."
    - "LANG-01: arms are independent — no C-style fall-through; first matching arm wins (naive linear scan per RESEARCH Open Question 1 default — decision-tree compile deferred to v1.6)."
    - "LANG-01: pattern AST lives in new Ast/Patterns/ folder distinct from Expressions/ and Statements/ (REQUIREMENTS.md LANG-01 explicit constraint); MatchExpression itself lives in Ast/Expressions/."
    - "Pitfall 2 disambiguation: note-stream `| C4 D4 |` parses correctly OUTSIDE (match ...); match arms `| pat => body` parse correctly INSIDE (match ...). The parser tracks a `parsingMatchArms` flag set after consuming `(match scrutinee` and cleared at the closing `)`."
    - "Pitfall 6: match-arm bindings die with the arm-body frame — bindings DO NOT leak past the match expression into enclosing scope. Implemented via PushFrame/PopFrame around arm body evaluation per ExecutionContext.cs:171-191."
    - "Non-exhaustive match handling deferred to Plan 35-06 — this plan only implements the matching machinery; the WARN-vs-error policy (D-v1.5-05) + matchExhaustive pragma lookup land alongside the music-aware extractors."
    - "All existing tests/test_*.flow scripts continue to pass — note-stream `| ... |` parsing unaffected outside (match ...)."
  artifacts:
    - path: flow-lang/Ast/Patterns/Pattern.cs
      provides: "Abstract base record Pattern(SourceLocation Location, Span? Span = null) — parallel family to AstNode per RESEARCH §Recommended Project Structure"
      contains: "abstract record Pattern"
    - path: flow-lang/Ast/Patterns/LiteralPattern.cs
      provides: "LiteralPattern(SourceLocation, object Value, Span?) — matches via Value.Equals"
      contains: "LiteralPattern"
    - path: flow-lang/Ast/Patterns/WildcardPattern.cs
      provides: "WildcardPattern(SourceLocation, Span?) — always matches; no payload"
      contains: "WildcardPattern"
    - path: flow-lang/Ast/Patterns/BindingPattern.cs
      provides: "BindingPattern(SourceLocation, string Name, Span?) — always matches; binds the scrutinee to Name in arm-body frame"
      contains: "BindingPattern"
    - path: flow-lang/Ast/Patterns/ConstructorPattern.cs
      provides: "ConstructorPattern(SourceLocation, string Name, IReadOnlyList<Pattern> SubPatterns, Span?) — generic constructor; Plan 35-06 extends with music-aware discriminator flags"
      contains: "ConstructorPattern"
    - path: flow-lang/Ast/Patterns/GuardPattern.cs
      provides: "GuardPattern(SourceLocation, Pattern Inner, Expression GuardExpression, Span?) — wraps Inner with a boolean side-condition"
      contains: "GuardPattern"
    - path: flow-lang/Ast/Patterns/MatchArm.cs
      provides: "Value record MatchArm(Pattern Pattern, Expression Body, Span? Span = null) — held by MatchExpression"
      contains: "MatchArm"
    - path: flow-lang/Ast/Expressions/MatchExpression.cs
      provides: "MatchExpression(SourceLocation, Expression Scrutinee, IReadOnlyList<MatchArm> Arms, Span?) : Expression(Location) — sibling of FlowExpression"
      contains: "MatchExpression"
    - path: flow-lang/Interpreter/PatternMatcher.cs
      provides: "Pattern dispatch (naive linear scan) per RESEARCH §Example 2 — PatternMatches(pattern, scrutinee, bindings) switch + Bind helper"
      contains: "PatternMatcher"
    - path: tests/test_pattern_matching.flow
      provides: "Composer-facing acceptance — exercises literal/wildcard/binding/guard arms via (test ...) framework"
      min_lines: 30
  key_links:
    - from: flow-lang/Parsing/Parser.cs
      to: flow-lang/Ast/Expressions/MatchExpression.cs
      via: "ParseMatch invoked when LParen is followed by Match keyword; consumes scrutinee then loops on Pipe-introduced arms"
      pattern: "ParseMatch|MatchExpression"
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Interpreter/PatternMatcher.cs
      via: "EvaluateMatch dispatches to PatternMatcher.PatternMatches for each arm; first match wins"
      pattern: "PatternMatcher|EvaluateMatch"
    - from: flow-lang/Lexing/SimpleLexer.cs
      to: flow-lang/Lexing/TokenType.cs
      via: "Keyword table maps `match` -> TokenType.Match and `when` -> TokenType.When"
      pattern: "TokenType.Match|TokenType.When"
---

<objective>
Land the pattern-matching foundation (LANG-01) — Wave 3 of Phase 35. Adds the new `Ast/Patterns/` family (6 pattern types + MatchArm), the MatchExpression AST node (in Ast/Expressions/), the lexer/parser changes to recognize `match` / `when` keywords + the `(match scrutinee | pat => body | ... | _ => default)` form (with disambiguation against note-stream `|` per Pitfall 2), and the runtime PatternMatcher implementing naive linear scan (per RESEARCH Open Question 1 default — decision-tree compile deferred to v1.6). Non-exhaustive match handling (the WARN-vs-error policy + `matchExhaustive` pragma lookup per D-v1.5-05) and music-aware extractors (chord-quality / roman-numeral / articulation-symbol patterns) are scoped to Plan 35-06 — this plan covers the structural matching machinery.

Purpose: LANG-01 — pattern matching expression with literal / wildcard / binding / constructor / guard pattern forms. Foundation for Phase 36 SECT-01 destructuring, Phase 39 articulation emit, Phase 40 MIDI event dispatch. Critical for unblocking the entire downstream v1.5 stack.

Output: 8 new AST records + PatternMatcher + lexer/parser changes + ExpressionEvaluator dispatch + 4 xUnit fact classes + composer-facing tests/test_pattern_matching.flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@.planning/phases/35-language-foundation/35-01-SUMMARY.md
@.planning/phases/35-language-foundation/35-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing AST shape — patterns mirror this with a parallel base record. -->

From flow-lang/Ast/AstNode.cs (do NOT inherit from this in Patterns/):
- abstract record AstNode(SourceLocation Location)
- abstract record Expression(SourceLocation Location) : AstNode(Location)
- abstract record Statement(SourceLocation Location) : AstNode(Location)

From flow-lang/Ast/Expressions/FlowExpression.cs (sibling shape — MatchExpression mirrors this):
- public record FlowExpression(SourceLocation Location, Expression Left, Expression Right) : Expression(Location);

From flow-lang/Ast/Expressions/LambdaExpression.cs:10:
- public record LambdaParameter(string Name, FlowType Type) — analog for MatchArm sub-record shape

From flow-lang/Interpreter/ExpressionEvaluator.cs (existing switch dispatch shape):
- Lines 50-51 contain the existing switch arms (FlowExpression / TupleUnpackFlowExpression) — add `MatchExpression matchEx => EvaluateMatch(matchEx),` here.
- Lines 325-346 EvaluateFlowExpression — shape reference for EvaluateMatch.

From flow-lang/Runtime/ExecutionContext.cs:171-191 (PushFrame/PopFrame for arm-body scoping):
- Standard frame-push lifecycle; PatternMatcher uses this to scope binding-pattern declarations to the arm body only (Pitfall 6).

From flow-lang/Lexing/SimpleLexer.cs:850-891 (keyword table):
- Existing entries: "proc" => TokenType.Proc, "voicePool" => TokenType.VoicePool, "tuning" => TokenType.Tuning, etc.
- Add: "match" => TokenType.Match, "when" => TokenType.When.

From flow-lang/Lexing/TokenType.cs:
- Underscore (TokenType.Underscore at line 91) — already exists; used for wildcard `_` pattern.
- FatArrow (TokenType.FatArrow at line 69) — already exists; used by lambdas; reused for match arms.
- Pipe (TokenType.Pipe) — already exists; disambiguation per Pitfall 2 below.

From flow-lang/Parsing/Parser.cs:1044 (note-stream `|` disambiguation):
- Note-stream `| C4 D4 |` parses ONLY from primary-expression start position. Match-arm Pipe is NOT a primary-expression start (it's a delimiter inside ParseMatch's arm loop).

From flow-lang/Parsing/Parser.cs:1089-1115 (parenthesized-call form):
- After consuming LParen + a keyword token, ParseMatch fires when the keyword is TokenType.Match.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing test stubs for match lexer/parser/AST/runtime + composer-facing tests/test_pattern_matching.flow</name>
  <files>flow-lang.Tests/Phase35/MatchLexerTests.cs, flow-lang.Tests/Phase35/MatchParserTests.cs, flow-lang.Tests/Phase35/PatternAstTests.cs, flow-lang.Tests/Phase35/MatchRuntimeTests.cs, tests/test_pattern_matching.flow</files>
  <read_first>
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 73-86 Wave 3 table; lines 118-131 Wave 0 list)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 429-440 Pitfall 2 disambiguation; lines 481-490 Pitfall 6 binding scope; lines 562-630 Example 2 EvaluateMatch + PatternMatches)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 587-861 Bucket 3 — file-by-file analogs)
    - flow-lang.Tests/Phase35/ (existing folder; Plan 35-04 added TestFramework fact classes — confirm xUnit shape)
    - tests/test_test_framework.flow (Plan 35-04 — meta-dogfooding template; use the same (test ...) idiom)
  </read_first>
  <behavior>
    - Test 1 (MatchLexerTests.MatchKeywordTokenized): Lex `match` → exactly one TokenType.Match token emitted.
    - Test 2 (MatchLexerTests.WhenKeywordTokenized): Lex `when` → exactly one TokenType.When token emitted.
    - Test 3 (MatchLexerTests.UnderscoreStillTokenizesAsWildcard): Lex `_` → TokenType.Underscore (no change from existing behavior).
    - Test 4 (MatchParserTests.SimpleMatchParses): Parse `(match x | 1 => "one" | 2 => "two" | _ => "other")` produces a MatchExpression with 3 arms (LiteralPattern, LiteralPattern, WildcardPattern).
    - Test 5 (MatchParserTests.BindingPatternParses): Parse `(match x | n => n)` produces an arm with BindingPattern(Name="n").
    - Test 6 (MatchParserTests.GuardPatternParses): Parse `(match n | x when (greater x 0) => "pos" | _ => "neg")` produces a GuardPattern wrapping BindingPattern with the (greater x 0) expression.
    - Test 7 (MatchParserTests.NoteStreamStillParsesOutsideMatch): Parse `Sequence s = | C4 D4 E4 |` — still produces a NoteStreamExpression (Pitfall 2 — disambiguation holds).
    - Test 8 (PatternAstTests.AllPatternKindsHaveSpan): Construct each of 5 pattern types; assert each has a non-null Span (or Span.At(loc) defaulted).
    - Test 9 (PatternAstTests.MatchArmIsValueRecord): MatchArm(...) equality semantics — two MatchArms with equal Pattern + Body are .Equals true.
    - Test 10 (MatchRuntimeTests.FirstMatchWins): `(match 5 | 5 => "first" | 5 => "second")` evaluates to "first" — no fall-through, no second-arm execution.
    - Test 11 (MatchRuntimeTests.WildcardMatchesAnything): `(match 42 | _ => "any")` returns "any".
    - Test 12 (MatchRuntimeTests.BindingPatternBindsScrutinee): `(match 42 | n => (mul n 2))` returns 84.
    - Test 13 (MatchRuntimeTests.BindingDoesNotLeakToEnclosingScope): After `(match 42 | n => n)` in a block, the next statement asserting `n` is unknown — Pitfall 6 verification.
    - Test 14 (MatchRuntimeTests.GuardPatternFiresOnlyWhenGuardTrue): `(match 5 | n when (greater n 0) => "pos" | _ => "neg")` returns "pos"; for `-5` returns "neg".
    - Test 15 (composer-facing tests/test_pattern_matching.flow): Contains at least 5 (test ...) blocks via the Plan 35-04 framework exercising literal / wildcard / binding / guard arms. Runnable via `flow test tests/test_pattern_matching.flow` — all PASS on completion.
  </behavior>
  <action>
    Create the 4 xUnit fact files in `flow-lang.Tests/Phase35/`. Mirror the existing class shape and use FlowEngine to execute Flow source for parser/runtime tests. Pattern AST tests construct patterns directly via the new records.

    For MatchLexerTests: drive SimpleLexer directly (or via FlowEngine if it exposes the token list); inspect emitted TokenType values.

    For MatchParserTests: use FlowEngine to parse the source; walk the resulting Program AST to find MatchExpression nodes and inspect arms.

    For PatternAstTests: construct each pattern subtype with sample SourceLocation + Span; assert non-null Span; assert record equality semantics.

    For MatchRuntimeTests: use FlowEngine.Execute to evaluate match expressions; capture the returned Value via `engine.LastValue` or analogous; assert against expected.

    Create `tests/test_pattern_matching.flow` per PATTERNS.md Bucket 3 — composer-facing meta-dogfooding via the Plan 35-04 test framework. At least 5 (test ...) blocks exercising literal, wildcard, binding, and guard arms.

    All tests MUST initially fail-to-compile (MatchExpression / Pattern records don't exist) OR fail-at-runtime (parser doesn't recognize `match` keyword). Tasks 2 + 3 + 4 make them GREEN.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; ls flow-lang.Tests/Phase35/Match*.cs flow-lang.Tests/Phase35/Pattern*.cs tests/test_pattern_matching.flow 2>&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source: 4 new xUnit test files exist in flow-lang.Tests/Phase35/
    - Source: tests/test_pattern_matching.flow exists with at least 5 `(test ` blocks
    - Behavior: dotnet build fails on missing Pattern/MatchExpression types (expected RED) OR builds with placeholders
    - Done: All 15 Wave 0 behaviors stubbed; expected-RED state confirmed
  </acceptance_criteria>
  <done>Wave 0 stubs land; expected-RED for the 4 xUnit fact classes + composer-facing .flow regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Pattern AST family (6 records + MatchArm + MatchExpression); lexer/parser additions for match + when keywords</name>
  <files>flow-lang/Ast/Patterns/Pattern.cs, flow-lang/Ast/Patterns/LiteralPattern.cs, flow-lang/Ast/Patterns/WildcardPattern.cs, flow-lang/Ast/Patterns/BindingPattern.cs, flow-lang/Ast/Patterns/ConstructorPattern.cs, flow-lang/Ast/Patterns/GuardPattern.cs, flow-lang/Ast/Patterns/MatchArm.cs, flow-lang/Ast/Expressions/MatchExpression.cs, flow-lang/Lexing/SimpleLexer.cs, flow-lang/Lexing/TokenType.cs</files>
  <read_first>
    - flow-lang/Ast/AstNode.cs (base record shape; Patterns DO NOT inherit from AstNode per RESEARCH §Recommended Project Structure)
    - flow-lang/Ast/Expressions/LiteralExpression.cs (shape analog for LiteralPattern)
    - flow-lang/Ast/Expressions/VariableExpression.cs (shape analog for BindingPattern)
    - flow-lang/Ast/Expressions/FunctionCallExpression.cs (shape analog for ConstructorPattern)
    - flow-lang/Ast/Expressions/LazyExpression.cs (shape analog for GuardPattern wrapper)
    - flow-lang/Ast/Expressions/FlowExpression.cs (shape analog for MatchExpression — sibling in Expressions/)
    - flow-lang/Ast/Expressions/LambdaExpression.cs:10 (LambdaParameter pattern for MatchArm sub-record)
    - flow-lang/Lexing/SimpleLexer.cs:850-891 (keyword table — add match + when)
    - flow-lang/Lexing/TokenType.cs (add Match + When entries in Keywords block alongside Tuning at line 29)
    - flow-lang/Core/Span.cs (from Plan 35-01)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 587-810 Bucket 3 — file-by-file analogs)
  </read_first>
  <behavior>
    - MatchLexerTests pass GREEN: TokenType.Match + TokenType.When emitted when source contains `match` + `when` keywords; TokenType.Underscore unchanged.
    - PatternAstTests pass GREEN: all 5 pattern types constructible with SourceLocation + Span; MatchArm value-record equality semantics hold.
    - Pattern records can be constructed via direct C# (test fixtures don't need parser yet — Task 3 wires the parser).
    - All 1003+ existing xUnit facts continue GREEN — adding new TokenType enum entries + new AST records does not break any existing parser/evaluator path.
  </behavior>
  <action>
    Create `flow-lang/Ast/Patterns/Pattern.cs` per PATTERNS.md Bucket 3 §Pattern.cs:
    `public abstract record Pattern(SourceLocation Location, Span? Span = null);`
    Namespace `FlowLang.Ast.Patterns`. Patterns DO NOT inherit from AstNode — they are a parallel family per RESEARCH §Recommended Project Structure.

    Create the 5 subtype records per PATTERNS.md Bucket 3:
    - `LiteralPattern(SourceLocation Location, object Value, Span? Span = null) : Pattern(Location, Span);`
    - `WildcardPattern(SourceLocation Location, Span? Span = null) : Pattern(Location, Span);` — no payload
    - `BindingPattern(SourceLocation Location, string Name, Span? Span = null) : Pattern(Location, Span);`
    - `ConstructorPattern(SourceLocation Location, string Name, IReadOnlyList<Pattern> SubPatterns, Span? Span = null) : Pattern(Location, Span)` with init-only `IsChordLiteral`, `IsRomanNumeral`, `IsArticulationSymbol` discriminator flags initialized to false (Plan 35-06 sets these). Per PATTERNS.md Bucket 3 §ConstructorPattern.cs — flag-based approach over separate subtypes for extensibility cost.
    - `GuardPattern(SourceLocation Location, Pattern Inner, Expression GuardExpression, Span? Span = null) : Pattern(Location, Span);`

    Create `flow-lang/Ast/Patterns/MatchArm.cs` per PATTERNS.md Bucket 3 §MatchArm.cs:
    `public record MatchArm(Pattern Pattern, Expression Body, Span? Span = null);`
    MatchArm is a value record NOT inheriting from Pattern/Expression/Statement — same posture as LambdaParameter.

    Create `flow-lang/Ast/Expressions/MatchExpression.cs` per PATTERNS.md Bucket 3 §MatchExpression.cs:
    `public record MatchExpression(SourceLocation Location, Expression Scrutinee, IReadOnlyList<MatchArm> Arms, Span? Span = null) : Expression(Location);`
    Sibling to FlowExpression in Expressions/ folder (NOT in Patterns/ — per RESEARCH §Recommended Project Structure intentional placement).

    Update `flow-lang/Lexing/TokenType.cs` per PATTERNS.md Bucket 3 §TokenType.cs — add `Match,` and `When,` in the Keywords block alongside `Tuning` at line 29.

    Update `flow-lang/Lexing/SimpleLexer.cs:850-891` (keyword table) per PATTERNS.md Bucket 3 §SimpleLexer.cs — add `"match" => TokenType.Match,` and `"when" => TokenType.When,` entries.

    DO NOT touch Parser.cs in this task — Task 3 implements ParseMatch.
    DO NOT touch ExpressionEvaluator.cs in this task — Task 4 implements EvaluateMatch.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.MatchLexerTests|FullyQualifiedName~Phase35.PatternAstTests" 2>&amp;1 | tail -15 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: flow-lang/Ast/Patterns/ folder contains 7 files (Pattern.cs + 5 subtypes + MatchArm.cs)
    - Source: flow-lang/Ast/Expressions/MatchExpression.cs exists
    - Source: grep -c "TokenType.Match" flow-lang/Lexing/TokenType.cs flow-lang/Lexing/SimpleLexer.cs returns at least 2
    - Source: grep -c "TokenType.When" flow-lang/Lexing/TokenType.cs flow-lang/Lexing/SimpleLexer.cs returns at least 2
    - Behavior: MatchLexerTests + PatternAstTests pass GREEN (`dotnet test --filter "FullyQualifiedName~Phase35.MatchLexerTests|FullyQualifiedName~Phase35.PatternAstTests"` exits 0)
    - Behavior: Full dotnet test exits 0 (no regression from new TokenType entries or new AST records)
    - Done: AST family + lexer keywords in place; pattern records constructible
  </acceptance_criteria>
  <done>8 new AST records + 2 new TokenType entries + lexer-keyword wiring; MatchLexerTests + PatternAstTests GREEN; full suite preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ParseMatch implementation — recognize `(match scrutinee | pat => body | ... | _ => default)` with Pitfall 2 disambiguation against note-stream `|`</name>
  <files>flow-lang/Parsing/Parser.cs</files>
  <read_first>
    - flow-lang/Parsing/Parser.cs:1043-1047 (existing note-stream `|` disambiguation rule — only fires from primary-expression start)
    - flow-lang/Parsing/Parser.cs:1089-1115 (parenthesized-call form ParseFunctionCall — pattern for ParseMatch entry)
    - flow-lang/Parsing/Parser.cs:850-913 (ParseFlowExpression — shape reference)
    - flow-lang/Ast/Expressions/MatchExpression.cs (Task 2 output)
    - flow-lang/Ast/Patterns/*.cs (Task 2 output — pattern records to construct)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 815-841 Bucket 3 §Parser.cs ParseMatch)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 429-440 Pitfall 2 disambiguation; lines 197-220 Architecture Diagram note on `(match` open paren disambiguator)
  </read_first>
  <behavior>
    - MatchParserTests.SimpleMatchParses passes — `(match x | 1 => "one" | 2 => "two" | _ => "other")` parses to MatchExpression with 3 arms.
    - MatchParserTests.BindingPatternParses passes — `(match x | n => n)` produces BindingPattern(Name="n") not LiteralPattern.
    - MatchParserTests.GuardPatternParses passes — `(match n | x when (greater x 0) => "pos" | _ => "neg")` produces GuardPattern wrapping BindingPattern; guard expression is the (greater x 0) call.
    - MatchParserTests.NoteStreamStillParsesOutsideMatch passes — `Sequence s = | C4 D4 E4 |` produces NoteStreamExpression (Pitfall 2 holds — disambiguation works).
    - All existing tests/test_*.flow scripts that use note streams continue to parse correctly (regression check).
    - Each MatchExpression + MatchArm + Pattern node carries non-Unknown Span set by the parser per Plan 35-01's sweep convention.
  </behavior>
  <action>
    Add a new `private Expression ParseMatch()` method to `flow-lang/Parsing/Parser.cs`. Per PATTERNS.md Bucket 3 §Parser.cs ParseMatch Apply section + RESEARCH §Architecture Diagram lines 197-220:

    Entry-point integration: In the existing parenthesized-call form at Parser.cs:1089-1115 (after consuming LParen + sniffing the next token), detect `if (Check(TokenType.Match))` BEFORE the existing Identifier/Pan/Gain branch. When matched, consume Match token and delegate to ParseMatch.

    ParseMatch body:
    1. Capture `var location = PreviousToken.Location;` (the `match` keyword's Location).
    2. Parse the scrutinee expression via `var scrutinee = ParseExpression();`.
    3. Loop: expect TokenType.Pipe (each Pipe introduces an arm); inside the loop call ParsePattern; expect TokenType.FatArrow; parse arm body via `var body = ParseExpression();`. Collect MatchArm(pattern, body, Span) into a List<MatchArm>.
    4. Loop terminates when next token is TokenType.RParen.
    5. Expect TokenType.RParen.
    6. Return `new MatchExpression(location, scrutinee, arms, Span: new Span(location, PreviousToken.Location));`

    Pitfall 2 disambiguation: ParseMatch's Pipe handling NEVER delegates to ParseNoteStream. The disambiguator is that ParseNoteStream only fires from primary-expression START position (Parser.cs:1043-1047 verified) — inside ParseMatch's arm loop, the Pipe is a delimiter, not a primary-expression start, so the existing note-stream rule does not fire. No new flag needed; the structural distinction holds. Verify by ensuring ParseMatch's loop body directly handles Pipe without falling through to the primary-expression dispatcher.

    Add a new `private Pattern ParsePattern()` method handling:
    - TokenType.Underscore → WildcardPattern
    - TokenType.IntLiteral / FloatLiteral / StringLiteral / BoolLiteral / NoteLiteral → LiteralPattern(value)
    - TokenType.Identifier:
      - If next token is TokenType.When → consume identifier as BindingPattern(name); consume When; parse guard expression via ParseExpression; wrap in GuardPattern(inner=BindingPattern, guard).
      - Else if identifier is the start of a constructor-form (followed by sub-patterns) → ConstructorPattern with collected sub-patterns. For Plan 35-05 the generic form is `name SubPatterns*` — but Plan 35-06 adds the music-aware extractors. Initial impl: treat any identifier-starting pattern as BindingPattern UNLESS the parser has explicit knowledge it's a constructor name; Plan 35-06 extends.
      - Else → BindingPattern(name) (identifier captures the scrutinee)
    - TokenType.ChordLiteral → ConstructorPattern(name=text, SubPatterns=[], IsChordLiteral=true) — flag set so Plan 35-06's PatternMatcher.MatchConstructor recognizes it as a chord-quality extractor.
    - Other tokens → emit a parse error via FlowDiagnostic (from Plan 35-03) referencing the unexpected token's Span.

    Capture Span at each pattern construction site per Plan 35-01's sweep convention — Span Start at the pattern's first token, End at the pattern's last token.

    Do NOT touch ExpressionEvaluator.cs in this task — Task 4 implements EvaluateMatch.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.MatchParserTests" 2>&amp;1 | tail -15 &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done &amp;&amp; echo "ALL .flow SCRIPTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.MatchParserTests" exits 0 (4 facts GREEN — SimpleMatchParses, BindingPatternParses, GuardPatternParses, NoteStreamStillParsesOutsideMatch)
    - Behavior: All existing tests/test_*.flow scripts continue to pass — no note-stream regression
    - Source: grep -c "ParseMatch" flow-lang/Parsing/Parser.cs returns at least 1
    - Source: grep -c "ParsePattern" flow-lang/Parsing/Parser.cs returns at least 1
    - Source: grep -c "MatchExpression" flow-lang/Parsing/Parser.cs returns at least 1
    - Done: ParseMatch + ParsePattern recognize the (match ...) form; Pitfall 2 disambiguation holds; note-stream regression preserved
  </acceptance_criteria>
  <done>Parser recognizes (match ...) form with literal/wildcard/binding/guard patterns; note-stream parses unchanged outside match; MatchParserTests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: PatternMatcher + EvaluateMatch dispatch — naive linear scan with bindings scoped to arm-body frame</name>
  <files>flow-lang/Interpreter/PatternMatcher.cs, flow-lang/Interpreter/ExpressionEvaluator.cs</files>
  <read_first>
    - flow-lang/Interpreter/ExpressionEvaluator.cs:50-51 (existing switch dispatch — add MatchExpression arm)
    - flow-lang/Interpreter/ExpressionEvaluator.cs:325-346 (EvaluateFlowExpression shape — analog for EvaluateMatch)
    - flow-lang/Runtime/ExecutionContext.cs:171-191 (PushFrame/PopFrame for arm-body scoping — Pitfall 6)
    - flow-lang/Runtime/Value.cs (Value.Equals static for literal matching)
    - flow-lang/Ast/Patterns/*.cs (Task 2 output)
    - flow-lang/Ast/Expressions/MatchExpression.cs (Task 2 output)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 757-810 Bucket 3 §PatternMatcher.cs + §ExpressionEvaluator.cs dispatch)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 562-630 Example 2 — EvaluateMatch + PatternMatches verbatim shape; lines 481-490 Pitfall 6)
  </read_first>
  <behavior>
    - MatchRuntimeTests pass GREEN: all 5 facts (FirstMatchWins, WildcardMatchesAnything, BindingPatternBindsScrutinee, BindingDoesNotLeakToEnclosingScope, GuardPatternFiresOnlyWhenGuardTrue).
    - tests/test_pattern_matching.flow passes via `flow test tests/test_pattern_matching.flow` — all (test ...) blocks PASS.
    - When no arm matches, the evaluator returns Value.Void() unconditionally (Plan 35-06 adds the WARN-to-stderr + matchExhaustive pragma check — this plan's pre-Plan-35-06 stub behavior is silent Void).
    - All existing tests/test_*.flow scripts + 1003+ xUnit facts remain GREEN — no regression from the new EvaluateMatch dispatch arm.
  </behavior>
  <action>
    Create `flow-lang/Interpreter/PatternMatcher.cs` per PATTERNS.md Bucket 3 §PatternMatcher.cs + RESEARCH §Example 2. Public static class with:
    - `public static bool PatternMatches(Pattern pattern, Value scrutinee, Dictionary<string, Value> bindings)` — switch expression dispatching by pattern subtype per RESEARCH lines 601-613:
      - WildcardPattern → true
      - BindingPattern b → bindings[b.Name] = scrutinee; return true
      - LiteralPattern lit → Value.Equals(scrutinee, lit.Value)
      - ConstructorPattern ctor → MatchConstructor(ctor, scrutinee, bindings)
      - GuardPattern guard → PatternMatches(guard.Inner, scrutinee, bindings) && EvaluateGuard(guard.GuardExpression, bindings, evaluator)
      - default → throw NotSupportedException
    - `private static bool MatchConstructor(ConstructorPattern ctor, Value scrutinee, Dictionary<string, Value> bindings)` — for Plan 35-05, default-shape: if no discriminator flag is set, fall back to structural equality on Name + sub-pattern matching against ResolvedConstructor (or return false for unknown constructors). Plan 35-06 extends this with IsChordLiteral / IsRomanNumeral / IsArticulationSymbol handling.
    - `private static bool EvaluateGuard(Expression guardExpr, Dictionary<string, Value> bindings, ExpressionEvaluator evaluator)` — push a temp frame with the bindings dict, evaluate guardExpr, pop frame, return the resulting bool.

    Because EvaluateGuard needs access to ExpressionEvaluator (to evaluate the guard expression in the bound scope), pass the evaluator (or its context) into PatternMatcher.PatternMatches. The cleanest shape per PATTERNS.md is `PatternMatcher.PatternMatches(pattern, scrutinee, bindings, evaluator)` — make evaluator the 4th param.

    Add EvaluateMatch to `flow-lang/Interpreter/ExpressionEvaluator.cs`. Per PATTERNS.md Bucket 3 §ExpressionEvaluator.cs:
    - At lines 50-51, add the new switch arm: `MatchExpression matchEx => EvaluateMatch(matchEx),`
    - Implement `private Value EvaluateMatch(MatchExpression match)` per RESEARCH §Example 2 lines 567-599 with one departure: this plan does NOT yet emit the non-exhaustive WARN or check the matchExhaustive pragma — when no arm matches, just `return Value.Void();` Plan 35-06 adds the policy.
    - Body shape: evaluate scrutinee → for each arm, attempt PatternMatcher.PatternMatches; on first match, PushFrame, declare each binding via `_context.DeclareVariable(name, value)`, Evaluate(arm.Body), PopFrame in finally; return the body's Value. Pitfall 6 enforces frame scoping so bindings don't leak.

    Do NOT touch the WARN/pragma path — leave a `// Plan 35-06: non-exhaustive warning + matchExhaustive pragma check` comment marker where Plan 35-06 will add the logic.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.MatchRuntimeTests|FullyQualifiedName~Phase35.MatchParserTests|FullyQualifiedName~Phase35.MatchLexerTests|FullyQualifiedName~Phase35.PatternAstTests" 2>&amp;1 | tail -20 &amp;&amp; dotnet run --project flow-cli -- test tests/test_pattern_matching.flow 2>&amp;1 | tail -15 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.MatchRuntimeTests" exits 0 (all 5 facts GREEN — FirstMatchWins, WildcardMatchesAnything, BindingPatternBindsScrutinee, BindingDoesNotLeakToEnclosingScope, GuardPatternFiresOnlyWhenGuardTrue)
    - Behavior: dotnet run --project flow-cli -- test tests/test_pattern_matching.flow exits 0 with PASS summary
    - Behavior: Full dotnet test exits 0 (no regression from new EvaluateMatch dispatch arm)
    - Source: flow-lang/Interpreter/PatternMatcher.cs exists with `public static bool PatternMatches(...)` + private MatchConstructor + EvaluateGuard
    - Source: grep -c "MatchExpression matchEx =>" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 1
    - Source: grep -c "EvaluateMatch" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 2 (declaration + dispatch)
    - Done: Pattern dispatch (naive linear scan) operational; bindings scoped to arm-body frame; non-exhaustive fallthrough returns Void (Plan 35-06 adds WARN policy)
  </acceptance_criteria>
  <done>PatternMatcher + EvaluateMatch dispatch land; 15 Wave 0 behaviors all GREEN; composer-facing pattern_matching.flow regression passes via flow test CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pattern matching is purely internal language extension — no untrusted-input crossings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-14 | Integrity | Note-stream `|` disambiguation regression | mitigate | MatchParserTests.NoteStreamStillParsesOutsideMatch + the full tests/test_*.flow regression loop verify Pitfall 2 holds. Disambiguation relies on the structural rule that ParseNoteStream only fires from primary-expression-start position — verified in Parser.cs:1043-1047. |
| T-35-15 | Integrity | Match-arm binding scope leak | mitigate | MatchRuntimeTests.BindingDoesNotLeakToEnclosingScope verifies Pitfall 6 — PushFrame/PopFrame wrapping ensures bindings die with the arm body. |
| T-35-16 | Denial of Service | Pattern matching in hot paths (Phase 36 SECT-01 / Phase 40 MIDI dispatch) | accept | Naive linear scan per RESEARCH Open Question 1 default; expected match-arm count is small (3-15). If profiling shows hotspots in later phases, swap to decision-tree compile in v1.6 (back-end-only change, no surface impact). |
</threat_model>

<verification>
- All Wave 0 Phase35 match facts pass: `dotnet test --filter "FullyQualifiedName~Phase35.MatchLexerTests|FullyQualifiedName~Phase35.MatchParserTests|FullyQualifiedName~Phase35.PatternAstTests|FullyQualifiedName~Phase35.MatchRuntimeTests"` exits 0
- Composer-facing acceptance: `flow test tests/test_pattern_matching.flow` exits 0 with PASS summary
- Full xUnit suite remains green: `dotnet test` exits 0
- .flow regression: every existing tests/test_*.flow continues to exit 0 (note-stream + everything else preserved)
</verification>

<success_criteria>
Plan complete when:
- [ ] Ast/Patterns/ folder contains Pattern.cs + 5 subtype records + MatchArm.cs
- [ ] Ast/Expressions/MatchExpression.cs exists
- [ ] TokenType.Match + TokenType.When added; SimpleLexer keyword table updated
- [ ] Parser recognizes (match scrutinee | pat => body | ... | _ => default) form
- [ ] PatternMatcher implements naive linear scan with literal / wildcard / binding / guard patterns
- [ ] ExpressionEvaluator dispatches MatchExpression to EvaluateMatch
- [ ] Pitfall 2 disambiguation holds — note-stream still parses outside (match ...)
- [ ] Pitfall 6 holds — bindings scoped to arm-body frame
- [ ] All 4 xUnit fact classes (MatchLexer / MatchParser / PatternAst / MatchRuntime) pass GREEN
- [ ] tests/test_pattern_matching.flow passes via flow test CLI
- [ ] Full xUnit suite + all .flow scripts remain GREEN
- [ ] Non-exhaustive behavior is silent Void (Plan 35-06 layers WARN + matchExhaustive policy)
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-05-SUMMARY.md` when done with: the Pattern AST family inventory, ParseMatch entry-point integration summary, PatternMatcher dispatch shape, ExpressionEvaluator EvaluateMatch behavior at non-exhaustive (silent Void pending Plan 35-06), and confirmation that Plans 35-06 (music-aware extractors + exhaustiveness) and 35-07 (-> as name) can proceed.
</output>
