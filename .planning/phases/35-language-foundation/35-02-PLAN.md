---
phase: 35-language-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/StandardLibrary/Transforms/TransformFunctions.cs
  - flow-lang/StandardLibrary/Composition/VariationFunctions.cs
  - tests/test_humanize_voice_block.flow
  - flow-lang.Tests/Phase35/HumanizeGaussianVoiceBlocksTests.cs
  - flow-lang.Tests/Phase35/MutateRhythmEnumValuesTests.cs
  - .planning/phases/17-flow-language-server/17-HUMAN-UAT.md
  - .planning/phases/04-composition-tools/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements: [HK-01, HK-02, HK-03, HK-04]
must_haves:
  truths:
    - "HK-01: humanizeGaussian applied to a Sequence whose Bars carry ParallelVoices preserves voice content; the rendered WAV is > 44 bytes and contains non-silent samples."
    - "HK-01: humanizeGaussian determinism preserved — single seeded Random shared across voices in a bar (no per-voice RNG); two-run cmp-clean for the new test_humanize_voice_block.flow output."
    - "HK-02: Phase 17 HUMAN-UAT rows 1-3 closure recorded — REQUIREMENTS.md HK-02 checkbox flipped; 17-HUMAN-UAT.md cross-references Phase 31 Plan 31-08 as the closure mechanism."
    - "HK-03: Phase 04 VERIFICATION.md gaps closed — REQUIREMENTS.md COMP-01 / COMP-02 checkboxes flipped to [x]; VariationFunctions.cs MutateRhythm switch corrected to NoteValueType enum integers; 04-VERIFICATION.md status flipped from gaps_found to verified."
    - "HK-04: CLAUDE.md 'Public as of v1.4' footnote rewritten to match the pre-traction-no-deprecation framing of the rewritten project_pre_public_no_legacy_burden external memory (per D-v1.5-01)."
    - "All 1003+ existing tests continue to pass; HK-03 regression fact added for MutateRhythm enum values."
  artifacts:
    - path: flow-lang/StandardLibrary/Transforms/TransformFunctions.cs
      provides: "HumanizeGaussian function fixed to recurse into ParallelVoices using the BarRenderer ParallelVoices recursion pattern"
      contains: "ParallelVoices"
    - path: tests/test_humanize_voice_block.flow
      provides: "Composer-facing regression for HK-01 — constructs a 2-voice block, applies humanizeGaussian, renders WAV, asserts >44 bytes + non-silent"
      min_lines: 15
    - path: flow-lang.Tests/Phase35/HumanizeGaussianVoiceBlocksTests.cs
      provides: "xUnit regression fact for HK-01 — same shape as the .flow script but invoked via FlowEngine"
      contains: "HumanizeGaussianVoiceBlocks"
    - path: flow-lang.Tests/Phase35/MutateRhythmEnumValuesTests.cs
      provides: "xUnit fact for HK-03 — assert MutateRhythm switch maps WHOLE->HALF, HALF->QUARTER, QUARTER->EIGHTH, EIGHTH->SIXTEENTH correctly"
      contains: "MutateRhythmEnumValues"
    - path: flow-lang/StandardLibrary/Composition/VariationFunctions.cs
      provides: "MutateRhythm switch at line 253 corrected to use NoteValueType enum integers"
      contains: "MutateRhythm"
    - path: .planning/REQUIREMENTS.md
      provides: "HK-01..04 checkboxes flipped to [x]; COMP-01 / COMP-02 also flipped per HK-03"
      contains: "[x] **HK-01"
    - path: CLAUDE.md
      provides: "Public as of v1.4 footnote rewritten to pre-traction-no-deprecation framing"
      contains: "pre-traction"
  key_links:
    - from: flow-lang/StandardLibrary/Transforms/TransformFunctions.cs
      to: flow-lang/StandardLibrary/Audio/BarRenderer.cs
      via: "HumanizeGaussian mirrors BarRenderer.cs:62-77 ParallelVoices recursion (PATTERNS.md Bucket 5 + Shared Patterns ParallelVoices Recursion)"
      pattern: "ParallelVoices"
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/04-composition-tools/04-VERIFICATION.md
      via: "HK-03 checkbox flip references VERIFICATION.md status change from gaps_found to verified"
      pattern: "\\[x\\] \\*\\*COMP-01"
    - from: CLAUDE.md
      to: "external memory project_pre_public_no_legacy_burden.md"
      via: "HK-04 footnote text aligned verbatim to the rewritten memory's pre-traction framing"
      pattern: "pre-traction"
---

<objective>
Close all four v1.4 housekeeping carryovers (HK-01..04) in a single parallel-safe plan that runs alongside the Span migration (Plan 35-01). HK-01 is a real bug fix (humanizeGaussian dropping voice-block content). HK-03 is a real bug fix (MutateRhythm enum-value mismatch) plus REQUIREMENTS.md checkbox closure. HK-02 and HK-04 are documentation-only — confirming Phase 17 UAT rows 1-3 already closed via Phase 31 Plan 31-08, and rewriting the CLAUDE.md footnote to match the rewritten external memory. The four items touch disjoint surfaces (Transforms + Composition for HK-01/03 source code; planning artifacts for HK-02/03 docs; CLAUDE.md for HK-04) so they fit cleanly in one plan.

Purpose: HK-01, HK-02, HK-03, HK-04 — v1.4 carryover closure clears the housekeeping debt before v1.5 language work proceeds.

Output: HumanizeGaussian voice-block-aware; MutateRhythm switch corrected; HK regression tests in place; planning docs (REQUIREMENTS / VERIFICATION) updated; CLAUDE.md footnote rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@.planning/phases/17-flow-language-server/17-HUMAN-UAT.md
@.planning/phases/04-composition-tools/04-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- The buggy HumanizeGaussian function — its current iteration ignores ParallelVoices. -->

From flow-lang/StandardLibrary/Transforms/TransformFunctions.cs:931-962 (current buggy shape per PATTERNS.md Bucket 5):
- HumanizeGaussian iterates ONLY bar.MusicalNotes; ignores bar.ParallelVoices; constructs new BarData(newNotes, bar.TimeSignature!) which drops the voice blocks entirely.
- Result: WAV is header-only (~44 bytes) when humanize is applied to voice-block content.

From flow-lang/StandardLibrary/Audio/BarRenderer.cs:62-77 (the canonical ParallelVoices recursion to mirror):
- if (bar.ParallelVoices != null && bar.ParallelVoices.Count > 0) recurse into each voiceBar (each a BarData) with sub-voice rendering, then combine results.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing regression tests for HK-01 (humanizeGaussian voice block) and HK-03 (MutateRhythm enum values)</name>
  <files>flow-lang.Tests/Phase35/HumanizeGaussianVoiceBlocksTests.cs, flow-lang.Tests/Phase35/MutateRhythmEnumValuesTests.cs, tests/test_humanize_voice_block.flow</files>
  <read_first>
    - flow-lang/StandardLibrary/Transforms/TransformFunctions.cs (full HumanizeGaussian function and surrounding context)
    - flow-lang/StandardLibrary/Composition/VariationFunctions.cs (focus on lines 240-270 around the MutateRhythm switch)
    - .planning/phases/04-composition-tools/04-VERIFICATION.md (lines 6-25 — exact gap description for HK-03)
    - tests/ (sample an existing tests/test_humanize*.flow if present for shape)
    - flow-lang.Tests/Phase35/ (Plan 35-01 created this folder — confirm xUnit fact shape)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 508-521 Pitfall 8 HK-01 root cause + verification approach; lines 845-848 HK-03 verification commands)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 1004-1101 Bucket 5)
  </read_first>
  <behavior>
    - Test 1 (HumanizeGaussianVoiceBlocksTests.HumanizeOverVoiceBlockProducesNonEmptyWav): Construct a Sequence containing a bar with ParallelVoices, apply humanizeGaussian with amount=0.03 and seed=314, render to WAV via writeWav, assert resulting file is > 44 bytes AND contains at least one non-zero PCM sample.
    - Test 2 (HumanizeGaussianVoiceBlocksTests.HumanizeOverVoiceBlockIsDeterministic): Same fixture as Test 1 but render twice to two temp paths; assert File.ReadAllBytes(a).SequenceEqual(File.ReadAllBytes(b)) — two-run byte-identical determinism preserved per CLAUDE.md Conventions.
    - Test 3 (MutateRhythmEnumValuesTests.WholeMutatesToHalf): Call MutateRhythm with a NoteValueType.WHOLE note, assert returned note's value enum is NoteValueType.HALF (per VERIFICATION.md case 0 => 1).
    - Test 4 (MutateRhythmEnumValuesTests.HalfMutatesToQuarter / QuarterToEighth / EighthToSixteenth): three additional facts mirroring Test 3 — case 1 => 2, case 2 => 3, case 3 => 4 per 04-VERIFICATION.md.
    - Test 5 (.flow regression — tests/test_humanize_voice_block.flow): Pure-Flow script that constructs a 2-voice block, applies (humanizeGaussian seq 0.03 42), renders to a temp WAV path via (writeWav ...), prints PASS if the run completes without error (per CLAUDE.md "verified by their console output (success = no errors)" convention).
  </behavior>
  <action>
    Create the two xUnit fact files in `flow-lang.Tests/Phase35/`. Mirror the existing class shape used elsewhere in flow-lang.Tests/. For HumanizeGaussianVoiceBlocksTests, use FlowEngine to evaluate a fixture Flow source that builds the voice-block sequence (cleaner than constructing SequenceData / BarData / ParallelVoices manually in C#) — pattern after any existing flow-lang.Tests/ class that evaluates a Flow source and inspects the resulting Value or rendered Buffer. For MutateRhythmEnumValuesTests, either call MutateRhythm directly (if its signature is accessible — VariationFunctions is in flow-lang) or wrap it via FlowEngine.Execute of a `(vary ...)` call and inspect the returned sequence's NoteValue.

    Create `tests/test_humanize_voice_block.flow` per PATTERNS.md Bucket 5 §tests/test_humanize_voice_block.flow — a small .flow script that builds a Sequence with `{voice C4q D4q}{voice E4h}` bar via the existing note-stream + voice-block syntax (per CLAUDE.md Phase 28 voice-block reference), applies `(humanizeGaussian seq 0.03 42)`, writes the result to `examples/output/test_humanize_voice_block.wav` (gitignored output dir per CLAUDE.md), and prints "PASS" on completion.

    All three test assets MUST initially FAIL (HK-01 fact: WAV will be ~44 bytes pre-fix; HK-03 facts: MutateRhythm switch returns wrong enum values pre-fix). The .flow script's RED signal is the WAV size, not the script exit code — for now, the script should also contain an inline check that prints FAIL if the WAV is undersize so the run-loop catches it.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5 &amp;&amp; dotnet test --filter "FullyQualifiedName~HumanizeGaussianVoiceBlocks|FullyQualifiedName~MutateRhythmEnumValues" 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: flow-lang.Tests/Phase35/HumanizeGaussianVoiceBlocksTests.cs exists with at least 2 [Fact] methods (NonEmptyWav + Deterministic)
    - Source: flow-lang.Tests/Phase35/MutateRhythmEnumValuesTests.cs exists with at least 4 [Fact] methods (one per WHOLE/HALF/QUARTER/EIGHTH transition)
    - Source: tests/test_humanize_voice_block.flow exists and contains a `{voice` voice-block construction + `humanizeGaussian` call + `writeWav` call
    - Behavior: dotnet build succeeds (tests compile)
    - Behavior: dotnet test --filter "FullyQualifiedName~HumanizeGaussianVoiceBlocks" reports FAIL on at least one fact (expected RED — bug not yet fixed)
    - Behavior: dotnet test --filter "FullyQualifiedName~MutateRhythmEnumValues" reports FAIL on at least one fact (expected RED — switch returns wrong values pre-fix)
  </acceptance_criteria>
  <done>Three regression test artifacts exist; at least one xUnit fact per HK item fails as expected RED; HK-01 .flow regression script in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HK-01 + HK-03 source fixes — humanizeGaussian recurses into ParallelVoices; MutateRhythm switch uses NoteValueType enum integers</name>
  <files>flow-lang/StandardLibrary/Transforms/TransformFunctions.cs, flow-lang/StandardLibrary/Composition/VariationFunctions.cs</files>
  <read_first>
    - flow-lang/StandardLibrary/Transforms/TransformFunctions.cs (full HumanizeGaussian function at lines 931-962 — apply PATTERNS.md Bucket 5 fix)
    - flow-lang/StandardLibrary/Audio/BarRenderer.cs:62-77 (canonical ParallelVoices recursion to mirror)
    - flow-lang/TypeSystem/SpecialTypes/NoteType.cs (find NoteValueType enum definition — confirm WHOLE=0, HALF=1, QUARTER=2, EIGHTH=3, SIXTEENTH=4)
    - flow-lang/StandardLibrary/Composition/VariationFunctions.cs (focus on MutateRhythm switch at line 253 per 04-VERIFICATION.md)
    - flow-lang/StandardLibrary/Audio/MidiExport.cs:467-470 + flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs:182-184 (two additional in-codebase ParallelVoices recursion precedents per PATTERNS.md Shared Patterns)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 508-521 Pitfall 8 — the fix description + verification approach)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 1004-1097 Bucket 5 §HK-01 + §HK-03)
  </read_first>
  <action>
    Fix HK-01 in `flow-lang/StandardLibrary/Transforms/TransformFunctions.cs:931-962`. Per PATTERNS.md Bucket 5 §HK-01 Apply section: restructure HumanizeGaussian to mirror the BarRenderer.cs:62-77 recursion. Concretely:
    1. Before iterating `bar.MusicalNotes`, check if `bar.ParallelVoices != null && bar.ParallelVoices.Count > 0` and, when true, recurse into each voice sub-bar (each voiceBar is itself a BarData with its own MusicalNotes list).
    2. Each recursion must reuse the SAME seeded `Random rng` instance (do NOT seed a per-voice RNG) so the composer-visible determinism contract holds per RESEARCH §Pitfall 8 Notable Departures + Phase 18/25 byte-identical determinism inheritance.
    3. Construct the output bar preserving ParallelVoices — either pass the recursively-humanized voice sub-bars through to the BarData constructor (verify the constructor accepts ParallelVoices) OR assign via the existing mutable property at BarType.cs:76 after construction.
    4. When the bar has no ParallelVoices, keep the existing MusicalNotes-only path unchanged.

    Fix HK-03 in `flow-lang/StandardLibrary/Composition/VariationFunctions.cs:253`. Per 04-VERIFICATION.md gap description + PATTERNS.md Bucket 5 §HK-03 Apply section: change the MutateRhythm switch case values to use NoteValueType enum integers — case 0 => 1 (WHOLE->HALF), case 1 => 2 (HALF->QUARTER), case 2 => 3 (QUARTER->EIGHTH), case 3 => 4 (EIGHTH->SIXTEENTH).

    Verify NoteValueType enum mapping by reading NoteType.cs before editing. If the actual enum values differ from RESEARCH/VERIFICATION's listed integers, follow the ACTUAL enum (the source of truth) — but the semantic intent is "each rhythm value mutates to the next-shorter division" per VERIFICATION.md.

    Do NOT touch any other functions or files in this task. The two fixes are independent and disjoint (Transforms vs Composition).
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~HumanizeGaussianVoiceBlocks|FullyQualifiedName~MutateRhythmEnumValues" 2>&amp;1 | tail -15 &amp;&amp; dotnet run --project flow-interpreter tests/test_humanize_voice_block.flow 2>&amp;1 | tail -10 &amp;&amp; ls -la examples/output/test_humanize_voice_block.wav 2>&amp;1 | awk '{print "WAV-SIZE-BYTES:", $5}'</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: dotnet test --filter "FullyQualifiedName~HumanizeGaussianVoiceBlocks" exits 0 (both facts GREEN — voice block WAV > 44 bytes AND deterministic across two runs)
    - Behavior: dotnet test --filter "FullyQualifiedName~MutateRhythmEnumValues" exits 0 (all 4 enum-transition facts GREEN)
    - Behavior: dotnet run --project flow-interpreter tests/test_humanize_voice_block.flow exits 0 AND the produced WAV is > 44 bytes (size logged via the ls -la | awk pipeline)
    - Source: grep -c "ParallelVoices" flow-lang/StandardLibrary/Transforms/TransformFunctions.cs returns at least 2 (the new branch + the constructor preservation)
    - Source: VariationFunctions.cs:253 switch contains the corrected enum mappings; grep -A 8 "MutateRhythm" flow-lang/StandardLibrary/Composition/VariationFunctions.cs returns the new case statements
    - Done: HK-01 + HK-03 source fixes land; regression tests pass GREEN; voice-block WAV size proves the bug is closed
  </acceptance_criteria>
  <done>HumanizeGaussian recurses into ParallelVoices with shared seeded Random; MutateRhythm switch corrected to NoteValueType integers; both HK xUnit fact sets pass GREEN; voice-block .flow script produces non-empty WAV.</done>
</task>

<task type="auto">
  <name>Task 3: HK-02 + HK-03 + HK-04 documentation closures — REQUIREMENTS.md / 17-HUMAN-UAT.md / 04-VERIFICATION.md / CLAUDE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/17-flow-language-server/17-HUMAN-UAT.md, .planning/phases/04-composition-tools/04-VERIFICATION.md, CLAUDE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full Phase 35 housekeeping block at lines 37-42; COMP-01 / COMP-02 entries — search for `COMP-01` to find their line numbers)
    - .planning/phases/17-flow-language-server/17-HUMAN-UAT.md (full file — confirm rows 1-3 already show [pass-via-phase-31-uat] / closed_via: Phase 31 Plan 31-08 UAT per RESEARCH Assumption A9)
    - .planning/phases/04-composition-tools/04-VERIFICATION.md (lines 6-25 — gap structure; the file's status: field at top)
    - CLAUDE.md (search for `Public as of v1.4` block — currently states the deprecation rule that is no longer in effect per HK-04 / D-v1.5-01)
    - /home/noah/.claude/projects/-home-noah-Desktop-projects-flow-sharp/memory/project_pre_public_no_legacy_burden.md (rewritten target framing for HK-04 — Read this file at the absolute path to extract the canonical pre-traction-no-deprecation prose; treat as the verbatim alignment target per PATTERNS.md Bucket 5)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 1066-1110 Bucket 5 §HK-02 / §HK-03 / §HK-04)
  </read_first>
  <action>
    HK-02 closure (documentation-only per RESEARCH Assumption A9):
    1. In .planning/REQUIREMENTS.md line 40, flip the HK-02 checkbox from `- [ ] **HK-02**` to `- [x] **HK-02**` and append a note: Closed via Phase 31 Plan 31-08 UAT (cross-reference: 17-HUMAN-UAT.md rows 1-3 status [pass-via-phase-31-uat]).
    2. In .planning/phases/17-flow-language-server/17-HUMAN-UAT.md, if a frontmatter status: field is present and not yet closed, append a note in the relevant rows or near the frontmatter clarifying Plan 35-02 confirms closure. If rows 1-3 already show [pass-via-phase-31-uat], leave the row entries alone and only add a header-level cross-reference comment so this closure is auditable.

    HK-03 closure (source fix already landed in Task 2; this task is the docs surface):
    1. In .planning/REQUIREMENTS.md, flip the COMP-01 and COMP-02 checkboxes from `- [ ]` to `- [x]` (per 04-VERIFICATION.md gap entry — the file gives the EXACT line numbers; verify them at edit time). Also flip the HK-03 checkbox at line 41.
    2. In .planning/phases/04-composition-tools/04-VERIFICATION.md, change the top-of-file `status: gaps_found` to `status: verified` AND append a closure note referencing Plan 35-02 + the MutateRhythmEnumValuesTests fact class.

    HK-04 closure (CLAUDE.md prose rewrite):
    1. Read the rewritten project_pre_public_no_legacy_burden.md external memory at the absolute path noted in read_first — extract the canonical pre-traction-no-deprecation framing.
    2. In CLAUDE.md, locate the `Public as of v1.4` footnote (the existing block currently asserts that project_pre_public_no_legacy_burden no longer applies).
    3. Rewrite the footnote prose to match the rewritten memory's framing: pre-traction no-deprecation latitude is STILL ACTIVE per D-v1.5-01; breaking syntax/builtin changes ship in single commits with in-repo migrators; no `flow migrate` CLI subcommand required yet. Keep the link to the external memory file. Keep the `Note (Public as of v1.4):` heading. Use the phrase "pre-traction" verbatim so the acceptance grep matches.
    4. In REQUIREMENTS.md, flip the HK-04 checkbox at line 42 from `- [ ]` to `- [x]`.

    Do NOT touch any source code, audio renders, or other planning artifacts in this task. The edits above are the entirety of the documentation closure work for HK-02 / HK-03 / HK-04.
  </action>
  <verify>
    <automated>grep -cE "\\[x\\] \\*\\*HK-0[1-4]" .planning/REQUIREMENTS.md &amp;&amp; grep -cE "\\[x\\] \\*\\*COMP-0[12]" .planning/REQUIREMENTS.md &amp;&amp; grep -c "status: verified" .planning/phases/04-composition-tools/04-VERIFICATION.md &amp;&amp; grep -c "pre-traction" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -cE "\\[x\\] \\*\\*HK-0[1-4]" .planning/REQUIREMENTS.md returns 4 (all four HK checkboxes flipped)
    - Source: grep -cE "\\[x\\] \\*\\*COMP-0[12]" .planning/REQUIREMENTS.md returns 2 (both COMP checkboxes flipped per 04-VERIFICATION.md gap closure)
    - Source: grep -c "status: verified" .planning/phases/04-composition-tools/04-VERIFICATION.md returns at least 1
    - Source: grep -c "pre-traction" CLAUDE.md returns at least 1 (HK-04 footnote rewrite landed)
    - Source: grep -c "Phase 31 Plan 31-08" .planning/REQUIREMENTS.md returns at least 1 (HK-02 cross-reference recorded)
    - Done: All four HK items closed; planning artifacts and CLAUDE.md reflect the post-v1.4 closure state
  </acceptance_criteria>
  <done>REQUIREMENTS.md HK-01..04 + COMP-01..02 checkboxes flipped; 04-VERIFICATION.md status flipped to verified; CLAUDE.md footnote rewritten to pre-traction framing; HK-02 cross-reference to Phase 31 recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All four HK items are internal — three source bug fixes/docs and one prose rewrite. No untrusted-input crossings introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Integrity | HumanizeGaussian determinism contract | mitigate | Task 2 reuses SAME seeded Random across voice recursion; HumanizeGaussianVoiceBlocksTests.HumanizeOverVoiceBlockIsDeterministic asserts byte-identical two-run output. Phase 18/25 byte-identical sentinels preserved. |
| T-35-05 | Integrity | MutateRhythm enum mapping | mitigate | Task 2 corrects the switch to NoteValueType enum integers; Task 1's 4 xUnit facts pin each WHOLE/HALF/QUARTER/EIGHTH transition. |
| T-35-06 | Information Disclosure | CLAUDE.md footnote prose | accept | Project documentation — read by the developer + Claude only. No data exposure surface. |
</threat_model>

<verification>
- xUnit Phase35 HK facts pass: `dotnet test --filter "FullyQualifiedName~HumanizeGaussianVoiceBlocks|FullyQualifiedName~MutateRhythmEnumValues"` exits 0
- .flow script regression: `dotnet run --project flow-interpreter tests/test_humanize_voice_block.flow` exits 0 with WAV > 44 bytes
- Full xUnit suite remains green: `dotnet test` exits 0
- All .flow scripts regression: `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t"; done` — every script exits 0
- Documentation closure greps: 4 HK checkboxes + 2 COMP checkboxes + `status: verified` + `pre-traction` all match per Task 3 acceptance
</verification>

<success_criteria>
Plan complete when:
- [ ] HumanizeGaussian recurses into ParallelVoices with shared seeded Random
- [ ] MutateRhythm switch corrected to NoteValueType enum integers
- [ ] HumanizeGaussianVoiceBlocksTests + MutateRhythmEnumValuesTests pass GREEN
- [ ] tests/test_humanize_voice_block.flow produces > 44-byte WAV
- [ ] REQUIREMENTS.md HK-01..04 + COMP-01..02 checkboxes all flipped to [x]
- [ ] 04-VERIFICATION.md status: gaps_found -> status: verified
- [ ] CLAUDE.md "Public as of v1.4" footnote rewritten with pre-traction framing
- [ ] HK-02 cross-reference to Phase 31 Plan 31-08 recorded
- [ ] Full xUnit suite + all .flow scripts remain GREEN
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-02-SUMMARY.md` when done with: HK-01 fix description (which BarData ParallelVoices assignment path was chosen), HK-03 enum-value confirmation (NoteValueType integer values), HK-02 closure mechanism (cross-reference text), HK-04 footnote diff summary, and the full-suite + .flow regression test results.
</output>
