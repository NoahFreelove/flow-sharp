---
phase: 35-language-foundation
plan: 06
type: execute
wave: 4
depends_on: ["35-01", "35-03", "35-04", "35-05"]
files_modified:
  - flow-lang/Interpreter/PatternMatcher.cs
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang/Parsing/Parser.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang/Ast/Expressions/MatchExpression.cs
  - flow-lang.Tests/Phase35/MusicAwarePatternsTests.cs
  - flow-lang.Tests/Phase35/MatchExhaustivenessDefaultTests.cs
  - flow-lang.Tests/Phase35/MatchExhaustivePragmaTests.cs
  - flow-lang.Tests/Phase35/PragmaScopeTests.cs
  - tests/test_pattern_match_music.flow
  - tests/test_match_exhaustive_pragma.flow
autonomous: true
requirements: [LANG-02]
must_haves:
  truths:
    - "LANG-02: ChordLiteralExpression token in a pattern position becomes ConstructorPattern with IsChordLiteral=true; MatchConstructor parses scrutinee via ChordParser.Parse + compares Quality string match. Example: `(match c | Cmaj7 => \"I\" | Dm7 => \"ii\" | _ => \"other\")`."
    - "LANG-02: Roman-numeral token in a pattern position becomes ConstructorPattern with IsRomanNumeral=true; MatchConstructor resolves via HarmonyFunctions.resolveNumeral against the active key from MusicalContext; matches Chord scrutinees by structural equality of the resolved chord. Example: `(match c | V7 => \"dominant\" | I => \"tonic\" | _ => \"other\")`."
    - "LANG-02: Symbol literal (#staccato / #legato / #accent / etc.) in a pattern position becomes ConstructorPattern with IsArticulationSymbol=true; MatchConstructor compares against Note.Articulation enum value. Example: `(match n | #staccato => \"short\" | #legato => \"smooth\" | _ => \"normal\")`."
    - "LANG-02: pitch-class matching uses guard pattern (no dedicated extractor) — `| n when (= (pitchClass n) 0) => \"C\"`. Verified composable with the GuardPattern dispatch from Plan 35-05."
    - "D-v1.5-05 charitable interpretation: non-exhaustive match WARN to stderr via RenderingDiagnostics.WarnOnce(sentinelKey: $\"match-non-exhaustive:{Span}\") + fall through to Value.Void(). One-shot dedup per match Span."
    - "D-v1.5-05 strict mode: `enable matchExhaustive;` pragma promotes the WARN to an ErrorReporter FlowDiagnostic via Plan 35-03 diagnostic infrastructure; evaluator queries ExecutionContext.ProgramPragmaSet.Has(\"matchExhaustive\") (matchExhaustive entry registered in PragmaRegistry.KnownPragmas by Plan 35-03)."
    - "Pitfall 4: pragma is per-file — does NOT propagate via `use` imports. Each imported file independently opts into strict mode. Verified by PragmaScopeTests (mod1 with pragma, mod2 without — mod2's matches stay charitable)."
    - "Plans 35-05's MatchRuntimeTests + tests/test_pattern_matching.flow still pass — Plan 35-06 ADDS behavior, doesn't replace."
  artifacts:
    - path: flow-lang/Interpreter/PatternMatcher.cs
      provides: "MatchConstructor extended with IsChordLiteral / IsRomanNumeral / IsArticulationSymbol dispatch branches per RESEARCH §Example 2 lines 615-630"
      contains: "IsChordLiteral|IsRomanNumeral|IsArticulationSymbol"
    - path: flow-lang/Interpreter/ExpressionEvaluator.cs
      provides: "EvaluateMatch extended with D-v1.5-05 non-exhaustive policy — pragmaSet.Has(matchExhaustive) check + WARN vs FlowDiagnostic emit"
      contains: "matchExhaustive"
    - path: flow-lang/Parsing/Parser.cs
      provides: "ParsePattern recognizes ChordLiteral token (IsChordLiteral=true), roman-numeral identifier (IsRomanNumeral=true via ChordParser.IsRomanNumeral), Symbol token (IsArticulationSymbol=true)"
      contains: "IsChordLiteral|IsRomanNumeral|IsArticulationSymbol"
    - path: flow-lang/Runtime/ExecutionContext.cs
      provides: "ProgramPragmaSet property exposing the active PragmaSet to the evaluator (per PATTERNS.md Bucket 3 §ExecutionContext.cs)"
      contains: "ProgramPragmaSet"
    - path: flow-lang/Ast/Expressions/MatchExpression.cs
      provides: "MatchExpression optionally carries its parse-time PragmaSet for evaluator query — alternative to ExecutionContext-level threading (per PATTERNS.md Bucket 3 §ExecutionContext.cs Notable Departures planner-decides)"
      contains: "PragmaSet|matchExhaustive"
    - path: tests/test_pattern_match_music.flow
      provides: "Composer-facing acceptance — chord-quality + roman-numeral + articulation extractors via (test ...) framework"
      min_lines: 25
    - path: tests/test_match_exhaustive_pragma.flow
      provides: "Composer-facing acceptance — `enable matchExhaustive;` pragma promotes non-exhaustive WARN to error"
      min_lines: 15
  key_links:
    - from: flow-lang/Interpreter/PatternMatcher.cs
      to: flow-lang/StandardLibrary/Harmony/ChordParser.cs
      via: "MatchChordQuality + MatchRomanNumeral reuse ChordParser.IsChordSymbol / ChordParser.Parse for structural matching"
      pattern: "ChordParser"
    - from: flow-lang/Interpreter/PatternMatcher.cs
      to: flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
      via: "MatchRomanNumeral calls HarmonyFunctions.resolveNumeral with the active key from MusicalContext"
      pattern: "resolveNumeral|HarmonyFunctions"
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Diagnostics/RenderingDiagnostics.cs
      via: "Non-exhaustive WARN emits via RenderingDiagnostics.WarnOnce(sentinel: match-non-exhaustive:{Span}, message)"
      pattern: "RenderingDiagnostics|WarnOnce"
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Lexing/PragmaRegistry.cs
      via: "Evaluator queries pragmaSet.Has(matchExhaustive) — entry registered in Plan 35-03"
      pattern: "matchExhaustive"
---

<objective>
Layer the music-aware pattern extractors (LANG-02) + non-exhaustive policy (D-v1.5-05) on top of Plan 35-05's pattern-matching foundation. Three constructor-pattern discriminator flags (IsChordLiteral / IsRomanNumeral / IsArticulationSymbol) drive specialized matching against Chord values (structural Quality-string comparison via ChordParser), Chord values resolved from the active key context (HarmonyFunctions.resolveNumeral lookup), and Note values' Articulation enum (Symbol-literal equality). Pitch-class matching is intentionally guard-based per LANG-02 wording — no new extractor needed; reuses GuardPattern. Non-exhaustive matches WARN to stderr by default (charitable interpretation per D-v1.5-05) and promote to errors when the file enables the `enable matchExhaustive;` pragma (entry already registered in Plan 35-03; per-file scope per Pitfall 4).

Purpose: LANG-02 — music-aware pattern extractors + D-v1.5-05 non-exhaustive policy. Plan 35-05 shipped the structural machinery; this plan adds the music citizenship + the exhaustiveness policy that completes LANG-01's REQUIREMENTS.md text ("non-exhaustive matches WARN to stderr and fall through to Void; enable matchExhaustive pragma promotes warnings to errors").

Output: ParsePattern extended for ChordLiteral / roman-numeral / Symbol tokens; PatternMatcher.MatchConstructor extended with 3 music-aware branches; EvaluateMatch's non-exhaustive path gains the WARN-vs-error policy; ExecutionContext exposes ProgramPragmaSet for query; 4 xUnit fact classes + 2 composer-facing .flow regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@.planning/phases/35-language-foundation/35-03-SUMMARY.md
@.planning/phases/35-language-foundation/35-04-SUMMARY.md
@.planning/phases/35-language-foundation/35-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Music harmony surface — reused for music-aware pattern extractors. -->

From flow-lang/StandardLibrary/Harmony/ChordParser.cs:
- public static bool IsChordSymbol(string text) at line 200 — recognizes "Cmaj7", "Dm", "F#dim", "Bb7"
- public static ChordData Parse(string text) at line 258 — returns ChordData with Root, Quality, Notes
- ChordData record has Root (string), Quality (string), Notes (list)

From flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs:
- resolveNumeral(string roman, string key) — resolves "I", "ii", "V7", "vi" against a key context; returns ChordData. Already correct from Phase 23 microtonal work.

From flow-lang/Runtime/MusicalContext.cs:
- Active key accessible via the existing MusicalContext stack — find the API for reading the current key (e.g., context.CurrentKey or analogous).

From flow-lang/TypeSystem/SpecialTypes/NoteType.cs (Articulation enum from Phase 28):
- Articulation values: Default, Accent, Staccato, Tenuto, Marcato, Legato, Sforzando

From flow-lang/Lexing/PragmaSet.cs:
- Has(string name) — query whether a pragma is enabled for the file
- PragmaSet.Empty static for default state

From flow-lang/Lexing/PragmaRegistry.cs (after Plan 35-03):
- KnownPragmas dict now contains matchExhaustive entry per Plan 35-03 Task 2.

From flow-lang/Runtime/ModuleLoader.cs:77-94:
- Per-import PragmaSet isolation — each imported file gets its own PragmaSet; pragmas do NOT propagate via `use` (Pitfall 4).

From flow-lang/Diagnostics/RenderingDiagnostics.cs:29-36:
- WarnOnce(sentinelKey, message) — one-shot stderr advisory with per-sentinel dedup.

From flow-lang/Diagnostics/FlowDiagnostic.cs (Plan 35-03 output):
- Static factories Create/Warning/Info; renderable via DiagnosticRenderer.

From Plan 35-05:
- ConstructorPattern has IsChordLiteral / IsRomanNumeral / IsArticulationSymbol init-only flags (default false).
- PatternMatcher.MatchConstructor currently falls back to structural equality — Plan 35-06 extends with the 3 music-aware branches.
- EvaluateMatch currently returns Value.Void() silently on non-exhaustive — Plan 35-06 adds the WARN + pragma check.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing test stubs for music-aware extractors + non-exhaustive default + matchExhaustive pragma + per-file pragma scope</name>
  <files>flow-lang.Tests/Phase35/MusicAwarePatternsTests.cs, flow-lang.Tests/Phase35/MatchExhaustivenessDefaultTests.cs, flow-lang.Tests/Phase35/MatchExhaustivePragmaTests.cs, flow-lang.Tests/Phase35/PragmaScopeTests.cs, tests/test_pattern_match_music.flow, tests/test_match_exhaustive_pragma.flow</files>
  <read_first>
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 73-86 Wave 3 table — final 4 rows for exhaustiveness + pragma scope)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 461-469 Pitfall 4; lines 615-629 Example 2 MatchConstructor dispatch for music extractors)
    - flow-lang/StandardLibrary/Harmony/ChordParser.cs (full file — pattern matching reuses IsChordSymbol + Parse)
    - flow-lang.Tests/Phase35/MatchRuntimeTests.cs (Plan 35-05 — meta-test pattern reference)
  </read_first>
  <behavior>
    - Test 1 (MusicAwarePatternsTests.ChordLiteralMatchesChordValueByQuality): `(match Cmaj7 | Cmaj7 => "match" | _ => "miss")` returns "match"; `(match Cmaj7 | Dm7 => "match" | _ => "miss")` returns "miss".
    - Test 2 (MusicAwarePatternsTests.ChordLiteralMatchesByQualityIgnoringRoot): `(match Cmaj7 | Dmaj7 => "match" | _ => "miss")` — depends on whether root-strict or quality-only is the canonical behavior. RESEARCH §Example 2 says "Chord literal (Cmaj7, Dm) matches Chord values by Root + Quality" — so this fact asserts "miss" (different root). DOCUMENT this in the SUMMARY and pin via test.
    - Test 3 (MusicAwarePatternsTests.RomanNumeralMatchesInKeyContext): `key Cmajor { (match (chord "G") | V => "dominant" | _ => "other") }` returns "dominant" (V in C major resolves to G).
    - Test 4 (MusicAwarePatternsTests.RomanNumeralRespectsKeyContextSwitch): Same `V` pattern matches differently in C vs G major (V is G in Cmajor, D in Gmajor). Two sub-tests confirm.
    - Test 5 (MusicAwarePatternsTests.ArticulationSymbolMatchesNoteArticulation): `(match (note "C4" articulation: #staccato) | #staccato => "short" | #legato => "smooth" | _ => "normal")` returns "short". For a non-articulated note returns "normal".
    - Test 6 (MusicAwarePatternsTests.PitchClassViaGuardComposes): `(match (note "C4") | n when (equals (pitchClass n) 0) => "C" | _ => "other")` returns "C" (pitch class of C4 = 0).
    - Test 7 (MatchExhaustivenessDefaultTests.NonExhaustiveDefaultWarnsAndReturnsVoid): Source `(match 5 | 1 => "one" | 2 => "two")` (no wildcard arm), match returns Void; stderr captured contains "warning: match expression" and "non-exhaustive".
    - Test 8 (MatchExhaustivenessDefaultTests.WarnDedupedPerMatchSpan): Same source evaluated 3× in one process — stderr contains the warning ONLY ONCE (Pitfall 5 RenderingDiagnostics.WarnOnce dedup behavior).
    - Test 9 (MatchExhaustivePragmaTests.PragmaPromotesWarnToError): Source `enable matchExhaustive;\n (match 5 | 1 => "one" | 2 => "two")` — match returns Void AND ErrorReporter.HasErrors is true; the diagnostic has Level=Error AND contains "non-exhaustive".
    - Test 10 (MatchExhaustivePragmaTests.WildcardArmSatisfiesExhaustiveness): Source `enable matchExhaustive;\n (match 5 | 1 => "one" | _ => "other")` — match returns "other"; no error emitted.
    - Test 11 (PragmaScopeTests.PragmaPerFileDoesNotPropagateViaUse): mod1.flow has `enable matchExhaustive;` + a non-exhaustive match (errors). mod2.flow `use "mod1";` (or similar; verify import API) + a non-exhaustive match — mod2's match WARNs only (no error), per Pitfall 4 + Phase 21 PRAG-02 precedent.
    - Test 12 (tests/test_pattern_match_music.flow): Composer-facing — at least 4 (test ...) blocks via Plan 35-04 framework covering chord-quality + roman-numeral (in key context) + articulation + pitch-class-via-guard. Runnable via flow test CLI.
    - Test 13 (tests/test_match_exhaustive_pragma.flow): Composer-facing — at least 2 (test ...) blocks covering charitable WARN (default) and strict error (pragma enabled).
  </behavior>
  <action>
    Create 4 xUnit fact files in `flow-lang.Tests/Phase35/`. Use FlowEngine.Execute to evaluate Flow source; capture stderr via redirected Console.Error; inspect engine.ErrorReporter.HasErrors / Diagnostics for the pragma-promoted error case.

    For chord-literal tests: construct ChordData scrutinees programmatically OR use Flow source `Chord c = Cmaj7;` then match against c.

    For roman-numeral tests: wrap matches in `key Cmajor { ... }` and `key Gmajor { ... }` blocks per the existing musical-context block syntax.

    For articulation tests: construct notes with Articulation enum via Flow source; pattern-match against #staccato / #legato symbol literals.

    For pitch-class guard test: relies on Plan 35-05's GuardPattern dispatch + an existing (pitchClass note) builtin. If pitchClass doesn't exist as a builtin, use the existing (mod (midi note) 12) idiom per CLAUDE.md.

    For non-exhaustive default WARN tests: redirect Console.Error to a StringWriter; execute the match source; assert stderr content. Use the existing RenderingDiagnostics.ResetForTesting() hook between tests to ensure dedup state doesn't bleed.

    For matchExhaustive pragma tests: prepend `enable matchExhaustive;\n` to the Flow source; assert ErrorReporter.HasErrors after evaluation.

    For per-file pragma scope test: write two .flow files to temp paths via File.WriteAllText; one with pragma + non-exhaustive match (expected ERROR); the other using `use` (per Plan 35-04 stdlib pattern) + non-exhaustive match (expected WARN-only). Assert via ErrorReporter introspection or stderr capture.

    Create composer-facing tests/test_pattern_match_music.flow + tests/test_match_exhaustive_pragma.flow per the PATTERNS.md test-fixture convention.

    All facts MUST initially fail (Plan 35-05 returns silent Void for non-exhaustive; music-aware extractors fall back to structural equality which doesn't match Chord/Numeral/Articulation correctly). Tasks 2 + 3 + 4 make them GREEN.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; ls flow-lang.Tests/Phase35/MusicAware*.cs flow-lang.Tests/Phase35/Match*Exhaust*.cs flow-lang.Tests/Phase35/PragmaScope*.cs tests/test_pattern_match_music.flow tests/test_match_exhaustive_pragma.flow 2>&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source: 4 new xUnit test files exist in flow-lang.Tests/Phase35/ matching the listed names
    - Source: 2 composer-facing .flow regression files exist in tests/
    - Behavior: dotnet build succeeds; tests RED (expected pre-implementation state)
    - Done: Wave 0 stubs land
  </acceptance_criteria>
  <done>4 xUnit fact classes + 2 .flow regressions in place; all RED pending Tasks 2-4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ParsePattern extension — recognize ChordLiteral / roman-numeral identifier / Symbol literal as discriminated ConstructorPattern</name>
  <files>flow-lang/Parsing/Parser.cs</files>
  <read_first>
    - flow-lang/Parsing/Parser.cs (focus on the ParsePattern method added in Plan 35-05 Task 3)
    - flow-lang/StandardLibrary/Harmony/ChordParser.cs:200,258 (IsChordSymbol + Parse)
    - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (verify IsRomanNumeral helper exists — if not, use a regex / direct ChordParser approach; the parser-time recognition needs SOME way to discriminate roman-numeral identifiers like "V7" or "vi" from regular identifiers)
    - flow-lang/Lexing/TokenType.cs (ChordLiteral token type — already exists from existing Flow chord literal support; also SymbolLiteral or similar)
    - flow-lang/Ast/Patterns/ConstructorPattern.cs (Plan 35-05 — init-only IsChordLiteral / IsRomanNumeral / IsArticulationSymbol flags)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 670-695 Bucket 3 §ConstructorPattern.cs Notable Departures — flag-based dispatch)
  </read_first>
  <behavior>
    - ParsePattern recognizes TokenType.ChordLiteral → ConstructorPattern(name=text, SubPatterns=[], IsChordLiteral=true). Plan 35-05's stub already covered this; extend if needed.
    - ParsePattern recognizes a roman-numeral identifier (matching the pattern `[ivxIVX]+[0-9]?` or via HarmonyFunctions's recognition helper) → ConstructorPattern(name=text, SubPatterns=[], IsRomanNumeral=true).
    - ParsePattern recognizes TokenType.SymbolLiteral (the `#name` syntax from Phase 26.1) → ConstructorPattern(name=symbolName, SubPatterns=[], IsArticulationSymbol=true). Note: SymbolLiteralExpression is the standard expression-position form; in a PATTERN position, it becomes a ConstructorPattern with the flag set.
    - MatchParserTests + Plan 35-05 facts remain GREEN (regression preserved).
  </behavior>
  <action>
    In `flow-lang/Parsing/Parser.cs`, extend the ParsePattern method (added in Plan 35-05 Task 3):
    1. When the current token is TokenType.ChordLiteral, consume and return `new ConstructorPattern(location, text, Array.Empty<Pattern>(), Span: ..., IsChordLiteral: true)`.
    2. When the current token is TokenType.Identifier AND its text is recognized as a roman numeral (call into a new helper `IsRomanNumeralLexeme(string text)` that checks regex `^[ivxIVX]+[0-9]?$` per Flow's existing roman-numeral conventions — or reuse a helper from HarmonyFunctions / ChordParser if one exists), consume and return ConstructorPattern with IsRomanNumeral=true.
    3. When the current token is TokenType.SymbolLiteral (the `#name` syntax — find the exact TokenType name in TokenType.cs), consume and return ConstructorPattern with IsArticulationSymbol=true and Name set to the symbol name (without the leading `#`).
    4. For all other identifier-position tokens, keep Plan 35-05's BindingPattern fallback (identifier captures the scrutinee).

    The roman-numeral lexeme test is a parse-time decision — it does NOT need access to the active key context (the key matters at MATCH time, not at PARSE time). The parser simply tags the pattern; PatternMatcher (Task 3) does the resolveNumeral lookup.

    Order matters: check ChordLiteral and SymbolLiteral tokens FIRST (they're distinct token types), then roman-numeral identifiers BEFORE the BindingPattern fallback (since both are identifier-positioned). When a name looks like a roman numeral AND was intended as a binding, the composer can disambiguate by wrapping in something more explicit — but per the locked LANG-02 design, roman-numeral form takes precedence in pattern position.

    Span on the new ConstructorPattern: Start = identifier token Location, End = identifier token Location + text.Length (per Plan 35-01 sweep convention).

    Do NOT touch PatternMatcher.cs in this task — Task 3 implements the music-aware match branches.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.MatchParserTests|FullyQualifiedName~Phase35.MusicAwarePatternsTests" 2>&amp;1 | tail -15 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "IsChordLiteral|IsRomanNumeral|IsArticulationSymbol" flow-lang/Parsing/Parser.cs returns at least 3 (one per flag)
    - Behavior: MatchParserTests continue GREEN (Plan 35-05 regression preserved)
    - Behavior: MusicAwarePatternsTests — chord-literal pattern facts may still RED (PatternMatcher needs Task 3 update) but the parser produces the correctly-flagged ConstructorPattern; verify by spot-checking the AST in a debug print test if needed
    - Behavior: Full dotnet test exits 0 (no regression from new ParsePattern branches)
    - Done: Parser recognizes the three music-aware pattern forms and tags ConstructorPattern with the appropriate discriminator flag
  </acceptance_criteria>
  <done>ParsePattern extended for ChordLiteral / roman-numeral / Symbol; ConstructorPattern flag set; AST produced correctly; PatternMatcher dispatch pending Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PatternMatcher.MatchConstructor extension — implement chord-quality / roman-numeral / articulation-symbol matching branches</name>
  <files>flow-lang/Interpreter/PatternMatcher.cs</files>
  <read_first>
    - flow-lang/Interpreter/PatternMatcher.cs (Plan 35-05 — MatchConstructor stub falls back to structural equality)
    - flow-lang/StandardLibrary/Harmony/ChordParser.cs:200,258 (IsChordSymbol + Parse)
    - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (resolveNumeral signature + key-context API)
    - flow-lang/Runtime/MusicalContext.cs (active-key accessor for resolveNumeral)
    - flow-lang/Runtime/ExecutionContext.cs (find how MusicalContext is exposed — likely via context.MusicalContext.CurrentKey or analogous)
    - flow-lang/TypeSystem/SpecialTypes/NoteType.cs (Articulation enum + Note.Articulation property access for #staccato matching)
    - flow-lang/Runtime/Value.cs (Value.As<ChordData>() / Value.As<MusicalNoteData>() unwrapping)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 615-629 Example 2 MatchConstructor dispatch — verbatim shape)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 757-781 Bucket 3 §PatternMatcher.cs)
  </read_first>
  <behavior>
    - MusicAwarePatternsTests all 6 facts pass GREEN (chord-quality by Root+Quality, roman-numeral with key context switch, articulation symbol, pitch-class via guard composes).
    - MatchConstructor signature accepts the ExpressionEvaluator (or its ExecutionContext) so it can read the active MusicalContext key for roman-numeral resolution.
    - Plan 35-05's MatchRuntimeTests + tests/test_pattern_matching.flow still pass (no regression on the generic / non-music match path).
  </behavior>
  <action>
    Extend `flow-lang/Interpreter/PatternMatcher.cs` MatchConstructor method per RESEARCH §Example 2 lines 615-629:

    ```
    private static bool MatchConstructor(ConstructorPattern ctor, Value scrutinee, Dictionary<string,Value> bindings, ExecutionContext context)
    {
        if (ctor.IsChordLiteral) return MatchChordQuality(ctor.Name, scrutinee);
        if (ctor.IsRomanNumeral) return MatchRomanNumeral(ctor.Name, scrutinee, context);
        if (ctor.IsArticulationSymbol) return MatchArticulation(ctor.Name, scrutinee);
        // Plan 35-05 generic fallback — structural equality on Name + sub-patterns
        return false; // or existing Plan 35-05 fallback
    }
    ```

    Implement the three helpers:
    - `MatchChordQuality(string chordText, Value scrutinee)`: Parse chordText via ChordParser.Parse → ChordData expected. Unwrap scrutinee via `scrutinee.As<ChordData>()`. If scrutinee.Root == expected.Root AND scrutinee.Quality == expected.Quality, return true. (Per RESEARCH §Example 2 the canonical behavior is Root+Quality match; pin this in MusicAwarePatternsTests.ChordLiteralMatchesByQualityIgnoringRoot's behavior expectation.)
    - `MatchRomanNumeral(string roman, Value scrutinee, ExecutionContext context)`: Read active key via context.MusicalContext.CurrentKey (or whatever the accessor is). Resolve `roman` to a ChordData via HarmonyFunctions.resolveNumeral(roman, key). Unwrap scrutinee.As<ChordData>(). Compare resolved.Root + Quality vs scrutinee.Root + Quality. Return true on match.
    - `MatchArticulation(string symbolName, Value scrutinee)`: scrutinee should be a MusicalNoteData; unwrap via scrutinee.As<MusicalNoteData>(). Parse symbolName → Articulation enum value (e.g., "staccato" → Articulation.Staccato; case-insensitive match). Return note.Articulation == expected.

    Plumb the ExecutionContext through. Plan 35-05's PatternMatches signature was `(pattern, scrutinee, bindings, evaluator)`. Pass the evaluator's context to MatchConstructor — extend the signature accordingly.

    DO NOT break Plan 35-05's generic ConstructorPattern fallback path — when no discriminator flag is set, the matcher returns false (or the existing structural fallback). Plan 35-05's MatchRuntimeTests + tests/test_pattern_matching.flow exercise non-music patterns; they must remain GREEN.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.MusicAwarePatternsTests|FullyQualifiedName~Phase35.MatchRuntimeTests" 2>&amp;1 | tail -15 &amp;&amp; dotnet run --project flow-cli -- test tests/test_pattern_match_music.flow 2>&amp;1 | tail -10 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: MusicAwarePatternsTests exits 0 (all 6 facts GREEN — chord literal by Root+Quality, roman numeral with key context, key-context switch, articulation symbol, pitch-class via guard)
    - Behavior: Plan 35-05 MatchRuntimeTests continue GREEN
    - Behavior: flow test tests/test_pattern_match_music.flow exits 0 with PASS summary
    - Behavior: Full dotnet test exits 0
    - Source: grep -c "MatchChordQuality|MatchRomanNumeral|MatchArticulation" flow-lang/Interpreter/PatternMatcher.cs returns at least 3
    - Done: Music-aware extractors operational; reuse existing ChordParser / HarmonyFunctions / Articulation machinery (no new music logic invented)
  </acceptance_criteria>
  <done>MatchConstructor dispatches to MatchChordQuality / MatchRomanNumeral / MatchArticulation branches; LANG-02 acceptance facts GREEN; composer-facing music-pattern .flow regression passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Non-exhaustive policy — EvaluateMatch WARN-vs-error policy + ExecutionContext.ProgramPragmaSet exposure</name>
  <files>flow-lang/Interpreter/ExpressionEvaluator.cs, flow-lang/Runtime/ExecutionContext.cs, flow-lang/Ast/Expressions/MatchExpression.cs</files>
  <read_first>
    - flow-lang/Interpreter/ExpressionEvaluator.cs (Plan 35-05 EvaluateMatch — currently silent Void on non-exhaustive)
    - flow-lang/Runtime/ExecutionContext.cs (Plan 35-04 added TestRegistry + SnapshotState — add ProgramPragmaSet here)
    - flow-lang/Runtime/ModuleLoader.cs:77-94 (per-file PragmaSet threading — find the integration point for ExecutionContext.ProgramPragmaSet)
    - flow-lang/Lexing/PragmaSet.cs (Has API)
    - flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce signature)
    - flow-lang/Diagnostics/FlowDiagnostic.cs (Plan 35-03 — static factories for error-level emit)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 587-598 Example 2 non-exhaustive policy; lines 461-469 Pitfall 4 pragma scope)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 886-895 Bucket 3 §ExecutionContext.cs — Notable Departures planner-decides on AST-vs-context pragma threading)
  </read_first>
  <behavior>
    - MatchExhaustivenessDefaultTests pass GREEN: non-exhaustive WARN to stderr, return Void, dedup per match Span.
    - MatchExhaustivePragmaTests pass GREEN: `enable matchExhaustive;` promotes WARN to error via ErrorReporter; wildcard arm satisfies exhaustiveness.
    - PragmaScopeTests passes GREEN: pragma per-file, does NOT propagate via use imports.
    - Plan 35-05 MatchRuntimeTests + Plan 35-06 MusicAwarePatternsTests all remain GREEN.
    - Composer-facing tests/test_match_exhaustive_pragma.flow passes via flow test CLI.
  </behavior>
  <action>
    Decision on pragma threading (per PATTERNS.md Bucket 3 §ExecutionContext.cs Notable Departures planner-decides): use the AST-carries-its-pragma-context approach — add a `PragmaSet? CapturedPragmas = null` field to MatchExpression (defaulted null for back-compat). The parser sets CapturedPragmas when constructing MatchExpression (it has access to the active per-file PragmaSet at parse time via the ParseSession). The evaluator queries `match.CapturedPragmas?.Has("matchExhaustive") ?? false`. This avoids the ExecutionContext-switching ambiguity per Pitfall 4 when match expressions imported via `use` are evaluated in the importing file's context.

    Update `flow-lang/Ast/Expressions/MatchExpression.cs` to add `PragmaSet? CapturedPragmas = null` as a defaulted-param (last positional, after Span — matching Plan 35-01's sweep convention with Span as terminal).

    Update Parser.cs ParseMatch: when constructing MatchExpression, set CapturedPragmas to the current ParseSession's PragmaSet (find the access pattern in Plan 21's pragma threading — Parser already has access via constructor).

    Update ExecutionContext.cs to add a `ProgramPragmaSet` property as a backup access point (some code paths may still want context-level pragma access; harmless to expose both).

    Update ExpressionEvaluator.cs EvaluateMatch per RESEARCH §Example 2 lines 587-598:
    ```
    // Non-exhaustive — D-v1.5-05 charitable interpretation
    bool strict = match.CapturedPragmas?.Has("matchExhaustive") ?? false;
    if (strict)
    {
        _errorReporter.Report(FlowDiagnostic.Create(
            $"match expression non-exhaustive — no arm matched scrutinee of type {scrutinee.Type}",
            match.Span ?? Span.At(match.Location)));
        return Value.Void();
    }
    RenderingDiagnostics.WarnOnce(
        $"match-non-exhaustive:{match.Span}",
        $"warning: match expression at {match.Span} non-exhaustive — fell through to Void");
    return Value.Void();
    ```

    Verify that the Plan 35-05 EvaluateMatch's `// Plan 35-06: ...` marker comment is replaced by the new policy logic.

    Do NOT regress Plan 35-05's MatchRuntimeTests — match expressions that DO have a matching arm continue to return the arm's body value unchanged.

    **Construction-site audit (post-edit, MANDATORY).** After adding `PragmaSet? CapturedPragmas = null` to MatchExpression, run `grep -n "new MatchExpression(" flow-lang/Parsing/Parser.cs` and confirm every existing call site (all written by Plan 35-05) still compiles with the new defaulted parameter. Defaulted positional params are additive — call sites that did NOT pass `CapturedPragmas` continue to compile unchanged. Use the grep output to confirm at least one call site exists in ParseMatch and that the build is GREEN before proceeding to the verify block. This is mirror-pattern to Plan 35-01 Task 3's grep-audit gates.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.MatchExhaustivenessDefaultTests|FullyQualifiedName~Phase35.MatchExhaustivePragmaTests|FullyQualifiedName~Phase35.PragmaScopeTests|FullyQualifiedName~Phase35.MatchRuntimeTests|FullyQualifiedName~Phase35.MusicAwarePatternsTests" 2>&amp;1 | tail -25 &amp;&amp; dotnet run --project flow-cli -- test tests/test_match_exhaustive_pragma.flow 2>&amp;1 | tail -10 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: MatchExhaustivenessDefaultTests exits 0 (WARN + Void + dedup all GREEN)
    - Behavior: MatchExhaustivePragmaTests exits 0 (pragma promotes WARN to error; wildcard satisfies exhaustiveness)
    - Behavior: PragmaScopeTests exits 0 (per-file pragma; no propagation via use)
    - Behavior: Plan 35-05 MatchRuntimeTests + Plan 35-06 MusicAwarePatternsTests still GREEN
    - Behavior: flow test tests/test_match_exhaustive_pragma.flow exits 0 with PASS summary
    - Behavior: Full dotnet test exits 0
    - Source: grep -c "matchExhaustive" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 1
    - Source: grep -c "WarnOnce" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 1
    - Source: grep -c "CapturedPragmas" flow-lang/Ast/Expressions/MatchExpression.cs flow-lang/Parsing/Parser.cs returns at least 2
    - Done: D-v1.5-05 policy operational — charitable WARN by default, strict error via pragma, per-file scope preserved
  </acceptance_criteria>
  <done>Non-exhaustive policy ships; CapturedPragmas threading per AST node avoids cross-file ambiguity; all 11 Wave 0 behaviors + Plan 35-05 facts GREEN; composer-facing regression passes via flow test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | LANG-02 + D-v1.5-05 are internal language extensions; no untrusted-input crossings introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-17 | Integrity | Roman-numeral key-context resolution correctness | mitigate | MusicAwarePatternsTests.RomanNumeralRespectsKeyContextSwitch verifies V resolves differently in C vs G major; reuses Phase 23-tested HarmonyFunctions.resolveNumeral. |
| T-35-18 | Integrity | Per-file pragma scope (Pitfall 4) | mitigate | PragmaScopeTests directly asserts mod1 enabling matchExhaustive does not affect mod2 imported via use; matches Phase 21 PRAG-02 existing semantics. |
| T-35-19 | Information Disclosure | Non-exhaustive WARN flooding stderr in REPL | mitigate | RenderingDiagnostics.WarnOnce per-Span dedup prevents flood; MatchExhaustivenessDefaultTests.WarnDedupedPerMatchSpan asserts dedup holds across repeated evaluation. |
</threat_model>

<verification>
- All Wave 0 Phase35 LANG-02 facts pass: `dotnet test --filter "FullyQualifiedName~Phase35.MusicAwarePatternsTests|FullyQualifiedName~Phase35.MatchExhaustivenessDefaultTests|FullyQualifiedName~Phase35.MatchExhaustivePragmaTests|FullyQualifiedName~Phase35.PragmaScopeTests"` exits 0
- Plan 35-05 facts preserved: `dotnet test --filter "FullyQualifiedName~Phase35.MatchLexerTests|FullyQualifiedName~Phase35.MatchParserTests|FullyQualifiedName~Phase35.PatternAstTests|FullyQualifiedName~Phase35.MatchRuntimeTests"` exits 0
- Composer acceptance: `flow test tests/test_pattern_match_music.flow tests/test_match_exhaustive_pragma.flow` exits 0
- Full xUnit suite + .flow regression remains green
</verification>

<success_criteria>
Plan complete when:
- [ ] ParsePattern recognizes ChordLiteral / roman-numeral / Symbol → ConstructorPattern with correct flag
- [ ] PatternMatcher.MatchConstructor dispatches IsChordLiteral / IsRomanNumeral / IsArticulationSymbol branches
- [ ] MatchChordQuality / MatchRomanNumeral / MatchArticulation helpers reuse ChordParser / HarmonyFunctions / Articulation enum (no new music logic invented)
- [ ] MatchExpression carries CapturedPragmas; Parser populates from ParseSession's PragmaSet
- [ ] EvaluateMatch non-exhaustive path: WARN via RenderingDiagnostics.WarnOnce by default; FlowDiagnostic error when matchExhaustive pragma set
- [ ] Per-file pragma scope preserved (Pitfall 4 + Phase 21 PRAG-02)
- [ ] 4 xUnit fact classes pass GREEN
- [ ] 2 composer-facing .flow regressions pass via flow test
- [ ] Plan 35-05 facts + full xUnit suite + .flow scripts remain GREEN
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-06-SUMMARY.md` when done with: the 3 music-aware match-branch behaviors (Root+Quality vs Quality-only documented), key-context dependence test confirmation, exhaustiveness policy summary (WARN default + strict pragma), per-file scope behavior, and confirmation that LANG-01 + LANG-02 are now fully closed for v1.5.
</output>
