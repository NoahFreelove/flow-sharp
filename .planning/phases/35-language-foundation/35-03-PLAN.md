---
phase: 35-language-foundation
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - flow-lang/Diagnostics/FlowDiagnostic.cs
  - flow-lang/Diagnostics/DiagnosticRenderer.cs
  - flow-lang/Diagnostics/LevenshteinHelper.cs
  - flow-lang/Diagnostics/ErrorReporter.cs
  - flow-lang/Lexing/PragmaRegistry.cs
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang/Parsing/Parser.cs
  - flow-interpreter/Program.cs
  - flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs
  - flow-lang.Tests/Phase35/LevenshteinSuggestionTests.cs
  - flow-lang.Tests/Phase35/DiagnosticTtyTests.cs
  - flow-lang.Tests/Phase35/MultiErrorRenderingTests.cs
  - flow-lang.Tests/Phase35/ReplDiagnosticTests.cs
  - flow-lang.Tests/baselines/Phase35/diagnostics/.gitkeep
autonomous: true
requirements: [LANG-04]
must_haves:
  truths:
    - "Rust-style multi-line diagnostic rendering: header line (error: <msg>), location line (--> file:line:col), pipe-prefixed source quote with line number, caret line sized to Span width, optional secondary `note:` lines, optional `help: did you mean '<suggestion>'?` line per RESEARCH §Example 4."
    - "Did-you-mean Levenshtein suggestion: closest match in scope within max(2, typed.Length / 3) distance per Pitfall 5; ties broken by longest common prefix then alphabetical; ONE suggestion only."
    - "ANSI color when emitting to TTY (Console.ForegroundColor); plain text when output redirected (.NET auto-handles per Program.cs:77 precedent — no explicit IsOutputRedirected check)."
    - "REPL/eval source (no file path) renders via SourceMap sentinel-keys `<eval>`, `<stdin>`, `<repl>` — the renderer reads source from the per-FlowEngine SourceMap registered in Plan 35-01."
    - "Multi-error rendering: ErrorReporter accumulates FlowDiagnostic[]; FormatDiagnostics(SourceMap) routes each through DiagnosticRenderer with blank-line separators per RESEARCH §Validation Architecture row 4."
    - "ExpressionEvaluator unknown-identifier error queries StackFrame.GetAllAccessibleVariables for the did-you-mean candidate set (PATTERNS.md Bucket 2a §ExpressionEvaluator.cs)."
    - "Legacy path preserved: FlowError + FormatErrors() unchanged; Parser/Evaluator sites that don't have rich span info keep emitting FlowError; new emit sites use FlowDiagnostic. Both collections coexist mid-migration (PATTERNS.md Bucket 2a §ErrorReporter.cs Notable Departures)."
  artifacts:
    - path: flow-lang/Diagnostics/FlowDiagnostic.cs
      provides: "Rich diagnostic record (Level, Message, Span Primary, Labels, Notes, Suggestion?) + DiagnosticLabel sub-record + static factories mirroring FlowError.Create / Warning / Info"
      contains: "FlowDiagnostic"
    - path: flow-lang/Diagnostics/DiagnosticRenderer.cs
      provides: "Static Render(FlowDiagnostic, SourceMap) producing multi-line rust-style string per RESEARCH §Example 4 format spec"
      contains: "DiagnosticRenderer"
    - path: flow-lang/Diagnostics/LevenshteinHelper.cs
      provides: "Lifted-from-PragmaRegistry public Levenshtein + SuggestNearest(typed, IEnumerable<string> candidates, int? threshold = null)"
      contains: "LevenshteinDistance"
    - path: flow-lang.Tests/baselines/Phase35/diagnostics/
      provides: "Directory housing golden diagnostic-rendering baselines (ANSI-stripped plain text)"
      contains: ""
    - path: flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs
      provides: "Golden-file tests for the Rust-style format (header + location + source quote + caret + label + note + suggestion)"
      contains: "DiagnosticRendererGoldenTests"
    - path: flow-lang.Tests/Phase35/LevenshteinSuggestionTests.cs
      provides: "Tests for did-you-mean threshold + tie-breaking behavior"
      contains: "LevenshteinSuggestion"
    - path: flow-lang.Tests/Phase35/DiagnosticTtyTests.cs
      provides: "Tests that color emission obeys Console.ForegroundColor only when not redirected"
      contains: "DiagnosticTty"
    - path: flow-lang.Tests/Phase35/MultiErrorRenderingTests.cs
      provides: "Tests that ErrorReporter.FormatDiagnostics renders each diagnostic separately with blank-line separator"
      contains: "MultiErrorRendering"
    - path: flow-lang.Tests/Phase35/ReplDiagnosticTests.cs
      provides: "Tests that REPL/eval sentinel-keyed SourceMap entries render properly (no file path)"
      contains: "ReplDiagnostic"
  key_links:
    - from: flow-lang/Diagnostics/DiagnosticRenderer.cs
      to: flow-lang/Core/SourceMap.cs
      via: "Render(diagnostic, sourceMap) retrieves source text via sourceMap.TryGetSource(diagnostic.Primary.Start.FileName)"
      pattern: "SourceMap|TryGetSource"
    - from: flow-lang/Diagnostics/DiagnosticRenderer.cs
      to: flow-lang/Diagnostics/LevenshteinHelper.cs
      via: "Did-you-mean rendering calls LevenshteinHelper.SuggestNearest for unknown-identifier diagnostics"
      pattern: "LevenshteinHelper|SuggestNearest"
    - from: flow-lang/Lexing/PragmaRegistry.cs
      to: flow-lang/Diagnostics/LevenshteinHelper.cs
      via: "PragmaRegistry.SuggestNearest delegates to LevenshteinHelper after the extraction"
      pattern: "LevenshteinHelper"
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Diagnostics/FlowDiagnostic.cs
      via: "Unknown-identifier and type-mismatch errors construct FlowDiagnostic with Span + Levenshtein-derived Suggestion"
      pattern: "FlowDiagnostic"
    - from: flow-interpreter/Program.cs
      to: flow-lang/Diagnostics/DiagnosticRenderer.cs
      via: "Top-level emit picks FormatDiagnostics when ErrorReporter._diagnostics is non-empty; falls back to FormatErrors otherwise"
      pattern: "FormatDiagnostics"
---

<objective>
Land the Rust-style multi-line diagnostic renderer (LANG-04 — the user-facing half of the diagnostic stack; Plan 35-01 landed the Span-on-AST prerequisite). Create FlowDiagnostic (richer than FlowError — primary Span, labels, notes, suggestion), DiagnosticRenderer (multi-line format per RESEARCH §Example 4), LevenshteinHelper (extracted-and-promoted from PragmaRegistry per PATTERNS.md Bucket 2a — both PragmaRegistry and the renderer converge on the new helper), extend ErrorReporter with a parallel FlowDiagnostic collection + FormatDiagnostics(SourceMap) routing, add the `matchExhaustive` entry to PragmaRegistry.KnownPragmas (used by Plan 35-06's exhaustiveness handling), and wire ExpressionEvaluator's unknown-identifier and parse-error paths to emit FlowDiagnostic when span-rich context is available. Legacy FlowError + FormatErrors() stays as the fallback for emit sites that don't yet have Span context.

Purpose: LANG-04 — Rust-style multi-line diagnostics with source-quoted spans, secondary notes, and did-you-mean suggestions, plus the prerequisite registration of the `matchExhaustive` pragma so Plan 35-06's evaluator can query it. Composer DX leap: errors locate themselves instantly without context-switching to the editor.

Output: Diagnostic renderer + 4 supporting files; ErrorReporter extended; flow-interpreter Program.cs picks the new format when diagnostics are present; 5 xUnit fact classes covering golden format / Levenshtein / TTY / multi-error / REPL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@.planning/phases/35-language-foundation/35-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing diagnostics surface (legacy — must continue to work). -->

From flow-lang/Diagnostics/FlowError.cs:
- public record FlowError(DiagnosticLevel Level, string Message, SourceLocation Location, Exception? InnerException = null)
- Static factories: Create(message, location?), Warning(message, location?), Info(message, location?)
- ToString() = "{Location}: {levelStr}: {Message}"

From flow-lang/Diagnostics/ErrorReporter.cs:
- Report(FlowError error) — accumulates into List<FlowError> _errors up to MaxErrorCount
- FormatErrors() — joins each error's ToString() with newlines

From flow-lang/Lexing/PragmaRegistry.cs:
- KnownPragmas: Dictionary<string, string> with 5 existing entries (hAsB, justIntonation, pythagorean, equalTemperament, scaleLint)
- private static int LevenshteinDistance(string a, string b) at lines 60-84 (Wagner-Fischer DP)
- SuggestNearest(typed) at lines 42-58 wraps LevenshteinDistance + threshold max(2, typed.Length / 3)

From flow-lang/Runtime/StackFrame.cs:80:
- public IReadOnlyDictionary<string, Value> GetAllAccessibleVariables() — enumerates current frame + parent chain

From flow-lang/Core/Span.cs (Plan 35-01 output):
- Span(SourceLocation Start, SourceLocation End) with Unknown / At / Between statics
- Token.EffectiveSpan helper for back-compat

From flow-lang/Core/SourceMap.cs (Plan 35-01 output):
- Register(path, source), GetSource(path), TryGetSource(path, out source)
- Sentinel keys `<eval>`, `<stdin>`, `<repl>` for REPL/eval sources

From flow-interpreter/Program.cs:77-79 (TTY color emit precedent — copy this shape for DiagnosticRenderer):
- Console.ForegroundColor = ConsoleColor.Red
- Console.Error.WriteLine(...)
- Console.ResetColor()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write golden-baseline test stubs for diagnostic rendering + Levenshtein + TTY + multi-error + REPL</name>
  <files>flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs, flow-lang.Tests/Phase35/LevenshteinSuggestionTests.cs, flow-lang.Tests/Phase35/DiagnosticTtyTests.cs, flow-lang.Tests/Phase35/MultiErrorRenderingTests.cs, flow-lang.Tests/Phase35/ReplDiagnosticTests.cs, flow-lang.Tests/baselines/Phase35/diagnostics/.gitkeep</files>
  <read_first>
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 50-60 Wave 2a table; lines 118-131 Wave 0 list)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 755-781 Example 4 — exact format spec; lines 471-480 Pitfall 5 — Levenshtein threshold)
    - flow-lang.Tests/Phase35/ (existing folder from Plan 35-01 — confirm xUnit shape)
    - flow-lang.Tests/Helpers/RmsRegressionTests.cs (file-baseline-comparison pattern reference)
    - flow-lang/Lexing/PragmaRegistry.cs:42-84 (Levenshtein impl to mirror behavior)
  </read_first>
  <behavior>
    - Test 1 (DiagnosticRendererGoldenTests.UnknownIdentifierDiagnostic_RendersExpectedFormat): Construct a FlowDiagnostic for "unknown identifier 'transpos'" with span pointing into a sample source, render via DiagnosticRenderer, compare ANSI-stripped output against `flow-lang.Tests/baselines/Phase35/diagnostics/unknown_identifier.txt`. Expected baseline mirrors RESEARCH §Example 4 verbatim.
    - Test 2 (DiagnosticRendererGoldenTests.TypeMismatchDiagnostic_RendersExpectedFormat): Construct a FlowDiagnostic for a type-mismatch with two labels (primary span + secondary span), render, compare against `type_mismatch.txt` baseline.
    - Test 3 (LevenshteinSuggestionTests.ClosestMatchWithinThreshold): typed="transpos", candidates=["transpose","reverse"], assert SuggestNearest returns "transpose".
    - Test 4 (LevenshteinSuggestionTests.NullWhenAllCandidatesExceedThreshold): typed="xyz", candidates=["abc","def"], assert SuggestNearest returns null.
    - Test 5 (LevenshteinSuggestionTests.TieBrokenByLongestCommonPrefixThenAlphabetical): typed="tra", candidates=["tray","trab","tron"], assert SuggestNearest returns "trab" (alphabetical earlier when tied on edit distance + LCP).
    - Test 6 (DiagnosticTtyTests.RenderEmitsAnsiWhenTty): Force Console.IsOutputRedirected to false (or simulate via test harness), render, assert output contains ANSI escape `\x1b[31m` or `Console.ForegroundColor` was set to Red.
    - Test 7 (DiagnosticTtyTests.RenderEmitsPlainWhenRedirected): With output redirected, render, assert NO ANSI escapes in output.
    - Test 8 (MultiErrorRenderingTests.TwoDiagnosticsSeparatedByBlankLine): Report 2 FlowDiagnostics via ErrorReporter, call FormatDiagnostics(sourceMap), assert output contains both rendered diagnostics separated by a single blank line.
    - Test 9 (ReplDiagnosticTests.SentinelKeyResolvesSourceFromSourceMap): Register a `<eval>` source via SourceMap, render a FlowDiagnostic whose Primary.Start.FileName == "<eval>", assert the rendered source-quote line matches the registered source.
  </behavior>
  <action>
    Create `flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs` with golden-file harness: each `[Fact]` reads a baseline `.txt` from `flow-lang.Tests/baselines/Phase35/diagnostics/`, constructs the corresponding FlowDiagnostic + SourceMap, calls DiagnosticRenderer.Render (with TTY-off forced to keep baselines plain), and asserts string equality. Baselines stored ANSI-stripped per VALIDATION.md §Golden-file strategy.

    Create `flow-lang.Tests/Phase35/LevenshteinSuggestionTests.cs` — 3 facts covering threshold, ties, and null-no-match. Use the LevenshteinHelper public surface (will exist after Task 2). If Task 2 lands later, mark these facts as expected-FAIL until then.

    Create `flow-lang.Tests/Phase35/DiagnosticTtyTests.cs` — 2 facts. For the TTY-on case, intercept `Console.Out` via a `StringWriter` substitute and assert color emission (.NET writes raw ANSI escapes through the substitute only if it identifies as TTY-equivalent — a simpler alternative is to inspect `Console.ForegroundColor` state before/after the renderer call by wrapping in a custom `TextWriter` that captures color-set side-effects). Use whatever path is cleanest given the existing flow-lang.Tests patterns.

    Create `flow-lang.Tests/Phase35/MultiErrorRenderingTests.cs` — 1 fact asserting two-diagnostic rendering with blank-line separator.

    Create `flow-lang.Tests/Phase35/ReplDiagnosticTests.cs` — 1 fact asserting REPL sentinel key resolution.

    Create the baseline directory `flow-lang.Tests/baselines/Phase35/diagnostics/` with a `.gitkeep` file. Initial baselines `unknown_identifier.txt` and `type_mismatch.txt` get created in Task 2 once DiagnosticRenderer exists (first run of golden tests writes the baseline; second run verifies — OR pre-populate baselines as Task 2 deliverable).

    All tests MUST initially fail-to-compile (FlowDiagnostic, DiagnosticRenderer, LevenshteinHelper don't yet exist) OR fail-at-runtime (baselines empty). Task 2 makes them GREEN.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; ls flow-lang.Tests/Phase35/Diagnostic*.cs flow-lang.Tests/Phase35/Levenshtein*.cs flow-lang.Tests/Phase35/Multi*.cs flow-lang.Tests/Phase35/Repl*.cs flow-lang.Tests/baselines/Phase35/diagnostics/ 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: 5 new test files exist in flow-lang.Tests/Phase35/ matching the listed names
    - Source: flow-lang.Tests/baselines/Phase35/diagnostics/.gitkeep exists (folder created)
    - Behavior: dotnet build either errors with FlowDiagnostic/DiagnosticRenderer/LevenshteinHelper references (expected RED) OR builds with placeholder facts marked Skip/expected-fail
    - Done: Wave 0 stubs in place; expected-RED state confirmed (or build fails on the missing types)
  </acceptance_criteria>
  <done>5 xUnit fact classes + baseline directory created; tests fail-to-compile or fail-at-runtime as expected pre-implementation RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FlowDiagnostic + DiagnosticRenderer + LevenshteinHelper extraction + ErrorReporter extension + matchExhaustive pragma entry</name>
  <files>flow-lang/Diagnostics/FlowDiagnostic.cs, flow-lang/Diagnostics/DiagnosticRenderer.cs, flow-lang/Diagnostics/LevenshteinHelper.cs, flow-lang/Diagnostics/ErrorReporter.cs, flow-lang/Lexing/PragmaRegistry.cs</files>
  <read_first>
    - flow-lang/Diagnostics/FlowError.cs (full file — copy factory shape for FlowDiagnostic.Create/Warning/Info)
    - flow-lang/Diagnostics/ErrorReporter.cs (full file — existing Report(FlowError) shape to mirror in Report(FlowDiagnostic) overload)
    - flow-lang/Lexing/PragmaRegistry.cs:16-24 (KnownPragmas dict to extend with matchExhaustive)
    - flow-lang/Lexing/PragmaRegistry.cs:42-84 (Levenshtein impl + SuggestNearest wrapper to lift)
    - flow-lang/Core/Span.cs (from Plan 35-01)
    - flow-lang/Core/SourceMap.cs (from Plan 35-01)
    - flow-interpreter/Program.cs:73-105 (sentinel key usage for <eval>/<stdin>/<repl>)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 755-781 Example 4 format spec; lines 471-480 Pitfall 5 Levenshtein behavior; lines 461-469 Pitfall 4 matchExhaustive pragma scope)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 237-401 Bucket 2a — every file in this task has its analog laid out)
  </read_first>
  <behavior>
    - All 9 Wave 0 facts from Task 1 pass GREEN.
    - FlowDiagnostic is a record with Level + Message + Span Primary + IReadOnlyList<DiagnosticLabel> Labels + IReadOnlyList<string> Notes + string? Suggestion; static factories Create/Warning/Info mirror FlowError's shape.
    - DiagnosticRenderer.Render(FlowDiagnostic, SourceMap) returns a multi-line string matching RESEARCH §Example 4 verbatim format (header + location + pipe-prefixed source quote + caret-line sized to span + optional note: rows + optional help: did you mean line).
    - LevenshteinHelper.LevenshteinDistance(a,b) returns the Wagner-Fischer DP result identical to the pre-lift PragmaRegistry impl; SuggestNearest(typed, candidates, threshold?) returns the single best within threshold or null.
    - ErrorReporter.Report(FlowDiagnostic) overload accumulates into a parallel List<FlowDiagnostic> _diagnostics collection alongside the existing _errors list; FormatDiagnostics(SourceMap) routes each through DiagnosticRenderer.Render with blank-line separator.
    - PragmaRegistry.KnownPragmas gains a `matchExhaustive` entry (used by Plan 35-06 evaluator).
  </behavior>
  <action>
    Create `flow-lang/Diagnostics/FlowDiagnostic.cs` per PATTERNS.md Bucket 2a §FlowDiagnostic.cs Apply section. Record shape per RESEARCH §Architecture Patterns System Architecture Diagram lines 246-249 with the nested DiagnosticLabel(Span Span, string Text) sub-record. Static factories Create/Warning/Info mirror FlowError's shape but take Span instead of SourceLocation. Keep FlowError UNCHANGED as the legacy path.

    Create `flow-lang/Diagnostics/LevenshteinHelper.cs` per PATTERNS.md Bucket 2a §LevenshteinHelper.cs Apply section. Lift the Wagner-Fischer impl from `PragmaRegistry.cs:60-84` verbatim into a public static class in namespace `FlowLang.Diagnostics`. Promote `SuggestNearest(string typed, IEnumerable<string> candidates, int? threshold = null)` so it accepts ANY candidate set (default threshold = `Math.Max(2, typed.Length / 3)` per existing PragmaRegistry choice). Tie-break: candidates within the same minimum distance — prefer the one with the longest common prefix to typed; if still tied, alphabetical.

    Update `flow-lang/Lexing/PragmaRegistry.cs` to delegate its existing private SuggestNearest to the new LevenshteinHelper.SuggestNearest (single source of truth — both call sites converge). Both APIs preserve their existing public shape. Also add the `matchExhaustive` entry to KnownPragmas: `["matchExhaustive"] = "Promote non-exhaustive match warnings (D-v1.5-05) to errors. File-scope only; does NOT propagate via use imports (PRAG-02)."` per PATTERNS.md Bucket 3 §PragmaRegistry.cs.

    Create `flow-lang/Diagnostics/DiagnosticRenderer.cs` per PATTERNS.md Bucket 2a §DiagnosticRenderer.cs Apply section + RESEARCH §Example 4 format spec. Implement static `Render(FlowDiagnostic d, SourceMap sources, bool useColor = true)`:
    1. Header line: `error: <Message>` (or warning/info depending on Level)
    2. Location line: `  --> {FileName}:{line}:{col}` (1-indexed line/col per VALIDATION.md §Golden-file strategy)
    3. Pipe-prefixed empty line: `   |`
    4. Source-quote line: ` {N} | {sourceLine}` (N right-aligned to max line-number width)
    5. Caret line: `   | {leading-spaces}{^^^...} {label?}` (carets sized to `Span.End.Column - Span.Start.Column`, minimum 1)
    6. For each note in Notes: `   = note: {text}`
    7. If Suggestion != null: `   = help: did you mean '{Suggestion}'?`
    Color: when useColor + not redirected — `Console.ForegroundColor = ConsoleColor.Red` for `error:` keyword + carets; Yellow for `warning:`; Cyan for `note:`/`help:` prefixes. Auto-suppressed by .NET when stdout is redirected per Program.cs:77 precedent. Use SourceMap.TryGetSource to fetch the source line for the file path; if missing return source-quote-omitted form.

    Extend `flow-lang/Diagnostics/ErrorReporter.cs` per PATTERNS.md Bucket 2a §ErrorReporter.cs Apply section: add `public void Report(FlowDiagnostic diagnostic)` overload that accumulates into a new `List<FlowDiagnostic> _diagnostics` (cap at MaxErrorCount same as existing _errors). Add `public string FormatDiagnostics(SourceMap sources)` that routes each diagnostic through `DiagnosticRenderer.Render` joined with double-newline separators. Existing FormatErrors stays unchanged.

    Pre-populate the golden baselines in `flow-lang.Tests/baselines/Phase35/diagnostics/unknown_identifier.txt` and `type_mismatch.txt` with the exact expected ANSI-stripped output for the fixtures in DiagnosticRendererGoldenTests Task 1. Use a one-time `dotnet test --filter ...` run with a baseline-write opt-in env var (if your test harness supports it) OR write them by hand from RESEARCH §Example 4.

    Do NOT touch flow-interpreter/Program.cs in this task — Task 3 wires the top-level emit.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~Phase35.DiagnosticRendererGoldenTests|FullyQualifiedName~Phase35.LevenshteinSuggestionTests|FullyQualifiedName~Phase35.DiagnosticTtyTests|FullyQualifiedName~Phase35.MultiErrorRenderingTests|FullyQualifiedName~Phase35.ReplDiagnosticTests" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: flow-lang/Diagnostics/FlowDiagnostic.cs exists with `public record FlowDiagnostic` and nested DiagnosticLabel; FlowError.cs unchanged
    - Source: flow-lang/Diagnostics/DiagnosticRenderer.cs exists with `public static string Render(FlowDiagnostic d, SourceMap sources, bool useColor = true)`
    - Source: flow-lang/Diagnostics/LevenshteinHelper.cs exists with `public static int LevenshteinDistance(string a, string b)` and `public static string? SuggestNearest(string typed, IEnumerable<string> candidates, int? threshold = null)`
    - Source: grep -c "LevenshteinHelper" flow-lang/Lexing/PragmaRegistry.cs returns at least 1 (delegation lands)
    - Source: grep -c "matchExhaustive" flow-lang/Lexing/PragmaRegistry.cs returns at least 1 (KnownPragmas entry added)
    - Source: grep -c "FormatDiagnostics" flow-lang/Diagnostics/ErrorReporter.cs returns at least 1
    - Source: flow-lang.Tests/baselines/Phase35/diagnostics/unknown_identifier.txt exists and is non-empty
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.DiagnosticRenderer" exits 0 (golden baselines match)
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.Levenshtein" exits 0 (3 facts GREEN)
    - Behavior: dotnet test --filter "FullyQualifiedName~Phase35.Multi|FullyQualifiedName~Phase35.Repl|FullyQualifiedName~Phase35.DiagnosticTty" exits 0
  </acceptance_criteria>
  <done>Diagnostic stack implemented; LevenshteinHelper extraction + PragmaRegistry delegation; matchExhaustive pragma registered; all 9 Wave 0 facts pass GREEN; golden baselines committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire ExpressionEvaluator unknown-identifier + Parser parse-error sites + flow-interpreter top-level emit to FlowDiagnostic</name>
  <files>flow-lang/Interpreter/ExpressionEvaluator.cs, flow-lang/Parsing/Parser.cs, flow-interpreter/Program.cs</files>
  <read_first>
    - flow-lang/Interpreter/ExpressionEvaluator.cs (focus on existing _errorReporter.ReportError sites; ExpressionEvaluator.cs:335 + :344 from PATTERNS.md Bucket 2a §ExpressionEvaluator.cs)
    - flow-lang/Parsing/Parser.cs (focus on `Expect(...)` failure points and unknown-token error sites)
    - flow-interpreter/Program.cs:73-115 (existing FormatErrors emit at lines 77-79 — extend to pick FormatDiagnostics first)
    - flow-lang/Runtime/StackFrame.cs:80 (GetAllAccessibleVariables for did-you-mean candidate set)
    - flow-lang/Runtime/ExecutionContext.cs (find CurrentFrame accessor)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 382-401 Bucket 2a §ExpressionEvaluator + §Parser)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 471-480 Pitfall 5 — did-you-mean shadow rule)
  </read_first>
  <action>
    In `flow-lang/Interpreter/ExpressionEvaluator.cs`: locate the unknown-identifier code path (currently `_errorReporter.ReportError(...)` with a "unknown variable" or similar message). Replace with FlowDiagnostic construction:
    1. Get the typed identifier name + its Span (`varExpr.Span ?? Span.At(varExpr.Location)` for mid-migration compatibility).
    2. Query `LevenshteinHelper.SuggestNearest(typed, _context.CurrentFrame.GetAllAccessibleVariables().Keys)`.
    3. Construct `FlowDiagnostic.Create("unknown identifier '" + typed + "'", span, suggestion: bestMatch)`. Add a label "not found in scope" pointing at the same Span and a note "tried looking in: enclosing function 'X', module 'std', module 'audio'" if scope chain info is available.
    4. Call `_errorReporter.Report(diagnostic)`.
    5. Keep the existing FlowError fallback at sites that don't have Span info yet (mid-migration). For the type-mismatch sites at ExpressionEvaluator.cs:335 + :344 (per PATTERNS.md), use FlowDiagnostic when both operand spans are available.

    In `flow-lang/Parsing/Parser.cs`: at parse-error throw sites with clear span boundaries (unmatched paren, missing semicolon, unknown keyword), construct FlowDiagnostic with `Primary = new Span(start, current)` where `start` is the production's open-delimiter Location. Use the new diagnostic for syntactic errors that have clear span boundaries. Existing bare-string errors keep emitting FlowError for v1.5 — migration is progressive per PATTERNS.md Bucket 2a §Parser.cs Notable Departures.

    In `flow-interpreter/Program.cs`: at the existing FormatErrors emit point (around line 77-79), change the logic to check `if (engine.ErrorReporter.HasDiagnostics) Console.Error.WriteLine(engine.ErrorReporter.FormatDiagnostics(engine.SourceMap)); else if (engine.ErrorReporter.HasErrors) Console.Error.WriteLine(engine.ErrorReporter.FormatErrors());`. Wrap each WriteLine in the existing `Console.ForegroundColor = ConsoleColor.Red; ...; Console.ResetColor();` block (FormatDiagnostics already inlines per-line color, but the wrapping block preserves the existing convention). Mirror across all 7 emit sites in flow-interpreter per CLAUDE.md TTY precedent.

    Do NOT touch the 70+ existing tests/test_*.flow scripts — their console-output contract must continue to hold (success = no errors per CLAUDE.md). If any existing diagnostic test in flow-lang.Tests asserts the LEGACY single-line format, leave it asserting the legacy path (FlowError → FormatErrors) and add NEW facts for the rich-format path.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done &amp;&amp; echo "ALL .flow SCRIPTS PASS" &amp;&amp; echo "// trigger unknown identifier" &gt; /tmp/diag_demo.flow &amp;&amp; echo "(print transpos)" &gt;&gt; /tmp/diag_demo.flow &amp;&amp; dotnet run --project flow-interpreter /tmp/diag_demo.flow 2&gt;&amp;1 | grep -E "did you mean|transpose|-->" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: dotnet test exits 0 (full suite + Phase35 + Phase 25 Gaussian byte-identical + Phase 28 articulation determinism all GREEN)
    - Behavior: All tests/test_*.flow scripts exit 0 (no .flow regression)
    - Behavior: A test script triggering an unknown identifier (eg `(print transpos)`) produces output containing `did you mean`, `transpose`, and `-->` — at least 1 match per grep
    - Source: grep -c "FormatDiagnostics" flow-interpreter/Program.cs returns at least 1
    - Source: grep -c "FlowDiagnostic" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 1
    - Done: ExpressionEvaluator emits rich diagnostics for unknown identifiers; Parser emits rich diagnostics for span-bounded parse errors; Program.cs picks the rich format when present; legacy FlowError path preserved for sites without span context
  </acceptance_criteria>
  <done>Diagnostic stack wired end-to-end; composer sees Rust-style multi-line errors for unknown identifiers and parser errors; full test suite + .flow regression remains GREEN; legacy FlowError path coexists for mid-migration emit sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer source → diagnostic renderer | Diagnostic source-quote text is the composer's own source — ANSI escapes passed through. Acceptable per single-user CLI threat model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | Source-text-injection in diagnostic rendering | accept | Diagnostic renderer emits to local stderr only; no network egress; ANSI escape sequences in source-quote text are passed through (composer's own source). Per RESEARCH §Security Domain — acceptable single-user CLI threat model. |
| T-35-08 | Information Disclosure | Levenshtein suggestion exposing private identifiers | accept | All identifiers in scope are accessible to the composer anyway (their own program). No privilege boundary crossed. |
| T-35-09 | Integrity | Golden-baseline test drift | mitigate | Baselines stored ANSI-stripped under flow-lang.Tests/baselines/Phase35/diagnostics/; line/col 1-indexed per Rust convention; baseline format documented in VALIDATION.md §Golden-file strategy. |
</threat_model>

<verification>
- All Wave 0 Phase35 diagnostic facts pass: `dotnet test --filter "FullyQualifiedName~Phase35.DiagnosticRenderer|FullyQualifiedName~Phase35.Levenshtein|FullyQualifiedName~Phase35.DiagnosticTty|FullyQualifiedName~Phase35.MultiError|FullyQualifiedName~Phase35.ReplDiagnostic"` exits 0
- Full xUnit suite remains green: `dotnet test` exits 0
- Existing .flow scripts continue to pass: `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t"; done`
- End-to-end smoke: a script with an unknown identifier produces `did you mean`, source quote, and `-->` location pointer in stderr output
</verification>

<success_criteria>
Plan complete when:
- [ ] FlowDiagnostic + DiagnosticRenderer + LevenshteinHelper exist; FlowError unchanged (legacy preserved)
- [ ] PragmaRegistry delegates SuggestNearest to LevenshteinHelper; matchExhaustive entry added to KnownPragmas
- [ ] ErrorReporter has Report(FlowDiagnostic) overload + FormatDiagnostics(SourceMap)
- [ ] ExpressionEvaluator unknown-identifier path emits FlowDiagnostic with Levenshtein-derived Suggestion
- [ ] Parser span-bounded parse errors emit FlowDiagnostic
- [ ] flow-interpreter/Program.cs picks FormatDiagnostics when diagnostics present
- [ ] 5 Wave 0 fact classes pass GREEN
- [ ] Golden baselines unknown_identifier.txt + type_mismatch.txt exist and match
- [ ] Full xUnit suite + all .flow scripts remain GREEN
- [ ] Smoke test: unknown-identifier script produces rust-style output with `did you mean`
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-03-SUMMARY.md` when done with: the rendered diagnostic format sample (paste from smoke test), the golden-baseline file list, the Levenshtein threshold + tie-break behavior summary, and confirmation that LANG-04 unblocks Plan 35-06's exhaustiveness handling (matchExhaustive pragma registered).
</output>
