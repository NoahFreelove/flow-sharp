---
phase: 35-language-foundation
plan: 07
type: execute
wave: 5
depends_on: ["35-01", "35-04", "35-05", "35-06"]
files_modified:
  - flow-lang/Lexing/SimpleLexer.cs
  - flow-lang/Lexing/TokenType.cs
  - flow-lang/Parsing/Parser.cs
  - flow-lang/Ast/Expressions/FlowExpression.cs
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang.Tests/Phase35/AsKeywordReservationTests.cs
  - flow-lang.Tests/Phase35/AsBindingParserTests.cs
  - flow-lang.Tests/Phase35/AsBindingScopeTests.cs
  - flow-lang.Tests/Phase35/AsBindingTypeTests.cs
  - tests/test_chain_naming.flow
autonomous: true
requirements: [LANG-03]
must_haves:
  truths:
    - "LANG-03: composer can write `seq -> (transpose 2) as melody -> (legato 0.5) as legato-melody -> render` and the names `melody` and `legato-melody` are bound as intermediate values without breaking the chain."
    - "LANG-03: `as` is a reserved keyword (TokenType.As); `as name` after a `->` RHS annotates the produced FlowExpression with IntermediateName via defaulted-param (no new AST node — REQUIREMENTS.md LANG-03 explicit constraint)."
    - "LANG-03: parser-level desugar — RESEARCH §Pattern 3: `LHS -> RHS as name -> NEXT` evaluates as `{ Type name = (RHS LHS); (NEXT name) }`. Bindings declared in CURRENT frame so subsequent chain steps + same-statement continuation can read it per Pitfall 7."
    - "LANG-03: scope visibility — the as-clause makes the result available under the given name from this point onward in the enclosing scope, until end of statement / block / function (Pitfall 7 composer-visible model)."
    - "Pitfall 7 + RESEARCH Open Question 5 (recommendation): `as` is right-associative with `->`; no parenthesized form `(EXPR as NAME)` shipped in v1.5 — keyword sequence is `EXPR -> CALL as NAME -> ...` only. Document in SUMMARY."
    - "Assumption A3: `as` is NOT currently a keyword (verified via grep at SimpleLexer.cs:850-891) — D-v1.5-01 latitude covers any composer code that used `as` as a variable name; breaks in one commit."
    - "All existing tests/test_*.flow scripts + 1003+ xUnit facts continue GREEN — adding the `as` keyword + extending FlowExpression with IntermediateName is purely additive."
  artifacts:
    - path: flow-lang/Lexing/TokenType.cs
      provides: "TokenType.As keyword token added"
      contains: "TokenType.As|As,"
    - path: flow-lang/Lexing/SimpleLexer.cs
      provides: "Keyword table maps `as` -> TokenType.As"
      contains: "TokenType.As"
    - path: flow-lang/Parsing/Parser.cs
      provides: "ParseFlowExpression extended — after consuming RHS, peek for As token; consume As + Identifier; annotate FlowExpression with IntermediateName per RESEARCH §Pattern 3"
      contains: "IntermediateName|TokenType.As"
    - path: flow-lang/Ast/Expressions/FlowExpression.cs
      provides: "FlowExpression record gains `string? IntermediateName = null` as defaulted-param (between Right and Span per Plan 35-01 sweep convention)"
      contains: "IntermediateName"
    - path: flow-lang/Interpreter/ExpressionEvaluator.cs
      provides: "EvaluateFlowExpression extended — after computing the result Value, if flowEx.IntermediateName != null then _context.DeclareVariable(intermediateName, result) in CURRENT frame per Pitfall 7"
      contains: "IntermediateName"
    - path: tests/test_chain_naming.flow
      provides: "Composer-facing acceptance — at least 3 (test ...) blocks exercising single-name + multi-name + type-carry-through scenarios"
      min_lines: 25
  key_links:
    - from: flow-lang/Parsing/Parser.cs
      to: flow-lang/Ast/Expressions/FlowExpression.cs
      via: "ParseFlowExpression sets FlowExpression.IntermediateName from the parsed identifier after the `as` keyword"
      pattern: "IntermediateName"
    - from: flow-lang/Interpreter/ExpressionEvaluator.cs
      to: flow-lang/Runtime/ExecutionContext.cs
      via: "EvaluateFlowExpression calls _context.DeclareVariable(intermediateName, result) in CURRENT frame so subsequent chain steps + same-statement continuation can read it"
      pattern: "DeclareVariable"
---

<objective>
Land `-> as name` mid-chain naming (LANG-03) — Wave 5 of Phase 35 (the final language deliverable; bumped from wave 4 to wave 5 to respect file_modified disjointness with Plan 35-06 which touches the same Parser + ExpressionEvaluator). Reserve `as` as a new keyword (verified non-colliding per RESEARCH Assumption A3); extend Parser's ParseFlowExpression to consume `as Identifier` after a `->` RHS and annotate the resulting FlowExpression with IntermediateName via the existing defaulted-param precedent (Phase 22 / Phase 21 pattern; no new AST node per REQUIREMENTS.md LANG-03 explicit constraint); extend ExpressionEvaluator.EvaluateFlowExpression to call `_context.DeclareVariable(intermediateName, result)` in the CURRENT frame after computing each chain step's result, so the bound name is visible to subsequent chain steps and same-statement continuation per Pitfall 7's composer-visible model.

Purpose: LANG-03 — Composer ergonomics. Replace the boilerplate `Sequence x = (transpose seq 2); Sequence y = (legato x 0.5); (render y)` pattern with the inline form `seq -> (transpose 2) as melody -> (legato 0.5) as legato-melody -> render` that names intermediate values without breaking the chain. Pure parser-level desugar — no new AST node, no new evaluator dispatch arm; minimal blast radius.

Output: TokenType.As + lexer keyword + FlowExpression IntermediateName field + Parser as-clause consumption + Evaluator binding-after-result + 4 xUnit fact classes + composer-facing tests/test_chain_naming.flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@.planning/phases/35-language-foundation/35-01-SUMMARY.md
@.planning/phases/35-language-foundation/35-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing FlowExpression shape — extend with IntermediateName per Plan 35-01 + Pattern 3. -->

From flow-lang/Ast/Expressions/FlowExpression.cs (after Plan 35-01 Span migration):
- public record FlowExpression(SourceLocation Location, Expression Left, Expression Right, Span? Span = null) : Expression(Location);
- LANG-03 extends with `string? IntermediateName = null` BETWEEN Right and Span (per the sweep convention where Span is the terminal defaulted param).

From flow-lang/Parsing/Parser.cs:850-913 (existing ParseFlowExpression):
- Parses `var left = ParseUnaryShorthand()` then loops on Arrow / TildeArrow tokens.
- After consuming Arrow, parses `var right = ParseUnaryShorthand()`.
- Eventually constructs a FunctionCallExpression or FlowExpression depending on RHS shape.
- LANG-03 hooks in AFTER the `right` is parsed: peek for As token; consume As + Identifier; capture intermediateName.

From flow-lang/Interpreter/ExpressionEvaluator.cs:325-346 (existing EvaluateFlowExpression):
- Evaluates Left, evaluates Right, dispatches based on Right's type (FunctionOverload / FunctionType / etc.), returns the result.
- LANG-03 extends: after computing result, if flowEx.IntermediateName != null then _context.DeclareVariable(intermediateName, result) in CURRENT frame.

From flow-lang/Runtime/ExecutionContext.cs:
- DeclareVariable(string name, Value value) — declares in the current frame (whatever the current top of _callStack is).
- DO NOT PushFrame around this — Pitfall 7 requires the binding to be visible in the SAME frame (not a temporary), so subsequent `->` steps and same-block statements can read it.

From flow-lang/Lexing/SimpleLexer.cs:850-891 (keyword table):
- Existing entries: "proc", "voicePool", "tuning", "match" (Plan 35-05), "when" (Plan 35-05).
- LANG-03 adds: "as" => TokenType.As.

From flow-lang/Lexing/TokenType.cs:
- Add `As,` in the Keywords block alongside `In` at line 35 (per PATTERNS.md Bucket 4).

From Plan 35-04:
- The (test) framework is available for composer-facing acceptance tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing tests for `as` keyword reservation + chain-naming parser + scope + type carry-through + composer regression</name>
  <files>flow-lang.Tests/Phase35/AsKeywordReservationTests.cs, flow-lang.Tests/Phase35/AsBindingParserTests.cs, flow-lang.Tests/Phase35/AsBindingScopeTests.cs, flow-lang.Tests/Phase35/AsBindingTypeTests.cs, tests/test_chain_naming.flow</files>
  <read_first>
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 88-95 Wave 4 table; lines 118-131 Wave 0 list)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 362-383 Pattern 3 parser-level desugar; lines 491-506 Pitfall 7 name visibility + scope; lines 982-986 Open Question 5 precedence)
    - flow-lang.Tests/Phase35/ (existing folder; mirror the existing fact-class shape)
    - tests/test_test_framework.flow (Plan 35-04 — meta-dogfooding template)
  </read_first>
  <behavior>
    - Test 1 (AsKeywordReservationTests.AsTokenEmittedFromKeyword): Lex `as` → exactly one TokenType.As token.
    - Test 2 (AsKeywordReservationTests.AsAsVariableNameNoLongerAllowed): Parse `Int as = 5` — per D-v1.5-01 latitude, this should now produce a parser error (or distinct parse outcome) because `as` is reserved. Document the diagnostic shape (FlowDiagnostic from Plan 35-03 OR FlowError fallback).
    - Test 3 (AsBindingParserTests.SingleAsClauseParses): Parse `seq -> (transpose 2) as melody -> render` produces a tree where the FlowExpression for the first chain step has IntermediateName="melody".
    - Test 4 (AsBindingParserTests.MultipleAsClausesParse): Parse `seq -> (transpose 2) as melody -> (legato 0.5) as legato_melody -> render` produces two FlowExpressions each with their respective IntermediateName.
    - Test 5 (AsBindingParserTests.AsRequiresIdentifierAfter): Parse `seq -> (f) as 42 -> render` produces a parse error — `as` must be followed by an Identifier, not a literal.
    - Test 6 (AsBindingScopeTests.BindingVisibleToSubsequentChainSteps): Evaluate `Int x = (5 -> (mul 2) as doubled -> (add doubled))`; assert x == 20 (5*2=10; 10+doubled where doubled=10 → 20). The chain step `(add doubled)` reads the bound name.
    - Test 7 (AsBindingScopeTests.BindingVisibleToSameBlockStatement): Evaluate
      ```
      proc test() {
        5 -> (mul 2) as doubled;
        (print doubled)
      }
      ```
      assert "10" printed. Same-block continuation reads the binding per Pitfall 7's composer-visible model.
    - Test 8 (AsBindingScopeTests.BindingDoesNotEscapeProcBoundary): Evaluate
      ```
      proc inner() { 5 -> (mul 2) as doubled }
      proc outer() { (inner); (print doubled) }
      ```
      assert (print doubled) errors with unknown identifier — bindings die with the enclosing proc's frame.
    - Test 9 (AsBindingTypeTests.IntermediateNameValueTypeMatchesRhsReturnType): After `seq -> (transpose 2) as melody`, the bound name `melody` has Value type Sequence (matches transpose's return type). Verified via subsequent use that requires Sequence semantics.
    - Test 10 (composer-facing tests/test_chain_naming.flow): At least 3 (test ...) blocks via Plan 35-04 framework — single binding, multi-binding chain, scope visibility within a block. Runnable via flow test CLI.
  </behavior>
  <action>
    Create the 4 xUnit fact files in `flow-lang.Tests/Phase35/`. Use FlowEngine to evaluate Flow source and inspect:
    - AST: walk the Program AST after parsing; find FlowExpression nodes; assert IntermediateName field.
    - Scope: capture the returned Value via engine.LastValue or analogous; assert against expected.
    - Type: inspect the resulting Value.Type after evaluation.

    For AsAsVariableNameNoLongerAllowed: try parsing `Int as = 5` and assert the parse fails (engine.ErrorReporter.HasErrors == true after Execute). Acceptable diagnostic shape is either FlowError (legacy) or FlowDiagnostic (Plan 35-03). Document the shape in the SUMMARY.

    Create tests/test_chain_naming.flow per PATTERNS.md Bucket 4 — composer-facing meta-dogfooding via Plan 35-04 framework with at least 3 (test ...) blocks.

    All tests MUST initially fail (TokenType.As doesn't exist; FlowExpression doesn't have IntermediateName). Tasks 2 + 3 + 4 make them GREEN.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; ls flow-lang.Tests/Phase35/As*.cs tests/test_chain_naming.flow 2>&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Source: 4 new xUnit fact files exist in flow-lang.Tests/Phase35/ matching the listed class names
    - Source: tests/test_chain_naming.flow exists with at least 3 `(test ` blocks
    - Behavior: dotnet build fails on missing TokenType.As / IntermediateName references (expected RED) OR builds with placeholders
    - Done: Wave 0 stubs land
  </acceptance_criteria>
  <done>4 xUnit fact classes + composer-facing regression in place; all RED pending Tasks 2-4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reserve `as` keyword + extend FlowExpression with IntermediateName field</name>
  <files>flow-lang/Lexing/TokenType.cs, flow-lang/Lexing/SimpleLexer.cs, flow-lang/Ast/Expressions/FlowExpression.cs</files>
  <read_first>
    - flow-lang/Lexing/TokenType.cs (add As entry in Keywords block alongside In at line 35)
    - flow-lang/Lexing/SimpleLexer.cs:850-891 (keyword table — add as => TokenType.As)
    - flow-lang/Ast/Expressions/FlowExpression.cs (current Plan 35-01 shape — extend with IntermediateName)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 898-973 Bucket 4 — file-by-file analogs)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (Assumption A3 — `as` is verified non-colliding via grep)
  </read_first>
  <behavior>
    - AsKeywordReservationTests.AsTokenEmittedFromKeyword passes GREEN.
    - FlowExpression has `string? IntermediateName = null` defaulted-param between Right and Span.
    - All 1003+ existing xUnit facts continue GREEN — adding TokenType.As entry + extending FlowExpression with defaulted-param is purely additive.
    - All existing tests/test_*.flow scripts continue to exit 0.
    - If any existing .flow script or .Flow source file uses `as` as a variable name, it now produces a parser error per D-v1.5-01 latitude (verify by running the full .flow regression loop — if any FAIL, fix the script to use a different name; document the breaking change in SUMMARY).
  </behavior>
  <action>
    Update `flow-lang/Lexing/TokenType.cs` per PATTERNS.md Bucket 4 §TokenType.cs — add `As,` in the Keywords block alongside `In` at line 35.

    Update `flow-lang/Lexing/SimpleLexer.cs:850-891` (keyword table) per PATTERNS.md Bucket 4 §SimpleLexer.cs — add `"as" => TokenType.As,` to the switch.

    Update `flow-lang/Ast/Expressions/FlowExpression.cs` per PATTERNS.md Bucket 4 §FlowExpression.cs Apply section + Plan 35-01 sweep convention — add `string? IntermediateName = null` between Right and Span:
    ```
    public record FlowExpression(
        SourceLocation Location,
        Expression Left,
        Expression Right,
        string? IntermediateName = null,
        Span? Span = null) : Expression(Location);
    ```

    Order of defaulted params matters for record-with syntax — IntermediateName positions BEFORE Span (which stays terminal per the Phase 35 sweep convention so the existing `Span: namedArg` calls don't need to reorder).

    Run the full .flow regression loop. If any script breaks because it used `as` as a variable name, edit that script to use a different name. Document the changed scripts in the SUMMARY per D-v1.5-01 latitude.

    Do NOT touch Parser.cs or ExpressionEvaluator.cs in this task — Task 3 + Task 4 wire the parser and evaluator.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.AsKeywordReservationTests" 2>&amp;1 | tail -10 &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || echo "BROKEN: $t"; done | tee /tmp/35-07-flow-regression.txt &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: grep -c "As," flow-lang/Lexing/TokenType.cs returns at least 1 (As enum entry added in Keywords block)
    - Source: grep -c "TokenType.As" flow-lang/Lexing/SimpleLexer.cs returns at least 1
    - Source: grep -c "IntermediateName" flow-lang/Ast/Expressions/FlowExpression.cs returns at least 1
    - Behavior: AsKeywordReservationTests.AsTokenEmittedFromKeyword passes GREEN
    - Behavior: Full dotnet test exits 0 (no xUnit regression)
    - Behavior: cat /tmp/35-07-flow-regression.txt has zero BROKEN lines OR documented + fixed BROKEN scripts (composer-side scripts only — flow-lang itself does not use `as` as an identifier per Assumption A3)
    - Done: `as` reserved; FlowExpression carries IntermediateName field
  </acceptance_criteria>
  <done>TokenType.As + lexer keyword + FlowExpression.IntermediateName land; AsKeywordReservationTests GREEN; full xUnit suite + .flow scripts preserved (or any broken .flow scripts fixed + documented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ParseFlowExpression extension — consume `as Identifier` after RHS; annotate FlowExpression with IntermediateName</name>
  <files>flow-lang/Parsing/Parser.cs</files>
  <read_first>
    - flow-lang/Parsing/Parser.cs:850-913 (existing ParseFlowExpression — the full method)
    - flow-lang/Ast/Expressions/FlowExpression.cs (Task 2 output — IntermediateName field)
    - flow-lang/Lexing/TokenType.cs (Task 2 output — TokenType.As)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 918-947 Bucket 4 §Parser.cs)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 362-383 Pattern 3 + lines 491-506 Pitfall 7 visibility rule)
  </read_first>
  <behavior>
    - AsBindingParserTests pass GREEN: SingleAsClauseParses, MultipleAsClausesParse, AsRequiresIdentifierAfter.
    - The parser produces FlowExpression with IntermediateName set to the consumed identifier text.
    - When `as` is NOT followed by an Identifier, the parser emits a parse error (FlowDiagnostic from Plan 35-03 ideally; FlowError fallback acceptable).
    - All 1003+ existing xUnit facts continue GREEN; all .flow scripts continue to pass.
  </behavior>
  <action>
    Extend `flow-lang/Parsing/Parser.cs` ParseFlowExpression per PATTERNS.md Bucket 4 §Parser.cs Apply section + RESEARCH §Pattern 3.

    Locate the existing ParseFlowExpression method (around lines 850-913 per RESEARCH §Sources). After the `var right = ParseUnaryShorthand();` line and BEFORE the existing transform/dispatch logic, add:
    ```
    string? intermediateName = null;
    if (Match(TokenType.As))
    {
        if (!Check(TokenType.Identifier))
        {
            _errorReporter.Report(FlowDiagnostic.Create(
                "expected identifier after 'as'",
                new Span(PreviousToken.Location, CurrentToken.Location)));
            // fall through — intermediateName stays null; chain continues without binding
        }
        else
        {
            intermediateName = Advance().Text;
        }
    }
    ```

    Then, where the method constructs the resulting FlowExpression (the existing `left = new FlowExpression(location, left, right);` or similar), thread the intermediateName through:
    ```
    left = new FlowExpression(location, left, right, IntermediateName: intermediateName, Span: ...);
    ```

    Per PATTERNS.md Notable Departures: when the existing parse-time desugar produces a FunctionCallExpression instead of a FlowExpression (the RHS-is-function-call branch at Parser.cs:850-913 prepends LHS to the RHS args), annotate the FunctionCallExpression by wrapping it in a FlowExpression carrying the IntermediateName, OR thread IntermediateName as a separate side-effect during evaluation. Cleanest approach per RESEARCH Pattern 3: KEEP the existing FunctionCallExpression desugar AND wrap it in a `new FlowExpression(location, left, funcCall, IntermediateName: name)` whose evaluator path computes the funcCall then declares the binding (Task 4 handles this).

    Verify that when `intermediateName == null` (no `as` clause), the resulting AST is byte-identical to pre-Plan-35-07 parses — the back-compat path is the existing ParseFlowExpression behavior unchanged.

    Do NOT touch ExpressionEvaluator.cs in this task — Task 4 implements the declare-in-current-frame semantics.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.AsBindingParserTests" 2>&amp;1 | tail -15 &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done &amp;&amp; echo "ALL .flow SCRIPTS PASS" &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: AsBindingParserTests exits 0 (SingleAsClauseParses + MultipleAsClausesParse + AsRequiresIdentifierAfter all GREEN)
    - Behavior: All tests/test_*.flow scripts continue to exit 0
    - Behavior: Full dotnet test exits 0
    - Source: grep -c "TokenType.As" flow-lang/Parsing/Parser.cs returns at least 1
    - Source: grep -c "IntermediateName" flow-lang/Parsing/Parser.cs returns at least 1
    - Done: ParseFlowExpression consumes `as Identifier` after RHS and annotates FlowExpression with IntermediateName; back-compat path (no `as`) unchanged
  </acceptance_criteria>
  <done>Parser recognizes the `-> RHS as name` form; FlowExpression annotated; AsBindingParserTests GREEN; full xUnit + .flow regression preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: EvaluateFlowExpression extension — DeclareVariable in CURRENT frame after computing result; scope + type carry-through verification</name>
  <files>flow-lang/Interpreter/ExpressionEvaluator.cs</files>
  <read_first>
    - flow-lang/Interpreter/ExpressionEvaluator.cs:325-346 (EvaluateFlowExpression — the full method)
    - flow-lang/Runtime/ExecutionContext.cs (DeclareVariable API + CurrentFrame access)
    - flow-lang/Runtime/StackFrame.cs (DeclareVariable semantics — verify it declares in the receiver frame, not a child)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 977-999 Bucket 4 §ExpressionEvaluator.cs)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 491-506 Pitfall 7 — declare in CURRENT frame, NOT pushed; scope visibility model)
  </read_first>
  <behavior>
    - AsBindingScopeTests pass GREEN: BindingVisibleToSubsequentChainSteps + BindingVisibleToSameBlockStatement + BindingDoesNotEscapeProcBoundary.
    - AsBindingTypeTests pass GREEN: IntermediateNameValueTypeMatchesRhsReturnType.
    - tests/test_chain_naming.flow passes via flow test CLI.
    - Plans 35-05 and 35-06 facts (MatchRuntimeTests, MusicAwarePatternsTests, MatchExhaustivenessDefaultTests, etc.) continue GREEN.
    - Full xUnit suite + .flow regression preserved.
  </behavior>
  <action>
    Extend `flow-lang/Interpreter/ExpressionEvaluator.cs` EvaluateFlowExpression per PATTERNS.md Bucket 4 §ExpressionEvaluator.cs Apply section + RESEARCH §Pitfall 7 declare-in-current-frame rule.

    At every return path of EvaluateFlowExpression (the method evaluates Left, evaluates Right, dispatches based on Right's type, returns the computed Value), add a binding declaration BEFORE the return:
    ```
    Value result = ...;  // existing computation per the current return path
    if (flowEx.IntermediateName != null)
    {
        _context.DeclareVariable(flowEx.IntermediateName, result);
    }
    return result;
    ```

    Per RESEARCH §Pitfall 7 Notable Departures: declaration MUST happen in CURRENT frame (NOT a pushed temporary frame) so subsequent `->` steps and same-block statements can read it. Do NOT call PushFrame/PopFrame around the declaration.

    If multiple return paths exist (the existing method dispatches by Right's type — FunctionOverload vs FunctionType vs plain Expression), wrap the dispatch with a common epilogue that declares the binding once. Refactor only as much as needed to avoid duplicating the declaration logic across return paths.

    Scope visibility rule per Pitfall 7: the binding is visible from the point of declaration onward in the enclosing scope — until end of statement / block / function. This is the natural consequence of declaring in the current frame (the frame's lifetime IS the enclosing scope). No extra plumbing needed; AsBindingScopeTests.BindingDoesNotEscapeProcBoundary verifies bindings die with the proc's frame via the existing PushFrame/PopFrame lifecycle.

    Type carry-through per AsBindingTypeTests.IntermediateNameValueTypeMatchesRhsReturnType: trivial — the Value bound is whatever `result` is, and Value carries its own .Type field. No explicit type inference logic needed.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.AsBindingScopeTests|FullyQualifiedName~Phase35.AsBindingTypeTests|FullyQualifiedName~Phase35.AsBindingParserTests|FullyQualifiedName~Phase35.AsKeywordReservationTests" 2>&amp;1 | tail -20 &amp;&amp; dotnet run --project flow-cli -- test tests/test_chain_naming.flow 2>&amp;1 | tail -10 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -5 &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done &amp;&amp; echo "ALL .flow SCRIPTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: All 4 AsBindingScope + AsBindingType facts pass GREEN
    - Behavior: AsBindingParserTests + AsKeywordReservationTests remain GREEN
    - Behavior: flow test tests/test_chain_naming.flow exits 0 with PASS summary
    - Behavior: Full dotnet test exits 0
    - Behavior: All tests/test_*.flow scripts continue to pass
    - Source: grep -c "IntermediateName" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 1
    - Source: grep -c "DeclareVariable" flow-lang/Interpreter/ExpressionEvaluator.cs returns at least 1
    - Done: -> as name end-to-end operational; bindings declared in current frame; scope dies with enclosing frame; type carries through naturally
  </acceptance_criteria>
  <done>EvaluateFlowExpression declares IntermediateName in current frame; chain bindings visible to subsequent steps + same-block statements; bindings die with proc boundary; LANG-03 GREEN end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | LANG-03 is pure parser-level desugar + evaluator extension. No untrusted-input crossings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-20 | Integrity | Binding visible in current frame vs leaking to outer scope | mitigate | Pitfall 7 — declare in CURRENT frame, NOT pushed. AsBindingScopeTests.BindingDoesNotEscapeProcBoundary verifies the proc-boundary lifecycle. |
| T-35-21 | Integrity | `as` keyword collision with composer code | accept | Assumption A3 — verified `as` not previously a keyword via grep at SimpleLexer.cs:850-891. Per D-v1.5-01 latitude, breaks in one commit; any broken .flow scripts surface in the Task 2 regression loop and are fixed inline. |
| T-35-22 | Integrity | Parser back-compat for chains WITHOUT `as` | mitigate | When IntermediateName is null, AST is byte-identical to pre-Plan-35-07 parses; full .flow regression loop + xUnit suite confirm zero regression. |
</threat_model>

<verification>
- All Wave 0 Phase35 LANG-03 facts pass: `dotnet test --filter "FullyQualifiedName~Phase35.AsKeywordReservation|FullyQualifiedName~Phase35.AsBindingParser|FullyQualifiedName~Phase35.AsBindingScope|FullyQualifiedName~Phase35.AsBindingType"` exits 0
- Composer acceptance: `flow test tests/test_chain_naming.flow` exits 0 with PASS summary
- Plans 35-01 through 35-06 facts preserved: `dotnet test --filter "FullyQualifiedName~Phase35"` exits 0
- Full xUnit suite green: `dotnet test` exits 0
- .flow regression: every existing tests/test_*.flow continues to exit 0 (or any breakage from `as` reservation is fixed + documented)
</verification>

<success_criteria>
Plan complete when:
- [ ] TokenType.As added; SimpleLexer keyword table maps `as` → TokenType.As
- [ ] FlowExpression record extended with `string? IntermediateName = null` defaulted-param
- [ ] ParseFlowExpression consumes `as Identifier` after RHS and annotates FlowExpression
- [ ] ParseFlowExpression emits parse error when `as` is not followed by Identifier
- [ ] EvaluateFlowExpression calls DeclareVariable(IntermediateName, result) in CURRENT frame
- [ ] Pitfall 7 scope rule holds — bindings visible to subsequent chain steps + same-block statements; die at enclosing proc/block boundary
- [ ] 4 xUnit fact classes (AsKeywordReservation + AsBindingParser + AsBindingScope + AsBindingType) pass GREEN
- [ ] tests/test_chain_naming.flow passes via flow test CLI
- [ ] Full xUnit suite + all .flow scripts remain GREEN (any composer-script breakage from `as` reservation fixed + documented per D-v1.5-01 latitude)
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-07-SUMMARY.md` when done with: confirmation that `as` is now reserved (Assumption A3 verified by Task 2 regression loop), the parse-time desugar approach taken (annotation on FlowExpression vs wrapped FunctionCallExpression), scope visibility behavior summary, and confirmation that ALL Phase 35 deliverables (LANG-01..04, TEST-01..02, HK-01..04) are closed — Phase 35 ready for `/gsd:verify-work` + closure.
</output>
