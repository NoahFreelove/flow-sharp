---
phase: 35-language-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/Core/Span.cs
  - flow-lang/Core/SourceMap.cs
  - flow-lang/Lexing/Token.cs
  - flow-lang/Lexing/SimpleLexer.cs
  - flow-lang/Parsing/Parser.cs
  - flow-lang/Parsing/Parser.NoteStream.cs
  - flow-lang/Ast/Expressions/ArrayIndexExpression.cs
  - flow-lang/Ast/Expressions/ArrayLiteralExpression.cs
  - flow-lang/Ast/Expressions/ChordLiteralExpression.cs
  - flow-lang/Ast/Expressions/FlowExpression.cs
  - flow-lang/Ast/Expressions/FunctionCallExpression.cs
  - flow-lang/Ast/Expressions/InterpolatedStringExpression.cs
  - flow-lang/Ast/Expressions/LambdaExpression.cs
  - flow-lang/Ast/Expressions/LazyExpression.cs
  - flow-lang/Ast/Expressions/LiteralExpression.cs
  - flow-lang/Ast/Expressions/MemberAccessExpression.cs
  - flow-lang/Ast/Expressions/NoteStreamExpression.cs
  - flow-lang/Ast/Expressions/ProgressionExpression.cs
  - flow-lang/Ast/Expressions/SongExpression.cs
  - flow-lang/Ast/Expressions/SymbolLiteralExpression.cs
  - flow-lang/Ast/Expressions/TupleLiteralExpression.cs
  - flow-lang/Ast/Expressions/TupleUnpackFlowExpression.cs
  - flow-lang/Ast/Expressions/VariableExpression.cs
  - flow-lang/Ast/Statements/AssignmentStatement.cs
  - flow-lang/Ast/Statements/BreakStatement.cs
  - flow-lang/Ast/Statements/ContinueStatement.cs
  - flow-lang/Ast/Statements/ExpressionStatement.cs
  - flow-lang/Ast/Statements/ForStatement.cs
  - flow-lang/Ast/Statements/ImportStatement.cs
  - flow-lang/Ast/Statements/MusicalContextStatement.cs
  - flow-lang/Ast/Statements/ProcDeclaration.cs
  - flow-lang/Ast/Statements/ReturnStatement.cs
  - flow-lang/Ast/Statements/SectionDeclaration.cs
  - flow-lang/Ast/Statements/TupleDestructureStatement.cs
  - flow-lang/Ast/Statements/TuningContextStatement.cs
  - flow-lang/Ast/Statements/VariableDeclaration.cs
  - flow-lang/Ast/Statements/WhileStatement.cs
  - flow-lang.Tests/Phase35/LexerSpanTests.cs
  - flow-lang.Tests/Phase35/AstSpanTests.cs
  - flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs
autonomous: true
requirements: [LANG-04]
must_haves:
  truths:
    - "Every Token returned by SimpleLexer carries a non-Unknown Span whose Start equals the token's existing Location and whose End is the SourceLocation one character past the last consumed character."
    - "Every AST record produced by Parser.cs and Parser.NoteStream.cs carries a non-Unknown Span whose Start equals the production's open delimiter and End equals the production's last consumed token Location."
    - "All 1003+ existing xUnit tests under flow-lang.Tests pass green (Span migration is purely additive)."
    - "All existing tests/test_*.flow scripts produce byte-identical output before and after the Span migration."
    - "Phase 18/25/27/28 byte-identical determinism sentinels remain GREEN (two-run cmp-clean preserved per CLAUDE.md Conventions)."
  artifacts:
    - path: flow-lang/Core/Span.cs
      provides: "Span(SourceLocation Start, SourceLocation End) record + Unknown singleton + At(loc) convenience constructor"
      contains: "public record Span"
    - path: flow-lang/Core/SourceMap.cs
      provides: "File-path-keyed source-text registry; REPL/eval special-cased to in-memory sentinel keys"
      contains: "SourceMap"
    - path: flow-lang/Lexing/Token.cs
      provides: "Token record extended with Span? Span = null defaulted-param + EffectiveSpan accessor"
      contains: "Span? Span = null"
    - path: flow-lang.Tests/Phase35/LexerSpanTests.cs
      provides: "xUnit facts asserting every Token has non-Unknown Span matching source position"
      contains: "LexerSpanTests"
    - path: flow-lang.Tests/Phase35/AstSpanTests.cs
      provides: "xUnit facts asserting every AST record produced by Parser carries non-Unknown Span"
      contains: "AstSpanTests"
    - path: flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs
      provides: "xUnit fact asserting the full pre-Phase-35 suite remains green and tests/test_*.flow scripts produce identical output"
      contains: "SpanMigrationRegressionTests"
  key_links:
    - from: flow-lang/Lexing/SimpleLexer.cs
      to: flow-lang/Core/Span.cs
      via: "Every new Token(...) site passes Span: new Span(start, end) named-arg"
      pattern: "Span:.*new Span"
    - from: flow-lang/Parsing/Parser.cs
      to: flow-lang/Core/Span.cs
      via: "Every new XxxExpression / new XxxStatement site passes Span: new Span(location, PreviousToken.Location)"
      pattern: "Span:.*new Span"
    - from: flow-lang/Core/SourceMap.cs
      to: flow-lang/Core/FlowEngine.cs
      via: "FlowEngine.Execute(source, filePath) registers source text into SourceMap before lexing"
      pattern: "SourceMap.*Register|sourceMap\\."
---

<objective>
Land the Span migration foundation — this is Wave 1 of Phase 35 and the dependency root for every subsequent plan. Add a new `Span(SourceLocation Start, SourceLocation End)` record alongside the existing `SourceLocation` (which stays untouched per RESEARCH §Pitfall 1's 200+ read-site warning), extend `Token` and every AST record with a defaulted `Span? Span = null` last positional parameter using the Phase 21 / Phase 22 defaulted-parameter precedent, propagate Span population through every `new Token(...)` site in SimpleLexer (46 sites per RESEARCH §Sources) and every `new XxxExpression(...)` / `new XxxStatement(...)` site in Parser (~86 sites per RESEARCH §Sources), introduce a `SourceMap` registry keyed by file path with REPL/eval sentinel-key support, and verify zero regression across the existing 1003+ xUnit suite and tests/test_*.flow scripts.

Purpose: LANG-04 — Rust-style diagnostics depend on Span-rich AST nodes. The migration MUST be additive (defaulted parameters; Location stays) to avoid an 86-site sweep of LSP / tests / interpreter read-sites.

Output: New Span + SourceMap records; extended Token + 16 expression records + 14 statement records; lexer/parser populate Spans at every construction site; full pre-Phase-35 test suite remains green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-language-foundation/35-RESEARCH.md
@.planning/phases/35-language-foundation/35-PATTERNS.md
@.planning/phases/35-language-foundation/35-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase. -->
<!-- Executor uses these directly — no codebase exploration needed for the shape. -->

From flow-lang/Core/SourceLocation.cs (full file — pattern to mirror in Span.cs):
```
public record SourceLocation(int Line, int Column, string? FileName = null)
{
    public static SourceLocation Unknown { get; } = new(0, 0, null);
    public override string ToString() { ... }
}
```

From flow-lang/Lexing/Token.cs (current Phase 21 shape — extend with Span):
```
public record Token(
    TokenType Type,
    string Text,
    SourceLocation Location,
    object? Value = null,
    string? OriginalText = null)
{
    public string DiagnosticText => OriginalText ?? Text;
}
```

From flow-lang/Ast/AstNode.cs (base records — DO NOT change):
```
public abstract record AstNode(SourceLocation Location) { public FlowType? ResolvedType { get; init; } }
public abstract record Expression(SourceLocation Location) : AstNode(Location);
public abstract record Statement(SourceLocation Location) : AstNode(Location);
```

From flow-lang/Ast/Expressions/FunctionCallExpression.cs (exemplar shape — apply to all 16 expressions + 14 statements):
```
public record FunctionCallExpression(
    SourceLocation Location,
    string Name,
    IReadOnlyList<Expression> Arguments) : Expression(Location);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write failing test stubs for Span migration (LexerSpanTests + AstSpanTests + SpanMigrationRegressionTests)</name>
  <files>flow-lang.Tests/Phase35/LexerSpanTests.cs, flow-lang.Tests/Phase35/AstSpanTests.cs, flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs</files>
  <read_first>
    - flow-lang.Tests/flow-lang.Tests.csproj — confirm xUnit.v3 3.2.2 + test discovery pattern
    - flow-lang.Tests/Helpers/RmsRegressionTests.cs:25-56 — existing baseline helper shape
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 41-48 + lines 118-131 Wave 0 list)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 419-428 Pitfall 1 + lines 856-878 Wave 0 Gaps)
  </read_first>
  <behavior>
    - Test 1 (LexerSpanTests.EveryTokenHasNonUnknownSpan): Lex `(add 1 2)` and assert every emitted Token has `tok.Span != null && tok.Span != Span.Unknown` AND `tok.Span.Start == tok.Location`.
    - Test 2 (LexerSpanTests.MultiCharTokenSpansToEndOfToken): Lex `transpose` and assert the resulting Identifier token's `Span.End.Column == Span.Start.Column + 9` (length of "transpose").
    - Test 3 (AstSpanTests.EveryAstNodeHasNonUnknownSpanAfterParse): Parse `Int x = (add 1 2); (print x)` and walk the resulting Program AST asserting every Expression and Statement node has a non-Unknown Span.
    - Test 4 (AstSpanTests.NestedExpressionSpansParentBracketsChild): Parse `(add (mul 2 3) 4)` and assert the outer FunctionCallExpression's Span Start is the open paren and End is the close paren.
    - Test 5 (SpanMigrationRegressionTests.ExistingTestsRemainGreen): Marker test asserting `dotnet test` returns 0 across pre-Phase-35 facts (placeholder until Wave 1 lands; this fact lives as a smoke check that NEW Span fields don't break OLD code).
  </behavior>
  <action>Create three test files in `flow-lang.Tests/Phase35/` (new folder). Mirror the existing xUnit.v3 fact-class shape used throughout flow-lang.Tests/ (no class-level attributes; `[Fact]` per method; static helpers as needed). Each test must initially FAIL because `Span` does not yet exist (compile error) OR PASS because the assertion is vacuous (Span optional + null check). Use `FlowEngine` from `flow-lang/Core/FlowEngine.cs` to lex/parse the sample sources; access the resulting `IReadOnlyList&lt;Token&gt;` via the existing engine surface (use `SimpleLexer` directly if the engine doesn't expose tokens — verify by reading FlowEngine.cs first). For SpanMigrationRegressionTests, use a no-op `[Fact]` that asserts `true` initially; Wave 1 marks this as the regression sentinel and Task 4 verifies the broader suite via the `<verify>` command. DO NOT use fenced code blocks beyond the interface block already shown in `<context>` — write the C# directly per the patterns from RmsRegressionTests.cs.</action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tee /tmp/wave0-build.log; grep -c "error CS" /tmp/wave0-build.log</automated>
  </verify>
  <acceptance_criteria>
    - Source: `flow-lang.Tests/Phase35/LexerSpanTests.cs` exists and contains class `LexerSpanTests` with at least 2 `[Fact]` methods
    - Source: `flow-lang.Tests/Phase35/AstSpanTests.cs` exists and contains class `AstSpanTests` with at least 2 `[Fact]` methods
    - Source: `flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs` exists
    - Behavior: `grep -c "error CS" /tmp/wave0-build.log` reports ONLY references-to-Span errors (expected RED — Span doesn't exist yet); no errors unrelated to the Span migration
    - Done: Tests are written and either fail-to-compile due to missing Span (expected) or fail at runtime due to vacuous-true placeholders; Wave 1 implementation makes them GREEN
  </acceptance_criteria>
  <done>Three xUnit fact classes exist under flow-lang.Tests/Phase35/ with at least the 5 behaviors listed above; build either errors with Span-references-only OR builds with placeholder facts that Wave 1 will tighten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Span + SourceMap records; extend Token + every AST record with defaulted Span? Span = null parameter</name>
  <files>flow-lang/Core/Span.cs, flow-lang/Core/SourceMap.cs, flow-lang/Lexing/Token.cs, flow-lang/Ast/Expressions/ArrayIndexExpression.cs, flow-lang/Ast/Expressions/ArrayLiteralExpression.cs, flow-lang/Ast/Expressions/ChordLiteralExpression.cs, flow-lang/Ast/Expressions/FlowExpression.cs, flow-lang/Ast/Expressions/FunctionCallExpression.cs, flow-lang/Ast/Expressions/InterpolatedStringExpression.cs, flow-lang/Ast/Expressions/LambdaExpression.cs, flow-lang/Ast/Expressions/LazyExpression.cs, flow-lang/Ast/Expressions/LiteralExpression.cs, flow-lang/Ast/Expressions/MemberAccessExpression.cs, flow-lang/Ast/Expressions/NoteStreamExpression.cs, flow-lang/Ast/Expressions/ProgressionExpression.cs, flow-lang/Ast/Expressions/SongExpression.cs, flow-lang/Ast/Expressions/SymbolLiteralExpression.cs, flow-lang/Ast/Expressions/TupleLiteralExpression.cs, flow-lang/Ast/Expressions/TupleUnpackFlowExpression.cs, flow-lang/Ast/Expressions/VariableExpression.cs, flow-lang/Ast/Statements/AssignmentStatement.cs, flow-lang/Ast/Statements/BreakStatement.cs, flow-lang/Ast/Statements/ContinueStatement.cs, flow-lang/Ast/Statements/ExpressionStatement.cs, flow-lang/Ast/Statements/ForStatement.cs, flow-lang/Ast/Statements/ImportStatement.cs, flow-lang/Ast/Statements/MusicalContextStatement.cs, flow-lang/Ast/Statements/ProcDeclaration.cs, flow-lang/Ast/Statements/ReturnStatement.cs, flow-lang/Ast/Statements/SectionDeclaration.cs, flow-lang/Ast/Statements/TupleDestructureStatement.cs, flow-lang/Ast/Statements/TuningContextStatement.cs, flow-lang/Ast/Statements/VariableDeclaration.cs, flow-lang/Ast/Statements/WhileStatement.cs</files>
  <read_first>
    - flow-lang/Core/SourceLocation.cs (full file — mirror its shape for Span)
    - flow-lang/Lexing/Token.cs (current shape — add Span as 6th positional param after OriginalText)
    - flow-lang/Ast/Expressions/FunctionCallExpression.cs (canonical AST record exemplar)
    - flow-lang/Ast/Statements/VariableDeclaration.cs (canonical statement record exemplar)
    - flow-lang/Ast/Expressions/LambdaExpression.cs (note nested LambdaParameter record at line 10 — DOES NOT get Span; it is a sub-record not derived from AstNode)
    - flow-lang/Ast/Statements/TupleDestructureStatement.cs (note nested TupleDestructurePattern at line 13 — also skip)
    - flow-lang/TypeSystem/SpecialTypes/NoteType.cs:292 (Phase 22+25 17-defaulted-param precedent)
    - flow-lang/Runtime/ExecutionContext.cs:85 (SymbolInternTable Dictionary registry pattern for SourceMap shape reference)
    - flow-lang/Core/FlowEngine.cs (find Execute(source, filePath) signature — SourceMap registration entry-point)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 113-233 Bucket 1)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 292-337 Pattern 1 + lines 419-428 Pitfall 1)
  </read_first>
  <behavior>
    - Test 1 (Span.Unknown is a singleton): `Span.Unknown == Span.Unknown` and `Span.At(SourceLocation.Unknown)` produces a Span whose Start == End == SourceLocation.Unknown.
    - Test 2 (Span.ToString collapses on equal endpoints): `new Span(loc, loc).ToString() == loc.ToString()`; `new Span(start, end).ToString() == "{start}..{end}"`.
    - Test 3 (Existing record-construction call sites still compile): No call site in SimpleLexer.cs or Parser.cs needs modification to compile (defaulted parameter back-compat).
    - Test 4 (SourceMap registers and retrieves source text by path): `sourceMap.Register("/tmp/foo.flow", "source"); sourceMap.GetSource("/tmp/foo.flow") == "source"`.
    - Test 5 (SourceMap REPL sentinel keys): `sourceMap.Register("&lt;eval&gt;", "x"); sourceMap.GetSource("&lt;eval&gt;") == "x"` — sentinel `&lt;eval&gt;`, `&lt;stdin&gt;`, `&lt;repl&gt;` accepted.
  </behavior>
  <action>
    Create `flow-lang/Core/Span.cs` as a new record `Span(SourceLocation Start, SourceLocation End)` in namespace `FlowLang.Core`, with static `Unknown` singleton, static `At(SourceLocation)` zero-width constructor, static `Between(SourceLocation, SourceLocation)` convenience, and `ToString()` per PATTERNS.md Bucket 1 §Span.cs (collapse to single Start when Start == End else `Start..End`). DO NOT replace SourceLocation; both records coexist.

    Create `flow-lang/Core/SourceMap.cs` per PATTERNS.md Bucket 2a §SourceMap.cs — a class (NOT static; per-engine instance) holding `Dictionary&lt;string, string&gt; _sourceTexts = new(StringComparer.Ordinal)` with methods `Register(string path, string source)`, `GetSource(string path)`, and `TryGetSource(string path, out string source)`. Sentinel keys `&lt;eval&gt;`, `&lt;stdin&gt;`, `&lt;repl&gt;` register the in-memory source per RESEARCH §Don't Hand-Roll table. Wire the SourceMap into FlowEngine — add `public SourceMap SourceMap { get; } = new()` as an engine property; in `FlowEngine.Execute(source, filePath)` call `SourceMap.Register(filePath ?? "&lt;eval&gt;", source)` BEFORE lexing.

    Extend `flow-lang/Lexing/Token.cs` per PATTERNS.md Bucket 1 §Token.cs — append `Span? Span = null` as the 6th positional ctor param. Add helper `public Span EffectiveSpan => Span ?? FlowLang.Core.Span.At(Location);`. Keep `Location` positional 3rd-param unchanged.

    For each of the 16 expression files and 14 statement files listed in `<files>`, append `Span? Span = null` as the LAST positional ctor param using the PATTERNS.md Bucket 1 §Ast/Expressions exemplar — e.g. `public record FunctionCallExpression(SourceLocation Location, string Name, IReadOnlyList&lt;Expression&gt; Arguments, Span? Span = null) : Expression(Location);`. DO NOT touch `LambdaExpression`'s nested `LambdaParameter` record at line 10 (sub-record, not an AstNode) NOR `TupleDestructureStatement`'s nested `TupleDestructurePattern` at line 13. DO NOT alter the `: Expression(Location)` / `: Statement(Location)` base call — Location stays for the 200+ LSP/test read-sites per RESEARCH §Pitfall 1.

    No new lexer/parser logic in this task — Task 3 populates Spans at construction sites. This task is purely the record-shape extension.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj 2>&amp;1 | tail -20 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35" 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source: `flow-lang/Core/Span.cs` contains `public record Span(SourceLocation Start, SourceLocation End)` with static `Unknown`, `At`, `Between` members
    - Source: `flow-lang/Core/SourceMap.cs` exists with `Register`, `GetSource`, `TryGetSource` methods keyed `StringComparer.Ordinal`
    - Source: `grep -c "Span? Span = null" flow-lang/Lexing/Token.cs` returns 1
    - Source: `grep -lc "Span? Span = null" flow-lang/Ast/Expressions/*.cs | grep -c "1$"` returns at least 16 (every listed expression record extended; LambdaParameter is not in Expressions/ so it cannot accidentally be touched)
    - Source: `grep -lc "Span? Span = null" flow-lang/Ast/Statements/*.cs | grep -c "1$"` returns at least 14
    - Behavior: `dotnet build flow-lang/flow-lang.csproj` exits 0 with no compile errors
    - Behavior: `dotnet test --filter "FullyQualifiedName~Phase35.LexerSpanTests"` builds without errors (tests may still FAIL because lexer/parser haven't been wired in Task 3, but build succeeds)
    - Source: `grep -c "SourceMap.Register" flow-lang/Core/FlowEngine.cs` returns at least 1 (FlowEngine.Execute registers source before lex)
  </acceptance_criteria>
  <done>Span + SourceMap records exist; Token + all 16 expression + 14 statement records extended; FlowEngine registers source into SourceMap; full solution builds; Phase35 test classes compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Populate Span at every Token construction site in SimpleLexer.cs and every AST construction site in Parser.cs + Parser.NoteStream.cs</name>
  <files>flow-lang/Lexing/SimpleLexer.cs, flow-lang/Parsing/Parser.cs, flow-lang/Parsing/Parser.NoteStream.cs</files>
  <read_first>
    - flow-lang/Lexing/SimpleLexer.cs (focus on the 46 `new Token(...)` sites — start at line 52, 71, 79, 92, 140 per RESEARCH §Sources; full file scan to find all)
    - flow-lang/Parsing/Parser.cs (focus on ~86 `new XxxExpression(...)` / `new XxxStatement(...)` sites — example at line 1067)
    - flow-lang/Parsing/Parser.NoteStream.cs (note-stream construction sites; full file scan)
    - flow-lang/Lexing/SimpleLexer.cs:911-923 (existing Phase 21 OriginalText propagation precedent)
    - flow-lang/Core/Span.cs (Span record from Task 2)
    - .planning/phases/35-language-foundation/35-PATTERNS.md (lines 166-235 Bucket 1 SimpleLexer + Parser sections)
    - .planning/phases/35-language-foundation/35-RESEARCH.md (lines 419-428 Pitfall 1 — span-sweep audit reminder)
  </read_first>
  <behavior>
    - Test 1 (Every Token has non-Unknown Span post-lex): LexerSpanTests.EveryTokenHasNonUnknownSpan passes — every emitted Token's Span is non-null and not Span.Unknown.
    - Test 2 (Multi-char Token spans length-correct): LexerSpanTests.MultiCharTokenSpansToEndOfToken passes — for `transpose` token, `Span.End.Column - Span.Start.Column == 9`.
    - Test 3 (Every AST record has non-Unknown Span post-parse): AstSpanTests.EveryAstNodeHasNonUnknownSpanAfterParse passes — walk parsed AST asserting every Expression/Statement has Span != Unknown.
    - Test 4 (Nested expression Span brackets children): AstSpanTests.NestedExpressionSpansParentBracketsChild passes — outer `(add ...)` Span Start == open paren, End == close paren.
  </behavior>
  <action>
    In `flow-lang/Lexing/SimpleLexer.cs`: at each `new Token(...)` construction site (per PATTERNS.md Bucket 1 §SimpleLexer.cs — 46 sites), capture an `end` SourceLocation AT the position one character past the token's last consumed character (use the existing `_position` / `_column` advance pattern). Pass `Span: new Span(start, end)` as a NAMED argument so the diff is auditable. For single-char tokens (the `SingleChar` path noted in PATTERNS.md, around line 201), use `Span.At(start)` so Start == End. Use the existing local `start` variable convention; introduce a parallel local `end` immediately before each `new Token(...)` call.

    In `flow-lang/Parsing/Parser.cs` and `flow-lang/Parsing/Parser.NoteStream.cs`: at each `new XxxExpression(location, ...)` / `new XxxStatement(location, ...)` construction site, capture `endLocation = PreviousToken.Location` AFTER the production's closing delimiter is consumed (post-`Expect(...)`), and pass `Span: new Span(location, endLocation)` as a NAMED argument. For productions ending mid-loop where PreviousToken is not the closing delimiter, capture endLocation at the last consumed token before the constructor call. Per PATTERNS.md Bucket 1 §Parser.cs Notable Departures, inner arg expressions already carry their own Spans set by recursion — the OUTER node spans open-delimiter through close-delimiter.

    DO NOT introduce any new lexer/parser behavior. This task is purely propagation of Span through existing construction sites. DO NOT change `Location` arguments or AST node ordering.

    After implementing, run `grep -rE "new (FunctionCall|Literal|Variable|Flow|Lambda|Lazy|MemberAccess|Array|Symbol|Tuple|Note|Chord|Song|Progression|Interpolated|TupleUnpack)(Expression)" flow-lang/Parsing/ flow-lang/Lexing/` and verify every match passes `Span:` explicitly. Also `grep -rE "new (Assignment|Break|Continue|Expression|For|Import|MusicalContext|Proc|Return|Section|TupleDestructure|TuningContext|Variable|While)(Statement|Declaration)" flow-lang/Parsing/` and verify each passes `Span:`.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase35.LexerSpanTests|FullyQualifiedName~Phase35.AstSpanTests" 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `dotnet test --filter "FullyQualifiedName~Phase35.LexerSpanTests"` exits 0 (all lexer-span facts GREEN)
    - Behavior: `dotnet test --filter "FullyQualifiedName~Phase35.AstSpanTests"` exits 0 (all AST-span facts GREEN)
    - Source: `grep -nE "new Token\(" flow-lang/Lexing/SimpleLexer.cs | grep -vE "Span:" | grep -v "^//"` returns at most 0 lines (every Token construction passes Span explicitly, comments excluded)
    - Source: `grep -nE "new [A-Z][a-zA-Z]+(Expression|Statement|Declaration)\(" flow-lang/Parsing/Parser.cs flow-lang/Parsing/Parser.NoteStream.cs | grep -vE "Span:" | grep -v "^//" | grep -v "^[^:]*://" | wc -l` returns 0 (every AST construction passes Span explicitly)
    - Done: All Phase35 Lexer/Ast span tests pass; lexer + parser fully populate Span on every output node
  </acceptance_criteria>
  <done>Lexer and parser populate Span at every construction site; LexerSpanTests + AstSpanTests pass GREEN; no construction site escapes the grep audit.</done>
</task>

<task type="auto">
  <name>Task 4: Full regression sweep — assert 1003+ xUnit suite + tests/test_*.flow scripts pass byte-identical after Span migration</name>
  <files>flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs</files>
  <read_first>
    - flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs (the stub from Task 1)
    - .planning/phases/35-language-foundation/35-VALIDATION.md (lines 41-48 Wave 1 regression row)
    - tests/ (top-level — list .flow files via `ls tests/test_*.flow`)
    - CLAUDE.md (Conventions section — two-run cmp-clean inheritance + Phase 18/25/27 byte-identical sentinels)
  </read_first>
  <action>
    Tighten the SpanMigrationRegressionTests.cs placeholder fact from Task 1 into a real regression-sentinel fact. Add `[Fact] public void Phase35SpanMigration_PreservesExistingSuite()` that asserts via shelled-out check OR via a marker that the pre-Phase-35 facts continue to pass (the actual full-suite run happens via the `<verify>` block below — this fact is the audit-trail breadcrumb).

    Then verify the full regression manually:
    1. Run `dotnet test` and confirm zero failures across all 1003+ existing flow-lang.Tests facts (Phase 18 determinism, Phase 25 humanize, Phase 27 byte-identical pragma, Phase 28 articulation determinism, Phase 33 SFZ all GREEN).
    2. Run `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" || { echo "FAIL: $t"; exit 1; }; done` — every .flow script exits 0 (per CLAUDE.md "verified by their console output (success = no errors)").
    3. Spot-check byte-identical determinism: render `examples/tutorial.flow` twice to two output paths and `cmp` them — must exit 0.

    Do NOT modify any non-Phase-35 source. If any test fails, root-cause and fix in Task 2 or Task 3 — DO NOT touch unrelated test files.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj 2>&amp;1 | tail -10 &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done &amp;&amp; echo "ALL .flow SCRIPTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `dotnet test flow-lang.Tests/flow-lang.Tests.csproj` exits 0 with "Failed: 0" in summary line
    - Behavior: Loop over tests/test_*.flow reports "ALL .flow SCRIPTS PASS" (every script exits 0)
    - Source: `flow-lang.Tests/Phase35/SpanMigrationRegressionTests.cs` contains a `[Fact]` named `Phase35SpanMigration_PreservesExistingSuite` (audit-trail marker)
    - Done: Span migration is purely additive — every pre-Phase-35 test asset (xUnit + .flow) continues to pass with no source-level regression; two-run cmp-clean determinism contract from CLAUDE.md Conventions preserved
  </acceptance_criteria>
  <done>Full xUnit suite GREEN; every .flow script in tests/ exits 0; regression sentinel fact in place; Span migration verified zero-regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 35 work is internal to the language-tooling stack (lexer, parser, AST). No new untrusted-input crossings introduced by Span migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | SourceMap registry | accept | Single-user CLI threat model per RESEARCH §Security Domain. SourceMap holds source-text in process memory only; no network egress; no persistence. |
| T-35-02 | Integrity | AST Span population correctness | mitigate | Task 3 grep-audit gates verify every construction site passes Span explicitly. Pitfall 1 lint via xUnit (AstSpanTests.EveryAstNodeHasNonUnknownSpanAfterParse) prevents regression. |
| T-35-03 | Denial of Service | SourceMap memory growth in long REPL sessions | accept | REPL re-registers source under sentinel key `<eval>` per execution, OVERWRITING the prior entry. No unbounded growth. Verified by Register's Dictionary semantics. |
</threat_model>

<verification>
- xUnit Phase35 facts pass: `dotnet test --filter "FullyQualifiedName~Phase35"`
- Full xUnit suite pass: `dotnet test` exits 0 across all 1003+ pre-existing facts
- .flow regression loop: `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t"; done` — every script exits 0
- Span audit: `grep -nE "new Token\(" flow-lang/Lexing/SimpleLexer.cs | grep -vE "Span:"` returns 0 lines (excluding comments)
- Determinism sentinels: Phase 18 + Phase 25 + Phase 27 + Phase 28 byte-identical facts remain GREEN
</verification>

<success_criteria>
Plan complete when:
- [ ] `flow-lang/Core/Span.cs` exists with the documented record shape
- [ ] `flow-lang/Core/SourceMap.cs` exists and is wired into FlowEngine
- [ ] Token + every listed expression and statement record carries `Span? Span = null` as defaulted last positional param
- [ ] Every `new Token(...)` site in SimpleLexer.cs passes Span explicitly
- [ ] Every `new XxxExpression(...)` / `new XxxStatement(...)` site in Parser.cs + Parser.NoteStream.cs passes Span explicitly
- [ ] All 4 Wave 0 Phase35 facts (LexerSpan + AstSpan + SpanMigrationRegression) pass GREEN
- [ ] Full `dotnet test` suite exits 0 with zero failures
- [ ] Every tests/test_*.flow script exits 0
- [ ] Phase 18/25/27/28 byte-identical determinism preserved
</success_criteria>

<output>
Create `.planning/phases/35-language-foundation/35-01-SUMMARY.md` when done with: list of every file touched, audit grep results, full-suite test summary line, and the Span migration's blast-radius confirmation (LANG-04 prereq satisfied; Wave 2a / 2b plans unblocked).
</output>
