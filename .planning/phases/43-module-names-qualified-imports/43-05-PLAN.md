---
phase: 43-module-names-qualified-imports
plan: 05
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
  - 43-03
  - 43-04
files_modified:
  - flow-lang/audio.flow
  - flow-lang/bars.flow
  - flow-lang/collections.flow
  - flow-lang/composition.flow
  - flow-lang/generative.flow
  - flow-lang/improv.flow
  - flow-lang/notation-io.flow
  - flow-lang/notation.flow
  - flow-lang/osc.flow
  - flow-lang/patterns.flow
  - flow-lang/sfz.flow
  - flow-lang/test.flow
autonomous: true
requirements:
  - REQ-MOD-06
  - REQ-MOD-09
  - REQ-MOD-11
  - REQ-MOD-12
tags:
  - phase-43
  - stdlib-migration
  - regression-bar
  - closer
must_haves:
  truths:
    - "12 stdlib .flow files declare `module <name>` per D-07 table — `audio`, `bars`, `collections`, `composition`, `generative`, `improv`, `notation` (in notation-io.flow), `notes` (in notation.flow per D-07 / Pitfall 6 rename-not-merge resolution), `osc`, `patterns`, `sfz`, `test`"
    - "`std.flow` remains declaration-less (always-on prelude — keeps existing unqualified-only behavior per D-07)"
    - "Each migrated file places the `module <name>` declaration as the first NON-COMMENT statement; existing `Note:` comments at the top stay above the declaration (D-01 — comments are not statements)"
    - "After migration, `use \"@notation-io\"` registers `notation` in ModuleRegistry; `use \"@notation\"` (loading notation.flow via @std transitive load) registers `notes`. Composer can `(notation.writeMusicXML ...)` and `(notes.whole)`"
    - "Composer scripts at `examples/symphony/symphony.flow`, `examples/ragtime/ragtime.flow`, `examples/dsp/granular.flow` run end-to-end without firing ANY `[module]` advisories (those scripts don't qualify; no overlapping exports between the 12 stdlib modules cause unprompted shadow advisories)"
    - "All 123 `tests/test_*.flow` happy-path scripts continue to PASS"
    - "Full xUnit suite GREEN modulo the 34 pre-existing Phase 28/29/35/38 failures per Phase 42 deferred-items.md baseline (no NEW failures from Phase 43)"
    - "D-11: 13-file stdlib migration ships in ONE commit (pre-traction no-deprecation latitude — `project_pre_public_no_legacy_burden`; breaking syntax in one commit is the project contract)"
    - "D-12: NO `flow migrate` composer-facing CLI subcommand — the in-repo migrator is sufficient (reconsider only if a third-party fork appears)"
  artifacts:
    - path: "flow-lang/audio.flow"
      provides: "module audio declaration as first non-comment statement"
      contains: "module audio"
    - path: "flow-lang/bars.flow"
      provides: "module bars declaration"
      contains: "module bars"
    - path: "flow-lang/collections.flow"
      provides: "module collections declaration"
      contains: "module collections"
    - path: "flow-lang/composition.flow"
      provides: "module composition declaration"
      contains: "module composition"
    - path: "flow-lang/generative.flow"
      provides: "module generative declaration"
      contains: "module generative"
    - path: "flow-lang/improv.flow"
      provides: "module improv declaration"
      contains: "module improv"
    - path: "flow-lang/notation-io.flow"
      provides: "module notation declaration (claims the canonical name per Pitfall 6 rename resolution)"
      contains: "module notation"
    - path: "flow-lang/notation.flow"
      provides: "module notes declaration (renamed per D-07 + Pitfall 6 — file stays at notation.flow path; declared name changes to notes)"
      contains: "module notes"
    - path: "flow-lang/osc.flow"
      provides: "module osc declaration"
      contains: "module osc"
    - path: "flow-lang/patterns.flow"
      provides: "module patterns declaration"
      contains: "module patterns"
    - path: "flow-lang/sfz.flow"
      provides: "module sfz declaration"
      contains: "module sfz"
    - path: "flow-lang/test.flow"
      provides: "module test declaration"
      contains: "module test"
  key_links:
    - from: "flow-lang/notation.flow"
      to: "flow-lang/Runtime/ModuleRegistry.cs"
      via: "module notes declaration — registers under name 'notes' (not 'notation') per Pitfall 6 rename"
      pattern: "^module notes$"
    - from: "flow-lang/notation-io.flow"
      to: "flow-lang/Runtime/ModuleRegistry.cs"
      via: "module notation declaration — claims canonical name"
      pattern: "^module notation$"
---

<objective>
Migrate the 12 stdlib `.flow` files to declare their module names per CONTEXT D-07 table, in ONE commit (D-11 pre-traction no-deprecation latitude). The migration is the surface that composers will actually feel — once 12 stdlib modules are registered, qualified access like `(notation.writeMusicXML song "out.xml")` becomes the disambiguation tool composers can reach for. The `notation.flow` ↔ `notation-io.flow` collision is resolved per Pitfall 6: `notation-io.flow` claims `module notation` (it IS the notation export/import surface), and `notation.flow` declares `module notes` (renamed because it's mostly Note-helper procs and is transitively loaded via @std — merging into notation-io would force the heavy notation surface into every script's prelude). This is the final wave of Phase 43; it consumes the parser/registry/dispatcher work from Plans 01/02/03 and the Beat-backfill from Plan 04. Closes REQ-MOD-06 + REQ-MOD-09 (notation rename) + REQ-MOD-11 (composer scripts unaffected) + REQ-MOD-12 (final regression bar).

Purpose: After this plan, the 12-file migration is live, the regression bar is met, and Phase 43 ships. STATE.md tracking + ROADMAP.md tickbox + REQUIREMENTS.md cross-insert happen here.

Output: 12 stdlib `.flow` files modified; `flow-lang/notation.flow` is still at its original path but declares `module notes` (Pitfall 6 — file rename NOT done; only the declared name changes); full xUnit + .flow-script regression bar GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-module-names-qualified-imports/43-CONTEXT.md
@.planning/phases/43-module-names-qualified-imports/43-RESEARCH.md
@.planning/phases/43-module-names-qualified-imports/43-PATTERNS.md
@.planning/phases/43-module-names-qualified-imports/43-VALIDATION.md
@.planning/phases/43-module-names-qualified-imports/43-01-PLAN.md
@.planning/phases/43-module-names-qualified-imports/43-02-PLAN.md
@.planning/phases/43-module-names-qualified-imports/43-03-PLAN.md
@.planning/phases/43-module-names-qualified-imports/43-04-PLAN.md

<interfaces>
<!-- Locked stdlib module-name assignments from CONTEXT D-07 + RESEARCH lines 28-43 + PATTERNS lines 586-600. -->

| File path | Module declaration | Notes |
|-----------|--------------------|-------|
| flow-lang/audio.flow | `module audio` | obvious |
| flow-lang/bars.flow | `module bars` | obvious |
| flow-lang/collections.flow | `module collections` | obvious |
| flow-lang/composition.flow | `module composition` | obvious |
| flow-lang/generative.flow | `module generative` | obvious |
| flow-lang/improv.flow | `module improv` | obvious |
| flow-lang/notation-io.flow | `module notation` | claims canonical name — IS the notation export/import surface (Phase 39) |
| flow-lang/notation.flow | `module notes` | RENAMED per D-07 + Pitfall 6 — file stays at notation.flow path; declared name changes (mostly Note helpers — `whole`/`half`/`quarter`/`eighth` + Note duration constants) |
| flow-lang/osc.flow | `module osc` | obvious |
| flow-lang/patterns.flow | `module patterns` | obvious |
| flow-lang/sfz.flow | `module sfz` | obvious |
| flow-lang/std.flow | _no declaration_ | always-on prelude — keeps existing unqualified-only behavior |
| flow-lang/test.flow | `module test` | obvious |

Per D-01: the `module` declaration is the first NON-COMMENT statement. Flow comments are `Note: ...` lines.

Skeleton from PATTERNS lines 632-643 + RESEARCH §"`module` declaration in a stdlib `.flow` file":
```flow
Note: Phase 36 Plan 36-05 — @patterns stdlib module
Note: 13 Tidal-style sequence combinators (D-36-01 hybrid)

module patterns

use "@std"

internal proc every (Int: n, Function: cb, Sequence: seq)
internal proc fast (Sequence: seq, Double: factor)
...
```

The migration adds exactly one new line (`module <name>`) followed by a blank line, immediately after the file's leading `Note:` comments (if any) and BEFORE any other content (including the `use "@std"` line that most stdlib files have).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module declarations to 12 stdlib .flow files (notation-io / notation rename per Pitfall 6) + composer-script smoke</name>
  <files>
    flow-lang/audio.flow,
    flow-lang/bars.flow,
    flow-lang/collections.flow,
    flow-lang/composition.flow,
    flow-lang/generative.flow,
    flow-lang/improv.flow,
    flow-lang/notation-io.flow,
    flow-lang/notation.flow,
    flow-lang/osc.flow,
    flow-lang/patterns.flow,
    flow-lang/sfz.flow,
    flow-lang/test.flow
  </files>
  <read_first>
    flow-lang/std.flow (verify it stays declaration-less per D-07 — DO NOT modify this file),
    flow-lang/audio.flow (header — verify leading Note: comments to identify where the module line goes),
    flow-lang/notation.flow (header AND a few procs to confirm it's mostly Note helpers — supports Pitfall 6 rename-not-merge),
    flow-lang/notation-io.flow (header — confirm it's the Phase 39 notation-IO surface),
    flow-lang/bars.flow,
    flow-lang/collections.flow,
    flow-lang/composition.flow,
    flow-lang/generative.flow,
    flow-lang/improv.flow,
    flow-lang/osc.flow,
    flow-lang/patterns.flow,
    flow-lang/sfz.flow,
    flow-lang/test.flow (all 11 — each first 5 lines is sufficient to locate the comment-vs-statement boundary),
    examples/symphony/symphony.flow (composer-facing smoke — REQ-MOD-11 manual check),
    examples/ragtime/ragtime.flow (composer-facing smoke),
    examples/dsp/granular.flow (composer-facing smoke),
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pitfall 6: notation.flow rename vs the notation-io collision" lines 501-510,
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"flow-lang/*.flow — 13 stdlib module declarations" lines 565-600,
    .planning/phases/43-module-names-qualified-imports/43-CONTEXT.md D-07 + D-11 (pre-traction breaking-change single-commit latitude)
  </read_first>
  <action>
    For EACH of the 12 files in `files_modified`:

    1. Read the file's first ~10 lines.
    2. Identify the leading `Note: ...` comment block (zero or more lines starting with `Note:` per Flow's comment grammar; Flow uses `Note: ...\n` as the standard comment form — verify against existing files).
    3. After the LAST `Note:` line and before any other content (any `use "@..."` lines, any `internal proc` / `proc` declarations, any anything else), insert TWO lines:
       - `module <name>` (the assignment from the D-07 table).
       - A blank line.
    4. If the file has zero leading `Note:` comments, the `module <name>` line becomes the very first line of the file (still followed by a blank line and then the existing content).

    File-by-file module assignments (verbatim from D-07):
    - `flow-lang/audio.flow` → `module audio`
    - `flow-lang/bars.flow` → `module bars`
    - `flow-lang/collections.flow` → `module collections`
    - `flow-lang/composition.flow` → `module composition`
    - `flow-lang/generative.flow` → `module generative`
    - `flow-lang/improv.flow` → `module improv`
    - `flow-lang/notation-io.flow` → `module notation` (claims canonical name per Pitfall 6)
    - `flow-lang/notation.flow` → `module notes` (RENAMED per Pitfall 6 — file path unchanged; declared name is `notes`)
    - `flow-lang/osc.flow` → `module osc`
    - `flow-lang/patterns.flow` → `module patterns`
    - `flow-lang/sfz.flow` → `module sfz`
    - `flow-lang/test.flow` → `module test`

    DO NOT TOUCH `flow-lang/std.flow` — per D-07 it stays declaration-less (always-on prelude). The ModuleLoader hook (Plan 03) skips files without a `module` declaration; std.flow's procs continue injecting unqualified-only.

    Per D-11 pre-traction no-deprecation latitude: this migration ships in ONE commit. No `flow migrate` subcommand. The in-repo `tests/test_*.flow` sweep is the verification gate.

    Per Pitfall 6: do NOT rename the file `notation.flow → notes.flow`. The composer-facing `use "@notation"` (which loads notation.flow via the @std transitive load) keeps working — only the *declared module name* changes. Composers don't currently call notation.flow's procs via qualified access (they're unqualified Note helpers); after migration they can choose to qualify with `notes.whole` etc. if desired. `use "@notation-io"` (Phase 39 explicit opt-in) loads notation-io.flow which now claims `module notation`. The two are no longer in collision.

    After all 12 file edits, run the composer-script smoke (REQ-MOD-11 manual check noted in VALIDATION.md §Manual-Only Verifications):
    - `dotnet run --project flow-interpreter examples/symphony/symphony.flow` → completes successfully, no `[module]` advisories in stderr.
    - `dotnet run --project flow-interpreter examples/ragtime/ragtime.flow` → same.
    - `dotnet run --project flow-interpreter examples/dsp/granular.flow` → same.

    These three composer scripts don't qualify (don't use `mod.fn` syntax); they don't import overlapping modules. They should be unaffected by the migration — running them WITH the new stdlib declarations should fire zero `[module]` advisories. If any advisory fires (e.g. duplicate-module from a re-`use` chain), investigate which two files claim the same module name and fix the migration. (Expected: no advisories fire because each module name in the D-07 table is unique after the notation-io/notation resolution.)
  </action>
  <verify>
    <automated>
    dotnet build flow-lang/flow-lang.csproj &amp;&amp;
    for f in audio bars collections composition generative improv osc patterns sfz test; do
      head -10 flow-lang/$f.flow | grep -v '^Note:' | grep -v '^$' | head -1 | grep -q "^module $f$" || { echo "FAIL: $f.flow missing 'module $f' as first non-comment statement"; exit 1; };
    done &amp;&amp;
    head -10 flow-lang/notation-io.flow | grep -v '^Note:' | grep -v '^$' | head -1 | grep -q "^module notation$" || { echo "FAIL: notation-io.flow"; exit 1; } &amp;&amp;
    head -10 flow-lang/notation.flow | grep -v '^Note:' | grep -v '^$' | head -1 | grep -q "^module notes$" || { echo "FAIL: notation.flow"; exit 1; } &amp;&amp;
    ! head -10 flow-lang/std.flow | grep -q '^module ' || { echo "FAIL: std.flow must remain declaration-less"; exit 1; } &amp;&amp;
    dotnet run --project flow-interpreter examples/symphony/symphony.flow &gt; /tmp/sym.stdout 2&gt; /tmp/sym.stderr; status=$?; [ $status -eq 0 ] || { echo "FAIL: symphony.flow exit $status"; cat /tmp/sym.stderr; exit 1; }; ! grep -q '\[module\]' /tmp/sym.stderr || { echo "FAIL: symphony.flow fired [module] advisory"; cat /tmp/sym.stderr; exit 1; } &amp;&amp;
    dotnet run --project flow-interpreter examples/ragtime/ragtime.flow &gt; /tmp/rag.stdout 2&gt; /tmp/rag.stderr; status=$?; [ $status -eq 0 ] || { echo "FAIL: ragtime.flow exit $status"; cat /tmp/rag.stderr; exit 1; }; ! grep -q '\[module\]' /tmp/rag.stderr || { echo "FAIL: ragtime.flow fired [module] advisory"; exit 1; } &amp;&amp;
    dotnet run --project flow-interpreter examples/dsp/granular.flow &gt; /tmp/gr.stdout 2&gt; /tmp/gr.stderr; status=$?; [ $status -eq 0 ] || { echo "FAIL: granular.flow exit $status"; exit 1; }; ! grep -q '\[module\]' /tmp/gr.stderr || { echo "FAIL: granular.flow fired [module] advisory"; exit 1; }
    </automated>
  </verify>
  <done>
    All 12 listed `.flow` files have their module declaration as the first non-comment statement; `std.flow` is unmodified; `notation-io.flow` declares `module notation`; `notation.flow` declares `module notes`; the three composer-facing examples (symphony/ragtime/granular) run without firing any `[module]` advisory and exit successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase 43 final regression bar — full xUnit + 123 .flow scripts + tracking sweep</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/43-module-names-qualified-imports/43-VERIFICATION.md
  </files>
  <read_first>
    .planning/STATE.md (current frontmatter — verify gsd_state_version + last_activity + Phase 43 row status),
    .planning/ROADMAP.md lines 102-135 (Phase 43 row + Plan list — currently empty; populate after migration ships),
    .planning/REQUIREMENTS.md (find the v1.5 Traceability table at line 195+; add REQ-MOD-01..12 rows mapped to Phase 43),
    .planning/phases/42-type-system-stdlib-audit/42-VERIFICATION.md (template for closure-doc style — Phase 42 just shipped this; same convention),
    .planning/phases/42-type-system-stdlib-audit/deferred-items.md (the 34 pre-existing failures baseline — Phase 43 inherits this as the regression bar baseline),
    .planning/phases/43-module-names-qualified-imports/43-VALIDATION.md (Manual-Only Verifications table + Sampling Rate + Pre-existing Failures Posture)
  </read_first>
  <action>
    Full regression bar — run in order and stop on any new failure:

    1. `dotnet build flow-lang/flow-lang.csproj` — must succeed with zero new warnings.
    2. `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43"` — must report all Phase 43 fixtures GREEN (Plan 01: 5, Plan 02: 6, Plan 03: 5+2+4=11, Plan 04: 6+5=11 → ~33 facts total; verify by counting).
    3. `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42.AuditHarnessTests"` — all 9 GREEN INCLUDING the polarity-flipped `OrphanList_DoesNotContainBeatType`.
    4. `dotnet test flow-lang.Tests` — full suite. Expected: pre-existing 34 failures from Phase 28/29/35/38 baseline (per deferred-items.md). ZERO new failures introduced by Phase 43.
    5. `for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" > /dev/null 2>&1 || { echo "FAIL: $t"; exit 1; }; done` — all 123 happy-path scripts pass.

    Tracking-file sweep (mirror Phase 42 closer Plan 42-04 style):

    Modify `.planning/STATE.md`:
    - Update `last_updated` to current ISO timestamp.
    - Update `last_activity` to today's date.
    - Update frontmatter `progress`: `completed_phases: 6` (35+36+37+38+39+42 already; +43 now = 6+1 wait — STATE.md shows 5/10 complete with 37 plans; recount Phase 43 plan total of 5 and adjust both `total_plans` and `completed_plans`). Verify current totals via `gsd-sdk query state.load`.
    - Update `stopped_at` to `Phase 43 shipped`.
    - Move Phase 43 from "Pending — AUDIT.md-fed" to a "Shipped 2026-05-24" entry in the v1.5 Phase Map table.
    - Add a Phase 43 highlights block under "Phase 38 highlights:" pattern (5-8 bullet lines summarizing: module keyword + Registry + dispatcher + Beat backfill + stdlib migration + Phase 42 polarity flip + all 22 new Phase 43 fixtures GREEN).
    - Update "Resume Instructions" to point at Phase 40 OR Phase 44 as the next composer pick (no longer Phase 43).

    Modify `.planning/ROADMAP.md`:
    - Phase 43 row: flip checkbox to `[x]`. Update completion date.
    - In the Phase 43 detail section (around line 130 — already says "completed 2026-05-24" though STATE.md said Pending; this plan finally truths it up), populate the **Plans:** subsection listing all 5 plans with their files modified + commit refs:
      ```
      - [x] 43-01-PLAN.md — Lexer/Parser/AST module-declaration surface (REQ-MOD-01)
      - [x] 43-02-PLAN.md — ModuleRegistry + ExecutionContext property (REQ-MOD-02)
      - [x] 43-03-PLAN.md — ModuleLoader hook + dispatcher + collision/shadow advisories (REQ-MOD-03/04/05)
      - [x] 43-04-PLAN.md — Beat backfill (beatToSec/secToBeat + delay/renderBarAtBeat overloads) + Phase 42 polarity flip (REQ-MOD-07/08/09/10/11 + D-10)
      - [x] 43-05-PLAN.md — 12-file stdlib migration + final regression bar (REQ-MOD-06/12 + closer)
      ```
    - Add Phase 43 row to the bottom "Progress" table at lines 300+ (Plan 43 progress 5/5 Complete).

    Modify `.planning/REQUIREMENTS.md`:
    - Add new section `### Module Names & Qualified Imports (Phase 43)` BEFORE the existing `### Type System & Stdlib Audit (Phase 42)` section, listing REQ-MOD-01..12 with brief descriptions cross-referenced to the plans that shipped them.
    - Add 12 new rows (REQ-MOD-01..12) to the v1.5 Traceability table at line 195+, each marked `Shipped (Plan 43-NN — <commit>)` matching the actual git commits.
    - Update the Coverage summary at line 263+ to reflect 75+12=87 v1.5 requirements (BUT — verify the count is 75 first, since Phase 42 added 9; current is 75 per STATE.md frontmatter; after Phase 43 should be 87 if REQ-MOD-NN are 12). Adjust per-phase counts accordingly.

    Create `.planning/phases/43-module-names-qualified-imports/43-VERIFICATION.md`:
    - Frontmatter mirroring Phase 42's 42-VERIFICATION.md (status `verified`, nyquist_compliant: true, full-suite-results record).
    - Section §1 "Truth verification" — each must-have truth from each plan checked off with the test that pins it.
    - Section §2 "Known Caveats" — the 34 pre-existing Phase 28/29/35/38 failures (cite Phase 42 deferred-items.md + this Phase's baseline).
    - Section §3 "REQ-MOD-NN ↔ Plan trace" — 12-row table.
    - Section §4 "D-NN decision trace" — 12-row table mapping each CONTEXT.md decision to the plan + commit that shipped it.
    - Section §5 "Two-run cmp-clean confirmation" — note that Phase 43 preserves the contract for non-`live` paths (Pitfall 8 — stderr is captured separately from WAV byte-cmp; default-120-BPM is deterministic).

    Git commit message ALL Phase 43 work as separate commits per plan/task — this Task 2 is the closer commit only; production-code commits already happened. The commit for this task is docs-only (.planning/* updates + 43-VERIFICATION.md creation).

    NOTE: If during the full-suite regression run any NEW xUnit failure (not in the pre-existing 34) appears OR any `tests/test_*.flow` script fails, STOP and route back to whichever plan introduced the regression. Do NOT mask failures by extending the deferred-items list (Phase 42 anti-pattern guard).
  </action>
  <verify>
    <automated>
    dotnet build flow-lang/flow-lang.csproj &amp;&amp;
    dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43" --logger "console;verbosity=minimal" &amp;&amp;
    dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42.AuditHarnessTests" --logger "console;verbosity=minimal" &amp;&amp;
    for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done &amp;&amp;
    test -f .planning/phases/43-module-names-qualified-imports/43-VERIFICATION.md &amp;&amp;
    grep -q '\[x\] \*\*Phase 43' .planning/ROADMAP.md &amp;&amp;
    grep -q 'REQ-MOD-01' .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <done>
    Full regression bar PASSED (Phase 43 xUnit fixtures GREEN, Phase 42 AuditHarnessTests GREEN including polarity flip, 123 `.flow` scripts pass, only the pre-existing 34 failures remain in the full suite); STATE.md updated with Phase 43 shipped status + highlights; ROADMAP.md Phase 43 row checked + plan list populated; REQUIREMENTS.md REQ-MOD-01..12 rows added + traceability rows mapped to commits; 43-VERIFICATION.md exists with the 5-section closure documentation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdlib .flow source → ModuleLoader → ModuleRegistry | All 12 modified files are operator-trusted in-repo source |
| composer .flow scripts → migrated stdlib | Composer-trusted scripts running against the new module registrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09 | Tampering | Stdlib module-name collision after migration | mitigate | The D-07 table is verified collision-free (notation.flow → `notes`; notation-io.flow → `notation`). Task 2 full-suite regression bar fires if any composer script hits an unexpected `[module]` advisory. |
| T-43-10 | Tampering | composer scripts breaking due to module-keyword reservation | mitigate | RESEARCH Pitfall 1 + grep-verified — zero `.flow` files use `module` as a proc/variable/identifier today. Task 1 composer-script smoke + Task 2 full-suite regression catch regressions immediately. |
| T-43-11 | Information Disclosure | Migration commit touches 12 files at once | accept | Single-commit per D-11 pre-traction no-deprecation latitude. In-repo migration; no third-party fork to coordinate with. |
</threat_model>

<verification>
- All 12 listed stdlib `.flow` files have their `module <name>` declaration as the first non-comment statement matching the D-07 table.
- `flow-lang/std.flow` remains unchanged (declaration-less per D-07).
- `notation-io.flow` declares `module notation`; `notation.flow` declares `module notes` (Pitfall 6 rename-not-merge).
- Composer scripts `symphony.flow`, `ragtime.flow`, `granular.flow` run end-to-end with zero `[module]` advisories.
- Phase 43 xUnit fixtures all GREEN (Plans 01-04 contribute ~33 facts).
- Phase 42 AuditHarnessTests all GREEN including the D-10 polarity-flipped `OrphanList_DoesNotContainBeatType`.
- 123 `tests/test_*.flow` happy-path scripts continue to PASS.
- Full xUnit suite: pre-existing 34 failures from Phase 28/29/35/38 baseline remain; ZERO new failures introduced by Phase 43.
- Tracking files (STATE.md + ROADMAP.md + REQUIREMENTS.md) reflect Phase 43 shipped status.
- 43-VERIFICATION.md exists documenting REQ-MOD-NN ↔ plan trace + D-NN decision trace + caveats.
</verification>

<success_criteria>
- 12 stdlib `.flow` files migrated per D-07.
- Composer-script smoke (3 scripts) shows zero `[module]` advisories.
- Phase 43 fixtures GREEN.
- Phase 42 fixtures GREEN (including the polarity flip from Plan 04).
- `.flow` happy-path suite GREEN.
- Full xUnit regression baseline preserved (pre-existing 34 only).
- Tracking-file sweep complete (STATE.md + ROADMAP.md + REQUIREMENTS.md + 43-VERIFICATION.md).
- Phase 43 ready to ship.
</success_criteria>

<output>
Create `.planning/phases/43-module-names-qualified-imports/43-05-SUMMARY.md` documenting: the 12-file migration commit reference, composer-script smoke results (any unexpected stderr observed + dispositions), the final xUnit fact count (Phase 43 total + Phase 42 inherited), the pre-existing-34 baseline preservation, and the tracking-sweep diff summary (STATE.md + ROADMAP.md + REQUIREMENTS.md changes). Cross-reference 43-VERIFICATION.md for the truth-by-truth audit.
</output>
