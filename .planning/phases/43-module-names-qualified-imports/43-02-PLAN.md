---
phase: 43-module-names-qualified-imports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/Runtime/ModuleRegistry.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang.Tests/Integration/Phase43/ModuleRegistryTests.cs
autonomous: true
requirements:
  - REQ-MOD-02
tags:
  - phase-43
  - runtime
  - registry
must_haves:
  truths:
    - "A fresh ExecutionContext exposes a non-null ModuleRegistry property"
    - "ModuleRegistry.Register('math', procs) followed by Contains('math') returns true"
    - "ModuleRegistry.TryGetProc('math', 'sin', out var proc) returns true with a non-null Value when 'sin' is in the registered ExportedProcSet"
    - "ModuleRegistry.TryGetProc('math', 'nope', out _) returns false; ModuleRegistry.TryGetProc('unknown', 'anything', out _) returns false"
    - "Registry is per-ExecutionContext (NOT process-global) — distinct FlowEngine instances do not share registry state, mirroring PrngRegistry / LiveBlockRegistry / StyleRegistry shape"
  artifacts:
    - path: "flow-lang/Runtime/ModuleRegistry.cs"
      provides: "sealed class with ConcurrentDictionary<string, IReadOnlyDictionary<string, Value>> backing and Register / Contains / TryGetProc / Snapshot / Clear API"
      contains: "public sealed class ModuleRegistry"
      min_lines: 30
    - path: "flow-lang/Runtime/ExecutionContext.cs"
      provides: "new ModuleRegistry property mirroring PrngRegistry/LiveBlockRegistry declaration shape"
      contains: "public ModuleRegistry ModuleRegistry"
    - path: "flow-lang.Tests/Integration/Phase43/ModuleRegistryTests.cs"
      provides: "Wave 0 unit-test scaffold covering register / lookup / dedup-on-duplicate / isolation"
      contains: "ModuleRegistry"
  key_links:
    - from: "flow-lang/Runtime/ExecutionContext.cs"
      to: "flow-lang/Runtime/ModuleRegistry.cs"
      via: "public ModuleRegistry ModuleRegistry { get; } = new();"
      pattern: "public ModuleRegistry ModuleRegistry"
---

<objective>
Create the `ModuleRegistry` runtime data structure (per-ExecutionContext, ConcurrentDictionary-backed, mirroring `LiveBlockRegistry` / `PrngRegistry` / `StyleRegistry` shape per RESEARCH A1) and expose it as a property on `ExecutionContext`. The registry is the lookup table that Wave 2 will fill (from ModuleLoader, Plan 03) and read (from ExpressionEvaluator, Plan 03). This plan ships ZERO behavior-visible changes to existing Flow scripts — it is pure infrastructure, parallelizable with Plan 01.

Purpose: Establish the data shape and lifecycle so Wave 2 plans can wire their hooks against a real type instead of a sketched interface. Independent file set from Plan 01 — both run in Wave 1.

Output: New `ModuleRegistry` class, new `ExecutionContext.ModuleRegistry` property, Wave 0 xUnit unit-tests pinning the registry shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/43-module-names-qualified-imports/43-CONTEXT.md
@.planning/phases/43-module-names-qualified-imports/43-RESEARCH.md
@.planning/phases/43-module-names-qualified-imports/43-PATTERNS.md
@.planning/phases/43-module-names-qualified-imports/43-VALIDATION.md

<interfaces>
<!-- Key shapes the executor needs. Extracted from codebase + PATTERNS.md. -->

From flow-lang/Runtime/LiveBlockRegistry.cs (the closest-by-shape analog — sealed class + ConcurrentDictionary backing + Register/Snapshot/Clear):
```csharp
public sealed class LiveBlockRegistry
{
    private readonly ConcurrentDictionary<int, LiveBlockRegistration> _registry = new();
    public void Register(LiveBlockRegistration registration) { _registry[registration.BlockId] = registration; }
    public IReadOnlyDictionary<int, LiveBlockRegistration> Snapshot() { return new Dictionary<int, LiveBlockRegistration>(_registry); }
    public void Clear() { _registry.Clear(); }
}
```

From flow-lang/Runtime/ExecutionContext.cs (lines 131-187 — registry-property precedents PrngRegistry, LiveBlockRegistry, StyleRegistry):
```csharp
public PrngRegistry PrngRegistry { get; } = new();
public LiveBlockRegistry LiveBlockRegistry { get; } = new();
public Dictionary<Value, DictData> StyleRegistry { get; } = new();
```

Each has a multi-paragraph XML doc following the cadence:
`Phase N Plan N-NN (D-X-NN) — <one-line role>. <details>. Mirrors the <see cref="X"/> shape — singleton-per-context, ConcurrentDictionary-backed for ...`

From flow-lang/Runtime/Value.cs (factory methods 23-52):
```csharp
public static Value Function(FunctionOverload overload); // produces a Function-typed Value the dispatcher returns
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ModuleRegistry class + ExecutionContext property + Wave 0 unit tests</name>
  <files>
    flow-lang/Runtime/ModuleRegistry.cs,
    flow-lang/Runtime/ExecutionContext.cs,
    flow-lang.Tests/Integration/Phase43/ModuleRegistryTests.cs
  </files>
  <read_first>
    flow-lang/Runtime/LiveBlockRegistry.cs (entire ~99-line file — closest-shape analog),
    flow-lang/Runtime/PrngRegistry.cs (entire file — secondary analog for XML doc cadence + lookup-or-create pattern),
    flow-lang/Runtime/ExecutionContext.cs lines 120-200 (insertion site adjacent to PrngRegistry / LiveBlockRegistry / StyleRegistry properties),
    flow-lang/Runtime/Value.cs lines 1-60 (factory methods),
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pattern 4: Per-ExecutionContext Registry Field" lines 350-367,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Alternatives Considered" line 147 (per-context vs process-global rationale; A1 ASSUMPTION),
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"flow-lang/Runtime/ModuleRegistry.cs" (recommended skeleton lines 131-160),
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"ExecutionContext.cs — ModuleRegistry field add",
    .planning/phases/43-module-names-qualified-imports/43-CONTEXT.md D-02 / D-05 / D-06
  </read_first>
  <behavior>
    - Test 1: `var reg = new ModuleRegistry();` produces a non-null instance with `Contains("anything") == false`.
    - Test 2: After `reg.Register("math", new Dictionary<string, Value> { ["sin"] = Value.Function(stub) })`, `reg.Contains("math") == true`.
    - Test 3: `reg.TryGetProc("math", "sin", out var v)` returns true with `v != null` and `v.Type == FunctionType.Instance`.
    - Test 4: `reg.TryGetProc("math", "nope", out var v)` returns false with `v == null`. `reg.TryGetProc("unknown", "anything", out _)` returns false.
    - Test 5: Calling `reg.Register("math", newProcs)` twice keeps the LAST registration (last-write-wins per D-06; advisory wiring lives in Plan 03 ModuleLoader hook, NOT here).
    - Test 6: Two distinct `ExecutionContext` instances expose two distinct `ModuleRegistry` instances (`!ReferenceEquals(c1.ModuleRegistry, c2.ModuleRegistry)`) — verifies per-context vs process-global per RESEARCH A1.
  </behavior>
  <action>
    Per RESEARCH §Pattern 4 + PATTERNS §"flow-lang/Runtime/ModuleRegistry.cs" recommended skeleton:

    Create `flow-lang/Runtime/ModuleRegistry.cs` with namespace `FlowLang.Runtime`. Define a `public sealed class ModuleRegistry` with:
    - Private field: `private readonly ConcurrentDictionary<string, IReadOnlyDictionary<string, Value>> _modules = new();` (using `System.Collections.Concurrent`).
    - `public bool Contains(string moduleName) => _modules.ContainsKey(moduleName);` — used by ModuleLoader to detect duplicate registrations (D-06).
    - `public void Register(string moduleName, IReadOnlyDictionary<string, Value> exportedProcs) { _modules[moduleName] = exportedProcs; }` — last-write-wins; advisory is the caller's responsibility (Plan 03).
    - `public bool TryGetProc(string moduleName, string procName, out Value? procValue)` returning the proc Value when both keys hit, null + false otherwise.
    - `public IReadOnlyDictionary<string, IReadOnlyDictionary<string, Value>> Snapshot()` returning a fresh `Dictionary<string, IReadOnlyDictionary<string, Value>>` copy (mirrors `LiveBlockRegistry.Snapshot`).
    - `public void Clear() { _modules.Clear(); }` for test isolation parity.

    XML doc summary on the class follows the LiveBlockRegistry/PrngRegistry cadence: `/// Phase 43 Plan 43-02 (D-05 / D-02) — per-context module registry. Populated by ModuleLoader at use-time when the loaded file declares <c>module &lt;name&gt;</c> as its first non-comment statement. Read at qualified-access dispatch time by ExpressionEvaluator.EvaluateMemberAccess (registry-first branch per D-02). Files without a module declaration are absent from this registry per D-01 back-compat. Mirrors the <see cref="LiveBlockRegistry"/> / <see cref="PrngRegistry"/> shape — singleton-per-context, ConcurrentDictionary-backed.`

    Modify `flow-lang/Runtime/ExecutionContext.cs`: insert `public ModuleRegistry ModuleRegistry { get; } = new();` IMMEDIATELY AFTER the existing `LiveBlockRegistry` property declaration (around line 156). Use the same XML-doc cadence as LiveBlockRegistry — cite Phase 43 Plan 43-02 (D-05 / D-02), explain the populated-by ModuleLoader + read-by ExpressionEvaluator wiring, cite the Pattern-4 mirror-shape comment. Do NOT modify any other ExecutionContext member.

    Create the Wave 0 test scaffold `flow-lang.Tests/Integration/Phase43/ModuleRegistryTests.cs`:
    - Namespace `FlowLang.Tests.Integration.Phase43`.
    - `[Collection("FlowScripts")]` + `IDisposable` with `RenderingDiagnostics.ResetForTesting()` in ctor + Dispose per S5 ceremony.
    - Helper: `private static Value StubFunction(string name)` returning `Value.Function(FunctionOverload.UserDefined(name, new FunctionSignature(name, new List<FlowType>()), procDecl: null!, new Dictionary<string, Value>()))` — or simpler: `Value.Function(FunctionOverload.Builtin(name, sig, args => Value.Void))` if available. Pick the simpler factory shape after reading Value.cs / FunctionOverload.cs.
    - 6 `[Fact]` methods covering Test 1-6 above.
    - For Test 6 isolation: construct two ExecutionContext instances via the standard public constructor. If construction requires registry seeding, use the canonical FlowEngine-fronted path (`using var engineA = new FlowEngine(); using var engineB = new FlowEngine();` then access `engineA.Context.ModuleRegistry` etc.) — mirrors AuditHarnessTests style.

    Anti-pattern to avoid (per RESEARCH §"Don't Hand-Roll" row 4): do NOT make `ModuleRegistry` a static singleton. The per-context shape is load-bearing for Phase 35 TEST-02 test hermetic isolation.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43.ModuleRegistryTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    `flow-lang/Runtime/ModuleRegistry.cs` exists with the sealed-class shape + 5 public methods (Contains / Register / TryGetProc / Snapshot / Clear); `ExecutionContext.cs` has the new `ModuleRegistry ModuleRegistry { get; } = new();` property positioned after `LiveBlockRegistry`; ModuleRegistryTests.cs ships with 6 GREEN Facts; full `dotnet build flow-lang/flow-lang.csproj` succeeds with zero new warnings; pre-existing xUnit suite shows zero new failures from this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ModuleLoader → ModuleRegistry | Same-process internal API; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering | ModuleRegistry per-context lifecycle | mitigate | Per-context (NOT process-global static) prevents cross-FlowEngine state leakage; matches PrngRegistry / LiveBlockRegistry / StyleRegistry precedent. ConcurrentDictionary backing handles the background-render + audio-thread two-actor pattern. |
| T-43-02a | DoS | Unbounded growth from repeated `Register` | accept | `.flow` source is operator-trusted; module-name space is bounded by the 13 stdlib files + composer additions. Low-value DoS target. |
</threat_model>

<verification>
- `new ModuleRegistry()` produces an empty registry.
- Register then Contains + TryGetProc round-trip correctly.
- Last-write-wins semantics: re-registering the same module name keeps the LATEST procs.
- Distinct ExecutionContext instances expose distinct ModuleRegistry instances (per-context vs process-global confirmed).
- All 6 ModuleRegistryTests Facts GREEN.
- `dotnet build flow-lang/flow-lang.csproj` completes without errors.
</verification>

<success_criteria>
- `flow-lang/Runtime/ModuleRegistry.cs` ships with sealed class + 5 public methods.
- `ExecutionContext.ModuleRegistry` property exists with `new()` default.
- 6 xUnit Facts GREEN.
- Zero pre-existing test regressions (this plan touches no behavior-visible surface).
</success_criteria>

<output>
Create `.planning/phases/43-module-names-qualified-imports/43-02-SUMMARY.md` documenting: the public ModuleRegistry API surface, how the per-context property fits in the ExecutionContext registry-field family, and which Factory pattern (FunctionOverload.UserDefined vs Builtin) was chosen for the test stub Value.
</output>
