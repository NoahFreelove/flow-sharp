---
phase: 43-module-names-qualified-imports
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs
  - flow-lang/StandardLibrary/Audio/EffectsFunctions.cs
  - flow-lang/StandardLibrary/BuiltInFunctions.cs
  - flow-lang.Tests/Integration/Phase43/BeatConversionTests.cs
  - flow-lang.Tests/Integration/Phase43/BeatCompanionOverloadTests.cs
  - flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs
autonomous: true
requirements:
  - REQ-MOD-07
  - REQ-MOD-08
  - REQ-MOD-09
  - REQ-MOD-10
  - REQ-MOD-12
tags:
  - phase-43
  - beat-backfill
  - builtins
  - audit-polarity-flip
must_haves:
  truths:
    - "`(beatToSec 1.0)` outside any tempo block returns 0.5 (Second value backed by 0.5 double) AND emits exactly one `[beatToSec] no active tempo — defaulting to 120 BPM (use tempo N { ... } to set explicitly)` advisory per process"
    - "`tempo 60 { (beatToSec 1.0) }` returns 1.0 (1 beat × 60/60) AND emits NO advisory (tempo is set)"
    - "`(secToBeat 1.0)` outside any tempo block returns 2.0 (2 beats at 120 BPM) AND emits exactly one `[secToBeat] no active tempo — defaulting to 120 BPM` advisory; symmetric to beatToSec"
    - "`tempo 120 { (delay buf 0.5 0.3 0.5) }` with the second arg lexed as Beat (1/2 beat) routes to the new delay(Buffer, Beat, Double, Double) overload; result is RMS-equivalent to `(delay buf 250.0 0.3 0.5)` because 0.5 beat × 60_000/120 = 250 ms"
    - "`tempo 120 { (renderBarAtBeat bar 1.0 \"piano\" 44100 120.0) }` with the second arg as Beat routes to the new renderBarAtBeat(Bar, Beat, ...) overload; voice-array length matches the Double-overload baseline"
    - "OverloadResolver picks the Beat overload at +1000 specificity when the second arg is a Beat-typed value; the Double overload still wins +1000 when the second arg is a bare Double (RESEARCH A5 — exact-type match outranks compatibility match)"
    - "AuditHarnessTests.OrphanList_DoesNotContainBeatType (renamed from OrphanList_ContainsBeatType) passes in the SAME commit as the Beat-overload registration (D-10 atomic polarity flip per Pitfall 5)"
    - "Two-run cmp-clean preserved: same git SHA → byte-identical WAV when scripts use the Beat overloads (default 120 BPM is deterministic)"
    - "D-08: beatToSec + secToBeat free builtins read MusicalContext.ActiveTempo with 120 BPM default + one-shot advisory (AUDIT §7a HIGH closure for §1 BeatType-orphan + §2 Beat↔Second-conversion)"
    - "D-09: Beat-companion overloads scoped to delay(Buffer, Beat) + renderBarAtBeat(Sequence, Beat) ONLY (AUDIT §1 explicit list — no speculative reverb/stretch/pitchShift additions)"
    - "D-10: AuditHarnessTests polarity flip in SAME commit as Beat-overload landing (Pitfall 5 — never red between commits)"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs"
      provides: "beatToSec + secToBeat registered via RegisterContextDependent pattern, reading MusicalContext.Tempo with 120 BPM default + one-shot WarnOnce advisory"
      contains: "beatToSec"
      min_lines: 40
    - path: "flow-lang/StandardLibrary/Audio/EffectsFunctions.cs"
      provides: "delay(Buffer, Beat, Double, Double) overload alongside the existing NoteValue/Millisecond/Double overloads"
      contains: "BeatType.Instance"
    - path: "flow-lang/StandardLibrary/BuiltInFunctions.cs"
      provides: "renderBarAtBeat(Bar, Beat, String, Int, Double) overload + BeatConversionFunctions.RegisterContextDependent wiring call"
      contains: "BeatConversionFunctions.RegisterContextDependent"
    - path: "flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs"
      provides: "polarity-flipped fixture: OrphanList_DoesNotContainBeatType (D-10)"
      contains: "DoesNotContain"
    - path: "flow-lang.Tests/Integration/Phase43/BeatConversionTests.cs"
      provides: "REQ-MOD-07/08 coverage with tempo-set and tempo-default cases + dedup-across-runs assertion"
      contains: "beatToSec"
    - path: "flow-lang.Tests/Integration/Phase43/BeatCompanionOverloadTests.cs"
      provides: "REQ-MOD-09/10 coverage via FlowEngine round-trip + reflective signature presence check"
      contains: "BeatType"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "context.GetMusicalContext().Tempo ?? 120.0 in closure captured by RegisterContextDependent"
      pattern: "context\\.GetMusicalContext\\(\\)\\.Tempo"
    - from: "flow-lang/StandardLibrary/BuiltInFunctions.cs"
      to: "flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs"
      via: "BeatConversionFunctions.RegisterContextDependent(registry, context) inside RegisterContextDependentFunctions wiring chain"
      pattern: "BeatConversionFunctions\\.RegisterContextDependent"
    - from: "flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs"
      to: "flow-lang/TypeSystem/SpecialTypes/BeatType.cs"
      via: "Assert.DoesNotContain('BeatType', snap.CoercibleOrphans) after Beat backfill ships"
      pattern: "DoesNotContain\\(\"BeatType\""
---

<objective>
Close the Phase 42 AUDIT.md §1 Beat-orphan anchor finding by shipping the four Beat-aware builtin signatures listed in CONTEXT D-08 + D-09: `(beatToSec Beat) → Second`, `(secToBeat Second) → Beat`, `delay(Buffer, Beat, Double, Double)`, `renderBarAtBeat(Bar, Beat, String, Int, Double)`. All four read the active tempo from `ExecutionContext.GetMusicalContext().Tempo ?? 120.0` (Phase 22 DX-12 canonical recipe at EffectsFunctions.cs:359-389) and emit a one-shot `[<name>] no active tempo — defaulting to 120 BPM` advisory when the default fires. In the SAME commit (D-10 atomic per Pitfall 5), flip the Phase 42 `AuditHarnessTests.OrphanList_ContainsBeatType` polarity to `OrphanList_DoesNotContainBeatType` — the Phase 42 fixture becomes the regression gate that fails if a future refactor drops the Beat consumers.

Purpose: Resolve the AUDIT-tracked orphan type into a first-class consumer surface. Independent file set from Plans 01-03 — runs in Wave 1 alongside Plans 01 and 02 (this work touches `StandardLibrary/Audio/` + `BuiltInFunctions.cs` + the Phase 42 test fixture; zero overlap with the parser/registry/dispatcher file set).

Output: 1 new + 2 modified production files + 2 new + 1 modified test fixture; the Beat type leaves the AUDIT orphan list; the Phase 42 fixture flips polarity atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT.md
@.planning/phases/43-module-names-qualified-imports/43-CONTEXT.md
@.planning/phases/43-module-names-qualified-imports/43-RESEARCH.md
@.planning/phases/43-module-names-qualified-imports/43-PATTERNS.md
@.planning/phases/43-module-names-qualified-imports/43-VALIDATION.md

<interfaces>
<!-- Concrete contracts the executor needs. -->

From flow-lang/TypeSystem/SpecialTypes/BeatType.cs (the type — IsCompatibleWith Double+Float; CLR backing is double):
```csharp
public sealed class BeatType : FlowType  // singleton via .Instance
{
    public override bool IsCompatibleWith(FlowType other) =>
        other is DoubleType || other is FloatType || other is BeatType;
}
```

From flow-lang/Runtime/Value.cs (factory methods 23-52):
```csharp
public static Value Beat(double value);     // line 42
public static Value Second(double value);   // line 37
public static Value Buffer(AudioBuffer buf);
```

From flow-lang/StandardLibrary/Audio/EffectsFunctions.cs lines 359-389 (the canonical Phase 22 DX-12 pattern):
```csharp
public static void RegisterContextDependent(
    InternalFunctionRegistry registry,
    FlowLang.Runtime.ExecutionContext context)
{
    var delaySyncedSig = new FunctionSignature("delay",
        [BufferType.Instance, NoteValueType.Instance, DoubleType.Instance, DoubleType.Instance],
        ParameterNames: ["buf", "rate", "feedback", "mix"]);
    registry.Register("delay", delaySyncedSig, args =>
    {
        var buffer = args[0].As<AudioBuffer>();
        int noteValueEnum = args[1].As<int>();
        float feedback = (float)args[2].As<double>();
        float mix = (float)args[3].As<double>();
        double bpm = context.GetMusicalContext().Tempo ?? 120.0;
        double delayMs = NoteValueToMs((NoteValueType.Value)noteValueEnum, bpm);
        if (buffer.Frames == 0)
            return Value.Buffer(new AudioBuffer(0, buffer.Channels, buffer.SampleRate));
        var result = Delay.Apply(buffer, (float)delayMs, feedback, mix);
        return Value.Buffer(result);
    });
}
```

From flow-lang/StandardLibrary/BuiltInFunctions.cs lines 1029-1038 (wiring chain — INSERT BeatConversionFunctions HERE):
```csharp
public static void RegisterContextDependentFunctions(
    InternalFunctionRegistry registry, FlowLang.Runtime.ExecutionContext context)
{
    Audio.SongRenderer.RegisterContextDependent(registry, context);
    Harmony.HarmonyFunctions.RegisterContextDependent(registry, context);
    Audio.EffectsFunctions.RegisterContextDependent(registry, context);   // Phase 22-04 DX-12
    // ↑ INSERT: Audio.BeatConversionFunctions.RegisterContextDependent(registry, context);  // Phase 43 D-08
    Transforms.TransformFunctions.RegisterContextDependent(registry, context);
    Audio.Vocalization.VocalizationFunctions.RegisterContextDependent(registry, context);
    Audio.MidiExport.RegisterContextDependent(registry, context);
}
```

From flow-lang/StandardLibrary/BuiltInFunctions.cs lines 1477-1492 (existing renderBarAtBeat Double overload — analog for Beat overload):
```csharp
var renderBarAtBeatSignature = new FunctionSignature("renderBarAtBeat",
    [BarType.Instance, DoubleType.Instance, StringType.Instance, IntType.Instance, DoubleType.Instance],
    ParameterNames: ["bar", "beat", "synth", "sampleRate", "bpm"]);
registry.Register("renderBarAtBeat", renderBarAtBeatSignature, args =>
{
    var bar = (BarData)args[0].Data!;
    double beatOffset = (double)args[1].Data!;
    string synthType = (string)args[2].Data!;
    int sampleRate = (int)args[3].Data!;
    double bpm = (double)args[4].Data!;
    var voices = Audio.BarRenderer.RenderBarAtBeat(bar, beatOffset, synthType, sampleRate, bpm);
    return Value.Array(voices.Select(v => Value.Voice(v)).ToArray(), VoiceType.Instance);
});
```

From flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs lines 231-242 (the D-10 polarity-flip target):
```csharp
[Fact]
public void OrphanList_ContainsBeatType()
{
    var snap = _snapshot.Value;
    Assert.Contains("BeatType", snap.CoercibleOrphans);
    // ... old XML comment about Beat being the sole coercible orphan ...
}
```

From flow-lang/Diagnostics/RenderingDiagnostics.cs:
```csharp
public static void WarnOnce(string sentinelKey, string message);
public static bool WasWarnedForTesting(string sentinelKey);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: beatToSec + secToBeat builtins via RegisterContextDependent + tests</name>
  <files>
    flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs,
    flow-lang/StandardLibrary/BuiltInFunctions.cs,
    flow-lang.Tests/Integration/Phase43/BeatConversionTests.cs
  </files>
  <read_first>
    flow-lang/StandardLibrary/Audio/EffectsFunctions.cs lines 1-50 + 359-389 (RegisterContextDependent canonical pattern + namespace/using imports),
    flow-lang/StandardLibrary/BuiltInFunctions.cs lines 1020-1050 (RegisterContextDependentFunctions chain — exact insertion site),
    flow-lang/TypeSystem/SpecialTypes/BeatType.cs entire file (verify .Instance accessor + IsCompatibleWith chain),
    flow-lang/TypeSystem/SpecialTypes/SecondType.cs entire file (mirror — SecondType.Instance backing),
    flow-lang/Runtime/Value.cs lines 1-60 (verify Value.Second + Value.Beat factory signatures),
    flow-lang/Runtime/MusicalContext.cs lines 1-60 (Tempo property semantics — nullable double),
    flow-lang/Runtime/ExecutionContext.cs lines 430-460 (GetMusicalContext() walks frame chain),
    flow-lang/Diagnostics/RenderingDiagnostics.cs entire 64-line file,
    flow-lang/TypeSystem/FunctionSignature.cs lines 1-180 (ParameterNames parameter; specificity scoring),
    flow-lang.Tests/Integration/Phase37/StretchAutoAdvisoryTests.cs entire ~100 lines (CaptureStderr + FlowEngine round-trip + advisory assertion pattern),
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Reading the active tempo with default fallback" + §"`beatToSec` builtin registration" lines 584-628,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pitfall 8: Default-120-BPM advisory two-run cmp-clean preservation" lines 519-526,
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs",
    .planning/phases/43-module-names-qualified-imports/43-CONTEXT.md D-08
  </read_first>
  <behavior>
    - Test 1: `(beatToSec 1.0)` executed outside any tempo block returns a Second value with `value == 0.5` (1 beat × 60/120). Stderr contains `[beatToSec] no active tempo — defaulting to 120 BPM (use tempo N { ... } to set explicitly)` exactly once.
    - Test 2: `tempo 60 { (beatToSec 1.0) }` returns Second value `1.0`. Stderr does NOT contain the advisory.
    - Test 3: `tempo 120 { (beatToSec 2.0) }` returns Second value `1.0` (2 beats × 60/120).
    - Test 4: `(secToBeat 1.0)` outside any tempo block returns Beat value `2.0` (1 sec × 120/60). Stderr contains the `[secToBeat] no active tempo — defaulting to 120 BPM` advisory once.
    - Test 5: `tempo 120 { (secToBeat 0.5) }` returns Beat value `1.0` (0.5 sec × 120/60). No advisory.
    - Test 6 (Pitfall 8 dedup): Re-running the same source containing `(beatToSec 1.0)` in a second FlowEngine.Execute (same process) does NOT re-emit the advisory — sentinel `beatToSec-no-tempo` is process-wide-deduped. Verify via `WasWarnedForTesting("beatToSec-no-tempo") == true` after FIRST run; second run does NOT call WarnOnce again (assert via captured-stderr line-count being 1, not 2).
  </behavior>
  <action>
    Per Pattern S4 (RegisterContextDependent closure):

    Create new file `flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs` (namespace `FlowLang.StandardLibrary.Audio`):
    - Public static class `BeatConversionFunctions`.
    - Public static method `RegisterContextDependent(InternalFunctionRegistry registry, FlowLang.Runtime.ExecutionContext context)`. XML doc citing Phase 43 D-08 + AUDIT.md §1 Beat-orphan anchor closure + the EffectsFunctions.cs:359-389 precedent.
    - Inside, register two signatures:
      1. `var beatToSecSig = new FunctionSignature("beatToSec", [BeatType.Instance], ParameterNames: ["beats"]);` then `registry.Register("beatToSec", beatToSecSig, args => { ... });` body:
         - `double beats = args[0].As<double>();` (BeatType backs double per BeatType.cs:25-28).
         - `double? tempo = context.GetMusicalContext().Tempo;`
         - `double bpm = tempo ?? 120.0;`
         - If `tempo == null`, call `RenderingDiagnostics.WarnOnce("beatToSec-no-tempo", "[beatToSec] no active tempo — defaulting to 120 BPM (use tempo N { ... } to set explicitly)");`
         - Return `Value.Second(beats * (60.0 / bpm));`.
      2. `var secToBeatSig = new FunctionSignature("secToBeat", [SecondType.Instance], ParameterNames: ["seconds"]);` then register lambda:
         - `double seconds = args[0].As<double>();`
         - Same tempo-read pattern; sentinel key `secToBeat-no-tempo`; message `[secToBeat] no active tempo — defaulting to 120 BPM (use tempo N { ... } to set explicitly)`.
         - Return `Value.Beat(seconds * (bpm / 60.0));`.

    Modify `flow-lang/StandardLibrary/BuiltInFunctions.cs`: insert `Audio.BeatConversionFunctions.RegisterContextDependent(registry, context);` immediately AFTER the existing `Audio.EffectsFunctions.RegisterContextDependent(registry, context);` call (around line 1035 per PATTERNS line 251). XML comment: `// Phase 43 D-08 — beatToSec + secToBeat tempo-aware conversion builtins (closes AUDIT.md §1 Beat-orphan anchor).`

    Create `flow-lang.Tests/Integration/Phase43/BeatConversionTests.cs`:
    - Namespace `FlowLang.Tests.Integration.Phase43`.
    - `[Collection("FlowScripts")]` + `IDisposable` ceremony per S5 (RenderingDiagnostics.ResetForTesting in ctor + Dispose).
    - Helper `private static string CaptureStderr(Action action)` per StretchAutoAdvisoryTests pattern.
    - 6 `[Fact]` methods covering Test 1-6. Drive end-to-end via `using var engine = new FlowEngine(); engine.Execute(source, "<test>");` capturing stderr.
    - For Test 1/4 (advisory fires): assert the captured stderr CONTAINS the exact expected substring.
    - For Test 2/3/5 (no advisory): assert the captured stderr does NOT contain `[beatToSec]` / `[secToBeat]`.
    - For Test 6 (dedup): two `engine.Execute` calls in the same `[Fact]` body sharing the same `_capturedError` StringBuilder; assert the advisory line appears exactly once across both runs.
    - Test wording on stderr-substring asserts MUST use exact substring matching (no regex needed): `Assert.Contains("[beatToSec] no active tempo", stderr)`.

    Per Pitfall 8: do NOT add stderr to any byte-cmp-clean tests. Phase 28/29/33 cmp-clean tests redirect Console.Out only; this builtin's stderr writes are out-of-band per the established convention.

    Verify ZERO new failures in the existing xUnit suite (the new builtins are purely additive — no existing test exercises `beatToSec`/`secToBeat` today per RESEARCH §"Runtime State Inventory" line 456).
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43.BeatConversionTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    `flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs` exists with the public static `RegisterContextDependent` method registering `beatToSec` + `secToBeat`; BuiltInFunctions.cs wires the new registration via the RegisterContextDependentFunctions chain; BeatConversionTests.cs ships with 6 GREEN Facts (tempo-set + tempo-default + dedup-across-runs for both directions); the two new signatures appear in `engine.Context.InternalRegistry.EnumerateSignatures()` (sanity verifiable via the existing Phase 42 audit harness).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Beat-companion overloads on delay + renderBarAtBeat + Phase 42 audit fixture polarity flip (SAME commit per Pitfall 5)</name>
  <files>
    flow-lang/StandardLibrary/Audio/EffectsFunctions.cs,
    flow-lang/StandardLibrary/BuiltInFunctions.cs,
    flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs,
    flow-lang.Tests/Integration/Phase43/BeatCompanionOverloadTests.cs
  </files>
  <read_first>
    flow-lang/StandardLibrary/Audio/EffectsFunctions.cs lines 280-400 (existing delay overloads — Double / Millisecond / NoteValue — verify all three are visible to confirm Beat overload doesn't collide),
    flow-lang/StandardLibrary/BuiltInFunctions.cs lines 1470-1500 (renderBarAtBeat Double overload),
    flow-lang/StandardLibrary/Audio/BarRenderer.cs lines 230-360 (RenderBarAtBeat static-method surface — verify the Double-typed signature),
    flow-lang/TypeSystem/SpecialTypes/BeatType.cs (specificity scoring inputs),
    flow-lang/TypeSystem/OverloadResolver.cs lines 200-250 (verify exact-match +1000 / IsCompatibleWith +500 / CanConvertTo +100 — RESEARCH A5),
    flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs entire ~300 lines (verify _snapshot.Value usage + EnumerateSignatures pattern at lines 124-178 + lines 231-242 polarity-flip target),
    flow-lang.Tests/Integration/Phase37/StretchAutoAdvisoryTests.cs (FlowEngine round-trip + WAV-assertion pattern),
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Beat-companion overload registration (delay)" lines 547-579,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"`AuditHarnessTests` polarity flip (D-10)" lines 646-665,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pitfall 5: Phase 42 audit fixture polarity flip — temporal coupling" lines 494-499,
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"flow-lang/StandardLibrary/Audio/EffectsFunctions.cs" + §"flow-lang/StandardLibrary/BuiltInFunctions.cs" + §"AuditHarnessTests.cs D-10 polarity flip",
    .planning/phases/43-module-names-qualified-imports/43-CONTEXT.md D-09 + D-10
  </read_first>
  <behavior>
    - Test 1 (REQ-MOD-09 delay Beat overload): `tempo 120 { Buffer b = (createSineTone 440 1.0); (delay b 0.5 0.3 0.5) }` where `0.5` is Beat-typed via context routes through the new delay(Buffer, Beat, Double, Double) overload. Result equivalence: RMS-windowed comparison against `tempo 120 { (delay b 250.0 0.3 0.5) }` (the Millisecond overload) shows match within ±0.5 dB / 100ms per SPEC-8 — same conversion math should yield identical buffers.
    - Test 2 (REQ-MOD-09 advisory): `(delay b 0.5b 0.3 0.5)` outside any tempo block emits `[delay] no active tempo — defaulting to 120 BPM` once (Beat-tagged literal `0.5b` lexed as Beat — verify via existing music-literal lexer behavior).
    - Test 3 (REQ-MOD-10 renderBarAtBeat Beat overload): `(renderBarAtBeat bar 1.0 "piano" 44100 120.0)` where the second arg is Beat-typed routes to the new overload; voice-array length matches the Double-overload baseline.
    - Test 4 (overload resolution sanity): Reflectively enumerate `engine.Context.InternalRegistry.EnumerateSignatures()` and assert: at least one registered "delay" signature has BeatType as the second parameter type; at least one registered "renderBarAtBeat" signature has BeatType as the second parameter type. Mirrors AuditHarnessTests.cs:279-298 reflective pattern.
    - Test 5 (RESEARCH A5 — no specificity tiebreaker ambiguity): bare Double in the second arg STILL resolves to the existing Double overload (+1000 exact match); Beat-typed value in the second arg resolves to the new Beat overload (+1000 exact match). Confirm via `(delay b 250.0 0.3 0.5)` not firing the Beat advisory AND returning the byte-identical buffer to pre-Phase-43 baseline.
    - Test 6 (D-10 polarity flip): `dotnet test --filter "FullyQualifiedName~Phase42.AuditHarnessTests.OrphanList_DoesNotContainBeatType"` PASSES (was `OrphanList_ContainsBeatType`).
    - Test 7 (back-compat): existing `tests/test_*.flow` happy-path scripts pass; Phase 42 `flow-lang.Tests --filter "FullyQualifiedName~Phase42"` reports zero NEW failures.
  </behavior>
  <action>
    Per PATTERNS §"flow-lang/StandardLibrary/Audio/EffectsFunctions.cs":

    Modify `flow-lang/StandardLibrary/Audio/EffectsFunctions.cs`. Inside `RegisterContextDependent` (around line 359, after the existing `delaySyncedSig` block), insert a new Beat overload:
    - `var delayBeatSig = new FunctionSignature("delay", [BufferType.Instance, BeatType.Instance, DoubleType.Instance, DoubleType.Instance], ParameterNames: ["buf", "beats", "feedback", "mix"]);`
    - `registry.Register("delay", delayBeatSig, args => { ... });` lambda body:
      - `var buffer = args[0].As<AudioBuffer>();`
      - `double beats = args[1].As<double>();` (BeatType backs double per BeatType.cs:25-28).
      - `float feedback = (float)args[2].As<double>();`
      - `float mix = (float)args[3].As<double>();`
      - `double? tempo = context.GetMusicalContext().Tempo;`
      - `double bpm = tempo ?? 120.0;`
      - If `tempo == null`, fire `RenderingDiagnostics.WarnOnce("delay-beat-no-tempo", "[delay] no active tempo — defaulting to 120 BPM (use tempo N { ... } to set explicitly)");`
      - `double delayMs = beats * (60_000.0 / bpm);`
      - Mirror the existing NoteValue overload's empty-buffer guard and `Delay.Apply(buffer, (float)delayMs, feedback, mix)` call, returning `Value.Buffer(result)`.
    - XML comment cites Phase 43 D-09 + RESEARCH lines 547-579 + AUDIT.md §1 anchor closure.

    Per PATTERNS §"flow-lang/StandardLibrary/BuiltInFunctions.cs":

    Modify `flow-lang/StandardLibrary/BuiltInFunctions.cs`. Find the existing `renderBarAtBeatSignature` registration at line 1477 area. Register a sibling Beat overload IMMEDIATELY AFTER:
    - `var renderBarAtBeatBeatSig = new FunctionSignature("renderBarAtBeat", [BarType.Instance, BeatType.Instance, StringType.Instance, IntType.Instance, DoubleType.Instance], ParameterNames: ["bar", "beat", "synth", "sampleRate", "bpm"]);`
    - `registry.Register("renderBarAtBeat", renderBarAtBeatBeatSig, args => { ... });` lambda body IDENTICAL to the existing Double overload (the cast `(double)args[1].Data!` works on a BeatType-backed Value because BeatType wraps double). No tempo read needed — `bpm` is passed explicitly as `args[4]`.
    - XML comment cites Phase 43 D-09. NOTE per PATTERNS line 541: this overload doesn't need context-closure, so it lives in the main `BuiltInFunctions` registration path, NOT in `RegisterContextDependentFunctions`.

    Per Pitfall 5 (D-10 atomic polarity flip — SAME COMMIT):

    Modify `flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs`. At lines 231-242 (or wherever `OrphanList_ContainsBeatType` lives — verify via read):
    - Rename the method to `OrphanList_DoesNotContainBeatType`.
    - Change `Assert.Contains("BeatType", snap.CoercibleOrphans);` to `Assert.DoesNotContain("BeatType", snap.CoercibleOrphans);`.
    - Replace the XML comment with the post-Phase-43 closure-context block (RESEARCH lines 656-664 verbatim or close adaptation):
      `// Phase 43 closure context: Before Phase 43, BeatType was the SOLE coercible orphan (AUDIT.md §1 anchor). Phase 43 shipped delay(Buffer, Beat, ...) + renderBarAtBeat(Bar, Beat, ...) + beatToSec(Beat) + secToBeat(Second) — Beat now has consumers, so the orphan-detection rule no longer applies. If a future refactor drops the Beat-companion overloads, this fact will fail with "BeatType found in CoercibleOrphans" — the expected failure mode that protects against accidental regression.`

    Both edits (production-overload additions + audit-fixture polarity flip) MUST land in the SAME git commit per Pitfall 5. Splitting across commits leaves the test suite RED between commits, breaking the CI gate.

    Create `flow-lang.Tests/Integration/Phase43/BeatCompanionOverloadTests.cs`:
    - Namespace + ceremony per S5.
    - 5 `[Fact]` methods covering Test 1-5 above.
    - For Test 4 (reflective signature presence): adapt the `AuditHarnessTests.Registry_WiresSfzAndNotationIoAndOsc` pattern (lines 279-298) — enumerate `engine.Context.InternalRegistry.EnumerateSignatures()`, filter to "delay" + "renderBarAtBeat" names, assert at least one signature with `BeatType.Instance` at position 1 in the `Parameters` array.
    - For Test 1 (RMS equivalence): use `FlowLang.Tests.Helpers.RmsRegressionTests.AssertRmsWithinTolerance` (or `AssertWavMatchesBaseline` — verify the helper name via repo grep) with ±0.5 dB / 100 ms per SPEC-8.
    - Resolution-sanity test 5: do NOT use a pinned WAV baseline (Phase 43 byte-impact is zero on the existing Double-resolution path); just assert the captured advisory line is absent.

    Run after both production + test changes: `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42.AuditHarnessTests" --logger "console;verbosity=minimal"` — every Phase 42 fact still GREEN, with the renamed `OrphanList_DoesNotContainBeatType` now passing.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43.BeatCompanionOverloadTests" --logger "console;verbosity=minimal" &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase42.AuditHarnessTests" --logger "console;verbosity=minimal" &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done</automated>
  </verify>
  <done>
    EffectsFunctions.cs has the new delay(Buffer, Beat, Double, Double) overload inside `RegisterContextDependent`; BuiltInFunctions.cs has the new renderBarAtBeat(Bar, Beat, ...) overload alongside the existing Double overload; Phase 42 AuditHarnessTests polarity flipped to `OrphanList_DoesNotContainBeatType` in the SAME commit; BeatCompanionOverloadTests.cs ships with 5 GREEN Facts; every Phase 42 audit fixture remains GREEN (26 total — none regressed); `tests/test_*.flow` happy-path scripts unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .flow source → MusicalContext.Tempo → builtin lambda | Composer-trusted source; tempo values originate from `tempo N { }` blocks in .flow source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06 | Tampering | beatToSec/secToBeat/delay-Beat lambdas reading active tempo | mitigate | `context.GetMusicalContext().Tempo ?? 120.0` is the canonical Phase 22 DX-12 idiom (EffectsFunctions.cs:378, Interpreter.cs:200, Interpreter.cs:210 — three established sites). No new code path; just additional consumer surface. |
| T-43-07 | DoS | Single double-multiply per call | accept | Bounded computation cost; no allocation in hot path beyond the AudioBuffer return (which mirrors existing delay overload behavior). |
| T-43-08 | Information Disclosure | Default-120-BPM advisory wording reveals tempo state to stderr | accept | Advisory is the composer-facing point. WarnOnce dedup bounds output volume per process. |
</threat_model>

<verification>
- 4 new builtin signatures registered (`beatToSec`, `secToBeat`, `delay(Buffer, Beat, ...)`, `renderBarAtBeat(Bar, Beat, ...)`).
- All 4 fire the appropriate `[<name>] no active tempo — defaulting to 120 BPM` advisory when called outside a tempo block; dedup-across-runs via WarnOnce.
- All 4 reflect tempo when inside a `tempo N { }` block.
- Phase 42 AuditHarnessTests polarity-flipped fixture (`OrphanList_DoesNotContainBeatType`) PASSES in the SAME commit as the overload registration.
- Phase 42 full suite remains GREEN (26 fixtures).
- 123 `tests/test_*.flow` happy-path scripts continue to PASS.
- Two-run cmp-clean preserved for non-`live` paths (Pitfall 8 — stderr is captured separately from WAV byte-cmp).
</verification>

<success_criteria>
- `flow-lang/StandardLibrary/Audio/BeatConversionFunctions.cs` ships with `RegisterContextDependent` registering both beatToSec and secToBeat.
- `flow-lang/StandardLibrary/Audio/EffectsFunctions.cs` gains the delay(Buffer, Beat, ...) overload.
- `flow-lang/StandardLibrary/BuiltInFunctions.cs` gains the renderBarAtBeat(Bar, Beat, ...) overload AND wires `BeatConversionFunctions.RegisterContextDependent` into the chain.
- `flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs` polarity-flipped (rename + Contains→DoesNotContain) IN THE SAME COMMIT.
- New Phase 43 fixtures (BeatConversionTests 6 + BeatCompanionOverloadTests 5) all GREEN.
- AuditHarnessTests `OrphanList_DoesNotContainBeatType` PASSES.
- Zero new failures in `flow-lang.Tests` modulo pre-existing 34.
- Zero regressions in 123 `.flow` happy-path scripts.
</success_criteria>

<output>
Create `.planning/phases/43-module-names-qualified-imports/43-04-SUMMARY.md` documenting: the 4 new builtin signatures (with parameter-name lists), the exact stderr advisory wordings shipped (so users of Plan 05's stdlib migration can reference consistent wording), the WarnOnce sentinel keys chosen, and a confirmation that the D-10 polarity flip landed in the same git commit as the overload registration (Pitfall 5 mitigation).
</output>
