---
phase: 43-module-names-qualified-imports
plan: 03
type: execute
wave: 2
depends_on:
  - 43-01
  - 43-02
files_modified:
  - flow-lang/Runtime/ModuleLoader.cs
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang/Interpreter/Interpreter.cs
  - flow-lang/Runtime/ExecutionContext.cs
  - flow-lang.Tests/Integration/Phase43/QualifiedAccessDispatchTests.cs
  - flow-lang.Tests/Integration/Phase43/ModuleCollisionAdvisoryTests.cs
autonomous: true
requirements:
  - REQ-MOD-02
  - REQ-MOD-03
  - REQ-MOD-04
  - REQ-MOD-05
  - REQ-MOD-11
tags:
  - phase-43
  - module-loader
  - dispatcher
  - advisory
must_haves:
  truths:
    - "Loading a .flow file whose first non-comment statement is `module math` causes `context.ModuleRegistry.Contains('math') == true` after `use \"@math\"` resolves"
    - "Calling `math.sin(0.5)` on a registered module dispatches via ModuleRegistry; `chord.root` continues evaluating via the existing instance-member dispatch (Pitfall 2 — no dispatch regression)"
    - "Two files declaring `module math` produce a one-shot `[module] duplicate module name 'math' — last load wins` stderr advisory; re-running the same FlowEngine.Execute does NOT re-emit (WarnOnce dedup)"
    - "Two `use` statements importing files that both export `sin` produce a one-shot `[module] 'sin' from '{b}' shadows 'sin' from '{a}' — qualify with '{a}.sin' or '{b}.sin' to disambiguate` stderr advisory; last-import wins for the unqualified call site (D-04)"
    - "Files WITHOUT a `module` declaration still inject their procs into the unqualified namespace exactly as before (back-compat per D-05); no entry appears in `ModuleRegistry` for those files"
    - "ModuleLoader hook runs ONCE per resolvedPath (Pitfall 7) — second `use \"@math\"` from a different consumer file does NOT double-register or fire the duplicate-module advisory"
    - "Interpreter encountering ModuleDeclarationStatement at execute-time is a no-op (ModuleLoader consumed it before Interpreter.Execute returns)"
  artifacts:
    - path: "flow-lang/Runtime/ModuleLoader.cs"
      provides: "registration hook between interpreter.Execute(program) and _loadedModules.Add(resolvedPath)"
      contains: "ModuleRegistry.Register"
    - path: "flow-lang/Interpreter/ExpressionEvaluator.cs"
      provides: "registry-first branch at the top of EvaluateMemberAccess"
      contains: "ModuleRegistry.TryGetProc"
    - path: "flow-lang/Interpreter/Interpreter.cs"
      provides: "ModuleDeclarationStatement no-op + shadow-advisory wiring in ExecuteImport"
      contains: "ModuleDeclarationStatement"
    - path: "flow-lang/Runtime/ExecutionContext.cs"
      provides: "ProcOwnership tracking dict for shadow detection"
      contains: "ProcOwnership"
    - path: "flow-lang.Tests/Integration/Phase43/QualifiedAccessDispatchTests.cs"
      provides: "REQ-MOD-02/03 dispatch + Pitfall 2 fall-through regression"
      contains: "math.sin"
    - path: "flow-lang.Tests/Integration/Phase43/ModuleCollisionAdvisoryTests.cs"
      provides: "REQ-MOD-04/05 advisory + dedup-across-runs assertions"
      contains: "shadows"
  key_links:
    - from: "flow-lang/Runtime/ModuleLoader.cs"
      to: "flow-lang/Runtime/ModuleRegistry.cs"
      via: "context.ModuleRegistry.Register(modDecl.Name, exportedProcs) inside first-load branch"
      pattern: "context\\.ModuleRegistry\\.Register"
    - from: "flow-lang/Interpreter/ExpressionEvaluator.cs"
      to: "flow-lang/Runtime/ModuleRegistry.cs"
      via: "_context.ModuleRegistry.TryGetProc(varExpr.Name, member.MemberName, out var procValue)"
      pattern: "ModuleRegistry\\.TryGetProc"
    - from: "flow-lang/Runtime/ModuleLoader.cs"
      to: "flow-lang/Diagnostics/RenderingDiagnostics.cs"
      via: "RenderingDiagnostics.WarnOnce('module-dup:<name>', ...) for D-06; WarnOnce('module-shadow:<a>:<b>:<name>', ...) for D-04 at proc-shadow site"
      pattern: "RenderingDiagnostics\\.WarnOnce"
---

<objective>
Wire the ModuleDeclarationStatement AST (Plan 01) and the ModuleRegistry type (Plan 02) into the runtime: (a) extend `ModuleLoader.LoadModule` to capture the leading `module <name>` declaration and register the file's exported procs into `context.ModuleRegistry` exactly once per resolvedPath (Pitfall 7); (b) extend `ExpressionEvaluator.EvaluateMemberAccess` with a registry-first branch (D-02) gated on `member.Object is VariableExpression varExpr` so all existing `chord.root` / `song.sections` / `voice.Pan` dispatch paths fall through unchanged (Pitfall 2); (c) emit the D-04 last-import-wins shadow advisory at the proc-shadowing site and the D-06 duplicate-module advisory at the ModuleLoader registration site, both via `RenderingDiagnostics.WarnOnce`; (d) teach `Interpreter` that `ModuleDeclarationStatement` is a no-op at execute-time (ModuleLoader already consumed it).

Purpose: After this plan, `use "@math"` followed by `(math.sin 0.5)` returns the correct value, advisories fire on collisions, and existing scripts continue working byte-identically. The Wave 1 AST and registry types stop being inert and start carrying load.

Output: 4 modified production files + 2 new test fixtures; all 123 `.flow` happy-path scripts continue passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/43-module-names-qualified-imports/43-CONTEXT.md
@.planning/phases/43-module-names-qualified-imports/43-RESEARCH.md
@.planning/phases/43-module-names-qualified-imports/43-PATTERNS.md
@.planning/phases/43-module-names-qualified-imports/43-VALIDATION.md
@.planning/phases/43-module-names-qualified-imports/43-01-PLAN.md
@.planning/phases/43-module-names-qualified-imports/43-02-PLAN.md

<interfaces>
<!-- Concrete contracts from Plan 01 and Plan 02 that this plan consumes. -->

From Plan 01 (flow-lang/Ast/Statements/ModuleDeclarationStatement.cs):
```csharp
public record ModuleDeclarationStatement(
    SourceLocation Location,
    string Name,
    Span? Span = null) : Statement(Location);
```

From Plan 02 (flow-lang/Runtime/ModuleRegistry.cs):
```csharp
public sealed class ModuleRegistry
{
    public bool Contains(string moduleName);
    public void Register(string moduleName, IReadOnlyDictionary<string, Value> exportedProcs);
    public bool TryGetProc(string moduleName, string procName, out Value? procValue);
    public IReadOnlyDictionary<string, IReadOnlyDictionary<string, Value>> Snapshot();
    public void Clear();
}
```

From Plan 02 (ExecutionContext.cs new property at line ~157):
```csharp
public ModuleRegistry ModuleRegistry { get; } = new();
```

From flow-lang/Runtime/ModuleLoader.cs (insertion site lines 114-118):
```csharp
// 4. Execute in current context (no new frame - imports add to current scope)
var interpreter = ParentInterpreter ?? new Interpreter.Interpreter(context, _errorReporter, this);
interpreter.Execute(program);

_loadedModules.Add(resolvedPath);  // <-- HOOK GOES BETWEEN Execute and Add
```

From flow-lang/Interpreter/ExpressionEvaluator.cs (insertion site at top of EvaluateMemberAccess, line 627-629):
```csharp
private Value EvaluateMemberAccess(MemberAccessExpression member)
{
    var obj = Evaluate(member.Object);
    // ... 75 lines of existing dispatch ...
}
```

From flow-lang/Diagnostics/RenderingDiagnostics.cs:29 (advisory primitive):
```csharp
public static void WarnOnce(string sentinelKey, string message);
public static bool WasWarnedForTesting(string sentinelKey);
public static void ResetForTesting();
```

From flow-lang/Ast/Expressions/VariableExpression.cs: Name is the bare-identifier string.
From flow-lang/Ast/Expressions/MemberAccessExpression.cs: 3-field record (Object: Expression, MemberName: string).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModuleLoader registration hook + Interpreter ModuleDeclarationStatement no-op</name>
  <files>
    flow-lang/Runtime/ModuleLoader.cs,
    flow-lang/Interpreter/Interpreter.cs,
    flow-lang.Tests/Integration/Phase43/ModuleCollisionAdvisoryTests.cs
  </files>
  <read_first>
    flow-lang/Runtime/ModuleLoader.cs entire file (verify insertion site at lines 114-118; understand _loadedModules dedup at line 53; AlreadyLoaded short-circuit semantics per Pitfall 7),
    flow-lang/Runtime/ExecutionContext.cs lines 120-200 (verify GlobalFrame accessor; verify ModuleRegistry property from Plan 02),
    flow-lang/Runtime/StackFrame.cs (Functions dict shape — IReadOnlyDictionary or Dictionary; how to read it from ModuleLoader),
    flow-lang/Ast/Statements/ProcDeclaration.cs (verify Name property — needed for the walk-statements approach per RESEARCH A2),
    flow-lang/Interpreter/Interpreter.cs (locate the statement-dispatch site that handles ImportStatement / ProcDeclaration / VariableDeclaration / etc. — needs new ModuleDeclarationStatement arm that no-ops),
    flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce + WasWarnedForTesting + ResetForTesting),
    flow-lang.Tests/Integration/Phase38/LiveBlockDeterminismAdvisoryTests.cs entire ~78 lines (stderr-capture + dedup-across-runs pattern — exact template),
    flow-lang.Tests/Integration/Phase37/StretchAutoAdvisoryTests.cs lines 39-56 (CaptureStderr helper),
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pattern 5: ModuleLoader Registry Hook" lines 371-396,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pitfall 7: ModuleLoader.AlreadyLoaded short-circuit" lines 512-517,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pitfall 4: WarnOnce dedup across hot-reload" lines 486-491,
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"flow-lang/Runtime/ModuleLoader.cs — module-declaration capture hook",
    .planning/phases/43-module-names-qualified-imports/43-CONTEXT.md D-04 / D-05 / D-06
  </read_first>
  <behavior>
    - Test 1: A file containing `module math\nproc sin (Double: x) (mul x 2.0)` loaded via `use "@math"` produces `context.ModuleRegistry.Contains("math") == true` AND `context.ModuleRegistry.TryGetProc("math", "sin", out var proc) == true` with proc being a Function Value.
    - Test 2: A file WITHOUT a `module` declaration loaded via `use "@x"` produces NO entry in ModuleRegistry; its procs continue to inject into the unqualified namespace exactly as before (verified via `context.GlobalFrame.Functions.ContainsKey(procName)`).
    - Test 3: Two `use` statements pointing at files that both declare `module math` produce ONE stderr line containing `[module] duplicate module name 'math' — last load wins` (D-06); re-running the same FlowEngine.Execute on the same source emits ZERO additional advisories (WarnOnce dedup per Pitfall 4).
    - Test 4: Pitfall 7 — two `use "@math"` lines pointing at the same file produce exactly ONE registration call (NOT a duplicate-advisory fire) because the `_loadedModules.Contains(resolvedPath)` early-out at ModuleLoader.cs:53 short-circuits the second load. Verify `WasWarnedForTesting("module-dup:math") == false` for the single-file-declared-once case.
    - Test 5: Interpreter encountering ModuleDeclarationStatement at execute-time does NOT throw, does NOT print, does NOT mutate context.
  </behavior>
  <action>
    Per Pattern 5 + Pitfall 7:

    Modify `flow-lang/Runtime/ModuleLoader.cs`. Insert the registration hook AFTER `interpreter.Execute(program);` (line 116) and BEFORE `_loadedModules.Add(resolvedPath);` (line 118). The hook lives INSIDE the first-load path so the `if (_loadedModules.Contains(resolvedPath)) return AlreadyLoaded` at line 53 already guards the dedup. Recipe (per PATTERNS line 376-409 / RESEARCH A2 walk-statements approach):
    1. Check `if (program.Statements.Count > 0 && program.Statements[0] is ModuleDeclarationStatement modDecl)`.
    2. If `context.ModuleRegistry.Contains(modDecl.Name)`, call `RenderingDiagnostics.WarnOnce($"module-dup:{modDecl.Name}", $"[module] duplicate module name '{modDecl.Name}' — last load wins");` (D-06).
    3. Build `var exportedProcs = new Dictionary<string, Value>();` by iterating `program.Statements` and for each `ProcDeclaration proc` (use the actual AST type confirmed via read), look up `proc.Name` in `context.GlobalFrame.Functions`. Insert `(proc.Name, value)` into exportedProcs when found. This is RESEARCH A2 walk-statements approach (preferred over snapshot-and-diff).
    4. Call `context.ModuleRegistry.Register(modDecl.Name, exportedProcs);`. Dictionary implements IReadOnlyDictionary implicitly.

    Sentinel-key shape per PATTERNS S1 (line 806-808): `module-dup:<name>` (per-name dedup, NOT `module-dup:<name>:<filepath>`). Per-name dedup mirrors Pitfall 4 hot-reload behavior — a `live { }` reload re-running the same module-name → same key → still deduped, which is the desired UX.

    Modify `flow-lang/Interpreter/Interpreter.cs`: locate the statement-dispatch site (likely a switch / cascade in `Execute(Statement stmt)` or `ExecuteStatement`). Add an arm for `ModuleDeclarationStatement` that does NOTHING — `case ModuleDeclarationStatement: break;` or equivalent. XML comment: `// Phase 43 D-05: ModuleDeclarationStatement is consumed by ModuleLoader BEFORE Interpreter.Execute returns. At execute-time the statement is metadata-only — no runtime action required.`

    Create `flow-lang.Tests/Integration/Phase43/ModuleCollisionAdvisoryTests.cs`:
    - Namespace `FlowLang.Tests.Integration.Phase43`.
    - `[Collection("FlowScripts")]` + `IDisposable` ceremony per S5 (RenderingDiagnostics.ResetForTesting in ctor + Dispose).
    - Helper `private static string CaptureStderr(Action action)` per StretchAutoAdvisoryTests pattern (PATTERNS lines 680-695).
    - 5 `[Fact]` methods covering Test 1-5 above. For Test 1+2+5, drive end-to-end via `using var engine = new FlowEngine(); engine.Execute(source, "<test>");` with source strings containing module declarations. For Test 3 (duplicate-module advisory), the simplest harness: write two temp files to disk via `Path.GetTempFileName()` + add their directory to `engine.ModuleLoader.AdditionalSearchPaths`, then execute a source that does `use "<file1>"\nuse "<file2>"`. Assert stderr contains exactly one `[module] duplicate module name 'math'` line via String.Split + count.
    - For Test 4 (dedup): execute source `use "@audio"\nuse "@audio"` and assert `WasWarnedForTesting("module-dup:audio") == false` AFTER Plan 05 lands stdlib migration. To make Task 1 self-contained BEFORE Plan 05, use the temp-file approach: declare ONE module file, then `use` it twice from the same source. Pitfall 7's `_loadedModules.Contains(resolvedPath)` short-circuits the second load — verify zero `module-dup` advisory.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43.ModuleCollisionAdvisoryTests" --logger "console;verbosity=minimal" &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43.ModuleRegistryTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    ModuleLoader.cs has the post-Execute / pre-loadedModules.Add hook firing the duplicate-module advisory (D-06) and registering the ExportedProcSet via the walk-statements approach (RESEARCH A2); Interpreter.cs has a no-op arm for ModuleDeclarationStatement; ModuleCollisionAdvisoryTests.cs ships with 5 GREEN Facts (D-06 advisory + Pitfall 7 dedup + Test 1/2/5 register-correctness); Plan 02's ModuleRegistryTests remain GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExpressionEvaluator registry-first dispatch + last-import-wins shadow advisory + Pitfall 2 fall-through regression test</name>
  <files>
    flow-lang/Interpreter/ExpressionEvaluator.cs,
    flow-lang/Interpreter/Interpreter.cs,
    flow-lang/Runtime/ExecutionContext.cs,
    flow-lang/Runtime/ModuleLoader.cs,
    flow-lang.Tests/Integration/Phase43/QualifiedAccessDispatchTests.cs,
    flow-lang.Tests/Integration/Phase43/ModuleCollisionAdvisoryTests.cs
  </files>
  <read_first>
    flow-lang/Interpreter/ExpressionEvaluator.cs lines 600-720 (entire EvaluateMemberAccess + verify _context field name and member.Object type),
    flow-lang/Ast/Expressions/VariableExpression.cs (verify Name property),
    flow-lang/Ast/Expressions/MemberAccessExpression.cs (verify Object + MemberName fields),
    flow-lang/Interpreter/Interpreter.cs lines 1060-1090 (ExecuteImport — the call-site that hands control to ModuleLoader; the shadow-detection happens here OR inside ModuleLoader's post-load registration step),
    flow-lang/Runtime/StackFrame.cs (Functions dict shape; how to enumerate keys to detect shadow),
    flow-lang.Tests/Integration/Phase38/LiveBlockDeterminismAdvisoryTests.cs (dispatch test ceremony),
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pattern 6: ExpressionEvaluator Registry-First Branch" lines 398-424,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Pitfall 2: chord.root must not regress" lines 469-475,
    .planning/phases/43-module-names-qualified-imports/43-RESEARCH.md §"Open Questions 2: unknown-proc error shape",
    .planning/phases/43-module-names-qualified-imports/43-PATTERNS.md §"flow-lang/Interpreter/ExpressionEvaluator.cs",
    .planning/phases/43-module-names-qualified-imports/43-CONTEXT.md D-02 + D-04
  </read_first>
  <behavior>
    - Test 1 (REQ-MOD-03 dispatch happy path): Source declaring a module that exports `square`, called via `(modname.square 2.0)`, returns 4.0.
    - Test 2 (Pitfall 2 fall-through): `Chord c = Cmaj7\n(print c.Root)` continues printing `"C"`. Also `chord.Quality`, `voice.Pan`, `bar.Count`, `track.SampleRate`, `track.Channels`, `song.Sections` all unchanged.
    - Test 3 (unknown proc on registered module): `use "@math"\n(math.nope)` produces a clear error mentioning module 'math' and proc 'nope'. Error wording planner-discretion within the `[module]` / "no proc" framing.
    - Test 4 (REQ-MOD-04 shadow advisory): Two `use` statements where both modules export a same-named proc produces one stderr line: `[module] 'sin' from 'b' shadows 'sin' from 'a' — qualify with 'a.sin' or 'b.sin' to disambiguate`; calling the unqualified `sin` resolves to b's (last-import-wins per D-04).
    - Test 5 (shadow dedup): Re-running same source does NOT re-emit the same shadow advisory (WarnOnce dedup keyed by `module-shadow:a:b:sin`).
    - Test 6 (back-compat full suite): `tests/test_*.flow` (123 happy-path scripts) continue passing.
  </behavior>
  <action>
    Per Pattern 6 (ExpressionEvaluator Registry-First Branch):

    Modify `flow-lang/Interpreter/ExpressionEvaluator.cs`. Inside `EvaluateMemberAccess(MemberAccessExpression member)` at line 627, insert the registry-first branch as the FIRST statements of the method (BEFORE `var obj = Evaluate(member.Object);`). Recipe:
    1. Add `if (member.Object is VariableExpression varExpr && _context.ModuleRegistry.TryGetProc(varExpr.Name, member.MemberName, out var procValue)) return procValue!;`. Verify `_context` is the correct field name; if the class uses `_ctx` / `_executionContext` / etc. use that.
    2. Add a SECOND check immediately after for the unknown-proc-on-registered-module case (RESEARCH Open Question 2): `if (member.Object is VariableExpression varExpr2 && _context.ModuleRegistry.Contains(varExpr2.Name)) { return ReportUnknownMember(/*module-name shape*/ ...); }` — call the existing error-reporting helper (`ReportUnknownMember` at ExpressionEvaluator.cs:710 area, or a new `ReportUnknownModuleProc` helper) with a message containing both module-name and proc-name (e.g. `module '{moduleName}' has no proc '{procName}'`). Planner-discretion on exact helper choice.
    3. Insert XML comment block citing Phase 43 D-02 + Pitfall 2 mitigation rationale: registry-first because (a) cheaper check, (b) clearer errors, (c) preserves all existing instance-member access — `chord.root`/`song.sections`/`voice.Pan` LHSes evaluate to non-null values that don't have entries in the registry; only bare `VariableExpression` references to registered module names hit the new branch.

    The existing code path (`var obj = Evaluate(member.Object);` + 75 lines of dispatch) stays UNCHANGED below the new branch.

    Per D-04 last-import-wins shadow advisory:

    Add a new field on `ExecutionContext`: `public Dictionary<string, string> ProcOwnership { get; } = new();` (or kept private if no other site needs it — public preferred for testability mirror PATTERNS S2). XML doc: `Phase 43 Plan 43-03 (D-04) — per-context map of unqualified proc name → owning module name. Populated by ModuleLoader at module-registration time; read by ExecuteImport-shadow-detection to fire the last-import-wins advisory. Last-write-wins on collision (the new module CLAIMS the unqualified name).`

    In `flow-lang/Runtime/ModuleLoader.cs`, EXTEND the Task 1 registration hook (after building exportedProcs, BEFORE calling `context.ModuleRegistry.Register`):
    For each `procName` in `exportedProcs.Keys`:
    - If `context.ProcOwnership.TryGetValue(procName, out var priorOwner)` AND `priorOwner != modDecl.Name`:
      - `RenderingDiagnostics.WarnOnce($"module-shadow:{priorOwner}:{modDecl.Name}:{procName}", $"[module] '{procName}' from '{modDecl.Name}' shadows '{procName}' from '{priorOwner}' — qualify with '{priorOwner}.{procName}' or '{modDecl.Name}.{procName}' to disambiguate");`
    - `context.ProcOwnership[procName] = modDecl.Name;` (always update — last-write-wins; the GlobalFrame.Functions overwrite at module-load time naturally gives last-import-wins for the unqualified call resolution, no additional code needed).

    For procs from MODULE-LESS files (those without `module` declarations), the ProcOwnership map stays unmodified. If a module-less file's proc collides with a later module's proc, the existing dictionary-overwrite at `GlobalFrame.Functions` handles last-write-wins; no advisory fires (acceptable per D-01 — module-less files don't claim a namespace). Document this in the XML comment on ProcOwnership.

    Modify `flow-lang.Tests/Integration/Phase43/ModuleCollisionAdvisoryTests.cs` (created in Task 1): add 2 new `[Fact]` methods covering Test 4 + Test 5 (shadow advisory + dedup). Use the same `CaptureStderr` helper; assert stderr substrings: module-a name, module-b name, proc-name, and the "qualify with" suggestion text. For Test 5, execute the same source twice and assert the shadow-line count is 1 (not 2).

    Create `flow-lang.Tests/Integration/Phase43/QualifiedAccessDispatchTests.cs`:
    - Namespace + ceremony per S5.
    - 4 `[Fact]` methods covering Test 1, Test 2 (Pitfall 2 regression), Test 3 (unknown proc).
    - For Test 1: drive `using var engine = new FlowEngine(); engine.Execute(source, "<test>");` where source declares a test module file (via temp file + AdditionalSearchPaths, OR by reusing a stdlib module like `@audio` AFTER Plan 05 lands — for Wave 2 ordering, use the temp-file approach). Assert no errors AND the printed/returned value matches expected.
    - For Test 2: 6 sub-assertions (chord.Root, chord.Quality, voice.Pan, bar.Count, track.SampleRate, song.Sections) — pin the existing instance-member dispatch surface (Pitfall 2 regression).
    - For Test 3: assert `errorReporter.HasErrors` AND first error message contains `'math'` AND `'nope'` (substring-tolerant).
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase43" --logger "console;verbosity=minimal" &amp;&amp; for t in tests/test_*.flow; do dotnet run --project flow-interpreter "$t" &gt; /dev/null 2&gt;&amp;1 || { echo "FAIL: $t"; exit 1; }; done</automated>
  </verify>
  <done>
    ExpressionEvaluator.EvaluateMemberAccess has the gated registry-first branch + unknown-proc-on-registered-module clearer-error branch; ExecutionContext gained the ProcOwnership tracking dict; ModuleLoader extension fires the D-04 shadow advisory at the proc-shadow site; ModuleCollisionAdvisoryTests.cs gained 2 new shadow-related Facts; QualifiedAccessDispatchTests.cs ships with 4 GREEN Facts (dispatch + Pitfall 2 fall-through + unknown-proc error); all 123 `tests/test_*.flow` happy-path scripts pass; pre-existing 34 xUnit failures remain pre-existing (not introduced by this plan).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .flow source → ModuleLoader → ModuleRegistry | Composer-trusted source; module-name identifiers validated at parse time |
| ModuleRegistry → ExpressionEvaluator | Same-process in-memory lookup; gate on VariableExpression prevents arbitrary-LHS bypass (RESEARCH §Security line 783) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Tampering | ExpressionEvaluator.EvaluateMemberAccess registry-first branch | mitigate | Gate `member.Object is VariableExpression varExpr` ensures only bare identifiers can hit the registry path; all existing instance-member dispatch preserved (Pitfall 2). Registry returns a Function Value the caller's existing function-call evaluator invokes — same security envelope as user-defined procs. |
| T-43-04 | Information Disclosure | Shadow advisory message reveals module-pair name | accept | Module names are .flow-source-author-controlled; revealing pair is the entire point of the advisory (helping composer disambiguate). No PII / no secrets. |
| T-43-05 | DoS | ModuleLoader re-registration on every `use` | mitigate | `_loadedModules.Contains(resolvedPath)` early-out at ModuleLoader.cs:53 ensures hook runs ONCE per resolved file. Pitfall 7 regression test pins this. |
</threat_model>

<verification>
- `(modname.fn args)` dispatches via ModuleRegistry for registered modules.
- `chord.root` / `voice.Pan` / `track.SampleRate` etc. continue dispatching via existing instance-member paths (Pitfall 2 regression-pinned).
- D-06 duplicate-module advisory fires once per process per module name.
- D-04 shadow advisory fires once per process per (priorOwner, newOwner, procName) triple.
- ModuleDeclarationStatement is no-op at execute-time.
- All Phase 43 fixtures GREEN.
- 123 `tests/test_*.flow` happy-path scripts continue to PASS.
- Full xUnit suite shows zero new failures introduced by this plan (pre-existing 34 from Phase 28/29/35/38 stay pre-existing per Phase 42 deferred-items.md baseline).
</verification>

<success_criteria>
- 4 production files modified per the `files_modified` list.
- 2 new test fixtures + 1 modified test fixture (Task 1 created ModuleCollisionAdvisoryTests, Task 2 extended it).
- Phase 43 xUnit fact total = Plan 01 (5) + Plan 02 (6) + Plan 03 (5 + 2 + 4) = 22 GREEN.
- Zero regressions in `tests/test_*.flow`.
- Zero new failures in the existing `flow-lang.Tests` suite (modulo pre-existing 34).
</success_criteria>

<output>
Create `.planning/phases/43-module-names-qualified-imports/43-03-SUMMARY.md` documenting: ModuleLoader hook code shape, the unknown-proc-on-registered-module error helper choice, ProcOwnership lifecycle decisions, and the exact stderr advisory wordings shipped (so Plan 04 + Plan 05 can mirror conventions).
</output>
