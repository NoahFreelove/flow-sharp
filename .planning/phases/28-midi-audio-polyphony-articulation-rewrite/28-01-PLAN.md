---
phase: 28-midi-audio-polyphony-articulation-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/Lexing/SimpleLexer.cs
  - flow-lang/Lexing/TokenType.cs
  - flow-lang/Parsing/Parser.NoteStream.cs
  - flow-lang/Ast/Expressions/NoteStreamExpression.cs
  - flow-lang/Runtime/NoteStreamCompiler.cs
  - flow-lang/TypeSystem/SpecialTypes/NoteType.cs
autonomous: true
requirements: [SPEC-1, SPEC-3]
must_haves:
  truths:
    - "Parser accepts `| {voice C4w} {voice C5q D5q E5q F5q} |` without error"
    - "Compiler emits two parallel BarData objects sharing onset 0 within one bar"
    - "Articulation enum contains Legato value"
    - "Parser accepts `leg` token; produces Articulation.Legato on the preceding note"
  artifacts:
    - flow-lang/Ast/Expressions/NoteStreamExpression.cs (or sibling) defining VoiceBlockElement
    - flow-lang/TypeSystem/SpecialTypes/NoteType.cs containing Articulation.Legato
  key_links:
    - "Parser.NoteStream.cs:153 brace dispatch peeks for `voice` keyword before falling to tuplet"
    - "TryParseArticulation switch adds `case \"leg\":` returning Articulation.Legato"
---

<objective>
Add the parser/lexer/compiler surface for single-sequence polyphony via `{voice ...}` blocks AND first-class `Articulation.Legato`. This plan delivers the source-level recognition and AST-emission paths that Plans 02-07 build on. No audio render changes yet — this is parser work plus the enum value.

Purpose: Composers can write held + running notes in one Sequence (SPEC-1) and per-note `leg` articulation (SPEC-3) before Phase 28's runtime/synthesizer changes land.
Output: New `VoiceBlockElement` AST node, `Articulation.Legato` enum value, `leg` token, parser dispatch, compiler emission of parallel `BarData` per voice block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-midi-audio-polyphony-articulation-rewrite/28-SPEC.md
@.planning/phases/28-midi-audio-polyphony-articulation-rewrite/28-CONTEXT.md
@.planning/phases/28-midi-audio-polyphony-articulation-rewrite/28-RESEARCH.md
@.planning/phases/28-midi-audio-polyphony-articulation-rewrite/28-PATTERNS.md
@flow-lang/Parsing/Parser.NoteStream.cs
@flow-lang/Lexing/SimpleLexer.cs
@flow-lang/Lexing/TokenType.cs
@flow-lang/Runtime/NoteStreamCompiler.cs
@flow-lang/TypeSystem/SpecialTypes/NoteType.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `Articulation.Legato` enum value</name>
  <files>flow-lang/TypeSystem/SpecialTypes/NoteType.cs</files>
  <read_first>flow-lang/TypeSystem/SpecialTypes/NoteType.cs</read_first>
  <action>In the `Articulation` enum at line 200, add `Legato` as a NEW value AFTER `Sforzando`. The complete enum becomes: `Normal, Staccato, Tenuto, Marcato, Accent, Sforzando, Legato`. Update the doc comment above the enum to mention Legato. Do NOT renumber existing values — add Legato at the end so existing enum-int casts (if any) remain stable.</action>
  <verify>grep -A 9 "public enum Articulation" flow-lang/TypeSystem/SpecialTypes/NoteType.cs | grep -c "Legato"</verify>
  <acceptance_criteria>
    - flow-lang/TypeSystem/SpecialTypes/NoteType.cs contains `Legato` exactly once on a line preceded by `Sforzando,` and followed by `}`
    - dotnet build flow-lang succeeds with zero errors after the edit
  </acceptance_criteria>
  <done>Articulation enum has 7 values; Legato is the last entry.</done>
</task>

<task type="auto">
  <name>Task 2: Add `VoicePool` to TokenType and `voicePool` keyword to lexer</name>
  <files>flow-lang/Lexing/TokenType.cs, flow-lang/Lexing/SimpleLexer.cs</files>
  <read_first>flow-lang/Lexing/TokenType.cs, flow-lang/Lexing/SimpleLexer.cs</read_first>
  <action>1) In `flow-lang/Lexing/TokenType.cs`, add `VoicePool,` to the enum near `ReverbTime,` (around line 27). Order doesn't matter for correctness, just keep adjacent to other musical-context tokens. 2) In `flow-lang/Lexing/SimpleLexer.cs` keyword switch (around line 867), add `"voicePool" => TokenType.VoicePool,` immediately after `"reverbTime" => TokenType.ReverbTime,`. Match the existing camelCase convention used by `reverbTime`.</action>
  <verify>grep -n "VoicePool" flow-lang/Lexing/TokenType.cs && grep -n "voicePool" flow-lang/Lexing/SimpleLexer.cs</verify>
  <acceptance_criteria>
    - flow-lang/Lexing/TokenType.cs contains `VoicePool`
    - flow-lang/Lexing/SimpleLexer.cs contains `"voicePool" => TokenType.VoicePool`
    - dotnet build flow-lang succeeds
  </acceptance_criteria>
  <done>Lexer recognizes `voicePool` keyword.</done>
</task>

<task type="auto">
  <name>Task 3: Add `leg` articulation token to parser</name>
  <files>flow-lang/Parsing/Parser.NoteStream.cs</files>
  <read_first>flow-lang/Parsing/Parser.NoteStream.cs</read_first>
  <action>In `TryParseArticulation` (lines 397-421), add `case "leg":` to the switch immediately after `case "marc":`. Body: `Advance(); return Articulation.Legato;`. Also update `IsEndOfNoteStream` (lines 427-448) — add `"leg"` to the literal-string set on line 437 so the parser keeps consuming the note-stream when it sees `leg`. Specifically, change the existing tuple `text is "stacc" or "ten" or "marc" or "cresc" or "decresc"` to `text is "stacc" or "ten" or "marc" or "leg" or "cresc" or "decresc"`.</action>
  <verify>grep -n '"leg"' flow-lang/Parsing/Parser.NoteStream.cs</verify>
  <acceptance_criteria>
    - flow-lang/Parsing/Parser.NoteStream.cs `TryParseArticulation` switch contains `case "leg":` returning `Articulation.Legato`
    - flow-lang/Parsing/Parser.NoteStream.cs `IsEndOfNoteStream` recognizes `"leg"` as a continuation token
  </acceptance_criteria>
  <done>Parser accepts `leg` after a note element; produces Articulation.Legato.</done>
</task>

<task type="auto">
  <name>Task 4: Define VoiceBlockElement AST node</name>
  <files>flow-lang/Ast/Expressions/NoteStreamExpression.cs</files>
  <read_first>flow-lang/Ast/Expressions/NoteStreamExpression.cs</read_first>
  <action>In the file containing the `NoteStreamElement` union (find via `grep -l "NoteStreamElement" flow-lang/Ast/Expressions/*.cs`), add a new record alongside the existing `TupletElement`, `NoteElement`, etc. Definition: `public record VoiceBlockElement(SourceLocation Location, IReadOnlyList<NoteStreamElement> Children) : NoteStreamElement(Location);`. Place it adjacent to `TupletElement` for discoverability. If there's no shared file, follow the existing pattern (one record per file or a shared union file — check sibling files in `flow-lang/Ast/Expressions/`).</action>
  <verify>grep -rn "record VoiceBlockElement" flow-lang/Ast/Expressions/</verify>
  <acceptance_criteria>
    - VoiceBlockElement record exists in flow-lang/Ast/Expressions/
    - Inherits from NoteStreamElement
    - Has Location and Children (IReadOnlyList<NoteStreamElement>) fields
  </acceptance_criteria>
  <done>VoiceBlockElement compiles as an AST node.</done>
</task>

<task type="auto">
  <name>Task 5: Add `{voice ...}` brace dispatch to ParseNoteStream</name>
  <files>flow-lang/Parsing/Parser.NoteStream.cs</files>
  <read_first>flow-lang/Parsing/Parser.NoteStream.cs</read_first>
  <action>In `ParseNoteStream` (line 38), modify the existing `if (Check(TokenType.LBrace))` arm at line 153. Before invoking the tuplet path that calls `Expect(TokenType.IntLiteral, ...)`, peek at the token AFTER the `{`. If it is an Identifier with text `"voice"`, dispatch to a new `ParseVoiceBlock(elemLoc)` helper; otherwise fall through to the existing tuplet logic. Concrete shape:

```csharp
if (Check(TokenType.LBrace))
{
    var elemLoc = CurrentToken.Location;
    int savedPos = _current;
    Advance(); // consume {
    if (Check(TokenType.Identifier) && CurrentToken.Text == "voice")
    {
        Advance(); // consume "voice"
        var children = ParseVoiceBlockChildren(); // new helper, see Task 6
        Expect(TokenType.RBrace, "Expected '}' to close voice block");
        currentBarElements.Add(new VoiceBlockElement(elemLoc, children));
        continue;
    }
    // Not a voice block — rewind and let tuplet path handle it
    _current = savedPos;
    // (existing tuplet code follows unchanged from line 156 onward)
}
```

Apply the same restructure to `ParseTupletChildren` (line 478) so `{voice ...}` works inside a tuplet too — but for Phase 28 scope this is OPTIONAL; voice-block-inside-tuplet can stay as a parse error if it simplifies the diff. Document the chosen behavior in a code comment.</action>
  <verify>grep -n 'CurrentToken.Text == "voice"' flow-lang/Parsing/Parser.NoteStream.cs</verify>
  <acceptance_criteria>
    - flow-lang/Parsing/Parser.NoteStream.cs ParseNoteStream contains a `voice` keyword arm before the tuplet IntLiteral expect
    - Falling through to tuplet logic preserves backward-compat for `{3 C4 D4 E4}q`
    - dotnet build flow-lang succeeds
  </acceptance_criteria>
  <done>`{voice ...}` parses to a VoiceBlockElement; existing `{3 ...}q` tuplet still parses.</done>
</task>

<task type="auto">
  <name>Task 6: Add ParseVoiceBlockChildren helper</name>
  <files>flow-lang/Parsing/Parser.NoteStream.cs</files>
  <read_first>flow-lang/Parsing/Parser.NoteStream.cs</read_first>
  <action>Add a new private method `ParseVoiceBlockChildren()` that parses note-stream elements until `}` is reached. Mirror the structure of `ParseTupletChildren` (line 478) but accept all element kinds inside a voice block: NoteElement, RestElement, ChordElement, NamedChordElement, RandomChoiceElement, TupletElement (nested OK), and VoiceBlockElement (nested voice = parse error for Phase 28 — emit clear message "Nested voice blocks are not supported"). Return `List<NoteStreamElement>`. The method handles its own loop and stops at RBrace; the caller `ParseNoteStream` then `Expect(TokenType.RBrace, ...)`.</action>
  <verify>grep -n "ParseVoiceBlockChildren" flow-lang/Parsing/Parser.NoteStream.cs</verify>
  <acceptance_criteria>
    - flow-lang/Parsing/Parser.NoteStream.cs has a private `ParseVoiceBlockChildren` method
    - Method handles NoteElement, RestElement, ChordElement, NamedChordElement, TupletElement
    - Method emits clear error on nested voice blocks
  </acceptance_criteria>
  <done>Voice block children parse correctly; nested voices report a clear error.</done>
</task>

<task type="auto">
  <name>Task 7: Add NoteStreamCompiler dispatch for VoiceBlockElement</name>
  <files>flow-lang/Runtime/NoteStreamCompiler.cs</files>
  <read_first>flow-lang/Runtime/NoteStreamCompiler.cs</read_first>
  <action>In `CompileBar` (around line 87), add `case VoiceBlockElement voiceBlock:` to the switch statement at line 104. Each voice block compiles to a SEPARATE `BarData` that shares the parent bar's onset (0). Per Research §1, the chosen strategy is "emit N parallel BarData per voice block, share lead bar onset." Implementation:

```csharp
case VoiceBlockElement voiceBlock:
    // Compile this voice block's children as a parallel mini-bar.
    // Each block becomes its own BarData; the rendering layer (BarRenderer/SongRenderer)
    // mixes them additively because they share the same beat offset.
    var voiceBar = CompileVoiceBlock(voiceBlock, timeSig, context, executionContext);
    // Tag the resulting voice notes onto the parent bar via BarData metadata
    // (concrete mechanism: extend BarData with a List<BarData> ParallelVoices field, or
    // attach voice-index to each MusicalNoteData and let BarRenderer demux. Lock the
    // ParallelVoices approach because it preserves the existing MusicalNoteData shape
    // and gives BarRenderer a clear iteration target.)
    barData.ParallelVoices ??= new List<BarData>();
    barData.ParallelVoices.Add(voiceBar);
    break;
```

Add `public List<BarData>? ParallelVoices { get; set; }` field to `BarData` (in flow-lang/StandardLibrary/Audio/ — find via `grep -rn "class BarData" flow-lang/`) — the modification to BarData lives in this same task because the compiler can't emit parallel voices without it.

Implement helper `CompileVoiceBlock(VoiceBlockElement block, TimeSignatureData timeSig, MusicalContext context, ExecutionContext? executionContext)` that returns a BarData containing the block's children compiled in sequence (mirrors CompileBar's element loop, but takes pre-parsed children instead of bar.Elements).</action>
  <verify>grep -n "VoiceBlockElement" flow-lang/Runtime/NoteStreamCompiler.cs && grep -n "ParallelVoices" flow-lang/Runtime/NoteStreamCompiler.cs flow-lang/StandardLibrary/Audio/*.cs</verify>
  <acceptance_criteria>
    - flow-lang/Runtime/NoteStreamCompiler.cs CompileBar has a `case VoiceBlockElement` arm
    - flow-lang/StandardLibrary/Audio/ has a `BarData` class containing `public List<BarData>? ParallelVoices`
    - CompileVoiceBlock helper produces a BarData from a VoiceBlockElement's children
  </acceptance_criteria>
  <done>VoiceBlockElement compiles to a child BarData hung off `ParallelVoices`.</done>
</task>

<task type="auto">
  <name>Task 8: Unit test — Articulation.Legato exists in enum</name>
  <files>flow-lang.Tests/Unit/Phase28/LegatoEnumTests.cs</files>
  <read_first>flow-lang.Tests/Unit/Phase26_2/, flow-lang/TypeSystem/SpecialTypes/NoteType.cs</read_first>
  <action>Create `flow-lang.Tests/Unit/Phase28/LegatoEnumTests.cs` containing a single xUnit Fact `Legato_EnumValueExists` that asserts `Enum.GetValues(typeof(Articulation)).Cast<Articulation>().Contains(Articulation.Legato)`. Use the same namespace pattern as `flow-lang.Tests.Unit.Phase26_2`. Include `[Collection("FlowScripts")]` if other Phase 28 tests will share state — otherwise omit. The test file establishes the `flow-lang.Tests/Unit/Phase28/` directory.</action>
  <verify>dotnet test flow-lang.Tests --filter "FullyQualifiedName~Legato_EnumValueExists"</verify>
  <acceptance_criteria>
    - flow-lang.Tests/Unit/Phase28/LegatoEnumTests.cs exists
    - Contains a [Fact] named Legato_EnumValueExists
    - Test passes (Articulation.Legato is in the enum)
  </acceptance_criteria>
  <done>Phase 28 unit test directory established; Legato enum existence is gate-tested.</done>
</task>

<task type="auto">
  <name>Task 9: Unit test — Parser accepts `leg` token and `{voice ...}` block</name>
  <files>flow-lang.Tests/Unit/Phase28/VoiceBlockParserTests.cs</files>
  <read_first>flow-lang.Tests/Unit/Phase26_2/, flow-lang.Tests/Fixtures/FlowEngineRunner.cs</read_first>
  <action>Create `flow-lang.Tests/Unit/Phase28/VoiceBlockParserTests.cs` with these xUnit Facts:

  1. `Parser_AcceptsLegToken` — feeds `Sequence s = | C4q leg D4q E4q |` to FlowEngineRunner.RunSource; asserts no parse errors AND that the resulting SequenceData's first MusicalNoteData has `Articulation.Legato` while the third has `Articulation.Normal`.

  2. `VoiceBlockParser_AcceptsBasicSyntax` — feeds `Sequence s = | {voice C4w} {voice C5q D5q E5q F5q} |` to FlowEngineRunner.RunSource; asserts no parse errors.

  3. `VoiceBlockCompiler_EmitsParallelBars` — feeds the same source as #2; reaches into the resulting SequenceData; asserts `bar.ParallelVoices` has Count == 2; asserts the first parallel voice contains a single C4 whole note, second contains four quarter notes C5/D5/E5/F5.

Use the FlowEngineRunner fixture (mirror Phase 18 ByteIdenticalShowcaseTests.cs pattern lines 60-72 for accessing the engine). For asserting on SequenceData, expose it via a script that assigns to a variable then queries — OR inspect the AST directly via the engine's parser surface.</action>
  <verify>dotnet test flow-lang.Tests --filter "FullyQualifiedName~VoiceBlockParser"</verify>
  <acceptance_criteria>
    - flow-lang.Tests/Unit/Phase28/VoiceBlockParserTests.cs exists with 3 [Fact] methods
    - All 3 tests pass
    - Tests cover: leg token → Articulation.Legato, voice-block parses, voice-block compiles to ParallelVoices
  </acceptance_criteria>
  <done>Voice block + leg token parser/compiler facts are GREEN.</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `dotnet build flow-lang flow-interpreter flow-lang.Tests` — zero errors
2. Run `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase28"` — all 4 facts (Legato_EnumValueExists + 3 voice-block facts) pass
3. Run `dotnet test flow-lang.Tests` — full suite still GREEN (no regression)
4. Spot-check: `grep -rn "Legato\|VoiceBlockElement\|ParallelVoices\|leg.*Articulation" flow-lang/ flow-lang.Tests/Unit/Phase28/` shows the new symbols
</verification>
