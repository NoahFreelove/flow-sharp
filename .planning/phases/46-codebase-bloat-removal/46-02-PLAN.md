---
phase: 46-codebase-bloat-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/Audio/TimelineMap.cs
  - flow-lang/StandardLibrary/Audio/SongRenderer.cs
  - flow-lang/StandardLibrary/Audio/BarRenderer.cs
  - flow-lang/StandardLibrary/Audio/SequenceRenderer.cs
autonomous: true
requirements: [CLEAN-02]
nyquist_compliant: true

must_haves:
  truths:
    - "D-02: the TimelineMap editor-highlighting stack is fully removed — TimelineMap.cs deleted + all parallel *WithTimeline / TimelineMap-typed renderer overloads removed"
    - "D-02: the PRIMARY (non-TimelineMap) render path is byte-identical — BarType.ToTimeline()/SequenceType.ToTimeline() are NOT touched (unrelated despite the name)"
  artifacts:
    - path: "flow-lang/Audio/TimelineMap.cs"
      provides: "DELETED — file no longer exists"
  key_links:
    - from: "grep TimelineMap|TimelineEntry across flow-lang/flow-interpreter/flow-cli/flow-lsp/flow-lang.Tests"
      to: "zero matches"
      via: "complete removal"
      pattern: "TimelineMap|TimelineEntry"
---

<objective>
D-02 (CLEAN-02): Remove the TimelineMap editor-highlighting stack — dead internal render plumbing scaffolded for an LSP live-highlighting feature that was never wired. Zero callers anywhere (`RenderSongWithTimeline` is `public` but unreferenced repo-wide). ~250 LOC.

Purpose: Delete genuinely unreachable internal plumbing (D-01(a)). The primary Song/Section render path is unaffected → rendered bytes stay byte-identical.
Output: `TimelineMap.cs` deleted; the four `*WithTimeline` / TimelineMap-typed renderer overloads removed; the non-timeline render path untouched.

Atomic commit (D-18): this is one self-contained target → one commit for selective revert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-codebase-bloat-removal/46-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TimelineMap.cs and all parallel *WithTimeline / TimelineMap-typed renderer overloads</name>
  <files>flow-lang/Audio/TimelineMap.cs, flow-lang/StandardLibrary/Audio/SongRenderer.cs, flow-lang/StandardLibrary/Audio/BarRenderer.cs, flow-lang/StandardLibrary/Audio/SequenceRenderer.cs</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (§D-02 — exact ranges table + the KEEP/Pitfall-4 warning about ToTimeline())
    - flow-lang/Audio/TimelineMap.cs (whole file 1-67 — the TimelineEntry record + TimelineMap class to delete)
    - flow-lang/StandardLibrary/Audio/SongRenderer.cs (lines 450-554 — RenderSongWithTimeline 453-505 + RenderSectionWithTimeline 510-554, plus the /// summaries at 450-452, 507-509)
    - flow-lang/StandardLibrary/Audio/BarRenderer.cs (lines 305-389 — the four timeline-threading overloads; confirm line 81 and 362 references to BarType.ToTimeline() so you do NOT touch those)
    - flow-lang/StandardLibrary/Audio/SequenceRenderer.cs (lines 123-165 — the timeline-aware RenderSequenceToVoices string+synth pair)
  </read_first>
  <action>
    Per RESEARCH §D-02 exact ranges:
    1. DELETE the whole file `flow-lang/Audio/TimelineMap.cs` (1-67: `TimelineEntry` record + `TimelineMap` class).
    2. `SongRenderer.cs`: delete `RenderSongWithTimeline` (453-505) + `RenderSectionWithTimeline` (510-554, private) including their `/// <summary>` blocks (450-452, 507-509).
    3. `BarRenderer.cs`: delete the timeline-threading overloads in 305-389 — `RenderBarToVoices(...,TimelineMap,...)` (309-329) + the `RenderBarAtBeat(...,TimelineMap,...)` string+synth pair (331-389). The non-timeline `RenderBarToVoices`/`RenderBarAtBeat` ABOVE line 305 STAY.
    4. `SequenceRenderer.cs`: delete the timeline-aware `RenderSequenceToVoices(...,TimelineMap,...)` string+synth pair (123-165). The non-timeline overloads above 123 STAY.

    CRITICAL (Pitfall 4): Do NOT delete `BarType.ToTimeline()` (BarType.cs:182) or `SequenceType.ToTimeline()` (SequenceType.cs:46). The name overlaps "Timeline" but these are the PRIMARY render path's beat-offset projection (used at BarRenderer.cs:81,362, VisualizationFunctions.cs:53). Only remove `TimelineMap`/`TimelineEntry`-TYPED members and the `*WithTimeline` overloads. Since line numbers shift as you delete, work bottom-up within each file and re-locate by the typed signature (`TimelineMap` parameter / `WithTimeline` name), not by raw line number.
  </action>
  <verify>
    <automated>grep -rn "TimelineMap\|TimelineEntry" flow-lang flow-interpreter flow-cli flow-lsp flow-lang.Tests ; test ! -f flow-lang/Audio/TimelineMap.cs && echo FILE_DELETED ; dotnet build flow-lang/flow-lang.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    `flow-lang/Audio/TimelineMap.cs` no longer exists; `grep -rn "TimelineMap\|TimelineEntry"` across flow-lang/flow-interpreter/flow-cli/flow-lsp/flow-lang.Tests returns ZERO; `BarType.ToTimeline()`/`SequenceType.ToTimeline()` still present; `dotnet build` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Removing dead internal render plumbing introduces no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02 | Tampering | render-path regression | mitigate | Primary render path untouched (only TimelineMap-typed members removed); full suite + two-run cmp-clean determinism gate proves byte-identity. Correctness control, not security. |

**Honest statement:** No new attack surface. This deletes unreachable editor-highlighting plumbing with zero callers; the only risk class is accidental render-path regression, covered by the test-green + determinism gate. No new input/network/file/parse surface. No package installs (T-46-SC N/A).
</threat_model>

<verification>
- `grep -rn "TimelineMap\|TimelineEntry"` returns zero across all projects.
- `dotnet build` green; full `dotnet test` green; primary render path byte-identical (two-run cmp-clean).
- `BarType.ToTimeline()` / `SequenceType.ToTimeline()` retained.
</verification>

<success_criteria>
- TimelineMap stack fully removed; build + full suite green; render bytes unchanged.
</success_criteria>

<output>
Create `.planning/phases/46-codebase-bloat-removal/46-02-SUMMARY.md` when done. Commit atomically (D-18): `refactor(46): remove TimelineMap editor-highlighting stack (D-02)`.
</output>
