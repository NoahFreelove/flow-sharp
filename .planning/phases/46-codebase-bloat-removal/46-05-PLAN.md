---
phase: 46-codebase-bloat-removal
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang.Tests/Unit/Phase46/ProgressionDslTests.cs
  - examples/showcase.flow
  - flow-lang/StandardLibrary/Audio/Timeline.cs
  - flow-lang/StandardLibrary/Audio/Track.cs
  - flow-lang/composition.flow
  - flow-lang/bars.flow
  - flow-lang/StandardLibrary/Bars.cs
autonomous: true
requirements: [CLEAN-12, CLEAN-16]
nyquist_compliant: true

must_haves:
  truths:
    - "D-12: the Progression DSL (`progression | I IV V |`) is KEPT + INVESTED — a new ProgressionDslTests covers 4-bar output, :N bar-count suffix, voices N modifier, no-key error, and voice-leading determinism; examples/showcase.flow gains a NON-rendered progression demo (byte-identical showcase WAV preserved)"
    - "D-16: kept-but-superseded surfaces (Track/Timeline, bars.flow) carry a short comment-only `legacy / superseded by X — kept as a usable surface` note. NO deprecation warnings, NO stderr advisories. std.flow:6 @bars import STAYS"
    - "Anti-scope KEEPs honored (no code change), per D-01 — the following composer-callable surfaces are DELIBERATELY KEPT and untouched by this removal phase: D-10 (Track/Timeline DAW multitrack layer — this plan's D-16 note documents it as legacy), D-11 (bars.flow legacy Bar API — D-16 legacy note added here; std.flow:6 `@bars` import retained), D-13 (OscillatorState/Envelope low-level synth API — no change), D-14 (audio.flow buffer convenience layer — no change), D-15 (`preview` builtin — no change). And D-17 (phase breadth = audit §1+§2 filtered by D-01; audit §3 low-confidence direction calls are OUT of scope — anti-scope honored). This truth records that the phase deliberately did NOT remove these surfaces."
  artifacts:
    - path: "flow-lang.Tests/Unit/Phase46/ProgressionDslTests.cs"
      provides: "Progression DSL unit coverage (mirrors EuclideanSwingTests idiom)"
      contains: "progression"
      min_lines: 40
  key_links:
    - from: "ProgressionDslTests.cs"
      to: "ProgressionCompiler via FlowEngineRunner.RunSource + GetVariable(...).As<SequenceData>()"
      via: "key Cmajor { Sequence s = progression | ... | }"
      pattern: "GetVariable"
---

<objective>
Two positive-work / keep-treatment tasks bundled (distinct files from all removal plans):

1. **D-12 (CLEAN-12):** The Progression DSL (`progression | I IV V |`) is KEPT (a distinct ergonomic chord-progression surface, D-01) + INVESTED. It currently has ZERO unit tests. Add a `ProgressionDslTests` class (mirroring the `EuclideanSwingTests` idiom) and extend `examples/showcase.flow` with a non-rendered progression demo.
2. **D-16 (CLEAN-16):** For the kept-but-superseded surfaces (Track/Timeline per D-10, bars.flow per D-11), add a short comment-only "legacy / superseded by X — kept as a usable surface" note so future readers know the canonical path. NO deprecation warnings, NO stderr advisories (pre-traction). The `std.flow:6` `@bars` import STAYS.

Purpose: Honor D-01 keeps with investment (D-12) + documentation (D-16) instead of removal.

**Anti-scope KEEPs honored (no code change), per D-01:** beyond the D-10/D-11 surfaces this plan documents, the following composer-callable surfaces are DELIBERATELY KEPT and untouched by the whole removal phase — D-13 (OscillatorState/Envelope low-level synth API), D-14 (audio.flow buffer convenience layer), D-15 (`preview` builtin). And D-17 fixes phase breadth = audit §1+§2 filtered by D-01; audit §3 low-confidence direction calls are OUT of scope. No task below removes any of these — they are recorded here so the decision-coverage gate sees the deliberate keep.

Output: New ProgressionDslTests (green); a non-rendered progression demo in showcase.flow; 5 comment-only legacy notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-codebase-bloat-removal/46-RESEARCH.md

<interfaces>
<!-- D-12 (verified live) -->
ProgressionExpression / ProgressionElement (flow-lang/Ast/Expressions/ProgressionExpression.cs)
ProgressionCompiler.Compile(ProgressionExpression, MusicalContext) -> SequenceData (Runtime/ProgressionCompiler.cs:50)
EvaluateProgression requires active key context else errors "progression requires an active key context" (ExpressionEvaluator.cs:1055-1069)
Parser: progression [voices N] | I IV V | (Parser.cs:1718-1721); :N bar-count suffix on a numeral
FlowEngineRunner.RunSource(source) -> (Success, Stdout, Stderr, ErrorCount) (fixtures/FlowEngineRunner.cs:31)
FlowEngineRunner.GetVariable(name) -> Value (.As<SequenceData>()) (fixtures/FlowEngineRunner.cs:45)
Idiom to copy: flow-lang.Tests/Unit/Phase15/EuclideanSwingTests.cs (rest-filter via HitNotes; walk seq.Bars[].MusicalNotes)

<!-- D-16 doc-note locations (verified live) -->
flow-lang/StandardLibrary/Audio/Timeline.cs:7-9 (the /// summary on Timeline)
flow-lang/StandardLibrary/Audio/Track.cs (class header)
flow-lang/composition.flow:1-5 (file header above the Track wrappers at 68-150)
flow-lang/bars.flow:1-2 (file header)
flow-lang/StandardLibrary/Bars.cs:5-7 (the /// summary on Bars)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProgressionDslTests + non-rendered showcase demo (D-12)</name>
  <files>flow-lang.Tests/Unit/Phase46/ProgressionDslTests.cs, examples/showcase.flow</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (§D-12 — the RunProg idiom, the 5 assertions to add, and the showcase Option A recommendation)
    - flow-lang.Tests/Unit/Phase15/EuclideanSwingTests.cs (the [Collection("FlowScripts")] class shell + HitNotes rest-filter + Bars[].MusicalNotes walk to copy)
    - flow-lang/Runtime/ProgressionCompiler.cs (Compile signature + FindNearestPitchClass determinism — drives the determinism assertion)
    - flow-lang/Interpreter/ExpressionEvaluator.cs (lines 1055-1069 — the no-key error message to assert against)
    - flow-lang/Parsing/Parser.cs (lines 1718-1721 — the `voices N` modifier + `:N` suffix syntax)
    - examples/showcase.flow (lines 1-12 the key Cmajor block; line 29 the hand-written pad whose comment says "outline the I/IV/V/I shape" — the natural demo site; confirm it renders to a pinned WAV so you keep the demo OUT of the render)
  </read_first>
  <behavior>
    - `progression | I IV V I |` in `key Cmajor { }` compiles to a SequenceData with 4 bars.
    - The `:N` bar-count suffix (`| I:2 V |`) yields the right bar count.
    - The `voices N` modifier path (`progression voices N | … |`) parses + compiles.
    - The no-key error case: a bare `progression | I IV V |` with no active key reports ErrorCount > 0 and a stderr substring matching "progression requires an active key context".
    - Voice-leading determinism: the same progression compiled twice yields identical pitches (FindNearestPitchClass is deterministic).
  </behavior>
  <action>
    Create flow-lang.Tests/Unit/Phase46/ProgressionDslTests.cs mirroring the EuclideanSwingTests shell (`[Collection("FlowScripts")]`, file-scoped namespace FlowLang.Tests.Unit.Phase46). Use the RunProg helper from RESEARCH §D-12: `FlowEngineRunner.RunSource("use \"@std\"\nkey Cmajor {\n  Sequence s = <body>\n}\n")` then `r.GetVariable("s").As<SequenceData>()`, walking `seq.Bars[].MusicalNotes` with a rest-filter (copy HitNotes from EuclideanSwingTests). Add the 5 assertions from the behavior block. Probe pitches via MusicalNoteData on the compiled bars for the determinism check.

    Then extend examples/showcase.flow with Option A (safe, byte-identical — RESEARCH recommendation + Open Q1): ADD a non-rendered demonstration near line 29 (e.g. `Sequence progDemo = progression | I IV V I |` plus a `(print ...)` describing it) WITHOUT feeding progDemo into the rendered `section showcase` / `Song`. The showcase renders a pinned WAV — keeping progDemo out of the render graph preserves byte-identity of the showcase output. Do NOT replace the existing pad at line 29 (that is Option B, which would change rendered bytes — out of scope for this pure-cleanup phase).
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~ProgressionDsl" 2>&1 | tail -10 ; dotnet run --project flow-interpreter examples/showcase.flow 2>&1 | tail -3</automated>
  </verify>
  <done>
    ProgressionDslTests.cs compiles and is GREEN with all 5 assertions (4-bar output, :N suffix, voices N, no-key error, determinism); examples/showcase.flow runs clean with the new non-rendered progDemo line; the rendered showcase graph is unchanged (progDemo not fed into the Song → showcase WAV byte-identical).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add comment-only legacy notes to Track/Timeline + bars.flow (D-16)</name>
  <files>flow-lang/StandardLibrary/Audio/Timeline.cs, flow-lang/StandardLibrary/Audio/Track.cs, flow-lang/composition.flow, flow-lang/bars.flow, flow-lang/StandardLibrary/Bars.cs</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (§D-16 — the exact 5 locations + the suggested note wording for each)
    - flow-lang/StandardLibrary/Audio/Timeline.cs (lines 7-9 — the /// summary on Timeline)
    - flow-lang/StandardLibrary/Audio/Track.cs (the class header)
    - flow-lang/composition.flow (lines 1-5 — file header above the Track wrappers at 68-150)
    - flow-lang/bars.flow (lines 1-2 — file header)
    - flow-lang/StandardLibrary/Bars.cs (lines 5-7 — the /// summary on Bars)
  </read_first>
  <action>
    Add a one-to-two-line COMMENT-ONLY "legacy / superseded by X — kept as a usable surface" note at each of the 5 locations (RESEARCH §D-16 table). Wording per RESEARCH:
    - Timeline.cs (/// summary): "Legacy DAW-style multitrack layer (pre-Phase-25). Superseded by the Song/Section render path (SongRenderer) as the canonical arrangement primitive — kept as a usable lower-level manual-mixing surface (shares the Voice type)."
    - Track.cs (class header): same framing, cross-ref Song/Section.
    - composition.flow (file header, above the Track wrappers): "Legacy fluent Track wrappers — superseded by Song/Section; kept as a usable surface."
    - bars.flow (file header): "Legacy bar/measure-construction API. Superseded by the | C4 D4 E4 | note-stream literal syntax (NoteStreamCompiler) — kept as a usable measure-construction surface. Orthogonal to Phase 45 Beats (measure axis, not duration). Imported by std.flow:6."
    - Bars.cs (/// summary): same framing as bars.flow.
    CRITICAL: comment-only. NO [Deprecated]/[Obsolete] attributes, NO stderr advisory, NO runtime warning (D-16). Do NOT touch the std.flow:6 `use "@bars"` import (D-11 keeps bars.flow).
  </action>
  <verify>
    <automated>grep -ln "[Ll]egacy" flow-lang/StandardLibrary/Audio/Timeline.cs flow-lang/StandardLibrary/Audio/Track.cs flow-lang/composition.flow flow-lang/bars.flow flow-lang/StandardLibrary/Bars.cs ; grep -rn "Obsolete\|Deprecated" flow-lang/bars.flow flow-lang/StandardLibrary/Audio/Timeline.cs flow-lang/StandardLibrary/Audio/Track.cs ; grep -n 'use "@bars"' flow-lang/std.flow ; dotnet build flow-lang/flow-lang.csproj 2>&1 | tail -3</automated>
  </verify>
  <done>
    All 5 surfaces carry a comment-only legacy note (grep finds "legacy" in each); NO `[Obsolete]`/`[Deprecated]`/stderr-advisory added; `std.flow:6 use "@bars"` retained; `dotnet build` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adding a test + a non-rendered demo line + comment-only notes introduces no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05 | Tampering | showcase render regression | mitigate | The Progression demo is added NON-rendered (kept out of the Song graph), so the pinned showcase WAV stays byte-identical; D-16 is comment-only. Full suite + two-run cmp-clean confirm. Correctness control, not security. |

**Honest statement:** No new attack surface. This plan ADDS a unit test, a non-rendered demo line, and comment-only documentation — no production logic changes. Sole risk class is an accidental showcase render-graph change, avoided by keeping the demo out of the render and covered by the determinism gate. No new input/network/file/parse surface. No package installs (T-46-SC N/A).
</threat_model>

<verification>
- D-12: ProgressionDslTests GREEN (5 assertions); showcase.flow runs clean; showcase WAV byte-identical (demo non-rendered).
- D-16: 5 comment-only legacy notes present; no Obsolete/Deprecated/advisory; @bars import retained; build green.
- Anti-scope KEEPs (no code change, per D-01): D-10 (Track/Timeline), D-11 (bars.flow), D-13 (OscillatorState/Envelope), D-14 (audio.flow buffer convenience layer), D-15 (`preview` builtin) are all UNTOUCHED by removal; D-17 phase breadth = audit §1+§2 filtered by D-01, §3 OUT of scope. Confirm no task in any phase-46 plan removes these surfaces.
- Full `dotnet test` + two-run cmp-clean unaffected.
</verification>

<success_criteria>
- Progression DSL gains real unit coverage + a showcase demo without changing rendered bytes.
- All 5 kept-surface legacy notes added (comment-only); build + full suite green.
- Anti-scope KEEPs recorded (per D-01): D-10, D-11, D-13, D-14, D-15 surfaces deliberately not removed; D-17 breadth honored (§3 out of scope).
</success_criteria>

<output>
Create `.planning/phases/46-codebase-bloat-removal/46-05-SUMMARY.md` when done. Commit each target atomically (D-18): `test(46): add Progression DSL unit tests + showcase demo (D-12)` and `docs(46): add legacy keep-notes to Track/Timeline + bars (D-16)` — two commits.
</output>
