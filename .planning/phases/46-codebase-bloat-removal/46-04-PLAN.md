---
phase: 46-codebase-bloat-removal
plan: 04
type: execute
wave: 2
depends_on: ["46-03"]
files_modified:
  - flow-lang/StandardLibrary/BuiltInFunctions.cs
  - flow-lang/audio.flow
  - flow-lang/StandardLibrary/Audio/FileIO.cs
  - flow-lang/test.flow
  - flow-lang.Tests/FlowScriptData.cs
  - tests/test_full_song.flow
  - tests/demo_feature_showcase.flow
  - tests/test_section_bare_expr.flow
  - tests/test_section_gain_bare_expr.flow
  - tests/test_wav_loading.flow
  - tests/test_writewav.flow
  - tests/test_test_library.flow
autonomous: true
requirements: [CLEAN-06, CLEAN-07]
nyquist_compliant: true

must_haves:
  truths:
    - "D-06: the `exportWav` legacy alias is removed (registration + audio.flow internal decls + FileIO.cs ExportWav/ExportWavWithBitDepth shims); all 7 callers migrated to path-first `writeWav` (arg-order swapped); `writeWav`/`WriteWavWithBitDepth`/the shared `ExportWavInternal` core are KEPT"
    - "D-06: the FlowScriptData expected-output map for test_writewav.flow is updated in lockstep with the rewritten file (no pinned exportWav-compat substring left dangling)"
    - "D-07: the legacy pure-Flow assertion half of test.flow (lines 30-136) is removed; the @test module surface (lines 1-29) is KEPT; the sole consumer test_test_library.flow is ported to the @test surface with FAIL-cases inverted via (assert (not …))"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/FileIO.cs"
      provides: "FileIO with exportWav shims removed; WriteWav/WriteWavWithBitDepth/ExportWavInternal core retained"
      contains: "ExportWavInternal"
    - path: "tests/test_test_library.flow"
      provides: "Ported to @test surface (use \"@test\" + (test name lazy(...)) + sentinel)"
      contains: "use \"@test\""
  key_links:
    - from: "migrated .flow callers"
      to: "writeWav (path-first)"
      via: "arg-order swap"
      pattern: "writeWav"
    - from: "tests/test_test_library.flow"
      to: "@test assert/assertEq + not builtin"
      via: "FAIL-case inversion"
      pattern: "assert \\(not"
---

<objective>
Two caller-migration removals bundled (both rewrite `.flow` callers + their FlowScriptData pins). Placed in Wave 2 (depends on 46-03) because both this plan and 46-03 edit `flow-lang/audio.flow` at non-overlapping regions — sequencing them preserves the strict same-wave zero-file-overlap invariant.

1. **D-06 (CLEAN-06):** Remove the `exportWav` legacy alias — pure reversed-arg redundancy of canonical `writeWav` (D-01(b), strictly-better equivalent exists). Migrate all 7 callers (arg-order swap), drop the `ExportWav`/`ExportWavWithBitDepth` shims, KEEP `WriteWav`/`WriteWavWithBitDepth` + the shared private `ExportWavInternal` core.
2. **D-07 (CLEAN-07):** Remove the legacy pure-Flow assertion half of `test.flow` (lines 30-136), superseded by the Phase 35 `@test` module. KEEP the `@test` surface (1-29). Port the sole consumer `tests/test_test_library.flow` to `@test`, inverting its deliberate FAIL-cases via `(assert (not …))` (since `@test` THROWS on failure, unlike the legacy Bool-returning asserts).

Purpose: Remove redundancy where a strictly-better equivalent already exists (D-01(b)).
Output: exportWav alias gone + callers on writeWav; test.flow legacy half gone + consumer ported; FlowScriptData expected-output map consistent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-codebase-bloat-removal/46-RESEARCH.md
@.planning/phases/46-codebase-bloat-removal/46-03-SUMMARY.md

<interfaces>
<!-- D-06 (verified live) -->
exportWav(buffer, filepath[, bitDepth]) — buffer-FIRST (FileIO.cs:32-33)
writeWav(filepath, buffer[, bitDepth])  — path-FIRST  (FileIO.cs:274-275)  -- migration target
  Migration = swap the two args. All 7 callers are 2-arg (no 3-arg bitDepth caller in repo).
KEEP: WriteWav (FileIO.cs:272-278), WriteWavWithBitDepth (283-290), private ExportWavInternal (55+)

<!-- D-07 (verified live) -->
@test surface KEEP (test.flow:1-29): module test + 6 internal proc decls (test/assert/assertEq/assertNotesMatch/assertBytesEqual/assertWithinDb)
Phase 35 @test asserts THROW AssertionException on failure (TestFunctions.cs:60-115)
`not` IS registered (BuiltInFunctions.cs:1139) — the legacy file's "not unavailable" comment is STALE
Reference port target: tests/test_test_framework.flow (use "@test" + (test "name" lazy(body)) + sentinel)

<!-- NOTE: 46-03 already removed the createSineTone internal decls from audio.flow before this plan runs.
     This plan touches DIFFERENT audio.flow lines (the exportWav internal proc decls ~:38, ~:41).
     Re-locate by signature, not raw line number. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove exportWav alias + migrate all 7 callers to writeWav (D-06)</name>
  <files>flow-lang/StandardLibrary/BuiltInFunctions.cs, flow-lang/audio.flow, flow-lang/StandardLibrary/Audio/FileIO.cs, flow-lang.Tests/FlowScriptData.cs, tests/test_full_song.flow, tests/demo_feature_showcase.flow, tests/test_section_bare_expr.flow, tests/test_section_gain_bare_expr.flow, tests/test_wav_loading.flow, tests/test_writewav.flow</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (section D-06 — registration/shim lines, the complete caller table, the arg-order note, and the special test_writewav.flow + FlowScriptData handling)
    - flow-lang/StandardLibrary/BuiltInFunctions.cs (lines 709-721 — the 2 exportWav registrations to delete)
    - flow-lang/audio.flow (lines 38, 41 — the 2 internal proc decls to delete; re-locate by signature, 46-03 may have shifted lines)
    - flow-lang/StandardLibrary/Audio/FileIO.cs (lines 30-50 ExportWav/ExportWavWithBitDepth shims to delete; 55+ ExportWavInternal, 272-290 WriteWav/WriteWavWithBitDepth to KEEP)
    - flow-lang.Tests/FlowScriptData.cs (lines 30-35 and 119-126 — the per-script expected-output map; the test_writewav.flow pinned substring)
    - tests/test_writewav.flow (whole file — its PURPOSE is the alias; lines 1,12,13,14,23 reference exportWav)
  </read_first>
  <action>
    Per RESEARCH section D-06 (re-locate by signature/content, not raw line, as deletions shift lines):
    1. Delete the 2 registrations in BuiltInFunctions.cs (~709-714 exportWav(Buffer,String) to ExportWav; ~716-721 exportWav(Buffer,String,Int) to ExportWavWithBitDepth).
    2. Delete the 2 internal proc decls in audio.flow (~:38, ~:41 — the exportWav decls, distinct from the createSineTone decls 46-03 removed).
    3. Delete the ExportWav (~30-37) and ExportWavWithBitDepth (~42-50) shims in FileIO.cs. KEEP WriteWav/WriteWavWithBitDepth/the shared private ExportWavInternal — do NOT rename ExportWavInternal (it is the shared core, not the alias).
    4. Migrate the 6 straightforward callers by swapping the 2 args (buffer-first to path-first):
       - tests/test_full_song.flow:159 (exportWav mixed outPath) to (writeWav outPath mixed)
       - tests/demo_feature_showcase.flow:237 to (writeWav "feature_showcase.wav" final)
       - tests/test_section_bare_expr.flow:20 swap
       - tests/test_section_gain_bare_expr.flow:34,35 swap each (x2)
       - tests/test_wav_loading.flow:6 swap
    5. SPECIAL: tests/test_writewav.flow is literally titled to test the alias and prints "PASS: exportWav(Buffer, String) backwards compat succeeded". Rewrite it to test ONLY writeWav (path-first) — drop the exportWav half + its PASS line + the title/comment references at lines 1,12,14,23. Then update FlowScriptData.cs:119-126 expected-output map to match the rewritten output (remove the pinned exportWav-compat substring) — Pitfall 3: the auto-discovered Fact fails if the file output changes without updating the map.
    Final grep for exportWav across flow-lang/flow-lang.Tests/tests/examples should be zero (ExportWavInternal excluded).
  </action>
  <verify>
    <automated>grep -rn "exportWav" flow-lang flow-lang.Tests tests examples ; dotnet build flow-lang/flow-lang.csproj 2>&1 | tail -3 ; for t in tests/test_full_song.flow tests/test_wav_loading.flow tests/test_writewav.flow; do dotnet run --project flow-interpreter "$t" 2>&1 | tail -1; done</automated>
  </verify>
  <done>
    grep for `exportWav` (case-sensitive, ExportWavInternal excluded) across flow-lang/flow-lang.Tests/tests/examples returns ZERO; FileIO.cs ExportWav/ExportWavWithBitDepth shims gone; WriteWav/WriteWavWithBitDepth/ExportWavInternal retained; all 7 migrated `.flow` scripts exit 0 and print their PASS sentinels; FlowScriptData WriteWav + FlowScript Facts green; `dotnet build` green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove test.flow legacy assertion half + port test_test_library.flow to @test (D-07)</name>
  <files>flow-lang/test.flow, tests/test_test_library.flow, flow-lang.Tests/FlowScriptData.cs</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (section D-07 — the KEEP 1-29 / DELETE 30-136 split, the FULL port-semantics list, and the FAIL-case-inversion recipe + the execution-model note)
    - flow-lang/test.flow (lines 1-29 the @test surface to KEEP; lines 30-136 the legacy half to DELETE incl the use "@std"/"@collections" at 34-35)
    - tests/test_test_library.flow (whole 46-line file — the sole consumer; note its 9 deliberate FAIL cases)
    - tests/test_test_framework.flow (the canonical @test port target to mirror — use "@test" + (test "name" lazy(body)) + sentinel)
    - flow-lang/StandardLibrary/TestFramework/TestFunctions.cs (lines 60-115 — confirm assert/assertEq THROW on failure, which is WHY FAIL-cases must be inverted)
    - flow-lang.Tests/FlowScriptData.cs (check whether test_test_library.flow has a pinned expected-output entry to update)
  </read_first>
  <action>
    Per RESEARCH section D-07:
    1. In flow-lang/test.flow: KEEP lines 1-29 (module test + the 6 internal proc decls). DELETE lines 30-136 — the legacy pure-Flow library: use "@std"/"@collections" (34-35), notBool (41-43), printResult (51-56), assertTrue/assertFalse/assertEqual/assertNotEqual/assertLess/assertGreater/assertLessOrEqual/assertGreaterOrEqual (61-98), assertApproxEqual (101-105), runTest (117-122), summary (127-136). Delete the two `use` imports too (they are used only by the legacy half; the @test surface needs no imports). Re-locate by content, not raw line.
    2. Port tests/test_test_library.flow to the @test surface, mirroring tests/test_test_framework.flow:
       - Add `use "@test"` at the top.
       - Wrap each test body in `(test "name" lazy(...))`.
       - Convert each negative/FAIL case to a POSITIVE assertion of the negation using the registered `not` builtin (RESEARCH Pitfall 5 — `not` IS registered; the legacy comment is stale). Examples:
         (assertTrue false ...) to (test "assertTrue FAIL case" lazy((assert (not false))))
         (assertEqual 2 3 ...) to (test "assertEq int FAIL case" lazy((assert (not (equals 2 3)))))
         (assertApproxEqual 1.0 1.5 0.001 ...) to assert (gt (abs (sub 1.0 1.5)) 0.001) is true.
       - Drop runTest/summary (no @test analog needed). End the file with a sentinel `(print "ALL TESTS REGISTERED")` mirroring tests/test_test_framework.flow:36.
    3. If FlowScriptData.cs has a pinned expected-output entry for test_test_library.flow, update it to match the new sentinel output.
    Validation nuance (RESEARCH): the auto-suite runs the file via FlowEngineRunner.RunFile which only REGISTERS `(test ...)` bodies (does not force them) — so the file passes the auto-suite as long as it returns success + prints the sentinel. The separate verify below runs it through `flow-cli test` to actually EXECUTE the assertions.
  </action>
  <verify>
    <automated>grep -n "assertTrue\|assertEqual\|runTest\|summary\|notBool\|printResult" flow-lang/test.flow ; grep -n "module test\|proc assert\b\|proc assertEq" flow-lang/test.flow ; dotnet run --project flow-interpreter tests/test_test_library.flow 2>&1 | tail -2 ; dotnet run --project flow-cli -- test tests/test_test_library.flow 2>&1 | tail -5</automated>
  </verify>
  <done>
    test.flow lines 30-136 legacy half removed (no assertTrue/assertEqual/runTest/summary/notBool/printResult remain); the @test surface (module test + assert/assertEq/...) retained; tests/test_test_library.flow ported to @test, exits 0 via flow-interpreter AND every assertion PASSES under `flow-cli test`; FlowScriptData map consistent if it pinned this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Removing an alias + porting a test stdlib to its canonical equivalent introduces no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering | WAV export / test-suite regression | mitigate | D-06 migrates to the identical writeWav core (ExportWavInternal unchanged); D-07 ports to the canonical @test surface. Full suite + flow-cli test execution prove no behavior loss. Correctness control, not security. |

**Honest statement:** No new attack surface. D-06 removes a reversed-arg alias of an existing export path; D-07 removes a legacy Flow assertion library superseded by the @test module. Sole risk class is accidental behavior change (a missed caller or an inverted-assertion error), covered by the test-green gate + the `flow-cli test` execution check. No new input/network/file/parse surface. No package installs (T-46-SC N/A).
</threat_model>

<verification>
- D-06: zero `exportWav` references (ExportWavInternal excluded); all 7 callers on writeWav; FlowScriptData consistent; build + WriteWav/FlowScript Facts green.
- D-07: test.flow legacy half gone, @test surface kept; ported consumer registers + PASSES under `flow-cli test`.
- Full `dotnet test` + the migrated/ported `.flow` scripts green; two-run cmp-clean unaffected (export core unchanged).
</verification>

<success_criteria>
- exportWav alias removed, callers migrated, expected-output map consistent.
- test.flow legacy half removed, @test surface intact, sole consumer ported and passing.
- Build + full suite green; zero behavior change to the retained export/test cores.
</success_criteria>

<output>
Create `.planning/phases/46-codebase-bloat-removal/46-04-SUMMARY.md` when done. Commit each target atomically (D-18): `refactor(46): remove exportWav legacy alias, migrate callers to writeWav (D-06)` and `refactor(46): remove test.flow legacy assertion half, port consumer to @test (D-07)` — two commits.
</output>
