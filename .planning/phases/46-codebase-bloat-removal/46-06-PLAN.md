---
phase: 46-codebase-bloat-removal
plan: 06
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs
autonomous: true
requirements: [CLEAN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "D-03: the 4x private BeatsToSeconds+CreateSilence helpers in NoteSynthesizer.cs are redirected to SynthUtils.BeatsToSeconds/SynthUtils.CreateSilence (byte-identical by inspection — SAFE half, always done)"
    - "D-03: the inline oscillator loops are EITHER redirected to SynthUtils.Generate* and PROVEN bit-identical by the Wave 0 exact-byte guard, OR — if the guard goes red — kept inline (per the documented fallback) because SynthUtils incremental-phase accumulation diverges in IEEE-754; the fallback branch is an explicit acceptance criterion, not a silent assumption"
    - "FlowFunctionSynthesizer (NoteSynthesizer.cs:188-208) is NOT touched (D-deferred section 2.6)"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs"
      provides: "NoteSynthesizer with the 8 private helpers removed (redirected to SynthUtils); oscillator math byte-identical (redirected or retained-inline per the guard)"
      contains: "SynthUtils"
  key_links:
    - from: "Sine/Saw/Square/Triangle RenderNote"
      to: "SynthUtils.BeatsToSeconds / SynthUtils.CreateSilence"
      via: "redirect (safe half)"
      pattern: "SynthUtils\\.(BeatsToSeconds|CreateSilence)"
    - from: "NoteSynthesizerByteGuardTests (Wave 0)"
      to: "post-redirect RenderNote output"
      via: "exact Assert.Equal(float[]) — GREEN gates the redirect; RED triggers the inline fallback"
      pattern: "Assert.Equal"
---

<objective>
D-03 (CLEAN-03): Redirect NoteSynthesizer's 4x private duplicate helpers (BeatsToSeconds+CreateSilence + the inline oscillator loops) to the existing SynthUtils.* — these generators are ALSO composer-callable builtins, so oscillator math MUST stay byte-identical.

This is the ONE byte-divergence risk in the phase: the inline loops recompute Math.Sin(2*pi*f*t) / (f*t)%1.0 fresh per sample, whereas SynthUtils.Generate* use incremental phase accumulation (phase += inc; wrap) — equal in exact arithmetic, NOT guaranteed bit-identical in IEEE-754 double (plus/minus 1-ULP drift produces different WAV bytes).

Depends on Plan 46-01's Wave 0 exact-byte guard (NoteSynthesizerByteGuardTests), which captured the pre-redirect oscillator output. This plan runs the redirect, then re-runs that guard:
- Guard GREEN -> oscillator redirect is bit-identical -> keep the full redirect.
- Guard RED -> divergence confirmed -> take the DOCUMENTED FALLBACK: redirect ONLY the safe BeatsToSeconds+CreateSilence halves, KEEP the inline oscillator loops (they ARE the byte contract). Still removes 8 helpers, zero risk.

Purpose: Remove duplicate internal plumbing (D-01) without breaking the byte contract of composer-callable synthesis.
Output: NoteSynthesizer.cs with the redundant helpers removed; the exact-byte guard GREEN either way (full redirect proven, or fallback taken).

Atomic commit (D-18): one self-contained target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-codebase-bloat-removal/46-RESEARCH.md
@.planning/phases/46-codebase-bloat-removal/46-01-SUMMARY.md

<interfaces>
<!-- SynthUtils redirect targets (verified signatures, flow-lang/StandardLibrary/Audio/SynthUtils.cs) -->
SynthUtils.BeatsToSeconds(double beats, double bpm)  (:32 — IDENTICAL body to the private ones -> SAFE)
SynthUtils.CreateSilence(int sampleRate, double durationBeats, double bpm)  (:40 — IDENTICAL body -> SAFE)
SynthUtils.GenerateSine(float[] buffer, double freq, double amp, int sr, double startPhase=0.0)  (:50 — ADDITIVE +=, incremental phase)
SynthUtils.GenerateSaw(...)  (:67)   SynthUtils.GenerateSquare(...)  (:85)   SynthUtils.GenerateTriangle(...)  (:103)
SynthUtils.ToMonoBuffer(float[] samples, int sampleRate)  (:248 — wraps a fresh float[] into a 1-channel AudioBuffer)

<!-- The redirect shape for the oscillator half (write into a fresh zero-init buffer; Generate* is additive) -->
var samples = new float[numSamples];
SynthUtils.GenerateSine(samples, frequency, amplitude, sampleRate);   // startPhase defaults 0.0
return SynthUtils.ToMonoBuffer(samples, sampleRate);

<!-- NoteSynthesizer.cs structure (verified): per-class RenderNote / inline osc loop / private BeatsToSeconds / private CreateSilence -->
SineSynthesizer 26-58 | SawSynthesizer 66-99 | SquareSynthesizer 107-140 | TriangleSynthesizer 148-181
DO NOT TOUCH: FlowFunctionSynthesizer 188-208 (D-deferred section 2.6)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redirect NoteSynthesizer helpers to SynthUtils, gated by the Wave 0 byte guard (D-03)</name>
  <files>flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (section D-03 — the structure table, the SAFE-half vs byte-risk split, the redirect shape, and BOTH discharge options incl. the fallback)
    - .planning/phases/46-codebase-bloat-removal/46-01-SUMMARY.md (the exact baseline-capture method the Wave 0 guard measures against — so you know what bit-identical means here)
    - flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs (lines 24-181 — the 4 synth classes; lines 188-208 FlowFunctionSynthesizer to NOT touch)
    - flow-lang/StandardLibrary/Audio/SynthUtils.cs (lines 32, 40, 50-116, 248 — the redirect targets + the incremental-phase accumulation that creates the byte risk)
    - flow-lang.Tests/Unit/Phase46/NoteSynthesizerByteGuardTests.cs (the guard this redirect must keep GREEN)
  </read_first>
  <behavior>
    - SAFE half (always): each of the 4 synths private BeatsToSeconds/CreateSilence is replaced by a call to SynthUtils.BeatsToSeconds/SynthUtils.CreateSilence (byte-identical by inspection) and the 8 private helper bodies are deleted.
    - Oscillator half (conditional on the guard): redirect to SynthUtils.Generate* using the fresh-zero-init-buffer + ToMonoBuffer shape, THEN re-run NoteSynthesizerByteGuardTests:
      - If GREEN -> keep the full oscillator redirect.
      - If RED -> revert the oscillator redirect only; keep the inline (f*t)%1.0 / Math.Sin(2*pi*f*t) loops (the byte contract). The SAFE BeatsToSeconds/CreateSilence redirect STAYS regardless.
    - FlowFunctionSynthesizer (188-208) unchanged.
  </behavior>
  <action>
    Step 1 (SAFE — always): For each of SineSynthesizer/SawSynthesizer/SquareSynthesizer/TriangleSynthesizer, replace the body's calls to the private BeatsToSeconds/CreateSilence with SynthUtils.BeatsToSeconds/SynthUtils.CreateSilence, then DELETE all 8 private helper methods. RESEARCH confirms these bodies are character-for-character identical to SynthUtils -> zero risk.

    Step 2 (oscillator redirect — attempt): Replace each synth's inline oscillator loop with the additive SynthUtils shape from the interfaces block (new float[numSamples] -> SynthUtils.GenerateSine/Saw/Square/Triangle(samples, frequency, amplitude, sampleRate) -> SynthUtils.ToMonoBuffer(samples, sampleRate)). Match each synth to its generator. Do NOT touch FlowFunctionSynthesizer (188-208).

    Step 3 (GATE): Run dotnet test --filter NoteSynthesizerByteGuard.
      - GREEN -> done; full redirect kept.
      - RED -> take the FALLBACK: revert ONLY the Step-2 oscillator changes (restore the inline loops verbatim) while KEEPING the Step-1 BeatsToSeconds/CreateSilence redirect. Document in the SUMMARY that the oscillator loops stay inline because SynthUtils incremental-phase accumulation diverges in IEEE-754 (RESEARCH section D-03 + Open Q2). Do NOT force an RMS-only regression for a pure-cleanup phase.

    Record which branch was taken in the SUMMARY (full redirect vs safe-half + inline-retained fallback).
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj 2>&1 | tail -3 ; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~NoteSynthesizerByteGuard" 2>&1 | tail -10 ; grep -c "private.*BeatsToSeconds\|private.*CreateSilence" flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs</automated>
  </verify>
  <done>
    NoteSynthesizer.cs has ZERO private BeatsToSeconds/CreateSilence helpers (all 8 redirected to SynthUtils); FlowFunctionSynthesizer (188-208) untouched; the NoteSynthesizerByteGuardTests guard is GREEN — either because the full oscillator redirect is bit-identical, OR because the fallback (inline loops retained, safe half redirected) was taken and the SUMMARY records which branch + why; dotnet build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Redirecting synth helpers to an existing already-tested code path introduces no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06 | Tampering | composer-callable synthesis byte contract | mitigate | The Wave 0 exact-byte guard (Plan 46-01) gates the redirect; RED triggers the documented inline-retention fallback. This is a CORRECTNESS control (the ±1-ULP byte contract for generateSine/etc.), not a security control. |

**Honest statement:** No new attack surface. This redirects duplicate internal synth helpers to the existing SynthUtils path. The sole risk class is the ±1-ULP IEEE-754 byte divergence of the composer-callable oscillators, mitigated by the exact-byte guard + the explicit fallback branch — a correctness control. No new input/network/file/parse surface. No package installs (T-46-SC N/A).
</threat_model>

<verification>
- NoteSynthesizer.cs has zero private BeatsToSeconds/CreateSilence; FlowFunctionSynthesizer untouched.
- NoteSynthesizerByteGuardTests GREEN (full redirect proven bit-identical OR fallback taken).
- Phase 28 RMS baselines within +/- 0.5 dB / 100 ms; two-run cmp-clean holds; full dotnet test green.
</verification>

<success_criteria>
- The 8 duplicate helpers removed; oscillator byte contract preserved (proven or fallback-retained); build + full suite + RMS baselines + two-run cmp-clean all green.
</success_criteria>

<output>
Create `.planning/phases/46-codebase-bloat-removal/46-06-SUMMARY.md` when done — explicitly state which D-03 branch was taken (full redirect vs safe-half fallback) and the guard result. Commit atomically (D-18): `refactor(46): redirect NoteSynthesizer helpers to SynthUtils (D-03)`.
</output>
