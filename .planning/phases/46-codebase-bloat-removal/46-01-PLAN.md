---
phase: 46-codebase-bloat-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang.Tests/Unit/Phase46/NoteSynthesizerByteGuardTests.cs
  - flow-lang.Tests/baselines/Phase46/.gitkeep
autonomous: true
requirements: [CLEAN-04, CLEAN-09, CLEAN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "D-04: no capital-F `Fixtures/` path strings or git paths remain in flow-lang.Tests (verification — merge already shipped Phase 44 e0d7274)"
    - "D-09: the Phase35 diagnostics .txt baselines are KEPT, with a recorded rationale that the confirm-gate FAILED (golden test live-reads them)"
    - "D-03 (prerequisite): an exact `Assert.Equal(float[])` byte guard captures pre-redirect oscillator output for all 4 primitive synths, so the Wave 2 redirect can be proven bit-identical or its divergence detected"
  artifacts:
    - path: "flow-lang.Tests/Unit/Phase46/NoteSynthesizerByteGuardTests.cs"
      provides: "Exact-byte guard for Sine/Saw/Square/Triangle RenderNote output (captured from current/pre-redirect dev build)"
      contains: "Assert.Equal"
      min_lines: 40
  key_links:
    - from: "NoteSynthesizerByteGuardTests.cs"
      to: "SynthesizerFactory.Create(\"sine\"|\"saw\"|\"square\"|\"triangle\").RenderNote"
      via: "factory dispatch + exact float[] compare"
      pattern: "SynthesizerFactory\\.Create"
---

<objective>
Wave 0 prerequisites + cheap verifications. Three jobs, none of which mutate production code:

1. **D-04 (CLEAN-04):** Confirm the `Fixtures/`→`fixtures/` merge already shipped (Phase 44 commit `e0d7274`, squashed into `5f61a1e`). The audit + STATE.md:809 are STALE on this point. This is a verification task, NOT a `git mv`.
2. **D-09 (CLEAN-09):** Confirm the Phase35 diagnostics `.txt` baselines are LIVE-READ by `DiagnosticRendererGoldenTests`. Per D-09's own escape clause ("REMOVE, but only after confirming the golden test does NOT read the .txt files"), the condition is UNMET → KEEP. Record the failed-condition rationale so the decision-coverage gate sees D-09 addressed. NO code change.
3. **D-03 prerequisite (CLEAN-03):** Capture an exact-byte guard from the CURRENT (pre-redirect) `dev` build for all 4 primitive synth oscillator outputs. This Fact MUST exist and be GREEN before the Wave 2 D-03 redirect runs, so the redirect can be proven bit-identical (or its IEEE-754 ±1-ULP divergence detected → fallback branch).

Purpose: De-risk the phase. The two "highest priority" audit items (D-04 latent FS bug, D-09 orphan baselines) are already-resolved / must-keep — this plan converts them to green checkmarks. The D-03 guard is the safety net the locked test-green gate cannot provide on its own (RMS tolerance + same-code two-run determinism cannot catch a before-vs-after ±1-ULP shift).
Output: New `NoteSynthesizerByteGuardTests.cs` (GREEN against current code); recorded D-04/D-09 verification notes in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-codebase-bloat-removal/46-RESEARCH.md
@.planning/phases/46-codebase-bloat-removal/46-CONTEXT.md

<interfaces>
<!-- From flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs (verified live) -->
INoteSynthesizer.RenderNote(MusicalNoteData note, int sampleRate, double durationBeats, double bpm, RenderTuning tuning) → AudioBuffer  (:18)
public static class SynthesizerFactory  (:214)  — Create(string name) dispatch to Sine/Saw/Square/Triangle/FlowFunction synths
SineSynthesizer (:24) / SawSynthesizer (:64) / SquareSynthesizer (:105) / TriangleSynthesizer (:146)

<!-- From flow-lang.Tests/fixtures/FlowEngineRunner.cs (verified live) -->
RunSource(string source, string fileName="<test>") → (bool Success, string Stdout, string Stderr, int ErrorCount)  (:31)
GetVariable(string name) → Value  (:45)

<!-- D-09 evidence (verified live) -->
flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs:39 File.ReadAllText(path)
  :77 ReadBaseline("unknown_identifier.txt"); :116 ReadBaseline("type_mismatch.txt")
flow-lang.Tests.csproj:95-102 live CopyToOutputDirectory rule for baselines\Phase35\diagnostics\*.txt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify D-04 Fixtures merge already shipped + D-09 diagnostics keep-rationale</name>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (§D-04, §D-09 — exact correction evidence)
    - flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs (lines 25, 34-39, 77, 116 — confirm File.ReadAllText of the .txt baselines)
    - flow-lang.Tests/flow-lang.Tests.csproj (lines 95-102 — confirm the live CopyToOutputDirectory rule)
  </read_first>
  <action>
    Pure verification — NO file edits. Run and record the evidence for two decisions:

    D-04 (CLEAN-04): Confirm the Phase-44 Fixtures merge holds. Run:
      `grep -rn '"Fixtures"\|"Fixtures/\|/Fixtures/' --include="*.cs" flow-lang.Tests` (expect EMPTY)
      `git ls-files | grep 'flow-lang.Tests/Fixtures/'` (expect EMPTY)
      `test ! -d flow-lang.Tests/Fixtures && test -d flow-lang.Tests/fixtures` (expect success)
    Note in the SUMMARY: D-04 is a no-op (already merged in e0d7274); the lone residual `flow-midi.Tests/Fixtures/` is a DIFFERENT project with no sibling lowercase dir → no collision → out of scope per RESEARCH §D-04 + A2.

    D-09 (CLEAN-09): Confirm the confirm-gate FAILS → KEEP. Run:
      `grep -n 'File.ReadAllText\|ReadBaseline\|unknown_identifier.txt\|type_mismatch.txt' flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs`
    Confirm :39 File.ReadAllText, :77/:116 read the .txt baselines, and the .csproj copies them. Record in the SUMMARY: per CONTEXT D-09's own condition the .txt files are LIVE-READ by two `[Fact]`s, so removal does NOT proceed — D-09 is KEPT, zero commit. Do NOT delete any file under flow-lang.Tests/baselines/Phase35/diagnostics/.
  </action>
  <verify>
    <automated>grep -rn '"Fixtures"\|/Fixtures/' --include="*.cs" flow-lang.Tests | grep -v 'fixtures' ; test -z "$(git ls-files | grep 'flow-lang.Tests/Fixtures/')" && echo D04_OK ; grep -q 'File.ReadAllText' flow-lang.Tests/Phase35/DiagnosticRendererGoldenTests.cs && echo D09_LIVEREAD_CONFIRMED</automated>
  </verify>
  <done>
    grep for capital-`Fixtures/` in flow-lang.Tests C# returns empty; no `flow-lang.Tests/Fixtures/` git path; `DiagnosticRendererGoldenTests.cs` confirmed to `File.ReadAllText` the .txt baselines (D-09 KEEP rationale recorded). No files changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact-byte guard for the 4 primitive synth oscillators (D-03 prerequisite)</name>
  <files>flow-lang.Tests/Unit/Phase46/NoteSynthesizerByteGuardTests.cs, flow-lang.Tests/baselines/Phase46/.gitkeep</files>
  <read_first>
    - flow-lang/StandardLibrary/Audio/NoteSynthesizer.cs (lines 18, 24-181, 214 — INoteSynthesizer.RenderNote signature, the 4 synth classes, SynthesizerFactory.Create dispatch)
    - flow-lang/StandardLibrary/Audio/SynthUtils.cs (lines 32, 40, 50-116, 248 — the redirect targets the Wave 2 task will route through; capture WHAT the current bytes are BEFORE that change)
    - flow-lang.Tests/Unit/Phase15/EuclideanSwingTests.cs (idiom for [Collection("FlowScripts")] xUnit class + AudioBuffer/float[] inspection)
    - flow-lang.Tests/baselines/Phase28/ (baseline-storage convention — captured arrays committed because RNG is deterministic; these synth paths have NO dither so are deterministic by construction)
  </read_first>
  <behavior>
    - Test sine: SynthesizerFactory.Create("sine").RenderNote(note, sampleRate=44100, durationBeats=1.0, bpm=120, RenderTuning default) → the resulting AudioBuffer's float[] EXACTLY equals a committed baseline captured from the CURRENT (pre-redirect) build.
    - Test saw / square / triangle: same shape, one Fact (or [Theory] row) each.
    - Use a fixed, non-trivial MusicalNoteData (e.g. a mid-register pitch like A4 / MIDI 69) and fixed sampleRate/bpm so the captured arrays are stable.
    - Assertion is EXACT element-wise float equality (Assert.Equal on float[] or per-element bit compare), NOT RMS tolerance — the whole point is to catch a ±1-ULP IEEE-754 shift that RMS would miss.
    - GREEN against current code (this captures the byte contract). Wave 2's D-03 redirect re-runs THIS test; if it goes red the redirect diverged → fallback branch.
  </behavior>
  <action>
    Create `flow-lang.Tests/Unit/Phase46/NoteSynthesizerByteGuardTests.cs` mirroring the `EuclideanSwingTests` class shell (`[Collection("FlowScripts")]`, file-scoped namespace `FlowLang.Tests.Unit.Phase46`, .NET 10 / C# 13). For each of the 4 primitive synths: obtain the synth via `SynthesizerFactory.Create(name)`, call `RenderNote` with the fixed inputs above, extract the mono `float[]` from the returned `AudioBuffer`, and `Assert.Equal` it against a baseline.

    Baseline strategy: capture the current bytes IN-TEST on first authorship — the simplest reliable form is a self-contained golden array embedded in the test (or a committed binary under `flow-lang.Tests/baselines/Phase46/`). Add `flow-lang.Tests/baselines/Phase46/.gitkeep` so the dir is tracked. Whichever storage you pick, the captured values MUST come from the CURRENT pre-redirect build (run once, freeze the output). Do NOT touch `NoteSynthesizer.cs` in this plan — this task only OBSERVES current behavior.

    Document in a class-level comment: this guard exists to prove the Wave 2 D-03 SynthUtils redirect is bit-identical; if it fails after the redirect, take the documented fallback (keep oscillator loops inline, redirect only BeatsToSeconds+CreateSilence) per RESEARCH §D-03 + Open Q2.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~NoteSynthesizerByteGuard" 2>&1 | tail -20</automated>
  </verify>
  <done>
    New `NoteSynthesizerByteGuardTests.cs` compiles and is GREEN against the current (pre-redirect) build, with one exact-`Assert.Equal(float[])` assertion per primitive synth (sine/saw/square/triangle). The baseline values were captured from the current build. `flow-lang.Tests/baselines/Phase46/.gitkeep` tracked. `NoteSynthesizer.cs` UNCHANGED by this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is pure internal dead-code verification + a new test. No new input perimeter, no new network/file-parse/auth surface.

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundary introduced or widened by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | (regression — D-03 byte contract) | mitigate | This plan IS the mitigation: the exact-byte guard is a CORRECTNESS control (not a security control) that pins the oscillator byte contract before any redirect touches it |

**Honest statement:** No removal in this plan introduces or widens an attack surface — Task 1 changes no code, Task 2 adds a test only. The one risk class for the phase is *accidental behavior change* (regression), covered by the test-green + byte-exact determinism gate. That is a correctness control, not a security control. No new auth, network, file-parsing, or input surface. No package installs (T-46-SC N/A — zero package-manager activity this phase).
</threat_model>

<verification>
- `grep` confirms no capital-`Fixtures/` path strings / git paths in flow-lang.Tests (D-04 no-op).
- `DiagnosticRendererGoldenTests.cs` confirmed to `File.ReadAllText` the .txt baselines (D-09 KEEP, no deletion).
- `dotnet test --filter NoteSynthesizerByteGuard` GREEN against the current pre-redirect build.
- `dotnet build` green; no production code modified.
</verification>

<success_criteria>
- D-04 verified as already-merged (no `git mv`, no path-string edits needed).
- D-09 recorded as KEEP with the failed-condition rationale; diagnostics .txt files untouched.
- The exact-byte synth guard exists and is GREEN, ready to gate the Wave 2 D-03 redirect.
</success_criteria>

<output>
Create `.planning/phases/46-codebase-bloat-removal/46-01-SUMMARY.md` when done. Record: the D-04 grep results (empty), the D-09 live-read confirmation, and the synth-guard baseline-capture method (so Wave 2 knows exactly what "bit-identical" is measured against).
</output>
