---
phase: 46-codebase-bloat-removal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/audio.flow
  - flow-lang/Audio/PulseAudioSimpleBackend.cs
  - flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs
autonomous: true
requirements: [CLEAN-05, CLEAN-08]
nyquist_compliant: true

must_haves:
  truths:
    - "D-05: the 2 dead `internal proc createSineTone` forward-decls (audio.flow:224,227) are removed; the composer-facing stereo proc wrappers (audio.flow ~352-411) are UNTOUCHED and still resolve"
    - "D-08: the 2 `ClampSamples` thin-wrapper shims are inlined to direct `AudioUtils.ClampSamples()` calls at all 3 callsites; same helper invoked → no behavior change"
  artifacts:
    - path: "flow-lang/audio.flow"
      provides: "audio.flow with the 2 dead internal createSineTone decls removed, stereo wrappers intact"
      contains: "proc createSineTone"
  key_links:
    - from: "PulseAudioSimpleBackend.cs / PlaybackFunctions.cs callsites"
      to: "AudioUtils.ClampSamples"
      via: "direct call (shim removed)"
      pattern: "AudioUtils\\.ClampSamples"
---

<objective>
Two independent, small, behavior-preserving cleanups bundled (distinct files, both ~indirection removal):

1. **D-05 (CLEAN-05):** Remove the 2 dead `internal proc createSineTone` forward-decls in `audio.flow:224,227`. (Audit's "16 declarations" was wrong — verified exactly 2; Saw/Square/Triangle have none.) The stereo Flow proc wrappers (`audio.flow:352-411`) fully shadow them and are UNTOUCHED. These internal decls are dead weight.
2. **D-08 (CLEAN-08):** Inline the 2 `ClampSamples` thin-wrapper shims to direct `AudioUtils.ClampSamples()` calls (3 callsites). `CoreAudioBackend.cs:149` already calls `AudioUtils.ClampSamples` directly — this aligns the other two backends to that precedent.

Purpose: Remove pure internal indirection (D-01). Zero behavior change, zero byte impact.
Output: `audio.flow` minus 2 internal decls; 2 shims deleted + 3 callsites rewritten to the direct call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-codebase-bloat-removal/46-RESEARCH.md

<interfaces>
<!-- D-08 redirect target (verified live) -->
AudioUtils.ClampSamples(float[] samples)  (flow-lang/Audio/AudioUtils.cs:12, namespace FlowLang.Audio)
PulseAudioSimpleBackend.cs in namespace FlowLang.Audio (same ns → no using needed)
PlaybackFunctions.cs already has `using FlowLang.Audio;` (line 1)
CoreAudioBackend.cs:149 — existing direct `AudioUtils.ClampSamples` call (precedent, do NOT touch)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the 2 dead internal createSineTone decls (D-05)</name>
  <files>flow-lang/audio.flow</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (§D-05 — the exact 2 decls + the "keep noteToFrequency at 222" warning + the mechanism explanation)
    - flow-lang/audio.flow (lines 220-230 to see the 2 target decls in context; lines 352-411 to confirm the stereo proc wrappers you must NOT touch)
  </read_first>
  <action>
    Delete exactly the 2 lines (re-locate by signature, not raw line number, since the file may have shifted):
      `internal proc createSineTone(Double: duration, Double: freq, Double: amp)`  (~:224)
      `internal proc createSineTone(Double: duration, Hertz: freqHz, Double: amplitude)`  (~:227)
    Also remove any orphaned lead-in comment that exclusively described these 2 decls. DO NOT touch:
      - `internal proc noteToFrequency` (~:222) — a DIFFERENT decl, keep it.
      - The composer-facing stereo `proc createSineTone` wrappers at ~:352 (Double,Double,Double) and ~:365 (Double,Hertz,Double) — these shadow the internal decls and call `createBufferStereo`+`generateSine`+`createOsc`, NOT the mono C# builtin. Keep intact.
    Per RESEARCH A3: leave the C# `SignalGeneration.CreateSineTone` builtin registered (BuiltInFunctions.cs:767-777) — D-05 is "internal proc decls ONLY"; the now-undeclared C# mono builtin is harmless dead weight and removing it widens the diff. Do NOT touch BuiltInFunctions.cs in this plan.
  </action>
  <verify>
    <automated>grep -n "internal proc createSineTone" flow-lang/audio.flow ; grep -c "proc createSineTone" flow-lang/audio.flow ; for t in tests/test_mix.flow tests/test_writewav.flow; do dotnet run --project flow-interpreter "$t" 2>&1 | tail -2; done</automated>
  </verify>
  <done>
    `grep "internal proc createSineTone" flow-lang/audio.flow` returns ZERO; the stereo `proc createSineTone` wrappers remain; `noteToFrequency` intact; sine-using `.flow` scripts still print their PASS sentinels (stereo wrapper resolves).
  </done>
</task>

<task type="auto">
  <name>Task 2: Inline the ClampSamples shims to direct AudioUtils.ClampSamples (D-08)</name>
  <files>flow-lang/Audio/PulseAudioSimpleBackend.cs, flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs</files>
  <read_first>
    - .planning/phases/46-codebase-bloat-removal/46-RESEARCH.md (§D-08 — exact shim + callsite lines + namespace verification)
    - flow-lang/Audio/PulseAudioSimpleBackend.cs (line 97 callsite, lines 269-271 the shim + its /// comment)
    - flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs (lines 189, 243 callsites, lines 372-374 the shim + its /// comment)
    - flow-lang/Audio/AudioUtils.cs (line 12 — confirm `ClampSamples(float[])` signature)
  </read_first>
  <action>
    1. `PulseAudioSimpleBackend.cs`: rewrite the callsite at ~:97 `var clamped = ClampSamples(samples);` → `var clamped = AudioUtils.ClampSamples(samples);`. Then delete the private shim at ~:271 `private static float[] ClampSamples(float[] samples) => AudioUtils.ClampSamples(samples);` plus its `/// Delegates...` comment (~269-270). Same namespace `FlowLang.Audio` → no `using` needed.
    2. `PlaybackFunctions.cs`: rewrite the 2 callsites at ~:189 and ~:243 `var clamped = ClampSamples(buffer.Data);` → `var clamped = AudioUtils.ClampSamples(buffer.Data);`. Then delete the private shim at ~:374 plus its `/// ...` comment (~372-373). The file already has `using FlowLang.Audio;` (line 1).
    Re-locate by content (the shim body / the `ClampSamples(` callsite), not raw line number. This is pure indirection removal — the SAME `AudioUtils.ClampSamples` is invoked → no behavior change, no byte impact.
  </action>
  <verify>
    <automated>grep -rn "private static float\[\] ClampSamples" flow-lang/Audio/PulseAudioSimpleBackend.cs flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs ; grep -rn "= ClampSamples(" flow-lang/Audio/PulseAudioSimpleBackend.cs flow-lang/StandardLibrary/Audio/PlaybackFunctions.cs ; dotnet build flow-lang/flow-lang.csproj 2>&1 | tail -5</automated>
  </verify>
  <done>
    No `private static float[] ClampSamples` shim remains in either file; no bare `= ClampSamples(` callsite remains (all rewritten to `AudioUtils.ClampSamples(`); `dotnet build` green. `CoreAudioBackend.cs:149` untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Removing a dead Flow decl + inlining a same-namespace helper introduces no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Tampering | audio render/playback regression | mitigate | D-05 removes a shadowed dead decl (stereo wrapper unaffected); D-08 inlines to the identical `AudioUtils.ClampSamples`. Full suite + two-run cmp-clean prove no behavior/byte change. Correctness control, not security. |

**Honest statement:** No new attack surface. D-05 deletes a dead internal Flow forward-decl; D-08 removes a same-namespace indirection shim invoking the identical helper. Sole risk class is accidental behavior change, covered by the determinism gate. No new input/network/file/parse surface. No package installs (T-46-SC N/A).
</threat_model>

<verification>
- D-05: no `internal proc createSineTone` in audio.flow; sine `.flow` scripts pass.
- D-08: no `ClampSamples` shim / bare callsite remains; build green; same helper invoked → playback byte-identical.
- Full `dotnet test` + touched `.flow` scripts green; two-run cmp-clean on a sine script unchanged.
</verification>

<success_criteria>
- 2 dead internal decls removed, stereo wrappers intact; 3 callsites inlined, 2 shims deleted; build + suite green; zero behavior change.
</success_criteria>

<output>
Create `.planning/phases/46-codebase-bloat-removal/46-03-SUMMARY.md` when done. Commit each target atomically (D-18): `refactor(46): remove dead internal createSineTone decls (D-05)` and `refactor(46): inline ClampSamples shims to AudioUtils (D-08)` — two commits.
</output>
