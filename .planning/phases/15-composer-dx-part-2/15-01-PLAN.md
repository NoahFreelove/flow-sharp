---
phase: 15
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - flow-lang.Tests/Unit/Phase15/.keep
  - flow-lang.Tests/Integration/Phase15/.keep
  - flow-lang.Tests/Shared/MidiReadHelpers.cs
  - flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs
  - flow-lang.Tests/FlowScriptData.cs
  - tests/output/.gitignore
  - tests/test_reverb_time.flow
  - tests/test_euclidean_swing.flow
  - tests/test_euclidean_humanize.flow
autonomous: true
requirements: [DX-07, DX-09]
nyquist_compliant: true
tags: [test-scaffolding, wave-0, midi-helpers, defer-05]
must_haves:
  truths:
    - "Wave-0 test directories exist so Wave-1 plans can create Phase15 Facts"
    - "MidiReadHelpers promoted out of DynamicsMidiVelocityTests inline block per DEFER-05"
    - "tests/output/ artifacts are git-ignored so WAV/MIDI regression runs do not pollute working tree"
    - "Three placeholder .flow scripts exist with sentinel echos, wired into FlowScriptData Theory rows"
  artifacts:
    - path: flow-lang.Tests/Unit/Phase15/
      provides: "Phase 15 unit Fact home directory"
    - path: flow-lang.Tests/Integration/Phase15/
      provides: "Phase 15 integration Fact home directory"
    - path: flow-lang.Tests/Shared/MidiReadHelpers.cs
      provides: "Shared MIDI byte/velocity read helper class"
      contains: "GetVelocityBytes, GetNoteNumbers, ReadAllBytes"
    - path: tests/output/.gitignore
      provides: "Ignore entry for WAV/MIDI regression artifacts"
    - path: tests/test_reverb_time.flow
      provides: "Phase 15 DX-07 script-level sanity test"
    - path: tests/test_euclidean_swing.flow
      provides: "Phase 15 DX-09 swing-only script-level sanity test"
    - path: tests/test_euclidean_humanize.flow
      provides: "Phase 15 DX-09 humanize + seed script-level sanity test"
  key_links:
    - from: "flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs"
      to: "flow-lang.Tests/Shared/MidiReadHelpers.cs"
      via: "GetVelocityBytes call replacing inline MidiFile.Read+GetNotes"
      pattern: "MidiReadHelpers\\.GetVelocityBytes"
    - from: "flow-lang.Tests/FlowScriptData.cs"
      to: "tests/test_reverb_time.flow / test_euclidean_*.flow"
      via: "RequiredSentinels entries gate Theory rows"
      pattern: "test_reverb_time\\.flow|test_euclidean_swing\\.flow|test_euclidean_humanize\\.flow"
---

<objective>
Wave-0 scaffolding for Phase 15. Create the Phase15 test subtree (Unit + Integration), promote the Phase 14 inline MidiFile.Read helper into a Shared/MidiReadHelpers.cs class (closes DEFER-05), add tests/output/.gitignore so regression artifacts are not committed, and seed three placeholder tests/test_*.flow scripts with PASSED sentinels wired into FlowScriptData. Wave 1+ plans depend on every output of this plan. Plan contains NO Facts green yet — scaffolding only.

Purpose: Eliminate "file/dir does not exist" blockers from downstream plans and retire the DEFER-05 debt before it gets duplicated a second time in Plan 05.
Output: 7 new files (6 code/config + 1 modified existing test) in a single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-composer-dx-part-2/15-CONTEXT.md
@.planning/phases/15-composer-dx-part-2/15-RESEARCH.md
@.planning/phases/15-composer-dx-part-2/15-VALIDATION.md
@.planning/phases/15-composer-dx-part-2/15-PATTERNS.md
@.planning/phases/14-composer-dx-part-1/deferred-items.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs (inline helper to promote, lines 56-61):
```csharp
var midiFile = MidiFile.Read(outputMidi);
byte[] velocities = midiFile
    .GetNotes()
    .Select(n => (byte)n.Velocity)
    .ToArray();
```

From flow-lang.Tests/FlowScriptData.cs (Theory-row shape to mirror, ~line 75):
```csharp
["test_transpose_int.flow"] = new[]
{
    "transpose with int: ok",
    "test_transpose_int: PASSED",
},
```

From tests/test_dynamics_midi_velocity.flow (placeholder script shape to mirror):
```flow
use "@std"
use "@audio"

tempo 120 {
    timesig 4/4 {
        Sequence base = | C4 D4 E4 F4 G4 |
        Sequence curve = base -> crescendo 0.25 0.75
        section s { curve }
        Song song = [s]
        (writeMidi "tests/output/dynamics_velocity.mid" song)
        (print "dynamics_velocity: PASSED")
    }
}
```

Target MidiReadHelpers signature (from 15-RESEARCH.md Wave 0 Gaps + 15-PATTERNS.md):
```csharp
namespace FlowLang.Tests.Shared;

internal static class MidiReadHelpers
{
    public static byte[] GetVelocityBytes(string midiPath);
    public static int[] GetNoteNumbers(string midiPath);
    public static byte[] ReadAllBytes(string midiPath);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase15 test subtree + promote MidiReadHelpers (DEFER-05)</name>
  <files>flow-lang.Tests/Unit/Phase15/.keep, flow-lang.Tests/Integration/Phase15/.keep, flow-lang.Tests/Shared/MidiReadHelpers.cs, flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs</files>
  <read_first>
    - flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs (full file, ~73 lines — source of inline snippet being promoted; lines 56-61 specifically)
    - .planning/phases/14-composer-dx-part-1/deferred-items.md §DEFER-05 (lines 122-140 — promotion trigger + signature sketch)
    - .planning/phases/15-composer-dx-part-2/15-PATTERNS.md §"MidiReadHelpers.cs (NEW — promotion per DEFER-05)" (lines 717-752 — exact class body)
    - .planning/phases/15-composer-dx-part-2/15-RESEARCH.md §"Wave 0 Gaps" (lines 878-897)
    - flow-lang.Tests/flow-lang.Tests.csproj (confirm Melanchall.DryWetMidi + System.IO + System.Linq already referenced — no package adds)
  </read_first>
  <action>
Create three filesystem artifacts and refactor one existing test:

**1a. Create directories with `.keep` sentinels (directories must exist for Wave 1 to compile Fact files into them):**

- Create `flow-lang.Tests/Unit/Phase15/.keep` (empty file; git tracks the directory via the placeholder).
- Create `flow-lang.Tests/Integration/Phase15/.keep` (empty file).
- Create `flow-lang.Tests/Shared/` directory (by creating the MidiReadHelpers.cs file inside it — directory creation is implicit).

**1b. Create `flow-lang.Tests/Shared/MidiReadHelpers.cs` verbatim from 15-PATTERNS.md lines 730-750:**

```csharp
using System.IO;
using System.Linq;
using Melanchall.DryWetMidi.Core;
using Melanchall.DryWetMidi.Interaction;

namespace FlowLang.Tests.Shared;

internal static class MidiReadHelpers
{
    public static byte[] GetVelocityBytes(string midiPath)
    {
        var midiFile = MidiFile.Read(midiPath);
        return midiFile.GetNotes().Select(n => (byte)n.Velocity).ToArray();
    }

    public static int[] GetNoteNumbers(string midiPath)
    {
        var midiFile = MidiFile.Read(midiPath);
        return midiFile.GetNotes().Select(n => (int)n.NoteNumber).ToArray();
    }

    public static byte[] ReadAllBytes(string midiPath) => File.ReadAllBytes(midiPath);
}
```

No changes to flow-lang.Tests.csproj — the xUnit project already globs `.cs` under the test root; Shared/ will be picked up automatically (confirmed via Phase 14 Integration/ subtree).

**1c. Refactor `flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs` to CALL the new helper (DEFER-05 "second duplicate triggers promotion" — Plan 05 will be the second user; we promote now to avoid diverging copies):**

- Add `using FlowLang.Tests.Shared;` to the using-list at the top of the file.
- Replace the 4-line inline block at lines 56-61 (exact current text: `var midiFile = MidiFile.Read(outputMidi); byte[] velocities = midiFile.GetNotes().Select(n => (byte)n.Velocity).ToArray();`) with `byte[] velocities = MidiReadHelpers.GetVelocityBytes(outputMidi);`.
- Leave ALL assertions on `velocities` unchanged — the local-variable name and its byte-array type are preserved, so downstream `Assert.Equal(new byte[] { 31, 47, 63, 79, 95 }, velocities)` continues to pass.
- The `using Melanchall.DryWetMidi.Core;` and `using Melanchall.DryWetMidi.Interaction;` imports become unused after the replacement — remove them (defensive cleanup).

**Rationale for refactoring Phase14 now** (not deferring to Plan 05): DEFER-05 explicitly says "Trigger for promotion: Second Fact duplicates the inline call shape." Plan 05 IS the second use. Doing the promotion + refactor in Wave 0 (vs Wave 3) means Plan 05 lands with only one code path to the helper; otherwise the codebase briefly has two copies of the inline snippet which then must be reconciled in Plan 05, doubling the work.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~DynamicsMidiVelocityTests.Crescendo_EmitsExpectedVelocityGradient" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `flow-lang.Tests/Unit/Phase15/.keep` exists (file with zero bytes acceptable).
    - `flow-lang.Tests/Integration/Phase15/.keep` exists.
    - `flow-lang.Tests/Shared/MidiReadHelpers.cs` exists AND contains `namespace FlowLang.Tests.Shared;` AND contains the three public method signatures `GetVelocityBytes(string)`, `GetNoteNumbers(string)`, `ReadAllBytes(string)` verbatim.
    - `grep -n "MidiReadHelpers.GetVelocityBytes" flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs` returns exactly 1 match.
    - `grep -c "MidiFile.Read" flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs` returns 0 (inline call fully removed).
    - `dotnet build flow-sharp.sln --nologo` exits 0 with zero warnings about unused `using` directives from the DryWetMidi namespaces in the edited test file.
    - `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~DynamicsMidiVelocityTests.Crescendo_EmitsExpectedVelocityGradient" --nologo` exits 0 (Phase 14 Fact stays GREEN after the refactor — zero regression in velocity byte array `[31, 47, 63, 79, 95]`).
  </acceptance_criteria>
  <done>Directories exist, MidiReadHelpers.cs compiles, Phase 14 DX-08 Fact remains GREEN after refactor to use the shared helper.</done>
</task>

<task type="auto">
  <name>Task 2: Seed tests/output/.gitignore + 3 placeholder .flow scripts + FlowScriptData Theory rows</name>
  <files>tests/output/.gitignore, tests/test_reverb_time.flow, tests/test_euclidean_swing.flow, tests/test_euclidean_humanize.flow, flow-lang.Tests/FlowScriptData.cs</files>
  <read_first>
    - tests/test_dynamics_midi_velocity.flow (full file — the analog shape for new scripts, 15-PATTERNS.md lines 802-820)
    - flow-lang.Tests/FlowScriptData.cs (full file, ~207 lines — RequiredSentinels entries, specifically around lines 60-206)
    - .planning/phases/15-composer-dx-part-2/15-PATTERNS.md §"FlowScriptData.cs — Theory rows" (lines 756-794 — exact sentinel text to wire)
    - .planning/phases/15-composer-dx-part-2/15-VALIDATION.md §"Wave 0 Requirements" (lines 75-96)
    - MEMORY.md `feedback_language_philosophy.md` (via memory) — S-expression / functional style, NO infix
  </read_first>
  <action>
**2a. Create `tests/output/.gitignore` with exactly this content (3 lines + trailing newline):**

```
# Phase 15 regression artifacts — regenerate on each run; do not commit binaries.
*.wav
*.mid
```

Rationale: Plan 05 and Plan 06 will write `phase15_*.wav` / `phase15_*.mid` here via `writeWav` / `writeMidi` during test runs. The existing `dynamics_velocity.mid` (checked in by Phase 14) is already committed — the `.gitignore` applies to newly-created files only; `git ls-files tests/output/` will continue to show the one pre-existing file.

**2b. Create `tests/test_reverb_time.flow`** (placeholder — Plan 03 expands with real reverb body; Wave 0 only needs the sentinel prints to satisfy FlowScriptData Theory rows):

```flow
use "@std"
use "@audio"

// Phase 15 DX-07 sanity — placeholder; Plan 03 replaces the body with real reverbTime render.
// WAVE-0 PLACEHOLDER — REWRITTEN BY PLAN 03.
(print "reverbTime 2.5: PASSED")
(print "reverbTime 0 dry: PASSED")
```

**2c. Create `tests/test_euclidean_swing.flow`** (placeholder — Plan 06 expands):

```flow
use "@std"
use "@audio"

// Phase 15 DX-09 sanity — placeholder; Plan 06 replaces the body with real euclidean swing call.
// WAVE-0 PLACEHOLDER — REWRITTEN BY PLAN 06.
(print "euclidean swing: PASSED")
```

**2d. Create `tests/test_euclidean_humanize.flow`** (placeholder — Plan 06 expands):

```flow
use "@std"
use "@audio"

// Phase 15 DX-09 sanity — placeholder; Plan 06 replaces body with euclidean humanize + writeMidi.
// WAVE-0 PLACEHOLDER — REWRITTEN BY PLAN 06.
(print "euclidean humanize seed=42: PASSED")
(print "two runs byte-identical: PASSED")
```

**Note on placeholders:** Plans 03 and 06 are GATED by this plan. They will overwrite these files with the real bodies (preserving the sentinel `(print ...)` lines at the end). The placeholders exist now so FlowScriptData entries (added below) do not create RED Theory rows between Wave 0 and Wave 2/3 completion.

**2e. Edit `flow-lang.Tests/FlowScriptData.cs`** — append three new `RequiredSentinels` entries at the end of the dictionary literal (immediately before the closing `};`), verbatim from 15-PATTERNS.md lines 773-792:

```csharp
// Phase 15 DX-07: reverbTime parses, renders, and short-circuits at 0.
["test_reverb_time.flow"] = new[]
{
    "reverbTime 2.5: PASSED",
    "reverbTime 0 dry: PASSED",
},

// Phase 15 DX-09: euclidean 4-arg swing overload.
["test_euclidean_swing.flow"] = new[]
{
    "euclidean swing: PASSED",
},

// Phase 15 DX-09: euclidean 6-arg humanize overload, same-seed byte-identical.
["test_euclidean_humanize.flow"] = new[]
{
    "euclidean humanize seed=42: PASSED",
    "two runs byte-identical: PASSED",
},
```

The existing FlowScriptData harness will pick up the new `test_*.flow` files from the `tests/` directory glob and match them against these RequiredSentinels entries. Because the placeholders print the sentinel strings verbatim, the Theory rows should go GREEN on this plan's test run.

**Do NOT touch any ExpectedErrorScripts entries** — these three new scripts are success-path scripts.
  </action>
  <verify>
    <automated>dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~FlowScriptTests" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `tests/output/.gitignore` exists and `grep -c "\.wav" tests/output/.gitignore` returns 1 AND `grep -c "\.mid" tests/output/.gitignore` returns 1.
    - `tests/test_reverb_time.flow`, `tests/test_euclidean_swing.flow`, `tests/test_euclidean_humanize.flow` all exist as files.
    - Each placeholder script contains the exact sentinel strings: `grep -Fq 'reverbTime 2.5: PASSED' tests/test_reverb_time.flow`, `grep -Fq 'reverbTime 0 dry: PASSED' tests/test_reverb_time.flow`, `grep -Fq 'euclidean swing: PASSED' tests/test_euclidean_swing.flow`, `grep -Fq 'euclidean humanize seed=42: PASSED' tests/test_euclidean_humanize.flow`, `grep -Fq 'two runs byte-identical: PASSED' tests/test_euclidean_humanize.flow`.
    - Each placeholder contains the literal string `WAVE-0 PLACEHOLDER — REWRITTEN BY PLAN` (so Plans 03/06 can grep for it to confirm they must overwrite).
    - `grep -c '"test_reverb_time.flow"' flow-lang.Tests/FlowScriptData.cs` returns 1.
    - `grep -c '"test_euclidean_swing.flow"' flow-lang.Tests/FlowScriptData.cs` returns 1.
    - `grep -c '"test_euclidean_humanize.flow"' flow-lang.Tests/FlowScriptData.cs` returns 1.
    - `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~FlowScriptTests" --nologo` exits 0 with all three new Theory rows GREEN (`test_reverb_time.flow`, `test_euclidean_swing.flow`, `test_euclidean_humanize.flow` all match their sentinels).
    - Full-suite baseline preserved: `dotnet test flow-sharp.sln --nologo` exits 0 with Phase 14 baseline Fact count intact.
  </acceptance_criteria>
  <done>Three placeholder .flow scripts exist, wired into FlowScriptData with matching sentinels, and the three new Theory rows are GREEN. Plans 03 and 06 will replace placeholder bodies in Wave 2/3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | New `tests/output/*.wav|.mid` files will be written during regression runs; paths are test-controlled (no user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `tests/output/.gitignore` | accept | Artifacts are regenerable; gitignore prevents accidental binary commits but does not protect against malicious local edits (not in threat model — single-user local scripting). |
| T-15-02 | Information Disclosure | `MidiReadHelpers.ReadAllBytes` | accept | Reads user-provided file paths; no path sanitization added because callers are in-process test code. Matches existing `File.ReadAllBytes` usage throughout the test suite. |

Flow is a single-user local-execution scripting language. No new network, auth, or crypto surface in this plan. Phase 15 security gate: LOW per VALIDATION §Security Domain.
</threat_model>

<verification>
- `dotnet build flow-sharp.sln --nologo` exits 0, zero warnings.
- `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase14" --nologo` exits 0 (Phase 14 baseline preserved post-MidiReadHelpers refactor).
- `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~FlowScriptTests" --nologo` exits 0 with 3 new GREEN Theory rows for the placeholder .flow scripts.
- Full suite `dotnet test flow-sharp.sln --nologo` exits 0; Phase 14 baseline (81 Facts green) + 3 new Theory rows = 84 passing, zero failing.
- `ls flow-lang.Tests/Unit/Phase15/ flow-lang.Tests/Integration/Phase15/ flow-lang.Tests/Shared/` all succeed (directories exist).
</verification>

<success_criteria>
- Phase 15 test subtree (Unit/Phase15, Integration/Phase15, Shared/) exists and is git-tracked via `.keep` sentinels + the MidiReadHelpers.cs file.
- DEFER-05 closed: DynamicsMidiVelocityTests.cs calls `MidiReadHelpers.GetVelocityBytes`, inline `MidiFile.Read(outputMidi)` pattern fully removed from that file.
- tests/output/.gitignore keeps future `phase15_*.wav|.mid` artifacts out of git.
- 3 placeholder .flow scripts wired into FlowScriptData; 3 Theory rows GREEN.
- Full suite GREEN post-changes (Phase 14 baseline preserved, 3 new Phase 15 Theory rows added).
</success_criteria>

<output>
After completion, create `.planning/phases/15-composer-dx-part-2/15-01-SUMMARY.md` with:
- Commit hash for the single atomic commit covering all Wave 0 scaffolding.
- Note DEFER-05 closed (link back to `.planning/phases/14-composer-dx-part-1/deferred-items.md` §DEFER-05 — strike through entry per deferred-items handling protocol).
- Confirm placeholder-vs-real contract: Plan 03 must replace `tests/test_reverb_time.flow` body; Plan 06 must replace `tests/test_euclidean_swing.flow` + `tests/test_euclidean_humanize.flow` bodies. Sentinels must remain.
- Fact count delta (0 Facts green this plan; 3 new Theory rows green).
</output>
