---
phase: 15-composer-dx-part-2
plan: 01
subsystem: testing

tags: [test-scaffolding, wave-0, midi-helpers, defer-05, drywetmidi]

# Dependency graph
requires:
  - phase: 14-composer-dx-part-1
    provides: "Inline MidiFile.Read + GetNotes velocity projection in DynamicsMidiVelocityTests.cs (source of DEFER-05 promotion)"
provides:
  - "flow-lang.Tests/Unit/Phase15/ directory (empty, with .keep sentinel) — home for Phase 15 unit Facts (F-01..F-18, F-21..F-23)"
  - "flow-lang.Tests/Integration/Phase15/ directory (.keep sentinel) — home for Phase 15 integration Facts (F-02, F-07, F-08, F-19, F-20)"
  - "flow-lang.Tests/Shared/MidiReadHelpers.cs — internal static helper class with GetVelocityBytes / GetNoteNumbers / ReadAllBytes"
  - "tests/output/.gitignore — ignores *.wav and *.mid regression artifacts"
  - "tests/test_reverb_time.flow — Wave 0 placeholder with PASSED sentinels (Plan 03 rewrites body)"
  - "tests/test_euclidean_swing.flow — Wave 0 placeholder (Plan 06 rewrites body)"
  - "tests/test_euclidean_humanize.flow — Wave 0 placeholder (Plan 06 rewrites body)"
  - "FlowScriptData.RequiredSentinels entries for the three new Theory rows (all GREEN)"
affects: [15-02, 15-03, 15-04, 15-05, 15-06, 15-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Shared/ subtree inside flow-lang.Tests for cross-phase test helpers — promoted on second use (DEFER-05 protocol)"
    - ".keep sentinel files to git-track empty test directories"
    - "Wave 0 placeholder .flow scripts: sentinel-only (print ...) bodies that let FlowScriptData Theory rows go GREEN before real feature code lands; downstream plans rewrite the body while preserving the sentinel lines"

key-files:
  created:
    - flow-lang.Tests/Unit/Phase15/.keep
    - flow-lang.Tests/Integration/Phase15/.keep
    - flow-lang.Tests/Shared/MidiReadHelpers.cs
    - tests/output/.gitignore
    - tests/test_reverb_time.flow
    - tests/test_euclidean_swing.flow
    - tests/test_euclidean_humanize.flow
  modified:
    - flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs
    - flow-lang.Tests/FlowScriptData.cs

key-decisions:
  - "DEFER-05 closed in Wave 0 (not deferred to Plan 05): promoting MidiReadHelpers now keeps Plan 05's byte-identical MIDI Fact landing with only one code path to the helper, avoiding a transient divergent-copies state"
  - "Placeholders use // comments (not Note:) because // is already exercised by test_comments.flow Theory row, confirming lexer support"
  - "Placeholder .flow scripts use pure (print ...) calls — no imports needed beyond the @std / @audio modules each downstream plan's real body will require anyway"

patterns-established:
  - "Shared/ helper promotion protocol: extract to flow-lang.Tests/Shared/*Helpers.cs at the second inline duplicate (DEFER-05 trigger wording)"
  - "Wave 0 placeholder convention: sentinel-only (print ...) body + 'WAVE-0 PLACEHOLDER — REWRITTEN BY PLAN NN' marker comment so downstream plans can grep and confirm they must rewrite"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-04-21
---

# Phase 15 Plan 01: Wave 0 Scaffolding Summary

**Scaffolded Phase 15 test subtree (Unit/Phase15, Integration/Phase15, Shared/), promoted MidiReadHelpers out of DynamicsMidiVelocityTests per DEFER-05, added tests/output/ binary-artifact gitignore, and seeded three placeholder .flow scripts wired into FlowScriptData — all downstream Phase 15 waves unblocked.**

## Performance

- **Duration:** 4 min (226 seconds from start to final commit)
- **Started:** 2026-04-21T03:39:34Z
- **Completed:** 2026-04-21T03:43:20Z
- **Tasks:** 2 (both committed atomically)
- **Files modified:** 9 (7 created, 2 edited)

## Accomplishments

- **DEFER-05 closed:** inline `MidiFile.Read(...).GetNotes().Select(n => (byte)n.Velocity).ToArray()` block extracted from `DynamicsMidiVelocityTests.cs` into `flow-lang.Tests/Shared/MidiReadHelpers.cs` with three methods (`GetVelocityBytes`, `GetNoteNumbers`, `ReadAllBytes`). Phase 14 DX-08 Fact remains GREEN after refactor (velocity bytes `[31, 47, 63, 79, 95]` unchanged).
- **Phase 15 test subtree in place:** `flow-lang.Tests/Unit/Phase15/` and `flow-lang.Tests/Integration/Phase15/` directories tracked via `.keep` sentinels; ready for Wave 1+ Fact files.
- **tests/output/ gitignore in place:** `*.wav` and `*.mid` regression artifacts generated by Plans 05/06 won't pollute the working tree (verified — full-suite run generated `dynamics_velocity.mid` + `test_full_song.wav` into `tests/output/`, both correctly ignored by git).
- **Three placeholder .flow scripts** with sentinel-only `(print ...)` bodies; FlowScriptData got three new `RequiredSentinels` entries. All three Theory rows are GREEN in the test suite.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Phase15 test subtree + promote MidiReadHelpers (DEFER-05)** — `893534f` (test)
2. **Task 2: Seed tests/output/.gitignore + 3 placeholder .flow scripts + FlowScriptData Theory rows** — `dc825d4` (test)

## Files Created/Modified

**Created (7):**

- `flow-lang.Tests/Unit/Phase15/.keep` — empty sentinel so git tracks the directory
- `flow-lang.Tests/Integration/Phase15/.keep` — empty sentinel
- `flow-lang.Tests/Shared/MidiReadHelpers.cs` — shared DryWetMidi helper class (internal static, 3 methods)
- `tests/output/.gitignore` — ignores `*.wav` and `*.mid` regression artifacts
- `tests/test_reverb_time.flow` — DX-07 placeholder; prints two `reverbTime ... PASSED` sentinels; Plan 03 will rewrite the body
- `tests/test_euclidean_swing.flow` — DX-09 swing placeholder; prints one `euclidean swing: PASSED` sentinel; Plan 06 rewrites
- `tests/test_euclidean_humanize.flow` — DX-09 humanize placeholder; prints two sentinels (seed + byte-identical); Plan 06 rewrites

**Modified (2):**

- `flow-lang.Tests/Integration/Phase14/DynamicsMidiVelocityTests.cs` — replaced inline `MidiFile.Read(...).GetNotes().Select(...)` block with `MidiReadHelpers.GetVelocityBytes(outputMidi)`; removed now-unused `using System.Linq;`, `using Melanchall.DryWetMidi.Core;`, `using Melanchall.DryWetMidi.Interaction;`; added `using FlowLang.Tests.Shared;`
- `flow-lang.Tests/FlowScriptData.cs` — appended three `RequiredSentinels` entries for the placeholder `.flow` scripts

## Decisions Made

- **DEFER-05 closed in Wave 0, not Plan 05:** The deferred-items wording says "second inline duplicate triggers promotion". Plan 05 (byte-identical MIDI) would be the second duplicate. Doing the promotion in Wave 0 (before any Phase 15 Fact lands) means Plan 05 hits a single shared path from day 1 instead of landing a second inline copy that gets reconciled after-the-fact. The plan explicitly authorized this refactor under the "Rationale for refactoring Phase14 now" clause.
- **Placeholders use `//` line comments** (exercised by `test_comments.flow` Theory row) rather than `Note:` style; keeps the placeholder's surface small and avoids any lexer-token surprises.
- **Preserved `using System.Linq;` would have been pointless** in the Phase 14 test file after the refactor — the only `.Select(...)` call moved into `MidiReadHelpers`. But `System.Linq` is also an implicit-usings member (per `<ImplicitUsings>enable</ImplicitUsings>` in the csproj), so removing the explicit `using` is equivalent and matches the plan's "defensive cleanup" instruction.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `tests/*` directory is globally gitignored — needed `git add -f` for new tracked .flow files**
- **Found during:** Task 2 (staging placeholder .flow scripts + tests/output/.gitignore)
- **Issue:** Root `.gitignore` line 7 is `tests/`. Every existing `tests/*.flow` script in the repo was tracked via `git add -f` (confirmed by `git ls-files tests/` showing ~70 .flow files all above the ignore rule). The plan assumed the new files would stage cleanly with `git add <path>` but they were silently filtered.
- **Fix:** Used `git add -f tests/output/.gitignore tests/test_reverb_time.flow tests/test_euclidean_swing.flow tests/test_euclidean_humanize.flow` to override the ignore rule, matching the existing tests/ tracking convention.
- **Files modified:** None — staging operation only.
- **Verification:** `git status --short` showed all four files staged as `A` (added); commit landed with all four `create mode 100644` entries.
- **Committed in:** `dc825d4` (Task 2 commit)

**2. [Minor — documented, not a code fix] Plan assumed `tests/output/dynamics_velocity.mid` was committed by Phase 14 — it is not**
- **Found during:** Task 2 verification (`git ls-files tests/output/`)
- **Issue:** Plan text says: "The existing `dynamics_velocity.mid` (checked in by Phase 14) is already committed". Reality: `tests/output/` didn't even exist as a directory in the worktree before this plan. The Phase 14 test generates the .mid at runtime; nothing was committed.
- **Fix:** None needed — the `.gitignore` correctly ignores the generated file on first test run. The plan's outcome ("existing file stays tracked, new files stay ignored") reduces trivially to "no files tracked under tests/output/, all runtime artifacts ignored", which is the stronger guarantee.
- **Files modified:** None.

---

**Total deviations:** 2 (1 auto-fixed staging issue, 1 spec-vs-reality documentation note)
**Impact on plan:** Zero impact on deliverables; both discoveries inform Phase 15 downstream plans (Plans 05/06 should use `git add -f` for any new test artifacts they commit; nothing pre-exists in `tests/output/` that they might collide with).

## Issues Encountered

None beyond the two deviations above.

## Deferred Items Closed

- **DEFER-05** (`.planning/phases/14-composer-dx-part-1/deferred-items.md` §DEFER-05): shared MIDI-read helper promotion. Closed by this plan — `MidiReadHelpers.cs` now lives at `flow-lang.Tests/Shared/MidiReadHelpers.cs` with the sketched signature (plus `ReadAllBytes` for File.ReadAllBytes trampoline, supporting byte-identical Fact comparisons without each caller re-typing `File.ReadAllBytes`). Phase 14 `DynamicsMidiVelocityTests` refactored to use it. Strike-through entry should be added to `.planning/phases/14-composer-dx-part-1/deferred-items.md` during next phase hygiene sweep.

## Placeholder-vs-Real Contract

The three Wave 0 placeholder scripts MUST be rewritten by downstream plans while preserving the sentinel `(print ...)` lines that FlowScriptData Theory rows match against.

| Placeholder file | Sentinels that must remain | Downstream plan |
|------------------|---------------------------|-----------------|
| `tests/test_reverb_time.flow` | `"reverbTime 2.5: PASSED"`, `"reverbTime 0 dry: PASSED"` | Plan 03 |
| `tests/test_euclidean_swing.flow` | `"euclidean swing: PASSED"` | Plan 06 |
| `tests/test_euclidean_humanize.flow` | `"euclidean humanize seed=42: PASSED"`, `"two runs byte-identical: PASSED"` | Plan 06 |

Each placeholder includes a `// WAVE-0 PLACEHOLDER — REWRITTEN BY PLAN NN` comment so Plans 03 and 06 can `grep WAVE-0` across `tests/` to confirm they haven't already been overwritten.

## Known Stubs

The three placeholder `.flow` scripts created by this plan are **intentional stubs** documented in the plan's `<tasks>` block and in the Placeholder-vs-Real Contract above. Each stub prints its sentinel(s) and returns — no real DX-07 / DX-09 code exercised yet.

| Stub | Location | Resolving plan |
|------|----------|----------------|
| `test_reverb_time.flow` body (lines 6-7) | `tests/test_reverb_time.flow` | Plan 03 (DX-07 reverbTime render) |
| `test_euclidean_swing.flow` body (line 6) | `tests/test_euclidean_swing.flow` | Plan 06 (DX-09 swing overload) |
| `test_euclidean_humanize.flow` body (lines 6-7) | `tests/test_euclidean_humanize.flow` | Plan 06 (DX-09 humanize overload + byte-identical MIDI) |

These stubs satisfy the FlowScriptData Theory rows immediately so Wave 1+ plans never see a RED baseline from uncreated scripts. Downstream plans rewrite the body while preserving the sentinel lines the Theory rows match.

## Fact Count Delta

- **Phase 15 Facts turning GREEN this plan:** 0 (Wave 0 is scaffolding only — no Unit/Phase15 or Integration/Phase15 Fact files created yet)
- **FlowScriptTests Theory rows added this plan:** 3 (all GREEN)
- **Phase 14 baseline:** 54 Phase14-filtered tests still passing (DX-08 Fact preserved post-MidiReadHelpers refactor)
- **Full suite:** 257 tests passing, 0 failing (previously 254 — net +3 from the new Theory rows)

## Next Phase Readiness

- All Wave 1+ plans that depend on `flow-lang.Tests/Unit/Phase15/`, `flow-lang.Tests/Integration/Phase15/`, or `flow-lang.Tests/Shared/MidiReadHelpers` can now compile their Fact files into those paths without blocking on directory creation.
- Plans 03 and 06 should use `git add -f` (matching repo convention) when overwriting the placeholder `.flow` scripts, if the overwrite is staged separately from untracked file creation.
- No blockers.

## Self-Check: PASSED

Verified all claims in this summary:

- `flow-lang.Tests/Unit/Phase15/.keep`: FOUND
- `flow-lang.Tests/Integration/Phase15/.keep`: FOUND
- `flow-lang.Tests/Shared/MidiReadHelpers.cs`: FOUND
- `tests/output/.gitignore`: FOUND
- `tests/test_reverb_time.flow`: FOUND
- `tests/test_euclidean_swing.flow`: FOUND
- `tests/test_euclidean_humanize.flow`: FOUND
- Commit `893534f`: FOUND in `git log --oneline` (Task 1)
- Commit `dc825d4`: FOUND in `git log --oneline` (Task 2)
- `dotnet test --filter "FullyQualifiedName~DynamicsMidiVelocityTests"`: 1/1 passed
- `dotnet test --filter "FullyQualifiedName~FlowScriptTests"`: 60/60 passed (3 new)
- `dotnet test flow-sharp.sln`: 257/257 passed

---
*Phase: 15-composer-dx-part-2*
*Completed: 2026-04-21*
