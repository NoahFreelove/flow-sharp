---
phase: 13-nyquist-validation-backfill
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-advanced-features/09-VALIDATION.md
  - flow-lang.Tests/Integration/Phase09/TutorialTests.cs
  - flow-lang.Tests/FlowScriptData.cs
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "Retroactive 09-VALIDATION.md exists at nyquist_compliant: true"
    - "AUDIO-08 tempoRamp pinned via tightened RequiredSentinels on test_tempo_ramp.flow (three boolean-result strings)"
    - "QOL-02 interactive tutorial pinned via NEW Integration Fact asserting examples/tutorial.flow exits 0"
    - "Tutorial-error finding (if any) flagged as ## Ultra-Important Finding + deferred to Phase 16 (QOL-03) — NOT fixed in Phase 13"
    - "Two-pass strict authorship produces documented Divergences"
  artifacts:
    - path: ".planning/phases/09-advanced-features/09-VALIDATION.md"
      provides: "Phase 9 Nyquist-compliant VALIDATION.md for AUDIO-08, QOL-02"
      contains: "nyquist_compliant: true"
    - path: "flow-lang.Tests/Integration/Phase09/TutorialTests.cs"
      provides: "QOL-02 tutorial-runs-to-completion Fact (examples/tutorial.flow exit 0)"
      exports: ["TutorialTests.TutorialRunsToCompletion"]
    - path: "flow-lang.Tests/FlowScriptData.cs"
      provides: "Tightened RequiredSentinels for test_tempo_ramp.flow"
      contains: "test_tempo_ramp.flow"
  key_links:
    - from: ".planning/phases/09-advanced-features/09-VALIDATION.md"
      to: "flow-lang.Tests/Integration/Phase09/TutorialTests.cs"
      via: "## Per-Task Verification Map citation"
      pattern: "Integration/Phase09/"
    - from: "flow-lang.Tests/Integration/Phase09/TutorialTests.cs"
      to: "examples/tutorial.flow"
      via: "runner.RunFile(tutorialPath)"
      pattern: "examples/tutorial.flow"
---

<objective>
Author `.planning/phases/09-advanced-features/09-VALIDATION.md` retroactively under TEST-04 via two-pass strict authorship. Covers AUDIO-08 tempoRamp and QOL-02 interactive tutorial. Adds one new Integration Fact (`TutorialTests`) loading `examples/tutorial.flow` via `FlowEngineRunner.RunFile` and asserting exit code 0. Adds one additive `RequiredSentinels` entry (test_tempo_ramp.flow) pinning the three "Test N: true/false" boolean-result strings — converting weak Theory coverage into strong observable-value pins with zero duplicate Fact authoring.

Purpose: Pin tempoRamp's ritardando/accelerando invariants via printed booleans and pin the tutorial's post-stability-fix executability via exit code. If the tutorial errors under current HEAD (post Phase 12 stability fixes), flag as `## Ultra-Important Finding` and defer fix to Phase 16 (QOL-03) — Phase 13 does NOT fix tutorial code.

Output: 09-VALIDATION.md at `nyquist_compliant: true`; 1 new xUnit Fact file under `flow-lang.Tests/Integration/Phase09/`; 1 additive entry in `flow-lang.Tests/FlowScriptData.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-nyquist-validation-backfill/13-CONTEXT.md
@.planning/phases/13-nyquist-validation-backfill/13-RESEARCH.md
@.planning/phases/13-nyquist-validation-backfill/13-PATTERNS.md

<interfaces>
<!-- Key contracts. -->

From flow-lang.Tests/Fixtures/FlowEngineRunner.cs:
```csharp
public (bool Success, string Stdout, string Stderr, int ErrorCount) RunFile(string path);
```

From flow-lang.Tests/FlowScriptData.cs:
```csharp
public static string FindTestsRoot();  // walks up from AppContext.BaseDirectory
```

From flow-lang.Tests/FlowScriptTests.cs (CWD pivot pattern):
```csharp
var origCwd = Environment.CurrentDirectory;
Environment.CurrentDirectory = Path.GetDirectoryName(testsRoot)!;
try { /* ... */ } finally { Environment.CurrentDirectory = origCwd; }
```

Note: `examples/tutorial.flow` (348 lines, existing file) is NOT currently covered by the Theory harness — `FlowScriptData.GetFlowScripts` globs `tests/` only, not `examples/`.
</interfaces>

# Project-Convention Corrections

- Target framework: **`net10.0`**; Solution: **`flow-sharp.sln`**; Test framework: **xUnit.v3 3.2.2**
- No new NuGet packages (D-12); no production code modifications
- File-scoped namespaces; `FlowLang.Tests.Integration.Phase09`
- `[Collection("FlowScripts")]` MANDATORY for TutorialTests (uses FlowEngineRunner)
- CWD pivot MANDATORY when tutorial.flow uses relative file paths (e.g., `writeWav "tutorial_output.wav"`)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass 1 — Requirements-only 09-VALIDATION.md draft</name>
  <files>.planning/phases/09-advanced-features/09-VALIDATION.md</files>

  <read_first>
    - .planning/milestones/v1.1-REQUIREMENTS.md
    - .planning/milestones/v1.1-ROADMAP.md (Phase 9 goal + 2 success criteria at lines 58–65 ONLY)
    - .planning/ROADMAP.md (Phase 13 goal + success criteria)
    - ~/.claude/get-shit-done/templates/VALIDATION.md (schema only)
    - .planning/phases/12-stability/12-VALIDATION.md (format exemplar only)
  </read_first>

  <forbidden_reads>
    - .planning/phases/09-advanced-features/09-01-SUMMARY.md
    - .planning/phases/09-advanced-features/09-02-SUMMARY.md
    - .planning/phases/09-advanced-features/09-01-PLAN.md
    - .planning/phases/09-advanced-features/09-02-PLAN.md
    - .planning/phases/09-advanced-features/09-CONTEXT.md
    - flow-lang/**/*
    - flow-lang.Tests/**/*
    - tests/**/*.flow
    - examples/**/*.flow
  </forbidden_reads>

  <action>
  **Pass 1 is REQUIREMENTS-ONLY.**

  Create `.planning/phases/09-advanced-features/09-VALIDATION.md`.

  **Frontmatter:**
  ```yaml
  ---
  phase: 9
  slug: advanced-features
  status: draft
  nyquist_compliant: false
  wave_0_complete: false
  created: 2026-04-19
  backfilled: true
  ---
  ```

  **Body sections:**

  1. `# Phase 9 — Validation Strategy` + intro.

  2. `## Test Infrastructure` — identical shape (`dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase09"`).

  3. `## Sampling Rate` — identical.

  4. `## Per-Task Verification Map`:
  ```markdown
  | 09-backfill-01 | 13-04 | 1 | AUDIO-08 | — | `tempoRamp` produces ritardando (more frames than constant fast) and accelerando (fewer frames than constant slow) | integration (Theory) | `RequiredSentinels["test_tempo_ramp.flow"]` | ❌ W0 | ⬜ pending |
  | 09-backfill-02 | 13-04 | 1 | QOL-02 | — | `examples/tutorial.flow` runs via FlowEngineRunner.RunFile with errorCount == 0 | integration | `dotnet test --filter "FullyQualifiedName~TutorialTests"` | ❌ W0 | ⬜ pending |
  ```

  5. `## Wave 0 Requirements`:
  ```markdown
  - [ ] `flow-lang.Tests/Integration/Phase09/` — NEW subdirectory
  ```

  6. `## Manual-Only Verifications`:
  ```markdown
  | Behavior | Requirement | Why Manual | Test Instructions |
  |----------|-------------|------------|-------------------|
  | Audible tempo transition character (gradual, not abrupt) | AUDIO-08 | Perceptual — requires listening to confirm smooth deceleration/acceleration | Render a tempoRamp via `dotnet run --project flow-interpreter tests/test_tempo_ramp.flow`, play output WAV, confirm gradual BPM change |
  | Tutorial pedagogical clarity (guides user from basics to full songs) | QOL-02 | Subjective — requires reading the tutorial as a new user | Open `examples/tutorial.flow`, verify each section builds on prior concepts |
  ```

  7. `## Observable Invariants`:
  ```markdown
  1. **AUDIO-08:** stdout of `tests/test_tempo_ramp.flow` contains three boolean-result strings proving the ritardando/accelerando invariants: (a) tempoRamp produces non-zero buffer, (b) ritardando produces more frames than constant-fast (i.e., `tempoRamp(seq, 120, 80)` slower than `tempoRamp(seq, 120, 120)`), (c) accelerando produces fewer frames than constant-slow. Exact sentinel strings pinned by Pass 2.
  2. **QOL-02:** `FlowEngineRunner.RunFile("examples/tutorial.flow")` returns `errorCount == 0` (tutorial script runs to completion without interpreter errors under the post-Phase-12 stability-fix codebase).
  ```

  8. `## Pass 1 Draft`:
  ```markdown
  - **AUDIO-08:** expected `tempoRamp(sequence, startBPM, endBPM) -> Buffer` to produce a buffer that, when rendered at `startBPM → endBPM`, has a frame-count different from a constant-tempo render. For ritardando (slowing down), the total duration should be LONGER than at the faster constant tempo (fewer notes per second = more seconds total). For accelerando (speeding up), the total duration should be SHORTER than at the slower constant tempo.
  - **QOL-02:** expected an `examples/tutorial.flow` script that walks a new user from basic expressions through note streams, sections, and full song creation. The script must run without interpreter errors — if it doesn't, there is no tutorial to speak of.
  ```

  9. `## Pass 2 Implementation Map` — stub: `[TBD in Task 2]`
  10. `## Divergences` — stub: `[TBD in Task 2]`
  11. `## Validation Sign-Off` — standard list. Approval: pending.

  **Special note for this plan:** If Pass 2 discovers `examples/tutorial.flow` errors under current HEAD (post-Phase-12 stability fixes), the Pass 2 executor MUST flag the finding in an `## Ultra-Important Finding` section between `## Divergences` and `## Validation Sign-Off`, document the error, and replace the Fact's `Assert.Equal(0, errorCount)` with a `Skip = "tracked to Phase 16 QOL-03 refresh"` attribute. DO NOT fix the tutorial — Phase 13 is pure docs + test-backfill.
  </action>

  <acceptance_criteria>
    - `test -f .planning/phases/09-advanced-features/09-VALIDATION.md` exits 0
    - `grep -q "^phase: 9$" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `grep -q "^slug: advanced-features$" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `grep -q "^nyquist_compliant: false$" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `grep -qE "AUDIO-08|QOL-02" .planning/phases/09-advanced-features/09-VALIDATION.md` matches both
    - `grep -q "## Pass 1 Draft" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `grep -q "## Pass 2 Implementation Map" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `grep -q "## Divergences" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
  </acceptance_criteria>

  <commit>docs(13-04): pass 1 — requirements-first 09-VALIDATION.md draft</commit>

  <done>09-VALIDATION.md exists with Pass 1 sections; Pass 2 sections are stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Pass 2 — Reality check, test authoring, divergences, promote to nyquist_compliant</name>
  <files>
    flow-lang.Tests/Integration/Phase09/TutorialTests.cs,
    flow-lang.Tests/FlowScriptData.cs,
    .planning/phases/09-advanced-features/09-VALIDATION.md
  </files>

  <read_first>
    - .planning/phases/09-advanced-features/09-VALIDATION.md (Pass 1 draft)
    - .planning/phases/09-advanced-features/09-01-SUMMARY.md
    - .planning/phases/09-advanced-features/09-02-SUMMARY.md
    - flow-lang.Tests/Fixtures/FlowEngineRunner.cs
    - flow-lang.Tests/FlowScriptData.cs
    - flow-lang.Tests/FlowScriptTests.cs (CWD pivot reference at lines 19-24, 50-53)
    - flow-lang.Tests/Unit/InterpreterTests.cs (Fact class shape)
    - tests/test_tempo_ramp.flow
    - examples/tutorial.flow (read first 50 lines to understand shape)
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md (for confirmed-working-at-shipped-time context, line 105)
  </read_first>

  <action>
  **Step 1 — Empirically capture test_tempo_ramp.flow stdout:**

  ```bash
  dotnet run --project flow-interpreter tests/test_tempo_ramp.flow
  ```

  Record exact printed strings for the three ritardando/accelerando assertions.

  **Step 2 — Edit `flow-lang.Tests/FlowScriptData.cs`** — append ONE entry to `RequiredSentinels`, BEFORE the closing `};`:

  ```csharp
          // Phase 13-04 (AUDIO-08): pin the ritardando/accelerando boolean outputs.
          // Script prints three Test-N: true/false lines. The test logic is in
          // the .flow script itself; this sentinel pins that all three invariants
          // evaluate true post-AUDIO-08 shipment.
          ["test_tempo_ramp.flow"] = new[]
          {
              "Test 1 - tempoRamp produces non-zero buffer: true",
              "Test 2 - Ritardando produces more frames than constant fast: true",
              "Test 3 - Accelerando produces fewer frames than constant slow: true",
          },
  ```

  **Sentinel verification rule:** If the script prints different wording (e.g., `"Test 1: tempoRamp..."` or `"PASS Test 1"`), substitute the actual strings and log in Divergences.

  **Step 3 — Run the tutorial manually to pre-check:**

  ```bash
  dotnet run --project flow-interpreter examples/tutorial.flow
  echo "Exit code: $?"
  ```

  - If exit code is 0 → proceed with the full Fact implementation below.
  - If exit code is non-zero → document the error in `## Ultra-Important Finding` and author the Fact with `[Fact(Skip = "Tracked to Phase 16 QOL-03 tutorial refresh — see 09-VALIDATION.md #ultra-important-finding")]`. DO NOT FIX `examples/tutorial.flow`.

  **Step 4 — Create `flow-lang.Tests/Integration/Phase09/TutorialTests.cs`** (NEW file + directory):

  ```csharp
  using FlowLang.Tests.Fixtures;
  using Xunit;

  namespace FlowLang.Tests.Integration.Phase09;

  /// <summary>
  /// QOL-02 regression test: examples/tutorial.flow runs to completion without
  /// interpreter errors post-Phase-12 stability fixes. The tutorial is NOT
  /// covered by the Theory harness (FlowScriptData globs tests/ only).
  /// This Fact pins exit-code-zero for the tutorial as it stands in HEAD;
  /// tutorial feature-refresh is tracked separately under QOL-03 (Phase 16).
  /// </summary>
  [Collection("FlowScripts")]
  public class TutorialTests
  {
      [Fact]
      public void TutorialRunsToCompletion()
      {
          var testsRoot = FlowScriptData.FindTestsRoot();
          var repoRoot = Path.GetDirectoryName(testsRoot)!;
          var tutorialPath = Path.Combine(repoRoot, "examples", "tutorial.flow");

          var origCwd = Environment.CurrentDirectory;
          Environment.CurrentDirectory = repoRoot;
          try
          {
              using var runner = new FlowEngineRunner();
              var (ok, _, stderr, errorCount) = runner.RunFile(tutorialPath);
              Assert.True(ok, $"tutorial errored: {stderr}");
              Assert.Equal(0, errorCount);
          }
          finally
          {
              Environment.CurrentDirectory = origCwd;
          }
      }
  }
  ```

  If the tutorial errors under HEAD (per Step 3 pre-check), replace the `[Fact]` line with:

  ```csharp
      [Fact(Skip = "Tracked to Phase 16 QOL-03 tutorial refresh — errors at runtime under post-Phase-12 codebase. See 09-VALIDATION.md #ultra-important-finding")]
  ```

  And include the `## Ultra-Important Finding` section in 09-VALIDATION.md documenting the observed error and the deferral.

  **Step 5 — Finalize `09-VALIDATION.md`:**

  Fill `## Pass 2 Implementation Map`:
  ```markdown
  - **AUDIO-08:** `flow-lang.Tests/FlowScriptData.cs::RequiredSentinels["test_tempo_ramp.flow"]` — pinned on three "Test N - …: true" strings per empirical capture.
  - **QOL-02:** `flow-lang.Tests/Integration/Phase09/TutorialTests.cs::TutorialRunsToCompletion` — loads `examples/tutorial.flow` via `runner.RunFile`, asserts `errorCount == 0`. [If skipped: cross-reference to Ultra-Important Finding below.]
  ```

  Fill `## Divergences` with empirical discrepancies (sentinel string format, tutorial API/grammar drift).

  **If tutorial pre-check in Step 3 errored,** insert between Divergences and Validation Sign-Off:
  ```markdown
  ## Ultra-Important Finding

  `examples/tutorial.flow` does NOT run cleanly under current HEAD (post-Phase-12 stability fixes).
  Error observed:
  ```
  <paste actual stderr from Step 3>
  ```

  Per CONTEXT D-21 (Phase 13 MAY NOT modify existing tests or fix code), this finding is
  DEFERRED to Phase 16 (QOL-03: Tutorial Refresh). The QOL-02 Fact is SKIPPED with a tracking
  message; the skip will be removed when Phase 16 lands the tutorial refresh.

  v1.1-MILESTONE-AUDIT.md line 105 recorded the tutorial as confirmed-working at v1.1 close;
  the observed error is a regression introduced by Phase 12's stability fixes (or by drift
  in stdlib between v1.1 and HEAD). Diagnosing the exact cause is out of scope for TEST-04.
  ```

  Flip frontmatter: `status: passed`, `nyquist_compliant: true`, `wave_0_complete: true`.

  **Step 6 — Verify:** `dotnet test flow-sharp.sln` MUST exit 0. A skipped Fact is acceptable — `Failed: 0` is the gate, not `Skipped: 0`.
  </action>

  <acceptance_criteria>
    - `test -f flow-lang.Tests/Integration/Phase09/TutorialTests.cs` exits 0
    - `grep -q "TutorialTests" flow-lang.Tests/Integration/Phase09/TutorialTests.cs` matches
    - `grep -q "\\[Collection(\"FlowScripts\")\\]" flow-lang.Tests/Integration/Phase09/TutorialTests.cs` matches
    - `grep -q "namespace FlowLang.Tests.Integration.Phase09" flow-lang.Tests/Integration/Phase09/TutorialTests.cs` matches
    - `grep -q "Environment.CurrentDirectory" flow-lang.Tests/Integration/Phase09/TutorialTests.cs` matches (CWD pivot present)
    - `grep -q "examples/tutorial.flow\|examples.*tutorial.flow\|Path.Combine.*examples" flow-lang.Tests/Integration/Phase09/TutorialTests.cs` matches
    - `grep -q "test_tempo_ramp.flow" flow-lang.Tests/FlowScriptData.cs` matches
    - `grep -q "nyquist_compliant: true" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `grep -q "^status: passed$" .planning/phases/09-advanced-features/09-VALIDATION.md` matches
    - `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase09"` exits 0 (skipped is OK; failed is NOT)
    - `dotnet test flow-sharp.sln` exits 0 with `Failed: 0`
  </acceptance_criteria>

  <commits>
    - `test(13-04): pass 2 — author Phase 9 validation Fact + sentinel`
    - `docs(13-04): pass 2 — promote 09-VALIDATION.md to nyquist_compliant`
  </commits>

  <done>TutorialTests compiles (passes or skips per tutorial's HEAD state); FlowScriptData.cs has test_tempo_ramp entry; 09-VALIDATION.md at nyquist_compliant: true; `dotnet test flow-sharp.sln` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 13 does NOT cross any trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | — | — | accept | **severity: none.** Pure docs + test-backfill. Reads `examples/tutorial.flow` via FlowEngineRunner (same trust boundary as running any .flow script). No STRIDE category applies. |
</threat_model>

<verification>
- `.planning/phases/09-advanced-features/09-VALIDATION.md` exists at `nyquist_compliant: true`
- `flow-lang.Tests/Integration/Phase09/TutorialTests.cs` compiles; passes OR skips with tracking message
- `flow-lang.Tests/FlowScriptData.cs` contains test_tempo_ramp.flow RequiredSentinels entry
- `dotnet test flow-sharp.sln` exits 0 with `Failed: 0`
- If tutorial errors: `## Ultra-Important Finding` present in 09-VALIDATION.md with Phase 16 deferral
</verification>

<success_criteria>
- Phase 9 retroactive VALIDATION.md satisfies ROADMAP Phase 13 criterion 1 + criterion 3 (boolean-result sentinel pins + exit-code pin)
- Tutorial-error discovery path explicit: flag + defer, never fix-in-Phase-13
- Zero new NuGet packages; zero production code modifications
</success_criteria>

<output>
After completion, create `.planning/phases/13-nyquist-validation-backfill/13-04-SUMMARY.md`.
</output>
