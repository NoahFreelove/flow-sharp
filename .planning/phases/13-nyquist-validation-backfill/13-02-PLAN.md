---
phase: 13-nyquist-validation-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-developer-experience/07-VALIDATION.md
  - flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs
  - flow-lang.Tests/FlowScriptData.cs
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "Retroactive 07-VALIDATION.md exists at nyquist_compliant: true"
    - "DX-01 `//` line comments verified via existing tests/test_comments.flow Theory row with tightened sentinels pinning numeric output values"
    - "DX-02 math stdlib verified via tightened sentinels with empirically-captured Double.ToString() formats"
    - "DX-03 writeWav + exportWav alias verified via tightened sentinels asserting BOTH signatures succeeded"
    - "DX-04 REPL auto-imports verified via NEW Integration Fact executing the three `use` statements the REPL hardcodes"
    - "Two-pass strict authorship produces documented Divergences (esp. DX-02 Double.ToString() format drift from draft)"
  artifacts:
    - path: ".planning/phases/07-developer-experience/07-VALIDATION.md"
      provides: "Phase 7 Nyquist-compliant VALIDATION.md for DX-01, DX-02, DX-03, DX-04"
      contains: "nyquist_compliant: true"
    - path: "flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs"
      provides: "DX-04 REPL auto-import resolution Fact"
      exports: ["RepLAutoImportTests.AutoImportedModulesResolve_StdAudioCollections"]
    - path: "flow-lang.Tests/FlowScriptData.cs"
      provides: "Tightened RequiredSentinels for test_comments.flow + test_math.flow + test_writewav.flow"
      contains: "test_comments.flow"
  key_links:
    - from: ".planning/phases/07-developer-experience/07-VALIDATION.md"
      to: "flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs"
      via: "## Per-Task Verification Map citations"
      pattern: "Integration/Phase07/"
    - from: "flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs"
      to: "FlowEngineRunner"
      via: "using FlowLang.Tests.Fixtures"
      pattern: "new FlowEngineRunner"
---

<objective>
Author `.planning/phases/07-developer-experience/07-VALIDATION.md` retroactively under TEST-04 via two-pass strict authorship. Covers DX-01 `//` comments, DX-02 math stdlib, DX-03 writeWav/exportWav alias, DX-04 REPL auto-imports. Adds one new Integration Fact (`RepLAutoImportTests`) and extends `FlowScriptData.RequiredSentinels` with three additive entries (test_comments.flow, test_math.flow, test_writewav.flow).

Purpose: Close documentation-lag tech debt for Phase 7 and convert three weak `print "PASSED"` Theory rows into strong observable-value pins.

Output: 07-VALIDATION.md at `nyquist_compliant: true`; 1 new xUnit Fact file under `flow-lang.Tests/Integration/Phase07/`; 3 additive entries in `flow-lang.Tests/FlowScriptData.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-nyquist-validation-backfill/13-CONTEXT.md
@.planning/phases/13-nyquist-validation-backfill/13-RESEARCH.md
@.planning/phases/13-nyquist-validation-backfill/13-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. DO NOT explore codebase. -->

From flow-lang.Tests/Fixtures/FlowEngineRunner.cs:
```csharp
public sealed class FlowEngineRunner : IDisposable {
    public FlowEngineRunner(bool verbose = false);
    public (bool Success, string Stdout, string Stderr, int ErrorCount) RunSource(string source, string fileName = "<test>");
}
```

From flow-lang.Tests/FlowScriptData.cs (additive edit only — append to dict):
```csharp
public static readonly Dictionary<string, string[]> RequiredSentinels = new() {
    [Path.Combine("spike", "c1-musical-context-body.flow")] = new[] { ... }
    // 13-02 appends 3 new entries here
};
```
</interfaces>

# Project-Convention Corrections

- Target framework: **`net10.0`**; Solution: **`flow-sharp.sln`**; Test framework: **xUnit.v3 3.2.2**
- No new NuGet packages (D-12); no production code under `flow-lang/` modified
- File-scoped namespaces; `FlowLang.Tests.Integration.Phase07` for DX-04 Fact
- `[Collection("FlowScripts")]` MANDATORY; `use "@std"` at start of every literal script (Pitfall 2)

# Existing Theory Coverage

| REQ | Existing asset | Action |
|-----|----------------|--------|
| DX-01 | `tests/test_comments.flow` (43 lines exercising 6 comment styles) | TIGHTEN sentinels — script already prints numeric outputs |
| DX-02 | `tests/test_math.flow` (prints sin/cos/sqrt/min/max/pi/etc. results) | TIGHTEN sentinels — Pass 2 empirically captures exact `str` formats |
| DX-03 | `tests/test_writewav.flow` (tests both writeWav + exportWav) | TIGHTEN sentinels — pin both-signatures-succeeded strings |
| DX-04 | `tests/test_repl_autoimport.flow` (script-mode only, comments explicitly note "must be tested interactively") | INSUFFICIENT — add NEW Integration Fact executing the three REPL `use` statements |

</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass 1 — Requirements-only 07-VALIDATION.md draft</name>
  <files>.planning/phases/07-developer-experience/07-VALIDATION.md</files>

  <read_first>
    - .planning/milestones/v1.1-REQUIREMENTS.md
    - .planning/milestones/v1.1-ROADMAP.md (Phase 7 goal + 4 success criteria at lines 29–37 ONLY)
    - .planning/ROADMAP.md (Phase 13 goal + success criteria)
    - ~/.claude/get-shit-done/templates/VALIDATION.md (schema only)
    - .planning/phases/12-stability/12-VALIDATION.md (format exemplar only)
  </read_first>

  <forbidden_reads>
    - .planning/phases/07-developer-experience/07-01-SUMMARY.md
    - .planning/phases/07-developer-experience/07-02-SUMMARY.md
    - .planning/phases/07-developer-experience/07-01-PLAN.md
    - .planning/phases/07-developer-experience/07-02-PLAN.md
    - .planning/phases/07-developer-experience/07-CONTEXT.md
    - flow-lang/**/*
    - flow-lang.Tests/**/*
    - tests/**/*.flow
  </forbidden_reads>

  <action>
  **Pass 1 is REQUIREMENTS-ONLY.**

  Create `.planning/phases/07-developer-experience/07-VALIDATION.md`.

  **Frontmatter:**
  ```yaml
  ---
  phase: 7
  slug: developer-experience
  status: draft
  nyquist_compliant: false
  wave_0_complete: false
  created: 2026-04-19
  backfilled: true
  ---
  ```

  **Body sections (same order as Plan 13-01):**

  1. `# Phase 7 — Validation Strategy` + one-line intro.

  2. `## Test Infrastructure` table — identical to 13-01 (`xUnit.v3 3.2.2`, `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase07"`, `~20 seconds`).

  3. `## Sampling Rate` — identical 4 bullets.

  4. `## Per-Task Verification Map` (4 rows):
  ```markdown
  | 07-backfill-01 | 13-02 | 1 | DX-01 | — | `//` comments tokenize as whitespace; existing `tests/test_comments.flow` executes without parse errors | integration (Theory) | `dotnet test --filter "FullyQualifiedName~FlowScriptTests" + RequiredSentinels["test_comments.flow"]` | ❌ W0 | ⬜ pending |
  | 07-backfill-02 | 13-02 | 1 | DX-02 | — | Math stdlib returns expected numeric values for sin/cos/abs/sqrt/min/max/floor/ceil/pi/tau | integration (Theory) | `RequiredSentinels["test_math.flow"]` | ❌ W0 | ⬜ pending |
  | 07-backfill-03 | 13-02 | 1 | DX-03 | — | Both `writeWav(String, Buffer)` AND `exportWav(Buffer, String)` signatures resolve and produce WAV files | integration (Theory) | `RequiredSentinels["test_writewav.flow"]` | ❌ W0 | ⬜ pending |
  | 07-backfill-04 | 13-02 | 1 | DX-04 | — | REPL auto-import of @std/@audio/@collections resolves print/list/createSineTone symbols | integration | `dotnet test --filter "FullyQualifiedName~RepLAutoImportTests"` | ❌ W0 | ⬜ pending |
  ```

  5. `## Wave 0 Requirements`:
  ```markdown
  - [ ] `flow-lang.Tests/Integration/Phase07/` — NEW subdirectory
  ```

  6. `## Manual-Only Verifications`:
  ```markdown
  | Behavior | Requirement | Why Manual | Test Instructions |
  |----------|-------------|------------|-------------------|
  | Interactive REPL auto-import via stdin piping | DX-04 | v1.1 audit line 49: "not e2e-testable via piped stdin" | Launch `dotnet run --project flow-interpreter` with no args; type `(print "ok")` — should succeed without `use "@std"` |
  ```

  7. `## Observable Invariants`:
  ```markdown
  1. **DX-01:** `tests/test_comments.flow` executes with errorCount == 0 AND stdout contains the numeric outputs of the arithmetic expressions that follow comments (values pinned by Pass 2's empirical capture).
  2. **DX-02:** stdout of `tests/test_math.flow` contains the exact `str`-formatted outputs of `sin(0.0)`, `cos(0.0)`, `sqrt(16.0)`, `min(3, 7)`, `max(3, 7)`, `pi`, `tau`, `floor`, `ceil`, `abs(-5)` — exact strings empirically locked by Pass 2 (Pitfall 5: Double.ToString() format is implementation-defined and MUST NOT be inferred from Math.PI.ToString()).
  3. **DX-03:** stdout of `tests/test_writewav.flow` contains sentinels proving BOTH `writeWav(String, Buffer)` AND `exportWav(Buffer, String)` signatures executed successfully (backwards-compat alias preserved).
  4. **DX-04:** `FlowEngineRunner.RunSource` of a literal script `use "@std"\nuse "@audio"\nuse "@collections"\nArray[Int] xs = (list 1 2 3)\nBuffer b = (createSineTone 0.1 440.0 0.3)\n(print "ok")` produces errorCount == 0 AND stdout contains `"ok"`. This proxies the REPL contract: the three modules the REPL hardcodes (per DX-04 definition: @std, @audio, @collections) expose `print`/`list`/`createSineTone` without further imports.
  ```

  8. `## Pass 1 Draft`:
  ```markdown
  - **DX-01:** expected a `.flow` script containing `// comment` lines on various positions (full-line, inline-after-code, pre-code) to parse without lexer errors.
  - **DX-02:** expected `(sin 0.0)` → `0`, `(cos 0.0)` → `1`, `(sqrt 16.0)` → `4`, `(min 3 7)` → `3`, `(max 3 7)` → `7`, `pi` → `3.141592653589793`, `tau` → `6.283185307179586`. DOUBLE FORMAT DRIFT WARNING: Pass 2 MUST empirically capture actual `str` outputs — `(str 0)` may print `"0"` or `"0.0"` depending on Int vs Double widening. Pass 1 cannot know this.
  - **DX-03:** expected `(writeWav "out.wav" buf)` AND `(exportWav buf "out.wav")` to both succeed against the same buffer, producing identical output files.
  - **DX-04:** expected starting the REPL and calling `(print "hi")` without `use "@std"` to succeed because the REPL hardcodes `use "@std"`, `use "@audio"`, `use "@collections"`.
  ```

  9. `## Pass 2 Implementation Map` — stub: `[TBD in Task 2]`
  10. `## Divergences` — stub: `[TBD in Task 2]`
  11. `## Validation Sign-Off` — standard 6-item list. Approval: pending.
  </action>

  <acceptance_criteria>
    - `test -f .planning/phases/07-developer-experience/07-VALIDATION.md` exits 0
    - `grep -q "^phase: 7$" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -q "^slug: developer-experience$" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -q "^nyquist_compliant: false$" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -q "^backfilled: true$" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -qE "DX-01|DX-02|DX-03|DX-04" .planning/phases/07-developer-experience/07-VALIDATION.md` matches all four
    - `grep -q "## Pass 1 Draft" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -q "## Pass 2 Implementation Map" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -q "## Divergences" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
  </acceptance_criteria>

  <commit>docs(13-02): pass 1 — requirements-first 07-VALIDATION.md draft</commit>

  <done>07-VALIDATION.md exists with Pass 1 sections from REQUIREMENTS alone; Pass 2 + Divergences are stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Pass 2 — Reality check, test authoring, divergences, promote to nyquist_compliant</name>
  <files>
    flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs,
    flow-lang.Tests/FlowScriptData.cs,
    .planning/phases/07-developer-experience/07-VALIDATION.md
  </files>

  <read_first>
    - .planning/phases/07-developer-experience/07-VALIDATION.md (Pass 1 draft)
    - .planning/phases/07-developer-experience/07-01-SUMMARY.md
    - .planning/phases/07-developer-experience/07-02-SUMMARY.md
    - flow-lang.Tests/Fixtures/FlowEngineRunner.cs
    - flow-lang.Tests/FlowScriptData.cs
    - flow-lang.Tests/Unit/InterpreterTests.cs
    - tests/test_comments.flow
    - tests/test_math.flow
    - tests/test_writewav.flow
    - tests/test_repl_autoimport.flow
    - flow-lang/Repl.cs (grep for the three `use` statements — read-only)
    - flow-lang/StandardLibrary/BuiltInFunctions.cs (lines 445-449: writeWav/exportWav registrations — read-only)
  </read_first>

  <action>
  **Step 1 — Empirically capture sentinels via actual script output.** Run each script and copy exact stdout substrings:

  ```bash
  dotnet run --project flow-interpreter tests/test_comments.flow
  dotnet run --project flow-interpreter tests/test_math.flow
  dotnet run --project flow-interpreter tests/test_writewav.flow
  ```

  Record the actual printed strings. DO NOT infer formats — especially `pi`/`tau`/`sqrt` outputs. `(str Math.PI)` in .NET 10 formats to `"3.141592653589793"` but Flow's `str` implementation may wrap it in additional precision or type labels.

  **Step 2 — Edit `flow-lang.Tests/FlowScriptData.cs`** — append three new entries to `RequiredSentinels`, BEFORE the closing `};`. Exact text (substitute empirical strings per Step 1 into the sentinel arrays):

  ```csharp
          // Phase 13-02 (DX-01): pin the arithmetic outputs that follow comment lines.
          // Script tests full-line, inline, pre-code, post-code, inside-proc, and
          // post-note-stream comments. Would fail to tokenize without // support.
          ["test_comments.flow"] = new[]
          {
              "5",
              "10",
              "15",
              "8",  // addOne(7) result
          },

          // Phase 13-02 (DX-02): pin math stdlib numeric outputs.
          // PASS-2 EMPIRICAL CAPTURE REQUIRED: exact Double.ToString() formats
          // copied from `dotnet run --project flow-interpreter tests/test_math.flow` stdout.
          ["test_math.flow"] = new[]
          {
              // Substitute these with empirically captured strings from Step 1:
              "0",                 // sin(0.0) — may print "0" or "0.0"
              "1",                 // cos(0.0)
              "4",                 // sqrt(16.0)
              "3",                 // min(3,7)
              "7",                 // max(3,7)
              "3.141592653589793", // pi — verify exact format empirically
          },

          // Phase 13-02 (DX-03): pin both writeWav + exportWav alias success.
          ["test_writewav.flow"] = new[]
          {
              "PASS: writeWav(String, Buffer) succeeded",
              "PASS: exportWav(Buffer, String) backwards compat succeeded",
              "All writeWav tests passed",
          },
  ```

  **Sentinel verification rule (Pitfall 1):** If the empirically captured string differs from the drafted substring, REPLACE the drafted value with the empirical value. Record all substitutions in Divergences with the format `DX-XX: Pass 1 drafted "<draft>"; Pass 2 captured "<actual>" from tests/test_XXX.flow stdout. Actual used; codebase canonical.`

  **Step 3 — Create `flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs`** (NEW file; also creates `Integration/Phase07/` directory):

  ```csharp
  using FlowLang.Tests.Fixtures;
  using Xunit;

  namespace FlowLang.Tests.Integration.Phase07;

  /// <summary>
  /// DX-04 regression test: REPL hardcodes auto-import of @std, @audio, @collections
  /// so interactive users can call print/list/createSineTone without typing `use`
  /// statements. The v1.1 audit noted DX-04 "is not e2e-testable via piped stdin —
  /// verified by code inspection only" [v1.1-MILESTONE-AUDIT.md line 49]. This Fact
  /// executes the SAME three imports via FlowEngine and verifies the symbols
  /// resolve — the best automated proxy.
  /// </summary>
  [Collection("FlowScripts")]
  public class RepLAutoImportTests
  {
      [Fact]
      public void AutoImportedModulesResolve_StdAudioCollections()
      {
          using var runner = new FlowEngineRunner();
          var (ok, stdout, stderr, errorCount) = runner.RunSource(@"
  use ""@std""
  use ""@audio""
  use ""@collections""
  Array[Int] xs = (list 1 2 3)
  Buffer b = (createSineTone 0.1 440.0 0.3)
  (print ""ok"")
  ");
          Assert.True(ok, $"script errored: {stderr}");
          Assert.Equal(0, errorCount);
          Assert.Contains("ok", stdout);
      }
  }
  ```

  If `createSineTone` signature is `(duration, freq, amplitude)` in a different argument order, or if the current stdlib name is `sineTone`/`createSine`/etc., grep `flow-lang/audio.flow` and `flow-lang/StandardLibrary/Audio/` (read-only) for the actual registered name, substitute, and log the substitution in Divergences. Same applies to `list` / `Array[Int]` type declaration syntax.

  **Step 4 — Finalize `07-VALIDATION.md`:**

  Fill `## Pass 2 Implementation Map`:
  ```markdown
  - **DX-01:** `flow-lang.Tests/FlowScriptData.cs::RequiredSentinels["test_comments.flow"]` — substring-pinned on the arithmetic outputs (values per empirical capture, see Divergences).
  - **DX-02:** `flow-lang.Tests/FlowScriptData.cs::RequiredSentinels["test_math.flow"]` — substring-pinned on sin/cos/sqrt/min/max/pi empirical outputs (Pitfall 5 honored: strings captured from actual script run, not inferred).
  - **DX-03:** `flow-lang.Tests/FlowScriptData.cs::RequiredSentinels["test_writewav.flow"]` — substring-pinned on both writeWav + exportWav success strings.
  - **DX-04:** `flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs::AutoImportedModulesResolve_StdAudioCollections` — executes the three hardcoded REPL imports + verifies symbols resolve (proxy for true REPL stdin piping per v1.1 audit).
  ```

  Fill `## Divergences` with actual empirical discrepancies — at minimum, DX-02 Double format drift (or "No divergences" if Pass 1 drafts matched reality). Example entry format:
  ```markdown
  - **DX-02:** Pass 1 drafted `"3.141592653589793"` for pi based on Math.PI.ToString().
    Pass 2 captured `"<actual string>"` from `tests/test_math.flow` stdout.
    Replaced draft with actual — codebase str-output format is canonical.
  ```

  Flip frontmatter: `status: passed`, `nyquist_compliant: true`, `wave_0_complete: true`.

  **Step 5 — Verify suite green:** `dotnet test flow-sharp.sln` MUST exit 0.
  </action>

  <acceptance_criteria>
    - `test -f flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs` exits 0
    - `grep -q "RepLAutoImportTests" flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs` matches
    - `grep -q "\\[Collection(\"FlowScripts\")\\]" flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs` matches
    - `grep -q "namespace FlowLang.Tests.Integration.Phase07" flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs` matches
    - `grep -q "test_comments.flow" flow-lang.Tests/FlowScriptData.cs` matches
    - `grep -q "test_math.flow" flow-lang.Tests/FlowScriptData.cs` matches
    - `grep -q "test_writewav.flow" flow-lang.Tests/FlowScriptData.cs` matches
    - `grep -q "nyquist_compliant: true" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `grep -q "^status: passed$" .planning/phases/07-developer-experience/07-VALIDATION.md` matches
    - `dotnet test flow-sharp.sln --filter "FullyQualifiedName~Phase07"` exits 0
    - `dotnet test flow-sharp.sln` exits 0 with `Failed: 0`
  </acceptance_criteria>

  <commits>
    - `test(13-02): pass 2 — author Phase 7 validation Fact + sentinels`
    - `docs(13-02): pass 2 — promote 07-VALIDATION.md to nyquist_compliant`
  </commits>

  <done>RepLAutoImportTests compiles + passes; FlowScriptData.cs has 3 new sentinel entries (test_comments, test_math, test_writewav); 07-VALIDATION.md at nyquist_compliant: true; `dotnet test flow-sharp.sln` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 13 does NOT cross any trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | — | — | accept | **severity: none.** Pure docs + test-backfill. Reads a handful of read-only .flow scripts and appends dictionary entries. No STRIDE category applies. |
</threat_model>

<verification>
- `.planning/phases/07-developer-experience/07-VALIDATION.md` exists at `nyquist_compliant: true`
- `flow-lang.Tests/Integration/Phase07/RepLAutoImportTests.cs` compiles + passes
- `flow-lang.Tests/FlowScriptData.cs` contains 3 new RequiredSentinels entries
- `dotnet test flow-sharp.sln` exits 0 with `Failed: 0`
- 07-VALIDATION.md Observable Invariants covers all 4 active requirements
</verification>

<success_criteria>
- Phase 7 retroactive VALIDATION.md satisfies ROADMAP Phase 13 criterion 1 + criterion 3 (observable-value pins: empirically-captured `str` outputs + Fact-asserted symbol resolution)
- DX-02 Double.ToString() format drift documented in Divergences per Pitfall 5
- DX-04 proxy-via-direct-imports honors v1.1 audit's "not e2e-testable via piped stdin" observation
- Zero new NuGet packages; zero production code modifications
</success_criteria>

<output>
After completion, create `.planning/phases/13-nyquist-validation-backfill/13-02-SUMMARY.md`.
</output>
