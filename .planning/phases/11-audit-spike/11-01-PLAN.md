---
phase: 11-audit-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/spike/c1-musical-context-body.flow
  - flow-lang/Interpreter/Interpreter.cs  # single-line AUDIT-VERIFIED comment near line 286 only
autonomous: true
requirements: [SPIKE-01]
tags: [audit-spike, interpreter, musical-context]

must_haves:
  truths:
    - "A `.flow` test exists that exercises ExecuteMusicalContext early-return on invalid tempo"
    - "The test outputs observable evidence of whether the block body executed after the validation error"
    - "The musical-context stack is balanced after the test runs (dotnet exit 0, no stack-unbalance exceptions)"
    - "The Interpreter.cs file carries a greppable `AUDIT-VERIFIED 2026-04-18: C1` marker naming the verdict"
    - "A verdict (Confirmed|Dismissed) is decided from test output + source reading, not assumption"
  artifacts:
    - path: "tests/spike/c1-musical-context-body.flow"
      provides: "Empirical probe of C1 — early return skips block body? frame leak? both? neither?"
      contains: "tempo -5"
    - path: "flow-lang/Interpreter/Interpreter.cs"
      provides: "Inline AUDIT-VERIFIED comment recording C1 verdict"
      contains: "AUDIT-VERIFIED 2026-04-18: C1"
  key_links:
    - from: "tests/spike/c1-musical-context-body.flow"
      to: "flow-lang/Interpreter/Interpreter.cs ExecuteMusicalContext (lines 130-290)"
      via: "tempo -5 { | C4 D4 E4 F4 | } followed by top-level probes"
      pattern: "tempo -[0-9]"
    - from: "Interpreter.cs AUDIT-VERIFIED comment"
      to: "tests/spike/c1-musical-context-body.flow"
      via: "evidence path in comment text"
      pattern: "AUDIT-VERIFIED 2026-04-18: C1"
---

<objective>
Produce decisive, empirical evidence for audit claim **C1** ("ExecuteMusicalContext frame leak / body skip on validation error") per SPIKE-01.

The architecture researcher argues the frame is balanced because `finally { _context.PopFrame(); }` wraps the method body at `Interpreter.cs:286-289`. The pitfalls researcher argues the *real* bug is that `return` statements inside the try (lines 151, 164, 178, 224, 240, 255, 263) exit before the `foreach (var stmt in ctx.Body)` loop at lines 270-284, silently dropping the block body. Both agents read the same file. The spike settles which claim reflects shipped behavior.

**Purpose:** Hand Phase 12 FIX-07 either a failing RED test to flip green, or a closed claim with inline evidence — so no one edits working code on a misread.
**Output:** `tests/spike/c1-musical-context-body.flow` + a single `// AUDIT-VERIFIED 2026-04-18: C1 — <verdict> (tests/spike/c1-musical-context-body.flow)` comment in Interpreter.cs near line 286.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-audit-spike/11-CONTEXT.md
@.planning/CODEBASE-AUDIT-2026-04-18.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md

<interfaces>
<!-- Core code region under investigation. Read before writing the test. -->
<!-- From flow-lang/Interpreter/Interpreter.cs:130-290: -->
```csharp
private void ExecuteMusicalContext(MusicalContextStatement ctx)
{
    _context.PushFrame();
    try
    {
        var musicalCtx = new MusicalContext();
        switch (ctx.ContextType)
        {
            case MusicalContextType.Tempo:
                // ...
                if (!MusicalContext.IsValidTempo(tempo))
                {
                    _errorReporter.ReportError($"Tempo must be positive, got {tempo}", ctx.Location);
                    return;   // <-- exits method. finally still runs. Body loop at 270 does NOT run.
                }
                musicalCtx.Tempo = tempo;
                break;
            // ... 6 more validation paths with the same pattern
        }
        _context.CurrentFrame.MusicalContext = musicalCtx;
        foreach (var stmt in ctx.Body) { ExecuteStatement(stmt); /* ... */ }   // <-- only reached on success
    }
    finally
    {
        _context.PopFrame();
    }
}
```

From flow-lang/Interpreter/Interpreter.cs:71-128 (ExecuteStatement, relevant to side-question):
```csharp
public void ExecuteStatement(Statement stmt)
{
    if (_returnValue != null) return;
    switch (stmt) { /* ... dispatch ... */ }
}
```
`_returnValue` is only set by `ExecuteReturn` (ReturnStatement case) per code reading — statements AFTER an invalid `tempo -5 { ... }` block at top level should still execute, but statements INSIDE the block body are skipped.
</interfaces>

<test-conventions>
Read `tests/test_musical_context_errors.flow` and `tests/test_for_loop.flow` before authoring. Conventions:
- First line is typically `use "@std"`
- Comments: `Note: <text>` (Flow has no `//` in .flow scripts — avoid C-style comments in .flow files)
- Assertions: `(print (str value))` — verified by eyeballing expected-value line following the assertion
- Exit 0 = pass; stderr errors (reported via `_errorReporter`) still allow exit 0 under the soft-failure model — verify by reading test_musical_context_errors.flow which emits a tempo error and still exits 0
</test-conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/spike/c1-musical-context-body.flow probing early-return body skip</name>
  <read_first>
    - flow-lang/Interpreter/Interpreter.cs (lines 130-290, pay attention to `return` statements at 151, 164, 178, 224, 240, 255, 263 and the body loop at 270-284)
    - tests/test_musical_context_errors.flow (existing prior-art for tempo -N error tests)
    - tests/test_for_loop.flow (general .flow convention)
    - .planning/research/PITFALLS.md §Pitfall 1 (expected bug mechanism)
    - .planning/research/ARCHITECTURE.md §"Audit Re-Verification" row C1 (competing hypothesis)
  </read_first>
  <action>
    Create the directory `tests/spike/` if it does not yet exist (use `mkdir -p tests/spike`).

    Write `tests/spike/c1-musical-context-body.flow` containing four independent probe sections. Each section prints a distinct sentinel string so the spike operator can read stdout and decide the verdict unambiguously.

    REQUIRED CONTENTS (exact structure — each numbered block is a probe):

    ```
    use "@std"

    Note: ===== SPIKE C1: ExecuteMusicalContext early-return body skip =====
    Note: Reference: Interpreter.cs lines 130-290; returns at 151, 164, 178, 224, 240, 255, 263
    Note: Hypothesis A (pitfalls agent): early `return` skips `foreach (var stmt in ctx.Body)` at 270-284
    Note: Hypothesis B (architecture agent): finally block at 286-289 pops frame; behavior is correct

    Note: --- Probe 1: top-level statement AFTER a bad tempo block still runs ---
    tempo -5 {
        (print "c1-probe1-body-ran")
    }
    (print "c1-probe1-after-block")

    Note: --- Probe 2: two statements inside a bad swing block ---
    swing 2.0 {
        (print "c1-probe2-stmt1")
        (print "c1-probe2-stmt2")
    }
    (print "c1-probe2-after-block")

    Note: --- Probe 3: bad key block ---
    key "NotAKey" {
        (print "c1-probe3-body-ran")
    }
    (print "c1-probe3-after-block")

    Note: --- Probe 4: good tempo block (control — body MUST run) ---
    tempo 120 {
        (print "c1-probe4-body-ran")
    }
    (print "c1-probe4-after-block")

    Note: --- Expected output under hypothesis A (pitfalls agent / real bug) ---
    Note:   <error: Tempo must be positive, got -5>
    Note:   c1-probe1-after-block
    Note:   <error: Swing must be between 0.0 and 1.0, got 2>
    Note:   c1-probe2-after-block
    Note:   <error: Unrecognized key 'NotAKey'...>
    Note:   c1-probe3-after-block
    Note:   c1-probe4-body-ran
    Note:   c1-probe4-after-block
    Note: (Note the absence of c1-probe1-body-ran, c1-probe2-stmt1/stmt2, c1-probe3-body-ran.)
    Note:
    Note: --- Expected output under hypothesis B (architecture agent / no bug) ---
    Note:   same as above PLUS c1-probe1-body-ran, c1-probe2-stmt1, c1-probe2-stmt2, c1-probe3-body-ran.
    ```

    Do NOT add C# source comments or modify Interpreter.cs in this task. Do NOT rename existing tests.

    Use ONLY Flow's `Note:` comment style inside the .flow file (Flow has no `//` line-comment; even if the lexer permits it in some contexts, stick with `Note:` to match the tests/ convention).
  </action>
  <verify>
    <automated>mkdir -p tests/spike && test -f tests/spike/c1-musical-context-body.flow && dotnet run --project flow-interpreter tests/spike/c1-musical-context-body.flow</automated>
  </verify>
  <acceptance_criteria>
    - `tests/spike/c1-musical-context-body.flow` exists on disk
    - `dotnet run --project flow-interpreter tests/spike/c1-musical-context-body.flow` exits 0 (errors reported via ErrorReporter do NOT fail the process under soft-failure model; verify by reading tests/test_musical_context_errors.flow behavior)
    - stdout contains the literal strings `c1-probe1-after-block`, `c1-probe2-after-block`, `c1-probe3-after-block`, `c1-probe4-body-ran`, `c1-probe4-after-block` (these are the non-controversial control outputs)
    - stderr contains the literal substrings `Tempo must be positive`, `Swing must be between 0.0 and 1.0`, and `Unrecognized key` (error reporter output from validation paths)
    - Executor records in the plan's SUMMARY whether each of `c1-probe1-body-ran`, `c1-probe2-stmt1`, `c1-probe2-stmt2`, `c1-probe3-body-ran` appeared on stdout — this IS the verdict evidence
  </acceptance_criteria>
  <done>The spike test exists, runs, and its stdout/stderr output unambiguously supports either hypothesis A or hypothesis B. The executor records observed behavior in the plan SUMMARY's "Verdict" section.</done>
</task>

<task type="auto">
  <name>Task 2: Derive verdict and record inline AUDIT-VERIFIED comment in Interpreter.cs</name>
  <read_first>
    - Task 1 output (stdout / stderr of the spike run)
    - flow-lang/Interpreter/Interpreter.cs lines 283-290 (insertion site just before the `finally` block)
    - .planning/phases/11-audit-spike/11-CONTEXT.md D-02 (comment format spec)
  </read_first>
  <action>
    Based on observed Task 1 stdout:

    **If `c1-probe1-body-ran`, `c1-probe2-stmt1`, `c1-probe2-stmt2`, `c1-probe3-body-ran` are ABSENT from stdout:** verdict is **Confirmed** — body IS silently skipped after validation error. This is the pitfalls agent's hypothesis A. The test stays on disk as a RED regression test per D-08. Phase 12 FIX-07a will flip it to GREEN.

    **If ALL of those sentinels APPEAR on stdout:** verdict is **Dismissed** — body executes correctly despite the early return. The test stays on disk as a GREEN regression test documenting the behavior invariant.

    **If some appear and some don't:** record the exact pattern in the SUMMARY; the verdict is **Confirmed (partial)** and Phase 12 scope is the subset.

    Then, using the **Edit** tool (NOT heredoc, NOT Write), append a single-line comment immediately AFTER the `finally` block's closing brace at Interpreter.cs:289 (i.e., insert a new line between line 289 `}` and line 290's blank/next-method, so the marker sits at the end of the ExecuteMusicalContext method). The comment MUST match this exact format per D-02:

    ```
        // AUDIT-VERIFIED 2026-04-18: C1 — <verdict> (tests/spike/c1-musical-context-body.flow)
    ```

    where `<verdict>` is one of:
    - `Confirmed: body skipped after validation error`
    - `Dismissed: body executes; frame pops via finally`
    - `Confirmed (partial): body skipped for <tempo|swing|key|all>`

    The `<verdict>` free-text MUST be greppable by `grep -rn "AUDIT-VERIFIED 2026-04-18: C1"`.

    Do NOT modify any other line of Interpreter.cs. Do NOT alter the behavior. This is a single-line insertion only, permitted under ROADMAP success criterion 4.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj -nologo -v q 2>&amp;1 | tail -5 &amp;&amp; grep -n "AUDIT-VERIFIED 2026-04-18: C1" flow-lang/Interpreter/Interpreter.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "AUDIT-VERIFIED 2026-04-18: C1"` against the repo returns exactly ONE hit in flow-lang/Interpreter/Interpreter.cs
    - The matching line is between Interpreter.cs:285 and Interpreter.cs:295 (inside or immediately after the `finally` block of ExecuteMusicalContext)
    - The comment contains one of the three mandated verdict substrings above
    - `dotnet build flow-lang/flow-lang.csproj` succeeds (no compile regression from the single-line comment insert)
    - No line of ExecuteMusicalContext other than the inserted comment line is changed (verify via `git diff flow-lang/Interpreter/Interpreter.cs` showing exactly +1 insertion, 0 deletions, 0 modifications)
  </acceptance_criteria>
  <done>Interpreter.cs carries the AUDIT-VERIFIED marker for C1, greppable across the repo, and the build still compiles. Verdict is recorded both in the source comment and in the plan's SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase's plan 11-01 creates one local `.flow` test under `tests/spike/` and inserts one inline comment in a C# source file. No new code paths, no network I/O, no user input parsing, no auth surface, no new dependency. The test runs inside the same CLI sandbox existing tests run in.

| Boundary | Description |
|----------|-------------|
| dotnet CLI → `.flow` parser | Existing trust boundary unchanged; spike test exercises the same paths as tests/test_musical_context_errors.flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | tests/spike/c1-musical-context-body.flow | accept | Local test file under version control; no elevated permissions; identical trust envelope as existing tests/*.flow |
| T-11-01-02 | Information Disclosure | Interpreter.cs inline comment | accept | Comment contains only audit metadata + file path — no secrets, no PII, no credentials |
| T-11-01-03 | Denial of Service | dotnet run loop on bad .flow | accept | Flow's ErrorReporter soft-fails; no infinite recursion introduced; iteration-limit guards in for/while (MaxIterations) already exist |
</threat_model>

<verification>
- `find tests/spike -name "c1-*.flow" | wc -l` == 1
- `dotnet run --project flow-interpreter tests/spike/c1-musical-context-body.flow; echo "exit: $?"` prints `exit: 0`
- `grep -c "AUDIT-VERIFIED 2026-04-18: C1" flow-lang/Interpreter/Interpreter.cs` == 1
- `git diff --stat flow-lang/Interpreter/Interpreter.cs` reports +1 insertion, 0 deletions
</verification>

<success_criteria>
- SPIKE-01 acceptance satisfied: either a failing .flow test reproduces the bug (hypothesis A observed) OR a written dismissal sits in Interpreter.cs (hypothesis B observed)
- ROADMAP success criterion 4 respected: single-line comment is the ONLY production-source change
- CONTEXT D-02 respected: comment format is `// AUDIT-VERIFIED 2026-04-18: C1 — <verdict> (<evidence path>)`
- CONTEXT D-08 respected: if verdict is Confirmed, the test is committed RED for Phase 12 to flip
- Evidence is greppable across the repo so Phase 12 and future audits can retrieve it with a single command
</success_criteria>

<output>
After completion, create `.planning/phases/11-audit-spike/11-01-SUMMARY.md` with sections:
1. **Verdict** (Confirmed | Dismissed | Confirmed (partial))
2. **Evidence** (stdout lines present, stdout lines absent, stderr error messages)
3. **Inline marker** (file:line of the AUDIT-VERIFIED comment)
4. **Next action** (→ Phase 12 FIX-07a if Confirmed, → Closed if Dismissed)
</output>
