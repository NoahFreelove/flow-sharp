---
phase: 11-audit-spike
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/11-audit-spike/11-VERIFICATION.md
autonomous: true
requirements: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04, SPIKE-05]
tags: [audit-spike, requirements-split, verification, phase-12-handoff]

must_haves:
  truths:
    - "All five claim verdicts (C1–C5) are aggregated in a single 11-VERIFICATION.md with a 4-column table per D-07"
    - "REQUIREMENTS.md's FIX-07 umbrella is split into per-confirmed-claim sub-requirements (FIX-07a..e) per D-04"
    - "Dismissed claims do NOT produce sub-requirements — they close with the inline comment + the VERIFICATION row"
    - "REQUIREMENTS.md traceability table is updated so Phase 12 sees exactly which FIX-07x items survived the spike"
    - "SPIKE-0N traceability rows flip from Pending to Complete"
  artifacts:
    - path: ".planning/phases/11-audit-spike/11-VERIFICATION.md"
      provides: "Terminal artifact per D-07 — 4-column claim / verdict / evidence / next action table"
      contains: "| C1 |"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FIX-07 split into sub-requirements; traceability table updated; SPIKE-0N rows marked Complete"
      contains: "FIX-07"
  key_links:
    - from: ".planning/phases/11-audit-spike/11-VERIFICATION.md"
      to: "tests/spike/*.flow + flow-lang/*/AUDIT-VERIFIED comments"
      via: "Evidence Path column"
      pattern: "tests/spike/|AUDIT-VERIFIED"
    - from: ".planning/REQUIREMENTS.md FIX-07a..e"
      to: ".planning/phases/11-audit-spike/11-VERIFICATION.md rows"
      via: "one sub-requirement per Confirmed verdict row"
      pattern: "FIX-07[a-e]"
---

<objective>
Aggregate the five parallel claim investigations (11-01..11-05) into the two terminal artifacts Phase 12 will consume:

1. **`11-VERIFICATION.md`** — single 4-column table (claim / verdict / evidence path / next action) per D-07
2. **`REQUIREMENTS.md` diff** — FIX-07 umbrella split into `FIX-07a`..`FIX-07e`, ONE sub-requirement per Confirmed claim (none for Dismissed) per D-04; Traceability table updated

This plan is the sequential aggregation wave — it runs AFTER 11-01..11-05 complete because it reads their SUMMARYs and synthesizes them. It is the only plan in Phase 11 that touches REQUIREMENTS.md.

**Purpose:** Hand Phase 12 a decisive, machine-readable description of what fix work remains and what is closed.
**Output:** `.planning/phases/11-audit-spike/11-VERIFICATION.md` + `.planning/REQUIREMENTS.md` edits (Stability-Contingent section + Traceability table).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-audit-spike/11-CONTEXT.md
@.planning/phases/11-audit-spike/11-01-SUMMARY.md
@.planning/phases/11-audit-spike/11-02-SUMMARY.md
@.planning/phases/11-audit-spike/11-03-SUMMARY.md
@.planning/phases/11-audit-spike/11-04-SUMMARY.md
@.planning/phases/11-audit-spike/11-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Current REQUIREMENTS.md Stability-Contingent section (before this plan runs), verbatim from REQUIREMENTS.md lines 31-33:

```
### Stability — Contingent on Spike Outcome

- [ ] **FIX-07**: Fix whichever of C1–C5 are confirmed real by SPIKE-01 through SPIKE-05. Each confirmed bug gets a regression test committed alongside the fix. Dismissed spikes close without code change but with a comment in the cited file marking the area as re-verified 2026-04-18.
```

Current REQUIREMENTS.md Traceability table relevant rows (around line 78-89):

```
| SPIKE-01 | Phase 11 | Pending |
| SPIKE-02 | Phase 11 | Pending |
| SPIKE-03 | Phase 11 | Pending |
| SPIKE-04 | Phase 11 | Pending |
| SPIKE-05 | Phase 11 | Pending |
| FIX-05 | Phase 12 | Pending |
| FIX-06 | Phase 12 | Pending |
| FIX-07 | Phase 12 | Pending (contingent on Phase 11) |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 11-VERIFICATION.md aggregating all five claim verdicts per D-07</name>
  <read_first>
    - .planning/phases/11-audit-spike/11-01-SUMMARY.md through 11-05-SUMMARY.md (verdicts, evidence paths, frame counts)
    - .planning/phases/11-audit-spike/11-CONTEXT.md D-07 (4-column table format)
    - Run `grep -rn "AUDIT-VERIFIED 2026-04-18:" flow-lang/` and capture exact output to embed
  </read_first>
  <action>
    Create `.planning/phases/11-audit-spike/11-VERIFICATION.md` using this template. Substitute every `<...>` placeholder from the 11-0N-SUMMARY.md files.

    ```markdown
    # Phase 11: Audit Spike — Verification

    **Date:** 2026-04-18
    **Phase:** 11-audit-spike
    **Status:** Complete
    **Downstream consumer:** Phase 12 Stability (FIX-07 scope determination)

    ## Verdict Table

    | Claim | Verdict | Evidence Path | Next Action |
    |-------|---------|---------------|-------------|
    | C1 — ExecuteMusicalContext body skip / frame leak | <Confirmed\|Dismissed\|Confirmed (partial)> | tests/spike/c1-musical-context-body.flow; flow-lang/Interpreter/Interpreter.cs:<N> | <→ Phase 12 FIX-07a \| Closed> |
    | C2 — `_returnValue` short-circuit masks errors | <Confirmed\|Dismissed> | tests/spike/c2-return-value-short-circuit.flow; flow-lang/Interpreter/Interpreter.cs:<N> | <→ Phase 12 FIX-07b \| Closed> |
    | C3 — EnvelopeProcessor div-by-zero on sub-frame segments | <Confirmed\|Dismissed> | <tests/spike/c3-envelope-short-segments.flow OR reasoning-only per D-05>; flow-lang/StandardLibrary/Audio/EnvelopeProcessor.cs:<N> | <→ Phase 12 FIX-07c \| Closed> |
    | C4 — BufferHelpers FadeIn/FadeOut div-by-zero | <Confirmed\|Dismissed> | <tests/spike/c4-fade-short-durations.flow OR reasoning-only per D-05>; flow-lang/StandardLibrary/Audio/BufferHelpers.cs:<N> | <→ Phase 12 FIX-07d \| Closed> |
    | C5 — augment/diminish semantic swap | <Confirmed\|Dismissed> | tests/spike/c5-augment-diminish.flow; flow-lang/StandardLibrary/Transforms/TransformFunctions.cs:<augment-line>,<diminish-line> | <→ Phase 12 FIX-07e (BREAKING-CHANGE comms if swapped) \| Closed> |

    ## Summary

    - **Total claims investigated:** 5
    - **Confirmed (require Phase 12 fix):** <N>
    - **Dismissed (closed with inline marker):** <5-N>

    ## Phase 12 Handoff

    FIX-07 has been split per D-04 into the following Phase 12 sub-requirements:
    <bulleted list of whichever FIX-07a..e exist after Task 2 runs>

    Dismissed claims close without code change. Their inline `AUDIT-VERIFIED 2026-04-18` markers in production source serve as "already-reviewed" signals so future audits do not re-raise closed items.

    ## Evidence — Inline Markers (cross-check)

    Output of `grep -rn "AUDIT-VERIFIED 2026-04-18:" flow-lang/`:

    ```
    <paste literal grep output here — expected 6 hits: 1 for C1, 1 for C2, 1 for C3, 1 for C4, 2 for C5>
    ```

    ## BREAKING CHANGE Trigger (C5-specific)

    <If C5 Confirmed swapped:>
    Triggered. ROADMAP Phase 12 success criterion 4 applies: Phase 12 FIX-07e MUST ship with a release-notes BREAKING CHANGE entry, `augmentV1`/`diminishV1` transitional aliases, and updated `examples/*.flow` call sites — all in the same release.

    <If C5 Dismissed:>
    Not triggered. No migration comms required for v1.2.

    ---

    *Phase 11 complete. All five claims have a decisive verdict. Phase 12 FIX-07 scope is now determined.*
    ```

    Fill in every `<...>`. The "Evidence Path" column cites both the test path AND the inline marker line number from each plan's SUMMARY. The grep output block must match actual grep output verbatim so reviewers can cross-check one-shot.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-audit-spike/11-VERIFICATION.md &amp;&amp; grep -c "^| C[1-5]" .planning/phases/11-audit-spike/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-audit-spike/11-VERIFICATION.md` exists
    - Exactly FIVE verdict rows (grep `^| C[1-5]` count == 5)
    - Each row has a non-empty Verdict cell (one of `Confirmed`, `Dismissed`, `Confirmed (partial)`)
    - Each row has a non-empty Evidence Path cell citing `tests/spike/*.flow` AND a `flow-lang/...:<line>` inline-marker location
    - Each row has a non-empty Next Action cell (`→ Phase 12 FIX-07<letter>` for Confirmed, `Closed` for Dismissed)
    - The embedded grep output block matches actual `grep -rn "AUDIT-VERIFIED 2026-04-18:" flow-lang/` output
  </acceptance_criteria>
  <done>11-VERIFICATION.md consolidates all five verdicts; cross-checked against inline markers; ready for Phase 12.</done>
</task>

<task type="auto">
  <name>Task 2: Split FIX-07 in REQUIREMENTS.md per D-04 and mark SPIKE-0N rows Complete</name>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file — need Stability-Contingent section AND Traceability table in one pass)
    - .planning/phases/11-audit-spike/11-VERIFICATION.md (Task 1 output)
    - .planning/phases/11-audit-spike/11-CONTEXT.md D-04
    - .planning/ROADMAP.md §Phase 12 Stability success criterion 4 (BREAKING CHANGE trigger; relevant only if C5 Confirmed)
  </read_first>
  <action>
    Using the **Edit** tool (NOT Write — preserve every other section verbatim), perform these three edits:

    ### Edit 1 — Replace the Stability-Contingent section

    Locate (REQUIREMENTS.md lines 31-33):
    ```
    ### Stability — Contingent on Spike Outcome

    - [ ] **FIX-07**: Fix whichever of C1–C5 are confirmed real by SPIKE-01 through SPIKE-05. Each confirmed bug gets a regression test committed alongside the fix. Dismissed spikes close without code change but with a comment in the cited file marking the area as re-verified 2026-04-18.
    ```

    Replace with (fill in only the FIX-07x bullets corresponding to Confirmed claims — omit Dismissed):

    ```
    ### Stability — Contingent on Spike Outcome

    **Spike outcome:** see `.planning/phases/11-audit-spike/11-VERIFICATION.md`. FIX-07 split below per D-04; Dismissed claims closed without sub-requirement (marker-only closure).

    <For each Confirmed claim, emit one bullet from this template:>
    - [ ] **FIX-07<letter>** (closes SPIKE-0<N>, Confirmed): <one-line description of the observed bug as recorded in the VERIFICATION.md row>. Regression test: `tests/spike/c<N>-<slug>.flow` (committed RED in Phase 11 per D-08). Source: `<file>:<line>`. Ships in Phase 12 with a behavior-preserving fix that turns the RED test green.

    <For FIX-07e ONLY if C5 Confirmed swapped, add the following sub-bullet indented under the FIX-07e bullet:>
      - Ships with BREAKING-CHANGE release-notes entry, `augmentV1`/`diminishV1` transitional aliases, and updated `examples/*.flow` call sites per ROADMAP Phase 12 success criterion 4.

    <Final trailing line, always present:>
    Dismissed claims (closed by inline AUDIT-VERIFIED markers; no Phase 12 action required): <comma-separated list of Dismissed Cs, or "none" if all Confirmed>.
    ```

    Letter mapping (fixed; do not reassign):
    - FIX-07a → C1
    - FIX-07b → C2
    - FIX-07c → C3
    - FIX-07d → C4
    - FIX-07e → C5

    ### Edit 2 — Update Traceability table FIX-07 row

    Locate:
    ```
    | FIX-07 | Phase 12 | Pending (contingent on Phase 11) |
    ```

    Replace with one row per FIX-07x sub-requirement that was written in Edit 1. Dismissed claims get no row. Example if C1, C2, C5 were Confirmed and C3, C4 were Dismissed:
    ```
    | FIX-07a | Phase 12 | Pending |
    | FIX-07b | Phase 12 | Pending |
    | FIX-07e | Phase 12 | Pending |
    ```

    If ALL claims were Dismissed, replace the row with:
    ```
    | FIX-07 | — | Closed (all SPIKE-0N Dismissed; see 11-VERIFICATION.md) |
    ```

    ### Edit 3 — Mark SPIKE-0N rows Complete

    Locate the five rows:
    ```
    | SPIKE-01 | Phase 11 | Pending |
    | SPIKE-02 | Phase 11 | Pending |
    | SPIKE-03 | Phase 11 | Pending |
    | SPIKE-04 | Phase 11 | Pending |
    | SPIKE-05 | Phase 11 | Pending |
    ```

    Replace each `Pending` cell with `Complete` — the spike produced a verdict whether Confirmed or Dismissed, which satisfies SPIKE-0N's acceptance criterion either way.

    ### Guard rails
    - Do NOT modify the Audit Spike REQUIREMENTS section (SPIKE-01..SPIKE-05 bullets stay intact; only Traceability changes)
    - Do NOT modify Stability-Confirmed Bugs (FIX-05, FIX-06 stay intact)
    - Do NOT touch Test Unblocking, Composer DX, Quality of Life, Future Requirements, Out of Scope, or the header/metadata
    - The file's `*Last updated*` footer line should be updated to `*Last updated: 2026-04-18 — Phase 11 Audit Spike closed; FIX-07 split per D-04*`
  </action>
  <verify>
    <automated>grep -E "^- \[ \] \*\*FIX-07[a-e]\*\*" .planning/REQUIREMENTS.md | wc -l; grep -E "^\| FIX-07" .planning/REQUIREMENTS.md | wc -l; grep -c "^| SPIKE-0[1-5] | Phase 11 | Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - FIX-07x bullets count in Stability-Contingent section == number of Confirmed verdicts in 11-VERIFICATION.md
    - FIX-07x rows in Traceability table == same count (OR the single `| FIX-07 | — | Closed...|` row if all Dismissed)
    - All five SPIKE-0N rows in Traceability table show `Complete` (grep count == 5)
    - The original generic `- [ ] **FIX-07**: Fix whichever of C1–C5...` bullet is GONE (grep `grep -c "^- \[ \] \*\*FIX-07\*\*" .planning/REQUIREMENTS.md` returns 0)
    - FIX-05 and FIX-06 bullets and Traceability rows are UNCHANGED (regression check)
    - If C5 Confirmed, the FIX-07e bullet contains the substring `BREAKING-CHANGE release-notes entry` and `augmentV1`/`diminishV1`
    - `*Last updated*` footer line updated
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects the post-spike reality; FIX-07 split matches 11-VERIFICATION.md; SPIKE-0N rows are Complete; Phase 12 can read REQUIREMENTS.md + 11-VERIFICATION.md and know exactly what work remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 11-06 is documentation-only — no source code touched, no tests run, no executables generated. Edits are limited to REQUIREMENTS.md (a planning artifact) and a new VERIFICATION.md (a planning artifact).

| Boundary | Description |
|----------|-------------|
| Planner → REQUIREMENTS.md | Existing trust boundary; same authorship model as any .planning/ edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | REQUIREMENTS.md mid-file edits | accept | Preserved via Edit tool's surgical replacements; regression guarded by acceptance criteria (FIX-05/FIX-06 unchanged check) |
| T-11-06-02 | Information Disclosure | 11-VERIFICATION.md | accept | Content is audit metadata and .flow test paths; no secrets |
| T-11-06-03 | Repudiation | "who decided this verdict" | mitigate | Verdicts cross-reference 11-0N-SUMMARY.md which cite specific test run output and grep evidence — fully traceable |
</threat_model>

<verification>
- `.planning/phases/11-audit-spike/11-VERIFICATION.md` exists with 5 verdict rows
- `.planning/REQUIREMENTS.md` no longer contains the generic `- [ ] **FIX-07**: Fix whichever of C1–C5...` bullet
- `.planning/REQUIREMENTS.md` SPIKE-0N traceability rows show `Complete` (all 5)
- `.planning/REQUIREMENTS.md` FIX-07x traceability rows match the Confirmed count from 11-VERIFICATION.md
- FIX-05 and FIX-06 content unchanged (`git diff .planning/REQUIREMENTS.md` shows edits ONLY in Stability-Contingent section, Traceability SPIKE/FIX-07 rows, and the Last-updated footer — no edits elsewhere)
</verification>

<success_criteria>
- CONTEXT D-04 satisfied: FIX-07 split into sub-requirements for Confirmed claims only
- CONTEXT D-07 satisfied: terminal artifact 11-VERIFICATION.md exists with 4-column table
- CONTEXT D-08 observed: failing tests for Confirmed bugs remain committed RED for Phase 12
- ROADMAP Phase 11 success criteria 1-4 all met at Phase 11 close:
  1. Each of C1–C5 has a .flow test OR a written dismissal — YES
  2. `AUDIT-VERIFIED 2026-04-18` marker present in source for every claim — YES (grep returns 6 hits)
  3. Outcome determines FIX-07 scope — YES (REQUIREMENTS.md split)
  4. No production source modified beyond single-line comments — YES (plans 11-01..11-05 self-enforce)
- Phase 12 has a decisive, machine-readable handoff
</success_criteria>

<output>
Create `.planning/phases/11-audit-spike/11-06-SUMMARY.md` with:
1. **Confirmed claims** (list of FIX-07x sub-requirements created)
2. **Dismissed claims** (list of Cs closed by marker-only)
3. **REQUIREMENTS.md diff summary** (edits made, files touched)
4. **Handoff to Phase 12** (reference 11-VERIFICATION.md + updated REQUIREMENTS.md)
5. **BREAKING CHANGE scope** (YES if FIX-07e exists with swap Confirmed; NO otherwise)
</output>
