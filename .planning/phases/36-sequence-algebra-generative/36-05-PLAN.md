---
phase: 36-sequence-algebra-generative
plan: 05
type: execute
wave: 4
depends_on: ["36-01", "36-02"]
files_modified:
  - flow-lang/StandardLibrary/Patterns/PatternFunctions.cs
  - flow-lang/Core/FlowEngine.cs
  - flow-lang/patterns.flow
  - flow-lang/std.flow
  - flow-lang.Tests/Phase36/PatternEveryTests.cs
  - flow-lang.Tests/Phase36/PatternChalkyEdgeCasesTests.cs
  - flow-lang.Tests/Phase36/PatternDeterminismTests.cs
  - tests/test_patterns_every.flow
  - tests/test_patterns_chain.flow
  - tests/test_patterns_edge_cases.flow
autonomous: true
modified: 2026-05-20
requirements: [PAT-01, PAT-02, GEN-05]
must_haves:
  truths:
    - "Composer can call all 13 combinators on a Sequence: every, fast, slow, chunk, phase, rev, jux, sometimes, iter, palindrome, degrade, superimpose, sparseSeq (D-36-01 + D-36-02)"
    - "Cycle-dependent combinators (every, chunk, phase) operate on bars per D-36-04"
    - "Transform-arg combinators (every, chunk, sometimes, jux, superimpose) accept a lambda — D-36-03 lambda-required style; no partial application"
    - "Stochastic combinators (sometimes, degrade, sparseSeq, jux-when-stochastic) route PRNG through `PrngRegistry.GetRandom(callSite, name)` — zero `new Random()` constructions in PatternFunctions.cs"
    - "Charitable interpretation on degenerate input (PAT-02 + Pitfall 2 + Pitfall 9): zero-length seq, n<=0, prob outside [0,1] → returns input unchanged + one-shot stderr advisory via RenderingDiagnostics.WarnOnce"
    - "Two-run cmp-clean: `scripts/test_two_run_determinism.sh tests/test_patterns_chain.flow` exits 0 (SHA-256 identical)"
    - "`@patterns` stdlib module exists at flow-lang/patterns.flow with `internal proc` forward-declarations for all 13 combinators"
  artifacts:
    - path: flow-lang/StandardLibrary/Patterns/PatternFunctions.cs
      provides: "13 combinator implementations + Register hook"
      contains: "class PatternFunctions"
    - path: flow-lang/patterns.flow
      provides: "@patterns stdlib module"
    - path: tests/test_patterns_chain.flow
      provides: "Composer-facing exercise of all 13 combinators in one playable file"
  key_links:
    - from: flow-lang/Core/FlowEngine.cs
      to: PatternFunctions.RegisterContextDependent
      via: "engine init"
      pattern: "PatternFunctions"
    - from: flow-lang/StandardLibrary/Patterns/PatternFunctions.cs
      to: PrngRegistry.GetRandom
      via: "stochastic combinator dispatch"
      pattern: "PrngRegistry"
---

<objective>
Ship the `@patterns` stdlib module with all 13 Tidal-style combinators per D-36-01 and D-36-02 (12 from REQUIREMENTS.md PAT-01 plus the Flow-native `sparseSeq` helper). Cycle unit is BARS per D-36-04. Lambda-required transform-arg style per D-36-03. All PRNG routed through Phase 36 Plan 01's PrngRegistry. Charitable interpretation per PAT-02 + Pitfalls 2 and 9.

Purpose: Composer surface `seq -> (every 4 (fn s => (fast s 2))) as varied -> (sometimes 0.3 rev) -> (jux (fn s => (transpose s 7))) as stereo -> render` is the headline ergonomic from RESEARCH §Code Examples. Without `@patterns`, Phase 36 has no PAT-01 deliverable.

Output: PatternFunctions.cs with 13 combinators; patterns.flow stdlib module; xUnit + composer tests; two-run determinism verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-sequence-algebra-generative/36-CONTEXT.md
@.planning/phases/36-sequence-algebra-generative/36-RESEARCH.md
@.planning/phases/36-sequence-algebra-generative/36-PATTERNS.md
@.planning/phases/36-sequence-algebra-generative/36-01-SUMMARY.md
@.planning/phases/36-sequence-algebra-generative/36-02-SUMMARY.md

# Source-of-truth analog files
@flow-lang/StandardLibrary/Transforms/TransformFunctions.cs
@flow-lang/StandardLibrary/Composition/VariationFunctions.cs
@flow-lang/StandardLibrary/Collections/DictFunctions.cs
@flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
@flow-lang/Runtime/PrngRegistry.cs
@flow-lang/Diagnostics/RenderingDiagnostics.cs
@flow-lang/std.flow
@flow-lang/Runtime/Value.cs
@tests/test_generative.flow

<interfaces>
Combinator implementation table (RESEARCH lines 437-453):

| Combinator | Signature | PRNG? | Cycle? |
|------------|-----------|-------|--------|
| every | (Int n, Fn, Sequence) → Sequence | no | yes (bars) |
| fast | (Sequence, Double factor) → Sequence | no | no |
| slow | (Sequence, Double factor) → Sequence | no | no |
| chunk | (Int n, Fn, Sequence) → Sequence | no | yes (bars) |
| phase | (Double offset, Sequence) → Sequence | no | yes (bars) |
| rev | (Sequence) → Sequence | no | no |
| jux | (Fn, Sequence) → Sequence | no (deterministic fn) | no |
| sometimes (overload A) | (Double prob, Fn, Sequence) → Sequence | **yes** | no | (D-36-05 — explicit prob)
| sometimes (overload B, default) | (Fn, Sequence) → Sequence | **yes** (prob=0.5) | no | (D-36-05 — default-arg overload for ergonomic shortcut)
| iter | (Int n, Sequence) → Sequence | no | no |
| palindrome | (Sequence) → Sequence | no | no |
| degrade | (Sequence) → Sequence | **yes** (fixed 50%) | no |
| sparseSeq | (Double prob, Sequence) → Sequence | **yes** | no |
| superimpose | (Fn, Sequence) → Sequence | no | no |

Lambda invocation pattern (copy from `Collections/DictFunctions.cs:41-46`):
```
private static Value InvokeCallback(ExecutionContext context, FunctionOverload cb, List<Value> args)
{
    return cb.IsInternal
        ? cb.Implementation!(args)
        : context.Invoker!.ExecuteUserFunctionWithCaptures(cb.Declaration!, args, cb.CapturedVariables);
}
```

Registration shape (lambda invocation requires ExecutionContext, so use `RegisterContextDependent(registry, context)` shape from `HarmonyFunctions.cs:23-27`, NOT the bare `Register(registry)` shape).

PRNG access pattern (Plan 36-01 deliverable):
```
var rng = context.PrngRegistry.GetRandom(callSite, "<combinator-name>");
```
The `callSite` SourceLocation must come from the FunctionCallExpression's Span/Location at the call site. Stash this via the existing args-and-context flow — if the registry signature doesn't accept SourceLocation, EXTEND `InternalFunctionRegistry.Register` overload to pass `SourceLocation` through, OR use the active call site stored on `ExecutionContext.CurrentCallSite` (verify which exists; if neither, the simplest path is to thread SourceLocation via a context property updated at evaluator dispatch — see how Phase 35 PatternMatcher does it).

Charitable interpretation pattern (Diagnostics/RenderingDiagnostics.cs):
```
RenderingDiagnostics.WarnOnce($"{combinatorName}:invalid:{srcLoc}",
    "<combinator>: <condition>; sequence unchanged");
return Value.Sequence(seq);  // input passthrough
```

SequenceData shape (read flow-lang/Runtime/NoteStreamCompiler.cs + ClassicalComposition.cs):
- `SequenceData.Bars: List<BarData>` — cycle-dependent combinators index into this
- `BarData.Notes: List<MusicalNoteData>` — note-level operations
- `BarData.ParallelVoices: List<BarData>?` — Phase 28 voice-block support; `jux` and `superimpose` may use this

`patterns.flow` shape (analog: std.flow lines 135-150 Transforms forward-decls):
```
Note: Phase 36 PAT-01 — 13 Tidal-style combinators on Sequence
internal proc every (Int: n, Function: fn, Sequence: seq)
internal proc fast (Sequence: seq, Double: factor)
internal proc slow (Sequence: seq, Double: factor)
internal proc chunk (Int: n, Function: fn, Sequence: seq)
internal proc phase (Double: offset, Sequence: seq)
internal proc rev (Sequence: seq)
internal proc jux (Function: fn, Sequence: seq)
internal proc sometimes (Double: prob, Function: fn, Sequence: seq)
internal proc sometimes (Function: fn, Sequence: seq)
internal proc iter (Int: n, Sequence: seq)
internal proc palindrome (Sequence: seq)
internal proc degrade (Sequence: seq)
internal proc sparseSeq (Double: prob, Sequence: seq)
internal proc superimpose (Function: fn, Sequence: seq)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PatternFunctions.cs — 10 deterministic combinators (every/fast/slow/chunk/phase/rev/iter/palindrome/jux/superimpose) + Wave 0 RED xUnit stubs</name>
  <files>
    flow-lang/StandardLibrary/Patterns/PatternFunctions.cs (NEW),
    flow-lang/Core/FlowEngine.cs (engine init wiring),
    flow-lang/patterns.flow (NEW),
    flow-lang/std.flow (add `use "@patterns"` if convention dictates; otherwise leave standalone),
    flow-lang.Tests/Phase36/PatternEveryTests.cs (NEW),
    tests/test_patterns_every.flow (NEW)
  </files>
  <read_first>
    flow-lang/StandardLibrary/Transforms/TransformFunctions.cs (legato/portamento RegisterContextDependent shape at lines 50-73),
    flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (RegisterContextDependent at lines 23-66),
    flow-lang/StandardLibrary/Collections/DictFunctions.cs (InvokeCallback at lines 41-46),
    flow-lang/Core/FlowEngine.cs (locate the existing RegisterXxx calls; add PatternFunctions.RegisterContextDependent in the same block),
    flow-lang/Runtime/Value.cs (Value.Sequence factory),
    flow-lang/Runtime/NoteStreamCompiler.cs (SequenceData + BarData shapes),
    flow-lang/std.flow (lines 135-150 Transforms forward-decl style),
    .planning/phases/36-sequence-algebra-generative/36-PATTERNS.md (PatternFunctions section)
  </read_first>
  <behavior>
    - Wave 0 stubs (RED before implementation):
      - PatternEveryTests.EveryAppliesFnAtCycleBoundary: source `(every 2 (fn s => (fast s 2)) | C4q D4q | E4q F4q | G4q A4q |)` — bars 0 and 2 (mod 2 == 0) have durations halved; bar 1 stays. Inspect resulting SequenceData
      - PatternEveryTests.EveryCharitablyIgnoresZeroN: `(every 0 (fn s => (rev s)) | C4q D4q |)` returns the input sequence unchanged; stderr advisory emitted (check via RenderingDiagnostics test hook)
      - PatternEveryTests.EveryCharitablyIgnoresEmptySeq: `(every 4 (fn s => (rev s)) <empty>)` returns empty + advisory
      - PatternEveryTests.FastHalvesDurations: `(fast | C4q D4q | 2.0)` halves note durations
      - PatternEveryTests.SlowDoublesDurations: `(slow | C4q D4q | 2.0)` doubles note durations
      - PatternEveryTests.RevReversesBarOrder: `(rev | C4q D4q | E4q F4q |)` returns bars in reversed order
      - PatternEveryTests.IterRotatesByNSteps: `(iter 2 | C4q D4q E4q F4q | G4q A4q B4q C5q |)` rotates the note list; pin via output structure
      - PatternEveryTests.PalindromeConcatenatesSeqAndRev: `(palindrome | C4q D4q |)` returns bars `[C4q D4q]` + `[D4q C4q]`
      - PatternEveryTests.ChunkApplyOneChunkPerCycle: `(chunk 4 fn seq)` divides into 4 chunks, applies fn to 1 per cycle (rotating). Pin a 4-bar input with a transpose fn
      - PatternEveryTests.PhaseRotatesBars: `(phase 0.5 4-bar-seq)` rotates by 2 bars (0.5 × 4)
      - PatternEveryTests.JuxLayersOriginalAndFn: `(jux (fn s => (transpose s 7)) seq)` produces a voice block with original + fn(seq)
      - PatternEveryTests.SuperimposeLayersOriginalAndFn: same shape as jux for v1.5
    - Composer test `tests/test_patterns_every.flow`: register 3-4 `(test ...)` blocks exercising every / fast / rev with assertions via assertNotesMatch
  </behavior>
  <action>
    1. Create `flow-lang/StandardLibrary/Patterns/PatternFunctions.cs` with `public static class PatternFunctions` and `public static void RegisterContextDependent(InternalFunctionRegistry registry, ExecutionContext context)`. Pattern verbatim from `HarmonyFunctions.cs:23-66`.

    2. Implement the 10 deterministic combinators (every, fast, slow, chunk, phase, rev, iter, palindrome, jux, superimpose). Each:
       - Builds FunctionSignature with `ParameterNames: [...]` per the table in `<interfaces>`
       - Registers a lambda that:
         a. Extracts typed args (`args[N].As<X>()`)
         b. Applies charitable-interpretation guards (n<=0, prob outside [0,1], empty seq) → returns input + WarnOnce
         c. Applies the transform per the table
         d. Wraps result via `Value.Sequence(new SequenceData(newBars))`
       - For lambda-bearing combinators, use the `InvokeCallback` pattern from `DictFunctions.cs:41-46`

    3. Cycle math (every, chunk, phase) operates on bars per D-36-04:
       - `every n fn seq`: iterate `seq.Bars`; for bar i where `i % n == 0`, wrap bar i in a synthetic single-bar SequenceData, invoke fn, replace bar i with fn's output bar(s)
       - `chunk n fn seq`: divide `seq.Bars.Count` into n equal chunks; on each cycle, apply fn to the next chunk in rotation; track rotation state per-call via PrngRegistry hash (deterministic per source position) OR derive from a frame counter — simplest: apply to chunk 0 on first invocation, advance via a static counter keyed by SourceLocation
       - `phase offset seq`: rotate bars by `(int)(offset * seq.Bars.Count)` positions

    4. Wire `PatternFunctions.RegisterContextDependent(registry, context)` into `FlowEngine.cs` engine init alongside existing `HarmonyFunctions.RegisterContextDependent`, `TransformFunctions.Register`, etc.

    5. Create `flow-lang/patterns.flow` per the shape in `<interfaces>`. Verify it loads via `use "@patterns"`.

    6. Create composer-facing `tests/test_patterns_every.flow` with 3-4 `(test ...)` blocks.

    7. Create xUnit `flow-lang.Tests/Phase36/PatternEveryTests.cs` with all 12 facts above.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj 2>&amp;1 | tail -5; dotnet test --filter "FullyQualifiedName~Phase36.PatternEveryTests" 2>&amp;1 | tail -20</automated>
  </verify>
  <done>flow-lang.csproj builds; 12 PatternEveryTests facts GREEN; `use "@patterns"` resolves; 10 combinators registered (verify via `grep -c "registry.Register" flow-lang/StandardLibrary/Patterns/PatternFunctions.cs` ≥ 10); no `new Random(` in PatternFunctions.cs (this task ships ONLY deterministic combinators).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stochastic combinators (sometimes/degrade/sparseSeq) routed via PrngRegistry + edge-case + determinism tests</name>
  <files>
    flow-lang/StandardLibrary/Patterns/PatternFunctions.cs (append),
    flow-lang.Tests/Phase36/PatternChalkyEdgeCasesTests.cs (NEW),
    flow-lang.Tests/Phase36/PatternDeterminismTests.cs (NEW),
    tests/test_patterns_chain.flow (NEW),
    tests/test_patterns_edge_cases.flow (NEW)
  </files>
  <read_first>
    flow-lang/StandardLibrary/Patterns/PatternFunctions.cs (from Task 1),
    flow-lang/Runtime/PrngRegistry.cs (from Plan 36-01),
    flow-lang/StandardLibrary/Composition/VariationFunctions.cs (PRNG threading idiom — VarySeeded vs VaryRandom),
    flow-lang/Diagnostics/RenderingDiagnostics.cs (WarnOnce + ResetForTesting),
    flow-lang.Tests/Phase35/HermeticIsolationTests.cs (WarnOnce reset between tests pattern),
    .planning/phases/36-sequence-algebra-generative/36-RESEARCH.md (§Common Pitfalls 1 + 2 + 9)
  </read_first>
  <behavior>
    - PatternDeterminismTests.SometimesRoutesPrng: `(sometimes 0.5 (fn s => (rev s)) <fixed-4-bar-seq>)` evaluated twice in the same process (with PrngRegistry.ResetAtRenderBoundary between calls) produces identical results — pin via assertEq on the resulting Sequence
    - PatternDeterminismTests.DegradeRoutesPrng: same shape for degrade
    - PatternDeterminismTests.SparseSeqRoutesPrng: same shape for sparseSeq
    - PatternDeterminismTests.NoNewRandomInPatternFunctions: source-grep gate — read `flow-lang/StandardLibrary/Patterns/PatternFunctions.cs` text and Assert zero `new Random(` occurrences (excluding `//`-prefixed comments)
    - PatternChalkyEdgeCasesTests.FastZeroFactor: `(fast seq 0.0)` returns input unchanged + WarnOnce
    - PatternChalkyEdgeCasesTests.SlowZeroFactor: `(slow seq 0.0)` returns input unchanged + WarnOnce
    - PatternChalkyEdgeCasesTests.ChunkZeroN: `(chunk 0 fn seq)` returns input + WarnOnce
    - PatternChalkyEdgeCasesTests.PhaseNonFiniteOffset: `(phase NaN seq)` returns input + WarnOnce
    - PatternChalkyEdgeCasesTests.SometimesProbOutOfRange: `(sometimes 1.5 fn seq)` clamps to [0,1] OR raises advisory + falls through; pick the charitable path (clamp + WarnOnce)
    - PatternChalkyEdgeCasesTests.EmptySequenceUnchanged: empty seq through every stochastic + deterministic combinator returns empty + advisory (parameterize via theory across combinator names)
    - Composer-facing `tests/test_patterns_chain.flow`: register `(test ...)` blocks exercising ALL 13 combinators in one playable file; render via `flow render` produces a WAV
    - Composer-facing `tests/test_patterns_edge_cases.flow`: register `(test ...)` blocks for the edge cases above
  </behavior>
  <action>
    1. **PatternFunctions.cs append:** add 3 (effectively 4 with sometimes overload) stochastic combinators:
       - `sometimes(Double prob, Function fn, Sequence seq)`: for each bar, draw `rng.NextDouble()` and apply fn to that bar if `< prob`. PRNG: `var rng = context.PrngRegistry.GetRandom(callSite, "sometimes");` per Plan 36-01.
       - `sometimes(Function fn, Sequence seq)` (default-prob overload): identical to above with prob=0.5
       - `degrade(Sequence seq)`: for each bar, draw `rng.NextDouble()`; KEEP bar if ≥ 0.5, DROP otherwise. PRNG name `"degrade"`.
       - `sparseSeq(Double prob, Sequence seq)`: same as degrade but with composer-supplied drop probability. PRNG name `"sparseSeq"`.

    2. **SourceLocation threading:** the lambda passed to `registry.Register` receives `args` but NOT a SourceLocation. Solutions:
       - Option A (simplest): extend `InternalFunctionRegistry.Register` to accept `Func<IReadOnlyList<Value>, SourceLocation, Value>` AND keep the existing `Func<IReadOnlyList<Value>, Value>` overload — evaluator passes the call-site SourceLocation through to the lambda
       - Option B: use `context.CurrentCallSite` (a new property on ExecutionContext set by ExpressionEvaluator before each builtin invocation)
       - Choose Option A if simpler; Option B if ExpressionEvaluator already tracks call-site for diagnostics

       Document the choice in the plan SUMMARY.

    3. Charitable interpretation per combinator (Pitfall 9 — empty seq; Pitfall 2 — invalid args):
       - All combinators: empty seq → return + WarnOnce sentinel `f"{name}:empty:{srcLoc}"`
       - `fast`/`slow`: factor == 0 → return + WarnOnce
       - `every`/`chunk`: n <= 0 → return + WarnOnce
       - `phase`: !double.IsFinite(offset) → return + WarnOnce
       - `sometimes`/`sparseSeq`: clamp prob to [0.0, 1.0] + WarnOnce if clamped

    4. Create `flow-lang.Tests/Phase36/PatternChalkyEdgeCasesTests.cs` with edge-case facts; use `[Collection]` setup calling `RenderingDiagnostics.ResetForTesting()` per Phase 35 hermetic-isolation pattern.

    5. Create `flow-lang.Tests/Phase36/PatternDeterminismTests.cs` with 4 facts:
       - Three two-call-with-reset tests for sometimes/degrade/sparseSeq
       - One source-grep test asserting zero `new Random(` in PatternFunctions.cs (reuses the PrngRegistryNewRandomGateTests pattern from Plan 36-01 but scoped to this single file)

    6. Create composer-facing `tests/test_patterns_chain.flow`:
       - Chain all 13 combinators in one Sequence-building expression
       - End with `writeWav` to render
       - This file is invoked by `scripts/test_two_run_determinism.sh` in the Phase gate

    7. Create composer-facing `tests/test_patterns_edge_cases.flow` exercising charitable paths.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~Phase36.Pattern" 2>&amp;1 | tail -25; bash scripts/test_two_run_determinism.sh tests/test_patterns_chain.flow 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All Phase36.Pattern* facts GREEN; `bash scripts/test_two_run_determinism.sh tests/test_patterns_chain.flow` exits 0 with matching SHA-256; `grep -v '^[[:space:]]*//' flow-lang/StandardLibrary/Patterns/PatternFunctions.cs | grep -c 'new Random('` returns 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer-source → stochastic combinator | PRNG state must NOT leak across two-run boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11 | Integrity (determinism) | Stochastic combinators using `new Random()` from wall-clock | mitigate | All stochastic paths route through `PrngRegistry.GetRandom(callSite, name)`; PatternDeterminismTests.NoNewRandomInPatternFunctions enforces source-grep gate |
| T-36-12 | DoS | Composer passes NaN/Infinity to fast/slow/phase | mitigate | Charitable advisory + input passthrough; never throws |
| T-36-13 | Integrity | Empty sequence silently swallows transforms | mitigate | Pitfall 9 charitable advisory ensures composer hears the WarnOnce on stderr |
</threat_model>

<verification>
- `dotnet test --filter "Phase36.Pattern"` → all facts GREEN (~20 total)
- `bash scripts/test_two_run_determinism.sh tests/test_patterns_chain.flow` → exit 0
- `flow test tests/test_patterns_chain.flow` → exit 0
- `flow test tests/test_patterns_edge_cases.flow` → exit 0
- `grep -v '^[[:space:]]*//' flow-lang/StandardLibrary/Patterns/PatternFunctions.cs | grep -c 'new Random('` → 0

## Determinism Verification

Two-run cmp-clean on `tests/test_patterns_chain.flow` (exercises all 13 combinators including 4 PRNG paths) must produce byte-identical WAV+MIDI output. Failure indicates either a missing PrngRegistry route or a wall-clock fallback path.
</verification>

<success_criteria>
- All 13 combinators registered + invocable via `use "@patterns"`
- Stochastic paths deterministic via PrngRegistry
- Charitable interpretation on all degenerate paths
- xUnit + composer tests GREEN
- Two-run determinism verified
</success_criteria>

<output>
Create `.planning/phases/36-sequence-algebra-generative/36-05-SUMMARY.md` when done — document the SourceLocation-threading choice (Option A vs B), the chunk-rotation-state mechanism, and any combinator semantics that needed clarification beyond RESEARCH.
</output>
