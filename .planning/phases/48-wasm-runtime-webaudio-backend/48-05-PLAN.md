---
phase: 48-wasm-runtime-webaudio-backend
plan: 05
type: execute
wave: 4
depends_on: ["48-04"]
files_modified:
  - flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs
  - flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs
  - .planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md
autonomous: true
requirements:
  - REQ-WASM-SIZE-01
  - REQ-WASM-DET-01

must_haves:
  truths:
    - "Compressed bundle size measured against ≤15 MB target per D-48-05"
    - "Measured size + per-file breakdown recorded in 48-BUNDLE-SIZE.md"
    - "If compressed size ≤15 MB: monolithic ship path confirmed; lazy-loading deferred to v1.6 per D-48-05 conditional"
    - "If compressed size >15 MB: lazy-load scaffold documented (no implementation required this plan — flagged for Plan 48-07 closer to schedule v1.6 follow-up OR escalate)"
    - "Two-run determinism preserved: same Flow source executed twice through the published WASM produces byte-identical Float32Array output (when audio capture is wired) OR byte-identical stdout/RunResult JSON serialization"
  artifacts:
    - path: ".planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md"
      provides: "Measured uncompressed + Brotli-compressed bundle size + per-file breakdown + lazy-load decision"
      min_lines: 30
    - path: "flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs"
      provides: "Brotli-compress simulation of every AppBundle/_framework/ file; sum into total compressed bytes; soft-assert ≤15 MB target + hard-assert ≤30 MB"
      exports: ["CompressedBundle_BelowTargetSize", "BundleSizeReport_WrittenToDisk"]
    - path: "flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs"
      provides: "Two-run cmp-clean determinism via captured RunResult.stdout serialized JSON byte equality"
      exports: ["SameSource_TwoRuns_IdenticalStdout", "SameSource_TwoRuns_IdenticalRunResultJson"]
  key_links:
    - from: "flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs"
      to: "AppBundle/_framework/* files"
      via: "Brotli.Compress(File.ReadAllBytes(f)) via System.IO.Compression.BrotliStream"
      pattern: "BrotliStream"
    - from: "flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs"
      to: "flow-lang/Runtime/WasmEntry.cs RunFromJs"
      via: "two invocations of the same source — JSON output byte-cmp"
      pattern: "RunFromJs.*RunFromJs"
---

<objective>
Measure the actual compressed bundle size produced by Plan 48-04's `dotnet publish`, decide whether D-48-05 lazy-loading is needed, and pin two-run determinism for the offline-render path.

Purpose: D-48-05 explicitly defers the lazy-load decision to a real measurement. RESEARCH §4 says ≤15 MB compressed is plausible-but-tight; Plan 48-01's hard cap of 30 MB uncompressed is the conservative gate. Plan 48-05 runs Brotli compression on every AppBundle/_framework/ file and computes the actual compressed total — the number Phase 49 will cite when explaining "first-paint cost".

Output:
- `48-BUNDLE-SIZE.md` — measurement record: uncompressed total, Brotli-compressed total, per-file top-10 by size, lazy-load decision.
- `BundleSizeBudgetTests.cs` — 2 facts (compressed total below target; report written).
- `WasmDeterminismTests.cs` — 2 facts pinning that the same Flow source executed twice produces byte-identical RunResult JSON (D-48-16).

**Scope boundary**: lazy-load IMPLEMENTATION (per-stdlib WASM module splits + `runtime.loadStdlib(name)` API) is deferred unless the measurement exceeds 15 MB compressed. If under: monolithic ship; document in 48-BUNDLE-SIZE.md that v1.6 may revisit. If over: Plan 48-05 closes with an `## ESCALATION: Lazy-Load Required` section in 48-BUNDLE-SIZE.md flagging Phase 48 scope split — composer chooses to either expand Plan 48-05 in-place or split to Plan 48-05.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-04-PLAN.md
@flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs

<interfaces>
<!-- Brotli compression is available via System.IO.Compression.BrotliStream (BCL,
     .NET 5+, no new package). The compression level affects size — production Brotli
     is level 11 (max); use that for accurate measurement. -->

BCL: System.IO.Compression.BrotliStream
- new BrotliStream(targetStream, CompressionLevel.SmallestSize) — equivalent to Brotli level 11.
- Pattern: read file bytes → write to BrotliStream wrapping MemoryStream → read MemoryStream.Length as compressed size.

From flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs (Plan 48-01):
- The `RunDotnetPublish` helper + `FindRepoRoot` — Plan 48-05 reuses (either copy verbatim or extract to a shared Phase48 harness).
- AppBundle path: `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/`
- Framework dir: `AppBundle/_framework/`

From CLAUDE.md ## Conventions:
- "Two-run cmp-clean determinism": same Flow source → byte-identical output. Two-run cmp-clean is the load-bearing contract for Phase 28+.
- D-48-16 carryforward: same Flow source → byte-identical Float32Array output across runtime.run() calls.

D-48-16 verification surface (Plan 48-05):
- Can't easily byte-cmp Float32Array audio from a Desktop test (no browser); CAN byte-cmp RunResult.stdout JSON which captures the Flow program's textual output deterministically.
- Two-run RunResult JSON match is the strongest determinism signal available without browser instrumentation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land BundleSizeBudgetTests + write 48-BUNDLE-SIZE.md measurement record</name>
  <files>flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs, .planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md</files>
  <read_first>
    - flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs (Plan 48-01 — reuse RunDotnetPublish + FindRepoRoot helpers)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md D-48-05 + Specific Idea 5
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md Finding #4 (≤15 MB plausible-but-tight)
  </read_first>
  <behavior>
    **BundleSizeBudgetTests.cs** (2 facts):

    1. `CompressedBundle_BelowTargetSize`:
       - Runs `dotnet publish -p:FlowTarget=Web -c Release` (reuse Plan 48-01 helper).
       - Enumerates every file under `AppBundle/_framework/**` (recursive).
       - For each file: reads bytes, compresses via `new BrotliStream(memoryStream, CompressionLevel.SmallestSize)`, records compressed length.
       - Sums all compressed lengths into `totalCompressedBytes`.
       - Soft-asserts via `Console.WriteLine($"[BundleSize] compressed total: {totalCompressedBytes / 1_000_000} MB ({totalCompressedBytes} bytes) — D-48-05 target ≤15 MB")`.
       - Hard-asserts `Assert.True(totalCompressedBytes < 20 * 1024 * 1024, $"Compressed bundle {totalCompressedBytes/1_000_000} MB exceeds 20 MB hard cap (15 MB target + 5 MB v1 latitude). D-48-05 lazy-load REQUIRED.")` — the 20 MB hard cap leaves 5 MB headroom over the 15 MB target before forcing an escalation.

    2. `BundleSizeReport_WrittenToDisk`:
       - Same enumeration as Fact 1.
       - Sort files by compressed size descending; take top 20.
       - Write Markdown report to `.planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md` (the same file as the artifact for this plan — the test BOTH measures AND writes the artifact).
       - Report format: see §"48-BUNDLE-SIZE.md format" below.
       - Assert the file exists and is non-empty after write.

    Both tests are plain `[Fact]` (run on Desktop; they shell to dotnet publish + read filesystem).

    **48-BUNDLE-SIZE.md format** (~30-60 lines):
    ```markdown
    # Phase 48 — WASM Bundle Size Measurement

    **Generated:** $(date)
    **Source SHA:** (git rev-parse HEAD)
    **Publish command:** `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release`

    ## Totals

    | Metric | Value |
    |--------|-------|
    | Uncompressed total | N MB |
    | Brotli-compressed total | N MB |
    | D-48-05 target (compressed) | ≤15 MB |
    | Hard cap (compressed) | 20 MB |

    ## Top 20 Files by Compressed Size

    | File | Uncompressed | Brotli | Compression % |
    |------|-------------:|-------:|--------------:|
    | dotnet.wasm | X MB | Y MB | Z% |
    | flow-lang.dll | X MB | Y MB | Z% |
    | ... |

    ## D-48-05 Lazy-Load Decision

    [ONE of:]
    - **Under target (≤15 MB compressed): MONOLITHIC SHIP.** v1.6 may revisit if any single
      Phase 36 / Phase 39 stdlib grows. Phase 48 ships without runtime.loadStdlib(name) split.
    - **Over target (>15 MB compressed, ≤20 MB hard cap): SHIP WITH FOLLOW-UP.** Document the
      excess in this report; Plan 48-07 closer schedules a v1.6 lazy-load follow-up.
    - **OVER HARD CAP (>20 MB compressed): ESCALATION REQUIRED.** This plan returns ## PHASE
      SPLIT RECOMMENDED to the orchestrator. Plan 48-05.1 introduces runtime.loadStdlib(name)
      + per-stdlib WASM module split for @patterns, @generative, @improv, @notation-io.
    ```
  </behavior>
  <action>
**Create `flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs`:**

Imports: `System`, `System.IO`, `System.IO.Compression`, `System.Linq`, `System.Text`, `Xunit`.

Two `[Fact]` methods per `<behavior>`. Helper: `BrotliCompressedLength(byte[] bytes)`:
```
using var ms = new MemoryStream();
using (var brotli = new BrotliStream(ms, CompressionLevel.SmallestSize))
{
    brotli.Write(bytes, 0, bytes.Length);
}
return ms.Length;
```

The `RunDotnetPublish` + `FindRepoRoot` helpers from Plan 48-01's WasmBuildPipelineTests can be copy/pasted (Rule 1 deviation latitude per CLAUDE.md — 4 LOC of duplication is acceptable; alternative is to extract to a shared `Phase48TestHarness` static class which executor MAY do).

For Fact 2: gather top 20 entries; format as Markdown table with right-aligned size columns. The report path: `Path.Combine(repoRoot, ".planning", "phases", "48-wasm-runtime-webaudio-backend", "48-BUNDLE-SIZE.md")`. The test WRITES the file — it is the artifact. The decision block at the bottom is auto-populated based on `totalCompressedBytes`:
- if `< 15 MB`: "MONOLITHIC SHIP" decision branch
- if `15 MB ≤ ≤ 20 MB`: "SHIP WITH FOLLOW-UP" decision branch  
- if `> 20 MB`: "ESCALATION REQUIRED" branch + the test FAILS Fact 1 (hard cap exceeded), forcing the planner to revisit

The test does NOT do `dotnet publish` twice — Fact 2 reuses Fact 1's published AppBundle. To avoid the second publish call in Fact 2, gate Fact 2 by reading the AppBundle directly (assume Fact 1 ran first OR call publish once in a class fixture). Simplest: each Fact runs its own publish; the test machine accepts the doubled cost (~30s each). Optional optimization: use xUnit `IClassFixture` to share a single publish across both facts.

**Create `48-BUNDLE-SIZE.md` (initial placeholder)**:
The actual file content gets WRITTEN by Fact 2 at test-run time. Create an initial placeholder with the template above (without numbers) so the file exists in the repo before the first test run; the test OVERWRITES it on each invocation.

The placeholder header should include `**Status:** Pending first measurement — run \`dotnet test --filter Phase48.BundleSizeBudgetTests\` to populate.`
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase48.BundleSizeBudgetTests" -v normal --nologo 2>&amp;1 | tail -20 &amp;&amp; test -f .planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md &amp;&amp; wc -l .planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-lang.Tests/Integration/Phase48/BundleSizeBudgetTests.cs` exists, compiles.
    - File contains exactly 2 `[Fact]` attributes.
    - File contains `using System.IO.Compression;` and `BrotliStream`.
    - File contains `CompressionLevel.SmallestSize`.
    - Running the test invokes both facts; Fact 1 + Fact 2 either both PASS (compressed total < 20 MB hard cap) OR Fact 1 FAILS with a precise size + escalation message (per `<behavior>` decision branch).
    - After test run, file `.planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md` exists, is at least 30 lines, contains the Totals table with concrete numbers, and contains exactly one of the 3 D-48-05 Decision branches selected.
    - Console output during test contains `[BundleSize] compressed total: N MB`.
  </acceptance_criteria>
  <done>2-fact xUnit suite measures compressed bundle; 48-BUNDLE-SIZE.md auto-generated with decision branch; D-48-05 lazy-load decision recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land WasmDeterminismTests — two-run cmp-clean RunResult JSON byte equality</name>
  <files>flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs</files>
  <read_first>
    - flow-lang/Runtime/WasmEntry.cs (Plan 48-04 — the RunFromJs entry point + RunResult/RunError POCOs)
    - flow-lang.Tests/Integration/Phase48/WebAudioBackendIntegrationTests.cs (Plan 48-03 — cross-target Fact pattern)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md D-48-16 (two-run cmp-clean preserved)
    - CLAUDE.md ## Conventions §"Two-run cmp-clean determinism" + §"Phase 36 chaos primitives — same-platform determinism only (D-36-09)"
  </read_first>
  <behavior>
    Two `[Fact]` methods exercising WasmEntry.RunFromJs DIRECTLY from C# (no browser host required — WasmEntry is a static class accessible from any test that doesn't need [JSImport] to fire; the [JSExport] decoration doesn't prevent direct invocation on Desktop):

    1. `SameSource_TwoRuns_IdenticalStdout`:
       - Source: `"(print \"hello flow\")\n(print 42)\n(print (add 1 2))"` — deterministic, no chaos primitives, no music timing.
       - Call `var json1 = WasmEntry.RunFromJs(source);` twice in succession.
       - Parse both JSON strings via `JsonDocument.Parse(json1).RootElement.GetProperty("stdout").GetString()`.
       - Assert `stdout1 == stdout2` (string equality).
       - Document the same-platform-only caveat per D-36-09 in a comment; this fact does NOT use Lorenz/logistic so cross-platform also holds.

    2. `SameSource_TwoRuns_IdenticalRunResultJson`:
       - Same source as Fact 1.
       - Call RunFromJs twice; the full JSON strings.
       - Strip the `durationMs` field (which legitimately differs across runs — measured wall-clock time).
       - Assert remaining JSON byte-identical.
       - Strip technique: parse JSON, write back to JSON via JsonNode/JsonObject with `durationMs` removed, then `Encoding.UTF8.GetBytes(jsonString)` and `Assert.Equal(bytes1, bytes2)`.

    Both tests are plain `[Fact]` runnable on Desktop. WasmEntry.RunFromJs's [SupportedOSPlatform("browser")] decoration generates CA1416 warnings when called from Desktop — suppress with `#pragma warning disable CA1416 // calling browser-only API from Desktop test for determinism check; FlowEngine path is platform-agnostic` around the calls.

    Per CLAUDE.md ## Conventions: chaos primitives are excluded from cross-platform determinism per D-36-09; THIS test uses no chaos. Pure arithmetic + print = byte-identical across runs on any platform.

    Per Phase 28 dither RNG seeding precedent: deterministic-seeded RNGs do preserve two-run cmp-clean.
  </behavior>
  <action>
**Create `flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs`:**

Imports: `System`, `System.Linq`, `System.Text`, `System.Text.Json`, `System.Text.Json.Nodes`, `FlowLang.Runtime`, `Xunit`.

Namespace: `FlowLang.Tests.Integration.Phase48`.

Class: `public class WasmDeterminismTests`.

Two `[Fact]` methods per `<behavior>`. For the JSON-strip helper:
```
private static string StripDurationMs(string json)
{
    var node = JsonNode.Parse(json)!.AsObject();
    node.Remove("durationMs");
    return node.ToJsonString(new JsonSerializerOptions { WriteIndented = false });
}
```

For Fact 1:
```
#pragma warning disable CA1416
var json1 = WasmEntry.RunFromJs(source);
var json2 = WasmEntry.RunFromJs(source);
#pragma warning restore CA1416
var stdout1 = JsonDocument.Parse(json1).RootElement.GetProperty("stdout").GetString();
var stdout2 = JsonDocument.Parse(json2).RootElement.GetProperty("stdout").GetString();
Assert.Equal(stdout1, stdout2);
```

For Fact 2:
```
#pragma warning disable CA1416
var json1 = WasmEntry.RunFromJs(source);
var json2 = WasmEntry.RunFromJs(source);
#pragma warning restore CA1416
var stripped1 = StripDurationMs(json1);
var stripped2 = StripDurationMs(json2);
Assert.Equal(stripped1, stripped2);
```

Caveat NOTE (in source comment):
- Both facts run on Desktop calling WasmEntry.RunFromJs directly. This exercises the same FlowEngine.Execute path the Web build will run, just without the [JSExport] boundary marshalling. Determinism comes from FlowEngine itself, not from WASM; verifying on Desktop is a valid proxy for the Web behavior. D-36-09 cross-platform caveat applies if and only if the source uses Lorenz/logistic chaos — this test source uses only `print` + `add` so cross-platform also holds.

If WasmEntry.RunFromJs's lazy-initialized `_sharedEngine` causes state contamination between runs (e.g. errors from run 1 polluting run 2's ErrorReporter), then a `DisposeFromJs()` call between the two RunFromJs calls fixes it. Verify: read WasmEntry.cs (Plan 48-04) to confirm the engine is reset OR add a `DisposeFromJs()` between the two `RunFromJs` calls in both facts.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase48.WasmDeterminismTests" -v quiet --nologo 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-lang.Tests/Integration/Phase48/WasmDeterminismTests.cs` exists.
    - File contains exactly 2 `[Fact]` attributes.
    - `dotnet test --filter "FullyQualifiedName~Phase48.WasmDeterminismTests"` reports 2 passed.
    - Both facts call `WasmEntry.RunFromJs` exactly twice.
    - The stripped-durationMs comparison in Fact 2 succeeds (no field-ordering issues — System.Text.Json preserves insertion order).
  </acceptance_criteria>
  <done>2-fact determinism pin lands; same Flow source → byte-identical RunResult JSON (excluding wall-clock durationMs).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brotli compression in test → CI report | reads our own publish output; no external input |
| 48-BUNDLE-SIZE.md auto-generated by test | overwrites a planning artifact every test run; git diff catches unexpected changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-18 | Tampering | 48-BUNDLE-SIZE.md auto-overwrite | accept | The Markdown file is regenerated on every test run; intentional behavior; git history tracks size drift over time |
| T-48-19 | Denial of service | publishing twice in BundleSizeBudgetTests | accept | Each test runs its own publish (~30s wall-clock); xUnit IClassFixture optional optimization for the executor |
| T-48-20 | Information disclosure | bundle file listing in 48-BUNDLE-SIZE.md | accept | Lists OUR OWN published bundle's filenames; not user data; no secrets |
</threat_model>

<verification>
1. `dotnet build flow-lang.Tests -p:FlowTarget=Desktop` exits 0.
2. `dotnet test --filter "FullyQualifiedName~Phase48.BundleSizeBudgetTests"` reports 2 passed (assuming compressed bundle < 20 MB hard cap; if not, Fact 1 fails with actionable escalation message).
3. `dotnet test --filter "FullyQualifiedName~Phase48.WasmDeterminismTests"` reports 2 passed.
4. After the BundleSizeBudgetTests run, `.planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md` exists with concrete numbers + decision branch.
5. Phase 47 + Phase 48-01 + Phase 48-02 + Phase 48-03 + Phase 48-04 facts all still PASS (no regression).
6. No new NuGet packages added (BCL Brotli + System.Text.Json + JsonNode all in .NET 10).
</verification>

<success_criteria>
- D-48-05 lazy-load decision made and recorded in 48-BUNDLE-SIZE.md.
- D-48-16 two-run cmp-clean determinism pinned.
- 4 new xUnit Facts land; all PASS (or Fact 1 fails with escalation per D-48-05 hard cap branch).
- Zero new NuGet packages.
</success_criteria>

<output>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-05-SUMMARY.md` when done. Record: actual compressed total in MB, which D-48-05 decision branch was selected, top-3 file contributors to bundle size, whether two-run determinism passed first try or required _sharedEngine reset between runs.
</output>
