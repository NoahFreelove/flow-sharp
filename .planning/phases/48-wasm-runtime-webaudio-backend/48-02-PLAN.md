---
phase: 48-wasm-runtime-webaudio-backend
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
  - flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs
  - flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs
  - flow-lang.Tests/Integration/Phase48/CultureInvariantSweepTests.cs
autonomous: true
requirements:
  - REQ-WASM-DRYWET-01
  - REQ-WASM-BUILD-05

must_haves:
  truths:
    - "DryWetMidi 8.0.3 assembly is reachable from the published WASM AppBundle (flow-lang.dll still references DryWetMidi types)"
    - "All `.ToUpper()` / `.ToLower()` calls without an explicit CultureInfo arg in flow-lang/ are either fixed to use InvariantCulture/OrdinalIgnoreCase or whitelisted with an inline justification comment"
    - "`writeMidi` builtin still registers and is callable on Web target (DryWetMidi stays in Web build; D-48-17 'WASM-compatible' branch confirmed)"
    - "Phase 47's existing `DryWetMidiWasmCompatTests` (Desktop-side green per REQ-WEB-TARGET-07) extends to the actual published WASM bundle"
  artifacts:
    - path: "flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs"
      provides: "Post-publish AppBundle assertion that flow-lang.dll references DryWetMidi types reachably"
      exports: ["FlowLangDll_PublishedToAppBundle", "FlowLangDll_RetainsDryWetMidiReference"]
    - path: "flow-lang.Tests/Integration/Phase48/CultureInvariantSweepTests.cs"
      provides: "Source-grep gate asserting zero culture-sensitive string operations without explicit InvariantCulture/Ordinal"
      exports: ["NoUnqualifiedToUpper_InProductionCode", "NoUnqualifiedToLower_InProductionCode"]
  key_links:
    - from: "flow-lang/flow-lang.csproj (Phase 48-01)"
      to: "DryWetMidi assembly under AppBundle/_framework/"
      via: "<PackageReference Include=\"Melanchall.DryWetMidi\" Version=\"8.0.3\" /> unconditional (no FlowTarget condition)"
      pattern: "Melanchall\\.DryWetMidi.*Version=\"8\\.0\\.3\""
    - from: "InvariantGlobalization=true (Phase 48-01)"
      to: "culture-sensitive string operations in flow-lang/"
      via: "Turkish-I hazard — `.ToUpper()` without InvariantCulture changes behavior under invariant mode"
      pattern: "ToUpper\\(\\)|ToLower\\(\\)"
---

<objective>
Verify Phase 47-04's Desktop-side DryWetMidi WASM-compat smoke extends to the actual Mono-WASM publish output, AND sweep `flow-lang/` for culture-sensitive string operations that could misbehave under `<InvariantGlobalization>true</InvariantGlobalization>`. Two coordinated changes.

Purpose: D-48-17 says "if DryWetMidi WASM-incompatible, strip from Web build and emit parse-time advisory." Phase 47 Plan 47-04 already verified the API surface is reachable on Desktop; this plan confirms the **published WASM bundle** retains DryWetMidi reachably, AND closes the latent invariant-globalization risk surfaced by D-48-03 (Turkish-I problem: `"file".ToUpper()` returns `"FİLE"` under Turkish locale unless InvariantCulture is passed).

Output:
- 2 culture-sensitive call sites fixed: `HarmonyFunctions.cs:441` (`direction.ToLower()`) → InvariantCulture, `ScaleDatabase.cs:182,233` (`char.ToUpper(rootNote[0]) + rootNote[1..].ToLower()`) → InvariantCulture.
- `flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs` with 2 facts (AppBundle present, flow-lang.dll retains DryWetMidi reachable).
- `flow-lang.Tests/Integration/Phase48/CultureInvariantSweepTests.cs` with 2 source-grep gate facts (no unqualified `ToUpper`/`ToLower` in flow-lang/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-01-PLAN.md
@flow-lang.Tests/Integration/Phase47/DryWetMidiWasmCompatTests.cs
@flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs
@flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
@flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs

<interfaces>
<!-- Phase 47 already established the DryWetMidi WASM-compat smoke at the Desktop level
     (DryWetMidiWasmCompatTests with 2 [FlowTargetFact("Desktop", "Web")] Facts —
     MidiFile_WriteAndRead_RoundTripsMinimalSmf + DryWetMidiAssembly_IsLoadable).
     Phase 48-02 extends this with a POST-PUBLISH AppBundle assertion. -->

From flow-lang.Tests/Integration/Phase47/DryWetMidiWasmCompatTests.cs:
```csharp
[FlowTargetFact("Desktop", "Web")]
public void DryWetMidiAssembly_IsLoadable()
{
    var asm = typeof(MidiFile).Assembly;
    Assert.NotNull(asm);
    Assert.Contains("Melanchall.DryWetMidi", asm.FullName ?? "");
}
```

From flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs (pattern for
Mono.Cecil-based assembly inspection — Phase 48-02 mirrors but inverts the polarity:
"asserts PRESENCE of DryWetMidi" instead of "asserts ABSENCE of Rug.Osc"):
```csharp
private static AssemblyDefinition LoadFlowLangAssembly()
{
    var asmPath = typeof(FlowLang.Core.FlowEngine).Assembly.Location;
    return AssemblyDefinition.ReadAssembly(asmPath);
}
```

Culture-sensitive sites surfaced by grep (verified 2026-05-25):
- flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs:441 — `switch (direction.ToLower())`
- flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs:182 — `rootNote = char.ToUpper(rootNote[0]) + rootNote[1..].ToLower();`
- flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs:233 — `rootNote = char.ToUpper(rootNote[0]) + rootNote[1..].ToLower();`

Whitelisted (already culture-safe — use Ordinal):
- flow-lang/Diagnostics/LevenshteinHelper.cs:79 — `string.CompareOrdinal(name, best) < 0`
- flow-lang/Lexing/PragmaScanner.cs:231 — `string.CompareOrdinal(lineText, p, "enable", 0, 6)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace 3 unqualified ToUpper/ToLower call sites with InvariantCulture overloads</name>
  <files>flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs, flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs</files>
  <read_first>
    - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs (line 441 area — `switch (direction.ToLower())`)
    - flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs (lines 175-200 + lines 225-240 — both `rootNote = ...ToLower()` sites)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md D-48-03 (InvariantGlobalization rationale + Turkish-I hazard)
  </read_first>
  <behavior>
    - After edit, `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs:441` reads `switch (direction.ToLowerInvariant())` (or the equivalent `.ToLower(CultureInfo.InvariantCulture)`).
    - After edit, `flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs:182` and `:233` use `char.ToUpperInvariant(...)` + `.ToLowerInvariant()` (or explicit InvariantCulture overloads).
    - Existing test fixture `ScaleDatabaseTests` and `HarmonyFunctionsTests` (if present) continue to pass — the semantic is "lowercase ASCII letters the same way every time" which is exactly what InvariantCulture does for the ASCII subset Flow scale/direction names use.
    - No additional `using` directives needed (`ToLowerInvariant` / `ToUpperInvariant` live in `System.String` / `System.Char` directly — already imported via implicit usings).
  </behavior>
  <action>
Per D-48-03, the Phase 48-01 build now sets `<InvariantGlobalization>true</InvariantGlobalization>`. Under invariant mode, `.ToUpper()` / `.ToLower()` still work BUT only for the ASCII subset; if a non-ASCII character flows in, results are undefined. The Turkish-I problem manifests when a non-invariant culture is implied. The safe path is to switch every culture-sensitive call site to the explicit `*Invariant` variant.

**Edit 1** — `flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs:441`:

Replace `switch (direction.ToLower())` with `switch (direction.ToLowerInvariant())`.

**Edit 2** — `flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs:182`:

Replace `rootNote = char.ToUpper(rootNote[0]) + rootNote[1..].ToLower();` with `rootNote = char.ToUpperInvariant(rootNote[0]) + rootNote[1..].ToLowerInvariant();`.

**Edit 3** — `flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs:233`:

Same replacement as Edit 2 (the same line appears twice in the file — at 182 and 233 per the 2026-05-25 grep).

After all three edits: `grep -rn "ToUpper()\\|ToLower()" flow-lang/` should return ZERO hits inside `flow-lang/` production source (`scripts/` and `flow-lang.Tests/` excluded — tests may use Desktop-locale-bound culture handling intentionally for testing that very behavior).

Whitelisted (do NOT change):
- `flow-lang/Diagnostics/LevenshteinHelper.cs:79` — uses `string.CompareOrdinal` (already culture-safe).
- `flow-lang/Lexing/PragmaScanner.cs:231` — uses `string.CompareOrdinal` (already culture-safe).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; grep -rn 'ToUpper()\|ToLower()' flow-lang/ 2>/dev/null | grep -v '^\s*//' | wc -l &amp;&amp; dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3 &amp;&amp; dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet --nologo 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'ToUpper()\|ToLower()' flow-lang/ | grep -v '^\s*//' | wc -l` returns 0 (no unqualified culture-sensitive calls remain in production source; comment lines excluded).
    - `grep -c 'ToLowerInvariant\|ToUpperInvariant' flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs` returns at least 1.
    - `grep -c 'ToLowerInvariant\|ToUpperInvariant' flow-lang/StandardLibrary/Harmony/ScaleDatabase.cs` returns at least 2 (two lines fixed at :182 + :233).
    - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exits 0.
    - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exits 0.
    - Any pre-existing `tests/test_*.flow` script that exercises scale/direction parsing continues to PASS (run `dotnet run --project flow-interpreter tests/test_chord_progressions.flow` if present, or another `.flow` test that uses scale-name lookup).
  </acceptance_criteria>
  <done>All 3 culture-sensitive call sites use `*Invariant` overloads; Desktop + Web builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land DryWetMidiWasmPublishTests + CultureInvariantSweepTests</name>
  <files>flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs, flow-lang.Tests/Integration/Phase48/CultureInvariantSweepTests.cs</files>
  <read_first>
    - flow-lang.Tests/Integration/Phase47/DryWetMidiWasmCompatTests.cs (analog for the cross-target Fact pattern)
    - flow-lang.Tests/Integration/Phase47/AssemblyReferenceScanTests.cs (analog for Mono.Cecil assembly inspection — lines 1-120)
    - flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs (Phase 48-01 — RunDotnetPublish helper to copy/adapt)
    - flow-lang.Tests/Helpers/FlowTargetFactAttribute.cs
  </read_first>
  <behavior>
    **DryWetMidiWasmPublishTests.cs** (2 facts):
    - `FlowLangDll_PublishedToAppBundle`: After running `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release`, the file `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/_framework/flow-lang.dll` (or `flow-lang.wasm`, depending on publish layout) exists.
    - `FlowLangDll_RetainsDryWetMidiReference`: Using Mono.Cecil (already in test deps per Phase 47-05), load the published `flow-lang.dll`, scan its `MainModule.AssemblyReferences` for an entry whose Name contains `"Melanchall.DryWetMidi"`. Assert presence. This confirms that the linker did NOT trim DryWetMidi away — `writeMidi` will work on Web.
    - Both facts are plain `[Fact]` (NOT `[FlowTargetFact]`) — they shell out to a publish process and run on Desktop.

    **CultureInvariantSweepTests.cs** (2 facts):
    - `NoUnqualifiedToUpper_InProductionCode`: Walks `flow-lang/` recursively (excluding `bin/`, `obj/`, and `*.Tests/`), reads every `.cs` file, counts lines matching the regex `\.ToUpper\(\)` that are NOT comment-only lines (skip lines whose leading non-whitespace is `//`). Asserts count == 0.
    - `NoUnqualifiedToLower_InProductionCode`: Same as above but for `\.ToLower\(\)`.
    - Both facts are plain `[Fact]`. They run from any target — the source files are inspected directly via `File.ReadAllLines`, not via reflection.
    - Both facts use a shared `FindRepoRoot()` helper modeled on Phase 47 `BuildConditioningSmokeTests.FindRepoRoot()` (walk up from `AppContext.BaseDirectory` until `flow-lang/flow-lang.csproj` is found).
    - Error message on failure includes the file path + line number + offending line text — so a developer can fix it in one read.
  </behavior>
  <action>
**Create `flow-lang.Tests/Integration/Phase48/DryWetMidiWasmPublishTests.cs`**:

Pattern: import `using Mono.Cecil;` per AssemblyReferenceScanTests.cs precedent. Two `[Fact]` methods.

`FlowLangDll_PublishedToAppBundle`:
1. Use the `FindRepoRoot()` + `RunDotnetPublish("-p:FlowTarget=Web -c Release")` helpers (consider extracting them to a Phase48-shared `WasmPublishHarness` static helper class in the same Phase48 directory to avoid duplication with WasmBuildPipelineTests; alternative is to copy verbatim — Rule 1 deviation latitude per CLAUDE.md, executor's choice).
2. Gate on `Assert.True(code == 0, "Publish must succeed")`.
3. Path: `Path.Combine(repoRoot, "flow-lang", "bin", "Release", "net10.0", "browser-wasm", "publish", "AppBundle", "_framework", "flow-lang.dll")`. Assert `File.Exists`.

`FlowLangDll_RetainsDryWetMidiReference`:
1. Run publish (gate as above).
2. Use Mono.Cecil: `AssemblyDefinition.ReadAssembly(publishedDllPath)` — loads the dll from the WASM publish output directory (NOT `typeof(FlowEngine).Assembly.Location` — that's the Desktop test runner's copy).
3. Iterate `module.AssemblyReferences` (where `module = asm.MainModule`).
4. Assert at least one reference's `.Name` starts with `"Melanchall.DryWetMidi"`.
5. If absent → `Assert.Fail($"Expected Melanchall.DryWetMidi assembly reference in published flow-lang.dll; actual refs: {string.Join(", ", refs.Select(r => r.Name))}")`.

**Create `flow-lang.Tests/Integration/Phase48/CultureInvariantSweepTests.cs`**:

Two `[Fact]` methods. Shared helper: `private static IEnumerable<string> EnumerateProductionCsFiles(string repoRoot) { ... }` that returns every `.cs` file under `repoRoot/flow-lang/` EXCLUDING any path whose any segment matches `bin`, `obj`, or any directory ending in `.Tests`.

`NoUnqualifiedToUpper_InProductionCode`:
1. Find repo root.
2. Enumerate production .cs files.
3. For each file, `File.ReadAllLines`, scan for the regex `\.ToUpper\(\)` per line.
4. Skip lines whose `TrimStart()` starts with `//` (comment-only lines).
5. Skip lines containing `ToUpperInvariant` (the safe variant — never a false positive since the regex requires `.ToUpper()` with empty parens).
6. Collect violations as `(filePath, lineNumber, lineText)`.
7. Assert `violations.Count == 0` with a multi-line message: `$"Found {N} unqualified .ToUpper() call(s) in production code (Phase 48 D-48-03 invariant-globalization gate):\n{string.Join("\n", violations.Select(v => $"  {v.filePath}:{v.lineNumber} {v.lineText.Trim()}"))}"`.

`NoUnqualifiedToLower_InProductionCode`: Same shape, regex `\.ToLower\(\)`, skip `ToLowerInvariant`.

After landing both files, both tests must PASS — they enforce the discipline established in Task 1.

Add namespace usings: `System`, `System.Collections.Generic`, `System.Diagnostics`, `System.IO`, `System.Linq`, `System.Text.RegularExpressions`, `Mono.Cecil` (for the DryWetMidi assembly inspection), `Xunit`.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase48.DryWetMidiWasmPublishTests|FullyQualifiedName~Phase48.CultureInvariantSweepTests" -v quiet --nologo 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and compile.
    - `dotnet test --filter "FullyQualifiedName~Phase48.DryWetMidiWasmPublishTests"` invokes 2 tests, both PASS.
    - `dotnet test --filter "FullyQualifiedName~Phase48.CultureInvariantSweepTests"` invokes 2 tests, both PASS.
    - The CultureInvariantSweepTests use a `Regex` against `\.ToUpper\(\)` and `\.ToLower\(\)` (with empty parens) — not the broader catch-all that would also reject `ToUpperInvariant`.
    - Failure message format includes file path + line number + line text — verify via temporary perturbation of one source file (add a fresh `.ToLower()` somewhere, run the test, confirm it fails with actionable output, then revert).
  </acceptance_criteria>
  <done>4 new xUnit Facts landed (2 DryWetMidi publish + 2 culture-invariant sweep); all PASS; writeMidi confirmed reachable from Web AppBundle.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MSBuild → published WASM bundle | `dotnet publish` includes DryWetMidi.dll in `_framework/`; the dll is shipped to the browser as inert data until loaded by Mono runtime |
| Source-code grep gate → test runner | regex scan over `.cs` files; reads file content but does not execute it |
| Mono.Cecil reading published .dll | static metadata inspection; no IL execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Tampering (regex bypass) | CultureInvariantSweepTests | accept | Regex catches `.ToUpper()` / `.ToLower()` with empty parens; a contributor could bypass via `.ToUpper(CultureInfo.CurrentCulture)` — D-48-03 acknowledges this as a defense-in-depth gate, not airtight; code review remains the primary control |
| T-48-06 | Information disclosure | published DryWetMidi.dll in browser | accept | DryWetMidi 8.0.3 is MIT-licensed open source — its IL is already public; shipping it as part of the Web bundle does not expose new info |
| T-48-07 | Denial of service | Mono.Cecil parsing untrusted dll | accept | The dll being parsed is OUR OWN publish output, not user-supplied; test failure on parse error is acceptable signal |
</threat_model>

<verification>
1. `grep -rn 'ToUpper()\|ToLower()' flow-lang/ | grep -v '^\s*//'` returns zero matches.
2. `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exits 0.
3. `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exits 0.
4. `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` succeeds (Phase 48-01 acceptance preserved).
5. The published `_framework/flow-lang.dll` retains an `AssemblyReference` to `Melanchall.DryWetMidi` (Mono.Cecil scan).
6. All 4 new Phase 48 Facts (2 DryWetMidi + 2 culture) PASS.
7. No regression to existing Phase 47 tests — `dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase47"` retains its passing baseline.
</verification>

<success_criteria>
- D-48-17 closed: DryWetMidi STAYS in Web build (not stripped); `writeMidi` callable from Web target.
- 3 culture-sensitive call sites fixed; CI gate prevents regression.
- 4 new xUnit Facts landed; all green.
- Zero new NuGet packages (Mono.Cecil already in test deps per Phase 47-05).
</success_criteria>

<output>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-02-SUMMARY.md` when done. Record: was DryWetMidi reachable in the published bundle (yes/no — if no, escalate to D-48-17 fallback)? Any additional culture-sensitive sites discovered that needed whitelist comments?
</output>
