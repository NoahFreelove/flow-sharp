---
phase: 48-wasm-runtime-webaudio-backend
plan: 07
type: execute
wave: 6
depends_on: ["48-01", "48-02", "48-03", "48-04", "48-05", "48-06"]
files_modified:
  - .planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md
  - .planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "48-VERIFICATION.md exists with per-REQ closure evidence + D-48-NN decision trace + known caveats"
    - "48-PHASE49-HANDOFF.md documents the flow-runtime.js API contract for SvelteKit consumers"
    - "ROADMAP.md Phase 48 entry flipped from Pending to Shipped + Plans list flipped to 7/7 complete"
    - "STATE.md frontmatter updated: phase=48 shipped, last_updated current, progress incremented"
    - "REQUIREMENTS.md gains the WASM Runtime Phase 48 section with all REQ closures"
    - "CLAUDE.md gains a WASM Runtime summary section documenting flow-runtime.js API + 30s cap + autoplay policy + bundle outcome"
    - "Full Desktop test suite still green; Phase 47 baseline preserved + new Phase 48 facts all passing"
  artifacts:
    - path: ".planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md"
      provides: "Per-REQ closure evidence + D-48-NN decision trace + known caveats from 48-HUMAN-UAT.md + bundle outcome"
      min_lines: 80
    - path: ".planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md"
      provides: "Phase 49 consumption contract for flow-runtime.js — API, paths, COOP/COEP, autoplay-gesture wiring"
      min_lines: 60
  key_links:
    - from: ".planning/STATE.md frontmatter"
      to: "Phase 48 status flip"
      via: "stopped_at marker + progress increment"
      pattern: "Phase 48 SHIPPED"
    - from: "CLAUDE.md WASM Runtime section"
      to: "flow-lang/wasm/flow-runtime.js + flow-lang/Runtime/WasmEntry.cs"
      via: "documentation cross-reference"
      pattern: "flow-runtime"
---

<objective>
Close Phase 48: write 48-VERIFICATION.md, write 48-PHASE49-HANDOFF.md, flip STATE.md / ROADMAP.md / REQUIREMENTS.md / CLAUDE.md to reflect Phase 48 SHIPPED.

Purpose: Phase 48 ships when all 6 prior plans (48-01..48-06) are complete and a closer commits the planning-artifact bookkeeping. The handoff doc to Phase 49 is the load-bearing deliverable — Phase 49's SvelteKit playground tab consumes flow-runtime.js, and this is the API contract document.

Output:
- 48-VERIFICATION.md — per-REQ closure table + D-48-NN decision trace + known caveats sourced from 48-HUMAN-UAT.md outcomes + bundle size from 48-BUNDLE-SIZE.md.
- 48-PHASE49-HANDOFF.md — explicit contract for Phase 49: which JS file to import, what API methods exist, what RunResult shape looks like, what autoplay/user-gesture chain Phase 49 must wire, what bundle size to expect.
- STATE.md, ROADMAP.md, REQUIREMENTS.md, CLAUDE.md flips per the project-bookkeeping conventions established in Phases 42-47.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-compile-target-flavors/47-VERIFICATION.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-01-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-02-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-03-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-04-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-05-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-06-SUMMARY.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-BUNDLE-SIZE.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 48-VERIFICATION.md per-REQ closure table + D-48-NN decision trace</name>
  <files>.planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/47-compile-target-flavors/47-VERIFICATION.md (canonical analog — closure-table format)
    - All 48-NN-SUMMARY.md files (extract commit SHAs + actual deliverables per plan)
    - 48-BUNDLE-SIZE.md (Plan 48-05 outcome)
    - 48-HUMAN-UAT.md (Plan 48-06 composer sign-offs + gotchas)
  </read_first>
  <action>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md` modeled on `47-VERIFICATION.md`. Sections:

1. **Header** — Phase, Status (Shipped YYYY-MM-DD), Plans (7/7), Requirements (count of closed REQ-WASM-BUILD + REQ-WEBAUDIO + REQ-WASM-DRYWET + REQ-WASM-API + REQ-WASM-SIZE + REQ-WASM-DET).

2. **Outcome Summary** — 2-3 paragraphs describing what Phase 48 introduced:
   - First Mono-WASM publish target for flow-lang.dll.
   - Real WebAudioBackend driving browser AudioContext via JSImport.
   - flow-runtime.js ES module API surface frozen per D-48-13.
   - Bundle size outcome per 48-BUNDLE-SIZE.md.
   - HUMAN-UAT outcome per 48-HUMAN-UAT.md (3-row browser smoke).

3. **Build surface** — analog to Phase 47:
   - `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` produces the AppBundle.
   - Path to flow-runtime.js in AppBundle.
   - How to serve locally (python3 -m http.server).
   - How a SvelteKit consumer imports (preview for Phase 49).

4. **Composer-facing behavior on Web target** — extend the Phase 47 list:
   - `(play buffer)` routes to WebAudioBackend → audible via browser AudioContext.
   - `(print "...")` captured in RunResult.stdout, surfaced to JS caller.
   - Advisories `[X] ...` captured in RunResult.stderr.
   - Parse / eval errors surface as RunResult.errors[] structured array.
   - 30s wall-clock cap (D-48-10) produces RunResult.errors[0].kind == "cancel" on timeout.
   - `live { }` block parse-time error inherited from Phase 47 D-47-09.

5. **Requirement Closure Table** — one row per REQ-WASM-BUILD-01..05 + REQ-WEBAUDIO-01..04 + REQ-WASM-API-01..03 + REQ-WASM-DRYWET-01 + REQ-WASM-SIZE-01 + REQ-WASM-DET-01 with:
   - REQ-ID | Description | Closure (Plan + commit SHA)

6. **Acceptance Evidence** — sub-sections:
   - Desktop build byte-identical (Phase 47 baseline preserved).
   - Web build links cleanly (Plan 48-01 acceptance).
   - WASM publish produces AppBundle with expected files (Plan 48-01 + 48-04).
   - WebAudioBackend stub-to-real swap (Plan 48-03).
   - Bundle size outcome (Plan 48-05; pull numbers from 48-BUNDLE-SIZE.md).
   - Two-run determinism preserved (Plan 48-05).
   - HUMAN-UAT browser smoke (Plan 48-06; pull from 48-HUMAN-UAT.md).

7. **Decision Trace (D-48-NN)** — table of all 19 decisions in CONTEXT.md, one-row Outcome each:
   - D-NN | Decision | Outcome
   - Example row: D-48-01 | Mono-WASM jiterpreter, not NativeAOT-LLVM | Confirmed; Plan 48-01 ships jiterpreter
   - Example row: D-48-02 | TrimMode=full + TrimmerRootDescriptor | Confirmed; PATTERNS.md Discrepancy 3 reconciled — descriptor preserves FlowType subclasses + AudioBuffer + Value + WebAudioBackend, NOT InternalFunctionRegistry

8. **Known Caveats** — 3-5 rows extracted from 48-HUMAN-UAT.md gotchas + 48-BUNDLE-SIZE.md decision + open items. Possible:
   - Safari-specific autoplay behavior (route to Phase 49 polish).
   - Cross-platform Lorenz/logistic determinism (D-36-09 carryforward).
   - Lazy-load decision per 48-BUNDLE-SIZE.md (monolithic shipped vs. v1.6 follow-up).
   - flow-runtime.js relative-path layout decision per 48-04-SUMMARY.md.

9. **Test Suite Outcome** — count of new Phase 48 Facts (sum from each plan's SUMMARY). Confirm Phase 47 baseline preserved.

Total length: 80-150 lines. Match the density of Phase 47 VERIFICATION (60 lines + closure table).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; test -f .planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md &amp;&amp; wc -l .planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists, at least 80 lines.
    - Contains at least 8 top-level or sub-level headings.
    - Contains a Requirement Closure Table with at least 14 rows (5 BUILD + 4 WEBAUDIO + 3 API + 1 DRYWET + 1 SIZE + 1 DET = 15 minimum).
    - Contains Decision Trace (D-48-NN) table with at least 19 rows.
    - Contains Known Caveats section.
    - Contains Test Suite Outcome section with passing baseline assertion.
  </acceptance_criteria>
  <done>48-VERIFICATION.md ships with full closure trace.</done>
</task>

<task type="auto">
  <name>Task 2: Write 48-PHASE49-HANDOFF.md — flow-runtime.js consumption contract for SvelteKit</name>
  <files>.planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md</files>
  <read_first>
    - flow-lang/wasm/flow-runtime.js (Plan 48-04 final shape — extract the actual exported API)
    - flow-lang/Runtime/WasmEntry.cs (RunResult + RunError shapes — extract for the JS-side TypeScript-like contract)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md D-48-09 (autoplay), D-48-13 (API surface), D-48-14 (RunResult), D-48-15 (stdout/stderr split)
    - .planning/ROADMAP.md Phase 49 section (lines 603-642 — understand what Phase 49 will build)
    - 48-HUMAN-UAT.md (any browser-specific gotchas to flag for Phase 49)
  </read_first>
  <action>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md`. Sections:

1. **Header** — Phase 48 to Phase 49 hand-off. Purpose: lets the Phase 49 planner / executor consume flow-runtime.js without re-deriving the contract from source.

2. **Where to find the bundle** — concrete paths:
   - Source: `flow-lang/wasm/flow-runtime.js` (the hand-written ES module).
   - Source: `flow-lang/Runtime/WasmEntry.cs` (the C# JSExport boundary).
   - Published: `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/wasm/flow-runtime.js` (or root, per 48-04-SUMMARY.md).
   - Mono-WASM loader: `AppBundle/_framework/dotnet.js` (loaded by flow-runtime.js).
   - Companion files: `dotnet.js.map` (Debug only), `*.wasm`, `*.dll` under `_framework/`.

3. **How to integrate into SvelteKit** — concrete steps:
   - Phase 49's build step needs to copy the AppBundle into the SvelteKit `static/` directory at some path (e.g., `static/wasm/`).
   - Or: SvelteKit imports flow-runtime.js dynamically via `import('/wasm/flow-runtime.js')` at runtime (defer-load on playground tab).
   - The dynamic-import path keeps marketing pages snappy per D-49-02 (no WASM on every page).

4. **API contract** — TypeScript-like signature (Markdown documentation, not enforced by a .ts file):

   ```
   export async function loadFlowRuntime(): Promise<Runtime>

   interface Runtime {
       run(source: string): Promise<RunResult>
       play(wav: Float32Array, sampleRate?: number, channels?: number): void
       stop(): void
       dispose(): void
       resumeAudio(): Promise<void>   // convenience — call from user-gesture chain
   }

   interface RunResult {
       wav?: Float32Array         // present if run produced audio
       midi?: Uint8Array          // present if run produced MIDI
       stdout: string             // captured print output
       stderr: string             // captured advisory [X] output
       errors: RunError[]         // structured errors (parse/eval/runtime/cancel)
       durationMs: number         // wall-clock ms (NOT byte-identical across runs)
   }

   interface RunError {
       kind: "parse" | "eval" | "runtime" | "cancel" | "platform-not-supported"
       message: string
       line?: number
       column?: number
       sourceSnippet?: string
   }
   ```

5. **User-gesture chain requirement (D-48-09)** — explicit instructions for Phase 49's playground:
   - The Run button's onclick handler MUST call BOTH `await runtime.resumeAudio()` AND `await runtime.run(source)` in the same async function frame.
   - DO NOT call `runtime.resumeAudio()` outside a click/keypress handler — browser autoplay policy will block AudioContext.resume() and silently fail.
   - The first time the playground tab loads, the user clicks Run to satisfy the gesture; subsequent Runs in the same session do not re-need the gesture, but it is idempotent to keep calling resumeAudio().

6. **Bundle size + first-paint cost** — quote the actual 48-BUNDLE-SIZE.md outcome:
   - "First-paint cost: ~N MB compressed Brotli (per 48-BUNDLE-SIZE.md). Includes Mono-WASM runtime + flow-lang.dll + stdlib + DryWetMidi."
   - "Defer-load on playground tab only per D-49-02; marketing pages do NOT trigger this download."

7. **COOP/COEP headers** — D-48-02 v1.6 backlog stretch needs Cross-Origin-Opener-Policy + Cross-Origin-Embedder-Policy for SharedArrayBuffer streaming. Phase 49 should set the headers AT DEPLOY TIME via Cloudflare Pages `_headers` file. Document the header names + values so Phase 49 can wire them preemptively (no cost if v1.6 is not done yet; preserves the option).

8. **Known browser gotchas** — copy from 48-HUMAN-UAT.md Gotchas columns. Phase 49 should test against these.

9. **What Phase 49 must NOT change** — explicit boundary:
   - DO NOT modify flow-runtime.js — it is the canonical surface. Wrap it instead if SvelteKit ergonomics need a thin adapter.
   - DO NOT modify WasmEntry.cs — Phase 49 consumes via JSON-parsed RunResult only.
   - DO NOT add new JSImport names — extend WasmEntry.cs in a future v1.6 phase if needed.

10. **MIDI + WAV export download mechanism** (D-48-18) — Phase 49 wires the download UI:
    - `runtime.run(source)` returns RunResult.midi as Uint8Array if the Flow source called `writeMidi`.
    - Phase 49 creates a Blob (`new Blob([midi], { type: "audio/midi" })`) + a BlobURL + a download anchor click.
    - WAV export parallel: RunResult.wav as Float32Array, wrap in a Wave header (~40 LOC hand-rolled) and trigger download. Phase 49 implements OR a v1.6 follow-up adds `runtime.exportWav()` helper to flow-runtime.js.

Total length: 80-150 lines. Treat as a contract document — Phase 49 reads this BEFORE writing any code.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; test -f .planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md &amp;&amp; wc -l .planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists, at least 60 lines.
    - Contains the API contract block with loadFlowRuntime, Runtime interface, RunResult interface, RunError interface.
    - Contains the user-gesture chain instructions referencing D-48-09.
    - Contains the bundle size quote referencing 48-BUNDLE-SIZE.md.
    - Contains the "What Phase 49 must NOT change" boundary section.
    - Contains the MIDI + WAV export download mechanism explanation referencing D-48-18.
  </acceptance_criteria>
  <done>48-PHASE49-HANDOFF.md ships as the Phase 49 consumption contract.</done>
</task>

<task type="auto">
  <name>Task 3: Bookkeeping flips — STATE.md + ROADMAP.md + REQUIREMENTS.md + CLAUDE.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, CLAUDE.md</files>
  <read_first>
    - .planning/STATE.md (current frontmatter — see the existing format)
    - .planning/ROADMAP.md lines 576-602 (current Phase 48 entry — needs status flip + Plans count + Plans list)
    - .planning/REQUIREMENTS.md (the "### Compile-Target Flavors (Phase 47)" section at line 173 — pattern for new sections; new section goes below it)
    - CLAUDE.md (existing structure — the "## Compile-Target Flavors" section from Phase 47 is the precedent; Phase 48 adds a sibling "## WASM Runtime" section)
    - 48-VERIFICATION.md (Task 1 output — pull final counts + caveats)
  </read_first>
  <action>
**Edit 1: STATE.md frontmatter**

Update the frontmatter:
- `stopped_at:` to "Phase 48 SHIPPED YYYY-MM-DD (WASM Runtime + WebAudioBackend — Mono-WASM publish + flow-runtime.js ES module + real WebAudioBackend via JSImport + N-MB compressed bundle)"
- `last_updated:` to current ISO timestamp.
- `last_activity:` similar narrative line summarizing Phase 48 close.
- `progress.completed_phases`: 8 → 9.
- `progress.completed_plans`: 60 → 67 (60 + 7 Phase 48 plans).
- `progress.percent`: recompute (67 / 71 = 94%; or adjust if total_plans changed).

Add a new "**Phase 48 highlights (YYYY-MM-DD):**" block below the existing Phase 47 highlights block, mirroring the Phase 47 highlights structure (~6-10 bullets):
- Mono-WASM publish pipeline lands at flow-lang.csproj (jiterpreter, invariant globalization, full trim, TrimmerRootDescriptor preserving FlowType singletons per PATTERNS.md Discrepancy 3 — NOT InternalFunctionRegistry).
- WebAudioBackend real implementation swaps Phase 47 stub bodies for JSImport-driven AudioContext lifecycle + stereo promotion (D-48-07) + 30s wall-clock cap (D-48-10).
- flow-runtime.js ES module ships with 5-export API surface per D-48-13 (loadFlowRuntime + run + play + stop + dispose + resumeAudio convenience).
- WasmEntry.cs exposes 4 JSExport methods + RunResult/RunError POCOs with structured-error marshalling (D-48-14) + stdout/stderr split (D-48-15).
- Bundle size outcome: <N> MB compressed (vs. 15 MB target per D-48-05). [Insert actual number from 48-BUNDLE-SIZE.md.]
- DryWetMidi WASM-compat confirmed — flow-lang.dll retains the reference reachably; writeMidi callable from Web target (D-48-17 "compatible" branch confirmed).
- 3 culture-sensitive call sites fixed for invariant-globalization mode (D-48-03 Turkish-I hazard closed); CultureInvariantSweepTests CI gate prevents regression.
- HUMAN-UAT outcome: <X>/<Y> browser rows signed off [reference 48-HUMAN-UAT.md].
- Phase 48 xUnit additions: <N> new Facts; full Desktop test suite remains green (Phase 47 baseline preserved).
- Zero new NuGet packages added (Mono-WASM ships in .NET 10 SDK; Brotli + System.Text.Json + JsonNode all BCL).

Update Resume Instructions:
- `/clear` then `/gsd:plan-phase 49` (Phase 49 unblocked — consumes flow-runtime.js per 48-PHASE49-HANDOFF.md).

**Edit 2: ROADMAP.md Phase 48 section**

Edit lines 576-602:
- The `**Goal**:` line stays.
- Change `**Plans:**` line from `0 plans` to `7 plans`.
- Replace the placeholder `- [ ] TBD (run /gsd-plan-phase 48 to break down)` Plans list with 7 actual plan entries:
  ```
  - [x] 48-01-PLAN.md — WASM build pipeline foundation (csproj + trim-roots.xml + WasmBuildPipelineTests)
  - [x] 48-02-PLAN.md — DryWetMidi WASM publish smoke + culture-invariant sweep
  - [x] 48-03-PLAN.md — WebAudioBackend real implementation (JSImport + stereo promotion + 30s cap)
  - [x] 48-04-PLAN.md — flow-runtime.js ES module + WasmEntry.cs JSExport + index.html dev harness
  - [x] 48-05-PLAN.md — Bundle size budget + two-run determinism pin
  - [x] 48-06-PLAN.md — HUMAN-UAT browser smoke (Chrome / Firefox / Safari)
  - [x] 48-07-PLAN.md — Closer (VERIFICATION + handoff + planning-artifact flips)
  ```
- Add an "Outcome" line above the "Depends on:" line summarizing the v1.5 phase progression:
  ```
  **Outcome:** Phase 48 SHIPPED YYYY-MM-DD. <N> REQs closed. Bundle: <N> MB compressed. flow-runtime.js API frozen for Phase 49. v1.5 milestone progress: 10/15 phases complete.
  ```

Also update the "### v1.5 Phase Map" table inside STATE.md if there is a Phase 48 row to flip from Pending to Shipped — likely there is one near line 91 (per the existing Phase 47 row "**Shipped 2026-05-25**"). Add a row for Phase 48 if absent, OR flip the existing one.

**Edit 3: REQUIREMENTS.md**

Insert a new `### WASM Runtime (Phase 48)` section BELOW the existing `### Compile-Target Flavors (Phase 47)` section (line 173 onwards in REQUIREMENTS.md). Format mirrors Phase 47:

```
### WASM Runtime (Phase 48)

**Phase 48 SHIPPED YYYY-MM-DD** — Mono-WASM publish target ships flow-lang.dll into a browser-WASM AppBundle. Real WebAudioBackend drives browser AudioContext via JSImport with stereo promotion and 30s wall-clock cap. ES module flow-runtime.js exposes 5-export API + 1 convenience to JS consumers (Phase 49 SvelteKit playground tab). RunResult shape: structured-error array + stdout/stderr split + camelCase JSON. Bundle size outcome: <N> MB compressed (vs. 15 MB target). DryWetMidi 8.0.3 retained in Web build (writeMidi callable from Web). 3 culture-sensitive call sites fixed for invariant-globalization mode. <N> HUMAN-UAT browser rows signed off (Chrome / Firefox / Safari).

- [x] **REQ-WASM-BUILD-01**: dotnet publish flow-lang -p:FlowTarget=Web -c Release produces AppBundle (Plan 48-01 commit <SHA>)
- [x] **REQ-WASM-BUILD-02**: trim-roots.xml preserves FlowType subclasses + AudioBuffer + Value + WebAudioBackend (Plan 48-01)
- [x] **REQ-WASM-BUILD-03**: jiterpreter + InvariantGlobalization + TrimMode=full enabled under FlowTarget=Web (Plan 48-01)
- [x] **REQ-WASM-BUILD-04**: WasmEmitSymbolMap gated on Debug only (Plan 48-01)
- [x] **REQ-WASM-BUILD-05**: 3 culture-sensitive call sites fixed (HarmonyFunctions:441 + ScaleDatabase:182,233); CultureInvariantSweepTests CI gate (Plan 48-02)
- [x] **REQ-WASM-DRYWET-01**: DryWetMidi WASM-compat smoke green via DryWetMidiWasmPublishTests; writeMidi reachable from Web (Plan 48-02)
- [x] **REQ-WEBAUDIO-01**: FlowRuntimeInterop.cs with 5 JSImport partial-static methods (Plan 48-03)
- [x] **REQ-WEBAUDIO-02**: WebAudioBackend stub-to-real swap; signatures pinned per D-47-05 (Plan 48-03)
- [x] **REQ-WEBAUDIO-03**: Mono-to-stereo promotion via PromoteToStereo (D-48-07); 30s wall-clock cap via Task.Run + Wait (D-48-10) (Plan 48-03)
- [x] **REQ-WEBAUDIO-04**: Charitable IsBrowser() fallback to throw + log on Initialize failure (D-48-11) (Plan 48-03)
- [x] **REQ-WASM-API-01**: flow-runtime.js ES module exports loadFlowRuntime + 4 runtime methods (Plan 48-04)
- [x] **REQ-WASM-API-02**: WasmEntry.cs 4 JSExport methods; RunResult + RunError POCOs (Plan 48-04)
- [x] **REQ-WASM-API-03**: index.html dev-smoke harness + Web-conditional CopyToPublishDirectory (Plan 48-04)
- [x] **REQ-WASM-SIZE-01**: Bundle size measured + recorded in 48-BUNDLE-SIZE.md; <N> MB compressed; D-48-05 decision branch <X> (Plan 48-05)
- [x] **REQ-WASM-DET-01**: Two-run cmp-clean determinism preserved via WasmDeterminismTests (RunResult JSON byte-identical excluding durationMs) (Plan 48-05)
```

Also add Phase 48 traceability rows to the "## v1.5 Traceability" section if such a section exists in REQUIREMENTS.md (mirror Phase 47's traceability entries).

**Edit 4: CLAUDE.md**

Add a new section `## WASM Runtime` to CLAUDE.md, near or after the existing Phase 47 section. The section should be ~30-50 lines:

```
## WASM Runtime

**Phase 48 SHIPPED 2026-MM-DD** — flow-lang ships under .NET 10 Mono-WASM via `FlowTarget=Web`. Browser audio out routes through a real WebAudioBackend (JSImport-driven AudioContext + AudioBufferSourceNode). flow-runtime.js ES module exposes a frozen 5-export API for Phase 49 SvelteKit consumption.

### Publish

```
dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release
# AppBundle: flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/
```

### flow-runtime.js API surface (D-48-13 frozen)

```
import { loadFlowRuntime } from "./wasm/flow-runtime.js";
const runtime = await loadFlowRuntime();
const result = await runtime.run("(play (createSineTone 440Hz 1.0 0.5))");
// result: { wav?, midi?, stdout, stderr, errors[], durationMs }
runtime.play(wav, sampleRate, channels);
runtime.stop();
runtime.dispose();
await runtime.resumeAudio(); // convenience — call from user-gesture chain
```

### Key contracts

- **Autoplay policy (D-48-09)**: Phase 49 must call resumeAudio() from inside a user-gesture (button onclick) frame. WebAudioBackend never calls resume() itself.
- **30s wall-clock cap (D-48-10)**: every runtime.run() is wrapped in Task.Run + Wait(30s). Long renders return RunResult.errors[0].kind === "cancel".
- **stdout/stderr split (D-48-15)**: Flow `print` writes go to RunResult.stdout. Advisories like `[tuning] ...` or `[live] ...` go to RunResult.stderr.
- **Structured errors (D-48-14)**: RunError = { kind, message, line?, column?, sourceSnippet? }. Phase 49 renders as Rust-style diagnostic boxes.
- **Stereo always (D-48-07)**: WebAudioBackend.PromoteToStereo dupes mono samples to L+R before JSImport marshal. Matches Phase 37 B2 LOCK.
- **One AudioContext per FlowEngine (D-48-08)**: lazy-created on first Initialize; Dispose closes it.
- **Two-run cmp-clean (D-48-16)**: same source produces byte-identical RunResult JSON (excluding durationMs).
- **Bundle size budget (D-48-05)**: <N> MB compressed Brotli (per 48-BUNDLE-SIZE.md). v1.6 lazy-load follow-up reserved if any single stdlib grows past budget.
- **DryWetMidi retained on Web (D-48-17 confirmed branch)**: writeMidi works on Web target; RunResult.midi as Uint8Array. Phase 49 wires Blob download.

### Composer-facing behavior on Web target (extends Phase 47)

- `(play buffer)` → audible via browser AudioContext.
- `(print ...)` → RunResult.stdout.
- `(writeMidi ...)` → RunResult.midi (Uint8Array; download via Phase 49 UI).
- Advisories → RunResult.stderr.
- Parse / eval / runtime errors → RunResult.errors[] structured.
- `use "@sfz"`, `use "@osc"` → ModuleLoader charitable advisory (Phase 47 D-47-09).
- `live { }` block → parse-time error (Phase 47 D-47-09).
- `(micBuffer ...)` → function not found (InputFunctions stripped per Phase 47).

### Hand-off to Phase 49

See `.planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md` for the full contract document.
```

Verify with `git diff --stat` that the 4 files are touched in expected places + no production code is modified.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; grep -c 'Phase 48 SHIPPED\|Phase 48 shipped' .planning/STATE.md &amp;&amp; grep -c '48-07-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -c 'WASM Runtime (Phase 48)' .planning/REQUIREMENTS.md &amp;&amp; grep -c '## WASM Runtime' CLAUDE.md &amp;&amp; dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md frontmatter contains "Phase 48 SHIPPED" in stopped_at.
    - .planning/STATE.md frontmatter progress.completed_plans is at least 67 (60 baseline + 7 Phase 48).
    - .planning/STATE.md has a "Phase 48 highlights" block with at least 6 bullets.
    - .planning/ROADMAP.md Phase 48 section has 7 Plan entries with `[x]` checkmarks.
    - .planning/ROADMAP.md Phase 48 section has "**Outcome:** Phase 48 SHIPPED" line.
    - .planning/REQUIREMENTS.md contains a "### WASM Runtime (Phase 48)" header section.
    - .planning/REQUIREMENTS.md has at least 15 `- [x] **REQ-WASM-*` or `- [x] **REQ-WEBAUDIO-*` rows in the Phase 48 section.
    - CLAUDE.md contains a "## WASM Runtime" section with at least 30 lines.
    - CLAUDE.md WASM Runtime section references flow-runtime.js, RunResult, D-48-09, D-48-10, D-48-14, D-48-15.
    - dotnet build flow-lang -p:FlowTarget=Desktop exits 0 (no production code touched).
    - dotnet build flow-lang -p:FlowTarget=Web exits 0 (no production code touched).
    - Optional: full Desktop test suite still green (run dotnet test if quick, OR cite Phase 47 baseline + new Phase 48 facts count).
  </acceptance_criteria>
  <done>Bookkeeping flips committed; Phase 48 SHIPPED is recorded across STATE / ROADMAP / REQUIREMENTS / CLAUDE.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifact edits | STATE.md / ROADMAP.md / REQUIREMENTS.md / CLAUDE.md — documentation-only changes; no production code |
| 48-VERIFICATION.md + 48-PHASE49-HANDOFF.md | new files in .planning/; consumed by Phase 49 planning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-24 | Tampering | bookkeeping flip introduces typos in REQ closures | mitigate | acceptance criteria spot-check the row counts + specific text patterns; CI build still runs as a sanity check |
| T-48-25 | Information disclosure | 48-PHASE49-HANDOFF.md exposes internal API surface | accept | The API surface IS the public Phase 49 contract; documenting it is the point |
| T-48-26 | Repudiation | commit message attribution | accept | Standard git commit with Co-Authored-By line per project convention; no special handling needed |
</threat_model>

<verification>
1. .planning/STATE.md frontmatter shows Phase 48 SHIPPED + progress incremented.
2. .planning/ROADMAP.md Phase 48 row shows 7/7 plans complete + Outcome line.
3. .planning/REQUIREMENTS.md has "WASM Runtime (Phase 48)" section with all REQ-WASM-* + REQ-WEBAUDIO-* + REQ-WASM-DRYWET-* + REQ-WASM-API-* + REQ-WASM-SIZE-* + REQ-WASM-DET-* rows marked closed.
4. CLAUDE.md has "## WASM Runtime" section documenting flow-runtime.js API + key contracts + Phase 49 handoff pointer.
5. .planning/phases/48-wasm-runtime-webaudio-backend/48-VERIFICATION.md exists with closure table + decision trace + caveats.
6. .planning/phases/48-wasm-runtime-webaudio-backend/48-PHASE49-HANDOFF.md exists with API contract + autoplay-gesture instructions + bundle size + COOP/COEP preview.
7. dotnet build flow-lang -p:FlowTarget=Desktop AND -p:FlowTarget=Web both exit 0 (Phase 47 + 48 baseline preserved; no production code touched).
8. Full Desktop xUnit suite passes (Phase 47 baseline + Phase 48 additions).
</verification>

<success_criteria>
- 4 planning files edited (STATE / ROADMAP / REQUIREMENTS / CLAUDE).
- 2 new files created (48-VERIFICATION.md + 48-PHASE49-HANDOFF.md).
- Zero production code modified (verify via git diff --stat — only .planning/ + CLAUDE.md touched).
- Zero new NuGet packages.
- Phase 49 has a clean consumption contract document to read.
</success_criteria>

<output>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-07-SUMMARY.md` when done. Record: total Phase 48 commit count, total xUnit Facts added (sum across all 7 plans), final bundle size in compressed MB, HUMAN-UAT signoff summary (X/Y browsers green).
</output>
