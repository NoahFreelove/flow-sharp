---
phase: 48-wasm-runtime-webaudio-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang/flow-lang.csproj
  - flow-lang/wasm/trim-roots.xml
  - flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs
autonomous: true
requirements:
  - REQ-WASM-BUILD-01
  - REQ-WASM-BUILD-02
  - REQ-WASM-BUILD-03
  - REQ-WASM-BUILD-04
  - REQ-WASM-SIZE-01

must_haves:
  truths:
    - "`dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` exits 0"
    - "Published AppBundle directory exists at `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/` and contains `_framework/dotnet.js`"
    - "Published bundle includes a `flow-lang.wasm` (or `dotnet.wasm`-loaded `flow-lang.dll`) under `_framework/`"
    - "FlowTarget=Desktop default build remains byte-clean (Phase 47 baseline preserved)"
    - "Phase 47 Web build (`dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web`) still exits 0 (no regression to library compile)"
    - "Total uncompressed `_framework/` bundle size measured and recorded in test output"
  artifacts:
    - path: "flow-lang/wasm/trim-roots.xml"
      provides: "Trimmer root descriptor preserving FlowType subclasses, AudioBuffer, Value, WebAudioBackend"
      contains: "<linker>"
    - path: "flow-lang/flow-lang.csproj"
      provides: "Extended FlowTarget=Web PropertyGroup with RuntimeIdentifier=browser-wasm + jiterpreter + invariant globalization + full trim + TrimmerRootDescriptor"
      contains: "browser-wasm"
    - path: "flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs"
      provides: "Nested-publish smoke (exit 0 + AppBundle present + bundle size measured)"
      exports: ["WasmPublish_ExitCodeIsZero", "WasmPublish_ProducesAppBundle", "WasmBundle_UncompressedSize_MeasuredAndRecorded"]
  key_links:
    - from: "flow-lang/flow-lang.csproj"
      to: "flow-lang/wasm/trim-roots.xml"
      via: "<TrimmerRootDescriptor Include=\"wasm\\trim-roots.xml\" />"
      pattern: "TrimmerRootDescriptor.*trim-roots\\.xml"
    - from: "flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs"
      to: "flow-lang/flow-lang.csproj"
      via: "ProcessStartInfo shelling `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release`"
      pattern: "dotnet publish flow-lang/flow-lang\\.csproj"
---

<objective>
Extend Phase 47's `FlowTarget=Web` MSBuild conditioning into a full Mono-WASM publish target. Adds `<RuntimeIdentifier>browser-wasm</RuntimeIdentifier>`, jiterpreter, invariant globalization, full trim mode, and a `<TrimmerRootDescriptor>` that preserves FlowType singletons (PATTERNS.md Discrepancy 3 — not InternalFunctionRegistry). Lands a nested-`dotnet publish` smoke test patterned on `BuildConditioningSmokeTests`.

Purpose: every later Plan 48-NN depends on `dotnet publish -p:FlowTarget=Web` exiting 0 and producing an AppBundle. This plan establishes that foundation and pins it in xUnit so regressions are caught at CI time.

Output:
- `flow-lang/flow-lang.csproj` extended with WASM publish properties (per D-48-01, D-48-02, D-48-03, D-48-04 — symbol map Debug-only)
- `flow-lang/wasm/trim-roots.xml` listing every FlowType subclass + AudioBuffer + Value + WebAudioBackend
- `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` with 3 facts (publish exit 0, AppBundle structure, bundle size recorded)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md
@flow-lang/flow-lang.csproj
@flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs

<interfaces>
<!-- The exact csproj surface Phase 47 established. Phase 48 EXTENDS the existing
     `<PropertyGroup Condition="'$(FlowTarget)' == 'Web'">` and existing conditional
     `<ItemGroup Condition="'$(FlowTarget)' == 'Web'">` — single source of truth per
     D-47-01 / Pattern A of PATTERNS.md. -->

From flow-lang/flow-lang.csproj (lines 18-20, 39-62 — Phase 47 baseline):
```xml
<PropertyGroup Condition="'$(FlowTarget)' == 'Web'">
    <DefineConstants>$(DefineConstants);FLOW_WEB</DefineConstants>
</PropertyGroup>

<ItemGroup Condition="'$(FlowTarget)' == 'Web'">
    <Compile Remove="Audio\PulseAudioSimpleBackend.cs" />
    <!-- ... 7 file strips + Samples/sfz.flow/osc.flow ... -->
</ItemGroup>
```

FlowType subclasses to enumerate in trim-roots.xml (glob `flow-lang/TypeSystem/SpecialTypes/*.cs`):
- BarType, BeatType, CentType, ChordType, DecibelType, DictType, HertzType,
  LsystemModelType, MarkovModelType, MillisecondType, MusicalNoteType, NoteType,
  NoteValueType, OscHandleType, SecondType, SectionType, SemitoneType, SequenceType,
  SfzType, SongType, TimeSignatureType, TuningType, TupleType
- Primitive types (`flow-lang/TypeSystem/PrimitiveTypes/*.cs`): VoidType, IntType,
  FloatType, LongType, DoubleType, StringType, BoolType, NumberType, BufferType,
  LazyType, FunctionType, EnvelopeType, OscillatorStateType, VoiceType, TrackType
- ArrayType (`flow-lang/TypeSystem/ArrayType.cs`)
- The base `FlowLang.TypeSystem.FlowType`

Note: per PATTERNS.md Discrepancy 3, `InternalFunctionRegistry` is NOT reflection-heavy
(grep returned ZERO hits for `GetMethods`/`BindingFlags`/`Activator.CreateInstance`).
The real trim risk is `*Type.Instance` static getters and the `Value` interop boundary type.

Per PATTERNS.md note: SfzType + OscHandleType are stripped on Web build (those files
live under `Sfz/**/*.cs` and `Network/Osc*.cs` per Phase 47 strip list). The
trim-roots.xml MUST omit them for Web (they won't exist in the Web assembly closure)
OR the descriptor must use `required="false"` semantics. Simpler: omit SfzType and
OscHandleType from the descriptor — they are stripped before the trimmer runs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend flow-lang.csproj FlowTarget=Web with Mono-WASM publish properties</name>
  <files>flow-lang/flow-lang.csproj</files>
  <read_first>
    - flow-lang/flow-lang.csproj (the entire file, lines 1-152 — Phase 47 baseline)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md §"`flow-lang/flow-lang.csproj` (modify — extend existing FlowTarget=Web ItemGroup)" (lines 195-238 — exact additions documented)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md D-48-01..04
    - .planning/ROADMAP.md Phase 48 §"Implementation surface" (lines 591-592)
  </read_first>
  <action>
Extend the existing `<PropertyGroup Condition="'$(FlowTarget)' == 'Web'">` block (currently lines 18-20) and the existing `<ItemGroup Condition="'$(FlowTarget)' == 'Web'">` block (currently lines 39-62) per PATTERNS.md §"`flow-lang/flow-lang.csproj` (modify)".

Inside the PropertyGroup, add AFTER the existing `<DefineConstants>$(DefineConstants);FLOW_WEB</DefineConstants>` line:
- `<RuntimeIdentifier>browser-wasm</RuntimeIdentifier>` per D-48-01 (Mono-WASM jiterpreter target)
- `<WasmEnableJiterpreter>true</WasmEnableJiterpreter>` per D-48-01
- `<InvariantGlobalization>true</InvariantGlobalization>` per D-48-03
- `<HybridGlobalization>false</HybridGlobalization>` per D-48-03 (explicit pin — ICU savings ~10 MB)
- `<TrimMode>full</TrimMode>` per D-48-02
- `<WasmEmitSymbolMap Condition="'$(Configuration)' == 'Debug'">true</WasmEmitSymbolMap>` per D-48-04 (Debug-only)

Inside the existing FlowTarget=Web ItemGroup (after the existing strip list lines 39-61), add:
- `<TrimmerRootDescriptor Include="wasm\trim-roots.xml" />`

Do NOT introduce a new PropertyGroup or ItemGroup — extend the existing conditional blocks per D-47-01 single-source-of-truth (Pattern A in PATTERNS.md). The csproj must remain a single source of truth — no separate `flow-lang.Web.csproj`.

Per D-v1.5-03 / D-v1.5-08 / D-48-01..04 (Plan 48-01 spec): NO new NuGet PackageReferences. Mono-WASM ships in the .NET 10 SDK; no additional packages needed (`Microsoft.NET.Sdk` already in the `<Project Sdk="...">` declaration at line 1).

Verify after edit: `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` still exits 0 (Desktop path unchanged); `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` still exits 0 (build still compiles — `dotnet publish` is the new acceptance, tested separately in Task 3).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3 ; dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet --nologo 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `flow-lang/flow-lang.csproj` now contains `<RuntimeIdentifier>browser-wasm</RuntimeIdentifier>` exactly once inside the `'$(FlowTarget)' == 'Web'` PropertyGroup.
    - `flow-lang/flow-lang.csproj` contains `<TrimmerRootDescriptor Include="wasm\trim-roots.xml" />` exactly once inside the `'$(FlowTarget)' == 'Web'` ItemGroup.
    - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop` exits 0.
    - `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web` exits 0 (compile, not publish).
    - `grep -c '<RuntimeIdentifier>browser-wasm</RuntimeIdentifier>' flow-lang/flow-lang.csproj` returns 1 (no duplicates).
    - `grep -c '<TrimMode>full</TrimMode>' flow-lang/flow-lang.csproj` returns 1.
    - `grep -c '<InvariantGlobalization>true</InvariantGlobalization>' flow-lang/flow-lang.csproj` returns 1.
  </acceptance_criteria>
  <done>Phase 47 build (Desktop + Web compile) preserved; Phase 48 publish properties present in single conditional block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create flow-lang/wasm/trim-roots.xml preserving FlowType singletons + interop boundary types</name>
  <files>flow-lang/wasm/trim-roots.xml</files>
  <read_first>
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md §"Discrepancy 3" (lines 31-43 — the actual trim risk is FlowType subclasses NOT InternalFunctionRegistry)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md §"`flow-lang/wasm/trim-roots.xml` (NEW, no analog)" (lines 242-267)
    - flow-lang/TypeSystem/SpecialTypes/CentType.cs (lines 10-14 — `Instance` static getter pattern)
    - List of files in flow-lang/TypeSystem/SpecialTypes/ + flow-lang/TypeSystem/PrimitiveTypes/ + flow-lang/TypeSystem/ArrayType.cs
    - Microsoft docs URL embedded in PATTERNS.md: https://learn.microsoft.com/en-us/dotnet/core/deploying/trimming/trimming-options#root-descriptors
  </read_first>
  <behavior>
    - The file parses as valid XML with root element `<linker>`.
    - Contains exactly one `<assembly fullname="flow-lang">` element.
    - Contains `<type fullname="FlowLang.TypeSystem.FlowType" preserve="all" />` (the base class).
    - Contains a `<type ... preserve="all" />` line for every type listed in the enumerable set below.
    - Contains `<type fullname="FlowLang.StandardLibrary.Audio.AudioBuffer" preserve="all" />` (interop boundary type, marshalled across `[JSImport]` in Plan 48-03 — see PATTERNS.md Discrepancy 2).
    - Contains `<type fullname="FlowLang.Runtime.Value" preserve="all" />` (the Flow runtime Value type — reachable by `[JSExport]` per PATTERNS.md).
    - Contains `<type fullname="FlowLang.Audio.WebAudioBackend" preserve="all" />` (the backend itself — Plan 48-03 adds `[JSImport]` partials).
    - OMITS `SfzType` and `OscHandleType` — these files are stripped from Web build via Phase 47 strip list (the trimmer never sees them).
    - OMITS `InternalFunctionRegistry` per PATTERNS.md Discrepancy 3 — Phase 47 grep confirmed zero reflection use; reachability analysis already covers it via FlowEngine constructor's static call chain.
  </behavior>
  <action>
Create `flow-lang/wasm/trim-roots.xml` (new file; parent directory `flow-lang/wasm/` does not exist — `mkdir -p` it before write). Use Microsoft's `<linker>` XML schema. The file MUST contain ONE `<assembly fullname="flow-lang">` element with `<type fullname="..." preserve="all" />` entries for:

**Base class (1 entry):**
- `FlowLang.TypeSystem.FlowType`

**SpecialTypes — enumerate from `flow-lang/TypeSystem/SpecialTypes/*.cs` (21 entries; Sfz + OscHandle EXCLUDED because their .cs files are stripped on Web build per Phase 47 strip list — referencing them would cause trim-roots.xml load error):**
- `FlowLang.TypeSystem.SpecialTypes.BarType`
- `FlowLang.TypeSystem.SpecialTypes.BeatType`
- `FlowLang.TypeSystem.SpecialTypes.CentType`
- `FlowLang.TypeSystem.SpecialTypes.ChordType`
- `FlowLang.TypeSystem.SpecialTypes.DecibelType`
- `FlowLang.TypeSystem.SpecialTypes.DictType`
- `FlowLang.TypeSystem.SpecialTypes.HertzType`
- `FlowLang.TypeSystem.SpecialTypes.LsystemModelType`
- `FlowLang.TypeSystem.SpecialTypes.MarkovModelType`
- `FlowLang.TypeSystem.SpecialTypes.MillisecondType`
- `FlowLang.TypeSystem.SpecialTypes.MusicalNoteType`
- `FlowLang.TypeSystem.SpecialTypes.NoteType`
- `FlowLang.TypeSystem.SpecialTypes.NoteValueType`
- `FlowLang.TypeSystem.SpecialTypes.SecondType`
- `FlowLang.TypeSystem.SpecialTypes.SectionType`
- `FlowLang.TypeSystem.SpecialTypes.SemitoneType`
- `FlowLang.TypeSystem.SpecialTypes.SequenceType`
- `FlowLang.TypeSystem.SpecialTypes.SongType`
- `FlowLang.TypeSystem.SpecialTypes.TimeSignatureType`
- `FlowLang.TypeSystem.SpecialTypes.TuningType`
- `FlowLang.TypeSystem.SpecialTypes.TupleType`

**PrimitiveTypes — enumerate from `flow-lang/TypeSystem/PrimitiveTypes/*.cs` via `ls` (use the actual filename → class name mapping):**
- Include every primitive type file present. Verify each `<type fullname="...">` corresponds to an actual class in the Web build closure before adding (run `dotnet build -p:FlowTarget=Web` after writing and confirm no `IL2007: Could not resolve assembly` error).

**Array type (1 entry):**
- `FlowLang.TypeSystem.ArrayType`

**Interop boundary types (3 entries):**
- `FlowLang.StandardLibrary.Audio.AudioBuffer` (Plan 48-03 marshals `AudioBuffer.Data` across `[JSImport]` per PATTERNS.md Discrepancy 1 — name is `Data`, not `Samples`)
- `FlowLang.Runtime.Value` (Plan 48-03 `[JSExport]` returns may surface Value)
- `FlowLang.Audio.WebAudioBackend` (Plan 48-03 adds `[JSImport]` partials inside this class)

Add a header comment block at the top of the file:
```xml
<!-- Phase 48 D-48-02: trimmer-root descriptor preserving FlowType subclasses,
     music-type singletons, and the interop boundary types Phase 48 marshals across
     [JSImport]/[JSExport]. Per PATTERNS.md §Discrepancy 3 (2026-05-25 audit),
     InternalFunctionRegistry is NOT reflection-heavy — registration is via explicit
     Register(...) calls statically reachable from FlowEngine constructor — so the
     descriptor does NOT enumerate its methods. The real risk is *Type.Instance
     singleton accessors and the Value/AudioBuffer interop boundary. -->
```

Reference: Microsoft's [.NET Trimming docs — Root descriptors XML](https://learn.microsoft.com/en-us/dotnet/core/deploying/trimming/trimming-options#root-descriptors).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; test -f flow-lang/wasm/trim-roots.xml &amp;&amp; xmllint --noout flow-lang/wasm/trim-roots.xml 2>/dev/null &amp;&amp; grep -c 'preserve="all"' flow-lang/wasm/trim-roots.xml</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-lang/wasm/trim-roots.xml` exists and parses as valid XML (`xmllint --noout` returns 0).
    - `grep -c '<assembly fullname="flow-lang">' flow-lang/wasm/trim-roots.xml` returns 1.
    - `grep -c 'FlowLang.TypeSystem.FlowType\b' flow-lang/wasm/trim-roots.xml` returns at least 1.
    - `grep -c 'FlowLang.TypeSystem.SpecialTypes.NoteType' flow-lang/wasm/trim-roots.xml` returns 1.
    - `grep -c 'FlowLang.StandardLibrary.Audio.AudioBuffer' flow-lang/wasm/trim-roots.xml` returns 1.
    - `grep -c 'FlowLang.Runtime.Value\b' flow-lang/wasm/trim-roots.xml` returns 1.
    - `grep -c 'FlowLang.Audio.WebAudioBackend' flow-lang/wasm/trim-roots.xml` returns 1.
    - `grep -c 'FlowLang.TypeSystem.SpecialTypes.SfzType' flow-lang/wasm/trim-roots.xml` returns 0 (Sfz stripped — must not appear).
    - `grep -c 'FlowLang.TypeSystem.SpecialTypes.OscHandleType' flow-lang/wasm/trim-roots.xml` returns 0 (OscHandle stripped).
    - `grep -c 'InternalFunctionRegistry' flow-lang/wasm/trim-roots.xml` returns 0 (PATTERNS.md Discrepancy 3 — not needed).
    - Total `preserve="all"` entries between 25 and 50 (sanity range — too few = forgotten subclasses, too many = polluted).
  </acceptance_criteria>
  <done>trim-roots.xml exists, parses, enumerates FlowType subclasses + interop boundary types correctly per PATTERNS.md Discrepancy 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land WasmBuildPipelineTests xUnit smoke (publish exit 0 + AppBundle structure + bundle size record)</name>
  <files>flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs</files>
  <read_first>
    - flow-lang.Tests/Integration/Phase47/BuildConditioningSmokeTests.cs (the exact analog — lines 1-84)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-PATTERNS.md §"`flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` (NEW)" (lines 352-446)
    - flow-lang.Tests/Helpers/FlowTargetFactAttribute.cs (no skip needed — these tests RUN on Desktop, they shell out to a separate publish process)
  </read_first>
  <behavior>
    - `WasmPublish_ExitCodeIsZero`: `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release` exits 0.
    - `WasmPublish_ProducesAppBundle`: After successful publish, `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/_framework/dotnet.js` exists.
    - `WasmBundle_UncompressedSize_MeasuredAndRecorded`: Sum of every file under `AppBundle/_framework/**` is computed, printed via `Console.WriteLine` for CI capture, and asserted below a 30 MB UNCOMPRESSED hard cap. (D-48-03 / D-48-05 — 15 MB COMPRESSED target ≈ 30 MB uncompressed after Brotli; Plan 48-05 lazy-loads if exceeded.)
    - All three tests are `[Fact]` (NOT `[FlowTargetFact]`) because they SHELL OUT to a separate `dotnet publish` process — they run from a Desktop test runner regardless of FLOW_WEB.
    - All three tests share a private static `RunDotnetPublish(string args)` helper modelled on Phase 47's `RunDotnetBuild` but with publish args + 10-minute timeout (WASM publish is slow; jiterpreter generation alone takes ~30s).
    - Each test that calls publish gates on its exit code FIRST before asserting downstream properties (otherwise stdout/stderr from a failed publish would shadow the real failure).
  </behavior>
  <action>
Create `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` (parent directory likely does not exist yet — `mkdir -p flow-lang.Tests/Integration/Phase48/` before write). The file structure follows `BuildConditioningSmokeTests.cs` verbatim with these modifications:

1. Namespace: `FlowLang.Tests.Integration.Phase48` (not Phase47).
2. Class name: `WasmBuildPipelineTests`.
3. The `RunDotnetBuild` helper renamed to `RunDotnetPublish`. Command line: `dotnet publish flow-lang/flow-lang.csproj <args> -v quiet --nologo`. Timeout: `600_000` (10 minutes) instead of 120_000.
4. Three `[Fact]` methods per PATTERNS.md sample at lines 403-444:
   - `WasmPublish_ExitCodeIsZero` — invokes RunDotnetPublish("-p:FlowTarget=Web -c Release"), asserts code == 0 with stdout/stderr in message.
   - `WasmPublish_ProducesAppBundle` — runs publish, then asserts `Directory.Exists(...)` for `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/` AND `File.Exists(...)` for `_framework/dotnet.js`. Gate downstream assertions behind initial `Assert.True(code == 0, "Publish must succeed before checking bundle")`.
   - `WasmBundle_UncompressedSize_MeasuredAndRecorded` — runs publish, enumerates `new DirectoryInfo(frameworkDir).EnumerateFiles("*", SearchOption.AllDirectories).Sum(f => f.Length)`, prints via `Console.WriteLine($"[WasmBundle] uncompressed size: {total / 1_000_000} MB ({total} bytes)")`, asserts `Assert.True(total < 30 * 1024 * 1024, "...")`.

Per PATTERNS.md note at line 446-450: tests shell out via `dotnet publish` (not `dotnet build`) because the WASM AppBundle artifacts are only produced at publish time. The `WaitForExit(600_000)` (10 min) accommodates WASM publish slowness.

Per PATTERNS.md Pattern A: do NOT use `[FlowTargetFact("Web")]` on these — the tests run from Desktop (the standard test runner) and shell to a separate publish process. Plain `[Fact]` is correct.

Add namespace usings: `System`, `System.Diagnostics`, `System.IO`, `System.Linq`, `Xunit`. Match the analog file's `using` block.

If the `WaitForExit(600_000)` returns false (timeout exceeded), `Assert.Fail(...)` with the partial stdout/stderr captured up to that point so CI sees actionable output.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj -p:FlowTarget=Desktop -v quiet --nologo 2>&amp;1 | tail -3 &amp;&amp; dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase48.WasmBuildPipelineTests" -v quiet --nologo 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` exists and compiles (`dotnet build flow-lang.Tests -p:FlowTarget=Desktop` exits 0).
    - File contains exactly 3 `[Fact]` attributes: `grep -c '\[Fact\]' flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` returns 3.
    - File contains `dotnet publish flow-lang/flow-lang.csproj` in the helper command line: `grep -c 'publish flow-lang/flow-lang.csproj' flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` returns at least 1.
    - File contains the 10-minute timeout: `grep -c '600_000' flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` returns at least 1.
    - Running `dotnet test --filter "FullyQualifiedName~Phase48.WasmBuildPipelineTests"` invokes 3 tests and all 3 PASS (publish must succeed end-to-end).
    - `WasmBundle_UncompressedSize_MeasuredAndRecorded` prints `[WasmBundle] uncompressed size: N MB` to test stdout (captured by xUnit; visible via `dotnet test -v normal`).
  </acceptance_criteria>
  <done>3-fact xUnit smoke pinned; `dotnet publish -p:FlowTarget=Web -c Release` end-to-end green; uncompressed bundle size measured + recorded; below 30 MB hard cap.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MSBuild eval → build environment | csproj reads trim-roots.xml via `<TrimmerRootDescriptor Include>`; XML is in-repo and reviewed via git, no external fetch |
| `dotnet publish` subprocess → test runner | nested process launched by xUnit; stdout/stderr captured into Assert messages — no shell expansion |
| trim-roots.xml → ILLink trimmer | reads attribute strings, parses XML, applies preserve directives; the descriptor is INERT data (no executable surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | trim-roots.xml | mitigate | XML is in-repo, code-reviewed; `xmllint --noout` validation in acceptance criteria detects malformed edits early |
| T-48-02 | Denial of Service | `dotnet publish` test subprocess | accept | 10-min `WaitForExit` timeout caps runaway publish; longer than Desktop build's 2 min but proportional to actual Mono-WASM publish time observed in research |
| T-48-03 | Information disclosure | WasmEmitSymbolMap in production | mitigate | D-48-04: WasmEmitSymbolMap gated on `Configuration == 'Debug'` — Release builds strip symbol maps (no source structure leaked to browser) |
| T-48-04 | Tampering (supply chain) | no new NuGet packages | accept | Plan 48-01 adds ZERO new NuGet PackageReferences (Mono-WASM ships in .NET 10 SDK); no package-legitimacy gate triggered |
</threat_model>

<verification>
1. `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Desktop -v quiet --nologo` exits 0 (Phase 47 Desktop baseline preserved).
2. `dotnet build flow-lang/flow-lang.csproj -p:FlowTarget=Web -v quiet --nologo` exits 0 (Phase 47 Web library compile preserved).
3. `dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release -v quiet --nologo` exits 0 (Phase 48 new acceptance — first WASM publish).
4. `flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/_framework/dotnet.js` exists after the publish.
5. Total uncompressed `_framework/` size below 30 MB (D-48-05 hard cap — Plan 48-05 lazy-loads if exceeded).
6. `xmllint --noout flow-lang/wasm/trim-roots.xml` exits 0 (XML well-formed).
7. `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase48.WasmBuildPipelineTests"` reports 3 passed / 0 failed.
8. Full Phase 47 test suite still green: `dotnet test flow-lang.Tests/flow-lang.Tests.csproj --filter "FullyQualifiedName~Phase47"` reports same pass count as Phase 47 closer baseline (2127 PASS / 9 SKIP / 0 FAIL ± Phase 47 baseline).
</verification>

<success_criteria>
- `flow-lang.csproj` extended in-place (no new conditional blocks).
- `flow-lang/wasm/trim-roots.xml` enumerates FlowType subclasses + AudioBuffer + Value + WebAudioBackend per PATTERNS.md Discrepancy 3.
- `flow-lang.Tests/Integration/Phase48/WasmBuildPipelineTests.cs` lands 3 xUnit Facts; all 3 PASS.
- Zero new NuGet packages.
- Uncompressed bundle size measured and recorded in test output; below 30 MB hard cap.
</success_criteria>

<output>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-01-SUMMARY.md` when done. Record: actual measured uncompressed bundle size, exact `dotnet publish` wall-clock time observed locally, any IL2xxx trimmer warnings emitted at publish time (Plan 48-05 input).
</output>
