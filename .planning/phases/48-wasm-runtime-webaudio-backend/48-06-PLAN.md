---
phase: 48-wasm-runtime-webaudio-backend
plan: 06
type: execute
wave: 5
depends_on: ["48-04", "48-05"]
files_modified:
  - .planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md
autonomous: false
requirements:
  - REQ-WEBAUDIO-04
  - REQ-WASM-API-03

must_haves:
  truths:
    - "Composer (human) loads the published AppBundle in a real browser and hears an audible 440 Hz tone"
    - "User-gesture chain works: clicking Run triggers audio (no autoplay) per D-48-09"
    - "Tested in Chrome 120+, Firefox 121+, Safari 17+ (3 separate browser smoke rows)"
    - "Composer verifies structured RunResult shape in browser DevTools console (stdout + errors[] visible)"
    - "Any browser-specific gotchas captured in 48-HUMAN-UAT.md before Plan 48-07 closer"
  artifacts:
    - path: ".planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md"
      provides: "3 HUMAN-UAT rows (Chrome/Firefox/Safari) with reproducible steps + composer sign-off slots + per-browser gotcha capture"
      min_lines: 50
  key_links:
    - from: ".planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md"
      to: "flow-lang/wasm/index.html (Plan 48-04)"
      via: "manual smoke harness ships with bundle; composer serves via `python3 -m http.server` and visits"
      pattern: "index\\.html"
---

<objective>
Pause for the composer to validate the Phase 48 WASM runtime end-to-end in three real browsers. This is the ONE plan in Phase 48 marked `autonomous: false` — Claude cannot smoke-test browser audio output autonomously.

Purpose: D-48-09 (autoplay policy compliance), D-48-12 (ES module loads), D-48-13 (API surface), D-48-14 (structured RunResult), D-48-15 (stdout/stderr split) ALL require browser-side verification. The Plan 48-03 + 48-04 + 48-05 xUnit suites pin the C# contract; only a human ear + a browser DevTools session can verify that the audio actually comes out and the playground UX is sane.

Output:
- `48-HUMAN-UAT.md` — 3 HUMAN-UAT rows (Chrome 120+ / Firefox 121+ / Safari 17+) with reproducible steps, composer sign-off slots, and per-browser gotcha capture.

**Scope boundary**: this plan does NOT modify production code. It creates the UAT scaffold + waits for the composer to fill in observations. Plan 48-07 (closer) reads 48-HUMAN-UAT.md and either closes Phase 48 or routes outstanding gotchas to Phase 49 / v1.6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-CONTEXT.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-04-PLAN.md
@.planning/phases/48-wasm-runtime-webaudio-backend/48-05-PLAN.md
@flow-lang/wasm/flow-runtime.js
@flow-lang/wasm/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 48-HUMAN-UAT.md with 3 browser smoke rows + reproducible steps</name>
  <files>.planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md</files>
  <read_first>
    - flow-lang/wasm/index.html (Plan 48-04 — verify the textarea pre-fill content + Run button wiring)
    - flow-lang/wasm/flow-runtime.js (Plan 48-04 — verify the loadFlowRuntime + run + play + resumeAudio surface)
    - .planning/phases/17/17-HUMAN-UAT.md OR any previous Phase HUMAN-UAT.md (look for a precedent format — Phase 17 / Phase 34 / Phase 37 typically have them)
    - .planning/phases/48-wasm-runtime-webaudio-backend/48-RESEARCH.md §"Validation Architecture" HUMAN-UAT (3 rows) — Chrome/Firefox/Safari
  </read_first>
  <action>
Create `.planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md` with the following structure:

```markdown
# Phase 48 — HUMAN-UAT: WASM Runtime Browser Smoke

**Status:** Pending composer verification
**Prerequisites:** Plan 48-04 shipped (flow-runtime.js + WasmEntry.cs + index.html in AppBundle).

## Setup

```bash
# 1. Publish the WASM bundle
dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release

# 2. Serve the AppBundle locally (Python 3.x available on most dev machines;
#    `python -m http.server` on macOS/Linux; the binary is `python3` on most systems)
cd flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/
python3 -m http.server 8080

# 3. In another terminal/browser, visit:
http://localhost:8080/wasm/index.html
```

If the relative-path adjustment in Plan 48-04 placed flow-runtime.js at AppBundle root,
visit `http://localhost:8080/index.html` instead. Match the actual layout per
48-04-SUMMARY.md.

## Reproducible Steps (run per browser)

For each of the 3 browser rows below:

1. Open the browser DevTools console BEFORE navigating to the URL.
2. Visit `http://localhost:8080/wasm/index.html` (or the adjusted path).
3. Wait for the runtime to boot. Expect to see one of:
   - In DevTools console: "Flow runtime loaded" (if flow-runtime.js logs on boot — verify in Plan 48-04 implementation).
   - In the page: the textarea pre-filled with `(play (createSineTone 440Hz 1.0 0.5))` + a Run button.
4. Click the Run button. Expect:
   - An audible 440 Hz tone for ~1 second at 50% volume.
   - The `stdout` pane displays no output (sine tone doesn't print anything).
   - The `errors` pane displays no entries.
   - In DevTools console (Network tab): a single `dotnet.js` load + a few `_framework/*` loads; no 404s.
5. Modify the textarea to `(print "hello flow")` and click Run again. Expect:
   - No audio.
   - The `stdout` pane shows `hello flow`.
   - No errors.
6. Modify the textarea to `(print (add 1 2))` and click Run. Expect: stdout shows `3`.
7. Try a deliberate parse error: `(print` (unclosed paren). Expect:
   - The `errors` pane shows at least one entry with `kind: "parse"`.
   - No crash; the page is still responsive.

## Per-Browser Rows

### Row 1: Chrome 120+ (Linux/macOS/Windows)

| Field | Value |
|-------|-------|
| Browser version | (composer fills in) |
| OS + version | (composer fills in) |
| Boot time (DevTools Network → DOMContentLoaded) | (composer fills in, target <3s) |
| Sine tone audible? | (yes/no) |
| stdout split working? | (yes/no) |
| errors[] structured? | (yes/no — inspect via `console.log(runtime.run(...))` in DevTools) |
| Autoplay policy: audio blocked before Run click? | (yes/no — should be YES; if NO, D-48-09 contract violated) |
| Composer sign-off | (initials + date OR "blocked: <reason>") |
| Gotchas observed | (free text) |

### Row 2: Firefox 121+ (Linux/macOS/Windows)

| Field | Value |
|-------|-------|
| Browser version | (composer fills in) |
| OS + version | (composer fills in) |
| Boot time | (composer fills in) |
| Sine tone audible? | (yes/no) |
| stdout split working? | (yes/no) |
| errors[] structured? | (yes/no) |
| Autoplay policy: audio blocked before Run click? | (yes/no) |
| Composer sign-off | (initials + date OR "blocked: <reason>") |
| Gotchas observed | (free text) |

### Row 3: Safari 17+ (macOS only, optional if composer is on Linux)

| Field | Value |
|-------|-------|
| Browser version | (composer fills in) |
| OS + version | (composer fills in) |
| Boot time | (composer fills in) |
| Sine tone audible? | (yes/no) |
| stdout split working? | (yes/no) |
| errors[] structured? | (yes/no) |
| Autoplay policy: audio blocked before Run click? | (yes/no) |
| Composer sign-off | (initials + date OR "skipped: no macOS available") |
| Gotchas observed | (free text) |

## Closure Conditions

Phase 48 HUMAN-UAT passes if AND ONLY IF:
- Row 1 (Chrome) signed off as pass OR documented gotcha non-blocking.
- Row 2 (Firefox) signed off as pass OR documented gotcha non-blocking.
- Row 3 (Safari) signed off as pass OR explicitly skipped with reason "no macOS available — defer to Phase 49 / v1.6".

If any row fails with a blocking issue:
- Plan 48-07 (closer) reads this file, routes the issue to either:
  - In-phase repair: open Plan 48-06.1 to fix.
  - v1.6 deferral: log the gotcha in MILESTONES.md v1.6 backlog.
  - Phase 49 hand-off: if the issue is browser UX-shaped (NOT a runtime defect).

## Composer Notes

(Free-form notes block — composer documents anything observed that the structured rows don't cover. Browser-specific [JSImport] quirks, AudioContext.resume() behavior differences, sample rate negotiation, etc.)
```

Once the file lands, the executor's Phase 48-06 work is DONE. Plan 48-07 (closer) reads the file at completion-check time to determine whether Phase 48 ships or splits.
  </action>
  <verify>
    <automated>test -f .planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md &amp;&amp; wc -l .planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md &amp;&amp; grep -c "Row 1: Chrome\|Row 2: Firefox\|Row 3: Safari" .planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md` exists.
    - At least 50 lines.
    - Contains 3 rows: "Row 1: Chrome", "Row 2: Firefox", "Row 3: Safari".
    - Each row has at least 8 fields (browser version, OS, boot time, sine tone, stdout split, errors structured, autoplay policy, composer sign-off + gotchas).
    - Contains a "Setup" section with the `dotnet publish` + `python3 -m http.server` commands.
    - Contains a "Reproducible Steps" section with 7 numbered steps.
    - Contains a "Closure Conditions" section listing the pass criteria.
    - NO production code modified by this plan.
  </acceptance_criteria>
  <done>48-HUMAN-UAT.md scaffold landed; ready for composer browser-smoke run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 48-06's deliverable is the 48-HUMAN-UAT.md scaffold (Task 1 above). The checkpoint is the composer's actual browser-smoke run — Claude pauses here and the composer fills in the 3 rows.
  </what-built>
  <how-to-verify>
1. Open a terminal, run:
   ```bash
   dotnet publish flow-lang/flow-lang.csproj -p:FlowTarget=Web -c Release
   cd flow-lang/bin/Release/net10.0/browser-wasm/publish/AppBundle/
   python3 -m http.server 8080
   ```

2. In Chrome 120+: open DevTools (F12), then visit `http://localhost:8080/wasm/index.html` (or the adjusted path per 48-04-SUMMARY.md).
   - Verify the page loads + textarea is pre-filled.
   - Click Run → expect audible 440 Hz tone for 1 second.
   - Try the 7 reproducible steps in 48-HUMAN-UAT.md.
   - Fill in Row 1 fields including Composer sign-off.

3. Repeat for Firefox 121+ → fill Row 2.

4. (Optional, macOS only) Repeat for Safari 17+ → fill Row 3. If Linux-only dev machine, skip with reason "no macOS available" — Plan 48-07 routes Safari verification to Phase 49 or v1.6.

5. Document any browser-specific gotchas in the Gotchas column.

6. When all rows are filled (sign-off OR skipped-with-reason), commit 48-HUMAN-UAT.md + reply "approved" (Phase 48 ready for closer) or "approved-with-followup" (Plan 48-07 schedules v1.6 follow-up for documented gotchas) or describe blocking issues.
  </how-to-verify>
  <resume-signal>Type "approved" (all rows pass), "approved-with-followup" (gotchas documented; closer schedules v1.6 follow-up), or describe blocking issues that require Plan 48-06.1 repair.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer browser session → local AppBundle | composer serves the AppBundle from `localhost:8080` via Python http.server; no remote network exposure |
| 48-HUMAN-UAT.md edits → git repo | composer commits the filled-in row data; standard PR review applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-21 | Information disclosure | DevTools console exposes source maps in Debug mode | mitigate | D-48-04 already gated WasmEmitSymbolMap on `Configuration == 'Debug'`; UAT runs Release build per Setup commands — no source structure leaked |
| T-48-22 | Tampering | composer's local http.server is unauthenticated | accept | Local-only smoke (localhost:8080); composer's responsibility to not expose the port externally during testing |
| T-48-23 | Elevation of privilege | autoplay policy bypass in any browser | mitigate | UAT explicitly tests "audio blocked before Run click" field per row — D-48-09 contract verified by human ear |
</threat_model>

<verification>
1. `.planning/phases/48-wasm-runtime-webaudio-backend/48-HUMAN-UAT.md` exists with the 3-row scaffold.
2. Composer browser-smoke run completed; each row has Composer sign-off OR documented skip reason.
3. Plan 48-07 (closer) reads the file and routes per Closure Conditions.

(Verification depends on composer interaction — Claude cannot fully verify without the browser session.)
</verification>

<success_criteria>
- 48-HUMAN-UAT.md scaffold landed with 3 browser rows + setup + reproducible steps + closure conditions.
- Composer signs off on at least Chrome + Firefox rows (Safari optional per platform availability).
- Any blocking issues route to either Plan 48-06.1 repair OR v1.6 backlog OR Phase 49 hand-off per closure conditions.
</success_criteria>

<output>
The composer's filled-in 48-HUMAN-UAT.md IS the output. Plan 48-07 (closer) consumes it. No SUMMARY.md from this plan beyond a brief acknowledgement that the checkpoint completed (Claude writes 48-06-SUMMARY.md after the human verification step resumes).
</output>
