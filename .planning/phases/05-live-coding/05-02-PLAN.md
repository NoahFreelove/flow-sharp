---
phase: 05-live-coding
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - flow-interpreter/Program.cs
  - tests/test_live_reload.flow
autonomous: false
requirements: [LIVE-01, LIVE-02]
must_haves:
  truths:
    - "User can run `--watch` and hear playback start immediately"
    - "User can edit the .flow file during playback and hear changes at the next bar boundary"
    - "If the edited file has a syntax error, playback continues with the previous version and the error is displayed"
    - "First Ctrl+C stops playback, second exits"
    - "Terminal shows 'Reloaded at bar N' on successful reload and error message on failed reload"
  artifacts:
    - path: "flow-interpreter/Program.cs"
      provides: "Refactored RunWithWatch delegates to LiveReloadManager"
      contains: "LiveReloadManager"
    - path: "tests/test_live_reload.flow"
      provides: "Test script that produces audio output suitable for live reload testing"
      min_lines: 10
  key_links:
    - from: "flow-interpreter/Program.cs"
      to: "flow-interpreter/LiveReloadManager.cs"
      via: "RunWithWatch creates and runs LiveReloadManager"
      pattern: "new LiveReloadManager"
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "flow-lang/Core/FlowEngine.cs"
      via: "Initial and background execution"
      pattern: "FlowEngine"
---

<objective>
Wire LiveReloadManager into Program.cs by refactoring RunWithWatch, add a test script for manual live-coding verification, and perform end-to-end validation.

Purpose: The infrastructure from Plan 01 is useless until wired into the CLI entry point. This plan completes the user-facing integration and provides a test script for manual verification.

Output: Refactored Program.cs, test_live_reload.flow test script
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-live-coding/05-CONTEXT.md
@.planning/phases/05-live-coding/05-01-SUMMARY.md

@flow-interpreter/Program.cs
@flow-interpreter/LiveReloadManager.cs

<interfaces>
<!-- From Plan 01 outputs -->

From flow-interpreter/LiveReloadManager.cs:
```csharp
public class LiveReloadManager : IDisposable
{
    public LiveReloadManager(string filePath, string? deviceName);
    public void Run();  // Blocks until stopped
    // Implements IDisposable
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Program.cs RunWithWatch to use LiveReloadManager and create test script</name>
  <files>
    flow-interpreter/Program.cs,
    tests/test_live_reload.flow
  </files>
  <read_first>
    flow-interpreter/Program.cs,
    flow-interpreter/LiveReloadManager.cs,
    tests/test_comprehensive.flow
  </read_first>
  <action>
    **Program.cs** -- Replace the body of `RunWithWatch(string filePath, string? deviceName)` (per D-14):

    The existing method (lines 126-194) manually creates a FlowEngine, FileSystemWatcher, debounce logic, and stop/re-execute cycle. Replace it entirely with:

    ```csharp
    static int RunWithWatch(string filePath, string? deviceName)
    {
        var fullPath = Path.GetFullPath(filePath);
        using var manager = new LiveReloadManager(fullPath, deviceName);
        manager.Run();
        return 0;
    }
    ```

    Remove the `ExecuteScript(FlowEngine, string)` helper method since it is no longer used by `RunWithWatch`. Check if any other method uses it -- if not, remove it. If `RunFromString` or `RunFromStdin` use similar logic, leave those untouched.

    Actually, keep `ExecuteScript` if `ScriptRunner` or other code references it. Grep first. If only `RunWithWatch` used it, remove it.

    Also remove `ConfigureDevice` ONLY if it is not used elsewhere. It is used by `RunFromString` and `RunFromStdin`, so keep it. LiveReloadManager handles device configuration internally.

    **tests/test_live_reload.flow** -- Create a simple test script that produces audio output suitable for live-coding testing:

    ```flow
    use "@std"
    use "@audio"

    tempo 120 {
      timesig 4/4 {
        key Cmajor {
          section main {
            | C4q D4q E4q F4q |
          }
    
          Song song = [main]
          Buffer result = renderSong(song, "piano")
          play(result)
        }
      }
    }
    ```

    This script is intentionally simple -- 1 bar of C major scale at 120 BPM. During manual testing, the user can edit notes (e.g., change `C4q` to `G4q`) and hear the change at the next bar boundary.

    Add a comment at the top of the test file:
    ```
    // Test script for live reload (--watch mode)
    // Run: dotnet run --project flow-interpreter -- --watch tests/test_live_reload.flow
    // Edit notes while playing to test bar-boundary reload
    ```
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "LiveReloadManager" flow-interpreter/Program.cs
    - grep -q "manager.Run" flow-interpreter/Program.cs
    - grep -c "FileSystemWatcher" flow-interpreter/Program.cs returns 0 (watcher moved to LiveReloadManager)
    - test -f tests/test_live_reload.flow
    - grep -q "renderSong" tests/test_live_reload.flow
    - dotnet build succeeds with 0 errors
  </acceptance_criteria>
  <done>
    Program.cs RunWithWatch delegates to LiveReloadManager. The old manual watch loop, FileSystemWatcher, and debounce logic are removed from Program.cs. test_live_reload.flow exists with a simple 1-bar piano pattern for manual live-coding testing. Build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live-coding works end-to-end</name>
  <files>none -- verification only</files>
  <action>
    Human verifies the complete live-coding experience by running watch mode, editing a file during playback, observing bar-boundary reload, testing error resilience, and confirming Ctrl+C behavior. No code changes in this task.
  </action>
  <what-built>
    Complete live-coding system: continuous streaming playback with bar-boundary buffer swapping on file save. Background rendering via capture mode. Error resilience (keeps playing old version on syntax errors).
  </what-built>
  <how-to-verify>
    1. Start live-coding mode:
       ```
       dotnet run --project flow-interpreter -- --watch tests/test_live_reload.flow
       ```
    2. Confirm you hear the C major scale pattern (C4 D4 E4 F4) looping continuously
    3. While it plays, edit `tests/test_live_reload.flow` -- change `C4q D4q E4q F4q` to `G4q A4q B4q C5q`
    4. Save the file. Confirm you see "Reloaded at bar N" in the terminal
    5. Confirm the new notes (G4 A4 B4 C5) start playing at the next bar -- no gap, click, or restart from silence
    6. Introduce a syntax error (e.g., delete a closing brace), save
    7. Confirm you see an error message in the terminal and the previous version keeps playing
    8. Fix the error, save again. Confirm reload succeeds
    9. Press Ctrl+C once -- confirm playback stops but watch mode continues
    10. Press Ctrl+C again -- confirm the program exits
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification checkpoint -- requires audio hardware and human judgment"</automated>
  </verify>
  <done>
    User confirms: playback starts on --watch, edits swap at bar boundary with no gap, errors keep old version playing, Ctrl+C behavior works correctly.
  </done>
  <resume-signal>Type "approved" or describe any issues with the live-coding experience</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds with 0 errors
- `Program.cs` uses `LiveReloadManager` instead of manual watch loop
- `tests/test_live_reload.flow` exists and produces audio
- All existing tests pass: `for test in tests/test_*.flow; do dotnet run --project flow-interpreter "$test"; done`
- Manual live-coding session confirms bar-boundary reload with no audible gap
</verification>

<success_criteria>
- Build compiles with zero errors
- Live-coding mode starts with `--watch` flag and plays audio continuously
- File edits swap at bar boundary with "Reloaded at bar N" feedback
- Syntax errors keep old version playing with error displayed
- Ctrl+C stops playback, second Ctrl+C exits
- No audible gap, click, or silence during reload
- All existing test scripts still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-coding/05-02-SUMMARY.md`
</output>
