---
phase: 44-strict-mode
plan: 06
type: execute
wave: 4
depends_on:
  - 44-02
  - 44-05
files_modified:
  - flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs
  - flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs
  - flow-lang/StandardLibrary/Patterns/PatternFunctions.cs
  - flow-lang/StandardLibrary/Audio/SongRenderer.cs
  - flow-lang/StandardLibrary/Audio/SampledInstrumentRenderer.cs
  - flow-lang/Interpreter/ExpressionEvaluator.cs
  - flow-lang/StandardLibrary/Audio/DSP/GranularFunctions.cs
  - flow-lang/StandardLibrary/Audio/DSP/PitchShiftFunctions.cs
  - flow-lang/StandardLibrary/Audio/DSP/StretchEngine.cs
  - flow-lang/StandardLibrary/Audio/DSP/StretchFunctions.cs
  - flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_High.cs
autonomous: true
requirements:
  - REQ-STRICT-08
tags:
  - phase-44
  - wave-4
  - axis-b
  - advisory-high
must_haves:
  truths:
    - "D-06 + D-07 + AUDIT §6b — all HIGH-priority advisory sites convert from WarnOnce to strict-error per RESEARCH §Site Inventory + AUDIT §7b"
    - "HIGH cluster covers: Audio/Sfz/* (22 sites) + Patterns/PatternFunctions.cs (17) + Audio/SongRenderer.cs (2) + Audio/SampledInstrumentRenderer.cs (3) + Audio/ExpressionEvaluator match-non-exhaustive (1) + Audio/DSP/* (5)"
    - "≈ 50 sites total in this plan (per AUDIT §6b HIGH grouping; Patterns 17 + SFZ 22 + Render 2 + Sampled 3 + Match 1 + DSP 5)"
    - "Error string format `[strict] <existing-tag> <existing-body>` — sentinel body preserved verbatim per D-07 + Pitfall 5 determinism"
    - "Non-strict path preserved byte-identical (WarnOnce sentinel + key unchanged)"
    - "5 carve-out sites STAY untouched (Interpreter.cs:476 + StyleRegistry.cs:156/244/258/265) per Pitfall 2"
  artifacts:
    - path: "flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs"
      provides: "Strict-mode branches at SFZ surface 22 sites"
      contains: "CallerStrictMode"
    - path: "flow-lang/StandardLibrary/Patterns/PatternFunctions.cs"
      provides: "Strict-mode branches at 17 pattern combinator sites"
      contains: "CallerStrictMode"
    - path: "flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_High.cs"
      provides: "xUnit Theory pinning all HIGH-priority sentinels via StrictErrorManifestLoader.LoadHighPrioritySites()"
      contains: "Axis_B_AdvisorySiteTests_High"
  key_links:
    - from: "flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "ctx.CallerStrictMode read at each in-scope WarnOnce site"
      pattern: "CallerStrictMode"
    - from: "flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_High.cs"
      to: ".planning/phases/44-strict-mode/strict-error-manifest.csv"
      via: "StrictErrorManifestLoader.LoadHighPrioritySites()"
      pattern: "LoadHighPrioritySites"
---

<objective>
Convert ~50 HIGH-priority `WarnOnce` advisory sites (AUDIT §6b HIGH grouping per §7b LOAD-BEARING list) to `[strict] ` errors when `ctx.CallerStrictMode==true`. Modules covered: `Audio/Sfz/` (22 sites — SfzBuiltins/SfzParser/SfzRenderer/SfzSampleCache) + `Patterns/PatternFunctions.cs` (17) + `Audio/SongRenderer.cs` (2) + `Audio/SampledInstrumentRenderer.cs` (3) + the `match-non-exhaustive` advisory at ExpressionEvaluator.cs:569 (1) + `Audio/DSP/*` (5).

Purpose: this is the largest mechanical-text-rewrite plan in Phase 44. It ships the bulk of Axis B in one focused commit. RESEARCH Pattern S3 + MarkovFunctions.cs:247-258 supply the per-site template; the manifest CSV from Plan 44-00 supplies the exact error strings.

Output: ~50 mechanical site rewrites across 12 production files, one MemberData-driven Theory test sourcing rows from the manifest's HIGH priority partition.
</objective>

<scope_note>
**Plan size justification (13 files modified — at planner threshold):**

This plan touches 13 files, hitting the planner's "consider splitting" signal. Decision (per W7/W11 review): **NOT SPLIT**, with the following justification:

1. **All edits are mechanical Pattern S3 rewrites** — each site converts the legacy WarnOnce + fallback shape to the strict-aware `if (ctx.CallerStrictMode) er.ReportError(... ctx.CurrentCallSite); return Value.Void(); else WarnOnce(...); fallback;` shape. No new logic, no new types, no new control flow.

2. **The CSV manifest from Plan 44-00 drives every rewrite** — the executor reads `StrictErrorManifestLoader.LoadXxxPrioritySites()` and walks the manifest rows, applying the same template at each site. Files-touched count is a function of the HIGH-priority advisory cluster (~50 sites); splitting would not reduce per-task complexity.

3. **Single Theory test covers all sites** — `Axis_B_AdvisorySiteTests_*` uses `[MemberData]` to drive all rows from one source-of-truth. Splitting would duplicate the Theory data-source plumbing.

4. **Context budget verification:** Per planner scope estimation, mechanical rewrites with shared template ~= 5-10% context per site bundle. Total estimated context for this plan: ~30-40% (within the 50% target).

If executor reports context pressure mid-plan, fall back to splitting on the natural module boundaries documented in `must_haves.truths`.
</scope_note>


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-strict-mode/44-CONTEXT.md
@.planning/phases/44-strict-mode/44-RESEARCH.md
@.planning/phases/44-strict-mode/44-PATTERNS.md
@.planning/phases/44-strict-mode/44-VALIDATION.md
@.planning/phases/44-strict-mode/44-02-PLAN.md
@.planning/phases/44-strict-mode/44-00-PLAN.md
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT.md

<interfaces>
<!-- Canonical advisory-site rewrite shape -->

From RESEARCH §"Code Examples" Axis B site rewrite — `[markov] order clamped to [1, 3]` (representative):
```csharp
// BEFORE
if (order > 3 || order < 1)
{
    RenderingDiagnostics.WarnOnce(
        $"markov:order-clamped:{ctx.CurrentCallSite}",
        $"[markov] order {order} clamped to [1, 3]");
    order = Math.Clamp(order, 1, 3);
}

// AFTER
if (order > 3 || order < 1)
{
    if (ctx.CallerStrictMode)
    {
        er.ReportError(
            $"[strict] [markov] order {order} clamped to [1, 3]",
            ctx.CurrentCallSite);
        return Value.Void();
    }
    RenderingDiagnostics.WarnOnce(
        $"markov:order-clamped:{ctx.CurrentCallSite}",
        $"[markov] order {order} clamped to [1, 3]");
    order = Math.Clamp(order, 1, 3);
}
```

Per RESEARCH §Pattern S3 + Pattern 6 Option (b): the enclosing builtin method must already accept `ExecutionContext ctx, ErrorReporter er` (most HIGH-priority modules already do — verify per-module at task start; if not, restructure via context-dependent re-registration mirroring Phase 36 MarkovFunctions.RegisterContextDependent).

HIGH cluster site counts per AUDIT §6b + RESEARCH §"Site Inventory":
| Module | Sites | Notes |
|--------|-------|-------|
| Audio/Sfz/SfzBuiltins.cs | 3 | `[sfz]` mostly load-time |
| Audio/Sfz/SfzParser.cs | 16 | `[sfz]` opcode / region parse advisories |
| Audio/Sfz/SfzRenderer.cs | 6 | `[sfz]` runtime — seq_length clamp, region miss, etc. |
| Audio/Sfz/SfzSampleCache.cs | 2 | `[sfz]` sample load |
| Patterns/PatternFunctions.cs | 17 | every/chunk/jux/sometimes/degrade — Phase 36 D-v1.5-05 opts INTO strict here |
| Audio/SongRenderer.cs | 2 | `[render]` unknown instrument / pool exhausted |
| Audio/SampledInstrumentRenderer.cs | 3 | `[piano]` mp_mf missing / release clamped |
| Interpreter/ExpressionEvaluator.cs:569 | 1 | `[match] non-exhaustive pattern` — Phase 35 hook |
| Audio/DSP/GranularFunctions.cs | 1 | `[granular]` windowing fallback |
| Audio/DSP/PitchShiftFunctions.cs | 1 | `[pitchShift]` > 12 st advisory |
| Audio/DSP/StretchEngine.cs | 2 | `[stretch]` mode=auto picks |
| Audio/DSP/StretchFunctions.cs | 1 | `[stretch]` related |
| **Total HIGH** | **~55** | (slightly more than RESEARCH's "~50" estimate; reconciled via manifest) |

Manifest source: `StrictErrorManifestLoader.LoadHighPrioritySites()` from Plan 44-00 — returns all rows with `priority==HIGH` AND `carve_out==false`. The Theory test consumes this MemberData source directly; no hardcoded sentinel lists in the test file.

Carve-out reminder per Pitfall 2: SKIP these 5 sites (do NOT modify):
- `flow-lang/Interpreter/Interpreter.cs:476` — `[live] entering live block`
- `flow-lang/StandardLibrary/Improv/StyleRegistry.cs:156, 244, 258, 265` — 4 `[improv]` style-pack
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert SFZ + Patterns + Render + Match HIGH-priority WarnOnce sites (~44 sites across 8 files) to strict-mode branches</name>
  <files>
    flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs,
    flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs,
    flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs,
    flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs,
    flow-lang/StandardLibrary/Patterns/PatternFunctions.cs,
    flow-lang/StandardLibrary/Audio/SongRenderer.cs,
    flow-lang/StandardLibrary/Audio/SampledInstrumentRenderer.cs,
    flow-lang/Interpreter/ExpressionEvaluator.cs
  </files>
  <read_first>
    All 8 files listed in files_modified — focus on the WarnOnce call sites identified by `scripts/audit/strict-site-grep.sh` (Plan 44-00),
    flow-lang/StandardLibrary/Generative/MarkovFunctions.cs lines 247-258 (canonical exemplar — Pattern S3 template),
    .planning/phases/44-strict-mode/strict-error-manifest.csv (Plan 44-00 — filter rows where priority=HIGH AND file_path matches one of the 8 files above),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Site Inventory" + §"Pattern S3" + §"Pitfall 2 carve-outs",
    .planning/phases/44-strict-mode/44-PATTERNS.md §"Pattern S3 WarnOnce → strict-error per-site rewrite"
  </read_first>
  <action>
    For each WarnOnce(...) call in the 8 files matching the HIGH-priority partition of the manifest, apply the Pattern S3 rewrite verbatim:
    1. Verify the enclosing method has access to `ExecutionContext ctx` + `ErrorReporter er` (or `errorReporter`). Most HIGH-priority modules already use the Phase 36 context-dependent registration pattern; if a target method doesn't, restructure to closure-capture form mirroring MarkovFunctions.RegisterContextDependent.
    2. Locate each WarnOnce(`<key>`, `<body>`) call site. Insert a `if (ctx.CallerStrictMode) { er.ReportError($"[strict] {<body verbatim with `[strict] ` prefix>}", ctx.CurrentCallSite); return <early-return value matching the surrounding method's signature>; }` ABOVE the existing WarnOnce call.
    3. Preserve the existing WarnOnce key + body strings byte-identical (CLAUDE.md two-run cmp-clean + Pitfall 5).
    4. Determine the early-return value per method:
       - Void-returning methods → `return;`
       - Value-returning builtins → `return Value.Void();` (per RESEARCH §Code Examples) — non-strict path returns the legitimate fallback (clamped result, charitable substitute, etc.), strict path returns Void to signal the error.
       - Pure helpers (no Value return) → omit the explicit return; rely on the strict branch raising the error and letting normal control flow exit.

    Per file:

    `flow-lang/StandardLibrary/Audio/Sfz/SfzBuiltins.cs` — 3 sites. Verify each existing call already has ctx available; if not, the SFZ builtins are already context-bound per Phase 33 D-33-04 — closure capture exists at registration.

    `flow-lang/StandardLibrary/Audio/Sfz/SfzParser.cs` — 16 sites. The SfzParser is invoked from SfzBuiltins; thread ctx + er into the SfzParser entry points if not already (use field initialization OR explicit parameter on the parse-entry method).

    `flow-lang/StandardLibrary/Audio/Sfz/SfzRenderer.cs` — 6 sites. The SfzRenderer is constructed fresh per render boundary per Phase 37 D-37-15; thread ctx + er via constructor injection.

    `flow-lang/StandardLibrary/Audio/Sfz/SfzSampleCache.cs` — 2 sites. Cache is per-FlowEngine; thread ctx via cache method signatures or via the consumer-supplied ExecutionContext at lookup time.

    `flow-lang/StandardLibrary/Patterns/PatternFunctions.cs` — 17 sites. Patterns already register via context-dependent path per Phase 36 D-v1.5-05. Each combinator (every/chunk/jux/sometimes/degrade/sparseSeq/etc.) has its WarnOnce sites — apply the strict branch per site.

    `flow-lang/StandardLibrary/Audio/SongRenderer.cs` — 2 sites. The renderer has direct access to context; locate the 2 WarnOnce calls (`[render] unknown instrument` + `[render] voice pool exhausted`) and apply the strict branch.

    `flow-lang/StandardLibrary/Audio/SampledInstrumentRenderer.cs` — 3 sites. Phase 29/37 sample renderer; verify ctx + er parameters in the rendering pipeline. Apply 3 strict-branch sites.

    `flow-lang/Interpreter/ExpressionEvaluator.cs:569` — 1 site (`[match] non-exhaustive pattern` advisory from Phase 35). ExpressionEvaluator already has direct context access via `_context`. Apply the strict branch with the `[strict] ` prefix. Per CONTEXT D-06: the matchExhaustive pragma is independent from `enable strict;` — but Plan 44-06 promotes the runtime non-exhaustive advisory under strict regardless of matchExhaustive pragma. Verify by re-reading RESEARCH §"Site Inventory" before locking the wording.

    Per Pitfall 2 — DO NOT modify these 5 carve-out sites in this plan:
    - `flow-lang/Interpreter/Interpreter.cs:476` — `[live] entering live block`
    - `flow-lang/StandardLibrary/Improv/StyleRegistry.cs:156, 244, 258, 265`

    Per Pitfall 5 — every error string is a deterministic concat of args + verbatim sentinel; NO PRNG/clock/Guid.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; dotnet run --project flow-interpreter tests/test_comprehensive.flow &gt; /dev/null &amp;&amp; dotnet run --project flow-interpreter tests/test_chord_runtime.flow &gt; /dev/null</automated>
  </verify>
  <done>
    ~44 strict-branch sites in 8 files; non-strict path byte-identical (verified by 2 smoke `.flow` scripts running without regression).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Audio/DSP/* HIGH advisory sites (5 sites in 4 files) + Axis_B_AdvisorySiteTests_High Theory</name>
  <files>
    flow-lang/StandardLibrary/Audio/DSP/GranularFunctions.cs,
    flow-lang/StandardLibrary/Audio/DSP/PitchShiftFunctions.cs,
    flow-lang/StandardLibrary/Audio/DSP/StretchEngine.cs,
    flow-lang/StandardLibrary/Audio/DSP/StretchFunctions.cs,
    flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_High.cs
  </files>
  <read_first>
    The 4 DSP files (locate WarnOnce sites — each has 1-2 per RESEARCH §Site Inventory),
    flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs (Plan 44-00 — verify LoadHighPrioritySites is implemented),
    Plan 44-05's Axis_B_ClampSiteTests.cs (sibling pattern — Theory + MemberData source),
    .planning/phases/44-strict-mode/strict-error-manifest.csv (Plan 44-00 — HIGH partition rows for the 8 + 4 files in Tasks 1 + 2)
  </read_first>
  <behavior>
    - Test: each HIGH-priority manifest row passes through a single Theory case — in-strict file calls the builtin with degenerate args, assert `[strict] <existing tag and body>` is captured by ErrorReporter.
    - Test: corresponding non-strict file calls succeed (charitable path preserved).
    - Back-compat: representative DSP test scripts (test_granular.flow if present, or examples/dsp/granular.flow) run unchanged.
  </behavior>
  <action>
    File 1-4 (DSP) — apply Pattern S3 to each of the 5 sites:
    - `GranularFunctions.cs` — `[granular] unknown windowing symbol — falling back to #hann` (1 site).
    - `PitchShiftFunctions.cs` — `[pitchShift] shift > 12 st advisory` (1 site).
    - `StretchEngine.cs` — `[stretch] mode=#auto picks N% vocoder / M% psola across N frames` (2 sites — verify count via grep).
    - `StretchFunctions.cs` — 1 site.

    All 5 sites use the same template: `if (ctx.CallerStrictMode) { er.ReportError($"[strict] {body verbatim}", ctx.CurrentCallSite); return <appropriate>; } existing-WarnOnce-and-fallback`.

    File 5 — `flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_High.cs`:
    Same namespace + traits + ceremony. `[Theory] [MemberData(nameof(HighPrioritySites))]` with `MemberData` sourced from `StrictErrorManifestLoader.LoadHighPrioritySites()`. Two Theory shapes:
    - `Fact_HighPriorityStrictSite_ProducesVerbatimError` — for each row: construct a minimal `enable strict;\n... (<builtin> <degenerate-args>)` source; the degenerate-args strategy depends on the builtin (e.g., `(markov ... order=5 ...)` triggers the order-clamp advisory; `(granular buf ... windowing=#unknown)` triggers the windowing-fallback). Assert ErrorReporter has an error containing `[strict] {row.Tag}` substring AND containing `{row.SentinelBody}` substring (split via dual-substring match — sentinel_body in the manifest is the BODY portion, the tag is the bracketed prefix).
    - `Fact_HighPriorityNonStrictSite_NoError` — same call without `enable strict;`; assert no error; assert the advisory was logged (introspect WarnOnce captures if a test seam exists; if not, just assert exit code 0).

    Plus 1 carve-out smoke:
    - `Fact_HighSiteCount_MatchesManifestPartition` — assert `LoadHighPrioritySites().Count() == <expected>` where expected is the actual count from Task 1 + Task 2 combined (filter manifest to HIGH).

    Recognize that constructing test-degenerate args is non-trivial per builtin; for sites whose surface is hard to trigger from .flow source (e.g., internal SFZ parser errors), the Theory may need to call into a C# test seam directly. RESEARCH §"Validation Architecture" allowed alternative xUnit Fact paths for sites that fire deep inside parsers. Defer those to a separate set of xUnit unit tests within the same file but as `[Fact]` rather than `[Theory]`, if `.flow` source-driven triggers are infeasible.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.Axis_B_AdvisorySiteTests_High" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    5 DSP advisory sites rewritten; ~50+ HIGH-priority sites total across this plan; Axis_B_AdvisorySiteTests_High Theory enumerates the HIGH partition and pins strict-error format; non-strict path preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stdlib leaf-site → ErrorReporter under strict | Mechanical text rewrite; per-site CallerStrictMode read; no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06-01 | Tampering | Accidental carve-out promotion (Pitfall 2) | mitigate | Plan 44-05's Phase44ClampGrepConsistencyTests Fact_CarveOutLiveAdvisoryStillCharitable + Fact_CarveOutImprovStillCharitable already enforce the 5 carve-out sites preserved; Plan 44-06 does not touch those files for any reason. |
| T-44-06-02 | Tampering | Sentinel string drift vs manifest | mitigate | Manifest is single source of truth; Theory MemberData consumes manifest; Theory asserts contains-substring match per row. |
| T-44-06-03 | Tampering | two-run cmp-clean determinism regression | mitigate | Pitfall 5 — no PRNG/clock/Guid in any new error message. Plan 44-11 runs cmp-clean across the showcase to detect regressions. |
</threat_model>

<verification>
- ~50 HIGH-priority advisory sites converted to strict-error branches.
- 5 carve-out sites untouched.
- HIGH partition Theory tests pass.
- Non-strict smoke `.flow` scripts unchanged.
</verification>

<success_criteria>
- All HIGH partition rows in the manifest emit `[strict] ` errors when invoked from strict files.
- Non-strict behavior byte-identical.
- Two-run cmp-clean preserved.
</success_criteria>

<output>
Create `.planning/phases/44-strict-mode/44-06-SUMMARY.md` documenting: exact site counts per file, the early-return shape adopted in each module, and any cases where a .flow source-driven Theory case was infeasible (deferred to xUnit-direct Fact).
</output>
</content>
</invoke>