---
phase: 44-strict-mode
plan: 09
type: execute
wave: 4
depends_on:
  - 44-08
files_modified:
  - flow-lang/StandardLibrary/StdLib.cs
  - flow-lang/StandardLibrary/BuiltInFunctions.cs
  - flow-lang/StandardLibrary/Utils.cs
  - flow-lang.Tests/Integration/Phase44/AxisCBoolRequiredTests.cs
  - flow-lang.Tests/Integration/Phase44/CrossTypeComparisonStrictTests.cs
  - flow-lang.Tests/Integration/Phase44/DictTypeStrictRegressionTests.cs
autonomous: true
requirements:
  - REQ-STRICT-09
  - REQ-STRICT-11
tags:
  - phase-44
  - wave-4
  - axis-c-strict
  - equality-comparison
must_haves:
  truths:
    - "D-12 strict: `(and Int Int)`/`(or Int Int)`/`(not Int)`/`if Int` all error with `[strict] (X) requires Bool — got <Type>`"
    - "D-12 strict: `(and)`/`(or)` require Bool args and return Bool. Plan 44-08 Task 3 shipped last-truthy NON-strict semantics (composer Area 4.2 choice); Plan 44-09 layers the Bool-required + Bool-return strict check ABOVE that short-circuit path so strict-mode behavior remains Value.Bool(...)."
    - "D-11 strict: cross-type `(gt 1 1.0)` / `(lt 1 1.0)` / `(gte 1 1.0)` / `(lte 1 1.0)` error with `[strict] cross-type comparison <T1> vs <T2> — use explicit (double x) / (int x)`"
    - "D-11 strict: `(equals 1 1.0)` returns FALSE (set-theoretic; per RESEARCH State-of-the-Art table + Open Question 1 recommendation)"
    - "D-11 non-strict: `(equals 1 1.0)` retains current Utils.LooseEquals behavior (returns TRUE via numeric coercion)"
    - "D-13 Dict type-strict lookup preserved in BOTH modes (regression-pin per Pitfall 13)"
    - "Plan 44-08's Void-wildcard branches already check CallerStrictMode for print/if/not — Plan 44-09 completes the Axis C strict surface for (and)/(or) + cross-type comparisons + (equals) strict-mode flip"
  artifacts:
    - path: "flow-lang/StandardLibrary/StdLib.cs"
      provides: "StrictAnd / StrictOr + strict-aware GreaterThan/LessThan/Equals branches"
      contains: "StrictAnd"
    - path: "flow-lang/StandardLibrary/BuiltInFunctions.cs"
      provides: "Void-wildcard and/or registrations + strict-aware gt/lt/gte/lte/equals via context-dependent path"
      contains: "registry.Register"
    - path: "flow-lang.Tests/Integration/Phase44/AxisCBoolRequiredTests.cs"
      provides: "Strict (and)/(or)/(not)/if Bool-required Theory"
      contains: "AxisCBoolRequiredTests"
    - path: "flow-lang.Tests/Integration/Phase44/CrossTypeComparisonStrictTests.cs"
      provides: "Strict cross-type gt/lt/gte/lte error + (equals 1 1.0) returns false"
      contains: "CrossTypeComparisonStrictTests"
    - path: "flow-lang.Tests/Integration/Phase44/DictTypeStrictRegressionTests.cs"
      provides: "D-13 Dict lookup type-strict regression-pin in both modes"
      contains: "DictTypeStrictRegressionTests"
  key_links:
    - from: "flow-lang/StandardLibrary/Utils.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "LooseEqualsStrict reads ctx.CallerStrictMode to return false on cross-type instead of coercing"
      pattern: "CallerStrictMode"
---

<objective>
Complete the Axis C strict surface per D-11 + D-12. Strict `(and)`/`(or)`/`(not)`/`if` require Bool args; non-strict charitable behavior already shipped in Plan 44-08. Strict cross-type comparisons `(gt)`/`(lt)`/`(gte)`/`(lte)` error with `[strict] cross-type comparison <T1> vs <T2>`. Strict `(equals 1 1.0)` returns FALSE (set-theoretic per D-11 + RESEARCH Open Question 1 recommendation). Non-strict `(equals)` preserves current Utils.LooseEquals behavior (numeric coercion → true). D-13 Dict lookup stays type-strict in both modes — regression-pin via dedicated xUnit test.

Purpose: Plan 44-08 fixed pre-strict ergonomics by adding Void-wildcard overloads with strict-aware branches inside. Plan 44-09 closes the strict-side semantics for the remaining Axis C operators (and/or + cross-type comparisons + equals). After this plan, strict mode is feature-complete except for the REPL + live + showcase work (Plans 44-10 + 44-11).

Output: 3 new strict-aware helpers in StdLib (StrictAnd / StrictOr — applies via Void-wildcard); strict-aware branches in existing GreaterThan/LessThan/Equals/etc. via context-dependent re-registration; 3 Phase44 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-strict-mode/44-CONTEXT.md
@.planning/phases/44-strict-mode/44-RESEARCH.md
@.planning/phases/44-strict-mode/44-PATTERNS.md
@.planning/phases/44-strict-mode/44-VALIDATION.md
@.planning/phases/44-strict-mode/44-08-PLAN.md

<interfaces>
<!-- Existing analogs -->

From flow-lang/StandardLibrary/StdLib.cs (current logical operations — verify existing And/Or/GreaterThan/LessThan implementations):
- `static Value And(IReadOnlyList<Value> args)` returns Value.Bool(...)
- `static Value Or(IReadOnlyList<Value> args)` same
- `static Value GreaterThan(IReadOnlyList<Value> args)` uses Utils.CompareNumeric
- `static Value Equals(IReadOnlyList<Value> args)` uses Utils.LooseEquals

From flow-lang/StandardLibrary/Utils.cs lines 28-131 (LooseEquals + CompareNumeric + ToComparableNumber):
- LooseEquals at lines 73-76 has numeric coercion making `(equals 1 1.0)` return true.
- Line 82-83 already returns false for cross-type non-numeric.

From flow-lang/StandardLibrary/BuiltInFunctions.cs lines 438-472 (Void-wildcard registrations for equals/lt/gt/lte/gte — already exist; Plan 44-09 needs to convert these to context-dependent so strict branch can fire).

D-11 cross-type comparison error format (RESEARCH §"Code Examples"):
`[strict] cross-type comparison {args[0].Type} vs {args[1].Type} — use explicit (double x) / (int x)`

D-11 equality behavior:
- Strict + cross-type → `false` (set-theoretic, NOT error per D-11 explicit wording).
- Non-strict + cross-type-numeric (e.g., `(equals 1 1.0)`) → CURRENT Utils.LooseEquals behavior (true via numeric coercion). RESEARCH Open Question 1 recommendation Option (b): keep non-strict unchanged.

RESEARCH Open Question 2 (RESOLVED): non-strict `(and)`/`(or)` adopt last-truthy semantics in Plan 44-08 Task 3 (composer Area 4.2 choice). Plan 44-09's strict-aware AndCharitable/OrCharitable variants check `args[0].Type != BoolType.Instance` BEFORE the short-circuit; on strict-mode Bool args, the strict branch returns `Value.Bool(args[0].As<bool>() && args[1].As<bool>())` (and/or) — exact original strict semantics. Non-strict path delegates to the Plan 44-08 last-truthy helpers (AndLastTruthy / OrLastTruthy) for any non-Bool operand.

D-13 Dict lookup: Phase 26.1 hashes by type+value. `(get d 1)` and `(get d 1.0)` are different keys. Phase 44 changes NOTHING — regression-pin via Fact_DictTypeStrictLookup_Preserved.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strict-aware (and)/(or) via context-dependent registration + AxisCBoolRequiredTests</name>
  <files>
    flow-lang/StandardLibrary/StdLib.cs,
    flow-lang/StandardLibrary/BuiltInFunctions.cs,
    flow-lang.Tests/Integration/Phase44/AxisCBoolRequiredTests.cs
  </files>
  <read_first>
    flow-lang/StandardLibrary/StdLib.cs (existing And + Or implementations — locate via grep `static Value And` / `static Value Or`),
    flow-lang/StandardLibrary/BuiltInFunctions.cs (locate where and/or are registered — likely near the equals/lt/gt block at 438-472),
    flow-lang/StandardLibrary/Utils.cs lines 28-131,
    flow-lang.Tests/Integration/Phase44/NotBuiltinTests.cs (Plan 44-08 pattern — strict-aware Void-wildcard),
    .planning/phases/44-strict-mode/44-CONTEXT.md D-12,
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Open Question 2" (preserve current Bool-return)
  </read_first>
  <behavior>
    - Test: `(and true false)` returns Bool(false) in both modes.
    - Test: `(or true false)` returns Bool(true) in both modes.
    - Test: `(and 1 2)` strict → ErrorReporter `[strict] (and) requires Bool — got Int`.
    - Test: `(and 1 2)` non-strict → returns Int `2` (last-truthy semantics per Plan 44-08 Task 3 D-12; both 1 and 2 are truthy, second wins). Note: this is a v1.5 behavior CHANGE vs current Value.Bool return — Plan 44-08 ships the migration; Plan 44-09 ensures the strict path layers above it cleanly.
    - Test: `(or "x" "")` strict → ErrorReporter `[strict] (or) requires Bool — got String`.
    - Test: `(or "x" "")` non-strict → returns String `"x"` (first truthy short-circuits per Plan 44-08 Task 3 D-12 last-truthy semantics).
    - Test: Plan 44-08's strict (not Int 5) / if Int 5 paths continue working (no regression).
  </behavior>
  <action>
    File 1 — `flow-lang/StandardLibrary/StdLib.cs`:
    Add helper `public static Value StrictAndOrCheck(IReadOnlyList<Value> args, string opName, ExecutionContext ctx, ErrorReporter er, Func<Value, Value, Value> nonStrictImpl)`:
    ```
    if (ctx.CallerStrictMode)
    {
        for (int i = 0; i < args.Count; i++)
        {
            if (args[i].Type != BoolType.Instance)
            {
                er.ReportError($"[strict] ({opName}) requires Bool — got {args[i].Type}", ctx.CurrentCallSite);
                return Value.Bool(false);
            }
        }
    }
    return nonStrictImpl(args[0], args[1]);
    ```

    Or simpler: change existing `And` + `Or` to context-dependent variants reading ctx.CallerStrictMode:
    ```
    public static Value AndCharitable(IReadOnlyList<Value> args, ExecutionContext ctx, ErrorReporter er)
    {
        if (ctx.CallerStrictMode)
        {
            if (args[0].Type != BoolType.Instance) { er.ReportError($"[strict] (and) requires Bool — got {args[0].Type}", ctx.CurrentCallSite); return Value.Bool(false); }
            if (args[1].Type != BoolType.Instance) { er.ReportError($"[strict] (and) requires Bool — got {args[1].Type}", ctx.CurrentCallSite); return Value.Bool(false); }
            return Value.Bool(args[0].As<bool>() && args[1].As<bool>());
        }
        // Non-strict: delegate to Plan 44-08 Task 3 last-truthy helper (D-12 composer Area 4.2 choice)
        return StdLib.AndLastTruthy(args, ctx, er);
    }
    ```
    Same shape for `OrCharitable`.

    File 2 — `flow-lang/StandardLibrary/BuiltInFunctions.cs`:
    Convert the existing `and(Bool, Bool)` + `or(Bool, Bool)` registrations to context-dependent paths invoking the new charitable variants. Verify existing tests for short-circuiting and lazy evaluation continue to pass (And historically may use Lazy args — preserve that surface).

    Add the Void-wildcard 2-arg registrations:
    - `and(Void, Void)` → `StdLib.AndCharitable` (Bool-strict path inside; charitable non-strict).
    - `or(Void, Void)` → `StdLib.OrCharitable` (same).

    Verify ordering: existing `and(Bool, Bool)` wins +1000 for Bool args; new Void-wildcard wins for non-Bool. Note that any existing lazy-arg path (`and(Lazy<Bool>, Lazy<Bool>)`) MUST be preserved for short-circuit semantics — Plan 44-09 does NOT touch lazy-arg overloads.

    File 3 — `flow-lang.Tests/Integration/Phase44/AxisCBoolRequiredTests.cs`:
    Same namespace + traits + ceremony. 10 Facts:
    - `Fact_AndBoolBool_BothModes_Works` — `(and true false)` → Bool(false).
    - `Fact_OrBoolBool_BothModes_Works` — `(or true false)` → Bool(true).
    - `Fact_AndInt_NonStrict_CharitableLastTruthyReturnsValue` — `(and 1 2)` non-strict → Int `2` (last-truthy per Plan 44-08 Task 3 D-12; regression-pin against accidental Bool-coercion revert).
    - `Fact_AndInt_Strict_ErrorReported` — strict `(and 1 2)` → `[strict] (and) requires Bool — got Int`.
    - `Fact_OrInt_NonStrict_CharitableLastTruthyReturnsValue` — `(or 0 5)` non-strict → Int `5` (last-truthy: first falsy 0, second truthy 5 wins; D-12).
    - `Fact_OrInt_Strict_ErrorReported` — strict `(or 0 5)` → `[strict] (or) requires Bool — got Int`.
    - `Fact_AndString_Strict_ErrorReported` — strict `(and "x" "y")` → `[strict] (and) requires Bool — got String`.
    - `Fact_NotInt_Strict_ErrorReported` — strict `(not 5)` → `[strict] (not) requires Bool — got Int` (regression-pin from Plan 44-08).
    - `Fact_IfInt_Strict_ErrorReported` — strict `if 5 (...) (...)` → `[strict] (if) requires Bool — got Int` (regression-pin from Plan 44-08).
    - `Fact_AndOrLazyShortCircuit_BothModes_Preserved` — confirm `(and true (lazyExpr-that-would-error))` still short-circuits before evaluating the lazy.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.AxisCBoolRequiredTests|FullyQualifiedName~Phase44.NotBuiltinTests|FullyQualifiedName~Phase44.IfTruthyCoerceTests" --logger "console;verbosity=minimal" &amp;&amp; for t in tests/test_lazy_eval.flow tests/test_comprehensive.flow; do [ -f "$t" ] &amp;&amp; dotnet run --project flow-interpreter "$t" &gt; /dev/null || { echo "REGRESSION: $t"; exit 1; }; done</automated>
  </verify>
  <done>
    AndCharitable + OrCharitable helpers exist; Void-wildcard 2-arg registrations added; existing Bool-Bool + Lazy-Bool overloads preserved; 10 Facts GREEN; lazy short-circuit unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict cross-type gt/lt/gte/lte error + strict (equals 1 1.0) returns false + D-13 Dict regression-pin</name>
  <files>
    flow-lang/StandardLibrary/StdLib.cs,
    flow-lang/StandardLibrary/Utils.cs,
    flow-lang/StandardLibrary/BuiltInFunctions.cs,
    flow-lang.Tests/Integration/Phase44/CrossTypeComparisonStrictTests.cs,
    flow-lang.Tests/Integration/Phase44/DictTypeStrictRegressionTests.cs
  </files>
  <read_first>
    flow-lang/StandardLibrary/Utils.cs lines 1-131 (LooseEquals + CompareNumeric + ToComparableNumber),
    flow-lang/StandardLibrary/StdLib.cs (existing GreaterThan / LessThan / GreaterThanOrEqual / LessThanOrEqual / Equals / StrictEquals),
    flow-lang/StandardLibrary/BuiltInFunctions.cs lines 438-472 (Void-wildcard registrations for gt/lt/gte/lte/equals),
    .planning/phases/44-strict-mode/44-CONTEXT.md D-11 + D-13,
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Code Examples" Comparison strict branch + §"Open Question 1"
  </read_first>
  <behavior>
    - Test: `(gt 1 1.0)` strict → ErrorReporter `[strict] cross-type comparison Int vs Double — use explicit (double x) / (int x)`.
    - Test: `(gt 1 1.0)` non-strict → `false` (1 vs 1.0 via numeric coercion → equal → not greater).
    - Test: `(lt 1 2)` BOTH modes → `true` (same type).
    - Test: `(gte (double 1) 1.0)` BOTH modes → `true` (explicit conversion makes both Double).
    - Test: `(equals 1 1.0)` strict → `false` (D-11 set-theoretic).
    - Test: `(equals 1 1.0)` non-strict → `true` (Utils.LooseEquals numeric coercion — RESEARCH Open Question 1 Option b — non-strict unchanged).
    - Test: `(equals "1" 1)` BOTH modes → `false` (cross-type non-numeric; LooseEquals line 82-83 already false).
    - Test: `(equals #foo "foo")` BOTH modes → `false` (Symbol vs String strict separation per Phase 26.1 SYM-01).
    - Test (D-13): Dict with Int 1 key + Float 1.0 key — both keys distinct in BOTH modes; `(get d 1)` vs `(get d 1.0)` return different values.
  </behavior>
  <action>
    File 1 — `flow-lang/StandardLibrary/Utils.cs`:
    Add helper `public static bool LooseEqualsStrict(Value a, Value b, ExecutionContext ctx)`:
    ```
    if (ctx.CallerStrictMode && !a.Type.Equals(b.Type))
    {
        return false;  // D-11 set-theoretic: cross-type strict equality returns false (NOT error)
    }
    return LooseEquals(a, b);
    ```
    Per RESEARCH Open Question 1 Option (b): non-strict path unchanged; strict short-circuits to false on cross-type.

    File 2 — `flow-lang/StandardLibrary/StdLib.cs`:
    Add context-dependent variants:
    ```
    public static Value EqualsCharitable(IReadOnlyList<Value> args, ExecutionContext ctx, ErrorReporter er)
        => Value.Bool(Utils.LooseEqualsStrict(args[0], args[1], ctx));

    public static Value GreaterThanCharitable(IReadOnlyList<Value> args, ExecutionContext ctx, ErrorReporter er)
    {
        if (ctx.CallerStrictMode && !args[0].Type.Equals(args[1].Type))
        {
            er.ReportError(
                $"[strict] cross-type comparison {args[0].Type} vs {args[1].Type} — use explicit (double x) / (int x)",
                ctx.CurrentCallSite);
            return Value.Void();
        }
        return Value.Bool(Utils.CompareNumeric(args[0], args[1]) > 0);
    }
    ```
    Same shape for LessThanCharitable / GreaterThanOrEqualCharitable / LessThanOrEqualCharitable.

    File 3 — `flow-lang/StandardLibrary/BuiltInFunctions.cs`:
    Convert the existing Void-wildcard registrations at lines 438-472 for equals/lt/gt/gte/lte to context-dependent paths invoking the new Charitable variants. Preserve registrations for typed overloads (Int-Int, Double-Double) at +1000 — those skip the wildcard.

    Sentinel for the cross-type comparison error is LOAD-BEARING. Add a row to `strict-error-manifest.csv` IF Plan 44-00 didn't already include the Axis C entries (Plan 44-00's manifest may need 6 additional rows: `(print)` + `(if)` + `(and)` + `(or)` + `(not)` + cross-type comparison; verify at task start; if missing, extend the manifest in this plan and bump Plan 44-00's count downstream).

    File 4 — `flow-lang.Tests/Integration/Phase44/CrossTypeComparisonStrictTests.cs`:
    Same namespace + traits + ceremony. 10 Facts:
    - `Fact_GtIntDouble_Strict_ErrorReported` — `[strict] cross-type comparison Int vs Double — use explicit ...`.
    - `Fact_GtIntDouble_NonStrict_ReturnsFalseViaCoercion` — non-strict `(gt 1 1.0)` → Bool(false).
    - `Fact_LtIntDouble_Strict_ErrorReported`.
    - `Fact_GteIntDouble_Strict_ErrorReported`.
    - `Fact_LteIntDouble_Strict_ErrorReported`.
    - `Fact_GtSameType_BothModes_Works` — `(gt 2 1)` → true.
    - `Fact_GtExplicitConversion_StrictWorks` — `(gt (double 1) 1.0)` strict → false (both Double, no cross-type).
    - `Fact_EqualsIntDouble_Strict_ReturnsFalse` — strict `(equals 1 1.0)` → Bool(false).
    - `Fact_EqualsIntDouble_NonStrict_ReturnsTrue` — non-strict `(equals 1 1.0)` → Bool(true) per current LooseEquals.
    - `Fact_EqualsSymbolString_BothModes_ReturnsFalse` — `(equals #foo "foo")` → false (Phase 26.1 SYM-01 regression-pin).

    File 5 — `flow-lang.Tests/Integration/Phase44/DictTypeStrictRegressionTests.cs`:
    Same namespace + traits + ceremony. 4 Facts (D-13 regression-pin per Pitfall 13):
    - `Fact_DictIntFloatKeys_DistinctBothModes` — `Dict<Number, String> d = (dict 1 "one" 1.0 "one-point-oh")`; `(get d 1)` → "one"; `(get d 1.0)` → "one-point-oh". Both in non-strict and strict modes (D-13 unchanged).
    - `Fact_DictHasInt_DoesNotMatchFloat_BothModes` — `(has d 1)` true; `(has d 1.0)` true; both as distinct keys.
    - `Fact_DictSymbolStringKeys_DistinctBothModes` — `(dict #foo "sym" "foo" "str")` — both keys distinct per Phase 26.1.
    - `Fact_DictSize_AfterMixedTypeKeys` — verify dict size grows as expected when inserting same-value-different-type keys.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.CrossTypeComparisonStrictTests|FullyQualifiedName~Phase44.DictTypeStrictRegressionTests" --logger "console;verbosity=minimal" &amp;&amp; for t in tests/test_dict_keys.flow tests/test_dict_ops.flow; do [ -f "$t" ] &amp;&amp; dotnet run --project flow-interpreter "$t" &gt; /dev/null || { echo "REGRESSION: $t"; exit 1; }; done</automated>
  </verify>
  <done>
    LooseEqualsStrict + 4 comparison Charitable variants + EqualsCharitable; existing Void-wildcard registrations switched to context-dependent paths; 14 Facts GREEN; Dict + dict_keys/dict_ops `.flow` tests unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer comparison/equality/logical call → strict-aware branch | Mode-dependent return; deterministic; no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09-01 | Tampering | Non-strict equality regression for (equals 1 1.0) | mitigate | RESEARCH Open Question 1 Option (b) — non-strict path UNCHANGED. Fact_EqualsIntDouble_NonStrict_ReturnsTrue pins current LooseEquals behavior. |
| T-44-09-02 | Tampering | (and)/(or) last-truthy migration regression | mitigate | Plan 44-08 Task 3 SHIPS last-truthy as a v1.5 breaking change per D-12 composer Area 4.2 choice. Plan 44-09's strict-aware variants delegate to Plan 44-08's AndLastTruthy / OrLastTruthy on the non-strict path; strict path returns Value.Bool(...) verbatim. Fact_AndOrLazyShortCircuit_BothModes_Preserved confirms Lazy short-circuit. Pre-traction latitude D-v1.5-01 covers the single-commit ship. |
| T-44-09-03 | Tampering | Dict type-strict regression | mitigate | DictTypeStrictRegressionTests' 4 Facts pin D-13. |
</threat_model>

<verification>
- StdLib.AndCharitable + OrCharitable + EqualsCharitable + 4 comparison Charitable variants exist.
- Utils.LooseEqualsStrict short-circuits on cross-type under strict.
- Void-wildcard registrations for and/or/equals/lt/gt/gte/lte all converted to context-dependent.
- 10 + 10 + 4 = 24 Facts GREEN.
- Dict + dict_keys `.flow` tests unchanged.
</verification>

<success_criteria>
- Strict Axis C complete: Bool-required for and/or/not/if; cross-type comparison errors; equals returns false on cross-type strict.
- Non-strict charitable behavior preserved across all Axis C operators.
- D-13 Dict regression-pin in place.
- No regressions.
</success_criteria>

<output>
Create `.planning/phases/44-strict-mode/44-09-SUMMARY.md` documenting: which existing registrations were converted to context-dependent, the LooseEqualsStrict short-circuit decision (Option b per Open Question 1), and the D-13 regression-pin extent (which dict ops were tested).
</output>
</content>
</invoke>