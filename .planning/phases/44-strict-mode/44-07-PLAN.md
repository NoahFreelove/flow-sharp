---
phase: 44-strict-mode
plan: 07
type: execute
wave: 3
depends_on:
  - 44-02
  - 44-06
files_modified:
  - flow-lang/StandardLibrary/Generative/MarkovFunctions.cs
  - flow-lang/StandardLibrary/Generative/LsystemFunctions.cs
  - flow-lang/StandardLibrary/Generative/CellularFunctions.cs
  - flow-lang/StandardLibrary/Generative/ChaosFunctions.cs
  - flow-lang/StandardLibrary/Improv/JamFunctions.cs
  - flow-lang/StandardLibrary/Notation/AbcImport.cs
  - flow-lang/StandardLibrary/Notation/AbcLexer.cs
  - flow-lang/StandardLibrary/Notation/MmlImport.cs
  - flow-lang/StandardLibrary/Network/OscFunctions.cs
  - flow-lang/StandardLibrary/Audio/Tuning/ScalaBuiltins.cs
  - flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs
  - flow-lang/StandardLibrary/Audio/InputFunctions.cs
  - flow-lang/StandardLibrary/Audio/MidiExport.cs
  - flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_MedLow.cs
  - flow-lang.Tests/Integration/Phase44/CarveOutsPreservedTests.cs
autonomous: true
requirements:
  - REQ-STRICT-08
tags:
  - phase-44
  - wave-3
  - axis-b
  - advisory-medlow
must_haves:
  truths:
    - "D-06 + D-07 + AUDIT §6b — all MED + LOW priority advisory sites convert to `[strict] ` errors under CallerStrictMode"
    - "Modules covered: Generative (24 sites) + Improv/JamFunctions (16; StyleRegistry's 4 SKIPPED) + Notation (15) + Network/OscFunctions (3) + Audio/Tuning (2) + Harmony (1) + Audio/InputFunctions (3) + Audio/MidiExport (1) ≈ 65 sites"
    - "5 carve-out sites STAY untouched: Interpreter.cs:476 + StyleRegistry.cs:156/244/258/265"
    - "RESEARCH D-36-09 chaos cross-platform divergence caveat preserved — strict error path is deterministic same-platform"
    - "Total Axis B coverage across Plans 44-05 + 44-06 + 44-07 ≈ 128 sites — matches manifest ~126 in-scope"
  artifacts:
    - path: "flow-lang/StandardLibrary/Improv/JamFunctions.cs"
      provides: "Strict-mode branches at 16 [jam] advisory sites"
      contains: "CallerStrictMode"
    - path: "flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_MedLow.cs"
      provides: "xUnit Theory pinning MED+LOW priority sentinels via StrictErrorManifestLoader.LoadMedLowPrioritySites"
      contains: "Axis_B_AdvisorySiteTests_MedLow"
    - path: "flow-lang.Tests/Integration/Phase44/CarveOutsPreservedTests.cs"
      provides: "Explicit verification 5 carve-out sites STILL emit WarnOnce in both modes (anti-Pitfall-2)"
      contains: "CarveOutsPreservedTests"
  key_links:
    - from: "flow-lang/StandardLibrary/Improv/JamFunctions.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "ctx.CallerStrictMode at each of 16 in-scope sites"
      pattern: "CallerStrictMode"
    - from: "flow-lang.Tests/Integration/Phase44/CarveOutsPreservedTests.cs"
      to: "flow-lang/Interpreter/Interpreter.cs"
      via: "File.ReadAllText + assert no `[strict]` or `CallerStrictMode` near line 476"
      pattern: "\\[live\\]"
---

<objective>
Convert ~65 MEDIUM + LOW priority `WarnOnce` advisory sites (AUDIT §6b MED + LOW per §7b) to `[strict] ` errors under `ctx.CallerStrictMode`. Modules: Generative (24) + JamFunctions (16) + Notation (15) + OscFunctions (3) + Audio/Tuning (2) + Harmony (1) + InputFunctions (3) + MidiExport (1). Plan 44-07 also lands `CarveOutsPreservedTests.cs` to pin the 5 carve-out sites stay charitable.

Purpose: close out Axis B coverage. Plans 44-05 + 44-06 + 44-07 together hit all in-scope manifest rows.

Output: ~65 mechanical rewrites in 13 files, one MED+LOW Theory test, one explicit carve-out preservation test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-strict-mode/44-CONTEXT.md
@.planning/phases/44-strict-mode/44-RESEARCH.md
@.planning/phases/44-strict-mode/44-PATTERNS.md
@.planning/phases/44-strict-mode/44-VALIDATION.md
@.planning/phases/44-strict-mode/44-06-PLAN.md
@.planning/phases/44-strict-mode/44-00-PLAN.md
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT.md

<interfaces>
<!-- Pattern S3 rewrite — verbatim from Plan 44-06 -->

Module → site count from AUDIT §6b:
| Module | Sites | Priority |
|--------|-------|----------|
| Generative/MarkovFunctions.cs | 6 | MED |
| Generative/LsystemFunctions.cs | 6 | MED |
| Generative/CellularFunctions.cs | 3 | MED |
| Generative/ChaosFunctions.cs | 9 | MED (D-36-09 caveat) |
| Improv/JamFunctions.cs | 16 | MED |
| Notation/AbcImport.cs | 8 | MED |
| Notation/AbcLexer.cs | 2 | MED |
| Notation/MmlImport.cs | 5 | MED |
| Network/OscFunctions.cs | 3 | MED |
| Audio/Tuning/ScalaBuiltins.cs | 2 | MED |
| Harmony/HarmonyFunctions.cs | 1 | MED |
| Audio/InputFunctions.cs | 3 | LOW |
| Audio/MidiExport.cs | 1 | LOW |

Carve-outs (DO NOT modify — Pitfall 2):
- `flow-lang/Interpreter/Interpreter.cs:476` — [live] (touched in Plan 44-06's scope? NO — Interpreter.cs only touched for the match-non-exhaustive advisory at line 569; line 476 is the live-entry advisory which stays charitable)
- `flow-lang/StandardLibrary/Improv/StyleRegistry.cs:156, 244, 258, 265`

Per RESEARCH D-36-09 + AUDIT §8 Limitation 6: chaos primitives' OUTPUT is same-platform-deterministic only; the strict error path short-circuits before chaotic computation — strict errors stay deterministic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Generative + Improv MED-priority WarnOnce sites (40 sites across 5 files)</name>
  <files>
    flow-lang/StandardLibrary/Generative/MarkovFunctions.cs,
    flow-lang/StandardLibrary/Generative/LsystemFunctions.cs,
    flow-lang/StandardLibrary/Generative/CellularFunctions.cs,
    flow-lang/StandardLibrary/Generative/ChaosFunctions.cs,
    flow-lang/StandardLibrary/Improv/JamFunctions.cs
  </files>
  <read_first>
    All 5 files (focus on WarnOnce sites — MarkovFunctions canonical),
    .planning/phases/44-strict-mode/strict-error-manifest.csv (filter MED rows for these 5 files),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Pattern S3",
    .planning/phases/44-strict-mode/44-PATTERNS.md §"flow-lang/StandardLibrary/Generative/MarkovFunctions.cs:247-258"
  </read_first>
  <action>
    Apply Pattern S3 to each WarnOnce site per the manifest MED-priority entries:
    - MarkovFunctions.cs — 6 sites. Already context-bound via Phase 36 RegisterContextDependent.
    - LsystemFunctions.cs — 6 sites. Same.
    - CellularFunctions.cs — 3 sites. Same.
    - ChaosFunctions.cs — 9 sites. Phase 36 context-bound. Per D-36-09: chaos OUTPUT diverges cross-platform; strict ERROR PATH short-circuits before chaotic compute → strict errors are deterministic same-platform.
    - JamFunctions.cs — 16 sites. Phase 36 IMPROV-01 context-bound. **CRITICAL Pitfall 2:** do NOT touch `flow-lang/StandardLibrary/Improv/StyleRegistry.cs` — its 4 sites are carve-outs. Plan 44-07 only touches JamFunctions in the Improv directory.

    Per-site shape (verbatim Pattern S3):
    ```
    if (ctx.CallerStrictMode)
    {
        er.ReportError($"[strict] {existing-sentinel-body}", ctx.CurrentCallSite);
        return Value.Void();   // or type-appropriate sentinel
    }
    RenderingDiagnostics.WarnOnce($"<existing-key>", $"<existing-body>");
    <existing-fallback>
    ```

    Preserve sentinel body verbatim per D-07; only prepend `[strict] `. Pitfall 5: no PRNG/clock/Guid.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; for t in tests/test_markov_smoke.flow tests/test_lsystem_smoke.flow tests/test_jam_smoke.flow; do [ -f "$t" ] &amp;&amp; dotnet run --project flow-interpreter "$t" &gt; /dev/null || true; done &amp;&amp; dotnet run --project flow-interpreter tests/test_comprehensive.flow &gt; /dev/null</automated>
  </verify>
  <done>
    40 strict-branch sites in 5 files; non-strict path preserved byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert Notation + Network + Tuning + Harmony + InputFunctions + MidiExport sites (25 sites across 8 files)</name>
  <files>
    flow-lang/StandardLibrary/Notation/AbcImport.cs,
    flow-lang/StandardLibrary/Notation/AbcLexer.cs,
    flow-lang/StandardLibrary/Notation/MmlImport.cs,
    flow-lang/StandardLibrary/Network/OscFunctions.cs,
    flow-lang/StandardLibrary/Audio/Tuning/ScalaBuiltins.cs,
    flow-lang/StandardLibrary/Harmony/HarmonyFunctions.cs,
    flow-lang/StandardLibrary/Audio/InputFunctions.cs,
    flow-lang/StandardLibrary/Audio/MidiExport.cs
  </files>
  <read_first>
    All 8 files (focus on WarnOnce sites),
    .planning/phases/44-strict-mode/strict-error-manifest.csv (filter MED+LOW for these 8),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Pattern S3"
  </read_first>
  <action>
    Apply Pattern S3 per site:
    - AbcImport.cs — 8 sites (Phase 39 D-39-17 charitable defaults flip).
    - AbcLexer.cs — 2 sites.
    - MmlImport.cs — 5 sites (Phase 39 D-39-18/19 — depth cap + opcode advisories).
    - OscFunctions.cs — 3 sites (Phase 38 OSC — bundle nesting + type-tag fallback + handler exception).
    - ScalaBuiltins.cs — 2 sites (Phase 32 — unmapped MIDI keys + malformed .scl line).
    - HarmonyFunctions.cs — 1 site (`[enharmonic] tuning ≠ ET cent shift advisory`).
    - InputFunctions.cs — 3 sites (Phase 38 AUDIO-IN — attenuation + resample + capture-failed).
    - MidiExport.cs — 1 site (`[midi] velocity floor applied`).

    Where the surrounding method doesn't yet have ctx + er parameters: restructure to context-dependent registration mirroring Phase 36 MarkovFunctions.RegisterContextDependent. For lexer/parser-deep sites that don't have natural context plumbing (e.g., AbcLexer running stateless), thread context through the parser constructor or pass via the entry point on AbcImport.

    Per Pitfall 2: carve-outs untouched. None of the 8 files contain carve-out sites — Plan 44-07 cannot accidentally touch them.

    Per Pitfall 5: every error string is deterministic concat.
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; for t in tests/test_notation*.flow tests/test_osc*.flow tests/test_tuning*.flow; do [ -f "$t" ] &amp;&amp; dotnet run --project flow-interpreter "$t" &gt; /dev/null || true; done &amp;&amp; dotnet run --project flow-interpreter tests/test_comprehensive.flow &gt; /dev/null</automated>
  </verify>
  <done>
    25 strict-branch sites in 8 files; non-strict path preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Axis_B_AdvisorySiteTests_MedLow Theory + CarveOutsPreservedTests anti-Pitfall-2 guard</name>
  <files>
    flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_MedLow.cs,
    flow-lang.Tests/Integration/Phase44/CarveOutsPreservedTests.cs
  </files>
  <read_first>
    flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs (Plan 44-00 — LoadMedLowPrioritySites + LoadCarveOutSites),
    flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_High.cs (Plan 44-06 sibling — Theory pattern template),
    flow-lang/Interpreter/Interpreter.cs lines 470-480 (verify line 476 still has WarnOnce — carve-out),
    flow-lang/StandardLibrary/Improv/StyleRegistry.cs lines 150-270 (verify lines 156/244/258/265 still have WarnOnce — carve-outs),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Pitfall 2"
  </read_first>
  <behavior>
    - Test: each MED+LOW manifest row passes through a Theory case — strict file calls builtin with degenerate args, ErrorReporter captures `[strict] <tag> <body>`.
    - Test: corresponding non-strict file calls succeed (WarnOnce path preserved).
    - Test: 5 carve-out files STILL contain WarnOnce(...) and DO NOT contain `CallerStrictMode` or `[strict]` near the carve-out line numbers.
    - Test: total in-scope manifest row count matches the sum of Plans 44-05 + 44-06 + 44-07 strict-branch insertions.
  </behavior>
  <action>
    File 1 — `flow-lang.Tests/Integration/Phase44/Axis_B_AdvisorySiteTests_MedLow.cs`:
    Same namespace + traits + ceremony. `[Theory] [MemberData(nameof(MedLowSites))]` sourced from `StrictErrorManifestLoader.LoadMedLowPrioritySites()`. Two Theory shapes mirror Plan 44-06's tests:
    - `Fact_MedLowStrictSite_ProducesVerbatimError(...)` — substring-match `[strict] {tag}` AND `{sentinelBody}` in captured error.
    - `Fact_MedLowNonStrictSite_NoError(...)` — assert no error, advisory logged.

    Plus:
    - `Fact_MedLowCount_MatchesManifest` — `LoadMedLowPrioritySites().Count() >= 60 && <= 70` (tolerance band for the ~65 estimate).
    - `Fact_TotalInScopeCount_MatchesSumOfPlans` — sum HIGH + MED + LOW partitions, assert equals `LoadInScopeSites().Count()` (sanity check the partition union covers all in-scope rows).

    File 2 — `flow-lang.Tests/Integration/Phase44/CarveOutsPreservedTests.cs`:
    Same namespace + traits + ceremony. 5 Facts (one per carve-out row from `LoadCarveOutSites()`):
    - `Fact_LiveAdvisoryAtInterpreter476_StillCharitable` — `File.ReadAllText("flow-lang/Interpreter/Interpreter.cs")` contains `[live] entering live block` AND the surrounding ±10 lines contain `WarnOnce(` AND DO NOT contain `CallerStrictMode` or `[strict]`.
    - `Fact_ImprovAdvisoryAtStyleRegistry156_StillCharitable` — same shape for line 156 of StyleRegistry.cs (verify WarnOnce + `[improv]` + no `[strict]`).
    - `Fact_ImprovAdvisoryAtStyleRegistry244_StillCharitable` — line 244.
    - `Fact_ImprovAdvisoryAtStyleRegistry258_StillCharitable` — line 258.
    - `Fact_ImprovAdvisoryAtStyleRegistry265_StillCharitable` — line 265.

    Implementation hint: read the full file once, locate the line by 1-indexed line number, take a ±10-line window via `.Skip(line-10).Take(20)`, and apply assertions. This is robust against minor line-number shifts (drift tolerance per Pitfall 2; per AUDIT D-42-01 stable-identifier convention of builtin-name-not-file-line). If line numbers shift significantly during Phase 44 work, update the manifest CSV in a follow-up commit.

    Plus 1 runtime smoke:
    - `Fact_LiveBlockInStrictFile_StillEmitsLiveAdvisory` — execute `"enable strict;\nlive 1bar { (print 1) }"`, assert RenderingDiagnostics captured a `[live] entering live block` advisory + did NOT report any `[strict]` error for the live-entry itself. (Plan 44-09 will land the full live + strict interaction tests; this is a smoke check.)
  </action>
  <verify>
    <automated>dotnet build flow-lang/flow-lang.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.Axis_B_AdvisorySiteTests_MedLow|FullyQualifiedName~Phase44.CarveOutsPreservedTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    Axis_B_AdvisorySiteTests_MedLow runs Theory over ~65 MED+LOW rows; CarveOutsPreservedTests pins 5 carve-out sites untouched; partition-union sanity check verifies HIGH+MED+LOW covers all in-scope manifest rows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stdlib leaf-site → ErrorReporter under strict | Mechanical text rewrite at ~65 leaf sites; no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07-01 | Tampering | Accidental carve-out promotion | mitigate | CarveOutsPreservedTests' 5 Facts pin Interpreter.cs:476 + StyleRegistry.cs:156/244/258/265 charitable. |
| T-44-07-02 | Tampering | Sentinel string drift vs manifest | mitigate | Theory MemberData consumes manifest CSV; substring-match per row. |
| T-44-07-03 | Reliability | Chaos primitive cross-platform divergence | accept | D-36-09 + AUDIT §8 Limitation 6 — strict ERROR PATH is same-platform deterministic; chaotic OUTPUT divergence is unchanged scope. |
| T-44-07-04 | Tampering | two-run cmp-clean determinism regression | mitigate | Pitfall 5 — no PRNG/clock/Guid; Plan 44-11 runs cmp-clean end-to-end. |
</threat_model>

<verification>
- ~65 MED + LOW priority sites rewritten across 13 production files.
- Carve-out preservation pinned by 5 Facts.
- MED+LOW Theory tests pass.
- Partition union covers all in-scope manifest rows.
- Non-strict scripts unchanged.
</verification>

<success_criteria>
- All MED + LOW partition rows emit `[strict] ` errors under strict.
- 5 carve-out sites STILL emit WarnOnce.
- Axis B coverage closed (Plans 44-05 + 44-06 + 44-07).
- No regressions.
</success_criteria>

<output>
Create `.planning/phases/44-strict-mode/44-07-SUMMARY.md` documenting: total sites per module, any context-plumbing restructures needed (especially for Notation/AbcLexer-style stateless code), and the final HIGH+MED+LOW partition union total compared to the manifest in-scope count.
</output>
</content>
</invoke>