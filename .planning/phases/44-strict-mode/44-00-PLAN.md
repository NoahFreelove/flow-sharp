---
phase: 44-strict-mode
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - flow-lang.Tests/Integration/Phase44/Phase44TestCategory.cs
  - flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs
  - .planning/phases/44-strict-mode/strict-error-manifest.csv
  - .planning/phases/44-strict-mode/strict-error-manifest-README.md
  - scripts/audit/strict-site-grep.sh
autonomous: true
requirements:
  - REQ-STRICT-08
tags:
  - phase-44
  - wave-0
  - test-infrastructure
  - manifest
must_haves:
  truths:
    - "A CSV manifest at .planning/phases/44-strict-mode/strict-error-manifest.csv enumerates every in-scope strict-mode site (~126 rows) with file:line + tag + sentinel-body + priority + carve-out flag"
    - "The 5 carve-out sites (Interpreter.cs:476 [live] + StyleRegistry.cs:156/244/258/265 [improv]) are present in the manifest with carve_out=true and STAY charitable in both modes"
    - "Phase44TestCategory.cs defines the xUnit Category=Phase44 trait used by `dotnet test --filter Category=Phase44`"
    - "The grep extractor script `scripts/audit/strict-site-grep.sh` re-emits the raw site lists deterministically"
    - "Reconciles the AUDIT §6b ~117 count vs RESEARCH grep ~120 count via a single authoritative manifest"
  artifacts:
    - path: ".planning/phases/44-strict-mode/strict-error-manifest.csv"
      provides: "Authoritative ~126-row site inventory feeding xUnit [Theory] data sources in Plans 44-05..44-08"
      contains: "file_path,line,builtin,tag,sentinel_body,priority,carve_out,axis"
    - path: "flow-lang.Tests/Integration/Phase44/Phase44TestCategory.cs"
      provides: "xUnit Category=Phase44 trait constant"
      contains: "public const string Phase44"
    - path: "flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs"
      provides: "Helper loading the manifest CSV for xUnit Theory data sources"
      contains: "public static IEnumerable<object[]> LoadInScopeSites"
    - path: "scripts/audit/strict-site-grep.sh"
      provides: "Re-runnable extractor for WarnOnce + input-perimeter clamp sites"
      contains: "WarnOnce"
  key_links:
    - from: "flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs"
      to: ".planning/phases/44-strict-mode/strict-error-manifest.csv"
      via: "File.ReadAllLines at xUnit Theory load time"
      pattern: "strict-error-manifest\\.csv"
    - from: "scripts/audit/strict-site-grep.sh"
      to: "flow-lang/StandardLibrary/"
      via: "grep -rn WarnOnce"
      pattern: "WarnOnce"
---

<objective>
Build the Wave 0 test infrastructure for Phase 44 BEFORE any production code touches strict mode. Reconcile the AUDIT §6b 117-site count against the RESEARCH grep ~120-site count via a single authoritative CSV manifest. The manifest drives every xUnit `[Theory]` in Plans 44-05 / 44-06 / 44-07 / 44-08 (Axis B HIGH + MED/LOW). Also lay down the `Integration/Phase44/` directory, the Category=Phase44 trait, and the grep extractor that re-emits the raw lists deterministically.

Purpose: prevent the "Plan 44-N starts writing a Theory and discovers a new in-scope site that needs a new RESEARCH-counted error string" failure mode. Wave 0 owns the count, the carve-out flagging, and the verbatim-sentinel pinning. Plans 44-05..44-08 consume the CSV verbatim.

Output: One CSV (~126 rows), one xUnit Category trait file, one CSV loader helper class, one Bash re-extractor script (re-runnable for regression), and a small README documenting the schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-strict-mode/44-CONTEXT.md
@.planning/phases/44-strict-mode/44-RESEARCH.md
@.planning/phases/44-strict-mode/44-PATTERNS.md
@.planning/phases/44-strict-mode/44-VALIDATION.md
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT.md
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/input-clamps.txt
@.planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/advisory-sites.txt

<interfaces>
<!-- Existing grep extractor precedent — Phase 42 -->

From scripts/audit/clamp-grep.sh (Phase 42 precedent for the extractor shape):
the existing script runs `grep -rn 'Math\.Clamp'` over `flow-lang/StandardLibrary/` and pipes to `42-AUDIT-data/all-clamps.txt`. Phase 44's `strict-site-grep.sh` mirrors this — `grep -rn 'WarnOnce' flow-lang/StandardLibrary/ flow-lang/Interpreter/ flow-lang/Ast/` plus a second pass for the §6a input-perimeter clamps from `TransformFunctions.cs` line numbers (the 13 sites pinned in RESEARCH §"Site Inventory" Table §6a).

From flow-lang.Tests/Integration/Phase42/ClampGrepConsistencyTests.cs:259-313 (helper functions reused):
- `IsBashAvailable()` — environment probe
- `FindRepoRoot()` — walks up to find `.git`
- `RunBashScript(repoRoot, scriptRelativePath)` — Process.Start `/bin/bash`

CSV schema (10 columns):
| Column | Example |
|--------|---------|
| file_path | flow-lang/StandardLibrary/Transforms/TransformFunctions.cs |
| line | 649 |
| builtin | crescendo |
| tag | crescendo |
| sentinel_body | crescendo startVel {value} outside [0.0, 1.0] |
| priority | HIGH |
| carve_out | false |
| axis | B |
| param | startVel |
| range | [0.0, 1.0] |

Carve-out rows (5 total — STAY charitable, exclude from xUnit Theory sources):
- flow-lang/Interpreter/Interpreter.cs:476 — [live] entering live block (D-v1.5-07 design-lock)
- flow-lang/StandardLibrary/Improv/StyleRegistry.cs:156 — [improv] style pack failed to load
- flow-lang/StandardLibrary/Improv/StyleRegistry.cs:244 — [improv] user style overrides shipped pack
- flow-lang/StandardLibrary/Improv/StyleRegistry.cs:258 — [improv] (style-pack discovery)
- flow-lang/StandardLibrary/Improv/StyleRegistry.cs:265 — [improv] (style-pack discovery)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase44 test directory + Category trait + Bash re-extractor + CSV schema doc</name>
  <files>
    flow-lang.Tests/Integration/Phase44/Phase44TestCategory.cs,
    scripts/audit/strict-site-grep.sh,
    .planning/phases/44-strict-mode/strict-error-manifest-README.md
  </files>
  <read_first>
    scripts/audit/clamp-grep.sh (Phase 42 extractor template),
    flow-lang.Tests/Integration/Phase42/ClampGrepConsistencyTests.cs lines 259-313 (helper functions),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Site Inventory" (counts + carve-outs),
    .planning/phases/44-strict-mode/44-PATTERNS.md §"flow-lang.Tests/Phase44/ directory" (layout precedent),
    .planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/advisory-sites.txt (the 117 raw entries),
    .planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/input-clamps.txt (the 13 raw entries)
  </read_first>
  <action>
    Create the directory `flow-lang.Tests/Integration/Phase44/` (mirrors Phase 42/43 layout per Phase 43 convention; do NOT use top-level `flow-lang.Tests/Phase44/` despite Phase 35/36 precedent — Phase 43 has set the integration-layout convention for AUDIT-fed phases).

    File 1: `flow-lang.Tests/Integration/Phase44/Phase44TestCategory.cs`
    - Namespace `FlowLang.Tests.Integration.Phase44`.
    - A `public static class Phase44TestCategory { public const string Phase44 = "Phase44"; }` — used as the value for xUnit `[Trait("Category", Phase44TestCategory.Phase44)]` on every Phase 44 test class.
    - XML doc citing REQ-STRICT-01..15 + the `dotnet test --filter Category=Phase44` quick-run command from `44-VALIDATION.md`.

    File 2: `scripts/audit/strict-site-grep.sh`
    - Bash shebang + `set -euo pipefail`.
    - Output target: `.planning/phases/44-strict-mode/strict-site-raw.txt` (regen'd at every run, not committed).
    - Two passes:
      Pass A — `grep -rn 'WarnOnce(' flow-lang/StandardLibrary/ flow-lang/Interpreter/ flow-lang/Ast/ | grep -v '^Binary'` → captures every WarnOnce call site verbatim.
      Pass B — pin the 13 §6a TransformFunctions.cs lines (per RESEARCH Site Inventory): emit `flow-lang/StandardLibrary/Transforms/TransformFunctions.cs:106` etc. for the 13 sites by exact line number.
    - Append a single-line summary footer: `## Total: <count> WarnOnce sites + 13 input-perimeter clamps`.
    - Use the same `IsBashAvailable` skip semantics as Phase 42 — the Bash script is invoked by xUnit `[Fact]` in Plan 44-05; if `/bin/bash` is missing, the Fact charitable-skips.
    - chmod 755 (set executable).

    File 3: `.planning/phases/44-strict-mode/strict-error-manifest-README.md`
    - Schema documentation for the CSV (10 columns enumerated above in `<interfaces>`).
    - Carve-out policy: 5 rows have `carve_out=true` and MUST NOT be promoted to strict errors per D-06 + RESEARCH Pitfall 2.
    - Regeneration policy: re-run `scripts/audit/strict-site-grep.sh` to refresh the raw counts; the curated manifest CSV is committed and updated by hand only.
    - Cite the relevant decisions (D-06 carve-outs, D-07 verbatim error sentinels, D-14 xUnit Theory consumption).
  </action>
  <verify>
    <automated>test -f flow-lang.Tests/Integration/Phase44/Phase44TestCategory.cs &amp;&amp; test -x scripts/audit/strict-site-grep.sh &amp;&amp; test -f .planning/phases/44-strict-mode/strict-error-manifest-README.md &amp;&amp; dotnet build flow-lang.Tests/flow-lang.Tests.csproj /clp:ErrorsOnly</automated>
  </verify>
  <done>
    Phase44 test directory exists; Category trait constant defined; Bash extractor exists + executable; README documents the manifest schema and carve-out policy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author strict-error-manifest.csv (~126 rows) + StrictErrorManifestLoader helper + Wave 0 sanity Facts</name>
  <files>
    .planning/phases/44-strict-mode/strict-error-manifest.csv,
    flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs,
    flow-lang.Tests/Integration/Phase44/StrictErrorManifestSanityTests.cs
  </files>
  <read_first>
    .planning/phases/44-strict-mode/strict-error-manifest-README.md (the schema authored in Task 1),
    .planning/phases/42-type-system-stdlib-audit/42-AUDIT.md §6a Column 5 (13 verbatim error strings — LOAD-BEARING per D-07 + CONTEXT specifics),
    .planning/phases/42-type-system-stdlib-audit/42-AUDIT.md §6b (117 sites grouped by module + representative sentinels),
    .planning/phases/42-type-system-stdlib-audit/42-AUDIT-data/advisory-sites.txt (raw file:line refs),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Site Inventory" Table §6a (13 site verbatim error strings with `[strict]` prefix applied),
    flow-lang.Tests/Integration/Phase42/ClampGrepConsistencyTests.cs (test helpers + IDisposable + RenderingDiagnostics.ResetForTesting pattern),
    flow-lang/StandardLibrary/Improv/StyleRegistry.cs lines 150-270 (verify 4 [improv] carve-out file:line),
    flow-lang/Interpreter/Interpreter.cs lines 470-480 (verify [live] carve-out file:line),
    flow-lang/StandardLibrary/Generative/MarkovFunctions.cs lines 247-258 (canonical advisory shape template)
  </read_first>
  <action>
    File 1: `.planning/phases/44-strict-mode/strict-error-manifest.csv`
    - First row: CSV header `file_path,line,builtin,tag,sentinel_body,priority,carve_out,axis,param,range`.
    - 13 §6a rows from AUDIT Table §6a Column 5 with `[strict] ` prefix applied per D-07. Use TransformFunctions.cs lines 106/107/649/650/657/658/666/667/785/821/904/960/1106. Mark axis=B, priority=HIGH, carve_out=false. Quote sentinel_body field if it contains commas (CSV quoting).
    - ~113 §6b advisory rows. For each module group in AUDIT §6b, walk `advisory-sites.txt` and pin one row per WarnOnce site. Use the representative sentinel from AUDIT §6b Column 4 verbatim with `[strict] ` prepended. Group by priority per AUDIT §6b routing column (HIGH/MED/LOW per RESEARCH §"Site Inventory").
    - 5 CARVE-OUT rows with carve_out=true (still listed for completeness — xUnit loader excludes them):
      - flow-lang/Interpreter/Interpreter.cs:476 | tag=live | priority=LOW | per D-v1.5-07
      - flow-lang/StandardLibrary/Improv/StyleRegistry.cs:156 | tag=improv | priority=LOW
      - flow-lang/StandardLibrary/Improv/StyleRegistry.cs:244 | tag=improv | priority=LOW
      - flow-lang/StandardLibrary/Improv/StyleRegistry.cs:258 | tag=improv | priority=LOW
      - flow-lang/StandardLibrary/Improv/StyleRegistry.cs:265 | tag=improv | priority=LOW
    - Per RESEARCH Assumption A4: exact in-scope count likely 112-113 depending on whether StyleRegistry:156 (`improv:userOverride`) is counted as composer-surface OR environmental. Plan-phase decision (locked here): treat ALL 4 StyleRegistry rows as carve-outs per D-06 wording "4 [improv] style-pack discovery advisories". The userOverride row is one of the 4 carve-outs.
    - Final row count target: 13 §6a + ~113 §6b + 5 carve-outs ≈ 131 total rows in CSV. In-scope rows (carve_out=false): 13 + ~113 ≈ ~126.
    - If grep finds additional WarnOnce sites not in AUDIT §6b (RESEARCH grep counted ~120 vs AUDIT ~117), include them with priority=MED and a sentinel_body sourced from the actual source-file string literal at that line.

    File 2: `flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs`
    - Namespace `FlowLang.Tests.Integration.Phase44`.
    - `public static class StrictErrorManifestLoader { ... }`
    - Method `public static IReadOnlyList<StrictErrorRow> LoadAll()` — reads `.planning/phases/44-strict-mode/strict-error-manifest.csv` (resolve via `FindRepoRoot` helper mirroring Phase 42's pattern; copy the helper inline OR reference Phase 42's ClampGrepConsistencyTests if its helper is `internal` accessible).
    - Method `public static IEnumerable<object[]> LoadInScopeSites()` — yields object[] arrays for xUnit `[MemberData]` consumption: each yields `{file_path, line, builtin, tag, sentinel_body, priority}` for `carve_out=false` rows only.
    - Method `public static IEnumerable<object[]> LoadCarveOutSites()` — yields object[] for the 5 carve-out rows (used by Plan 44-08's carve-out preservation Facts).
    - Method `public static IEnumerable<object[]> LoadHighPrioritySites()` / `LoadMedLowPrioritySites()` — partitions in-scope rows by priority for Plan 44-06 / 44-07.
    - `StrictErrorRow` record: `(string FilePath, int Line, string Builtin, string Tag, string SentinelBody, string Priority, bool CarveOut, string Axis, string Param, string Range)`.

    File 3: `flow-lang.Tests/Integration/Phase44/StrictErrorManifestSanityTests.cs`
    - Namespace `FlowLang.Tests.Integration.Phase44`.
    - `[Trait("Category", Phase44TestCategory.Phase44)]` class attribute.
    - Class ceremony: `[Collection("FlowScripts")]` + `IDisposable` + ctor `RenderingDiagnostics.ResetForTesting()` (mirror Phase 42/43 pattern).
    - 6 Facts:
      - `Fact_ManifestFileExists` — `File.Exists(manifest path)`.
      - `Fact_ManifestHeaderMatchesSchema` — first line equals expected header.
      - `Fact_InScopeRowCount_BetweenLowerAndUpperBound` — assert `LoadInScopeSites().Count() >= 120 && <= 132` (tolerance band per RESEARCH §"Site Inventory" reconciliation; tighten to exact in Plan 44-05 after Wave 1 lands).
        > **W8 NOTE:** the [120, 132] tolerance is INTENTIONALLY wide for Wave 1. The downstream Plans 44-06 + 44-07 tighten via per-site Theory `[InlineData]` rows — each row is a strict equality check on the exact count of in-scope sites in its module (e.g., `[InlineData("Generative/MarkovFunctions.cs", 8)]`). Plan 44-00 owns the upper-bound regression-pin; per-module exactness is owned downstream.
      - `Fact_CarveOutCount_ExactlyFive` — assert `LoadCarveOutSites().Count() == 5`.
      - `Fact_Axis6aClampCount_ExactlyThirteen` — filter rows where `builtin in { quantize, crescendo, decrescendo, swell, humanize, humanizeGaussian, vary, legato, repeat }` and `Param != null` and `Axis==B` and `priority==HIGH` should yield exactly 13.
      - `Fact_NoSentinelLacksStrictPrefix` — every in-scope row's sentinel_body MUST start with `[strict] ` (per D-07 format).

    All 6 Facts MUST pass at Plan 44-00 commit close.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.StrictErrorManifestSanityTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    strict-error-manifest.csv exists with ~131 rows (13 + ~113 + 5); CSV header line matches schema; StrictErrorManifestLoader compiles + loads all rows; 6 sanity Facts GREEN; carve-out partition isolates exactly 5 rows; every in-scope sentinel has `[strict] ` prefix.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo file system → test process | CSV file consumed at xUnit Theory load time; reads scoped to `.planning/phases/44-strict-mode/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-00-01 | Information Disclosure | CSV contents | accept | Manifest is composer-readable .planning artifact; no secrets, no PII, no credentials. Committed to repo. |
| T-44-00-02 | Tampering | Manifest hand-edit drift | mitigate | `strict-site-grep.sh` re-extractor regenerates raw lists; Plan 44-05 + 44-06 + 44-07 each pin row counts via Theory cardinality assertions; per-row file:line pinning catches accidental row drops. |
| T-44-00-03 | DoS | xUnit Theory data-source size | accept | ~126 rows × ~30 bytes each = ~4KB; well within MemberData practical limits. |
</threat_model>

<verification>
- `.planning/phases/44-strict-mode/strict-error-manifest.csv` exists with header + ~131 rows.
- `flow-lang.Tests/Integration/Phase44/Phase44TestCategory.cs` declares the Category constant.
- `flow-lang.Tests/Integration/Phase44/StrictErrorManifestLoader.cs` parses the CSV cleanly.
- 6 Wave 0 sanity Facts (StrictErrorManifestSanityTests) all GREEN.
- `scripts/audit/strict-site-grep.sh` is executable + re-emits raw lists.
- README documents the schema + carve-out policy.
</verification>

<success_criteria>
- CSV row count: 13 (§6a) + ~113 (§6b) + 5 (carve-outs) ≈ 131 total; ~126 in-scope rows.
- Sentinels for the 13 §6a rows match AUDIT §6a Column 5 verbatim with `[strict] ` prefix.
- 5 carve-out rows pinned at exact file:line: Interpreter.cs:476 + StyleRegistry.cs:{156,244,258,265}.
- xUnit Category=Phase44 trait works: `dotnet test --filter Category=Phase44` discovers the 6 sanity Facts.
- StrictErrorManifestLoader exposes partitioned MemberData sources used by Plans 44-05..44-08.
</success_criteria>

<output>
Create `.planning/phases/44-strict-mode/44-00-SUMMARY.md` documenting: total in-scope vs carve-out row count, the AUDIT vs RESEARCH count reconciliation outcome, the partitioning scheme (HIGH/MED/LOW), and the wave-1 dependencies that will consume the loader.
</output>
</content>
</invoke>