---
phase: 44-strict-mode
plan: 11
type: execute
wave: 6
depends_on:
  - 44-03
  - 44-04
  - 44-05
  - 44-06
  - 44-07
  - 44-09
  - 44-10
files_modified:
  - tests/strict/test_strict_axis_a_overload.flow
  - tests/strict/test_strict_axis_b_clamps.flow
  - tests/strict/test_strict_explicit_conversions.flow
  - tests/strict/test_strict_equality.flow
  - tests/strict/test_strict_with_justintonation.flow
  - tests/strict/test_strict_dict_typecheck.flow
  - tests/strict/showcase_strict.flow
  - flow-lang.Tests/Integration/Phase44/Phase44TwoRunDeterminismTests.cs
  - flow-lang.Tests/Integration/Phase44/StrictFlowScriptSuiteTests.cs
autonomous: true
requirements:
  - REQ-STRICT-14
  - REQ-STRICT-15
tags:
  - phase-44
  - wave-6
  - showcase
  - determinism
  - closure
must_haves:
  truths:
    - "D-14 positive `tests/strict/*.flow` suite — 6 fixtures + 1 showcase demonstrating composer-facing strict patterns"
    - "Each fixture begins with `enable strict;` and runs to completion (validates strict files work with explicit conversions + no coercion errors)"
    - "showcase_strict.flow — ~16 bar single-instrument piece using (db x) / (hz x) / (cents x) naturally per CONTEXT specifics"
    - "CONTEXT specifics: `enable strict;` + `enable justIntonation;` compose — test_strict_with_justintonation.flow verifies"
    - "D-13 baseline regression-pin via test_strict_dict_typecheck.flow"
    - "REQ-STRICT-15: two-run cmp-clean determinism preserved — Phase44TwoRunDeterminismTests `[Theory]` over ALL 7 `tests/strict/*.flow` files (showcase_strict + 6 narrow fixtures), running each twice and asserting SHA256 stdout equality + writeWav byte equality (where applicable). Per W10 expansion (was: representative subset).
    - "StrictFlowScriptSuiteTests xUnit Fact wraps `for f in tests/strict/test_*.flow; do dotnet run --project flow-interpreter $f; done` as the integration phase-gate (per VALIDATION.md per-phase command)"
  artifacts:
    - path: "tests/strict/test_strict_axis_a_overload.flow"
      provides: "Composer uses explicit conversions; (gain buf -12dB) works"
      contains: "enable strict;"
    - path: "tests/strict/test_strict_axis_b_clamps.flow"
      provides: "In-range args to all 13 §6a clamp sites — succeeds without errors"
      contains: "enable strict;"
    - path: "tests/strict/test_strict_explicit_conversions.flow"
      provides: "Exercise all 6 forward + 4 reverse builtins"
      contains: "(db"
    - path: "tests/strict/test_strict_equality.flow"
      provides: "(equals 1 1.0) returns false; cross-type comparison errors handled"
      contains: "equals"
    - path: "tests/strict/test_strict_with_justintonation.flow"
      provides: "Both pragmas compose"
      contains: "enable justIntonation;"
    - path: "tests/strict/test_strict_dict_typecheck.flow"
      provides: "D-13 baseline regression-pin"
      contains: "(dict"
    - path: "tests/strict/showcase_strict.flow"
      provides: "16-bar piece naturally using explicit music-type conversions"
      contains: "enable strict;"
    - path: "flow-lang.Tests/Integration/Phase44/Phase44TwoRunDeterminismTests.cs"
      provides: "REQ-STRICT-15 — two-run cmp-clean preserved across strict-mode introduction"
      contains: "Phase44TwoRunDeterminismTests"
    - path: "flow-lang.Tests/Integration/Phase44/StrictFlowScriptSuiteTests.cs"
      provides: "Integration phase-gate — every tests/strict/*.flow runs to completion"
      contains: "StrictFlowScriptSuiteTests"
  key_links:
    - from: "flow-lang.Tests/Integration/Phase44/StrictFlowScriptSuiteTests.cs"
      to: "tests/strict/"
      via: "Directory.EnumerateFiles + dotnet run --project flow-interpreter per file"
      pattern: "tests/strict"
    - from: "flow-lang.Tests/Integration/Phase44/Phase44TwoRunDeterminismTests.cs"
      to: "tests/strict/showcase_strict.flow"
      via: "Execute twice + SHA-256 over stdout / writeWav bytes"
      pattern: "SHA"
---

<objective>
Close Phase 44 with the composer-facing positive `.flow` test suite + showcase + two-run determinism Fact + integration phase-gate. 6 narrow fixtures cover each strict-mode behavior in isolation; one showcase (`showcase_strict.flow`, ~16 bars) demonstrates a naturally-written strict piece. xUnit `Phase44TwoRunDeterminismTests` runs a representative subset twice and asserts SHA-equal output — verifies REQ-STRICT-15 (no PRNG sites added; mechanical advisory→error path is deterministic). `StrictFlowScriptSuiteTests` wraps the per-file `dotnet run` loop as an xUnit Fact so the integration phase-gate fits in CI.

Purpose: Plan 44-11 is the closure milestone. After this plan ships, the phase is feature-complete: pragma + foundation + AST + snapshot + Axis A + Axis B + Axis C + explicit conversions + REPL + live + showcase + determinism. Composers can write `enable strict;` files, use the 6 forward + 4 reverse conversions, see clear `[strict] ...` errors when something's off, and trust that strict mode itself didn't introduce non-determinism.

Output: 7 `.flow` files under `tests/strict/`, 2 Phase44 test files closing the suite, plus the existing manifest-driven Theory tests (Plans 44-05/06/07) and the per-axis test files (Plans 44-01/02/03/04/08/09/10) running in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-strict-mode/44-CONTEXT.md
@.planning/phases/44-strict-mode/44-RESEARCH.md
@.planning/phases/44-strict-mode/44-PATTERNS.md
@.planning/phases/44-strict-mode/44-VALIDATION.md
@.planning/phases/44-strict-mode/44-10-PLAN.md

<interfaces>
<!-- Existing `.flow` test analogs to mirror -->

From `tests/test_tuning_*.flow` (5 existing pragma-using `.flow` test scripts — same surface pattern):
- Begin with `enable <pragma>;` at top.
- Use Flow-language builtins exercising the pragma's behavior.
- Verify via `print` output (no unit framework — console-output verified per CLAUDE.md).
- Each script prints a clear `PASS` or `OK` line at end so the grep/exit-code check is unambiguous.

CONTEXT §specifics — showcase guidance:
- ~16 bars, single instrument.
- Demonstrates composer using `(db x)`, `(hz x)`, `(cents x)` explicit conversions NATURALLY.
- NOT a contrived type-error gauntlet.
- Optional but discoverable.

Two-run cmp-clean per CLAUDE.md "Conventions":
- Run a fixture twice via `dotnet run --project flow-interpreter <file>`.
- Capture stdout + any `writeWav` output bytes.
- Compute SHA-256 over each run's output bytes.
- Assert SHA-256 of run 1 == SHA-256 of run 2.

`writeWav` precedent in Phase 28+ RmsRegressionTests + Phase 37 deterministic-output suites — the showcase MAY include a `writeWav` call to a temp path so the cmp-clean assertion has audio bytes to compare.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 6 narrow positive .flow fixtures + showcase_strict.flow</name>
  <files>
    tests/strict/test_strict_axis_a_overload.flow,
    tests/strict/test_strict_axis_b_clamps.flow,
    tests/strict/test_strict_explicit_conversions.flow,
    tests/strict/test_strict_equality.flow,
    tests/strict/test_strict_with_justintonation.flow,
    tests/strict/test_strict_dict_typecheck.flow,
    tests/strict/showcase_strict.flow
  </files>
  <read_first>
    tests/test_tuning_equal.flow (sibling pragma-using .flow precedent),
    tests/test_scale_lint.flow (pragma + behavior surface),
    tests/test_pragma_isolation.flow (cross-pragma test pattern),
    tests/test_dict_keys.flow (dict-typed-key precedent for test_strict_dict_typecheck.flow),
    examples/showcase.flow (Phase 27 minimal polyrhythmic — structural template for showcase_strict.flow),
    .planning/phases/44-strict-mode/44-CONTEXT.md §specifics (showcase guidance),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Validation Architecture" + §"Wave 0 Gaps"
  </read_first>
  <action>
    Each file MUST begin with `enable strict;` (and where applicable, additional pragmas). Each ends with a clear `(print "PASS")` line so exit-code + grep verification works.

    `tests/strict/test_strict_axis_a_overload.flow` — Axis A coverage:
    - Demonstrate explicit conversions making strict-mode calls work: `(gain buf -12dB)` succeeds (NOT `(gain buf -12.0)` which would fail strict).
    - Show a small DSP pipeline using only typed args: `Buffer b = (createSineTone 440Hz 1.0s); Buffer g = (gain b -6dB); Buffer r = (reverb g 1.5s)`.
    - End with `(print "test_strict_axis_a_overload PASS")`.

    `tests/strict/test_strict_axis_b_clamps.flow` — Axis B coverage:
    - In-range args to all 13 §6a clamp sites — strict succeeds because args are within bounds.
    - Example: `Sequence s = | C4 D4 E4 F4 |; Sequence c = (crescendo s 0.3 0.9); Sequence h = (humanize s 0.5); Sequence r = (repeat s 4)`.
    - End with `(print "test_strict_axis_b_clamps PASS")`.

    `tests/strict/test_strict_explicit_conversions.flow` — Explicit conversion matrix:
    - Forward: `(db 5)`, `(hz 440)`, `(ms 100)`, `(sec 2.5)`, `(cents 50)`, `(semitones 2)`.
    - Reverse: `(double -12dB)`, `(float 440Hz)`, `(int +2st)`, `(int 100ms)`, `(long 2.5s)`, `(double 50c)`.
    - Print each result + verify expected stringification.
    - End with `(print "test_strict_explicit_conversions PASS")`.

    `tests/strict/test_strict_equality.flow` — D-11 + D-12 surface:
    - `Bool a = (equals 1 1)` → true.
    - `Bool b = (equals 1 1.0)` → false in strict.
    - `Bool c = (equals (double 1) 1.0)` → true (explicit conversion makes both Double).
    - `Bool d = (gt 2 1)` → true (same type).
    - In a comment, document that `(gt 1 1.0)` would error in strict — don't actually call it (would abort the script).
    - End with `(print "test_strict_equality PASS")`.

    `tests/strict/test_strict_with_justintonation.flow` — Pragma composition per CONTEXT specifics:
    - Two pragmas at top: `enable strict; enable justIntonation;`.
    - Render a small Cmaj-key piece — verify both pragmas active.
    - End with `(print "test_strict_with_justintonation PASS")`.

    `tests/strict/test_strict_dict_typecheck.flow` — D-13 regression-pin:
    - Build a Dict with both Int 1 and Float 1.0 keys: `Dict<Number, String> d = (dict 1 "one" 1.0 "one-point-oh")`.
    - `(get d 1)` → "one"; `(get d 1.0)` → "one-point-oh".
    - `(size d)` → 2.
    - End with `(print "test_strict_dict_typecheck PASS")`.

    `tests/strict/showcase_strict.flow` — ~16-bar composer-facing piece per CONTEXT specifics:
    - File begins `enable strict;`.
    - Define a small melody / chord progression.
    - Naturally use explicit conversions: `(gain buf -6dB)`, `(reverb buf 1.5s)`, possibly `(transpose seq (cents 50))` and `(lowpass buf 2000Hz)`.
    - Single instrument (e.g., piano via `(renderSong song "piano")`).
    - Optional: `(writeWav "out_strict_showcase.wav" finalBuf)` so Phase44TwoRunDeterminismTests has audio bytes to SHA. If included, ensure the output path is `/tmp/` or a `.gitignore`-covered location.
    - End with `(print "showcase_strict PASS")`.

    Each script's PASS line is the verification gate. The integration test in Task 2 confirms exit-code 0 + stdout contains "PASS".
  </action>
  <verify>
    <automated>for f in tests/strict/test_*.flow tests/strict/showcase_strict.flow; do dotnet run --project flow-interpreter "$f" 2&gt;&amp;1 | tee /tmp/flow_$(basename $f).out &amp;&amp; grep -q "PASS" /tmp/flow_$(basename $f).out || { echo "FAIL: $f"; exit 1; }; done</automated>
  </verify>
  <done>
    All 7 .flow files written; each ends with PASS line; each runs to completion under strict mode without errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StrictFlowScriptSuiteTests + Phase44TwoRunDeterminismTests — phase-gate integration + cmp-clean</name>
  <files>
    flow-lang.Tests/Integration/Phase44/StrictFlowScriptSuiteTests.cs,
    flow-lang.Tests/Integration/Phase44/Phase44TwoRunDeterminismTests.cs
  </files>
  <read_first>
    The 7 .flow files from Task 1 (verify shapes + PASS lines),
    flow-lang.Tests/Integration/Phase42/AuditHarnessTests.cs (Process.Start dotnet run pattern),
    flow-lang.Tests/Helpers/RmsRegressionTests.cs if exists (SHA-256 over WAV bytes pattern),
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Validation Architecture" + §"Pitfall 5",
    CLAUDE.md §"Conventions" two-run cmp-clean contract
  </read_first>
  <behavior>
    - Test: every `tests/strict/test_*.flow` file runs to completion with exit code 0 + stdout contains PASS.
    - Test: `tests/strict/showcase_strict.flow` runs to completion.
    - Test: running `showcase_strict.flow` twice produces SHA-equal stdout (after stripping any timestamp/CallSite reproducibility-tagged lines).
    - Test: running any selected fixture twice produces SHA-equal stdout.
    - Test: if showcase_strict.flow writes a WAV file, two runs produce SHA-equal WAV bytes.
  </behavior>
  <action>
    File 1 — `flow-lang.Tests/Integration/Phase44/StrictFlowScriptSuiteTests.cs`:
    Same namespace + traits + ceremony. Use `[Theory] [MemberData(nameof(StrictFlowFiles))]` sourced from `Directory.EnumerateFiles(Path.Combine(repoRoot, "tests/strict"), "*.flow")`.

    Single Theory body:
    ```
    [Theory] [MemberData(nameof(StrictFlowFiles))]
    public void Fact_StrictFlowFile_RunsToCompletion(string relativePath)
    {
        var repoRoot = FindRepoRoot();
        var fullPath = Path.Combine(repoRoot, relativePath);
        var psi = new ProcessStartInfo("dotnet",
            $"run --project flow-interpreter \"{fullPath}\"")
        {
            RedirectStandardOutput = true,
            RedirectStandardError = true,
            UseShellExecute = false,
            WorkingDirectory = repoRoot
        };
        using var proc = Process.Start(psi);
        proc.WaitForExit(60000);  // 60s timeout
        Assert.Equal(0, proc.ExitCode);
        var stdout = proc.StandardOutput.ReadToEnd();
        Assert.Contains("PASS", stdout, StringComparison.Ordinal);
    }
    ```

    Plus one summary Fact:
    - `Fact_AtLeastSevenStrictFiles_Exist` — verify `Directory.EnumerateFiles` returns ≥ 7 entries (regression-pin against accidental fixture deletion).

    File 2 — `flow-lang.Tests/Integration/Phase44/Phase44TwoRunDeterminismTests.cs`:
    Same namespace + traits + ceremony. Run the showcase + one selected narrow fixture twice and compare SHA-256.

    Per W10: convert to `[Theory] [MemberData(nameof(AllStrictFlowFiles))]` over ALL 7 `tests/strict/*.flow` files (showcase_strict + 6 narrow fixtures). MemberData sources `Directory.EnumerateFiles(Path.Combine(repoRoot, "tests/strict"), "*.flow")`.

    Theory body:
    - `Fact_StrictFlowFile_TwoRunsSHAEqual_Stdout(string relativePath)` — run via Process.Start dotnet run twice (separate processes, same args), capture stdout, normalize (strip trailing whitespace + any `[live]` advisory dedup), assert SHA256(run1) == SHA256(run2).

    Plus 1 special-case Fact (showcase audio):
    - `Fact_ShowcaseStrictWav_TwoRunsByteEqual` — IF showcase_strict.flow writes a WAV file, run twice with separate temp paths; SHA-256 the WAV bytes; assert SHA-equal. If no WAV output, skip via `[Fact(Skip="showcase_strict.flow does not invoke writeWav")]`.

    Total Facts: 7 (Theory rows: one per strict fixture) + 1 (WAV byte-equal special case) = 8 expected to GREEN.

    Per RESEARCH Pitfall 5: cmp-clean is the END-TO-END verification that strict mode introduced ZERO new PRNG sites. Plans 44-05/06/07's mechanical advisory→error rewrites all use deterministic concat — Plan 44-11 confirms.

    Helper: copy FindRepoRoot from Phase 42 ClampGrepConsistencyTests; if Process.Start is timeout-prone, raise the timeout but cap at 60s.

    Charitable skip: if the dotnet CLI is unavailable in the test environment, skip both Facts (mirrors Phase 42's IsBashAvailable charitable-skip pattern).
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests/flow-lang.Tests.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.StrictFlowScriptSuiteTests|FullyQualifiedName~Phase44.Phase44TwoRunDeterminismTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    Both xUnit fixtures exist; StrictFlowScriptSuiteTests Theory runs over 7+ files and asserts PASS-exit; Phase44TwoRunDeterminismTests `[Theory]` over ALL 7 strict fixtures (per W10) — 7 SHA-equal Theory rows + 1 WAV byte-equal Fact GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test seed → composer-facing fixture | All test fixtures are composer-readable; no secrets; no external network |
| Process.Start dotnet run → test harness | Spawned subprocess invokes the interpreter in a worker; output captured + asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-11-01 | DoS | Test hangs from infinite-loop fixture | mitigate | 60s WaitForExit timeout on each Process.Start; xUnit Fact fails (not hangs) if exceeded. |
| T-44-11-02 | Tampering | Two-run cmp-clean regression | mitigate | Phase44TwoRunDeterminismTests is THE verification — if regression introduced, Fact fails. CLAUDE.md Conventions contract preserved. |
| T-44-11-03 | Availability | Showcase audio doesn't play correctly | accept | VALIDATION.md manual-only check covers audio quality; cmp-clean covers SHA byte-equality. |
</threat_model>

<verification>
- 7 .flow files written; each runs to completion with PASS.
- StrictFlowScriptSuiteTests Theory enumerates all files and pins PASS exit.
- Phase44TwoRunDeterminismTests verifies SHA-equal across representative scripts.
- Two-run cmp-clean preserved per CLAUDE.md Conventions.
- Phase 44 feature-complete: pragma + foundation + AST + snapshot + Axis A + Axis B + Axis C + explicit conversions + REPL + live + showcase + determinism.
</verification>

<success_criteria>
- All 7 `tests/strict/*.flow` files run cleanly.
- Two-run cmp-clean passes on ALL 7 strict fixtures (W10 — Theory expansion vs prior "representative subset").
- Manifest-driven Theory tests (Plans 44-05/06/07) + per-axis tests (Plans 44-01..10) all GREEN.
- Phase 44 closes with `/gsd:verify-work` ready.
</success_criteria>

<output>
Create `.planning/phases/44-strict-mode/44-11-SUMMARY.md` documenting: the 7 .flow files shipped, the cmp-clean SHA values for the showcase (note them for future regression reference), and a phase-level summary linking back to REQ-STRICT-01..15 coverage.
</output>
</content>
</invoke>