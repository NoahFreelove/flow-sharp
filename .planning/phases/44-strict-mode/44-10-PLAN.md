---
phase: 44-strict-mode
plan: 10
type: execute
wave: 4
depends_on:
  - 44-08
files_modified:
  - flow-interpreter/Repl.cs
  - flow-interpreter/LiveReloadManager.cs
  - flow-lang.Tests/Integration/Phase44/ReplStrictMetaCommandTests.cs
  - flow-lang.Tests/Integration/Phase44/LiveBlockStrictTests.cs
autonomous: true
requirements:
  - REQ-STRICT-12
  - REQ-STRICT-13
tags:
  - phase-44
  - wave-4
  - repl
  - live-coding
must_haves:
  truths:
    - "D-16 REPL: `:strict on` / `:strict off` meta-commands toggle a sticky session flag mutating engine.Context.StrictMode"
    - "D-16 REPL: typing `enable strict;` at the REPL also flips the sticky flag (per-line PragmaScanner observes and updates _sessionStrict)"
    - "D-16 REPL: per-line input inherits the sticky flag — every engine.Execute(line, fileName) call sets context.StrictMode from _sessionStrict BEFORE evaluating"
    - "D-15 Live: `enable strict;` file with `live 1bar { ... }` block runs the block body under strict on initial parse AND on every live-reload re-eval (via existing Phase 38 LiveReloadManager.RenderScript fresh-engine path that re-runs PragmaScanner)"
    - "D-15 Carve-out: `[live] entering live block` advisory STAYS charitable regardless of strict (Pitfall 2 / D-v1.5-07 design-lock); already verified by Plan 44-07's CarveOutsPreservedTests"
    - "RESEARCH Pattern 7: Live re-eval AUTOMATICALLY applies strict via fresh FlowEngine.Execute → PragmaScanner.Scan → ApplyStrictPragma path from Plan 44-01 — zero new plumbing in LiveReloadManager"
    - "REPL test seam: HandleCommandForTesting at Repl.cs:232 already exposed; reuse for unit testing"
  artifacts:
    - path: "flow-interpreter/Repl.cs"
      provides: ":strict on/off meta-command arms + _sessionStrict sticky field + SetStrict helper"
      contains: "\":strict on\""
    - path: "flow-interpreter/LiveReloadManager.cs"
      provides: "NO functional change required — Pattern 7 auto-apply. May add a Console advisory line if `enable strict;` is detected at reload time for composer UX."
      contains: "RenderScript"
    - path: "flow-lang.Tests/Integration/Phase44/ReplStrictMetaCommandTests.cs"
      provides: "Sticky session + meta-command + enable strict;-at-prompt coverage"
      contains: "ReplStrictMetaCommandTests"
    - path: "flow-lang.Tests/Integration/Phase44/LiveBlockStrictTests.cs"
      provides: "live { } block runs strict when enclosing file declares enable strict;"
      contains: "LiveBlockStrictTests"
  key_links:
    - from: "flow-interpreter/Repl.cs"
      to: "flow-lang/Runtime/ExecutionContext.cs"
      via: "_engine.Context.StrictMode = _sessionStrict mutation before each engine.Execute(line)"
      pattern: "StrictMode\\s*=\\s*_sessionStrict"
    - from: "flow-interpreter/LiveReloadManager.cs"
      to: "flow-lang/Core/FlowEngine.cs"
      via: "Fresh engine.Execute path re-runs PragmaScanner + ApplyStrictPragma per Plan 44-01"
      pattern: "engine\\.Execute"
---

<objective>
Wire D-15 (live-block strict) and D-16 (REPL sticky strict + :strict on/off meta-commands). The live-block path is largely a no-op: RESEARCH Pattern 7 confirms LiveReloadManager.RenderScript already constructs a fresh FlowEngine and calls Execute → PragmaScanner → ApplyStrictPragma (Plan 44-01) — strict re-applies automatically. Plan 44-10 adds the LiveBlockStrictTests to PIN this behavior + optionally adds a Console advisory line when a live-reload triggers strict errors. The REPL path adds `:strict on/off` meta-commands + a sticky `_sessionStrict` flag + per-line mutation of `engine.Context.StrictMode` before Execute.

Purpose: composers need a fast feedback loop for strict-mode work. REPL toggling lets them flip mode without restarting the interpreter. Live-block strict lets them edit-time check during live performance sessions (with the `[live] entering` advisory staying charitable per the design lock — never die mid-set).

Output: Repl meta-command extension (~30 LOC), zero or one LiveReloadManager hint (cosmetic), 2 Phase44 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-strict-mode/44-CONTEXT.md
@.planning/phases/44-strict-mode/44-RESEARCH.md
@.planning/phases/44-strict-mode/44-PATTERNS.md
@.planning/phases/44-strict-mode/44-VALIDATION.md
@.planning/phases/44-strict-mode/44-08-PLAN.md
@.planning/phases/44-strict-mode/44-01-PLAN.md

<interfaces>
<!-- Existing analogs -->

From flow-interpreter/Repl.cs lines 199-232 (HandleCommand meta-command dispatcher + HandleCommandForTesting test seam):
```csharp
private bool HandleCommand(string command)
{
    var trimmed = command.TrimEnd();
    // Phase 38 :help <name> extension at lines 204-214 ...
    return command.ToLower() switch
    {
        ":quit" or ":q" or ":exit" => false,
        ":help" or ":h" => ShowHelp(),
        ":clear" or ":cls" => ClearScreen(),
        ":stop" => StopAudio(),
        _ => UnknownCommand(command)
    };
}
```

From flow-interpreter/Repl.cs (per-line execution loop — locate the `_engine.ExecuteScriptAndGetResult(input, "<repl>")` call around line 70 per RESEARCH §"Pattern 8"). Plan 44-10 mutates `_engine.Context.StrictMode = _sessionStrict` BEFORE each call.

From flow-interpreter/LiveReloadManager.cs lines 836-883 (RenderScript fresh-engine + Execute path — RESEARCH Pattern 7 confirms strict re-applies automatically via PragmaScanner).

From Plan 44-01 — `FlowEngine.ApplyStrictPragma(program)` runs between parse and interpret in every `Execute` call, including per-line REPL Execute and per-reload Live RenderScript Execute. Plan 44-10 leverages this — no new plumbing needed beyond the Repl meta-command.

Pattern 8 (RESEARCH §"REPL sticky strict flag"):
```csharp
private bool _sessionStrict = false;     // D-16 sticky flag

return command.ToLower() switch
{
    ":strict on" => SetStrict(true),
    ":strict off" => SetStrict(false),
    // ... existing arms ...
};

private bool SetStrict(bool on)
{
    _sessionStrict = on;
    _engine.Context.StrictMode = on;     // mutate immediately
    Console.WriteLine($"[strict] {(on ? "on" : "off")}");
    return true;
}

// Before each line execution:
// _engine.Context.StrictMode = _sessionStrict;
// (if the typed line contains `enable strict;`, PragmaScanner observes and the per-line context.StrictMode is already true; sync _sessionStrict if so after Execute returns)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REPL `:strict on/off` meta-command + sticky `_sessionStrict` + per-line context mutation + ReplStrictMetaCommandTests</name>
  <files>
    flow-interpreter/Repl.cs,
    flow-lang.Tests/Integration/Phase44/ReplStrictMetaCommandTests.cs
  </files>
  <read_first>
    flow-interpreter/Repl.cs lines 1-260 (full file — locate per-line execution loop + HandleCommand + HandleCommandForTesting test seam),
    flow-interpreter/Repl.cs around line 70 (per-line Execute call site — _engine.ExecuteScriptAndGetResult or equivalent),
    .planning/phases/44-strict-mode/44-CONTEXT.md D-16,
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Pattern 8: REPL sticky strict flag",
    .planning/phases/44-strict-mode/44-PATTERNS.md §"flow-interpreter/Repl.cs:216-223"
  </read_first>
  <behavior>
    - Test: typing `:strict on` flips `_sessionStrict` to true and mutates `_engine.Context.StrictMode = true`.
    - Test: typing `:strict off` flips back to false.
    - Test: typing `:strict on` then a non-meta line like `(print 42)` — the line runs under strict and reports `[strict] (print) requires String — got Int`.
    - Test: typing `:strict off` then `(print 42)` — line runs charitable and prints `42`.
    - Test: typing `enable strict;` at the REPL — PragmaScanner picks it up; `_sessionStrict` is updated to true after Execute returns (sync via observing the resulting context.StrictMode value).
    - Test: typing `:strikt on` (typo) — falls through to UnknownCommand without modifying _sessionStrict.
    - Test: `HandleCommandForTesting(":strict on")` returns true (command consumed).
  </behavior>
  <action>
    File 1 — `flow-interpreter/Repl.cs`:
    1. Add a private field `private bool _sessionStrict = false;` near other Repl state.
    2. Add private helper `private bool SetStrict(bool on)`:
       ```
       _sessionStrict = on;
       _engine.Context.StrictMode = on;
       Console.WriteLine($"[strict] {(on ? "on" : "off")}");
       return true;
       ```
    3. Extend the existing HandleCommand switch (around line 216-222) with two new arms inserted BEFORE the discard:
       ```
       ":strict on" => SetStrict(true),
       ":strict off" => SetStrict(false),
       ```
    4. Modify the per-line execution loop (around line 70) — BEFORE calling `_engine.ExecuteScriptAndGetResult(input, "<repl>")` (or equivalent), set `_engine.Context.StrictMode = _sessionStrict;`. AFTER the call returns, if PragmaScanner observed `enable strict;` in the just-typed line, the context's StrictMode may be true even if _sessionStrict was false — sync the session flag: `if (_engine.Context.StrictMode != _sessionStrict) _sessionStrict = _engine.Context.StrictMode;`. The latter sync line means typing `enable strict;` at the REPL implicitly flips the sticky flag (per RESEARCH §Pattern 8 subtle requirement).
    5. Verify HandleCommandForTesting at line 232 is callable from xUnit — if it's already `internal` or `public`, no change; otherwise expose via `[InternalsVisibleTo("flow-lang.Tests")]` if needed.

    File 2 — `flow-lang.Tests/Integration/Phase44/ReplStrictMetaCommandTests.cs`:
    Same namespace + traits + ceremony. 8 Facts:
    - `Fact_StrictOn_SetsSessionFlagAndContextStrictMode` — call `HandleCommandForTesting(":strict on")`; assert `repl.SessionStrictForTesting == true` AND `engine.Context.StrictMode == true`.
    - `Fact_StrictOff_FlipsBack`.
    - `Fact_StrictOnFollowedByPrintInt_ReportsStrictError` — `_sessionStrict=true`; execute `(print 42)` via repl pipeline; assert ErrorReporter has `[strict] (print) requires String — got Int`.
    - `Fact_StrictOffFollowedByPrintInt_AutoStrs` — `_sessionStrict=false`; `(print 42)` prints "42".
    - `Fact_EnableStrictAtPrompt_FlipsSessionFlag` — execute `enable strict;` then assert _sessionStrict observed true post-Execute via the sync path.
    - `Fact_StrictTypo_FallsThroughUnknownCommand` — `HandleCommandForTesting(":strikt on")` returns true (consumed as unknown — UnknownCommand prints and returns true to continue), but _sessionStrict UNCHANGED.
    - `Fact_StrictOn_StatusOutputMatches` — capture stdout; assert `[strict] on\n` printed.
    - `Fact_HandleCommandForTesting_Accessible` — confirm xUnit can call the test seam.

    Expose a `SessionStrictForTesting` getter on Repl OR use reflection to read `_sessionStrict` for the assertion. Reflection is acceptable given the existing test-seam pattern at HandleCommandForTesting.

    Capture stdout via Console.SetOut redirection.
  </action>
  <verify>
    <automated>dotnet build flow-interpreter/flow-interpreter.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.ReplStrictMetaCommandTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    Repl.cs has _sessionStrict + SetStrict + :strict on/off arms; per-line execution loop syncs StrictMode bidirectionally; 8 ReplStrictMetaCommandTests Facts GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LiveBlockStrictTests — D-15 verification (Pattern 7 auto-apply via PragmaScanner re-eval) + optional Console advisory hint</name>
  <files>
    flow-interpreter/LiveReloadManager.cs,
    flow-lang.Tests/Integration/Phase44/LiveBlockStrictTests.cs
  </files>
  <read_first>
    flow-interpreter/LiveReloadManager.cs lines 836-883 (RenderScript fresh-engine path),
    flow-interpreter/LiveReloadManager.cs full file (verify the existing error-reporting path on reload — composers see errors via existing LiveStatusPanel mechanism),
    flow-lang/Core/FlowEngine.cs (verify ApplyStrictPragma from Plan 44-01 runs on every Execute call — confirms Pattern 7 auto-apply),
    .planning/phases/44-strict-mode/44-CONTEXT.md D-15,
    .planning/phases/44-strict-mode/44-RESEARCH.md §"Pattern 7: Live block re-eval through full engine.Execute" + §"Pitfall 6"
  </read_first>
  <behavior>
    - Test: a `.flow` file with `enable strict;\nlive 1bar { (print 1) }` — on initial load, the live block body parses and the (print 1) call hits the Int-not-String overload mismatch in strict (or auto-strs in non-strict). Assertion path: ErrorReporter captures the strict error AT the live block body parse/dispatch.
    - Test: a `.flow` file with `enable strict;\nlive 1bar { (print "ok") }` — runs cleanly; (print "ok") matches the String overload regardless of mode.
    - Test: a non-strict file with `live 1bar { (print 1) }` — Plan 44-08's auto-str path fires; "1" is printed via AutoStr.
    - Test (Pitfall 6): a NON-strict file initially loads with `live 1bar { (print 1) }`; composer edits file to ADD `enable strict;` and saves. On next reload, the body now runs strict — captured error `[strict] (print) requires String — got Int`. Live audio engine fades out the dropped voice per existing Phase 38 LIVE-03 path.
    - Test: `[live] entering live block` advisory STILL fires even in strict files (D-15 carve-out + Plan 44-07's CarveOutsPreservedTests regression-pin).
    - Test: existing Phase 38 LIVE-01 + LIVE-02 + LIVE-03 fixtures continue passing (no regression in live-coding semantics).
  </behavior>
  <action>
    File 1 — `flow-interpreter/LiveReloadManager.cs`:
    RESEARCH Pattern 7 says NO change required — fresh engine.Execute path via Plan 44-01's ApplyStrictPragma re-applies strict on every reload.

    OPTIONAL composer UX hint per Pitfall 6: when a reload introduces a strict-mode error AND the previous reload was non-strict (composer just added `enable strict;`), emit a one-time advisory at the LiveStatusPanel:
    `[live] file became strict — body runs with `enable strict;` checks on this reload`.
    This is a courtesy nudge, not load-bearing. Plan-phase decision: include it if cleanly implementable in <10 LOC; skip if it requires structural changes to LiveReloadManager.

    File 2 — `flow-lang.Tests/Integration/Phase44/LiveBlockStrictTests.cs`:
    Same namespace + traits + ceremony. 6 Facts:
    - `Fact_StrictFileLiveBlockBody_RunsStrict` — author a .flow source string `"enable strict;\nlive 1bar { (print 1) }"`, execute via FlowEngine (not via watch-mode), assert ErrorReporter captures `[strict] (print) requires String — got Int`.
    - `Fact_StrictFileLiveBlockBody_NoErrorOnValidString` — `"enable strict;\nlive 1bar { (print \"ok\") }"` runs cleanly.
    - `Fact_NonStrictFileLiveBlockBody_AutoStrs` — `"live 1bar { (print 1) }"` (no pragma) → Plan 44-08 auto-str path fires; "1" appears in captured stdout.
    - `Fact_LiveEntryAdvisoryStillCharitableInStrict` — verify `RenderingDiagnostics` has the `[live] entering live block` advisory recorded after executing a strict live source, AND no `[strict]` error fires on the entry itself (only on the body's content if any).
    - `Fact_LiveReloadAddStrictPragma_BodyRerunStrict` — simulate Phase 38 LIVE-02 reload sequence: load source A (non-strict with `live 1bar { (print 1) }`), then re-execute source B (`enable strict;` added). Confirm the second execution reports a strict error (the live re-eval picks up the new pragma per Pattern 7).
    - `Fact_BackCompat_Phase38LiveBlockTests_StillPass` — run a subset of Phase 38 LiveBlock*Tests via the dotnet test filter, assert all still GREEN. (This Fact may be implemented as a `dotnet test` invocation OR as a documented manual check; defer to existing test seam capabilities.)

    Live test sources should NOT actually drive the audio engine — use FlowEngine.Execute directly on the .flow source strings. The xUnit Facts verify the BEHAVIOR at the parse + dispatch level, not the watch-mode file-system-event handling (manual verification per VALIDATION.md "REPL `:strict on` ..." manual table covers the watch-mode UX).
  </action>
  <verify>
    <automated>dotnet build flow-interpreter/flow-interpreter.csproj /clp:ErrorsOnly &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase44.LiveBlockStrictTests" --logger "console;verbosity=minimal" &amp;&amp; dotnet test flow-lang.Tests --filter "FullyQualifiedName~Phase38.LiveBlockParserTests" --logger "console;verbosity=minimal"</automated>
  </verify>
  <done>
    LiveReloadManager unchanged (or has a small courtesy advisory addition); 6 LiveBlockStrictTests Facts GREEN; Phase 38 LiveBlock*Tests fixtures unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REPL stdin → engine state mutation | Composer-trusted input; meta-commands are dispatched before evaluation; no new attack surface |
| File-watch event → fresh engine.Execute | Phase 38 LIVE-02 boundary; strict-aware via PragmaScanner; no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10-01 | Tampering | Session flag drift vs context state | mitigate | Sync bidirectionally: SetStrict mutates both; per-line execution sets context from session AND syncs back when PragmaScanner observes the pragma. Fact_EnableStrictAtPrompt_FlipsSessionFlag pins behavior. |
| T-44-10-02 | Availability | Live session dies mid-set due to strict error | mitigate | `[live] entering` advisory STAYS charitable (Plan 44-07's CarveOutsPreservedTests regression-pin); D-v1.5-07 design-lock. Strict errors in body are captured via ErrorReporter and surfaced through existing Phase 38 LIVE-03 stale-closure gate — audio fades out the broken voice without killing the engine. |
</threat_model>

<verification>
- Repl.cs has _sessionStrict + SetStrict + :strict on/off arms.
- Per-line execution syncs StrictMode bidirectionally.
- 8 + 6 = 14 Facts GREEN.
- Phase 38 LiveBlock fixtures unchanged.
</verification>

<success_criteria>
- `:strict on` / `:strict off` works in REPL.
- `enable strict;` typed at REPL flips sticky flag.
- Live block body runs strict when enclosing file declares the pragma.
- Live-entry advisory stays charitable.
- No regression in Phase 38 LIVE fixtures.
</success_criteria>

<output>
Create `.planning/phases/44-strict-mode/44-10-SUMMARY.md` documenting: per-line execution mutation site in Repl.cs, whether LiveReloadManager received the optional courtesy advisory, and any reflection-based test-seam usage.
</output>
</content>
</invoke>