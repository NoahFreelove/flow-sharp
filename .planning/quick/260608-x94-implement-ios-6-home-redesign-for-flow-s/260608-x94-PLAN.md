---
quick_id: 260608-x94
type: execute
autonomous: true
files_modified:
  - flow-site/src/routes/+page.svelte
  - flow-site/src/routes/+page.ts
  - flow-site/src/routes/+layout.svelte
must_haves:
  truths:
    - "Visiting / renders the iOS-6 skeuomorphic home (linen ground, brushed-aluminum toolbar, Aqua jelly buttons, code wells, leather rack, bottom tab bar)"
    - "The 3 hero code wells show Flow-highlighted source rendered at prerender time (no client highlight JS, no window access at module top level)"
    - "Hero + leather Play buttons make Web-Audio sound on click; leather rows animate their VU meters while playing"
    - "Home toolbar + bottom tab bar route to /, /docs, /playground, /showcase; GitHub opens https://github.com/noahfreelove/flow-sharp"
    - "/docs, /playground, /showcase keep the old Logic-Pro-wood chrome unchanged and still work"
    - "pnpm -C flow-site build succeeds with a clean prerender of /"
  artifacts:
    - path: "flow-site/src/routes/+page.svelte"
      provides: "iOS-6 home page (markup + component-scoped skeuo CSS + Svelte-5-runes interactivity)"
      contains: "ios6-page"
    - path: "flow-site/src/routes/+page.ts"
      provides: "prerender-only home load"
      contains: "prerender = true"
    - path: "flow-site/src/routes/+layout.svelte"
      provides: "isHome guard suppressing old chrome on /"
      contains: "isHome"
  key_links:
    - from: "flow-site/src/routes/+page.svelte"
      to: "$lib/home/flow-highlight"
      via: "import highlightFlow, precomputed in <script>"
      pattern: "highlightFlow"
    - from: "flow-site/src/routes/+page.svelte"
      to: "$lib/home/tones"
      via: "import playTone/playMelody/playChord, called from button onclick"
      pattern: "playMelody|playChord|playTone"
    - from: "flow-site/src/routes/+layout.svelte"
      to: "site-chrome + mobile nav"
      via: "{#if !isHome} wrappers"
      pattern: "if !isHome"
---

<objective>
Translate the existing `flow-site/REDESIGN-HANDOFF.md` (§4) into the actual iOS-6 skeuomorphic
HOME page. The `/` route ONLY is rewritten; `/docs`, `/playground`, `/showcase` stay visually
unchanged. Three of the four files are already-built dependencies (`flow-highlight.ts` +
`tones.ts` are ported and dormant); this plan WIRES them into a rewritten `+page.svelte`,
simplifies `+page.ts` to prerender-only, and adds an `isHome` guard to `+layout.svelte`.

This is greenfield web code: TS / Svelte 5 (runes) / pnpm / ESM — NOT the repo-root C# rules
(see CLAUDE.md "## flowlang.dev Site").

Purpose: ship the user's new marketing-page direction (authentic iOS-6 skeuo) over the old
Phase-49 "Logic-Pro-wood" home draft.
Output: a prerendered, audible, route-correct iOS-6 home page.
</objective>

<context>
@flow-site/REDESIGN-HANDOFF.md
@flow-site/.design-handoff/project/index.html
@flow-site/.design-handoff/project/flow.css
@flow-site/src/lib/home/flow-highlight.ts
@flow-site/src/lib/home/tones.ts
@flow-site/src/routes/+page.svelte
@flow-site/src/routes/+page.ts
@flow-site/src/routes/+layout.svelte

<interfaces>
<!-- Contracts the executor consumes directly — no codebase exploration needed. -->

From $lib/home/flow-highlight.ts (pure, prerender-safe):
```ts
export function highlightFlow(src: string): string; // returns <span class="tk-*"> tokens, HTML-escaped
```

From $lib/home/tones.ts (browser-only; AudioContext created lazily on first click — inert at SSR):
```ts
export type ToneType = OscillatorType; // 'sine' | 'square' | 'triangle' | 'sawtooth'
export function playTone(f?: number, d?: number, type?: ToneType): number;       // returns seconds
export function playMelody(notes: string[], dur?: number, type?: ToneType): number; // returns seconds
export function playChord(notes: string[], dur?: number, type?: ToneType): number;  // returns seconds
export function noteFreq(name: string): number;
```

Verbatim hero snippets (copy EXACTLY from index.html — do not paraphrase):
- hello.flow (index.html L109-110):
```
use "@audio"
(play (createSineTone 440Hz 1.0 0.5))
```
- scale.flow (index.html L123-128):
```
use "@audio"
use "@composition"

tempo 120 {
  (play | C4q D4q E4q F4q G4q |)
}
```
- cadence.flow (index.html L141-145):
```
use "@composition"

key Cmajor {
  (play | [D4 F4 A4]h [G3 B3 D4]h [C4 E4 G4]w |)
}
```
</interfaces>
</context>

<must_not_break>
- NEVER edit `flow-site/static/wasm/flow-runtime.js` (frozen — HANDOFF §8). This plan does not touch it.
- Home MUST stay PRERENDERED: `highlightFlow` is pure and is called in the `<script>` (module/component
  init), NOT in template `{@html}` expressions that re-run; NO `window`/DOM access at component top level.
  `tones.ts` already guards `AudioContext` behind the click — only call its functions inside `onclick`.
- Light-only: do NOT render the layout's `<Toggle>` on `/`; do NOT theme the home dark. The home has
  NO dark variant — do not add `[data-theme='dark']` rules.
- The 3 hero snippets MUST be copied VERBATIM from index.html (see <interfaces> above).
- Play-button audio uses the Web-Audio `tones` helper, NOT the real WASM runtime (intentional for the
  marketing page; the real engine lives on /playground).
- `/docs`, `/playground`, `/showcase` MUST stay visually UNCHANGED (they keep the existing chrome).
</must_not_break>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite +page.svelte as the iOS-6 home (markup + scoped skeuo CSS + runes interactivity)</name>
  <files>flow-site/src/routes/+page.svelte</files>
  <action>
Fully replace the current Phase-49 home with the iOS-6 design from `.design-handoff/project/index.html`,
faithful to its structure. Wrap the ENTIRE page body in a single `<div class="ios6-page"> … </div>`.

MARKUP (reproduce index.html L71-219 exactly, with the route/URL substitutions below):
- `.toolbar`: `.brand` (`<span class="glyph">` ♪ icon via CSS ::after + `<span class="word">Flow</span>`),
  then `.nav` with 5 links — Home (`href="/"` class="active"), Docs `/docs`, Playground `/playground`,
  Showcase `/showcase`, GitHub (`class="ext"`, `href="https://github.com/noahfreelove/flow-sharp"`,
  `target="_blank" rel="noopener"`).
- `.layout` containing:
  - Hero `<section class="plate hero">` with 4 `<span class="screw tl|tr|bl|br">`, the
    `<div class="reflect" data-text="Flow">` wrapping `.tagline` + `<h1 class="hero-word">Flow</h1>`,
    and `.hero-blurb` (paragraph from index.html L93-94) + `.hero-cta` with two `<a class="btn lg">` /
    `<a class="btn lg gray">` → `/playground` and `/docs`.
  - `.cards3` with the 3 `.ex` wells (hello / scale / cadence). Each `.ex`: `<h3>` + `<p>` (copy titles +
    blurbs from index.html L105-106 / L119-120 / L137-138), a `.well` containing `.code-head`
    (`<span class="dot r"></span><span class="dot y"></span><span class="dot g"></span>FILENAME`) and
    `<pre class="code flow-code">{@html hello}</pre>` where `hello`/`scale`/`cadence` are the precomputed
    highlighted strings (see SCRIPT). Then `.row` with a `<button class="btn sm green">▶ Play</button>`
    (onclick wired per SCRIPT) and `<a class="open" href="/playground">Open in playground →</a>`.
  - "Why Flow" `.h-rule` (`<h2>Why Flow</h2><span class="line">`) + `.feat` with 3 `.plate`s
    (`.ic` ✎/◷/♪ + `<h3>` + `<p>`), copy from index.html L158-172.
  - "How it sounds" `.h-rule` + `<div class="leather">` with 2 leather screws (the amber-tinted inline
    style from index.html L178-179), the intro `<p>` (L180-181), and 3 `.player.plate` rows. Each row:
    `<div class="led">`, `<button class="btn sm amber">▶ Play</button>`, `<div class="meta">` (`.t` title +
    `.s` subtitle), `<div class="vu">`. Titles/subtitles from index.html L186 / L193 / L200. Apply the
    leather player inline background style (index.html L183/190/197). Bind `class:playing={playingIndex===i}`
    on each row instead of the static `playing` class. Drive the VU bars from a per-row reactive array
    (see SCRIPT) — do NOT mutate the DOM directly.
  - `.footer` (license line from index.html L205-208; route Docs link to `/docs`, GitHub to the real repo URL).
- `<nav class="tabbar">` with the 5 tabs and the inline SVG icons copied VERBATIM from index.html
  L213-219 (Home active `href="/"`, Docs `/docs`, Playground `/playground`, Showcase `/showcase`,
  GitHub external to the real repo URL).

SCRIPT (`<script lang="ts">`, Svelte 5 runes — replace the old script entirely):
- `import { highlightFlow } from '$lib/home/flow-highlight';`
- `import { playTone, playMelody, playChord } from '$lib/home/tones';`
- Define the 3 verbatim snippet strings (`HELLO`, `SCALE`, `CADENCE` — exact text from <interfaces>),
  then precompute `const hello = highlightFlow(HELLO);` etc. at script top (pure → runs at prerender;
  the `{@html ...}` in markup just emits the precomputed string).
- Hero card Play handlers: ex1 `playTone(440, 0.9, 'triangle')`; ex2
  `playMelody(['C4','D4','E4','F4','G4'], 0.34)`; ex3 fire 3 chords 900ms apart via `setTimeout` —
  `playChord(['D4','F4','A4'], 1.0)`, then `['G3','B3','D4']`, then `['C4','E4','G4']`.
- Leather players: define `const PLAYERS = [{ melody:['C4','E4','G4','C5','G4','E4'], type:'sine' }, …]`
  (3 rows; melodies/types from index.html L185/192/199 — symphony sine / ragtime square / microtonal
  triangle). A `let playingIndex = $state(-1)` and a per-row VU state (e.g. `let vu = $state<number[]>([])`
  with 14 entries). On a leather Play click: set `playingIndex = i`, call
  `const dur = playMelody(p.melody, 0.34, p.type)`, then start a `setInterval` (~90ms) that sets the 14
  vu bars to random heights `6 + Math.random()*20`, and a `setTimeout(dur*1000)` that clears the interval,
  resets vu bars to height 6, and sets `playingIndex = -1`. Render the bars as
  `{#each vu as h}<i style="height:{h}px"></i>{/each}` inside each row's `.vu`. Guard the interval/timeout
  so a second click doesn't leak timers (clear any prior interval first).

STYLE (`<style>` block — port BOTH `flow.css` (337 lines) AND index.html's inline `<style>` L9-66):
- Component-scoped per HANDOFF §4.1 option (a): paste the rule bodies; Svelte auto-scopes generic class
  names so they cannot leak to the other routes.
- Replace flow.css's global `body{ …linen ground… }` (L53-66) with
  `.ios6-page{ <same linen background-color/background-image>; min-height:100dvh; background-attachment:fixed; font-family:var(--font-ui); color:var(--ink); -webkit-font-smoothing:antialiased; text-rendering:optimizeLegibility; }`
  so the linen is a full-viewport wrapper, not the shared `<body>` (app.css owns
  `body{ background: var(--color-paper) }`). Keep `*{ box-sizing:border-box }` and `html,body{margin:0;padding:0}`
  out of the component (app.css/global already handle reset; do NOT re-declare global `body`).
- Keep the `:root{ … }` token block (Svelte leaves `:root` global; `--ink/--aqua-*/--linen/--font-ui`
  don't collide with app.css's `--color-*`).
- Mark the code-token rules `:global(...)` because `{@html}` output is NOT scoped:
  `:global(.tk-kw)`, `:global(.tk-str)`, `:global(.tk-num)`, `:global(.tk-com)`, `:global(.tk-fn)`,
  `:global(.tk-op)`. (Also `:global(.flow-code .tk-*)` is acceptable to namespace them.)
- Include index.html's inline landing CSS: `.hero*`, `.tagline`, `.cards3`, `.ex*`, `.feat*`, `.player*`,
  `.vu*`, `.footer*`, and the `@media(max-width:820px)` block. Include `@keyframes blink`.
- Do NOT add any `[data-theme='dark']` rules (light-only).

DELETIONS: drop all old imports (`Panel`, `Button`, `CodeCard`, `AudioEmbed`, `CODE_FIRST_ANNOTATIONS`,
`PageData`, `data` prop, `copyInstall`, `VALUE_PROPS`/`SOUND_EMBEDS` consts) and the entire old
`.home*` style block — they are replaced.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && pnpm -C flow-site build 2>&1 | tail -25</automated>
  </verify>
  <done>+page.svelte renders the full iOS-6 structure wrapped in .ios6-page; the 3 hero wells show
  highlightFlow output; Play buttons call tones helpers; leather rows bind playingIndex + render vu bars;
  styles are component-scoped with :global(.tk-*); build succeeds with a clean prerender of /.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify +page.ts to prerender-only and add the +layout.svelte isHome guard</name>
  <files>flow-site/src/routes/+page.ts, flow-site/src/routes/+layout.svelte</files>
  <action>
`+page.ts` — replace the entire file with exactly:
```ts
export const prerender = true;
```
Drop the shiki `import`, the `HighlightedExample` interface, `highlightExample`, and the `load`
function (the new home self-highlights via `highlightFlow` in the component; `data.heroExamples` /
`data.codeFirst` are gone, which is why Task 1 removes the `data` prop).

`+layout.svelte` — suppress the OLD chrome on `/` only:
- Add `const isHome = $derived(current === '/');` near the existing `const current = $derived(...)`
  (line ~45).
- Wrap the `<header class="site-chrome surface-brushed-metal"> … </header>` block (L70-96) in
  `{#if !isHome}` … `{/if}`.
- Wrap the mobile `{#if mobileOpen}<nav id="mobile-nav" …>…</nav>{/if}` block (L99-123) in an
  OUTER `{#if !isHome}` … `{/if}` (so it nests as `{#if !isHome}{#if mobileOpen}…{/if}{/if}`).
- Leave `{@render children()}` (L125), the `<script>`, the `<svelte:head>`, the `$effect` theme
  hydration, `NAV_ITEMS`, `mobileOpen`, `isActive`, and the entire `<style>` block UNCHANGED.
- Result: `/` renders ONLY its own toolbar + tabbar (from Task 1); `/docs`, `/playground`,
  `/showcase` keep the existing site-chrome + mobile nav exactly as before.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/flow-sharp && pnpm -C flow-site build 2>&1 | tail -25 && grep -q "isHome" flow-site/src/routes/+layout.svelte && grep -q "prerender = true" flow-site/src/routes/+page.ts && echo GUARD_OK</automated>
  </verify>
  <done>+page.ts is the single prerender line; +layout.svelte gates site-chrome and the mobile nav
  behind {#if !isHome}; build still succeeds and prints GUARD_OK.</done>
</task>

</tasks>

<verification>
- `pnpm -C flow-site build` succeeds with a clean prerender (no "could not prerender / window is not
  defined" errors for `/`). The build crawls `/` first — any DOM-at-module-top regression fails here.
- Optional: `pnpm -C flow-site check` (svelte-check) passes (or only pre-existing warnings remain).
- Manual (HANDOFF §6, run `pnpm -C flow-site dev`):
  - `/` shows the iOS-6 design (linen, brushed-aluminum toolbar, Aqua buttons, code wells, leather rack,
    bottom tab bar); hero + leather Play buttons make sound; leather VU meters animate while playing.
  - Toolbar nav + bottom tab bar route to `/`, `/docs`, `/playground`, `/showcase`; GitHub opens the
    real repo in a new tab.
  - `/docs`, `/playground`, `/showcase` are visually UNCHANGED (old chrome) and still work; `/playground`
    still boots the WASM runtime.
</verification>

<success_criteria>
- The 4-file-scope change (one rewrite + one port + two edits) lands: `+page.svelte` is the iOS-6 home,
  `+page.ts` is prerender-only, `+layout.svelte` has the `isHome` guard.
- All `must_haves.truths` hold; no `must_not_break` item is violated (frozen WASM untouched, home
  prerendered + light-only, snippets verbatim, tones-not-WASM audio, other 3 routes unchanged).
- `pnpm -C flow-site build` is green.
</success_criteria>

<output>
Create `.planning/quick/260608-x94-implement-ios-6-home-redesign-for-flow-s/260608-x94-SUMMARY.md` when done.
</output>
