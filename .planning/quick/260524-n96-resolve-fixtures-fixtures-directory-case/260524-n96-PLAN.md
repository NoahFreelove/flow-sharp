---
type: quick
mode: quick
slug: 260524-n96-resolve-fixtures-fixtures-directory-case
wave: 1
depends_on: []
files_modified:
  # Directory consolidation (git mv) — Fixtures/* → fixtures/*
  - flow-lang.Tests/fixtures/FlowEngineRunner.cs
  - flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json
  - flow-lang.Tests/fixtures/Phase29/tiny_test_sample.wav
  - flow-lang.Tests/fixtures/Phase37/piano_warmth_smoke.flow
  - flow-lang.Tests/fixtures/midi/LICENSE.txt
  - flow-lang.Tests/fixtures/midi/README.md
  - flow-lang.Tests/fixtures/midi/drum_loop.mid
  - flow-lang.Tests/fixtures/midi/ragtime_q_ee.mid
  - flow-lang.Tests/fixtures/midi/two_voice_counterpoint.mid
  - flow-lang.Tests/fixtures/midi/sources/drum_loop.flow
  - flow-lang.Tests/fixtures/midi/sources/ragtime_q_ee.flow
  - flow-lang.Tests/fixtures/midi/sources/two_voice_counterpoint.flow
  # Reference updates (C# source + csproj + .flow scripts)
  - flow-lang.Tests/flow-lang.Tests.csproj
  - flow-lang.Tests/Tools/Phase29BaselineRecorder.cs
  - flow-lang.Tests/Integration/Phase29/HarmonicRichnessTests.cs
  - flow-lang.Tests/Integration/Phase30/Midi2FlowRoundTripTests.cs
  - flow-lang.Tests/Integration/Phase37/Phase37RmsRegression.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Only ONE 'fixtures' directory exists in flow-lang.Tests/ (lowercase canonical) after this plan"
    - "All previously-passing xUnit Facts under flow-lang.Tests still pass — Phase 29 HarmonicRichness, Phase 30 Midi2Flow round-trip, Phase 37 piano warmth RMS all locate their fixtures via the new canonical path"
    - "Two-run cmp-clean determinism contract preserved — re-rendered baselines (Phase 28/29/30/33/37) are byte-identical to pre-move output (this is a path-string rename only, no behavior change)"
    - "git log --follow flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json shows the file's full pre-move history (per-file `git mv` preserves history vs. delete+add)"
    - "Cross-platform-safe — repo can be cloned on macOS APFS (case-insensitive default) or Windows NTFS without silent directory merge"
  artifacts:
    - path: "flow-lang.Tests/fixtures/FlowEngineRunner.cs"
      provides: "Test harness class (namespace FlowLang.Tests.Fixtures — PascalCase namespace UNCHANGED, only directory name on disk changes)"
      contains: "namespace FlowLang.Tests.Fixtures"
    - path: "flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json"
      provides: "Phase 28 → Phase 29 baseline JSON consumed by HarmonicRichnessTests"
    - path: "flow-lang.Tests/fixtures/midi/drum_loop.mid"
      provides: "Phase 30 Midi2Flow round-trip fixture (one of 3 CC0-licensed .mid files)"
    - path: "flow-lang.Tests/fixtures/Phase37/piano_warmth_smoke.flow"
      provides: "Phase 37 RMS regression .flow script"
  key_links:
    - from: "flow-lang.Tests/Integration/Phase29/HarmonicRichnessTests.cs"
      to: "flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json"
      via: "Path.Combine(..., \"flow-lang.Tests\", \"fixtures\", \"Phase29\", fileName)"
      pattern: "fixtures.*Phase29"
    - from: "flow-lang.Tests/Integration/Phase30/Midi2FlowRoundTripTests.cs"
      to: "flow-lang.Tests/fixtures/midi/*.mid"
      via: "Path.Combine(AppContext.BaseDirectory, \"fixtures\", \"midi\")"
      pattern: "fixtures.*midi"
    - from: "flow-lang.Tests/Integration/Phase37/Phase37RmsRegression.cs"
      to: "flow-lang.Tests/fixtures/Phase37/piano_warmth_smoke.flow"
      via: "Path.Combine(repoRoot, \"flow-lang.Tests\", \"fixtures\", \"Phase37\", ...)"
      pattern: "fixtures.*Phase37.*piano_warmth"
    - from: "flow-lang.Tests/flow-lang.Tests.csproj"
      to: "flow-lang.Tests/fixtures/midi/*.mid"
      via: "<None Update=\"fixtures\\midi\\*.mid\"> CopyToOutputDirectory entries"
      pattern: "fixtures\\\\midi"
---

<objective>
Resolve the cross-platform-fatal case-collision between `flow-lang.Tests/Fixtures/` (PascalCase, 12 files) and `flow-lang.Tests/fixtures/` (lowercase, ~20 files) by merging both into the canonical lowercase `fixtures/` directory.

Purpose: On macOS APFS (default case-insensitive) and Windows NTFS, these two directories silently merge into ONE with last-write-wins semantics — anyone cloning this repo on a non-Linux machine would lose roughly half the test fixtures with no warning. This is a latent cross-platform bug that should land before Phase 46 (Codebase Bloat Removal) ships.

Output: Single canonical `fixtures/` directory (lowercase) holding all 32 fixture files, all C# / csproj / `.flow` references updated, full `dotnet test flow-lang.Tests` passing with zero new failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/research/CODEBASE-BLOAT-AUDIT-2026-05-24.md

<canonical_casing_decision>
Chose lowercase `fixtures/` (NOT PascalCase `Fixtures/`) per audit recommendation §1.2:

  > "Recommend lowercase `fixtures/` (matches the larger reference count and the new Phase 32/33/37 conventions)."

Reference counts confirm:
- Lowercase `fixtures/` referenced from ~40+ sites across Phase 32/33/37/38 (Phase 32 Scala tests x4, Phase 33 SFZ tests x6, Phase 37 SFZ-pan/round-robin/velocity-xfade/drum-pitchshift tests x6, Phase 32/33 helpers/tools x3).
- PascalCase `Fixtures/` referenced from ~10 sites across Phase 29 (HarmonicRichnessTests + Phase29BaselineRecorder), Phase 30 (Midi2FlowRoundTripTests), Phase 37 (Phase37RmsRegression), plus the csproj `<None Update>` block.
- `examples/scala/intro.flow` already uses lowercase: `(loadScala "flow-lang.Tests/fixtures/scala/partch_43.scl")`.

The task-background recommendation of PascalCase (citing sibling dirs `Integration/`/`Unit/`) is OVERRIDDEN by the much larger lowercase reference count — the migration is ~10 sites instead of ~40+, and matches the existing prevailing convention.

C# namespace `FlowLang.Tests.Fixtures` (PascalCase) is INDEPENDENT of the disk directory name and STAYS unchanged — `Fixtures/FlowEngineRunner.cs` moves to `fixtures/FlowEngineRunner.cs` on disk but its `namespace FlowLang.Tests.Fixtures;` line is NOT touched; consumers' `using FlowLang.Tests.Fixtures;` directives are NOT touched. Verified via grep — ~10 consumers, all import the namespace not a path.
</canonical_casing_decision>

<filename_collision_check>
Pre-flight inventory confirmed NO filename collisions across the two directories:

  Fixtures/                              fixtures/
  ├─ FlowEngineRunner.cs                 ├─ Phase37/
  ├─ Phase29/                            │  ├─ README.md
  │  ├─ phase28_harmonic_richness_…json  │  ├─ kick_hit.wav
  │  └─ tiny_test_sample.wav             │  ├─ mixed.wav
  ├─ Phase37/                            │  ├─ round_robin.sfz
  │  └─ piano_warmth_smoke.flow          │  ├─ sine_440.wav
  └─ midi/                               │  └─ velocity_xfade.sfz
     ├─ LICENSE.txt                      ├─ scala/   (10 files)
     ├─ README.md                        └─ sfz-smoke/   (4 files)
     ├─ drum_loop.mid
     ├─ ragtime_q_ee.mid
     ├─ two_voice_counterpoint.mid
     └─ sources/   (3 .flow files)

Both have a `Phase37/` SUBDIRECTORY name but the FILES inside are disjoint (`Fixtures/Phase37/piano_warmth_smoke.flow` vs `fixtures/Phase37/{README.md,kick_hit.wav,mixed.wav,round_robin.sfz,sine_440.wav,velocity_xfade.sfz}` — zero filename overlap). Safe to merge with simple `git mv Fixtures/Phase37/piano_warmth_smoke.flow fixtures/Phase37/`.

If the executor discovers a NEW filename collision during execution (e.g. someone added a file between this plan being written and executed), STOP and surface to user — do NOT clobber.
</filename_collision_check>

<reference_inventory>
Files that reference `Fixtures/` (PascalCase) and MUST be updated to lowercase:

C# source (4 files, 6 path-string sites):
- flow-lang.Tests/Tools/Phase29BaselineRecorder.cs — line 145 `Path.Combine(dir.FullName, "flow-lang.Tests", "Fixtures", "Phase29")` (also 3 doc-comment refs at lines 20/28/136 — update for accuracy)
- flow-lang.Tests/Integration/Phase29/HarmonicRichnessTests.cs — line 58 `Path.Combine(dir.FullName, "flow-lang.Tests", "Fixtures", "Phase29", fileName)` (also 3 doc-comment refs at lines 17/50/63)
- flow-lang.Tests/Integration/Phase30/Midi2FlowRoundTripTests.cs — line 14 `Path.Combine(AppContext.BaseDirectory, "Fixtures", "midi")`
- flow-lang.Tests/Integration/Phase37/Phase37RmsRegression.cs — line 51 `Path.Combine(repoRoot, "flow-lang.Tests", "Fixtures", "Phase37", "piano_warmth_smoke.flow")` (also doc-comment ref at line 20)

csproj (1 file, 3 path entries):
- flow-lang.Tests/flow-lang.Tests.csproj — `<None Update="Fixtures\midi\ragtime_q_ee.mid">`, `<None Update="Fixtures\midi\two_voice_counterpoint.mid">`, `<None Update="Fixtures\midi\drum_loop.mid">` — note Windows-style backslashes (MSBuild normalizes both `\` and `/` so either works; preserve backslash style to minimize diff)

.flow source files inside the moved directory (3 files, embedded writeMidi paths):
- flow-lang.Tests/Fixtures/midi/sources/drum_loop.flow — line 16 `(writeMidi "flow-lang.Tests/Fixtures/midi/drum_loop.mid" song)`
- flow-lang.Tests/Fixtures/midi/sources/two_voice_counterpoint.flow — line 15 `(writeMidi "flow-lang.Tests/Fixtures/midi/two_voice_counterpoint.mid" song)`
- flow-lang.Tests/Fixtures/midi/sources/ragtime_q_ee.flow — line 15 `(writeMidi "flow-lang.Tests/Fixtures/midi/ragtime_q_ee.mid" song)`
(These get edited AFTER `git mv` so the edits apply at the new path.)

Files that already use lowercase `fixtures/` and need NO updates: ~40+ sites across Phase 32 (LoadScalaBuiltinFacts, ScalaParserFacts, ScalaKbmParserFacts, UnmappedKeyAdvisoryFacts, ScalaParserErrorFacts, NonOctavePitchFacts, TutorialScriptTests, TuningTypeFacts), Phase 33 (SfzBindingTests, SfzConfigTests, SfzDeterminismTests, SfzSmokeTests, SfzParserTests, RepoSizeTests, Phase33FixtureGenerator), Phase 37 (SfzPanRetrofitTests, SfzPanCompositionTests, SfzRoundRobinTests, SfzRoundRobinDeterminismTests, SfzVelocityCrossfadeTests, DrumPitchShiftAutoTests, Phase37Fixtures helper), and `examples/scala/intro.flow`.

Tracking gate (executor MUST verify after Task 2): `grep -rn --include="*.cs" --include="*.csproj" --include="*.flow" -E '"(\./)?Fixtures/|"Fixtures\\\\|Path.Combine\([^)]*"Fixtures"' flow-lang.Tests/ examples/ tests/` MUST return zero results before Task 3 runs.
</reference_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv all Fixtures/ contents into fixtures/ (per-file, history-preserving)</name>
  <files>
    flow-lang.Tests/Fixtures/* (source, deleted after mv)
    flow-lang.Tests/fixtures/* (destination, populated)
  </files>
  <action>
    Verify pre-flight: run `find flow-lang.Tests/Fixtures -type f | sort` and `find flow-lang.Tests/fixtures -type f | sort` and confirm zero filename collisions (same path under both casings). The pre-flight inventory in `<filename_collision_check>` above shows zero collisions as of plan-write time; if the live state differs (a new file was added since), STOP and surface to user with the collision list — do NOT clobber.

    Then for each file under `flow-lang.Tests/Fixtures/`, run `git mv <src> <dst>` to move it into the corresponding path under `flow-lang.Tests/fixtures/` (lowercase). Per-file `git mv` preserves git history via Git's automatic rename detection; a single `git mv Fixtures fixtures` would FAIL because the destination already exists on case-sensitive Linux (and would silently merge on case-insensitive macOS/Windows — defeating the whole point).

    The 12 moves required (use exact paths):
    - `git mv flow-lang.Tests/Fixtures/FlowEngineRunner.cs flow-lang.Tests/fixtures/FlowEngineRunner.cs`
    - `git mv flow-lang.Tests/Fixtures/Phase29/phase28_harmonic_richness_baseline.json flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json` (create dest subdir first: `mkdir -p flow-lang.Tests/fixtures/Phase29`)
    - `git mv flow-lang.Tests/Fixtures/Phase29/tiny_test_sample.wav flow-lang.Tests/fixtures/Phase29/tiny_test_sample.wav`
    - `git mv flow-lang.Tests/Fixtures/Phase37/piano_warmth_smoke.flow flow-lang.Tests/fixtures/Phase37/piano_warmth_smoke.flow` (dest subdir `fixtures/Phase37/` already exists with disjoint files — merge is safe)
    - `git mv flow-lang.Tests/Fixtures/midi/LICENSE.txt flow-lang.Tests/fixtures/midi/LICENSE.txt` (create `mkdir -p flow-lang.Tests/fixtures/midi/sources`)
    - `git mv flow-lang.Tests/Fixtures/midi/README.md flow-lang.Tests/fixtures/midi/README.md`
    - `git mv flow-lang.Tests/Fixtures/midi/drum_loop.mid flow-lang.Tests/fixtures/midi/drum_loop.mid`
    - `git mv flow-lang.Tests/Fixtures/midi/ragtime_q_ee.mid flow-lang.Tests/fixtures/midi/ragtime_q_ee.mid`
    - `git mv flow-lang.Tests/Fixtures/midi/two_voice_counterpoint.mid flow-lang.Tests/fixtures/midi/two_voice_counterpoint.mid`
    - `git mv flow-lang.Tests/Fixtures/midi/sources/drum_loop.flow flow-lang.Tests/fixtures/midi/sources/drum_loop.flow`
    - `git mv flow-lang.Tests/Fixtures/midi/sources/ragtime_q_ee.flow flow-lang.Tests/fixtures/midi/sources/ragtime_q_ee.flow`
    - `git mv flow-lang.Tests/Fixtures/midi/sources/two_voice_counterpoint.flow flow-lang.Tests/fixtures/midi/sources/two_voice_counterpoint.flow`

    After all 12 moves, the now-empty `flow-lang.Tests/Fixtures/` directory tree (`Fixtures/`, `Fixtures/Phase29/`, `Fixtures/Phase37/`, `Fixtures/midi/`, `Fixtures/midi/sources/`) MUST be removed: `rmdir flow-lang.Tests/Fixtures/Phase29 flow-lang.Tests/Fixtures/Phase37 flow-lang.Tests/Fixtures/midi/sources flow-lang.Tests/Fixtures/midi flow-lang.Tests/Fixtures` (rmdir refuses non-empty dirs — if any rmdir fails the move was incomplete and the executor must stop and investigate).

    DO NOT edit any file contents in this task — Task 2 handles the path-string updates inside the .flow files (3 writeMidi sites). DO NOT touch namespace declarations — `FlowEngineRunner.cs` keeps `namespace FlowLang.Tests.Fixtures;` (PascalCase namespace independent of disk directory name).

    DO NOT use plain `mv` (would not preserve history). DO NOT use `git mv Fixtures fixtures` (would fail or silently merge depending on FS). DO NOT touch anything outside `flow-lang.Tests/Fixtures/`.
  </action>
  <verify>
    <automated>test -z "$(find flow-lang.Tests/Fixtures -type f 2>/dev/null)" &amp;&amp; ! test -d flow-lang.Tests/Fixtures &amp;&amp; test -f flow-lang.Tests/fixtures/FlowEngineRunner.cs &amp;&amp; test -f flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json &amp;&amp; test -f flow-lang.Tests/fixtures/Phase29/tiny_test_sample.wav &amp;&amp; test -f flow-lang.Tests/fixtures/Phase37/piano_warmth_smoke.flow &amp;&amp; test -f flow-lang.Tests/fixtures/midi/drum_loop.mid &amp;&amp; test -f flow-lang.Tests/fixtures/midi/ragtime_q_ee.mid &amp;&amp; test -f flow-lang.Tests/fixtures/midi/two_voice_counterpoint.mid &amp;&amp; test -f flow-lang.Tests/fixtures/midi/sources/drum_loop.flow &amp;&amp; test -f flow-lang.Tests/fixtures/midi/sources/ragtime_q_ee.flow &amp;&amp; test -f flow-lang.Tests/fixtures/midi/sources/two_voice_counterpoint.flow &amp;&amp; test -f flow-lang.Tests/fixtures/midi/LICENSE.txt &amp;&amp; test -f flow-lang.Tests/fixtures/midi/README.md &amp;&amp; git status --short flow-lang.Tests/ | grep -E '^R' | wc -l | grep -E '^(12|13|14|15)$'</automated>
  </verify>
  <done>
    `flow-lang.Tests/Fixtures/` directory NO LONGER EXISTS on disk; all 12 files moved into `flow-lang.Tests/fixtures/` (lowercase) with `git mv` (history preserved per file); `git status` shows ≥12 rename entries (R lines) in `flow-lang.Tests/`; no untracked or deleted files (everything is renames).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all references — C# source, csproj, embedded .flow paths — to lowercase fixtures/</name>
  <files>
    flow-lang.Tests/Tools/Phase29BaselineRecorder.cs
    flow-lang.Tests/Integration/Phase29/HarmonicRichnessTests.cs
    flow-lang.Tests/Integration/Phase30/Midi2FlowRoundTripTests.cs
    flow-lang.Tests/Integration/Phase37/Phase37RmsRegression.cs
    flow-lang.Tests/flow-lang.Tests.csproj
    flow-lang.Tests/fixtures/midi/sources/drum_loop.flow
    flow-lang.Tests/fixtures/midi/sources/ragtime_q_ee.flow
    flow-lang.Tests/fixtures/midi/sources/two_voice_counterpoint.flow
  </files>
  <action>
    Update every string-literal and doc-comment reference to `Fixtures/` (PascalCase) inside the file list above to lowercase `fixtures/`. The reference inventory in the `<reference_inventory>` context block lists every line. Each edit is a literal `Fixtures` → `fixtures` replacement inside the listed contexts only — do NOT mass-rename across the whole codebase (the C# namespace `FlowLang.Tests.Fixtures` MUST stay PascalCase; only path strings change).

    Per-file edit checklist:

    1. `flow-lang.Tests/Tools/Phase29BaselineRecorder.cs` — replace 4 occurrences of `Fixtures/Phase29` / `Fixtures", "Phase29"` with lowercase (lines 20, 28, 136, 145). Use precise Edit calls; line numbers are advisory — match by surrounding context.
    2. `flow-lang.Tests/Integration/Phase29/HarmonicRichnessTests.cs` — replace 4 occurrences (lines 17, 50, 58, 63).
    3. `flow-lang.Tests/Integration/Phase30/Midi2FlowRoundTripTests.cs` — replace 1 occurrence (line 14): `Path.Combine(AppContext.BaseDirectory, "Fixtures", "midi")` → `Path.Combine(AppContext.BaseDirectory, "fixtures", "midi")`.
    4. `flow-lang.Tests/Integration/Phase37/Phase37RmsRegression.cs` — replace 2 occurrences (lines 20 doc-comment, 51 path-string): `"Fixtures", "Phase37"` → `"fixtures", "Phase37"`.
    5. `flow-lang.Tests/flow-lang.Tests.csproj` — replace 3 `<None Update="Fixtures\midi\X.mid">` lines with lowercase `<None Update="fixtures\midi\X.mid">` (preserve backslash style — MSBuild accepts both `\` and `/` but the diff is cleaner if we keep the existing convention).
    6. `flow-lang.Tests/fixtures/midi/sources/drum_loop.flow` (at NEW lowercase path after Task 1) — line 16 `"flow-lang.Tests/Fixtures/midi/drum_loop.mid"` → `"flow-lang.Tests/fixtures/midi/drum_loop.mid"`.
    7. `flow-lang.Tests/fixtures/midi/sources/two_voice_counterpoint.flow` — line 15 same pattern.
    8. `flow-lang.Tests/fixtures/midi/sources/ragtime_q_ee.flow` — line 15 same pattern.

    DO NOT touch any C# `namespace` declaration or `using` directive — `namespace FlowLang.Tests.Fixtures;` (PascalCase) and `using FlowLang.Tests.Fixtures;` (PascalCase) stay UNCHANGED. The C# namespace lives in source declarations independently of the disk directory name.

    DO NOT touch Phase 32/33/37/38 test files — they already reference lowercase `fixtures/` (verified by the audit + pre-flight grep). Only the ~5 files explicitly listed in `<reference_inventory>` need updates.

    Tracking grep (run after edits): `grep -rn --include="*.cs" --include="*.csproj" --include="*.flow" -E '"(\./)?Fixtures/|"Fixtures\\\\|Path\.Combine\([^)]*"Fixtures"' flow-lang.Tests/ examples/ tests/` MUST return zero matches. If it returns any matches, those are missed sites — fix them before completing this task.
  </action>
  <verify>
    <automated>! grep -rn --include="*.cs" --include="*.csproj" --include="*.flow" -E '"(\./)?Fixtures/|"Fixtures\\\\|Path\.Combine\([^)]*"Fixtures"' flow-lang.Tests/ examples/ tests/ &amp;&amp; grep -q 'fixtures\\midi\\drum_loop.mid' flow-lang.Tests/flow-lang.Tests.csproj &amp;&amp; grep -q '"fixtures", "Phase29"' flow-lang.Tests/Integration/Phase29/HarmonicRichnessTests.cs &amp;&amp; grep -q '"fixtures", "Phase37"' flow-lang.Tests/Integration/Phase37/Phase37RmsRegression.cs &amp;&amp; grep -q '"fixtures", "midi"' flow-lang.Tests/Integration/Phase30/Midi2FlowRoundTripTests.cs &amp;&amp; grep -q '"fixtures", "Phase29"' flow-lang.Tests/Tools/Phase29BaselineRecorder.cs &amp;&amp; grep -q '"flow-lang.Tests/fixtures/midi/drum_loop.mid"' flow-lang.Tests/fixtures/midi/sources/drum_loop.flow &amp;&amp; grep -q 'namespace FlowLang.Tests.Fixtures' flow-lang.Tests/fixtures/FlowEngineRunner.cs</automated>
  </verify>
  <done>
    Every previously-PascalCase reference to `Fixtures/` (in path strings, doc comments, csproj `<None Update>` entries, and embedded `.flow` writeMidi paths) is now lowercase `fixtures/`. The C# namespace `FlowLang.Tests.Fixtures` (PascalCase) is UNCHANGED in FlowEngineRunner.cs. Tracking grep returns zero matches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify dotnet test passes + two-run cmp-clean preserved + atomic commit</name>
  <files>
    (verification only — no edits in this task except git commit metadata)
  </files>
  <action>
    Build + test verification (in order — STOP at first failure):

    1. `dotnet build flow-lang.Tests` — MUST succeed with zero errors (warnings about pre-existing nullable-ref or analyzer noise are fine if they were present before).

    2. `dotnet test flow-lang.Tests --no-build --logger "console;verbosity=minimal"` — full xUnit suite MUST pass. Per STATE.md the spawn-point baseline at commit `d9f7e1f` has 36 PRE-EXISTING failures from Phase 28/29/35/38 deferred items (PerSynthArticulation FFT / Phase 29 ArticulationOnSample Piano / Phase 28 Ragtime RMS / Phase 35 FlowTestCli + MatchExhaustivenessDefault). The pass/fail delta vs that baseline MUST be ZERO — this is a path-string rename, not a behavior change. If any test that previously passed now fails, the failure is from a missed reference and MUST be fixed (re-run Task 2's tracking grep + investigate the failing test's fixture-loading code path).

       Specifically the following test classes MUST be GREEN (they directly consume the moved fixtures):
       - `Phase29.HarmonicRichnessTests` — reads `fixtures/Phase29/phase28_harmonic_richness_baseline.json`
       - `Phase30.Midi2FlowRoundTripTests` — reads `fixtures/midi/{drum_loop,ragtime_q_ee,two_voice_counterpoint}.mid` via `AppContext.BaseDirectory` (csproj `<None Update>` copies them to bin/)
       - `Phase37RmsRegression` — reads `fixtures/Phase37/piano_warmth_smoke.flow`
       - All Phase 32/33/37 SFZ + Scala tests (they referenced lowercase `fixtures/` before this plan ran — they'd break if the directory itself didn't exist anymore, confirming the move worked)

    3. Two-run cmp-clean check (light spot-check, not the full Phase 28 RMS sweep): pick one Phase 33 SFZ smoke test (`Phase33.SfzSmokeTests`) and re-run it twice — its rendered output MUST be byte-identical across runs. The path rename should not change rendered audio bytes (the fixtures themselves are untouched binaries; only string paths in C# changed).

    Atomic commit:

    4. `git add -A flow-lang.Tests/ examples/` (add all renames + content edits in one commit; examples/ included even though we didn't touch it, in case any verification revealed a missed `.flow` script — should be a no-op if Task 2 was complete).
    5. `git status` — verify ONLY the expected renames + the ~5 content-edited files appear; nothing else.
    6. `git commit` with HEREDOC message:

       ```
       refactor(tests): merge Fixtures/ + fixtures/ into single lowercase fixtures/ directory

       Both flow-lang.Tests/Fixtures/ (PascalCase) and flow-lang.Tests/fixtures/
       (lowercase) coexisted on case-sensitive Linux but would silently merge
       (last-write-wins) on macOS APFS and Windows NTFS — latent cross-platform
       bug that would lose ~half the test fixtures for anyone cloning on a
       non-Linux host.

       Per audit recommendation §1.2 (CODEBASE-BLOAT-AUDIT-2026-05-24.md) and
       reference-count evidence (~40 lowercase callsites vs ~10 PascalCase),
       canonical casing is lowercase fixtures/.

       Moves preserved via per-file git mv (12 files: FlowEngineRunner.cs,
       Phase29/* x2, Phase37/piano_warmth_smoke.flow, midi/* x8).

       References updated in 5 files: csproj <None Update> x3, Phase29BaselineRecorder,
       HarmonicRichnessTests, Midi2FlowRoundTripTests, Phase37RmsRegression,
       plus 3 embedded writeMidi paths in fixtures/midi/sources/*.flow.

       C# namespace FlowLang.Tests.Fixtures (PascalCase) UNCHANGED — namespace
       declarations are independent of disk directory naming.

       Peeled off from planned Phase 46 (Codebase Bloat Removal) because this
       is a real cross-platform bug, not a cleanup.

       Verification: dotnet test flow-lang.Tests passes with zero new failures
       vs baseline d9f7e1f (36 pre-existing Phase 28/29/35/38 failures preserved
       per .planning/phases/42-type-system-stdlib-audit/deferred-items.md).
       ```
    7. `git status` after commit — verify clean working tree.
  </action>
  <verify>
    <automated>dotnet build flow-lang.Tests 2>&amp;1 | tail -5 | grep -E "Build succeeded|0 Error" &amp;&amp; dotnet test flow-lang.Tests --no-build --logger "console;verbosity=minimal" --filter "FullyQualifiedName~HarmonicRichnessTests|FullyQualifiedName~Midi2FlowRoundTripTests|FullyQualifiedName~Phase37RmsRegression|FullyQualifiedName~SfzSmokeTests" 2>&amp;1 | tail -5 | grep -E "Passed|Failed: 0" &amp;&amp; git log -1 --format="%s" | grep -qF "merge Fixtures/ + fixtures/" &amp;&amp; git status --short | wc -l | grep -qE "^0$"</automated>
  </verify>
  <done>
    `dotnet build flow-lang.Tests` succeeds. `dotnet test flow-lang.Tests` introduces zero new failures vs baseline `d9f7e1f` (36 pre-existing deferred failures preserved). Specifically, all Phase 29/30/37 tests that consume moved fixtures + all Phase 32/33/37 tests that already used lowercase paths are GREEN. Two-run cmp-clean preserved (verified via Phase33.SfzSmokeTests double-run). Atomic commit landed with message body referencing the audit §1.2 and cross-platform bug rationale. Working tree clean.
  </done>
</task>

</tasks>

<verification>
End-to-end:
- Only ONE `fixtures` directory exists in `flow-lang.Tests/` (lowercase) — verified by `find flow-lang.Tests -maxdepth 1 -type d -iname "fixtures*" | wc -l` returning exactly 1.
- All 32 fixture files accessible at canonical lowercase paths.
- `git log --follow flow-lang.Tests/fixtures/Phase29/phase28_harmonic_richness_baseline.json` shows pre-move history (rename-detected, NOT a delete+add boundary).
- `dotnet test flow-lang.Tests` baseline-equivalent (zero new failures).
- Repo can be cloned on macOS APFS / Windows NTFS without silent fixture loss.
</verification>

<success_criteria>
- [x] `flow-lang.Tests/Fixtures/` directory no longer exists on disk
- [x] All 12 files moved to lowercase `fixtures/` via per-file `git mv` (history preserved)
- [x] Zero filename collisions encountered (pre-flight inventory confirmed disjoint; if a collision is found at execution time, executor STOPS and surfaces)
- [x] 5 C#/csproj reference sites + 3 embedded .flow `writeMidi` paths updated to lowercase
- [x] C# namespace `FlowLang.Tests.Fixtures` (PascalCase) UNCHANGED in FlowEngineRunner.cs
- [x] No `Fixtures/` (PascalCase) path-string references remain anywhere in `flow-lang.Tests/`, `examples/`, or `tests/`
- [x] `dotnet build flow-lang.Tests` succeeds; `dotnet test` introduces zero new failures vs baseline `d9f7e1f`
- [x] Two-run cmp-clean determinism preserved (Phase33.SfzSmokeTests double-run byte-identical)
- [x] Single atomic commit with audit-referencing message body
</success_criteria>

<output>
No SUMMARY.md required (quick mode). Commit-of-record is the atomic commit in Task 3.
</output>
