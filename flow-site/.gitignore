node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env / secrets — never commit (D-49 Runtime State Inventory)
.env
.env.*
!.env.example
!.env.test
.dev.vars

# Build-time wiki clone (D-49-25 — git-cloned into src/docs/wiki/ at build, never committed)
src/docs/wiki/

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright transient run artifacts (the committed baselines live in *-snapshots/, which
# stay tracked — only the per-run output below is ignored).
test-results/
playwright-report/
/blob-report/
/.cache/

# Lighthouse CI transient reports (regenerated by `pnpm lh:audit`; never committed).
.lighthouseci/

# NOTE: static/wasm/ is INTENTIONALLY committed (the Phase 48 AppBundle) — do NOT ignore it.
